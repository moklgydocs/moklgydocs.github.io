import{M as e,O as t,d as n,h as r,p as i}from"./runtime-core.esm-bundler-jC72uHyJ.js";import{t as a}from"./app-0lC-SUUF.js";var o=JSON.parse(`{"path":"/ai-eng/19-capstone-projects/45-gradient-clipping-amp.html","title":"梯度裁剪与混合精度","lang":"zh-CN","frontmatter":{},"git":{"createdTime":1787385595000,"updatedTime":1787488693000,"contributors":[{"name":"jackie.liu","username":"","email":"moklgy@foxmail.com","commits":3}]},"readingTime":{"minutes":7.86,"words":2357},"filePathRelative":"ai-eng/19-capstone-projects/45-gradient-clipping-amp.md"}`),s={name:`45-gradient-clipping-amp.md`};function c(a,o,s,c,l,u){let d=e(`Mermaid`);return t(),n(`div`,null,[o[0]||=i(`<h1 id="梯度裁剪与混合精度" tabindex="-1"><a class="header-anchor" href="#梯度裁剪与混合精度"><span>梯度裁剪与混合精度</span></a></h1><blockquote><p>上一课的优化器和日程,假设梯度是体面的。梯度通常并不体面。一个坏批次就能把梯度范数冲三个数量级。混合精度训练又在 loss 侧引入 FP16 溢出,把问题放大。本课构建生产训练缺了就不能交付的两条安全带:按配置的全局 L2 范数做梯度裁剪,以及一个带 autocast 和 GradScaler 的混合精度循环——检测 NaN 和 Inf,干净地跳过这一步,并把缩放因子记下来供事后追查。</p></blockquote><p><strong>类型:</strong> 动手构建<br><strong>编程语言:</strong> Python<br><strong>前置要求:</strong> 第 19 阶段 第 30-37 课<br><strong>预计耗时:</strong> 约 90 分钟</p><h2 id="学习目标" tabindex="-1"><a class="header-anchor" href="#学习目标"><span>学习目标</span></a></h2><ul><li>对所有参数梯度计算全局 L2 范数,超过配置阈值时就地裁剪。</li><li>用 autocast 加 GradScaler 包住训练步,让 FP16 前向和反向扛过溢出。</li><li>检测 loss 或梯度里的 NaN 和 Inf,跳过优化器步,并记录这次跳过。</li><li>每步报告 GradScaler 的缩放因子,让一长串跳过立刻可见。</li></ul><h2 id="问题" tabindex="-1"><a class="header-anchor" href="#问题"><span>问题</span></a></h2><p>昨天还干干净净的训练运行,loss 曲线在第 8,217 步垂直起飞。元凶是单个批次:梯度范数 4,200,是此前峰值的二十倍。没有裁剪,优化器一步下去,模型过去一小时学到的东西全部归零。在范数 1.0 处做全局 L2 裁剪,同一个批次只贡献一次单位范数的更新;loss 留在趋势线上;运行存活。</p><p>混合精度训练把前向和反向的大部分放进 FP16,吞吐提升 2-3 倍。代价是 FP16 的指数范围窄。一个在 FP16 里溢出的典型梯度求值为 Inf,Inf 在后续层里传播成 NaN,下一次优化器步就把所有权重置成 NaN。PyTorch 的 GradScaler 这样解:反向前把 loss 乘上一个大缩放因子,优化器步前把梯度除以同一个因子。反缩放时若有任何梯度是 Inf 或 NaN,scaler 跳过这一步并把缩放因子减半;若此前 N 步干净,scaler 把因子加倍。训练过程中,因子会找到 FP16 范围允许的最高值。</p><p>构建侧的问题是把两者接线接对。在反缩放之前裁剪,阈值量的是缩放后的梯度;在反缩放之后裁剪,GradScaler 上的操作顺序就有讲究。正确顺序是:<code>scaler.scale(loss).backward()</code>,然后 <code>scaler.unscale_(optimizer)</code>,然后 <code>clip_grad_norm_</code>,然后 <code>scaler.step(optimizer)</code>,最后 <code>scaler.update()</code>。任何其他顺序都会产出一个悄无声息坏掉的循环。</p><h2 id="概念" tabindex="-1"><a class="header-anchor" href="#概念"><span>概念</span></a></h2>`,10),r(d,{code:``}),o[1]||=i(`<h3 id="全局-l2-范数" tabindex="-1"><a class="header-anchor" href="#全局-l2-范数"><span>全局 L2 范数</span></a></h3><p>全局 L2 范数是拼接后梯度向量的欧几里得范数,不是逐参数范数。PyTorch 的实现是 <code>torch.nn.utils.clip_grad_norm_(parameters, max_norm)</code>。函数返回裁剪前的范数,所以本课能同时记下自然值和裁剪值——诊断&quot;我们是不是每一步都在裁剪&quot;时必须两个都有。</p><h3 id="autocast-与-gradscaler" tabindex="-1"><a class="header-anchor" href="#autocast-与-gradscaler"><span>autocast 与 GradScaler</span></a></h3><p><code>torch.amp.autocast(device_type)</code> 是那个有选择地把符合条件的运算(大多数矩阵乘类运算)跑在 FP16 的上下文管理器。<code>torch.amp.GradScaler(device_type)</code> 是那个在反向前缩放 loss、优化器步前反缩放梯度的辅助器。两者是配套设计的;只用一个是配置错误,测试应当抓住。</p><p>本课用 CPU autocast,因为 CI 上跑的是它;同一模式逐字照搬到 CUDA,只需把 <code>device_type=&quot;cpu&quot;</code> 改成 <code>device_type=&quot;cuda&quot;</code>。CPU 上的 GradScaler 是个桩(CPU autocast 默认就走 BF16,不需要 loss 缩放),但本课保留了调用点,让接线与 GPU 循环完全一致。</p><h3 id="nan-与-inf-检测" tabindex="-1"><a class="header-anchor" href="#nan-与-inf-检测"><span>NaN 与 Inf 检测</span></a></h3><p>检测发生在两处。第一,反向前用 <code>torch.isfinite</code> 检查 loss 本身;Inf 或 NaN 的 loss 产不出有用的梯度,不进优化器直接跳过。第二,<code>scaler.unscale_(optimizer)</code> 之后,本课用 <code>has_non_finite_grad(...)</code> 扫描反缩放后的梯度,任何 Inf 或 NaN 都按跳过处理。两处检查合起来,盖住前向和反向两种失败模式。</p><h3 id="缩放因子诊断" tabindex="-1"><a class="header-anchor" href="#缩放因子诊断"><span>缩放因子诊断</span></a></h3><p>缩放因子是 GradScaler 的内部状态。每步本课读 <code>scaler.get_scale()</code>,把它与学习率、梯度范数并排记录。健康的运行里,缩放因子以二的幂次爬升,直到在 <code>2^17</code> 或 <code>2^18</code> 附近饱和。出问题的运行里,因子在高值和低值之间震荡——这信号说明模型的梯度时而在范围内、时而不在。不记录,这个诊断就是隐形的。</p><div class="language-figure line-numbers-mode" data-highlighter="shiki" data-ext="figure" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-figure"><span class="line"><span>grad-clip-monitor</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="动手构建" tabindex="-1"><a class="header-anchor" href="#动手构建"><span>动手构建</span></a></h2><p><code>code/main.py</code> 实现:</p><ul><li><code>clip_global_l2_norm</code>——对 <code>torch.nn.utils.clip_grad_norm_</code> 的包装,同时返回裁剪前和裁剪后范数。</li><li><code>has_non_finite_grad</code>——扫描梯度找 NaN 和 Inf 的辅助函数。</li><li><code>AmpTrainState</code>——包住模型、<code>AdamW</code> 优化器、GradScaler 和 autocast 设备。暴露 <code>step(inputs, targets)</code>,跑完整的裁剪、缩放、遇 NaN 跳过流水线。</li><li><code>StepLog</code> 和 <code>SkipLog</code>——结构化的逐步记录。</li><li>一个演示:训练一个小 <code>nn.Linear</code> 模型 20 步,在第 5 步往梯度里注入一个 Inf 来走跳过路径,打印结果日志。</li></ul><p>运行:</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="color:#61AFEF;">python3</span><span style="color:#98C379;"> code/main.py</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>脚本以零退出码结束,打印逐步日志,每行标记 <code>STEP</code> 或 <code>SKIP</code>;至少有一行是 <code>SKIP</code>。</p><h2 id="生产环境里的实战模式" tabindex="-1"><a class="header-anchor" href="#生产环境里的实战模式"><span>生产环境里的实战模式</span></a></h2><p>四个模式把循环提升为生产训练步。</p><p><strong>跳过计数是告警,不是日志行。</strong> 一次训练运行跳过寥寥几步是健康的。每个 epoch 跳过几百步是硬告警:模型进入了 FP16 装不下的区间,循环正在静默失败。本课追踪 1,000 步的滚动跳过率;生产上跳过率超过 5% 就应当叫人。</p><p><strong>裁剪阈值住配置里。</strong> <code>max_norm = 1.0</code> 是语言模型训练的现代默认。先在小模型上扫;更大的阈值让模型能从真正困难的批次里恢复,更小的阈值以 loss 曲线更噪为代价兜住最坏情况。阈值与第 44 课的日程放在同一份 YAML 或 JSON 配置里。</p><p><strong>范数日志和日程进同一份 CSV。</strong> CSV 列是 <code>step, lr, grad_l2_pre_clip, grad_l2_post_clip, loss, skipped, skip_reason, scaler_scale</code>。打开文件的审阅者在一行里就能看到日程、梯度的故事、缩放因子和跳过结局(带原因)。把这些列拆到多个文件,是分析错位的配方。</p><p><strong><code>scaler.update()</code> 每步都跑,跳过那步也跑。</strong> 干净步上,scaler 读自己的无 inf 计数器,自增,可能把因子加倍;跳过步上,scaler 把因子减半并重置计数器。跳过路径上忘了 <code>update()</code>,就是那个&quot;缩放因子怎么永远不变&quot;的 bug。</p><h2 id="投入使用" tabindex="-1"><a class="header-anchor" href="#投入使用"><span>投入使用</span></a></h2><p>生产模式:</p><ul><li><strong>autocast 设备与优化器设备一致。</strong> GPU 训练用 <code>torch.amp.autocast(device_type=&quot;cuda&quot;)</code>,CPU 用 <code>torch.amp.autocast(device_type=&quot;cpu&quot;)</code>。混用设备会产出静默的类型错误,表现为 loss 曲线看着正常、模型却没在学。</li><li><strong>反向前先查 loss。</strong> <code>torch.isfinite(loss).all()</code> 是一次张量归约;开销可忽略,而在 NaN loss 上省下的是整个训练步。永远跑它。</li><li><strong><code>zero_grad</code> 里用 <code>set_to_none=True</code>。</strong> 把梯度设为 <code>None</code> 而不是零,优化器就能跳过未受影响参数组的计算。这个设置是白捡的吞吐提升,还略微缩小了 bug 面。</li></ul><h2 id="交付" tabindex="-1"><a class="header-anchor" href="#交付"><span>交付</span></a></h2><p>在真实项目里,<code>outputs/skill-clip-amp.md</code> 会描述:训练步用哪个裁剪阈值和 autocast 设备、逐步 CSV 放在版本控制的哪里、生产跳过率的告警阈值是多少。本课交付的是引擎。</p><h2 id="练习" tabindex="-1"><a class="header-anchor" href="#练习"><span>练习</span></a></h2><ol><li>把合成的 Inf 注入换成真实的 loss 尖峰(把某批次的目标乘 1e8),验证跳过路径被触发。</li><li>加 <code>--bf16</code> 模式,把 autocast 从 FP16 切到 BF16。BF16 的指数范围比 FP16 宽,几乎不需要 loss 缩放;在同一个演示上验证跳过率降到零。</li><li>加一个单元测试:没有发生裁剪时,梯度裁剪包装正确返回裁剪前和裁剪后范数。</li><li>加滚动窗口跳过率计算,加一个 CLI 参数:跳过率连续 100 步超过配置阈值就让运行失败。</li><li>把循环接上写规范 CSV(<code>step, lr, grad_l2_pre_clip, grad_l2_post_clip, loss, skipped, skip_reason, scaler_scale</code>),每行落盘后 flush,确认文件能活过 Ctrl-C。</li></ol><h2 id="关键术语" tabindex="-1"><a class="header-anchor" href="#关键术语"><span>关键术语</span></a></h2><table><thead><tr><th>术语</th><th>人们口中的说法</th><th>实际含义</th></tr></thead><tbody><tr><td>Global L2 norm</td><td>&quot;裁剪目标&quot;</td><td>所有可训练参数的拼接梯度向量的欧几里得范数</td></tr><tr><td>autocast</td><td>&quot;混合精度&quot;</td><td>在 <code>with</code> 块内有选择地以 FP16(或 BF16)执行符合条件的运算</td></tr><tr><td>GradScaler</td><td>&quot;loss 缩放器&quot;</td><td>反向前乘 loss、优化器步前反缩放梯度的辅助器</td></tr><tr><td>Skip</td><td>&quot;坏步&quot;</td><td>因梯度或 loss 非有限而被拒绝的优化器步;scaler 把因子减半</td></tr><tr><td>Scaling factor</td><td>&quot;scaler 状态&quot;</td><td>GradScaler 当前的乘数;干净段后加倍,每次跳过后减半</td></tr></tbody></table><h2 id="延伸阅读" tabindex="-1"><a class="header-anchor" href="#延伸阅读"><span>延伸阅读</span></a></h2><ul><li><a href="https://arxiv.org/abs/1710.03740" target="_blank" rel="noopener noreferrer">Micikevicius et al., Mixed Precision Training (arXiv 1710.03740)</a>——loss 缩放的原始提案</li><li><a href="https://arxiv.org/abs/1211.5063" target="_blank" rel="noopener noreferrer">Pascanu, Mikolov, Bengio, On the difficulty of training recurrent neural networks (arXiv 1211.5063)</a>——梯度裁剪的参考论文</li><li><a href="https://docs.pytorch.org/docs/stable/amp.html" target="_blank" rel="noopener noreferrer">PyTorch torch.amp.GradScaler</a>——本课包装的 scaler API</li><li><a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.utils.clip_grad_norm_.html" target="_blank" rel="noopener noreferrer">PyTorch torch.nn.utils.clip_grad_norm_</a>——本课使用的裁剪原语</li><li>第 19 阶段 · 42——其语料喂给本循环的下载器</li><li>第 19 阶段 · 43——本循环消费的数据加载器</li><li>第 19 阶段 · 44——与本循环组合的日程</li></ul>`,33)])}var l=a(s,[[`render`,c]]);export{o as _pageData,l as default};