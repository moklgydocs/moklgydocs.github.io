import{M as e,O as t,d as n,h as r,p as i}from"./runtime-core.esm-bundler-jC72uHyJ.js";import{t as a}from"./app-fxBgVz1h.js";var o=JSON.parse(`{"path":"/AI%E7%9F%A5%E8%AF%86%E5%BA%93/llm-basis/llm-operation-mechanism.html","title":"LLM 运行机制：Token、上下文窗口与采样参数怎么影响输出","lang":"zh-CN","frontmatter":{"title":"LLM 运行机制：Token、上下文窗口与采样参数怎么影响输出","description":"从结构化输出不稳定、长上下文失忆和采样参数失控等真实问题出发，拆解 Token、上下文窗口、Temperature、Top-p、Top-k 与 Token 预算的工程影响。","category":"AI 应用开发","icon":"mdi:robot-outline","head":[["meta",{"name":"keywords","content":"LLM,大语言模型,Token,上下文窗口,Temperature,Top-p,采样参数,AI 应用开发"}]]},"git":{"createdTime":1787385570000,"updatedTime":1787385570000,"contributors":[{"name":"jackie.liu","username":"","email":"moklgy@foxmail.com","commits":1}]},"readingTime":{"minutes":19.35,"words":5804},"filePathRelative":"AI知识库/llm-basis/llm-operation-mechanism.md"}`),s={name:`llm-operation-mechanism.md`};function c(a,o,s,c,l,u){let d=e(`Mermaid`);return t(),n(`div`,null,[o[0]||=i(`<blockquote><p>本文转载自 <a href="https://javaguide.cn/ai/" target="_blank" rel="noopener noreferrer">JavaGuide《AI 应用开发》专栏</a>(作者:Snailclimb),原文:<a href="https://javaguide.cn/ai/llm-basis/llm-operation-mechanism.html" target="_blank" rel="noopener noreferrer">https://javaguide.cn/ai/llm-basis/llm-operation-mechanism.html</a>。著作权归原作者所有,本站按作者转载要求注明出处,仅供个人学习使用。</p></blockquote><p>温度已经设为 0，结构化输出仍可能解析失败；上下文塞满文档后，模型也可能漏掉中间位置的关键约束。这些现象需要从 Token 切分、上下文容量和解码策略分别排查。</p><p>排查这些问题，要先看一次调用由哪些 Token 组成，再核对上下文预算以及 Temperature、Top-p、Top-k 等解码参数。文中的 Token 数和参数范围只用于解释机制，实际计费与能力上限仍以目标模型的 API 文档和响应 <code>usage</code> 为准。</p><h2 id="token-和上下文为什么决定成本与效果" tabindex="-1"><a class="header-anchor" href="#token-和上下文为什么决定成本与效果"><span>Token 和上下文为什么决定成本与效果？</span></a></h2><p>当你在输入法里打“今天天气真”，它会自动建议“好”。大模型同样在预测后续内容，只不过它参考的是前面几千甚至几十万个字。每次生成一个 Token（文本碎片），再把它加入上下文并预测下一个，直到回答结束。</p><p>这个过程叫做<strong>自回归生成（Autoregressive Generation）</strong>。</p><p>自回归生成把后面的几个概念串在了一起：</p><ul><li><strong>Token</strong>：模型每一步“补”的文本碎片。</li><li><strong>上下文窗口</strong>：一次调用里模型可处理的总 Token 上限，系统提示词、历史消息、当前输入和输出预算都会占用。</li><li><strong>Temperature / Top-p</strong>：模型选哪个候选碎片的策略。</li><li><strong>Max Tokens</strong>：允许模型最多“补”多少步。</li></ul><p>Tokenizer 接到文本后，会把它拆成大小不等的片段。比如 <code>你好，我是小 G。</code> 可以得到这样一组示意结果：</p><ul><li>原文：<code>你好，我是小 G。</code></li><li>切分：<code>[你好]</code> <code>[，]</code> <code>[我是]</code> <code>[小 G]</code> <code>[。]</code></li><li>统计：原文 9 字符 → Token 数 5 个 → 压缩比约 1.8 倍</li></ul><figure><img src="https://oss.javaguide.cn/github/javaguide/ai/llm/llm-token-process.png" alt="Token 化过程示例" tabindex="0" loading="lazy"><figcaption>Token 化过程示例</figcaption></figure><p>这组切分只用于说明过程。实际结果取决于目标模型的 Tokenizer，同一段文本换一个供应商或模型版本，Token 序列就可能改变。OpenAI 也提供了可直接查看切分结果的 <a href="https://platform.openai.com/tokenizer" target="_blank" rel="noopener noreferrer">Tokenizer 工具</a>。</p><p>如果固定按字切，词表比较小，序列却会变长；固定按词切可以缩短序列，但中文词组数量会让词表快速膨胀。BPE、Unigram 等<strong>子词切分算法</strong>在两者之间取舍：高频片段尽量作为整体保留，低频词继续拆小。因此，Token 和字、词都没有固定的一一对应关系。英文单词可能占多个 Token；中文也可能一词多 Token，或多字合成一个 Token。</p><p>容量规划可以先用经验值估算：英文 1 Token 大约对应 3~4 个字符；中文通常在 1~2 个汉字之间，混排内容还会继续波动。DeepSeek 官方数据给出的换算是 1 个英文字符约消耗 0.3 Token、1 个中文字符约消耗 0.6 Token，也就是每个 Token 约为 3.3 个英文字符或 1.7 个中文字符。</p><p>Tokenizer 版本也会改变换算结果。早期模型（如 GPT-3.5）的中文压缩率较低，约为 1 字 1.5~2 Token；GPT-4o 使用词表约 20 万的 o200k_base Tokenizer，Qwen2.5 词表约 15 万。现有实测中，新闻类文本约为 1.5 字/Token，技术文档约为 1.2 字/Token，但这些数字不适合直接用于结算。</p><p>“趋近 1 字 1 Token”只可能出现在部分高频词上。预算阶段可以用经验值留出余量，计费与监控则读取 API 返回的 <code>usage</code>。中文歧义、生僻字和低频专业术语被切成什么粒度，也会影响模型对文本的处理效果。</p><p><strong>特殊 Token</strong>：除了文本内容对应的 Token，模型内部还会使用一些特殊标记，这些也会计入 Token 总数：</p><table><thead><tr><th>特殊 Token</th><th>用途</th><th>示例</th></tr></thead><tbody><tr><td>BOS（Beginning of Sequence）</td><td>标记序列开始</td><td><code>&lt;s&gt;</code></td></tr><tr><td>EOS（End of Sequence）</td><td>标记序列结束</td><td><code>&lt;/s&gt;</code></td></tr><tr><td>PAD（Padding）</td><td>批处理时填充短序列</td><td><code>&lt;pad&gt;</code></td></tr><tr><td>工具调用标记</td><td>Function Calling 边界</td><td><code>&lt;tool_call/&gt;</code></td></tr></tbody></table><p>这些特殊 Token 通常对用户不可见，但会占用上下文窗口。精确计数时建议使用官方 Tokenizer 工具而非手动估算。</p><h3 id="多模态输入的-token-开销" tabindex="-1"><a class="header-anchor" href="#多模态输入的-token-开销"><span>多模态输入的 Token 开销</span></a></h3><p>支持视觉输入的模型会把图片转换为内部表示，并按各自规则折算输入 Token。这个数不能只根据“有一张图”估算：</p><ul><li>OpenAI 视觉模型会结合模型、图片尺寸和 <code>detail</code> 模式计费。以采用 512 像素分块规则的模型为例，低细节使用固定基础额度；高细节还会按缩放后的分块数量增加 Token。</li><li>Anthropic 按缩放后的图片像素数估算，官方给出的近似公式是 <code>tokens ≈ width × height / 750</code>。一张没有被缩放的 1024×1024 图片约为 1398 Tokens，并非固定的 5 或 85 Tokens。</li><li>Gemini 对较小图片和较大图片使用不同的分块规则。官方文档中的 258 Tokens 有尺寸条件，不能直接套到任意 1024×1024 图片上。</li></ul><p>模型版本会改变图片计费规则。上线前应使用供应商提供的 Token 计算接口或官方公式，并分别覆盖缩略图、截图、长图和多图请求。可参考 <a href="https://developers.openai.com/api/docs/guides/images-vision" target="_blank" rel="noopener noreferrer">OpenAI 图片与视觉输入</a>、<a href="https://platform.claude.com/docs/en/build-with-claude/vision" target="_blank" rel="noopener noreferrer">Anthropic Vision</a> 和 <a href="https://ai.google.dev/gemini-api/docs/tokens" target="_blank" rel="noopener noreferrer">Gemini Token 计算</a>。</p><p>图片进入多模态 RAG 后，这笔开销会同时影响预算和延迟：</p><ul><li>图片 Token 要和文本 Token 一起计入单次调用预算。</li><li>批量图片会拉长首字延迟（TTFT），需要单独压测。</li><li>任务只需要 OCR 结果时，可以先提取文字，再把纯文本送入模型。</li></ul><h3 id="上下文窗口的容量边界" tabindex="-1"><a class="header-anchor" href="#上下文窗口的容量边界"><span>上下文窗口的容量边界</span></a></h3><p>模型标注的 128K、200K 或 1M，指一次调用能够容纳的 Token 上限。窗口越大，单次可传入的文档和对话历史越多，但这部分容量还要分给系统提示词、工具定义和模型输出。大多数模型将输入与输出合并计算，部分供应商（如 Google Gemini）则分别设置输入和输出上限。</p><figure><img src="https://oss.javaguide.cn/github/javaguide/ai/llm/llm-context-window.png" alt="上下文窗口（Context Window）= LLM 的「工作记忆」" tabindex="0" loading="lazy"><figcaption>上下文窗口（Context Window）= LLM 的「工作记忆」</figcaption></figure><ul><li><strong>固定内容</strong>：System Prompt、工具调用 Schema 和格式标记。</li><li><strong>本次输入</strong>：User Prompt、历史消息与 RAG 检索片段。</li><li><strong>生成预算</strong>：模型即将生成的输出 Token。</li></ul><p>标称窗口减掉这些内容，剩下的才是业务数据实际可用的空间。</p><p>注意：上下文窗口（Context Window）≠ 最大生成长度。许多模型支持 128K 甚至 1M 上下文，但单次输出上限因模型和 API 而异；参数名也可能是 <code>max_tokens</code>、<code>max_completion_tokens</code> 或 <code>max_output_tokens</code>。不要根据供应商名称推断上限，调用前读取目标模型的能力页。</p><p>推理模型的多轮消息格式需要按供应商区分：</p><ul><li><a href="https://api-docs.deepseek.com/guides/thinking_mode" target="_blank" rel="noopener noreferrer">DeepSeek 思考模式</a>会分别返回 <code>reasoning_content</code> 和最终 <code>content</code>。没有工具调用时，上一轮 <code>reasoning_content</code> 无需参与下一轮上下文，即使传入也会被忽略；发生工具调用时，当前轮的 <code>reasoning_content</code> 必须随工具结果回传，直到该轮完成。</li><li><a href="https://developers.openai.com/api/docs/guides/reasoning" target="_blank" rel="noopener noreferrer">OpenAI 推理模型</a>不向应用暴露原始 chain-of-thought。Responses API 可以通过 <code>previous_response_id</code> 延续上下文，或者按 API 要求回传 reasoning item；应用能拿到的是可选的推理摘要，而不是内部推理原文。</li></ul><p>推理 Token 虽然不一定出现在下一轮消息文本里，但本轮仍会消耗生成预算，并可能计入输出 Token 和上下文限制。不要据此得出“推理过程不占窗口或不计费”的通用结论。</p><h3 id="长上下文背后的计算约束" tabindex="-1"><a class="header-anchor" href="#长上下文背后的计算约束"><span>长上下文背后的计算约束</span></a></h3><p>Transformer 的<strong>自注意力机制（Self-Attention）</strong>会为长上下文带来三类开销：</p><ul><li>计算成本平方级增长：计算需求与序列长度呈平方级关系（O(N²)）。输入 Token 翻倍，处理能力需求可能变为 4 倍。</li><li>推理延迟增加：上下文变长后，模型生成每个新 Token 时需要关注的历史 Token 变多，首字延迟 TTFT 会显著增加。</li><li>安全风险增加：更长的上下文意味着更大的攻击面。</li></ul><p>FlashAttention、GQA/MQA、Sliding Window Attention、Ring Attention 等技术可以减少计算量或显存占用，但没有让所有长上下文都变成线性成本，O(N²) 仍是标准自注意力需要面对的理论复杂度。</p><h3 id="上下文溢出的真实表现" tabindex="-1"><a class="header-anchor" href="#上下文溢出的真实表现"><span>上下文溢出的真实表现</span></a></h3><p>System Prompt 明明要求“必须输出 JSON”，模型却漏掉这条约束，是上下文过长时最容易观察到的现象之一。回答还可能在后半段偏题，或者因为 RAG 片段太多而抓不住真正相关的证据。</p><p>信息放进窗口也不等于模型能同等利用每个位置。“中间丢失”描述的就是模型更容易利用开头和结尾、对中间内容召回较弱的情况。窗口扩到 1M 也不能自动消除这个问题。</p><p>长上下文还有可以直接监控的代价：输入 Token 随内容量增加，账单随之增长；预填充耗时和 TTFT 也会变长。出现这些信号时，应先检查检索片段数量、历史消息裁剪和关键信息位置，而不是继续把窗口塞满。</p><h3 id="输入-token-与输出-token-的计费差异" tabindex="-1"><a class="header-anchor" href="#输入-token-与输出-token-的计费差异"><span>输入 Token 与输出 Token 的计费差异</span></a></h3><p>多数供应商会分别计算输入、缓存输入和输出 Token，推理模型还可能单列 reasoning tokens。输出单价经常高于输入单价，但不存在稳定的“2~4 倍”行业比例：模型版本、批处理、缓存命中和服务等级都会改变价格。</p><p>价格表更新频繁，不适合固化在原理文章里。成本核算应读取供应商当前价格页，并在网关中保存 <code>model_version</code>、各类 <code>usage</code> 和实际账单单价。</p><p>成本核算要分别记录输入、缓存输入、输出以及供应商返回的 reasoning tokens。RAG 先控制检索片段数量，避免无关内容推高输入 Token；输出和推理过程则用生成上限约束。由于 reasoning tokens 经常计入输出侧费用，推理模型还要单独看这部分用量。</p><h3 id="prompt-caching-的省钱逻辑" tabindex="-1"><a class="header-anchor" href="#prompt-caching-的省钱逻辑"><span>Prompt Caching 的省钱逻辑</span></a></h3><p>批量评测经常重复发送同一份 System Prompt，只替换末尾的样本；多轮对话也会重复携带一段固定历史。这类请求可以利用 <strong>Prompt Caching</strong> 复用前缀。后续请求满足模型、前缀长度、内容和有效期等条件时，命中部分会按缓存输入计费，并减少一部分预填充计算。</p><p>固定前缀较长、重复率较高的任务更容易受益：</p><ul><li>多轮对话（System Prompt + 历史 Message 不变）。</li><li>RAG 应用（检索片段重复率高）。</li><li>批量评估（同一份 System Prompt，不同的简历/文章）。</li></ul><p>OpenAI Prompt Caching、Anthropic Prompt Caching 和 DeepSeek Context Caching 的触发门槛、缓存寿命与价格并不相同，有些还区分自动缓存、显式缓存和延长缓存。配置前查当前官方文档，不要把缓存时长和折扣写死在业务代码里。</p><p>请求内容的排列会直接影响缓存命中：</p><ol><li>把不变的内容放前面（System Prompt、工具定义、RAG Context），把变化的内容放后面（User Prompt）。</li><li>按供应商响应字段监控缓存读取和缓存写入 Token，验证缓存命中率。</li><li>批量任务尽量在缓存时间窗口内完成。</li></ol><h3 id="一次调用的-token-预算公式" tabindex="-1"><a class="header-anchor" href="#一次调用的-token-预算公式"><span>一次调用的 Token 预算公式</span></a></h3><p>把“上下文窗口”当成一个固定容量的桶，下图展示了一个典型调用的 Token 预算分配：</p>`,55),r(d,{code:``}),o[1]||=i('<p>图中的数值只用于示意。普通生成模型先检查这个关系是否成立：</p><p><strong>window ≥ input_tokens + max_output_tokens</strong></p><p>推理模型要按目标 API 的参数定义单独计算。有些 API 的 <code>max_output_tokens</code> 或 <code>max_completion_tokens</code> 已经同时包含推理 Token 和可见回答，此时再加一次 <code>reasoning_tokens</code> 会重复；另一些 API 会分别限制思考过程和最终回答。上线前可以用返回的 <code>usage</code> 反推一次真实调用的组成，再校准预算公式。</p><p>其中 <code>input_tokens</code> 至少包含：</p><ul><li>system prompt（含 schema / 工具定义）</li><li>user prompt（含变量替换后的实际文本）</li><li>历史消息（多轮对话时）</li><li>RAG context（如果拼进来了）</li></ul><p>结构化任务的输出长度通常更容易控制，可以先确定 <code>max_output_tokens</code>，再给输入留出 10%~20% 的安全余量。预算仍然不够时，按顺序减少 RAG Top-K、合并重复片段、摘要或截断长字段；一轮塞不下的任务再拆成分批评估或两阶段生成。</p><h2 id="采样参数如何影响输出稳定性" tabindex="-1"><a class="header-anchor" href="#采样参数如何影响输出稳定性"><span>采样参数如何影响输出稳定性？</span></a></h2><h3 id="从-logits-到概率采样" tabindex="-1"><a class="header-anchor" href="#从-logits-到概率采样"><span>从 logits 到概率采样</span></a></h3><p>模型每一步会给词表中<strong>每个</strong>候选 Token 打一个分数（内部叫 <strong>logits</strong>），分数越高说明模型越觉得这个词应该出现在这里。</p><p>举个例子，假设模型正在补全“今天天气真__”，它可能给出这样的分数：</p><table><thead><tr><th>候选 Token</th><th>原始分数（logit）</th></tr></thead><tbody><tr><td>好</td><td>5.0</td></tr><tr><td>不错</td><td>3.2</td></tr><tr><td>棒</td><td>2.1</td></tr><tr><td>糟糕</td><td>0.5</td></tr><tr><td>紫色</td><td>-8.0</td></tr></tbody></table><p>原始分数还不是概率，需要经过 <strong>softmax</strong> 才能得到候选 Token 的概率分布。变换后大致是：</p><table><thead><tr><th>候选 Token</th><th>概率</th></tr></thead><tbody><tr><td>好</td><td>81.21%</td></tr><tr><td>不错</td><td>13.42%</td></tr><tr><td>棒</td><td>4.47%</td></tr><tr><td>糟糕</td><td>0.90%</td></tr><tr><td>紫色</td><td>约 0.00%</td></tr></tbody></table><p>得到概率分布后，模型再通过采样决定输出哪个 Token。</p><p>解码参数（Temperature、Top-p、Top-k 等）就是在这个“打分 → 概率 → 抽签”的过程中施加控制：</p><ul><li>Temperature：调整概率分布的“形状”，让高分选项更突出，或者让各选项更均匀。</li><li>Top-p / Top-k：直接砍掉不靠谱的候选项，缩小“抽签池”。</li><li>Penalty 系列：对已经出现过的词降分，防止“复读机”。</li></ul><h3 id="temperature-的-冒险程度" tabindex="-1"><a class="header-anchor" href="#temperature-的-冒险程度"><span>Temperature 的“冒险程度”</span></a></h3><figure><img src="https://oss.javaguide.cn/github/javaguide/ai/llm/llm-temperature-params.png" alt="Temperature 参数：控制模型输出的随机性" tabindex="0" loading="lazy"><figcaption>Temperature 参数：控制模型输出的随机性</figcaption></figure><p>Temperature 的工作原理很简单：在 softmax 之前，先把所有分数<strong>除以</strong>温度值 T。</p><p><strong>p(t) = softmax(z_t / T)</strong></p><p>把前面的“今天天气真__”代入公式，可以看到温度怎样改变同一组 logits：</p><table><thead><tr><th>温度</th><th>概率分布的变化</th><th>示例结果</th></tr></thead><tbody><tr><td>T = 0.2</td><td>分布变尖，高概率 Token 更集中</td><td>“好”约 99.99%</td></tr><tr><td>T = 1.0</td><td>保持原始分布</td><td>“好”约 81.21%，“不错”约 13.42%</td></tr><tr><td>T = 1.5</td><td>分布变平，低概率 Token 获得更多机会</td><td>“好”约 66.85%，“不错”约 20.14%，“棒”约 9.67%</td></tr></tbody></table><p>温度越低，输出越确定；温度越高，输出越随机。</p><p>工程建议（经验值，非硬规则）：</p><table><thead><tr><th>场景</th><th>推荐温度</th><th>说明</th></tr></thead><tbody><tr><td>结构化提取 / JSON 输出</td><td>0 ~ 0.3</td><td>配合严格 schema + 解析失败重试策略</td></tr><tr><td>评估 / 分析 / 代码评审</td><td>0.4 ~ 0.8</td><td>平衡确定性与表达多样性</td></tr><tr><td>创作类内容（文案、头脑风暴）</td><td>0.8 ~ 1.2+</td><td>增加多样性，但要承担格式一致性风险</td></tr></tbody></table><p>如果目标 API 支持 <code>seed</code>，可以把它和固定模型版本、固定 Prompt 及固定采样参数一起保存。<code>seed</code> 通常只提供尽力而为的可复现性，不保证逐字一致；DeepSeek 当前 API 参数也没有公开 <code>seed</code>。</p><p>以下情况仍可能导致结果不一致：</p><ul><li>模型版本更新（底层权重变化）。</li><li>跨区域调用（不同集群可能部署不同版本）。</li><li>服务端解码实现、并行计算和后端配置发生变化。</li></ul><p>CI/CD 可以把真实 LLM 调用留给冒烟测试；需要稳定复现的逻辑测试仍然使用 Mock。</p><h3 id="top-p-与-top-k-的-抽签池" tabindex="-1"><a class="header-anchor" href="#top-p-与-top-k-的-抽签池"><span>Top-p 与 Top-k 的“抽签池”</span></a></h3><p>Temperature 调整概率分布的形状。Top-p 和 Top-k 会截断候选集合，把尾部候选排除在采样范围之外。</p><p>还是用“今天天气真__”的例子：</p><table><thead><tr><th>候选 Token</th><th>概率</th><th>累计概率</th></tr></thead><tbody><tr><td>好</td><td>81.21%</td><td>81.21%</td></tr><tr><td>不错</td><td>13.42%</td><td>94.63%</td></tr><tr><td>棒</td><td>4.47%</td><td>99.10%</td></tr><tr><td>糟糕</td><td>0.90%</td><td>约 100%</td></tr><tr><td>紫色</td><td>约 0.00%</td><td>100%</td></tr></tbody></table><p><code>Top-k = 3</code> 固定保留概率最高的“好、不错、棒”，其余候选不再参与采样。<code>Top-p = 0.9</code> 则从高到低累加，当前示例中“好 + 不错”已经达到 94.63%，候选集只保留这两个；如果第一名自身超过 90%，就只留下一个。</p><p>Top-k 控制固定数量，Top-p 控制累计概率，所以 Top-p 的候选数量会随分布变化。与 Temperature 组合后，常见行为如下：</p><table><thead><tr><th>组合</th><th>效果</th><th>适用场景</th></tr></thead><tbody><tr><td>T=0（通常按贪婪解码处理）</td><td>每步选最高分，结果仍受模型版本和服务端实现影响</td><td>结构化输出、低随机性场景</td></tr><tr><td>低温 + Top-p=0.9</td><td>相对稳定，但允许措辞上有些变化</td><td>分析报告、摘要</td></tr><tr><td>中高温 + Top-p=0.95</td><td>多样性较高，但排除了极端离谱选项</td><td>创意写作、对话</td></tr></tbody></table><p>注意：贪婪解码虽然最稳定，但可能更容易陷入重复循环。</p><h3 id="停止条件与截断风险" tabindex="-1"><a class="header-anchor" href="#停止条件与截断风险"><span>停止条件与截断风险</span></a></h3><p>生成达到 Max Tokens 后会直接停止，哪怕 JSON 还缺右括号、列表还有项目没写完。服务端收到 <code>finish_reason=length</code> 时，应把结果标记为截断，不能交给下游继续执行。</p><p>Stop Sequences 会在模型生成指定字符串时提前结束，例如 <code>&quot;\\n\\n&quot;</code> 或 <code>&quot;```&quot;</code>。停止词如果与业务文本重合，同样会截断关键字段。结构化输出需要分别覆盖达到生成上限和误触停止词这两条失败路径。</p><p>推理模型还要确认生成上限是否同时覆盖推理过程与最终回答。部分 API 共用一个生成预算，推理 Token 用得越多，可见回答剩余空间就越少；另一些 API 通过 <code>reasoning_effort</code> 等参数间接控制推理量。发现 <code>finish_reason=length</code> 时，要把推理 Token 和最终回答一起纳入排查。</p><h3 id="penalty-与复读问题" tabindex="-1"><a class="header-anchor" href="#penalty-与复读问题"><span>Penalty 与复读问题</span></a></h3><p>可能遇到过模型反复输出同一句话，或者在长回答里不断重复相同观点。Penalty 参数用来缓解这类问题，它们在解码时<strong>降低已出现 Token 的分数</strong>：</p><table><thead><tr><th>参数</th><th>作用</th><th>通俗理解</th></tr></thead><tbody><tr><td>Repetition Penalty</td><td>降低所有已出现 Token 的概率</td><td>“说过的词，再说就扣分”</td></tr><tr><td>Presence Penalty</td><td>只要 Token 出现过就扣分（不看次数）</td><td>“鼓励聊新话题”</td></tr><tr><td>Frequency Penalty</td><td>Token 出现次数越多扣分越重</td><td>“同一个词说了三遍？重罚”</td></tr></tbody></table><p>JSON 对象可能反复出现 <code>&quot;name&quot;</code>、<code>&quot;score&quot;</code> 等字段名，Repetition Penalty 过高会连这些必要重复一起降分，结果可能缺字段。RAG 问答使用 Presence Penalty 时，模型会更倾向引入没有出现在检索内容里的新词，也会削弱回答对证据的忠实度。</p><p>不同供应商对 Penalty 的定义并不完全一致。没有明确调参依据时，保留默认值，再用输出长度、Prompt 约束和 Schema 控制结构，排查路径会更清楚。</p><h3 id="思维链模式的参数限制" tabindex="-1"><a class="header-anchor" href="#思维链模式的参数限制"><span>思维链模式的参数限制</span></a></h3><p>推理模型的参数支持范围由具体 API 决定，不能把 DeepSeek 思考模式和 OpenAI 推理模型概括成同一种行为。</p><ul><li>DeepSeek V4 思考模式会返回 <code>reasoning_content</code> 与最终 <code>content</code>，并明确列出不支持或不会生效的采样参数。</li><li>OpenAI 不返回原始内部推理过程。Responses API 可以返回推理 item 和可选摘要；不同推理模型对 <code>temperature</code>、<code>top_p</code> 等参数的支持也可能不同。</li></ul><p>调用前应按模型能力表过滤不支持的参数。结构稳定性仍要依赖 Structured Outputs、服务端校验和失败处理，不能只调 Temperature。</p><h3 id="流式输出与首字延迟" tabindex="-1"><a class="header-anchor" href="#流式输出与首字延迟"><span>流式输出与首字延迟</span></a></h3><p>同步接口要等完整内容生成后再返回；流式接口生成一个或几个 Token 就会推送增量，因此用户更早看到第一个 Token，首字延迟（TTFT，Time-To-First-Token）也更低。这里容易混淆的是首字延迟、总耗时和费用：</p><ul><li>流式输出不一定降低总耗时（E2E latency），模型生成的总 Token 量没有因此减少。</li><li>流式输出不会自动省钱，Token 计费不变，仍然受限流和配额影响。</li><li>如果需要结构化输出（如 JSON），流式场景要考虑“半成品 JSON”在前端/网关层的处理。</li></ul><h3 id="logprobs-与置信度排查" tabindex="-1"><a class="header-anchor" href="#logprobs-与置信度排查"><span>Logprobs 与置信度排查</span></a></h3><p>部分 API（如 OpenAI）支持返回每个生成 Token 的<strong>对数概率</strong>（logprobs），可以理解为模型对该 Token 的“确信程度”。logprob 越接近 0，模型越确信；值越小（如 -5.0），说明模型越“犹豫”。</p><p>工程应用场景：</p><ul><li><strong>置信度评估</strong>：提取“金额: 1000”时，若对应 Token 的 logprob 很低，说明模型不太确定，可能需要人工复核。</li><li><strong>异常检测</strong>：监控生产环境中模型输出的平均 logprob，若突然下降可能提示 Prompt 漂移或输入数据异常。</li><li><strong>多候选对比</strong>：获取 Top-N 候选 Token 及其概率，用于纠错或二次排序。</li></ul><p>注意事项：logprobs 会增加响应体积，且并非所有供应商都支持。使用前请查阅 API 文档。</p><h3 id="采样参数配置建议" tabindex="-1"><a class="header-anchor" href="#采样参数配置建议"><span>采样参数配置建议</span></a></h3><table><thead><tr><th>场景</th><th>Temperature</th><th>Top-p</th><th>Penalty</th><th>其他建议</th></tr></thead><tbody><tr><td>JSON / 结构化输出</td><td>0 ~ 0.3</td><td>1.0</td><td>保持默认</td><td>配合 Strict Mode + 重试策略</td></tr><tr><td>代码评审 / 技术分析</td><td>0.4 ~ 0.7</td><td>0.9</td><td>保持默认</td><td>结合 CoT Prompt</td></tr><tr><td>多轮对话</td><td>0.6 ~ 0.8</td><td>0.9</td><td>适度开启</td><td>控制历史消息长度</td></tr><tr><td>创意写作 / 头脑风暴</td><td>0.8 ~ 1.2</td><td>0.95</td><td>按需开启</td><td>接受输出多样性，做好后处理</td></tr><tr><td>推理模型</td><td>查模型文档</td><td>查模型文档</td><td>查模型文档</td><td>过滤不支持参数，保留服务端校验</td></tr></tbody></table><h2 id="落地时保留哪些数据" tabindex="-1"><a class="header-anchor" href="#落地时保留哪些数据"><span>落地时保留哪些数据</span></a></h2><p>容量规划按 Token 做，计费与告警以 API 返回的 <code>usage</code> 为准。每次调用至少保存模型版本、输入 Token、缓存 Token、推理 Token（如果供应商返回）、输出 Token、上下文裁剪策略和采样参数。</p><p>长上下文只提高可容纳的信息量，不保证模型能同等利用每个位置。结构化任务还要配合 Schema 和服务端校验；需要复现实验时，则固定模型版本、Prompt、输入与解码参数，并接受供应商服务仍可能带来的小幅非确定性。</p>',63)])}var l=a(s,[[`render`,c]]);export{o as _pageData,l as default};