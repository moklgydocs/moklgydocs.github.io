import{O as e,d as t,p as n}from"./runtime-core.esm-bundler-jC72uHyJ.js";import{t as r}from"./app--CEjSbyQ.js";var i=JSON.parse(`{"path":"/ai-eng/16-multi-agent-and-swarms/23-failure-modes-mast-groupthink.html","title":"失败模式 —— MAST、团体迷思、单一文化与级联错误","lang":"zh-CN","frontmatter":{},"git":{"createdTime":1787385595000,"updatedTime":1787488693000,"contributors":[{"name":"jackie.liu","username":"","email":"moklgy@foxmail.com","commits":3}]},"readingTime":{"minutes":9.58,"words":2873},"filePathRelative":"ai-eng/16-multi-agent-and-swarms/23-failure-modes-mast-groupthink.md"}`),a={name:`23-failure-modes-mast-groupthink.md`};function o(r,i,a,o,s,c){return e(),t(`div`,null,[...i[0]||=[n(`<h1 id="失败模式-——-mast、团体迷思、单一文化与级联错误" tabindex="-1"><a class="header-anchor" href="#失败模式-——-mast、团体迷思、单一文化与级联错误"><span>失败模式 —— MAST、团体迷思、单一文化与级联错误</span></a></h1><blockquote><p>2026 年的参考分类法是 <strong>MAST</strong>(Cemri et al.,NeurIPS 2025,arXiv:2503.13657),源自对 7 个最先进开源 MAS 的 1642 条执行轨迹的分析,这些系统的<strong>失败率在 41–86.7%</strong>。三大根因类别:<strong>规约问题(Specification Problems,41.77%)</strong>——角色含糊、任务定义不清;<strong>协调失败(Coordination Failures,36.94%)</strong>——通信中断、状态失同步;<strong>验证缺口(Verification Gaps,21.30%)</strong>——缺少校验、没有质量检查。<strong>团体迷思(Groupthink)</strong>家族(arXiv:2508.05687)补充了:单一文化崩塌(同一基座模型 → 相关失败)、从众偏差(智能体互相强化彼此的错误)、心智理论不足、混合动机动态、级联可靠性失败。级联的例子:重试风暴——一次支付失败触发订单重试,订单重试触发库存重试,最终压垮库存服务(几秒内 10 倍负载——需要熔断器)。记忆投毒:一个智能体的幻觉进入共享记忆,下游智能体把它当事实;准确率缓慢衰减,让根因诊断非常痛苦。<strong>STRATUS</strong>(NeurIPS 2025)报告,通过专用的检测/诊断/验证智能体,缓解成功率提升 1.5 倍。本课把失败模式当作一等工程目标来对待。</p></blockquote><p><strong>类型:</strong> 学习<br><strong>编程语言:</strong> Python(标准库)<br><strong>前置要求:</strong> 第 16 阶段 · 13(共享记忆)、第 16 阶段 · 14(共识与 BFT)、第 16 阶段 · 15(投票与辩论拓扑)<br><strong>预计耗时:</strong> 约 75 分钟</p><h2 id="问题" tabindex="-1"><a class="header-anchor" href="#问题"><span>问题</span></a></h2><p>多智能体系统在真实任务上的失败率是 41-86.7%(Cemri et al. 2025 在 7 个开源 MAS 上测得)。这可不是&quot;再加几个智能体&quot;就能调试的。失败有结构性原因。MAST 分类法给你这些类别。本课把每个类别映射到具体的检测、诊断和缓解模式,让这些数字不再显得莫名其妙。</p><p>2026 年的生产实践是把失败模式当作设计输入。除非你能指着 MAST 的每个类别、说出你部署了什么缓解措施,否则你的架构就不算&quot;够好&quot;。</p><h2 id="概念" tabindex="-1"><a class="header-anchor" href="#概念"><span>概念</span></a></h2><h3 id="mast-类别" tabindex="-1"><a class="header-anchor" href="#mast-类别"><span>MAST 类别</span></a></h3><p><strong>规约问题(占失败的 41.77%)。</strong> 智能体的任务定义得不够紧。例子:</p><ul><li>角色含糊:两个智能体都以为自己是 reviewer。</li><li>任务欠规约:用户想要特定角度,任务却只写了&quot;总结一下这个&quot;。</li><li>成功标准隐含:智能体无法判断自己是否成功。</li></ul><p>缓解:</p><ul><li>写显式的角色契约。每个智能体的提示词说明它做什么<em>以及不做什么</em>。</li><li>每个任务配验收测试。智能体开工前,先定义&quot;完成长什么样&quot;。</li><li>派发前的规约检查:由一个独立智能体在派发前评审任务定义。</li></ul><p><strong>协调失败(36.94%)。</strong> 通信或状态层面的崩溃。</p><p>例子:</p><ul><li>两个智能体在没有同步的情况下更新共享状态。</li><li>智能体之间消息丢失(队列故障、超时)。</li><li>状态漂移:智能体 A 以为任务完成了;智能体 B 还在执行。</li></ul><p>缓解:</p><ul><li>带版本的共享状态 + 乐观并发。</li><li>关键消息显式确认(重试直到收到 ack)。</li><li>周期性状态同步检查点;尽早发现漂移。</li></ul><p><strong>验证缺口(21.30%)。</strong> 输出没有独立检查。</p><p>例子:</p><ul><li>一个智能体声称成功;没人验证。</li><li>一条智能体链,每个都信任上游的输出。</li><li>涌现出来的组合行为没有测试覆盖。</li></ul><p>缓解:</p><ul><li>独立验证者智能体(第 13 课)。只读、独立的信息源访问。</li><li>显式的交接契约:&quot;A 的输出必须通过检查器 C,B 才能开工。&quot;</li><li>记录结果日志,供事后分析。</li></ul><h3 id="团体迷思家族-arxiv-2508-05687" tabindex="-1"><a class="header-anchor" href="#团体迷思家族-arxiv-2508-05687"><span>团体迷思家族(arXiv:2508.05687)</span></a></h3><p>当智能体同质化或互相模仿时的五种相关失败:</p><p><strong>单一文化崩塌。</strong> 同一基座模型或训练数据 → 相关错误。三个智能体共用一个 LLM,也就共享了它的幻觉。</p><p><strong>从众偏差。</strong> 智能体向声音最大或最自信的同伴靠拢,哪怕对方是错的。</p><p><strong>心智理论不足。</strong> 智能体无法建模彼此的信念;协调土崩瓦解(第 18 课)。</p><p><strong>混合动机动态。</strong> 激励只部分对齐的智能体漂向折中中间态,结果谁都不满意。</p><p><strong>级联可靠性失败。</strong> 一个组件的错误模式触发依赖组件的错误模式。</p><h3 id="级联示例-——-重试风暴" tabindex="-1"><a class="header-anchor" href="#级联示例-——-重试风暴"><span>级联示例 —— 重试风暴</span></a></h3><p>一个经典的 2026 年事故模式:</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-"><span class="line"><span>payment service fails 10% of requests</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>order agent retries payment (exponential backoff but naive)</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>each retry is a new order-inventory check</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>inventory service sees 2x normal load</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>inventory service starts timing out</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>every order retries inventory check</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>inventory service sees 10x normal load</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>cluster goes down</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>修法是经典的:<strong>熔断器</strong>。当下游错误率超过阈值时短路,用缓存或默认结果兜底。外加每个请求的重试预算上限。</p><p>熔断器是少数几个可以不加修改直接从分布式系统搬进多智能体的失败缓解手段之一。</p><h3 id="记忆投毒-再谈" tabindex="-1"><a class="header-anchor" href="#记忆投毒-再谈"><span>记忆投毒(再谈)</span></a></h3><p>第 13 课讲过:一个智能体的幻觉变成共享记忆里的事实;下游智能体基于这个被污染的事实推理。用 MAST 的话说,这是共享记忆层的验证缺口。</p><p>症状是准确率缓慢衰减。你等不到一次崩溃;你得到的是难以根因分析的缓慢漂移。</p><p>缓解:只追加日志、来源记录(provenance)、不可篡改的验证者。第 13 课已经讲过。</p><h3 id="stratus-——-失败检测专用智能体" tabindex="-1"><a class="header-anchor" href="#stratus-——-失败检测专用智能体"><span>STRATUS —— 失败检测专用智能体</span></a></h3><p>STRATUS(NeurIPS 2025)报告,部署以下角色可带来 1.5 倍的缓解成功率提升:</p><ul><li><strong>检测智能体。</strong> 盯症状模式(高分歧、重试尖峰、准确率漂移)。</li><li><strong>诊断智能体。</strong> 给定症状,从 MAST 分类法中推断最可能的根因。</li><li><strong>验证智能体。</strong> 缓解措施落地后,检查症状是否消除。</li></ul><p>这就是 SRE 风格的事故响应,应用到智能体系统上。三个角色都可以是带专用提示词的 LLM 智能体。</p><h3 id="失败模式审计" tabindex="-1"><a class="header-anchor" href="#失败模式审计"><span>失败模式审计</span></a></h3><p>2026 年的最佳实践是每年(或每个大版本)做一次失败模式审计:</p><ol><li><strong>采样轨迹。</strong> 收集约 1000 条真实执行轨迹。</li><li><strong>分类。</strong> 把每条轨迹的失败映射到 MAST + 团体迷思类别。</li><li><strong>计算各类失败率。</strong> 你的系统里哪些类别占主导?</li><li><strong>给缓解措施排序。</strong> 哪个修复能消掉最多失败?</li><li><strong>挑 2-3 个缓解措施。</strong> 实施;下季度重新审计。</li></ol><p>纪律本身比具体选择更重要。不做审计,失败就会混进噪声,永远得不到系统性解决。</p><h3 id="无声失败的系统" tabindex="-1"><a class="header-anchor" href="#无声失败的系统"><span>无声失败的系统</span></a></h3><p>最危险的失败类别是无声的正确性失败。大声失败的系统(崩溃、异常、告警)可以被监控;产出&quot;貌似合理但错误&quot;输出的系统,靠异常日志发现不了。这就是为什么验证缺口虽然只占 21.30%,却是单次失败代价最高的类别。</p><p>投入方向:</p><ul><li>基于采样的人工评审。</li><li>黄金数据集回归测试。</li><li>重要输出上的跨智能体交叉核对。</li></ul><h3 id="失败-vs-缓慢失败" tabindex="-1"><a class="header-anchor" href="#失败-vs-缓慢失败"><span>失败 vs 缓慢失败</span></a></h3><p>有些失败是即时的;有些是缓慢的。即时失败(超时、schema 不匹配、认证错误)检测便宜。缓慢失败(记忆投毒、单一文化漂移、角色含糊)检测和预防都昂贵。</p><p>2026 年的工程动作:给缓慢失败找代理指标并插桩,这样你能在漂移变成可见错误之前抓住它。一致率、重试率、输出长度分布、相邻版本智能体输出的编辑距离,都是有用的代理指标。</p><div class="language-figure line-numbers-mode" data-highlighter="shiki" data-ext="figure" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-figure"><span class="line"><span>a5-retry-cascade</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="动手构建" tabindex="-1"><a class="header-anchor" href="#动手构建"><span>动手构建</span></a></h2><p><code>code/main.py</code> 实现了:</p><ul><li><code>FailureTaxonomy</code> —— 把模拟事故分类到 MAST + 团体迷思类别。</li><li><code>CircuitBreaker</code> —— 经典模式;错误率超阈值时断开。</li><li><code>RetryStormSimulator</code> —— 展示级联失败;可开关熔断器。</li><li><code>DetectionAgent</code> —— 脚本化的 STRATUS 式症状匹配器。</li></ul><p>运行:</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-"><span class="line"><span>python3 code/main.py</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>预期输出:</p><ul><li>无熔断器的重试风暴:库存错误爆炸(模拟)。</li><li>有熔断器:在阈值处封顶;以降级模式响应。</li><li>检测智能体标记该模式并说出 MAST 类别名。</li></ul><h2 id="投入使用" tabindex="-1"><a class="header-anchor" href="#投入使用"><span>投入使用</span></a></h2><p><code>outputs/skill-mast-auditor.md</code> 对一个多智能体系统跑 MAST 式失败模式审计。轨迹 → 分类 → 缓解排序。</p><h2 id="交付" tabindex="-1"><a class="header-anchor" href="#交付"><span>交付</span></a></h2><p>生产中的失败模式纪律:</p><ul><li><strong>每季度一次 MAST 审计。</strong> 不是每年。类别会随系统成长而变化。</li><li><strong>熔断器无处不在。</strong> 每个对依赖服务的出站调用。默认断开阈值设在 5-10% 错误率。</li><li><strong>黄金数据集。</strong> 小、精、人工审计过。每周跑一次回归。</li><li><strong>STRATUS 三件套。</strong> 检测 + 诊断 + 验证智能体监控生产。先只上检测智能体;症状噪声大时再加诊断。</li><li><strong>失败预算。</strong> 按类别给失败率定显式 SLO。超预算就触发一次&quot;停止发版&quot;对话。</li></ul><h2 id="练习" tabindex="-1"><a class="header-anchor" href="#练习"><span>练习</span></a></h2><ol><li>运行 <code>code/main.py</code>。确认熔断器能压住重试风暴。调整失败阈值,观察权衡。</li><li>实现一个<strong>缓慢失败代理指标</strong>:3 个并行智能体之间的一致率。当它骤降时触发告警。通过逐渐让智能体输出相关化来模拟单一文化漂移。</li><li>阅读 Cemri et al.(arXiv:2503.13657)。挑他们 7 个 MAS 系统中的一个,映射它的前三大失败类别。与 MAST 的预测相比如何?</li><li>阅读团体迷思论文(arXiv:2508.05687)。五种模式中哪种在生产里最难检测?提出一个代理指标。</li><li>为一个你熟悉的具体多智能体系统设计 STRATUS 式&quot;检测-诊断-验证&quot;三件套。检测盯哪些症状?诊断会推荐什么缓解?验证如何确认它们有效?</li></ol><h2 id="关键术语" tabindex="-1"><a class="header-anchor" href="#关键术语"><span>关键术语</span></a></h2><table><thead><tr><th>术语</th><th>大家嘴里的说法</th><th>实际含义</th></tr></thead><tbody><tr><td>MAST</td><td>&quot;2026 年的分类法&quot;</td><td>Cemri 2025;3 个根因类别 + 14 个子类型。</td></tr><tr><td>规约问题</td><td>&quot;角色含糊&quot;</td><td>任务或角色定义不足;智能体不知道干什么。</td></tr><tr><td>协调失败</td><td>&quot;状态漂移&quot;</td><td>智能体间的通信或同步崩溃。</td></tr><tr><td>验证缺口</td><td>&quot;没人检查&quot;</td><td>输出未经独立验证就被接受。</td></tr><tr><td>团体迷思家族</td><td>&quot;同质化失败&quot;</td><td>单一文化、从众、ToM 不足、混合动机、级联。</td></tr><tr><td>单一文化崩塌</td><td>&quot;同一模型,同一幻觉&quot;</td><td>共享基座模型或训练数据导致的相关错误。</td></tr><tr><td>重试风暴</td><td>&quot;级联错误放大&quot;</td><td>一次失败触发重试,重试放大下游负载。</td></tr><tr><td>熔断器</td><td>&quot;错误率高就快速失败&quot;</td><td>错误率超阈值时断开;用默认值短路。</td></tr><tr><td>STRATUS</td><td>&quot;事故响应三件套&quot;</td><td>检测 + 诊断 + 验证智能体。缓解成功率提升 1.5 倍。</td></tr><tr><td>记忆投毒</td><td>&quot;幻觉扩散&quot;</td><td>共享记忆里的事实被污染;下游智能体基于毒药推理。</td></tr></tbody></table><h2 id="延伸阅读" tabindex="-1"><a class="header-anchor" href="#延伸阅读"><span>延伸阅读</span></a></h2><ul><li><a href="https://arxiv.org/abs/2503.13657" target="_blank" rel="noopener noreferrer">Cemri et al. —— Why Do Multi-Agent LLM Systems Fail?</a> —— MAST 分类法,NeurIPS 2025</li><li><a href="https://arxiv.org/abs/2508.05687" target="_blank" rel="noopener noreferrer">多智能体 LLM 中的团体迷思失败</a> —— 单一文化、从众与五家族分类法</li><li><a href="https://neurips.cc/" target="_blank" rel="noopener noreferrer">STRATUS —— 面向 MAS 事故响应的专用智能体</a> —— NeurIPS 2025 会议论文(检测 + 诊断 + 验证)</li><li><a href="https://pragprog.com/titles/mnee2/release-it-second-edition/" target="_blank" rel="noopener noreferrer">Release It! —— 稳定性模式(Nygard)</a> —— 熔断器的权威参考</li><li><a href="https://www.anthropic.com/engineering/multi-agent-research-system" target="_blank" rel="noopener noreferrer">Anthropic —— Multi-agent research system</a> —— 生产失败模式笔记</li></ul>`,72)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};