import{O as e,d as t,p as n}from"./runtime-core.esm-bundler-jC72uHyJ.js";import{t as r}from"./app--CEjSbyQ.js";var i=JSON.parse(`{"path":"/ai-eng/13-tools-and-protocols/06-mcp-fundamentals.html","title":"MCP 基础 —— 原语、生命周期、JSON-RPC 底座","lang":"zh-CN","frontmatter":{},"git":{"createdTime":1787385595000,"updatedTime":1787488693000,"contributors":[{"name":"jackie.liu","username":"","email":"moklgy@foxmail.com","commits":3}]},"readingTime":{"minutes":7.47,"words":2241},"filePathRelative":"ai-eng/13-tools-and-protocols/06-mcp-fundamentals.md"}`),a={name:`06-mcp-fundamentals.md`};function o(r,i,a,o,s,c){return e(),t(`div`,null,[...i[0]||=[n(`<h1 id="mcp-基础-——-原语、生命周期、json-rpc-底座" tabindex="-1"><a class="header-anchor" href="#mcp-基础-——-原语、生命周期、json-rpc-底座"><span>MCP 基础 —— 原语、生命周期、JSON-RPC 底座</span></a></h1><blockquote><p>在 MCP 之前，每个集成都是一次性的手工活。Model Context Protocol 由 Anthropic 在 2024 年 11 月首发，现在由 Linux 基金会旗下的 Agentic AI Foundation 托管，它把发现与调用标准化，让任何客户端都能和任何服务器对话。2025-11-25 版规范定义了六个原语（服务端三个、客户端三个）、三阶段生命周期和 JSON-RPC 2.0 线上格式。学会这些，本阶段 MCP 章节的其余部分就只是阅读了。</p></blockquote><p><strong>类型：</strong> Learn<br><strong>编程语言：</strong> Python（标准库，JSON-RPC 解析器）<br><strong>前置要求：</strong> 第 13 阶段 · 01 至 05（工具接口与函数调用）<br><strong>预计耗时：</strong> 约 45 分钟</p><h2 id="学习目标" tabindex="-1"><a class="header-anchor" href="#学习目标"><span>学习目标</span></a></h2><ul><li>说出全部六个 MCP 原语（服务端的 tools、resources、prompts；客户端的 roots、sampling、elicitation)，各给一个用例</li><li>走完三阶段生命周期（initialize、operation、shutdown)，说清每个阶段谁发什么消息</li><li>解析和生成 JSON-RPC 2.0 的 request、response、notification 信封</li><li>解释 <code>initialize</code> 时的能力协商是什么，没有它会坏掉什么</li></ul><h2 id="问题" tabindex="-1"><a class="header-anchor" href="#问题"><span>问题</span></a></h2><p>MCP 之前，每个用工具的智能体都有自己的协议。Cursor 有一个形似 MCP 但不兼容的工具系统；Claude Desktop 自带另一个；VS Code 的 Copilot 扩展是第三个。一个团队做&quot;Postgres 查询&quot;工具，同一个工具要写三遍，分别对接三个宿主的 API。想复用？复制代码。</p><p>结果是寒武纪大爆发式的一次性集成，生态速度撞上了天花板。</p><p>MCP 通过标准化线上格式解决这个问题：一个 MCP 服务器能在每一个 MCP 客户端里工作——Claude Desktop、ChatGPT、Cursor、VS Code、Gemini、Goose、Zed、Windsurf，到 2026 年 4 月已有 300+ 客户端；SDK 月下载量 1.1 亿；公开服务器超过 1 万个。Linux 基金会于 2025 年 12 月在新成立的 Agentic AI Foundation 下接管了托管。</p><p>本阶段使用的规范版本是 <strong>2025-11-25</strong>。它新增了异步 Tasks(SEP-1686)、URL 模式 elicitation(SEP-1036)、带工具的 sampling(SEP-1577)、增量 scope 同意（SEP-835）和 OAuth 2.1 resource-indicator 语义。第 13 阶段 · 09 至 16 会讲这些扩展。本课只打地基。</p><h2 id="概念" tabindex="-1"><a class="header-anchor" href="#概念"><span>概念</span></a></h2><h3 id="三个服务端原语" tabindex="-1"><a class="header-anchor" href="#三个服务端原语"><span>三个服务端原语</span></a></h3><ol><li><strong>Tools（工具）。</strong> 可调用的动作。就是 第 13 阶段 · 01 那个四步循环。</li><li><strong>Resources（资源）。</strong> 暴露的数据。只读内容，用 URI 寻址：<code>file:///path</code>、<code>db://query/...</code> 或自定义 scheme。</li><li><strong>Prompts（提示词）。</strong> 可复用的模板。宿主 UI 里的斜杠命令：服务器供模板，客户端填参数。</li></ol><h3 id="三个客户端原语" tabindex="-1"><a class="header-anchor" href="#三个客户端原语"><span>三个客户端原语</span></a></h3><ol start="4"><li><strong>Roots（根范围）。</strong> 服务器被允许触碰的 URI 集合。客户端声明，服务器遵守。</li><li><strong>Sampling（采样）。</strong> 服务器请求客户端的模型执行一次补全。让服务器端能跑智能体循环，而不需要服务器自己持 API key。</li><li><strong>Elicitation（征询）。</strong> 服务器在流程中途向客户端的用户请求结构化输入。表单或 URL(SEP-1036)。</li></ol><p>MCP 里每一项能力都恰好属于这六个之一。第 13 阶段 · 10 至 14 逐个深入。</p><h3 id="线上格式-json-rpc-2-0" tabindex="-1"><a class="header-anchor" href="#线上格式-json-rpc-2-0"><span>线上格式：JSON-RPC 2.0</span></a></h3><p>每条消息都是一个 JSON 对象，字段如下：</p><ul><li>请求：<code>{jsonrpc: &quot;2.0&quot;, id, method, params}</code>。</li><li>响应：<code>{jsonrpc: &quot;2.0&quot;, id, result | error}</code>。</li><li>通知：<code>{jsonrpc: &quot;2.0&quot;, method, params}</code> —— 没有 <code>id</code>，不期待响应。</li></ul><p>基础规范约 15 个方法，按原语分组。重要的几个：</p><ul><li><code>initialize</code> / <code>initialized</code>（握手）</li><li><code>tools/list</code>、<code>tools/call</code></li><li><code>resources/list</code>、<code>resources/read</code>、<code>resources/subscribe</code></li><li><code>prompts/list</code>、<code>prompts/get</code></li><li><code>sampling/createMessage</code>（服务器 → 客户端）</li><li><code>notifications/tools/list_changed</code>、<code>notifications/resources/updated</code>、<code>notifications/progress</code></li></ul><h3 id="三阶段生命周期" tabindex="-1"><a class="header-anchor" href="#三阶段生命周期"><span>三阶段生命周期</span></a></h3><p><strong>第 1 阶段：initialize。</strong></p><p>客户端发送 <code>initialize</code>，带自己的 <code>capabilities</code> 和 <code>clientInfo</code>。服务器响应自己的 <code>capabilities</code>、<code>serverInfo</code> 和它讲的规范版本。客户端消化完响应后，发送 <code>notifications/initialized</code>。从这里开始，双方按协商好的能力互发请求。</p><p><strong>第 2 阶段：operation。</strong></p><p>双向进行。客户端调 <code>tools/list</code> 发现，然后 <code>tools/call</code> 调用。声明了该能力的服务器可以发 <code>sampling/createMessage</code>。工具集变化时，服务器可以发 <code>notifications/tools/list_changed</code>。用户改了根范围时，客户端可以发 <code>notifications/roots/list_changed</code>。</p><p><strong>第 3 阶段：shutdown。</strong></p><p>任一方关闭传输层。MCP 没有结构化的 shutdown 方法；传输层（stdio 或 Streamable HTTP，见 第 13 阶段 · 09）承载连接结束信号。</p><h3 id="能力协商" tabindex="-1"><a class="header-anchor" href="#能力协商"><span>能力协商</span></a></h3><p><code>initialize</code> 握手里的 <code>capabilities</code> 就是契约。服务器示例：</p><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="color:#ABB2BF;">{</span></span>
<span class="line"><span style="color:#E06C75;">  &quot;tools&quot;</span><span style="color:#ABB2BF;">: {</span><span style="color:#E06C75;">&quot;listChanged&quot;</span><span style="color:#ABB2BF;">: </span><span style="color:#D19A66;">true</span><span style="color:#ABB2BF;">},</span></span>
<span class="line"><span style="color:#E06C75;">  &quot;resources&quot;</span><span style="color:#ABB2BF;">: {</span><span style="color:#E06C75;">&quot;subscribe&quot;</span><span style="color:#ABB2BF;">: </span><span style="color:#D19A66;">true</span><span style="color:#ABB2BF;">, </span><span style="color:#E06C75;">&quot;listChanged&quot;</span><span style="color:#ABB2BF;">: </span><span style="color:#D19A66;">true</span><span style="color:#ABB2BF;">},</span></span>
<span class="line"><span style="color:#E06C75;">  &quot;prompts&quot;</span><span style="color:#ABB2BF;">: {</span><span style="color:#E06C75;">&quot;listChanged&quot;</span><span style="color:#ABB2BF;">: </span><span style="color:#D19A66;">true</span><span style="color:#ABB2BF;">}</span></span>
<span class="line"><span style="color:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>服务器声明：它能发 <code>tools/list_changed</code> 通知，支持 <code>resources/subscribe</code>。客户端也声明自己的，作为应答：</p><div class="language-json line-numbers-mode" data-highlighter="shiki" data-ext="json" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-json"><span class="line"><span style="color:#ABB2BF;">{</span></span>
<span class="line"><span style="color:#E06C75;">  &quot;roots&quot;</span><span style="color:#ABB2BF;">: {</span><span style="color:#E06C75;">&quot;listChanged&quot;</span><span style="color:#ABB2BF;">: </span><span style="color:#D19A66;">true</span><span style="color:#ABB2BF;">},</span></span>
<span class="line"><span style="color:#E06C75;">  &quot;sampling&quot;</span><span style="color:#ABB2BF;">: {},</span></span>
<span class="line"><span style="color:#E06C75;">  &quot;elicitation&quot;</span><span style="color:#ABB2BF;">: {}</span></span>
<span class="line"><span style="color:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>客户端没声明 <code>sampling</code>，服务器就不得调用 <code>sampling/createMessage</code>。对称地：服务器没声明 <code>resources.subscribe</code>，客户端就不得尝试订阅。</p><p>这就是防止生态漂移的机制。不支持 sampling 的客户端仍是合法的 MCP 客户端；不调 <code>sampling</code> 的服务器仍是合法的 MCP 服务器。它们只是不一起用那个特性。</p><h3 id="结构化内容与错误形状" tabindex="-1"><a class="header-anchor" href="#结构化内容与错误形状"><span>结构化内容与错误形状</span></a></h3><p><code>tools/call</code> 返回一个类型化块的 <code>content</code> 数组：<code>text</code>、<code>image</code>、<code>resource</code>。第 13 阶段 · 14 会给这个列表加上 MCP Apps(<code>ui://</code> 交互界面）。</p><p>错误用 JSON-RPC 错误码。规范新增的：<code>-32002</code> &quot;Resource not found&quot;、<code>-32603</code> &quot;Internal error&quot;，外加放在 <code>error.data</code> 里的 MCP 专属错误数据。</p><h3 id="客户端能力-vs-工具调用细节" tabindex="-1"><a class="header-anchor" href="#客户端能力-vs-工具调用细节"><span>客户端能力 vs 工具调用细节</span></a></h3><p>一个常见混淆：<code>capabilities.tools</code> 表示客户端是否支持工具列表变更通知。客户端<em>会不会</em>调用某个具体工具，是它的模型在运行时的选择，不是能力标记。能力标记是规范层的契约，模型的选择是另一回事。</p><h3 id="为什么用-json-rpc-而不是-rest" tabindex="-1"><a class="header-anchor" href="#为什么用-json-rpc-而不是-rest"><span>为什么用 JSON-RPC 而不是 REST?</span></a></h3><p>JSON-RPC 2.0(2010）是轻量级的双向协议。REST 只能客户端发起。MCP 需要服务器发起的消息（sampling、通知），所以请求/响应形状对称的 JSON-RPC 是天作之合。JSON-RPC 在 stdio 和 WebSocket/Streamable HTTP 上也组合得很干净，不用重新发明 HTTP 的请求形状。</p><div class="language-figure line-numbers-mode" data-highlighter="shiki" data-ext="figure" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-figure"><span class="line"><span>mcp-tool-call</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="投入使用" tabindex="-1"><a class="header-anchor" href="#投入使用"><span>投入使用</span></a></h2><p><code>code/main.py</code> 交付一个最小的 JSON-RPC 2.0 解析器与生成器，然后手动走一遍 <code>initialize</code> → <code>tools/list</code> → <code>tools/call</code> → <code>shutdown</code> 序列，打印每一条消息。没有真实传输层，只看消息形状。对照延伸阅读里的规范链接，逐个信封验证。</p><p>要看的地方：</p><ul><li><code>initialize</code> 双向声明能力；响应里有 <code>serverInfo</code> 和 <code>protocolVersion: &quot;2025-11-25&quot;</code>。</li><li><code>tools/list</code> 返回 <code>tools</code> 数组；每项有 <code>name</code>、<code>description</code>、<code>inputSchema</code>。</li><li><code>tools/call</code> 用 <code>params.name</code> 和 <code>params.arguments</code>。</li><li>响应的 <code>content</code> 是 <code>{type, text}</code> 块的数组。</li></ul><h2 id="交付" tabindex="-1"><a class="header-anchor" href="#交付"><span>交付</span></a></h2><p>本课产出 <code>outputs/skill-mcp-handshake-tracer.md</code>。给它一份 pcap 风格的 MCP 客户端-服务器交互记录，这个 skill 给每条消息标注：属于哪个原语、哪个生命周期阶段、依赖哪项能力。</p><h2 id="练习" tabindex="-1"><a class="header-anchor" href="#练习"><span>练习</span></a></h2><ol><li><p>跑 <code>code/main.py</code>。找出能力协商发生的那一行，描述如果服务器没声明 <code>tools.listChanged</code> 会有什么变化。</p></li><li><p>扩展解析器，处理 <code>notifications/progress</code>。消息形状：<code>{method: &quot;notifications/progress&quot;, params: {progressToken, progress, total}}</code>。在一个长时间运行的 <code>tools/call</code> 期间发出它，确认客户端处理器会显示进度条。</p></li><li><p>把 MCP 2025-11-25 规范从头读到尾——全文约 80 页。找出大多数服务器并不需要的那一个能力标记。（提示：和资源订阅有关。)</p></li><li><p>在纸上勾画一个假想的&quot;定时任务&quot;特性该归到哪个原语。（提示：服务器希望客户端在指定时间调用它。今天六个原语都不合适。)MCP 的 2026 路线图里有一份这方面的 SEP 草案。</p></li><li><p>从 GitHub 上找一个开放的 MCP 服务器，解析它的一份会话日志。数一数 request、response、notification 各多少条，算出生命周期流量和操作流量的占比。</p></li></ol><h2 id="关键术语" tabindex="-1"><a class="header-anchor" href="#关键术语"><span>关键术语</span></a></h2><table><thead><tr><th>术语</th><th>大家嘴里的说法</th><th>实际含义</th></tr></thead><tbody><tr><td>MCP</td><td>&quot;Model Context Protocol&quot;</td><td>模型与工具之间发现与调用的开放协议</td></tr><tr><td>服务端原语</td><td>&quot;服务器暴露什么&quot;</td><td>tools（动作）、resources（数据）、prompts（模板）</td></tr><tr><td>客户端原语</td><td>&quot;客户端让服务器用什么&quot;</td><td>roots（范围）、sampling(LLM 回调）、elicitation（用户输入）</td></tr><tr><td>JSON-RPC 2.0</td><td>&quot;线上格式&quot;</td><td>对称的 request/response/notification 信封</td></tr><tr><td><code>initialize</code> 握手</td><td>&quot;能力协商&quot;</td><td>第一对消息；服务器与客户端声明各自支持的特性</td></tr><tr><td><code>tools/list</code></td><td>&quot;发现&quot;</td><td>客户端向服务器询问当前工具集</td></tr><tr><td><code>tools/call</code></td><td>&quot;调用&quot;</td><td>客户端让服务器带参数执行一个工具</td></tr><tr><td><code>notifications/*_changed</code></td><td>&quot;变更事件&quot;</td><td>服务器告知客户端它的原语清单变了</td></tr><tr><td>内容块</td><td>&quot;类型化结果&quot;</td><td>工具结果里的 <code>{type: &quot;text&quot; | &quot;image&quot; | &quot;resource&quot; | &quot;ui_resource&quot;}</code></td></tr><tr><td>SEP</td><td>&quot;规范演进提案&quot;</td><td>有编号的草案提案（如异步 Tasks 的 SEP-1686)</td></tr></tbody></table><h2 id="延伸阅读" tabindex="-1"><a class="header-anchor" href="#延伸阅读"><span>延伸阅读</span></a></h2><ul><li><a href="https://modelcontextprotocol.io/specification/2025-11-25" target="_blank" rel="noopener noreferrer">Model Context Protocol —— 规范 2025-11-25</a> —— 权威规范文档</li><li><a href="https://modelcontextprotocol.io/docs/concepts/architecture" target="_blank" rel="noopener noreferrer">Model Context Protocol —— 架构概念</a> —— 六原语心智模型</li><li><a href="https://www.anthropic.com/news/model-context-protocol" target="_blank" rel="noopener noreferrer">Anthropic —— Introducing the Model Context Protocol</a> —— 2024 年 11 月发布文章</li><li><a href="https://blog.modelcontextprotocol.io/posts/2025-11-25-first-mcp-anniversary/" target="_blank" rel="noopener noreferrer">MCP 博客 —— MCP 一周年</a> —— 一年回顾与 2025-11-25 规范变更</li><li><a href="https://workos.com/blog/mcp-2025-11-25-spec-update" target="_blank" rel="noopener noreferrer">WorkOS —— MCP 2025-11-25 规范更新</a> —— SEP-1686、1036、1577、835、1724 综述</li></ul>`,55)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};