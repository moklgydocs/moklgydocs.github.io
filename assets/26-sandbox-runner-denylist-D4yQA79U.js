import{M as e,O as t,d as n,h as r,p as i}from"./runtime-core.esm-bundler-jC72uHyJ.js";import{t as a}from"./app-0lC-SUUF.js";var o=JSON.parse(`{"path":"/ai-eng/19-capstone-projects/26-sandbox-runner-denylist.html","title":"结课项目第 26 课:带拒绝名单与路径牢笼的沙箱运行器","lang":"zh-CN","frontmatter":{},"git":{"createdTime":1787385595000,"updatedTime":1787488693000,"contributors":[{"name":"jackie.liu","username":"","email":"moklgy@foxmail.com","commits":3}]},"readingTime":{"minutes":5.44,"words":1633},"filePathRelative":"ai-eng/19-capstone-projects/26-sandbox-runner-denylist.md"}`),s={name:`26-sandbox-runner-denylist.md`};function c(a,o,s,c,l,u){let d=e(`Mermaid`);return t(),n(`div`,null,[o[0]||=i(`<h1 id="结课项目第-26-课-带拒绝名单与路径牢笼的沙箱运行器" tabindex="-1"><a class="header-anchor" href="#结课项目第-26-课-带拒绝名单与路径牢笼的沙箱运行器"><span>结课项目第 26 课:带拒绝名单与路径牢笼的沙箱运行器</span></a></h1><blockquote><p>校验门决定一次工具调用该不该跑,沙箱决定它跑起来之后会发生什么。本课交付一个子进程运行器:拒绝危险的可执行文件、拒绝危险的 argv 形态、把每个文件路径都关进项目根目录、截断超大输出,并在墙钟超时后杀掉失控进程。它是夹在模型与操作系统之间的两层防护中的第二层。</p></blockquote><p><strong>类型:</strong> 动手构建<br><strong>编程语言:</strong> Python (stdlib)<br><strong>前置要求:</strong> 第 19 阶段 · 25(校验门与观察预算),第 14 阶段 · 33(指令即约束),第 14 阶段 · 38(校验门)<br><strong>预计耗时:</strong> 约 90 分钟</p><h2 id="学习目标" tabindex="-1"><a class="header-anchor" href="#学习目标"><span>学习目标</span></a></h2><ul><li>构建一个包装 <code>subprocess.run</code> 的 <code>Sandbox</code> 类,带超时、输出捕获和截断。</li><li>按名字用拒绝名单拒绝命令,按结构用 argv 检查器拒绝命令。</li><li>拒绝任何解析后落在声明的项目根目录之外的路径参数。</li><li>在非 shell 模式下拒绝 shell 元字符。</li><li>返回结构化的 <code>SandboxResult</code>,供下游可观测性系统和评测框架消费。</li></ul><h2 id="问题" tabindex="-1"><a class="header-anchor" href="#问题"><span>问题</span></a></h2><p>一个能开 shell 的编程智能体,一轮之内就能装后门、外泄密钥、变砖开发者的笔记本、把云账单刷爆。成本最低的防御是根本不给它 shell,次低的是一个能对一份精确模式清单说&quot;不&quot;的沙箱。</p><p>智能体轨迹里反复出现三类失败。</p><p>第一类是危险可执行文件。一个被逼着修路径问题的模型会尝试 <code>sudo</code>、<code>chmod -R 777</code>、<code>rm -rf</code>、<code>mkfs</code>、<code>dd</code>。这些都不该出现在智能体的运行里。拒绝名单按名字和别名把它们拦下。</p><p>第二类是 argv 花招。被告知不许用 shell 的模型会把攻击借道解释器管道送出去:<code>python3 -c &quot;import os; os.system(&#39;rm -rf /&#39;)&quot;</code>、<code>bash -c &#39;...&#39;</code>、<code>node -e &#39;...&#39;</code>、<code>perl -e &#39;...&#39;</code>。沙箱必须明白:任何带 <code>-c</code> 类标志运行的解释器,都只是多绕了几步的 shell 调用。</p><p>第三类是路径逃逸。模型被要求读 <code>./src/main.py</code>,结果读的是 <code>../../etc/passwd</code>。沙箱把每个路径参数都关进牢笼:用 <code>os.path.realpath</code> 解析,再断言前缀。</p><p>沙箱不是操作系统意义上的安全边界。铁了心的攻击者拿到代码执行能力后仍然能逃出去。沙箱是开发期的护栏:它让常见失败模式变得响亮,拦住智能体因为纯粹的无能而造成破坏。</p><h2 id="概念" tabindex="-1"><a class="header-anchor" href="#概念"><span>概念</span></a></h2>`,13),r(d,{code:``}),o[1]||=i(`<p>沙箱有四条拒绝轴:名字、argv、路径、结构。每条轴都是调用的纯函数,此时还没起子进程。所有轴都通过之后,子进程才会启动。</p><p><code>SandboxResult</code> 的退出码是约定俗成的那套:0 成功,非零失败,外加三个哨兵码——denied(-100)、timed_out(-101),以及 truncated(退出码是真实退出码,但会置一个标志位)。后续课程读这个结构化结果,而不是去解析 stderr。</p><div class="language-figure line-numbers-mode" data-highlighter="shiki" data-ext="figure" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-figure"><span class="line"><span>cg-path-jail</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="架构" tabindex="-1"><a class="header-anchor" href="#架构"><span>架构</span></a></h2>`,4),r(d,{code:`eJxdjrEOwjAMRHe+wj9QQJXYUCU2BgZUxqqDm5o2KDhR4kCR+vFUaViYfM8++e5u7FuN6AUu9QbgjJ4phOY0EEuGY+d3lVmEZSj3RXlooSiqWaExM9yQ+85OTZ7J2xN/jA6SwKGM8EBtEqEfXqA5OFLrWfSTbMzaR1Yo2nK7VMkfUxZNpGa4WkfchNg5b9XSZ5sWf16oKUQjv0IrtZsvdPpSqg==`}),o[2]||=i(`<p>拒绝名单是一个可执行文件 basename 的 frozenset。别名(<code>/bin/rm</code>、<code>/usr/bin/rm</code>)都解析到同一个 basename。argv 检查器认得解释器形态:argv[0] 是解释器、且后面任何参数以 <code>-c</code> 或 <code>-e</code> 开头的 argv 一律拒绝。当调用没有显式请求 shell 时,shell 元字符(<code>;</code>、<code>|</code>、<code>&amp;</code>、<code>&gt;</code>、<code>&lt;</code>、反引号、<code>$()</code>)导致拒绝。</p><p>路径牢笼是最微妙的一块。沙箱在构造时接受一个 <code>project_root</code>。任何看起来像路径的参数(含 <code>/</code> 或能匹配到现存文件)都经过 <code>os.path.realpath</code> 规范化,然后与项目根目录的 realpath 比对。解析后的目标不在根目录之下,拒绝。符号链接逃逸(项目根目录里一个指向外部的符号链接)会被挡住,因为检查的是 realpath,不是字面路径。</p><h2 id="你要构建什么" tabindex="-1"><a class="header-anchor" href="#你要构建什么"><span>你要构建什么</span></a></h2><p>实现是 <code>main.py</code> 加一个测试目录。</p><ol><li><code>SandboxResult</code> dataclass:exit_code、stdout、stderr、truncated、timed_out、denied、reason、duration_ms。</li><li><code>SandboxConfig</code> dataclass:project_root、max_output_bytes、timeout_seconds、denylist、interpreter_block。</li><li><code>Sandbox</code> 类:<code>run(argv, *, shell=False, cwd=None)</code> 返回一个 <code>SandboxResult</code>。</li><li>内部拒绝辅助函数:<code>_check_executable_denylist</code>、<code>_check_argv_interpreter</code>、<code>_check_shell_metachars</code>、<code>_check_path_jail</code>。</li><li>输出截断,带清晰的 <code>truncated</code> 标志,并在捕获流里加一行标记。</li><li>文件底部的演示:一串合法调用和对抗性调用,各自带结果展示。</li></ol><p>沙箱默认用 <code>subprocess.run</code> 配 <code>shell=False</code> 和 <code>capture_output=True</code>。墙钟超时用 <code>timeout</code> 参数;撞上 <code>TimeoutExpired</code> 时,沙箱杀掉整个进程组并合成一个 SandboxResult。</p><h2 id="为什么这不是真沙箱" tabindex="-1"><a class="header-anchor" href="#为什么这不是真沙箱"><span>为什么这不是真沙箱</span></a></h2><p>本课的沙箱没有用 namespace、cgroup、seccomp、gVisor、Firecracker,也没用任何内核级隔离。子进程能做的事,沙箱都能做。防护是结构性的:最常见的那批危险调用被拒绝,响亮的拒绝会进可观测性系统,而不是悄无声息地跑掉。</p><p>生产级智能体还要往上叠层:在非特权 Docker 容器里跑、在 microVM 里跑、drop capabilities、把项目根目录只读挂载、给临时目录读写挂载、对内存和 CPU 设 ulimit、把环境变量清洗成一份已知安全的白名单。第 29 课会做其中一部分。操作系统级隔离超出本课范围。</p><h2 id="运行方式" tabindex="-1"><a class="header-anchor" href="#运行方式"><span>运行方式</span></a></h2><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="background-color:#282c34;color:#abb2bf;"><pre class="shiki one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="color:#56B6C2;">cd</span><span style="color:#98C379;"> phases/19-capstone-projects/26-sandbox-runner-denylist</span></span>
<span class="line"><span style="color:#61AFEF;">python3</span><span style="color:#98C379;"> code/main.py</span></span>
<span class="line"><span style="color:#61AFEF;">python3</span><span style="color:#D19A66;"> -m</span><span style="color:#98C379;"> pytest</span><span style="color:#98C379;"> code/tests/</span><span style="color:#D19A66;"> -v</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>演示创建一个临时目录,往里放一个干净的文件,然后跑一连串调用。合法调用成功;被拒绝的调用返回 <code>denied=True</code> 且带理由的 SandboxResult;超时返回 <code>timed_out=True</code>;截断会置 <code>truncated=True</code>。演示打印一张 JSON 结果表,并以零退出码结束。</p><h2 id="如何与-track-a-其余部分组合" tabindex="-1"><a class="header-anchor" href="#如何与-track-a-其余部分组合"><span>如何与 Track A 其余部分组合</span></a></h2><p>第 25 课产出了门链,第 26 课是门链 ALLOW 之后接棒的执行器。第 27 课的评测框架把沙箱结果与每个任务的期望退出码比对。第 28 课在每次 <code>Sandbox.run</code> 调用外围发出一个 <code>gen_ai.tool.execution</code> span。第 29 课的端到端演示让一个真实的编程智能体同时穿过这两层。</p>`,14)])}var l=a(s,[[`render`,c]]);export{o as _pageData,l as default};