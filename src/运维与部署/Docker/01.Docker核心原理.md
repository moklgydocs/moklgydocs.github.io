---
title: Docker 核心原理
date: 2025-04-14
category:
  - Docker
tag:
  - Docker
  - 容器
  - Namespace
  - Cgroup
order: 1
---

# Docker 核心原理

Docker 不是虚拟机。搞清楚这一点，后面的东西才好理解。

---

## 容器 vs 虚拟机

```
虚拟机：
┌─────────────────────────────┐
│         App A    App B      │
│      ┌────────┐ ┌────────┐ │
│      │ Guest  │ │ Guest  │ │
│      │  OS    │ │  OS    │ │
│      └────────┘ └────────┘ │
│      ┌────────────────────┐ │
│      │    Hypervisor      │ │
│      └────────────────────┘ │
│      ┌────────────────────┐ │
│      │     Host OS        │ │
│      └────────────────────┘ │
└─────────────────────────────┘
每个虚拟机有自己的操作系统，重，慢，吃资源

容器：
┌─────────────────────────────┐
│    App A    App B    App C  │
│   ┌──────┐ ┌──────┐ ┌────┐ │
│   │ bins │ │ bins │ │bins│ │
│   │ libs │ │ libs │ │libs│ │
│   └──────┘ └──────┘ └────┘ │
│   ┌────────────────────────┐│
│   │    Docker Engine       ││
│   └────────────────────────┘│
│   ┌────────────────────────┐│
│   │      Host OS           ││
│   └────────────────────────┘│
└─────────────────────────────┘
共享宿主机内核，轻，快，秒启动
```

| 对比项 | 虚拟机 | 容器 |
|--------|--------|------|
| 启动速度 | 分钟级 | 秒级 |
| 体积 | GB 级 | MB 级 |
| 性能损耗 | 5-15% | 接近原生 |
| 隔离级别 | 强（独立内核） | 弱（共享内核） |
| 典型场景 | 多租户、强隔离 | 应用部署、微服务 |

---

## 三大核心技术

Docker 靠 Linux 内核的三个特性实现容器化：

### 1. Namespace（隔离）

让容器觉得自己是个独立的系统：

```
Namespace 类型     隔离了什么
─────────────────  ────────────────
PID Namespace      进程号（容器里 PID 1 是你的应用）
Network Namespace  网络栈（独立 IP、端口）
Mount Namespace    文件系统（独立的目录树）
UTS Namespace      主机名
IPC Namespace      进程间通信
User Namespace     用户 ID
```

```bash
# 看看容器里的进程
docker exec erp-api ps aux
# PID 1 就是你的 dotnet 进程
# 容器里看不到宿主机的其他进程

# 看容器的网络
docker exec erp-api ip addr
# 有独立的 IP 地址，和宿主机不同
```

### 2. Cgroup（资源限制）

限制容器能用多少 CPU、内存：

```bash
# 启动容器时限制资源
docker run -d \
  --memory=512m \           # 最多用 512M 内存
  --cpus=1.5 \              # 最多用 1.5 核 CPU
  --name erp-api \
  erp-api:latest

# 查看容器资源使用
docker stats erp-api
# CONTAINER   CPU %   MEM USAGE / LIMIT     MEM %
# erp-api     2.35%   256MiB / 512MiB       50.00%
```

### 3. UnionFS（联合文件系统）

Docker 镜像是一层一层叠起来的，每一层只读，最上面加一层可写层：

```
┌─────────────────────┐
│  可写层（容器运行时） │  ← 你的应用写的日志、临时文件
├─────────────────────┤
│  应用层              │  ← COPY publish/ /app
├─────────────────────┤
│  .NET Runtime 层     │  ← FROM mcr.microsoft.com/dotnet/aspnet:8.0
├─────────────────────┤
│  Debian 基础层       │  ← 基础操作系统
└─────────────────────┘

好处：
- 多个容器共享基础层，省磁盘
- 拉取镜像时只下载缺少的层，省带宽
- 构建时只重建变化的层，省时间
```

---

## Docker 架构

```
你敲命令 → Docker Client（CLI）
              ↓
         Docker Daemon（dockerd）
              ↓
    ┌─────────┼──────────┐
    ↓         ↓          ↓
  Images  Containers  Networks/Volumes
    ↓
  Registry（镜像仓库：Docker Hub / 私有仓库）
```

核心概念：

| 概念 | 类比 | 说明 |
|------|------|------|
| Image（镜像） | 安装包 | 只读模板，包含运行环境+代码 |
| Container（容器） | 运行中的程序 | 镜像的运行实例 |
| Registry（仓库） | 应用商店 | 存放和分发镜像 |
| Dockerfile | 安装脚本 | 描述如何构建镜像 |

```bash
# 一条命令理解整个流程
docker run -d --name erp-api -p 5000:5000 erp-api:v1.0

# 1. 本地找 erp-api:v1.0 镜像
# 2. 找不到就去 Registry 拉取
# 3. 基于镜像创建容器
# 4. 在容器里启动进程
# 5. 映射端口 宿主机5000 → 容器5000
```

---

## 理解就够了

不需要背这些原理，但要知道：

1. **容器不是虚拟机** —— 共享宿主机内核，所以快、轻，但隔离没有 VM 强
2. **镜像是分层的** —— 所以 Dockerfile 写得好，构建就快
3. **容器是临时的** —— 容器删了数据就没了，要持久化得用数据卷
4. **资源可以限制** —— 别让一个容器把整台机器内存吃光
