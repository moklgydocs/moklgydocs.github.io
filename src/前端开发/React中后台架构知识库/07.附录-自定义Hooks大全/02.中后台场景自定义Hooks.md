---
title: 中后台场景自定义 Hooks：15 个开箱即用的业务 Hook
icon: code
order: 2
category:
  - React
  - 中后台
tag:
  - 自定义 Hook
  - 中后台
  - 可复用逻辑
---

# 中后台场景自定义 Hooks：15 个开箱即用的业务 Hook

> **TL;DR**
> 中后台项目有大量重复的交互逻辑——分页控制、弹窗开关、URL 参数同步、防抖搜索、点击外部关闭、剪贴板操作。把这些逻辑封装成自定义 Hook，一行代码就能在任何页面中复用。本章提供 15 个精心测试、TypeScript 严格类型化的生产级 Hook。

## 1. usePagination — 分页状态管理

```tsx
// src/hooks/use-pagination.ts
import { useState, useCallback, useMemo } from "react";

interface PaginationOptions {
  defaultPage?: number;
  defaultPageSize?: number;
  pageSizeOptions?: number[];
}

interface PaginationState {
  page: number;
  pageSize: number;
  total: number;
}

function usePagination(options: PaginationOptions = {}) {
  const {
    defaultPage = 1,
    defaultPageSize = 20,
    pageSizeOptions = [10, 20, 50, 100],
  } = options;

  const [state, setState] = useState<PaginationState>({
    page: defaultPage,
    pageSize: defaultPageSize,
    total: 0,
  });

  const setPage = useCallback((page: number) => {
    setState((prev) => ({ ...prev, page }));
  }, []);

  const setPageSize = useCallback((pageSize: number) => {
    setState((prev) => ({ ...prev, pageSize, page: 1 })); // 改页大小时回到第一页
  }, []);

  const setTotal = useCallback((total: number) => {
    setState((prev) => ({ ...prev, total }));
  }, []);

  const reset = useCallback(() => {
    setState({ page: defaultPage, pageSize: defaultPageSize, total: 0 });
  }, [defaultPage, defaultPageSize]);

  const pageCount = useMemo(
    () => Math.ceil(state.total / state.pageSize) || 1,
    [state.total, state.pageSize]
  );

  return {
    ...state,
    pageCount,
    pageSizeOptions,
    setPage,
    setPageSize,
    setTotal,
    reset,
  };
}

export { usePagination };
```

## 2. useModal — 弹窗状态机

```tsx
// src/hooks/use-modal.ts
import { useState, useCallback } from "react";

interface ModalState<TData = unknown> {
  open: boolean;
  data: TData | null;
  mode: "create" | "edit" | "view";
}

function useModal<TData = unknown>() {
  const [state, setState] = useState<ModalState<TData>>({
    open: false,
    data: null,
    mode: "create",
  });

  const openCreate = useCallback(() => {
    setState({ open: true, data: null, mode: "create" });
  }, []);

  const openEdit = useCallback((data: TData) => {
    setState({ open: true, data, mode: "edit" });
  }, []);

  const openView = useCallback((data: TData) => {
    setState({ open: true, data, mode: "view" });
  }, []);

  const close = useCallback(() => {
    setState((prev) => ({ ...prev, open: false }));
  }, []);

  return {
    ...state,
    openCreate,
    openEdit,
    openView,
    close,
    isEdit: state.mode === "edit",
    isView: state.mode === "view",
    isCreate: state.mode === "create",
  };
}

export { useModal };

// 使用示例：
// const modal = useModal<Order>();
// <Button onClick={modal.openCreate}>新建</Button>
// <Button onClick={() => modal.openEdit(order)}>编辑</Button>
// <Dialog open={modal.open} onOpenChange={(v) => !v && modal.close()}>
//   {modal.isEdit && <EditForm data={modal.data!} />}
// </Dialog>
```

## 3. useSearchParams — URL 参数双向同步

```tsx
// src/hooks/use-search-params-state.ts
import { useSearchParams } from "react-router-dom";
import { useCallback, useMemo } from "react";

// 将筛选条件同步到 URL，支持浏览器前进后退、分享链接
function useSearchParamsState<T extends Record<string, string>>(
  defaults: T
): [T, (updates: Partial<T>) => void, () => void] {
  const [searchParams, setSearchParams] = useSearchParams();

  const params = useMemo(() => {
    const result = { ...defaults };
    for (const key of Object.keys(defaults)) {
      const value = searchParams.get(key);
      if (value !== null) {
        (result as Record<string, string>)[key] = value;
      }
    }
    return result;
  }, [searchParams, defaults]);

  const setParams = useCallback(
    (updates: Partial<T>) => {
      setSearchParams((prev) => {
        const next = new URLSearchParams(prev);
        for (const [key, value] of Object.entries(updates)) {
          if (value === undefined || value === "" || value === null) {
            next.delete(key);
          } else {
            next.set(key, String(value));
          }
        }
        return next;
      });
    },
    [setSearchParams]
  );

  const resetParams = useCallback(() => {
    setSearchParams({});
  }, [setSearchParams]);

  return [params, setParams, resetParams];
}

export { useSearchParamsState };

// 使用示例：
// const [params, setParams, reset] = useSearchParamsState({
//   keyword: "", status: "", page: "1"
// });
// → URL: /orders?keyword=手机&status=pending&page=2
// → 刷新页面后筛选条件恢复
```

## 4. useDebounce — 防抖值

```tsx
// src/hooks/use-debounce.ts
import { useState, useEffect } from "react";

function useDebounce<T>(value: T, delay: number = 300): T {
  const [debouncedValue, setDebouncedValue] = useState(value);

  useEffect(() => {
    const timer = setTimeout(() => setDebouncedValue(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);

  return debouncedValue;
}

export { useDebounce };

// 使用：输入停止 300ms 后才触发搜索请求
// const [keyword, setKeyword] = useState("");
// const debouncedKeyword = useDebounce(keyword, 300);
// const { data } = useOrderList({ keyword: debouncedKeyword });
```

## 5. useClickOutside — 点击外部关闭

```tsx
// src/hooks/use-click-outside.ts
import { useEffect, useRef } from "react";

function useClickOutside<T extends HTMLElement>(
  handler: () => void
): React.RefObject<T | null> {
  const ref = useRef<T>(null);

  useEffect(() => {
    function handleClick(event: MouseEvent) {
      if (ref.current && !ref.current.contains(event.target as Node)) {
        handler();
      }
    }
    document.addEventListener("mousedown", handleClick);
    return () => document.removeEventListener("mousedown", handleClick);
  }, [handler]);

  return ref;
}

export { useClickOutside };

// 使用：
// const dropdownRef = useClickOutside<HTMLDivElement>(() => setOpen(false));
// <div ref={dropdownRef}>...</div>
```

## 6. useMediaQuery — 响应式断点检测

```tsx
// src/hooks/use-media-query.ts
import { useSyncExternalStore } from "react";

function useMediaQuery(query: string): boolean {
  return useSyncExternalStore(
    (callback) => {
      const mql = window.matchMedia(query);
      mql.addEventListener("change", callback);
      return () => mql.removeEventListener("change", callback);
    },
    () => window.matchMedia(query).matches,
    () => false
  );
}

// 便捷封装
function useIsMobile(): boolean {
  return useMediaQuery("(max-width: 768px)");
}

function useIsTablet(): boolean {
  return useMediaQuery("(min-width: 769px) and (max-width: 1024px)");
}

function useIsDesktop(): boolean {
  return useMediaQuery("(min-width: 1025px)");
}

export { useMediaQuery, useIsMobile, useIsTablet, useIsDesktop };
```

## 7. useCopyToClipboard — 剪贴板 Hook

```tsx
// src/hooks/use-copy-to-clipboard.ts
import { useState, useCallback, useRef } from "react";
import { copyToClipboard } from "@/utils/dom";

function useCopyToClipboard(resetDelay: number = 2000) {
  const [copied, setCopied] = useState(false);
  const timerRef = useRef<ReturnType<typeof setTimeout>>();

  const copy = useCallback(
    async (text: string) => {
      const success = await copyToClipboard(text);
      setCopied(success);

      if (timerRef.current) clearTimeout(timerRef.current);
      timerRef.current = setTimeout(() => setCopied(false), resetDelay);

      return success;
    },
    [resetDelay]
  );

  return { copy, copied };
}

export { useCopyToClipboard };

// 使用：
// const { copy, copied } = useCopyToClipboard();
// <Button onClick={() => copy(order.orderNo)}>
//   {copied ? "已复制" : "复制订单号"}
// </Button>
```

## 8. useCountdown — 倒计时

```tsx
// src/hooks/use-countdown.ts
import { useState, useEffect, useCallback, useRef } from "react";

function useCountdown(initialSeconds: number) {
  const [seconds, setSeconds] = useState(0);
  const [isRunning, setIsRunning] = useState(false);
  const intervalRef = useRef<ReturnType<typeof setInterval>>();

  const start = useCallback(() => {
    setSeconds(initialSeconds);
    setIsRunning(true);
  }, [initialSeconds]);

  const stop = useCallback(() => {
    setIsRunning(false);
    if (intervalRef.current) clearInterval(intervalRef.current);
  }, []);

  useEffect(() => {
    if (!isRunning) return;

    intervalRef.current = setInterval(() => {
      setSeconds((prev) => {
        if (prev <= 1) {
          setIsRunning(false);
          clearInterval(intervalRef.current);
          return 0;
        }
        return prev - 1;
      });
    }, 1000);

    return () => clearInterval(intervalRef.current);
  }, [isRunning]);

  return { seconds, isRunning, start, stop };
}

export { useCountdown };

// 使用：发送验证码倒计时
// const { seconds, isRunning, start } = useCountdown(60);
// <Button onClick={start} disabled={isRunning}>
//   {isRunning ? `${seconds}s 后重发` : "发送验证码"}
// </Button>
```

## 9. usePrevious — 记住上一次的值

```tsx
// src/hooks/use-previous.ts
import { useRef, useEffect } from "react";

function usePrevious<T>(value: T): T | undefined {
  const ref = useRef<T>(undefined);

  useEffect(() => {
    ref.current = value;
  }, [value]);

  return ref.current;
}

export { usePrevious };

// 使用：对比新旧值做动画/提示
// const prevTotal = usePrevious(total);
// const isIncreased = prevTotal !== undefined && total > prevTotal;
```

## 10. useIntersectionObserver — 元素可见性检测

```tsx
// src/hooks/use-intersection-observer.ts
import { useEffect, useRef, useState } from "react";

interface UseIntersectionOptions {
  threshold?: number;
  rootMargin?: string;
  once?: boolean; // 只触发一次后取消观察
}

function useIntersectionObserver<T extends HTMLElement>(
  options: UseIntersectionOptions = {}
) {
  const { threshold = 0, rootMargin = "0px", once = false } = options;
  const ref = useRef<T>(null);
  const [isIntersecting, setIsIntersecting] = useState(false);

  useEffect(() => {
    const element = ref.current;
    if (!element) return;

    const observer = new IntersectionObserver(
      ([entry]) => {
        setIsIntersecting(entry.isIntersecting);
        if (entry.isIntersecting && once) {
          observer.unobserve(element);
        }
      },
      { threshold, rootMargin }
    );

    observer.observe(element);
    return () => observer.disconnect();
  }, [threshold, rootMargin, once]);

  return { ref, isIntersecting };
}

export { useIntersectionObserver };

// 使用：懒加载图片 / 无限滚动的"加载更多"触发器
// const { ref, isIntersecting } = useIntersectionObserver({ once: true });
// {isIntersecting ? <img src={realSrc} /> : <Skeleton />}
// <div ref={ref} />
```

## 11. useLocalStorage — 持久化状态

```tsx
// src/hooks/use-local-storage.ts
import { useState, useCallback } from "react";

function useLocalStorage<T>(
  key: string,
  initialValue: T
): [T, (value: T | ((prev: T) => T)) => void, () => void] {
  const [storedValue, setStoredValue] = useState<T>(() => {
    try {
      const item = localStorage.getItem(key);
      return item ? (JSON.parse(item) as T) : initialValue;
    } catch {
      return initialValue;
    }
  });

  const setValue = useCallback(
    (value: T | ((prev: T) => T)) => {
      setStoredValue((prev) => {
        const nextValue = value instanceof Function ? value(prev) : value;
        localStorage.setItem(key, JSON.stringify(nextValue));
        return nextValue;
      });
    },
    [key]
  );

  const removeValue = useCallback(() => {
    localStorage.removeItem(key);
    setStoredValue(initialValue);
  }, [key, initialValue]);

  return [storedValue, setValue, removeValue];
}

export { useLocalStorage };
```

## 12. useToggle — 布尔值切换

```tsx
// src/hooks/use-toggle.ts
import { useState, useCallback } from "react";

function useToggle(initialValue: boolean = false) {
  const [value, setValue] = useState(initialValue);

  const toggle = useCallback(() => setValue((v) => !v), []);
  const setTrue = useCallback(() => setValue(true), []);
  const setFalse = useCallback(() => setValue(false), []);

  return { value, toggle, setTrue, setFalse, setValue };
}

export { useToggle };

// 使用：
// const sidebar = useToggle(false);
// <Button onClick={sidebar.toggle}>
//   {sidebar.value ? "收起" : "展开"}
// </Button>
```

## 13. useInterval — 安全的定时器

```tsx
// src/hooks/use-interval.ts
import { useEffect, useRef } from "react";

function useInterval(callback: () => void, delay: number | null) {
  const savedCallback = useRef(callback);

  useEffect(() => {
    savedCallback.current = callback;
  }, [callback]);

  useEffect(() => {
    if (delay === null) return; // delay 为 null 时暂停
    const id = setInterval(() => savedCallback.current(), delay);
    return () => clearInterval(id);
  }, [delay]);
}

export { useInterval };

// 使用：每 30 秒刷新通知数（传 null 暂停）
// useInterval(() => fetchNotifications(), isActive ? 30000 : null);
```

## 14. useDocumentTitle — 动态页面标题

```tsx
// src/hooks/use-document-title.ts
import { useEffect, useRef } from "react";

function useDocumentTitle(title: string, restoreOnUnmount: boolean = true) {
  const previousTitle = useRef(document.title);

  useEffect(() => {
    document.title = title ? `${title} - GlobalMart` : "GlobalMart";
  }, [title]);

  useEffect(() => {
    if (restoreOnUnmount) {
      const prevTitle = previousTitle.current;
      return () => { document.title = prevTitle; };
    }
  }, [restoreOnUnmount]);
}

export { useDocumentTitle };

// 使用：每个页面组件中
// useDocumentTitle("订单管理");
// → 浏览器标签显示 "订单管理 - GlobalMart"
```

## 15. useLoading — 异步操作 loading 管理

```tsx
// src/hooks/use-loading.ts
import { useState, useCallback } from "react";

function useLoading() {
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const run = useCallback(async <T,>(fn: () => Promise<T>): Promise<T | undefined> => {
    setLoading(true);
    setError(null);
    try {
      const result = await fn();
      return result;
    } catch (err) {
      setError(err instanceof Error ? err : new Error(String(err)));
      return undefined;
    } finally {
      setLoading(false);
    }
  }, []);

  return { loading, error, run };
}

export { useLoading };

// 使用：
// const { loading, error, run } = useLoading();
// const handleExport = () => run(async () => {
//   await orderApi.exportExcel(params);
// });
// <Button onClick={handleExport} disabled={loading}>
//   {loading ? "导出中..." : "导出"}
// </Button>
```

## Hooks 索引速查

| Hook | 用途 | 常见场景 |
|------|------|---------|
| `usePagination` | 分页状态管理 | 列表页 |
| `useModal` | 弹窗状态机 | 新建/编辑/查看弹窗 |
| `useSearchParamsState` | URL 参数同步 | 可分享的筛选链接 |
| `useDebounce` | 防抖 | 搜索输入 |
| `useClickOutside` | 点击外部关闭 | 下拉菜单 |
| `useMediaQuery` | 响应式断点 | 自适应布局 |
| `useCopyToClipboard` | 剪贴板 | 复制订单号/链接 |
| `useCountdown` | 倒计时 | 验证码/限时操作 |
| `usePrevious` | 记住上一次值 | 变化对比/动画 |
| `useIntersectionObserver` | 元素可见性 | 懒加载/无限滚动 |
| `useLocalStorage` | 持久化状态 | 用户偏好 |
| `useToggle` | 布尔切换 | 开关/展开收起 |
| `useInterval` | 安全定时器 | 轮询/自动刷新 |
| `useDocumentTitle` | 页面标题 | 每个路由页面 |
| `useLoading` | 异步 loading | 导出/批量操作 |

---

*Hooks 武器库就位！接下来进入 [中后台常见开发模式](../08.附录-中后台开发模式/01.组件设计模式.md) — 复合组件、KeepAlive、URL 驱动状态等架构级模式。*
