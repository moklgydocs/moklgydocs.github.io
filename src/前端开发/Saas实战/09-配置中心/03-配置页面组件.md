# 03 - 配置页面组件：分组展示与 CRUD 交互

> **这一步解决什么问题？**
> 配置中心的核心是一个 610 行的 `ConfigPage` 组件——它同时处理数据加载、分组展示、搜索过滤、新增/编辑/删除操作。这节我们逐段解析它的完整实现，理解"一个组件如何服务五个后端"。
>
> **ASP.NET Core 开发者的直觉**：这个组件 ≈ 一个完整的 Razor Page（`Index.cshtml` + `Index.cshtml.cs`），数据加载 = `OnGetAsync()`，表单提交 = `OnPostAsync()`，搜索过滤 = LINQ 查询。

---

## 一、组件结构总览

```
ConfigPage 组件内部结构（610 行）：

┌─────────────────────────────────────────────────────────────┐
│  1. 导入（1-27 行）          UI 组件 + 图标 + 工具函数       │
│  2. 类型定义（29-100 行）     ConfigItemDto / UpsertRequest   │
│  3. 分组工具函数（68-100 行） groupConfigs / isDangerous      │
│  4. 组件主体（102-609 行）                                  │
│     ├── 4.1 State 声明（119-133 行）                        │
│     ├── 4.2 HTTP 客户端（135 行）                            │
│     ├── 4.3 数据加载（138-152 行）                           │
│     ├── 4.4 分组+搜索（155-176 行）                          │
│     ├── 4.5 CRUD 操作（178-268 行）                         │
│     └── 4.6 JSX 渲染（270-609 行）                          │
│         ├── PageHeader + 操作按钮                            │
│         ├── 搜索框                                           │
│         ├── 错误/加载/空状态                                  │
│         ├── 分组卡片列表（Collapsible Card）                 │
│         ├── 统计信息                                         │
│         ├── 新增/编辑 Dialog                                │
│         └── 删除确认 Dialog                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、State 设计

```typescript
// ── 数据 ──
const [items, setItems] = useState<ConfigItemDto[]>([])
const [loading, setLoading] = useState(true)
const [error, setError] = useState<string | null>(null)

// ── UI 状态 ──
const [dialogOpen, setDialogOpen] = useState(false)
const [deleteKey, setDeleteKey] = useState<string | null>(null)
const [editItem, setEditItem] = useState<ConfigItemDto | null>(null)
const [form, setForm] = useState<UpsertRequest>(EMPTY_FORM)
const [showValues, setShowValues] = useState(false)
const [saving, setSaving] = useState(false)
const [deleting, setDeleting] = useState(false)
const [reloading, setReloading] = useState(false)
const [searchQuery, setSearchQuery] = useState("")
const [collapsedGroups, setCollapsedGroups] = useState<Set<string>>(new Set())
```

逐行精讲：

| 标记 | State | 解读 |
|------|-------|------|
| **【设计取舍】** | `items` + `loading` + `error` | 三件套！数据列表、加载状态、错误信息分开管理，而不是用 `status: "idle" \| "loading" \| "error"` 联合类型。这和 ASP.NET 的 `Try/Catch` 异常处理思路一致——错误是独立状态，不是数据的一部分 |
| **【易错点】** | `deleteKey: string \| null` | 只存 Key，不存整个对象！删除确认 Dialog 只需要显示 Key，没必要存完整 DTO。减少 State 体积 |
| **【易错点】** | `editItem: ConfigItemDto \| null` | 存的是**原始数据**，不是表单数据！`editItem` 用于判断"是新增还是编辑"（`!!editItem`），表单数据在 `form` 中 |
| **【设计取舍】** | `showValues` 是全局开关 | 不是每条配置单独控制！点击一次显示/隐藏所有加密值。因为加密值通常是少数，逐条切换反而增加操作步骤 |
| **【易错点】** | `collapsedGroups: Set<string>` | 用 `Set` 而不是数组！`Set.has()` 是 O(1)，`Array.includes()` 是 O(n)。虽然配置项不多，但这是好习惯 |
| **【性能陷阱】** | `saving` / `deleting` / `reloading` 三个独立 loading | 如果用 `loadingType: "saving" \| "deleting" \| ...`，一个状态就够了。但分开更直观，且不会互相干扰（比如保存中不会误触发删除的 loading） |

> **🤔 导师提问**：对比 ASP.NET Core Razor Page 的 `PageModel`，`ConfigPage` 把 14 个 State 变量全放在一个组件里。在 Razor Page 中，你会把加载状态、表单状态、UI 开关分别放在什么地方？React 没有类似 `TempData` 或 `ViewData` 的机制——如果 State 越来越多，你会如何重构？

💡 **ASP.NET Core 类比**：
- `items` ≈ `List<ConfigItemDto>`（属性）
- `loading` ≈ `IsLoading`（属性）
- `error` ≈ `Exception`（异常）
- `form` ≈ `[BindProperty] UpsertRequest Form`（模型绑定）

---

## 三、HTTP 客户端创建

```typescript
const http = useMemo(() => createHttp(serviceBaseUrl), [serviceBaseUrl])
```

⚠️ **【易错点】** 为什么用 `useMemo` 而不是 `useRef`？

```typescript
// ❌ useRef 版本：serviceBaseUrl 变化时不会重新创建
const httpRef = useRef(createHttp(serviceBaseUrl))

// ✅ useMemo 版本：serviceBaseUrl 变化时自动重新创建
const http = useMemo(() => createHttp(serviceBaseUrl), [serviceBaseUrl])
```

`useMemo` 的依赖数组确保当路由切换（`serviceBaseUrl` 从 `/perm-api` 变为 `/file-api`）时，HTTP 客户端会重新创建，绑定新的后端地址。`useRef` 不会响应 Props 变化。

💡 **但实际上**，在当前路由设计中，`serviceBaseUrl` 不会在组件生命周期内变化（路由切换 = 组件卸载重建）。`useMemo` 是防御性编程。

---

## 四、数据加载

```typescript
const fetchItems = useCallback(async () => {
  setLoading(true)
  setError(null)
  try {
    const res = await http.get<ConfigItemDto[]>("/api/runtime-config")
    setItems(res.data ?? [])
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : String(err)
    setError(msg)
  } finally {
    setLoading(false)
  }
}, [http])

useEffect(() => { fetchItems() }, [fetchItems])
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【易错点】** | `useCallback(..., [http])` | 依赖 `http`！因为 `fetchItems` 内部调用了 `http.get`。`http` 由 `useMemo` 创建，依赖 `serviceBaseUrl`。整条依赖链：`serviceBaseUrl` → `http` → `fetchItems` → `useEffect` |
| **【易错点】** | `res.data ?? []` | 用 `??` 不用 `||`！`res.data` 可能是空数组 `[]`（falsy 但合法），`||` 会错误地替换为 `[]`。这里效果一样但 `??` 语义更准确 |
| **【易错点】** | `err instanceof Error ? err.message : String(err)` | `catch` 中的 `err` 类型是 `unknown`，不能直接 `.message`。先检查是否是 `Error` 实例，再安全访问 |
| **【设计取舍】** | `setLoading(true)` + `finally { setLoading(false) }` | 无论成功或失败都关闭 loading。如果只在 `try` 末尾关闭，失败时 loading 永远为 true |

### 4.1 为什么不用 React Query？

AdminWeb 的数据获取是手写 `useState + useEffect + useCallback`，而不是用 `@tanstack/react-query`。

| 维度 | 手写 | React Query |
|------|------|-------------|
| 代码量 | 多（每个请求 ~15 行） | 少（`useQuery` 一行） |
| 缓存 | 无（每次 mount 重新请求） | 自动缓存 + 后台刷新 |
| 学习曲线 | 低（原生 React） | 中（需理解 Query Key、Stale Time） |
| 适合场景 | 配置中心（数据变化少、无实时性要求） | 通知中心（需要实时刷新、乐观更新） |

⚠️ **【设计取舍】** 配置中心选择了手写，因为配置项变化频率低（几分钟才改一次），不需要 React Query 的缓存和自动刷新。通知中心等高频场景更适合 React Query。

---

## 五、分组 + 搜索过滤

```typescript
const filteredGroups = useMemo(() => {
  const groups = groupConfigs(items)
  if (!searchQuery.trim()) return groups

  const q = searchQuery.toLowerCase()
  return groups
    .map(g => ({
      ...g,
      items: g.items.filter(item =>
        item.key.toLowerCase().includes(q) ||
        (item.description ?? "").toLowerCase().includes(q) ||
        (item.value ?? "").toLowerCase().includes(q)
      ),
    }))
    .filter(g => g.items.length > 0)
}, [items, searchQuery])
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【设计取舍】** | `useMemo` 依赖 `[items, searchQuery]` | 只有数据或搜索词变化时才重新计算分组。输入搜索词时不触发数据请求，纯前端过滤 |
| **【易错点】** | `q = searchQuery.toLowerCase()` | 大小写不敏感搜索！统一转小写比较。不能用 `===`（精确匹配）——用户不会输入完整的 `Jwt:Secret` |
| **【易错点】** | `(item.description ?? "").toLowerCase()` | `description` 可能为 `null`！`null.toLowerCase()` 会报错，必须用 `??` 提供默认值 |
| **【设计取舍】** | `.filter(g => g.items.length > 0)` | 搜索后如果某分组没有匹配项，整个分组不显示。避免空分组卡片 |

### 5.1 已有分组名列表（用于表单下拉建议）

```typescript
const existingGroupNames = useMemo(
  () => [...new Set(items.map(i => i.groupName).filter(Boolean))] as string[],
  [items]
)
```

这是给新增/编辑 Dialog 的 `<datalist>` 用的——输入分组名时下拉显示已有分组，避免拼写不一致。

---

## 六、渲染结构：三层状态 + 分组卡片

```typescript
return (
  <AnimatedPage>
    <div className="space-y-6">
      {/* 1. PageHeader + 操作按钮 */}
      {/* 2. 搜索框 */}
      {/* 3. 错误提示 */}
      {/* 4. 加载骨架 */}
      {/* 5. 空状态 */}
      {/* 6. 搜索无结果 */}
      {/* 7. 分组卡片列表 */}
      {/* 8. 统计信息 */}
      {/* 9. 新增/编辑 Dialog */}
      {/* 10. 删除确认 Dialog */}
    </div>
  </AnimatedPage>
)
```

三层状态处理：

```
┌──────────────────────────────────────────────────────────────┐
│  loading=true     → 显示骨架屏（3 张 Card，每张 3 行 Skeleton）│
│  loading=false    → 检查数据                                  │
│    items=[]       → 显示空状态（"暂无配置项"）                 │
│    items.length>0 → 检查搜索                                  │
│      filteredGroups=[] → 搜索无结果                           │
│      filteredGroups>0 → 渲染分组卡片                          │
└──────────────────────────────────────────────────────────────┘
```

### 6.1 PageHeader 操作按钮

```typescript
<PageHeader
  title={`${serviceLabel} — 运行时配置`}
  description="配置写入后 5 秒内自动热重载。加密项以 AES-256 存储。按分组管理，每项配置含用途说明和修改影响范围。"
  action={
    <div className="flex items-center gap-2">
      <Button variant="outline" size="sm" onClick={() => setShowValues(v => !v)}>
        {showValues ? <EyeOff className="w-4 h-4 mr-1.5" /> : <Eye className="w-4 h-4 mr-1.5" />}
        {showValues ? "隐藏值" : "显示值"}
      </Button>
      <Button variant="outline" size="sm" onClick={handleReload} disabled={reloading}>
        <RefreshCw className={`w-4 h-4 mr-1.5 ${reloading ? "animate-spin" : ""}`} />
        强制重载
      </Button>
      <Button size="sm" onClick={openCreate}>
        <Plus className="w-4 h-4 mr-1.5" />
        新增配置
      </Button>
    </div>
  }
/>
```

三个操作按钮的设计考量：

| 按钮 | 变体 | 为什么 |
|------|------|--------|
| 显示/隐藏值 | `outline`（描边） | 切换操作，不是主要行动。描边按钮视觉权重低 |
| 强制重载 | `outline` + `animate-spin` | 危险操作（强制刷新 IConfiguration），不是常规操作。加载时图标旋转 |
| 新增配置 | `default`（实心） | 主要行动！实心按钮视觉权重最高，引导用户点击 |

### 6.2 分组卡片渲染

```typescript
{filteredGroups.map(group => {
  const isCollapsed = collapsedGroups.has(group.groupName)
  const encryptedCount = group.items.filter(i => i.isEncrypted).length

  return (
    <Collapsible key={group.groupName} open={!isCollapsed} onOpenChange={() => toggleGroup(group.groupName)}>
      <Card>
        <CollapsibleTrigger asChild>
          <CardHeader className="cursor-pointer select-none py-3 px-4 hover:bg-muted/50 transition-colors">
            <div className="flex items-center justify-between">
              <div className="flex items-center gap-2.5">
                {isCollapsed ? <ChevronRight /> : <ChevronDown />}
                <CardTitle className="text-sm font-semibold">{group.groupName}</CardTitle>
                <Badge variant="secondary">{group.items.length} 项</Badge>
                {encryptedCount > 0 && (
                  <Badge variant="outline" className="text-xs text-warning border-warning/30">
                    <Shield className="w-3 h-3 mr-1" />
                    {encryptedCount} 项加密
                  </Badge>
                )}
              </div>
            </div>
          </CardHeader>
        </CollapsibleTrigger>
        <CollapsibleContent>
          <CardContent className="px-4 pt-0 pb-3">
            {group.items.map((item, idx) => (
              <div key={item.key}>
                {idx > 0 && <Separator className="my-3" />}
                {/* 配置项内容 */}
              </div>
            ))}
          </CardContent>
        </CollapsibleContent>
      </Card>
    </Collapsible>
  )
})}
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【易错点】** | `key={group.groupName}` | 分组名作为 key！前提是 groupName 唯一。如果两个配置项有同名分组但不同 groupSort，会有冲突 |
| **【设计取舍】** | `Collapsible` + `Card` 组合 | 分组可折叠！配置项多时（一个分组 20+ 项），折叠后只看标题和统计，减少信息过载 |
| **【易错点】** | `{idx > 0 && <Separator />}` | 首项前不加分割线！用 index 判断而非"每项都加"，避免首尾多余线条 |
| **【设计取舍】** | `encryptedCount > 0` 才显示加密 Badge | 信息密度控制！如果没有加密项，不显示无意义的"0 项加密" |

> **🤔 导师提问**：分组卡片用了 `Collapsible` + `Card` 的组合，折叠状态存在 `collapsedGroups: Set<string>` 中。在 ASP.NET Core Razor Page 里，折叠状态通常不需要后端保存——页面刷新就恢复默认。但 React 单页应用中，用户切换路由再回来，State 也会重置。如果需要"记住折叠状态"，你会用什么方案？提示：想想 `localStorage` 和 React Context 的区别。

---

## 七、单个配置项渲染

每个配置项的渲染结构：

```
┌──────────────────────────────────────────────────────────────┐
│  左侧：配置信息                                               │
│    ├── Key + 加密标记（Badge "加密"）                         │
│    ├── 配置说明（description）                                 │
│    ├── 当前值（加密项显示 ****** 或明文）                     │
│    ├── 影响范围（含"危险"/"失败"关键词变红）                    │
│    └── 环境 Badge + 更新时间                                  │
│                                                              │
│  右侧：操作按钮                                               │
│    ├── 编辑（Pencil 图标）                                    │
│    └── 删除（Trash2 图标，destructive 色）                    │
└──────────────────────────────────────────────────────────────┘
```

关键渲染逻辑：

```typescript
{/* 当前值 */}
<div className="font-mono text-xs text-foreground/80 bg-muted/50 rounded px-2 py-1 inline-block max-w-full truncate">
  {item.isEncrypted && !showValues ? (
    <span className="text-muted-foreground select-none">******</span>
  ) : (
    <span title={item.value}>{item.value || <span className="text-muted-foreground italic">（空）</span>}</span>
  )}
</div>

{/* 影响范围 */}
{item.impactScope && (
  <div className={`flex items-start gap-1.5 text-xs leading-relaxed ${
    isDangerous(item.impactScope) ? "text-destructive" : "text-warning"
  }`}>
    <AlertTriangle className="w-3.5 h-3.5 mt-0.5 shrink-0" />
    <span>{item.impactScope}</span>
  </div>
)}
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【易错点】** | `item.isEncrypted && !showValues` | 双重条件！加密 AND 未开启显示。两个条件缺一不可——非加密项不受 `showValues` 影响 |
| **【设计取舍】** | `select-none` on `******` | 禁止选中！防止用户复制密文遮盖符。`select-none` 是 Tailwind 的 `user-select: none` |
| **【设计取舍】** | `max-w-full truncate` | 长值截断！数据库连接串可能 200+ 字符，不截断会撑破布局。`title={item.value}` 让鼠标悬停可看完整值 |
| **【易错点】** | `isDangerous(item.impactScope) ? "text-destructive" : "text-warning"` | 两级风险颜色！包含"危险"/"失败"→红色，其他→橙色。不是简单的有/无风险，而是严重/一般 |
| **【易错点】** | `item.value \|\| <span>（空）</span>` | 空值的友好显示！`""` falsy，显示斜体"（空）"。不能用 `??`，因为空字符串也是 falsy |

> **🤔 导师提问**：加密值的显示控制是 `item.isEncrypted && !showValues`——用前端 State 控制遮盖，这意味着明文值已经在浏览器内存中了。在 ASP.NET Core 中，你会如何设计 API 返回值来避免敏感配置明文传到前端？如果后端只返回 `isEncrypted: true` 而不返回 `value`，前端的"显示值"功能还能实现吗？

### 7.1 isDangerous 风险判断

```typescript
function isDangerous(text: string): boolean {
  return /危险|失败|所有.*失败|无需认证|全部失败|切勿/.test(text)
}
```

⚠️ **【易错点】** 这是前端硬编码的关键词匹配，不是后端返回的风险等级！如果后端新增了 `impactScope` 包含"致命"但前端正则没覆盖，会显示橙色而非红色。

💡 **改进方向**：后端可以增加 `riskLevel: "high" | "medium" | "low"` 字段，前端根据等级着色，而非依赖关键词匹配。

> **🤔 导师提问**：`isDangerous` 用正则匹配关键词来判断风险等级——这是纯前端逻辑，后端完全不知道前端做了这个判断。在 ASP.NET Core 中，如果你要给 `impactScope` 增加风险等级，你会选择：A）在 DTO 中加 `riskLevel` 字段（后端决定），还是 B）前端维护映射表？两种方案各有什么维护成本？

---

> **🤔 导师提问**：组件结构总览中 CRUD 操作占 178-268 行（约 90 行），JSX 渲染占 270-609 行（约 340 行）。渲染代码是业务逻辑的近 4 倍。在 ASP.NET Core 中，Razor 模板和 PageModel 代码通常也是这个比例吗？React 的 JSX 把逻辑和渲染混在一个文件里，你觉得这和 Razor 的"代码分离"相比，是优势还是劣势？在什么情况下你会考虑拆分组件？

> **🔍 验证步骤**
>
> 1. 打开配置中心页面，确认分组卡片正常渲染，每个分组显示配置项数量 Badge
> 2. 点击某个加密配置项所在的分组，确认加密值显示为 `******`，且无法选中复制遮盖符
> 3. 点击 PageHeader 的"显示值"按钮，确认所有加密值从 `******` 变为明文显示；再点击"隐藏值"确认恢复遮盖
> 4. 点击某个配置项的编辑按钮，修改配置值后保存，确认列表中对应项的值已更新
> 5. 点击"新增配置"，填写 Key、分组名、值后保存，确认新配置项出现在对应分组卡片中

## 🤔 思考题

### 概念级（理解 Why）
1. 为什么用 `useMemo` 创建 HTTP 客户端而不是 `useRef`？在当前路由设计下，两者有实际区别吗？
2. `showValues` 是全局开关（控制所有加密值），如果改成每条配置单独控制，需要改哪些 State？

### 推理级（推导 What-if）
3. 如果配置项有 500 条（一个大型微服务），当前的 `useMemo` 前端过滤性能够吗？需要改用什么方案？
4. `isDangerous` 函数的正则 `/危险|失败/` 如果匹配到了 `impactScope: "此配置修改不会导致服务失败"`（否定句），会错误地标红。怎么解决？

### 动手级（代码实践）
5. 给配置项增加一个"复制值"按钮（Copy 按钮），点击后把 `item.value` 写入剪贴板，用 `navigator.clipboard.writeText()`。
6. 把 `loading + error + items` 三个 State 改成 `useReducer` 实现，定义 `type Action = { type: "loading" } | { type: "success"; data: ConfigItemDto[] } | { type: "error"; message: string }`。

---

## ✅ 输出检查清单

完成本节后，你应该能回答：

- [ ] 理解 14 个 State 变量的用途和设计原因
- [ ] 知道 `useMemo` 创建 HTTP 客户端的必要性
- [ ] 理解数据加载的 `useState + useCallback + useEffect` 模式
- [ ] 掌握分组 + 搜索过滤的 `useMemo` 计算逻辑
- [ ] 理解三层状态处理（loading → empty → filtered）
- [ ] 能解释 `isDangerous` 的关键词匹配及其局限性

---

[← 上一篇：02-路由与类型定义](02-路由与类型定义.md) | [下一篇：04-加密值交互逻辑 →](04-加密值交互逻辑.md)
