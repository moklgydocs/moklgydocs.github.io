# 04 - 通知铃铛：实时推送的入口

> **这一步解决什么问题？**
> 用户在使用 SaaS 系统时，不可能一直盯着通知列表页。我们需要一个**全局可见的入口**——铃铛图标 + 未读徽标，让用户在任何页面都能感知到新通知，点击即可查看最近的通知并快速操作。
>
> **ASP.NET Core 类比**：铃铛组件 ≈ `_Layout.cshtml` 里的通知 PartialView——每个页面都渲染它，它负责展示未读数和最近通知列表。

---

## 一、铃铛组件的完整架构

```
┌──────────────────────────────────────────────────────────────────┐
│ NotificationBell 组件架构                                         │
│                                                                  │
│  ┌─────────────┐                                                 │
│  │ 🔔 铃铛按钮  │ ← 点击展开/收起下拉                             │
│  │   ③ 未读徽标 │                                                 │
│  └──────┬──────┘                                                 │
│         │ 点击                                                    │
│         ▼                                                        │
│  ┌──────────────────────────────┐                                │
│  │ 下拉面板                      │                                │
│  │ ┌──────────────────────────┐ │                                │
│  │ │ 通知        [全部已读]    │ │                                │
│  │ ├──────────────────────────┤ │                                │
│  │ │ 🔵 渲染完成               │ │ ← 未读通知（蓝色背景）         │
│  │ │    报表已生成  10:30      │ │                                │
│  │ ├──────────────────────────┤ │                                │
│  │ │    订单发货通知            │ │ ← 已读通知（白色背景）         │
│  │ │    您的订单已发  09:15    │ │                                │
│  │ ├──────────────────────────┤ │                                │
│  │ │    查看全部通知 →         │ │ ← 跳转通知列表页               │
│  │ └──────────────────────────┘ │                                │
│  └──────────────────────────────┘                                │
│                                                                  │
│  数据来源：                                                       │
│  ┌─────────────────────────┐  ┌─────────────────────────────┐   │
│  │ 60秒轮询 fetchUnreadCount│  │ SignalR ReceiveNotification  │   │
│  │ (断线兜底)               │  │ (实时推送，优先)             │   │
│  └─────────────────────────┘  └─────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

⚠️ **【组件思维】为什么 notification-bell.tsx 放在 components/ 根目录？**

AdminWeb 的 `src/components/` 目录结构：

```
components/
├── brand/              ← 品牌相关组件（Logo 等）
├── layout/             ← 布局组件（Header、Sidebar）
├── shared/             ← 通用共享组件（Button、Table 封装）
├── tree/               ← 树形组件
├── ui/                 ← shadcn/ui 基础组件
└── notification-bell.tsx  ← 为什么不在 shared/ 里？
```

`notification-bell.tsx` 放在 `components/` 根目录而不是 `components/shared/`，原因是：

1. **它不是"通用组件"** —— `shared/` 里的组件（如 Button、Dialog）是任何页面都能复用的，而铃铛只在顶栏 Header 中使用
2. **它是"应用级入口组件"** —— 它的地位类似于 layout 组件，是应用壳的一部分
3. **它有自己的数据依赖** —— 直接依赖 `notification-store` 和 `jobsApi`，不像 `shared/` 组件只接收 props

> **讲师提问**：如果铃铛组件需要同时显示在顶栏和侧边栏底部（两个位置），它的文件位置应该怎么调整？
>
> <details><summary>参考答案</summary>位置不需要调整，仍然放在 components/ 根目录。因为"在两处使用"不等于"是通用组件"——它仍然只服务于通知功能，只是渲染位置多了。如果把它挪到 shared/，反而会给 shared/ 增加业务耦合，破坏 shared/ 的"纯 UI"定位。真正需要调整的是数据层：两个实例不应各自 initSignalR，而应该共享同一个 store（AdminWeb 已经这样做了——Zustand store 是全局单例）。</details>

---

## 二、完整代码

```typescript
/**
 * 通知铃铛组件
 *
 * 功能：
 * - 顶栏通知图标 + 未读徽标
 * - 点击展开下拉通知列表
 * - 支持标记已读 / 全部已读
 * - 首次挂载时初始化 SignalR 监听
 * - 打印任务通知点击可直接下载
 */
import { useEffect, useRef, useState } from "react"
import { Link } from "react-router-dom"
import { toast } from "sonner"
import { useNotificationStore } from "@/stores/notification-store"
import { jobsApi } from "@/api/print/jobs"
import { NotificationStatus } from "@/types/notification"

/**
 * 通知铃铛组件
 *
 * 在应用顶栏展示通知图标和未读数徽标，点击展开最近通知下拉列表。
 * 集成 SignalR 实时推送，并配合 60 秒轮询作为断线兜底。
 * 支持单条标记已读和一键全部已读。
 */
export function NotificationBell() {
  const { unreadCount, recentItems, loading, fetchUnreadCount, fetchRecent, markRead, markAllRead, initSignalR, disposeSignalR } =
    useNotificationStore()
  const [open, setOpen] = useState(false)
  const dropdownRef = useRef<HTMLDivElement>(null)

  // 初始化：拉取未读数 + 注册 SignalR 监听
  useEffect(() => {
    fetchUnreadCount()
    initSignalR()

    // 每 60 秒轮询未读数（SignalR 断线时兜底）
    const timer = setInterval(fetchUnreadCount, 60_000)

    return () => {
      clearInterval(timer)
      disposeSignalR()
    }
  }, [])

  // 展开下拉时加载最近通知
  useEffect(() => {
    if (open) {
      fetchRecent()
    }
  }, [open])

  // 点击外部关闭下拉
  useEffect(() => {
    function handleClickOutside(e: MouseEvent) {
      if (dropdownRef.current && !dropdownRef.current.contains(e.target as Node)) {
        setOpen(false)
      }
    }
    document.addEventListener("mousedown", handleClickOutside)
    return () => document.removeEventListener("mousedown", handleClickOutside)
  }, [])

  /** 点击通知项：如果是打印任务且有 jobId 则下载，否则标记已读 */
  const handleClickItem = (item: typeof recentItems[number]) => {
    if (item.status !== NotificationStatus.Read) {
      markRead(item.id)
    }
    // 解析 dataJson，打印完成通知可点击下载
    if (item.dataJson) {
      try {
        const data = JSON.parse(item.dataJson)
        if (data.jobId && data.status === "completed") {
          // 优先使用 presigned URL（无需 token），降级走 API
          const downloadUrl = data.downloadUrl
          if (downloadUrl && downloadUrl.startsWith("http")) {
            window.open(downloadUrl, "_blank")
          } else {
            jobsApi.download(data.jobId).then((res) => {
              const blob = new Blob([res.data as BlobPart])
              const url = URL.createObjectURL(blob)
              const a = document.createElement("a")
              a.href = url
              // 从 Content-Disposition 取文件名
              const cd = res.headers?.["content-disposition"] ?? ""
              const match = cd.match(/filename\*?=(?:UTF-8'')?([^;\n]+)/i)
              a.download = match ? decodeURIComponent(match[1].replace(/"/g, "")) : `report_${data.jobId.slice(0, 8)}`
              a.click()
              URL.revokeObjectURL(url)
            }).catch(() => toast.error("下载失败"))
          }
        }
      } catch {
        // 忽略
      }
    }
  }

  return (
    <div ref={dropdownRef} className="relative">
      {/* 铃铛按钮 */}
      <button
        onClick={() => setOpen(!open)}
        className="relative p-2 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors"
        aria-label="通知"
      >
        <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
          <path d="M10 2a6 6 0 00-6 6v3.586l-.707.707A1 1 0 004 14h12a1 1 0 00.707-1.707L16 11.586V8a6 6 0 00-6-6zM10 18a3 3 0 01-3-3h6a3 3 0 01-3 3z" />
        </svg>
        {/* 未读徽标 - 超过 99 显示 "99+" */}
        {unreadCount > 0 && (
          <span className="absolute -top-1 -right-1 inline-flex items-center justify-center px-1.5 py-0.5 text-xs font-bold leading-none text-white bg-red-500 rounded-full animate-pulse">
            {unreadCount > 99 ? "99+" : unreadCount}
          </span>
        )}
      </button>

      {/* 下拉面板 */}
      {open && (
        <div className="absolute right-0 mt-2 w-80 bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-lg shadow-lg z-50">
          {/* 头部 */}
          <div className="flex items-center justify-between px-4 py-3 border-b border-gray-200 dark:border-gray-700">
            <span className="text-sm font-semibold">通知</span>
            {/* 有未读时显示"全部已读"按钮 */}
            {unreadCount > 0 && (
              <button
                onClick={() => markAllRead()}
                className="text-xs text-blue-600 hover:text-blue-800 dark:text-blue-400"
              >
                全部已读
              </button>
            )}
          </div>

          {/* 通知列表 */}
          <div className="max-h-96 overflow-y-auto">
            {loading ? (
              <div className="px-4 py-8 text-center text-sm text-gray-500">加载中...</div>
            ) : recentItems.length === 0 ? (
              <div className="px-4 py-8 text-center text-sm text-gray-500">暂无通知</div>
            ) : (
              recentItems.map((item) => (
                <div
                  key={item.id}
                  onClick={() => handleClickItem(item)}
                  className={`px-4 py-3 border-b border-gray-100 dark:border-gray-800 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors ${
                    item.status !== NotificationStatus.Read ? "bg-blue-50/50 dark:bg-blue-900/10" : ""
                  }`}
                >
                  <div className="flex items-start gap-2">
                    {/* 未读指示点 */}
                    {item.status !== NotificationStatus.Read && (
                      <span className="mt-1.5 h-2 w-2 rounded-full bg-blue-500 flex-shrink-0" />
                    )}
                    <div className="flex-1 min-w-0">
                      <p className="text-sm font-medium truncate">{item.title ?? "通知"}</p>
                      <p className="text-xs text-gray-500 mt-1 line-clamp-2">{item.body}</p>
                      {/* 通知时间 */}
                      <p className="text-xs text-gray-400 mt-1">
                        {new Date(item.creationTime).toLocaleString("zh-CN")}
                      </p>
                    </div>
                  </div>
                </div>
              ))
            )}
          </div>

          {/* 底部链接 - 跳转通知列表页 */}
          <div className="px-4 py-2 border-t border-gray-200 dark:border-gray-700 text-center">
            <Link to="/notifications" onClick={() => setOpen(false)} className="text-xs text-blue-600 hover:text-blue-800 dark:text-blue-400">
              查看全部通知
            </Link>
          </div>
        </div>
      )}
    </div>
  )
}
```

---

## 三、三个 useEffect 的职责与时序

这是铃铛组件最精妙的设计——三个 `useEffect` 各司其职：

```
useEffect #1: 初始化（只运行一次）
├── fetchUnreadCount()   // 立即拉取未读数
├── initSignalR()        // 注册 SignalR 监听
├── setInterval(60s)     // 定时轮询兜底
└── cleanup: clearInterval + disposeSignalR

useEffect #2: 展开时加载
└── if (open) fetchRecent()  // 用户点击铃铛时才拉列表

useEffect #3: 点击外部关闭
└── document.addEventListener("mousedown", handleClickOutside)
```

```
时间线：
──────────────────────────────────────────────────────→
  │                     │            │
  组件挂载              用户点击铃铛    用户点击外部
  ├─ fetchUnreadCount   ├─ fetchRecent  ├─ setOpen(false)
  ├─ initSignalR        │  (拉最近10条)  │
  ├─ 启动60s轮询        │               │
  │                     │               │
  ...60s后...           │               │
  ├─ fetchUnreadCount   │               │
  │                     │               │
  ...SignalR推送...      │               │
  └─ onNewNotification  │               │
     (unreadCount+1)    │               │
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【设计取舍】** | `useEffect(() => { ... }, [])` | 空依赖数组 = 只在挂载时执行。`fetchUnreadCount`、`initSignalR`、`setInterval` 都是 Zustand store 的方法，引用稳定，不需要加到依赖数组 |
| **【易错点】** | `const timer = setInterval(...)` | 必须保存 timer 引用！cleanup 时需要 `clearInterval(timer)`。如果写成 `setInterval(fetchUnreadCount, 60_000)` 不保存引用，组件卸载时无法清除定时器 |
| **【性能陷阱】** | `fetchRecent` 只在 `open` 时调用 | 不要在初始化时就 `fetchRecent`！用户可能永远不点铃铛，提前拉取浪费请求。**懒加载**是更好的策略 |
| **【易错点】** | `handleClickOutside` 用 `mousedown` 而非 `click` | `click` = mousedown + mouseup。如果用 `click`，用户在面板内 mousedown、在面板外 mouseup 也会触发关闭，体验不好。`mousedown` 更早响应且更准确 |

⚠️ **【易错点】badge count 与 SignalR 的竞态条件**

考虑这个场景：SignalR 推送了一条新通知（`unreadCount + 1`），同时 60 秒轮询也在请求最新的 `unreadCount`。如果轮询的响应在 SignalR 推送之前发出、但晚于推送到达浏览器，会发生什么？

```
时间线：
  t0: unreadCount = 5
  t1: SignalR 推送通知 → onNewNotification → unreadCount = 6
  t2: 60s 轮询响应到达（基于 t0 时刻的数据）→ unreadCount = 5 ❌

结果：SignalR 推送的 +1 被轮询的旧数据覆盖了！
```

AdminWeb 的 `fetchUnreadCount` 用的是**服务端权威值覆盖**（`set({ unreadCount: res.data.unreadCount })`），而不是增量更新。这意味着轮询响应会把 SignalR 的增量更新覆盖掉。

但这个问题在实践中影响有限，因为：
1. 轮询间隔是 60 秒，竞态窗口很小
2. 下一次轮询或 SignalR 推送会修正计数
3. 用户点击铃铛时 `fetchRecent` 会重新拉取完整列表

> **讲师提问**：如果要彻底解决这个竞态问题，你会怎么改 `fetchUnreadCount` 的实现？
>
> <details><summary>参考答案</summary>方案一：fetchUnreadCount 改用增量式——`set((state) => ({ unreadCount: Math.max(state.unreadCount, res.data.unreadCount) }))`，取本地和服务端的最大值。方案二：给轮询请求加一个版本号，如果响应的版本号早于最近一次 SignalR 推送，则丢弃响应。方案一简单但有上限漂移风险（服务端已读数可能小于本地）；方案二更精确但实现复杂。AdminWeb 选择的是"容忍短时不一致 + 下次轮询自修正"的务实策略。</details>

---

## 四、handleClickItem 的双功能设计

```typescript
const handleClickItem = (item: typeof recentItems[number]) => {
  if (item.status !== NotificationStatus.Read) {
    markRead(item.id)
  }
  if (item.dataJson) {
    try {
      const data = JSON.parse(item.dataJson)
      if (data.jobId && data.status === "completed") {
        // 优先使用 presigned URL（无需 token），降级走 API
        const downloadUrl = data.downloadUrl
        if (downloadUrl && downloadUrl.startsWith("http")) {
          window.open(downloadUrl, "_blank")
        } else {
          jobsApi.download(data.jobId).then((res) => {
            const blob = new Blob([res.data as BlobPart])
            const url = URL.createObjectURL(blob)
            const a = document.createElement("a")
            a.href = url
            // 从 Content-Disposition 取文件名
            const cd = res.headers?.["content-disposition"] ?? ""
            const match = cd.match(/filename\*?=(?:UTF-8'')?([^;\n]+)/i)
            a.download = match ? decodeURIComponent(match[1].replace(/"/g, "")) : `report_${data.jobId.slice(0, 8)}`
            a.click()
            URL.revokeObjectURL(url)
          }).catch(() => toast.error("下载失败"))
        }
      }
    } catch {
      // 忽略
    }
  }
}
```

⚠️ **【易错点】为什么不用 `window.open(downloadUrl)` 下载？**
因为文件下载 API 需要认证！`window.open` 发出的请求不带 `Authorization` header，服务端会返回 401，然后重定向到登录页。

```
❌ window.open("/api/print/jobs/xxx/download")
   → 浏览器发 GET 请求（无 Bearer Token）
   → 后端返回 401
   → 重定向到登录页
   → 用户懵了：我只是想下载个文件！

✅ jobsApi.download(jobId)
   → axios 发请求（自动带 Bearer Token）
   → 后端返回 Blob
   → 前端用 URL.createObjectURL + <a> 触发下载
```

💡 **但 AdminWeb 源码中有一个改进：presigned URL 优先**
仔细看源码，如果 `dataJson` 中包含 `downloadUrl`（presigned URL），组件会用 `window.open(downloadUrl, "_blank")` 直接打开。因为 presigned URL 自带签名参数，不需要 `Authorization` header。只有没有 presigned URL 时，才降级走 API 下载。

```
presigned URL:
https://storage.example.com/reports/xxx.pdf?sig=abc123&se=2024-01-01T00:00:00Z
→ URL 自带访问签名，无需 Token
→ window.open() 直接打开即可

API 下载（降级）:
/api/print/jobs/xxx/download
→ 需要 Bearer Token
→ 必须用 axios + Blob 方式下载
```

💡 **【设计取舍】`typeof recentItems[number]`**
这是 TypeScript 从数组类型提取元素类型的技巧：
```typescript
recentItems: NotificationDto[]
typeof recentItems[number]  // 等价于 NotificationDto
```
为什么不用直接写 `NotificationDto`？因为如果 `recentItems` 的类型变了，`typeof` 写法会自动跟着变，减少维护成本。

> **讲师提问**：`handleClickItem` 中，`markRead` 和下载逻辑是串行还是并行？如果 `markRead` 请求失败，下载还会执行吗？
>
> <details><summary>参考答案</summary>两者是独立的，不是 await 串行。`markRead(item.id)` 是异步调用但没有 await，所以不管 `markRead` 成功还是失败，下载逻辑都会继续执行。这是一个"标记已读"非关键操作的设计——即使标记失败，用户也不应该被阻止下载文件。</details>

---

## 五、铃铛 UI 结构拆解

```
<div ref={dropdownRef} className="relative">
│
├── <button> 铃铛按钮
│   ├── <svg> 铃铛图标 (内联 SVG，20x20)
│   └── <span> 未读徽标 (红色圆点 + 数字)
│       ├── 条件渲染: unreadCount > 0
│       ├── 超过99显示 "99+"
│       └── animate-pulse (闪烁动画吸引注意)
│
└── {open && <div>} 下拉面板 (条件渲染)
    ├── 头部: "通知" + "全部已读"按钮
    ├── 通知列表 (max-h-96 滚动)
    │   ├── 加载中状态
    │   ├── 空状态
    │   └── 通知项列表
    │       ├── 未读蓝点 (条件渲染)
    │       ├── 标题 (truncate 单行截断)
    │       ├── 正文 (line-clamp-2 两行截断)
    │       └── 时间 (zh-CN 格式化)
    └── 底部: "查看全部通知" 链接
```

### 关键 CSS 类解读

| CSS 类 | 作用 | 为什么 |
|--------|------|--------|
| `relative` | 相对定位 | 下拉面板用 `absolute` 定位，需要相对父元素 |
| `animate-pulse` | 脉冲动画 | 未读数闪烁，吸引用户注意 |
| `max-h-96` | 最大高度 24rem | 通知列表不超过 384px，超出滚动 |
| `truncate` | 文字截断 | 标题过长时显示省略号 |
| `line-clamp-2` | 两行截断 | 正文最多显示 2 行，超出显示省略号 |
| `bg-blue-50/50` | 半透明蓝色背景 | 未读通知的视觉区分（50% 透明度） |

⚠️ **【易错点】`line-clamp-2` 的原理**
这是 Tailwind 的 `@tailwindcss/line-clamp` 插件（v3.3+ 内置）。底层是 `-webkit-line-clamp` CSS 属性，兼容性很好（现代浏览器都支持）。

---

## 六、实时推送 + 轮询的双重保障

```
                    新通知到达
                       │
          ┌────────────┴────────────┐
          │                         │
    SignalR 推送 ✅              60s 轮询 ✅
    (实时，毫秒级)               (兜底，分钟级)
          │                         │
          ▼                         ▼
   onNewNotification()       fetchUnreadCount()
   ├─ unreadCount +1         ├─ 从服务端拿最新计数
   ├─ 插入 recentItems       └─ 覆盖本地 unreadCount
   └─ 显示 toast
```

⚠️ **【设计取舍】为什么需要双重保障？**
SignalR 可能断线（网络波动、服务端重启）。如果只依赖 SignalR，断线期间的通知用户永远看不到。60 秒轮询是"安全网"——即使 SignalR 断了，最多延迟 60 秒也能拿到最新计数。

💡 **但为什么轮询只拉计数，不拉列表？**
因为列表数据量大（每条通知有标题、正文、时间等），60 秒拉一次列表太浪费。计数只有一个数字，轮询代价极低。用户真正看列表时（点击铃铛），才按需拉取。

⚠️ **【性能陷阱】通知轮询频率的选择**

```
轮询间隔选择的影响：

间隔太短（如 5 秒）：
├── 优点：断线时发现快，通知延迟低
├── 缺点：100 个在线用户 = 每分钟 1200 次 HTTP 请求
└── 结论：对服务端压力太大，不可取

间隔太长（如 5 分钟）：
├── 优点：服务端压力小
├── 缺点：断线后用户可能 5 分钟看不到新通知
└── 结论：用户体验差

AdminWeb 选择 60 秒：
├── 断线后最多 1 分钟感知到新通知
├── 100 用户 = 每分钟 100 次请求，服务端轻松承受
└── 配合 SignalR 实时推送，正常情况下轮询只是"心跳确认"
```

> **讲师提问**：如果 SignalR 已连接且工作正常，60 秒轮询还在运行。这是不是浪费？怎么优化？
>
> <details><summary>参考答案</summary>是浪费，但优化要看性价比。方案：监听 `onConnectionStateChange`，connected 时把轮询间隔改为 5 分钟（仅做心跳确认），disconnected/reconnecting 时恢复 60 秒。但实际节省的请求量很小（每个用户少了几次/分钟的 HTTP 请求），代码复杂度增加（需要动态调整 setInterval），所以 AdminWeb 选择"简单但略有浪费"的策略。在用户量特别大的场景下，这个优化才有实际价值。</details>

---

## 七、notification-store 中的 SignalR 集成

铃铛组件通过 `useNotificationStore` 使用 SignalR，但实际的 SignalR 注册逻辑在 store 中：

```typescript
// notification-store.ts 核心片段

let _handlerRef: ((...args: unknown[]) => void) | null = null

initSignalR: () => {
  // 防止重复注册
  if (_handlerRef) return

  // 确保 WebSocket 连接已建立
  startConnection()

  const handler = (message: unknown) => {
    const msg = message as {
      type: string
      title?: string
      body?: string
      payload?: { recordId?: string; dataJson?: string }
    }
    // 仅处理 type 为 "notification" 的消息
    if (msg.type === "notification") {
      const notification: NotificationDto = {
        id: msg.payload?.recordId ?? crypto.randomUUID(),
        templateCode: "",
        channel: NotificationChannel.InApp,
        status: NotificationStatus.Sent,
        priority: NotificationPriority.Normal,
        title: msg.title,
        body: msg.body,
        dataJson: msg.payload?.dataJson,
        creationTime: new Date().toISOString(),
      }
      get().onNewNotification(notification)

      // 打印任务通知：解析 dataJson 显示可操作 toast
      if (notification.dataJson) {
        try {
          const data = JSON.parse(notification.dataJson)
          if (data.status === "completed" && data.jobId) {
            toast.success(notification.title ?? "渲染完成", {
              description: notification.body,
              action: {
                label: "下载",
                onClick: () => downloadJob(data.jobId, data.downloadUrl),
              },
              duration: 10000,
            })
          } else if (data.status === "failed") {
            toast.error(notification.title ?? "渲染失败", {
              description: notification.body,
              duration: 10000,
            })
          }
        } catch {
          // dataJson 不是合法 JSON，忽略
        }
      }
    }
  }

  _handlerRef = handler
  platformHub.on("ReceiveNotification", handler)
},

disposeSignalR: () => {
  if (_handlerRef) {
    platformHub.off("ReceiveNotification", _handlerRef)
    _handlerRef = null
  }
},
```

⚠️ **【易错点】`_handlerRef` 的防重复注册机制**
`initSignalR` 开头检查 `if (_handlerRef) return`，这是防止重复注册的关键。如果铃铛组件被多次挂载/卸载（比如路由切换），每次挂载都会调用 `initSignalR`。没有这个守卫，同一个 handler 会被注册多次，导致同一条通知弹出多个 toast。

> **讲师提问**：`initSignalR` 在 `if (_handlerRef) return` 之后调用 `startConnection()`，但如果 `startConnection()` 失败了呢？handler 仍然被注册了，但连接没建立，消息永远不会到达。这有问题吗？
>
> <details><summary>参考答案</summary>没有大问题。handler 注册到 HubConnection 实例上，即使连接未建立，handler 只是"待命"状态，不会消耗额外资源。而且 `startConnection()` 内部有重连机制——如果首次连接失败，SignalR 的 `withAutomaticReconnect` 会自动重试。所以 handler 注册是"先占位，等连接成功后自动生效"。</details>

---

## 八、组件卸载时的资源清理

```typescript
return () => {
  clearInterval(timer)    // 清除定时器
  disposeSignalR()        // 移除 SignalR 监听
}
```

如果不清理会怎样？

| 泄漏类型 | 后果 |
|----------|------|
| `setInterval` 未清除 | 组件卸载后仍然每 60 秒发 HTTP 请求，浪费流量 + 服务端压力 |
| `disposeSignalR` 未调用 | SignalR handler 仍然在处理消息，`_handlerRef` 不为 null，下次挂载时 `initSignalR` 直接 return，新组件收不到通知 |
| `mousedown` 监听器未移除 | 组件卸载后点击事件处理器仍然绑定在 document 上，可能操作已卸载的组件状态 |

⚠️ **【设计取舍】徽标放在顶栏 Header 还是侧边栏 Sidebar？**

```
方案对比：

┌─── 顶栏 Header ────────────────────────┐
│ Logo    搜索    🔔③  用户头像  ▼        │ ← 徽标在右上角
└────────────────────────────────────────┘

┌─── 侧边栏 Sidebar ────────────────────┐
│ 📊 仪表盘                              │
│ 📋 订单管理                            │
│ 🔔 通知中心  ③                         │ ← 徽标在菜单项旁边
│ ⚙️ 系统设置                            │
└────────────────────────────────────────┘
```

| 维度 | 顶栏 Header | 侧边栏 Sidebar |
|------|-------------|----------------|
| 可见性 | 任何页面都可见 | 侧边栏收起时不可见 |
| 视觉冲击 | 角标醒目（红色+脉冲动画） | 菜单项旁数字，不够醒目 |
| 点击距离 | 鼠标移动距离短 | 需要先展开侧边栏 |
| 下拉面板空间 | 有足够空间展开 | 侧边栏宽度受限 |

AdminWeb 选择顶栏方案，原因是**通知的全局性和紧急性**——用户需要无论在哪个页面、无论侧边栏是否展开，都能第一时间感知到新通知。

---

## 九、验证步骤

在继续下一节之前，用以下步骤验证铃铛组件是否正确运行：

### 步骤 1：检查初始未读数

刷新页面，观察铃铛图标上的红色徽标数字。打开 Network 面板，应该看到一个请求：

```
GET /notify-api/api/notifications/unread-count → 200
响应：{ "unreadCount": 3 }
```

如果徽标数字和响应不一致，检查 `fetchUnreadCount` 的 `set` 逻辑。

### 步骤 2：检查 SignalR 实时推送

1. 在另一个浏览器标签页中，用管理员账号发送一条通知
2. 观察当前页面的铃铛徽标是否立即 +1
3. 是否弹出了 toast 通知

如果 toast 弹出了但徽标没变，检查 `onNewNotification` 是否正确更新了 `unreadCount`。

### 步骤 3：测试下拉面板

1. 点击铃铛图标，观察下拉面板是否出现
2. Network 面板应该有一个新请求：`GET /notify-api/api/notifications?page=1&pageSize=10`
3. 点击面板外部区域，面板应该关闭

### 步骤 4：测试标记已读

1. 点击一条未读通知（蓝色背景）
2. 观察蓝点是否消失、背景是否变白
3. 徽标数字是否 -1

### 步骤 5：测试"全部已读"

1. 点击"全部已读"按钮
2. 所有通知背景变白、蓝点消失
3. 徽标归零

### 步骤 6：验证组件卸载清理

1. 打开浏览器控制台
2. 导航到另一个路由（铃铛组件卸载）
3. 确认没有报错（如 `Can't perform a React state update on an unmounted component`）
4. 回到有铃铛的页面，确认 SignalR 重新初始化正常

---

## 🤔 思考题

### 概念级（理解 Why）
1. 为什么铃铛初始化时 `fetchUnreadCount` 而不是 `fetchRecent`？
2. 点击外部关闭用 `mousedown` 而非 `click`，具体哪个场景会有差异？

### 推理级（推导 What-if）
3. 如果 `initSignalR` 和 `disposeSignalR` 不在 cleanup 中成对调用，而是只在挂载时 `initSignalR`，用户登出再登录会发生什么？
4. 当前 `handleClickItem` 对打印完成通知的处理是"点击即下载"。如果用户不小心点了不想下载的通知呢？改进方案是什么？
5. 如果 `_handlerRef` 守卫被移除（删除 `if (_handlerRef) return`），用户在页面间快速切换时会出现什么现象？

### 动手级（代码实践）
6. 给铃铛添加"连接状态指示器"：在铃铛图标右下角显示一个小圆点，绿色=已连接，黄色=重连中，灰色=已断开。利用 `onConnectionStateChange` 实现。

   <details><summary>提示</summary>
   ```typescript
   import { onConnectionStateChange } from "@/lib/signalr"

   // 在 useEffect 中监听连接状态
   const [connState, setConnState] = useState<"connected" | "reconnecting" | "disconnected">("disconnected")

   useEffect(() => {
     const unsub = onConnectionStateChange(setConnState)
     return () => unsub()
   }, [])

   // 在铃铛按钮中渲染指示点
   <span className={`absolute bottom-0 right-0 h-2 w-2 rounded-full ${
     connState === "connected" ? "bg-green-500" :
     connState === "reconnecting" ? "bg-yellow-500" : "bg-gray-400"
   }`} />
   ```
   </details>

7. 当前轮询间隔是 60 秒。试着实现"自适应轮询"：SignalR 已连接时轮询间隔改为 5 分钟（因为实时推送已覆盖），断线时恢复 60 秒。

   <details><summary>提示</summary>不能直接修改 `setInterval` 的间隔，需要 `clearInterval` + 重新 `setInterval`。可以用 `useRef` 保存当前 interval ID，在 `onConnectionStateChange` 回调中根据状态调整。</details>

8. 当前下载文件名是从 `Content-Disposition` header 中解析的。如果 header 不存在，降级用 `report_${jobId.slice(0, 8)}`。试着给下载文件加上正确的文件扩展名（从 Content-Type 推断，如 `application/pdf` → `.pdf`）。

---

## ✅ 输出检查清单

完成本节后，你应该能回答：

- [ ] 能说出三个 `useEffect` 的各自职责
- [ ] 理解 `fetchRecent` 的懒加载策略（展开时才加载）
- [ ] 掌握点击外部关闭的实现模式（mousedown + ref.contains）
- [ ] 理解为什么下载文件用 axios 而非 `window.open`
- [ ] 知道 SignalR + 轮询双重保障的设计原因
- [ ] 掌握 `typeof array[number]` 提取元素类型的技巧
- [ ] 理解组件卸载时清理定时器和 SignalR 监听的必要性
- [ ] 知道 `truncate`、`line-clamp-2`、`animate-pulse` 的作用
- [ ] 理解 `_handlerRef` 防重复注册的原理和必要性
- [ ] 知道 badge count 与 SignalR 推送的竞态风险
- [ ] 理解为什么 notification-bell.tsx 放在 components/ 根目录
- [ ] 知道 presigned URL 优先于 API 下载的原因

---

[下一节 →](05-通知列表页.md)
