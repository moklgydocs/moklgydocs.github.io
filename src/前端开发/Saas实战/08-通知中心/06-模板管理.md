# 06 - 模板管理：CRUD + 多维度发送通知

> **这一步解决什么问题？**
> 通知不是随意发的——需要有模板定义内容格式（标题、正文、变量），还需要选择收件人（按用户、角色、部门、公司、岗位）。模板管理页是通知中心最复杂的页面，1058 行代码涵盖了模板 CRUD 和多维度发送通知两大功能。
>
> **ASP.NET Core 类比**：模板管理 ≈ 你的 `NotificationTemplatesController` 的全部 Action（CRUD），发送通知 ≈ 一个复杂的 Service 方法，需要聚合多个数据源（用户搜索、角色成员、部门人员等）来确定收件人列表。

---

## 前置知识

- 第 1-5 节的 SignalR、通知 Store、API 层、铃铛和通知列表页
- React `useState` + `useEffect` + `useCallback` 的组合模式
- ASP.NET Core 的 DTO 分离模式（CreateRequest vs UpdateRequest）
- Scriban 模板语法（`{{ model.PropertyName }}`）和模板变量替换
- `Map` 数据结构的基本操作（`get`/`set`/`new Map(prev)`）

---

## 一、页面架构总览

```
┌───────────────────────────────────────────────────────────────────┐
│ NotificationTemplatesPage (1058行)                                 │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ 通知模板列表 (主页面)                                        │  │
│  │  [新建模板] [发送通知] [刷新]    [搜索编码或名称...]          │  │
│  │  AgTable: 编码 | 名称 | 描述 | 状态 | 创建时间 | 操作▼       │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ TemplateFormDialog (模板表单对话框)                           │  │
│  │  编码 | 名称 | 描述 | [启用]                                 │  │
│  │  [站内信] [邮件] [短信]  ← Tabs 切换渠道模板                  │  │
│  │  标题模板: {{ model.UserName }}，您有一条新通知               │  │
│  │  正文模板: 支持 Scriban 语法                                  │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ SendNotificationDialog (发送通知对话框) — 最复杂的部分！       │  │
│  │  选择模板                                                    │  │
│  │  收件人 (5个Tab):                                            │  │
│  │   [按用户] [按角色] [按部门] [按公司] [按岗位]                │  │
│  │  已选用户: 张三(用户搜索), 李四(角色:Admin)  ✕               │  │
│  │  发送渠道: ☑站内信 ☑邮件 ☐短信                              │  │
│  │  优先级: 普通▼                                               │  │
│  │  模板变量: { "UserName": "张三", "OrderNo": "ORD-001" }      │  │
│  │  [取消] [发送 (3人)]                                         │  │
│  └─────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────┘
```

---

## 二、模板表单对话框（TemplateFormDialog）

### 2.1 组件接口

```typescript
interface TemplateFormProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  template?: TemplateDetailDto | null
  onSuccess: () => void
}

function TemplateFormDialog({ open, onOpenChange, template, onSuccess }: TemplateFormProps) {
  const isEdit = !!template
  // ...
}
```

💡 **`template` 参数决定新建/编辑模式**：
- `template === null/undefined` → 新建模式（表单为空）
- `template !== null` → 编辑模式（表单预填模板数据）

⚠️ **【易错点】`isEdit = !!template`**
`!!` 双重取反，把 truthy/falsy 转为真正的 `boolean`。`null` → `false`，对象 → `true`。

> **🤔 导师提问**：`TemplateFormDialog` 用 `template` 参数是否为 `null` 来区分新建/编辑模式。在 ASP.NET Core 中，你通常怎么区分 Create 和 Update Action？是分成两个 Controller 方法，还是一个方法里判断？前端的"一个组件两种模式"和后端的"一个 Action 两种路径"，各自的优缺点是什么？

### 2.2 表单状态与初始化

```typescript
const [form, setForm] = useState<CreateTemplateRequest>({
  code: "",
  name: "",
  description: "",
  isEnabled: true,
  inAppTitleTemplate: "",
  inAppBodyTemplate: "",
  emailSubjectTemplate: "",
  emailBodyTemplate: "",
  smsTemplate: "",
})

useEffect(() => {
  if (template) {
    setForm({
      code: template.code,
      name: template.name,
      description: template.description ?? "",
      isEnabled: template.isEnabled,
      inAppTitleTemplate: template.inAppTitleTemplate ?? "",
      inAppBodyTemplate: template.inAppBodyTemplate ?? "",
      emailSubjectTemplate: template.emailSubjectTemplate ?? "",
      emailBodyTemplate: template.emailBodyTemplate ?? "",
      smsTemplate: template.smsTemplate ?? "",
    })
  } else {
    setForm({
      code: "", name: "", description: "", isEnabled: true,
      inAppTitleTemplate: "", inAppBodyTemplate: "",
      emailSubjectTemplate: "", emailBodyTemplate: "", smsTemplate: "",
    })
  }
}, [template, open])
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【设计取舍】** | `useState<CreateTemplateRequest>` | 新建和编辑共用同一个表单状态类型。编辑时只是初始值不同，提交时区分调用 `createTemplate` 还是 `updateTemplate` |
| **【易错点】** | `useEffect` 依赖 `[template, open]` | 必须依赖 `open`！因为对话框关闭再打开时，需要重置表单。只依赖 `template` 不够——同一个模板编辑两次，`template` 引用没变，不会重新初始化 |
| **【易错点】** | `template.inAppTitleTemplate ?? ""` | 后端可选字段可能是 `undefined`，但受控组件的 `value` 不能是 `undefined`（React 会报警告），必须用空字符串兜底 |

### 2.3 通用 set 辅助函数

```typescript
const set = (key: keyof CreateTemplateRequest, value: unknown) =>
  setForm((prev) => ({ ...prev, [key]: value }))
```

💡 **这是 React 表单的常用模式**：一个 `set` 函数替代 10 个 `setCode`、`setName`、`setDescription`...

```typescript
// 没有 set 辅助函数：
<Input value={form.code} onChange={(e) => setForm(prev => ({ ...prev, code: e.target.value }))} />
<Input value={form.name} onChange={(e) => setForm(prev => ({ ...prev, name: e.target.value }))} />

// 有 set 辅助函数：
<Input value={form.code} onChange={(e) => set("code", e.target.value)} />
<Input value={form.name} onChange={(e) => set("name", e.target.value)} />
```

### 2.4 提交逻辑

```typescript
const handleSubmit = async () => {
  if (!form.code || !form.name) {
    toast.error("编码和名称不能为空")
    return
  }
  setLoading(true)
  try {
    if (isEdit) {
      const updateData: UpdateTemplateRequest = {
        isEnabled: form.isEnabled,
        inAppTitleTemplate: form.inAppTitleTemplate,
        inAppBodyTemplate: form.inAppBodyTemplate,
        emailSubjectTemplate: form.emailSubjectTemplate,
        emailBodyTemplate: form.emailBodyTemplate,
        smsTemplate: form.smsTemplate,
      }
      await updateTemplate(template!.id, updateData)
      toast.success("更新成功")
    } else {
      await createTemplate(form)
      toast.success("创建成功")
    }
    onOpenChange(false)
    onSuccess()
  } catch {
    toast.error(isEdit ? "更新失败" : "创建失败")
  } finally {
    setLoading(false)
  }
}
```

⚠️ **【易错点】编辑时只传 `UpdateTemplateRequest`，不传 `code` 和 `name`！**

```typescript
const updateData: UpdateTemplateRequest = {
  isEnabled: form.isEnabled,
  inAppTitleTemplate: form.inAppTitleTemplate,
  // ... 没有 code 和 name！
}
```

因为 `UpdateTemplateRequest` 类型定义里没有 `code` 和 `name`——模板编码和名称创建后不可修改。UI 上编码输入框也有 `disabled={isEdit}` 来体现这一点。

> **🤔 导师提问**：`UpdateTemplateRequest` 不允许修改 `code` 和 `name`，类型系统就是你的防线——如果有人试图 `updateTemplate(id, { code: "new-code" })`，TypeScript 会直接报错。在 ASP.NET Core 中，你会用什么机制防止客户端修改不该修改的字段？`[BindNever]`？DTO 分离？还是 ModelState 验证？

### 2.5 渠道模板 Tabs

```typescript
<Tabs defaultValue="inapp">
  <TabsList>
    <TabsTrigger value="inapp">站内信</TabsTrigger>
    <TabsTrigger value="email">邮件</TabsTrigger>
    <TabsTrigger value="sms">短信</TabsTrigger>
  </TabsList>
  <TabsContent value="inapp" className="space-y-3 mt-3">
    <div className="space-y-1.5">
      <Label>标题模板</Label>
      <Input
        value={form.inAppTitleTemplate ?? ""}
        onChange={(e) => set("inAppTitleTemplate", e.target.value)}
        placeholder="{{ model.UserName }}，您有一条新通知"
      />
    </div>
    <div className="space-y-1.5">
      <Label>正文模板</Label>
      <Textarea
        value={form.inAppBodyTemplate ?? ""}
        onChange={(e) => set("inAppBodyTemplate", e.target.value)}
        placeholder="支持 Scriban 模板语法：{{ model.PropertyName }}"
        rows={4}
      />
    </div>
  </TabsContent>
  <TabsContent value="email" className="space-y-3 mt-3">
    {/* 邮件主题 + HTML正文 */}
  </TabsContent>
  <TabsContent value="sms" className="space-y-3 mt-3">
    {/* 短信内容 */}
  </TabsContent>
</Tabs>
```

💡 **Scriban 模板语法**：后端用 [Scriban](https://github.com/scriban/scriban) 模板引擎，语法是 `{{ model.PropertyName }}`。

```
模板: "{{ model.UserName }}，您的订单 {{ model.OrderNo }} 已发货"
变量: { "UserName": "张三", "OrderNo": "ORD-001" }
渲染: "张三，您的订单 ORD-001 已发货"
```

⚠️ **【设计取舍】placeholder 中展示 Scriban 语法**
这是教学式 UI 设计——用户看到 placeholder 就知道怎么写模板变量，不需要查文档。

> **🤔 导师提问**：Scriban 的 `{{ model.UserName }}` 语法和 ASP.NET Core 的 Razor 语法 `@Model.UserName` 非常相似。但 Scriban 是在服务端渲染的，不是在浏览器里。如果模板内容包含恶意脚本（如 `<script>alert('xss')</script>`），Scriban 渲染后直接发送给用户，安全风险在哪里？前端展示通知时需要做什么防护？

---

## 三、发送通知对话框（SendNotificationDialog）—— 最复杂的部分

### 3.1 Scriban 变量提取

```typescript
/** 从 Scriban 模板中提取变量名，如 {{ model.UserName }} → UserName */
function extractVariables(template: string): string[] {
  const re = /\{\{\s*model\.(\w+)\s*\}\}/g
  const vars = new Set<string>()
  let m: RegExpExecArray | null
  while ((m = re.exec(template)) !== null) vars.add(m[1])
  return [...vars]
}
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【易错点】** | `/\{\{\s*model\.(\w+)\s*\}\}/g` | 正则解析 Scriban 模板。`\{\{` 匹配 `{{`，`\s*` 匹配可选空格，`(\w+)` 捕获变量名，`/g` 全局匹配 |
| **【设计取舍】** | `new Set<string>()` | 用 Set 去重！如果模板中 `{{ model.UserName }}` 出现多次，只提取一次 |

> **🤔 导师提问**：`extractVariables` 用正则从模板字符串中提取变量名。这和 ASP.NET Core 中用反射从 `SendNotificationRequest.Model` 属性获取字段名是两种完全不同的思路。正则提取的局限是什么？如果用户写了 `{{ model.User.Name }}`（嵌套属性），当前正则能匹配到吗？

### 3.2 统一已选用户（跨 Tab 去重）

```typescript
interface SelectedEntry {
  userName: string
  displayName: string | null
  sources: string[]
}

const [selectedUsers, setSelectedUsers] = useState<Map<string, SelectedEntry>>(new Map())
```

⚠️ **【易错点】为什么用 `Map` 而不是数组？**
5 个 Tab（用户/角色/部门/公司/岗位）可能选出同一个人。`Map<userId, SelectedEntry>` 用 userId 去重，`sources` 数组记录这个用户来自哪些来源。

```
用户"张三"可能同时出现在：
- 按用户搜索：张三(用户搜索)
- 按角色 Admin：张三(角色:Admin)
- 按部门 技术部：张三(部门:技术部)

Map 中只有一条记录：
  key: "zhang-san-id"
  value: { userName: "张三", sources: ["用户搜索", "角色:Admin", "部门:技术部"] }
```

### 3.3 添加/移除用户

```typescript
const addUsers = (users: Array<{ id: string; userName: string; displayName?: string | null }>, source: string) => {
  setSelectedUsers(prev => {
    const next = new Map(prev)
    for (const u of users) {
      const existing = next.get(u.id)
      if (existing) {
        if (!existing.sources.includes(source)) existing.sources = [...existing.sources, source]
      } else {
        next.set(u.id, { userName: u.userName, displayName: u.displayName ?? null, sources: [source] })
      }
    }
    return next
  })
}

const removeSelectedUser = (userId: string) => {
  setSelectedUsers(prev => { const next = new Map(prev); next.delete(userId); return next })
}
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【易错点】** | `const next = new Map(prev)` | 必须创建新 Map！直接修改 `prev` 违反 React 不可变数据原则，状态不会触发重渲染 |
| **【设计取舍】** | `existing.sources = [...existing.sources, source]` | 同样要创建新数组！直接 `push` 是修改原数组 |

### 3.4 5 个收件人 Tab 的数据流

```
┌────────────────────────────────────────────────────────────────┐
│ 5 个收件人 Tab 的数据加载与解析                                  │
│                                                                │
│ Tab: 按用户                                                    │
│  搜索 → membersApi.searchSso(keyword)                          │
│  结果 → 直接 toggle 到 selectedUsers                            │
│                                                                │
│ Tab: 按角色                                                    │
│  加载 → ssoRolesApi.getList() → roleList                       │
│  选择 → checkedRoles (Set)                                     │
│  解析 → ssoRolesApi.getUsersInRole(roleId) → addUsers()        │
│                                                                │
│ Tab: 按部门                                                    │
│  加载 → orgUnitsApi.getTree() → orgTree                        │
│  选择 → checkedOrgUnits (Set)                                  │
│  解析 → orgUnitsApi.getUsers(unitId) → addUsers()              │
│                                                                │
│ Tab: 按公司                                                    │
│  加载 → companiesApi.getTree() → companyTree                   │
│  选择 → checkedCompanies (Set)                                 │
│  解析 → companiesApi.getUsers(companyId) → addUsers()          │
│                                                                │
│ Tab: 按岗位                                                    │
│  加载 → positionsApi.getList() → positionList                  │
│  选择 → checkedPositions (Set)                                 │
│  解析 → positionsApi.getUsers(positionId) → addUsers()         │
└────────────────────────────────────────────────────────────────┘
```

### 3.5 懒加载 Tab 数据

```typescript
useEffect(() => {
  if (!open) return
  if (recipientTab === "role" && roleList.length === 0) {
    setRoleLoading(true)
    ssoRolesApi.getList({ page: 1, pageSize: 100 }).then(res => {
      if (res.success && res.data) setRoleList(res.data.items ?? [])
    }).catch(() => toast.error("加载角色列表失败")).finally(() => setRoleLoading(false))
  }
  if (recipientTab === "dept" && orgTree.length === 0) {
    setOrgLoading(true)
    orgUnitsApi.getTree().then(res => { if (res.data) setOrgTree(res.data) })
      .catch(() => toast.error("加载部门树失败")).finally(() => setOrgLoading(false))
  }
  // ... company, position 同理
}, [open, recipientTab])
```

⚠️ **【性能陷阱】懒加载的关键**：只在 Tab 切换到对应面板时才加载数据。如果 5 个 Tab 的数据都初始化时加载，用户可能只用到"按用户"一个 Tab，白白发了 4 个 HTTP 请求。

条件判断 `roleList.length === 0`：已经加载过的不再重复加载。

### 3.6 树形复选框渲染

```typescript
function renderTreeCheckboxes<T extends { id: string; children?: T[] }>(
  nodes: T[],
  checked: Set<string>,
  setChecked: (fn: (prev: Set<string>) => Set<string>) => void,
  getLabel: (node: T) => string,
  depth = 0,
) {
  return nodes.map(node => (
    <div key={node.id}>
      <label className="flex items-center gap-2 px-3 py-1.5 hover:bg-muted/50 cursor-pointer"
        style={{ paddingLeft: `${12 + depth * 20}px` }}>
        <Checkbox checked={checked.has(node.id)} onCheckedChange={c => {
          setChecked(prev => { const next = new Set(prev); c ? next.add(node.id) : next.delete(node.id); return next })
        }} />
        <span className="text-sm truncate">{getLabel(node)}</span>
      </label>
      {node.children?.length && renderTreeCheckboxes(node.children, checked, setChecked, getLabel, depth + 1)}
    </div>
  ))
}
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【设计取舍】** | 泛型 `<T extends { id: string; children?: T[] }>` | 部门和公司都是树形结构，用泛型统一渲染逻辑，避免写两遍几乎相同的代码 |
| **【易错点】** | `depth * 20` 缩进 | 递归渲染树形结构时，用 `depth` 控制缩进层级。`depth=0` 是根节点，`depth=1` 是子节点，以此类推 |
| **【易错点】** | `c ? next.add(node.id) : next.delete(node.id)` | `onCheckedChange` 的参数 `c` 是 `boolean | "indeterminate"`。这里简化处理：truthy 添加，falsy 删除 |

### 3.7 部门解析：包含子部门

```typescript
const resolveOrgUnits = async () => {
  if (checkedOrgUnits.size === 0) return
  setOrgResolving(true)
  try {
    const allIds = orgIncludeChildren
      ? Array.from(checkedOrgUnits).flatMap(id => collectDescendantIds(orgTree, id))
      : Array.from(checkedOrgUnits)
    const uniqueIds = [...new Set(allIds)]
    const promises = uniqueIds.map(async unitId => {
      const res = await orgUnitsApi.getUsers(unitId)
      if (res.success && res.data) {
        const ids: string[] = res.data
        const users = ids.map(uid => ({ id: uid, userName: uid.slice(0, 8), displayName: null as string | null }))
        const unit = findTreeNode(orgTree, unitId)
        addUsers(users, `部门: ${unit?.displayName ?? unitId.slice(0, 8)}`)
      }
    })
    await Promise.all(promises)
    toast.success(`已解析 ${checkedOrgUnits.size} 个部门的用户`)
    setCheckedOrgUnits(new Set())
  } catch { toast.error("解析部门用户失败") }
  finally { setOrgResolving(false) }
}
```

逐行精讲：

| 标记 | 代码 | 解读 |
|------|------|------|
| **【设计取舍】** | `orgIncludeChildren` | 复选框"包含子部门"：勾选时选中部门下所有后代部门的用户，不勾选时只选当前部门 |
| **【易错点】** | `[...new Set(allIds)]` | 去重！包含子部门时，多个已选部门可能有共同的子部门。Set 自动去重，避免对同一部门调两次 `getUsers` |
| **【性能陷阱】** | `Promise.all(promises)` | 并发请求所有部门的用户列表。如果选了 50 个部门，会同时发 50 个请求。对于大规模使用，应该考虑批量接口 |

### 3.8 提交发送

```typescript
const handleSubmit = async () => {
  if (!activeTemplate) { toast.error("请选择通知模板"); return }
  const userIds = Array.from(selectedUsers.keys())
  if (userIds.length === 0) { toast.error("请选择收件人"); return }

  let model: Record<string, unknown> | undefined
  if (modelJson.trim()) {
    try { model = JSON.parse(modelJson) } catch { toast.error("模板变量 JSON 格式错误"); return }
  }

  setLoading(true)
  try {
    const req: SendNotificationRequest = {
      templateCode: activeTemplate.code,
      recipientUserIds: userIds,
      model,
      channels: selectedChannels.length > 0 ? selectedChannels : undefined,
      priority: priority as NotificationPriority,
    }
    const res = await sendNotification(req)
    const d = res.data
    if (d) toast.success(`发送完成：成功 ${d.successCount}，失败 ${d.failureCount}，跳过 ${d.skippedCount}`)
    onOpenChange(false)
    onSuccess()
  } catch { toast.error("发送失败") }
  finally { setLoading(false) }
}
```

⚠️ **【易错点】`channels: selectedChannels.length > 0 ? selectedChannels : undefined`**
- 如果选了渠道，传选中的渠道
- 如果没选任何渠道，传 `undefined`（不是空数组 `[]`！），让后端使用模板默认渠道

> **🤔 导师提问**：`channels` 为空时传 `undefined` 而非 `[]`，让后端使用模板默认渠道。在 ASP.NET Core 的 API 设计中，`null` 和空数组 `[]` 的语义通常是不同的。你怎么在 Swagger 文档中向调用者说明"不传 channels = 使用默认，传空数组 = 不发任何渠道"这种区别？

⚠️ **【易错点】`JSON.parse(modelJson)`**
模板变量由用户在 Textarea 里手动输入 JSON。这很容易格式错误（少个逗号、多个逗号、忘了引号...），必须 try-catch。

---

## 四、主页面：模板列表管理

### 4.1 编辑与发送的预加载

```typescript
const handleEdit = async (item: TemplateListDto) => {
  try {
    const res = await getTemplate(item.id)
    if (res.data) {
      setEditTemplate(res.data)
      setFormOpen(true)
    }
  } catch {
    toast.error("获取模板详情失败")
  }
}

const handleSend = async (item: TemplateListDto) => {
  try {
    const res = await getTemplate(item.id)
    if (res.data) {
      setSendTemplate(res.data)
      setSendOpen(true)
    }
  } catch {
    toast.error("获取模板详情失败")
  }
}
```

💡 **为什么列表点编辑/发送时，还要调 `getTemplate`？**
因为列表返回的是 `TemplateListDto`（只有基本信息），编辑/发送需要 `TemplateDetailDto`（包含各渠道模板内容）。这是一个**按需加载详情**的模式。

### 4.2 操作列的 DropdownMenu

```typescript
{
  field: "__actions__",
  headerName: "操作",
  width: 100,
  pinned: "right",
  cellRenderer: ({ data: row }: { data: TemplateListDto }) => (
    <DropdownMenu>
      <DropdownMenuTrigger className="text-xs text-muted-foreground hover:text-foreground">
        操作 ▾
      </DropdownMenuTrigger>
      <DropdownMenuContent>
        <DropdownMenuItem onClick={() => handleSend(row)}>
          <Send className="mr-2 h-3.5 w-3.5" />发送通知
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => handleEdit(row)}>
          <Pencil className="mr-2 h-3.5 w-3.5" />编辑
        </DropdownMenuItem>
        <DropdownMenuItem
          className="text-destructive"
          onClick={() => { setDeleteTarget(row); setDeleteOpen(true) }}
        >
          <Trash2 className="mr-2 h-3.5 w-3.5" />删除
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  ),
}
```

⚠️ **【设计取舍】操作列用 DropdownMenu 而非直接放按钮**
每行 3 个按钮会占太多空间。DropdownMenu 收起只显示"操作 ▾"，展开显示所有选项。删除按钮用 `text-destructive` 红色提示危险操作。

---

## 五、整体数据流图

```
用户操作                          状态变更                          API 调用
─────────                        ─────────                        ─────────
点击"新建模板"                    setEditTemplate(null)             无
                                  setFormOpen(true)

点击"编辑"                        getTemplate(id)                   GET /api/notification-templates/{id}
                                  setEditTemplate(detail)
                                  setFormOpen(true)

提交表单(新建)                    createTemplate(form)              POST /api/notification-templates
                                  onSuccess() → fetchData()

提交表单(编辑)                    updateTemplate(id, data)          PUT /api/notification-templates/{id}
                                  onSuccess() → fetchData()

点击"发送通知"                    getTemplate(id)                   GET /api/notification-templates/{id}
                                  setSendTemplate(detail)
                                  setSendOpen(true)

切Tab到"按角色"                   setRoleLoading(true)              GET /api/roles?page=1&pageSize=100
                                  setRoleList(items)

勾选角色 → 点击"添加"            resolveRoles()                    GET /api/roles/{id}/users (多个)
                                  addUsers(users, "角色:xxx")
                                  setCheckedRoles(new Set())

点击"发送"                        sendNotification(req)             POST /api/notifications/send
                                  toast.success(结果)
                                  onOpenChange(false)
```

---

## 踩坑提醒

1. **编辑模板时 `code` 和 `name` 字段必须 `disabled`**：这两个字段是模板的唯一标识，修改会导致已有的发送记录无法关联到正确模板
2. **`selectedUsers` 用 `Map` 而不是数组**：跨 Tab 选择用户时，`Map` 的 key 天然去重，避免同一用户被多次添加
3. **`extractVariables` 的正则要容错空格**：`{{ model.UserName }}` 和 `{{model.UserName}}` 都应该被匹配，正则中需要 `\s*` 容差
4. **5 个收件人 Tab 是懒加载**：只有切换到对应 Tab 时才加载数据，避免一次性发 5 个请求拖慢页面
5. **部门"包含子部门"需要递归收集**：`collectDescendantIds` 递归遍历部门树，收集所有后代部门 ID

---

## 验证步骤

在继续下一节之前，确认以下内容已经正确实现：

1. 导航到模板管理页，观察 AgTable 加载模板列表（预期：显示编码、名称、描述、状态、创建时间、操作列）
2. 点击"新建模板"，填写编码（如 `test.notice`）、名称和站内信模板内容 `{{ model.UserName }}，您有新消息`，点击保存（预期：创建成功，列表刷新显示新模板）
3. 重新点击编辑刚创建的模板，观察编码输入框是否为 disabled 状态（预期：编辑模式下编码和名称不可修改）
4. 在模板内容中输入 `{{ model.OrderNo }} 订单已发货`，保存后在发送通知对话框选择此模板，观察"模板变量"区域是否显示提取的变量名（预期：显示 `OrderNo`）
5. 点击操作列"发送通知"，选择收件人后提交（预期：toast 显示发送结果，包含成功/失败/跳过计数）

---

## 自测题

### 概念级（理解 Why）

1. 为什么编辑模板时 `code` 和 `name` 字段要 `disabled`？
2. `selectedUsers` 用 `Map` 而不是数组，主要原因是什么？

### 推理级（推导 What-if）

3. 如果 `extractVariables` 的正则只匹配 `{{model.UserName}}`（没有空格容差），哪些模板变量会被漏掉？
4. 用户在"按角色"Tab 选了 Admin 角色，点"添加选中角色的用户"。然后切到"按部门"Tab，选了技术部，又点"添加"。此时 `selectedUsers` 里有几个人？如果 Admin 角色和技术部有重叠用户呢？

### 动手级（代码实践）

5. 给"按用户"Tab 添加分页功能：当前 `searchSso` 返回最多 50 条，如果结果超过 50 条，显示"结果过多，请缩小搜索范围"的提示。
6. 实现模板预览功能：在 `TemplateFormDialog` 中添加"预览"按钮，输入模板变量后，用正则替换 `{{ model.xxx }}` 为实际值，展示渲染效果。

---

## 输出检查清单

- [ ] 理解 `TemplateFormDialog` 的新建/编辑模式切换逻辑
- [ ] 掌握通用 `set` 辅助函数的表单更新模式
- [ ] 理解 `UpdateTemplateRequest` 不含 `code`/`name` 的原因
- [ ] 知道 Scriban 模板语法和 `extractVariables` 的正则解析
- [ ] 掌握 `Map<userId, SelectedEntry>` 的跨 Tab 去重设计
- [ ] 理解 5 个收件人 Tab 的懒加载策略
- [ ] 掌握 `renderTreeCheckboxes` 的泛型递归渲染
- [ ] 理解部门"包含子部门"的 `collectDescendantIds` 逻辑
- [ ] 知道 `Promise.all` 并发请求和 `[...new Set()]` 去重
- [ ] 理解操作列 DropdownMenu 的设计取舍

---

[← 上一篇](05-通知列表页.md) | [下一篇 →](07-偏好设置.md)
