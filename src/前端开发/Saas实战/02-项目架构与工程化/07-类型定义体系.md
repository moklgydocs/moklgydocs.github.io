# 类型定义体系

## 本步目标

建立统一的 TypeScript 类型定义，理解前端类型系统和 .NET DTO 体系的对应关系，并验证 Vite 代理配置确保所有后端服务可达。

## 前置知识

### TypeScript 类型 vs C# DTO

| TypeScript | C# | 说明 |
|-----------|-----|------|
| `interface ApiResult<T>` | `class Result<T>` | 统一响应包装 |
| `interface PagedResult<T>` | `class PagedResult<T>` | 分页查询结果 |
| `interface CurrentUser` | `class CurrentUser` | 当前用户信息 |
| `interface MenuDto` | `class MenuDto` | 菜单传输对象 |
| `const MENU_TYPE` | `enum MenuType` | 枚举常量 |
| `type MenuType = typeof MENU_TYPE[keyof typeof MENU_TYPE]` | `enum MenuType` | 从常量对象派生联合类型 |

### 关键区别：TypeScript 类型在编译后被擦除

C# 的 DTO 类在运行时仍然存在——你可以用反射读取属性、序列化为 JSON、做数据验证。TypeScript 的接口只在编译时存在，编译为 JavaScript 后完全消失。这意味着：

1. TypeScript 接口不能做运行时验证——需要用 Zod 等库做 schema 验证
2. TypeScript 类型不需要考虑包体积——1000 个接口定义不会增加 JS 文件大小
3. 所以我们把所有类型放在一个 `types/index.ts` 文件中，而不是按模块拆分

> **.NET 类比**：就像 C# 的 `record` 类型——纯数据载体，没有行为。但 C# 的 record 在运行时仍然存在，TypeScript 的 interface 不存在。

## 代码实现

### src/types/index.ts — 通用基础类型

```typescript
// ─── 通用 API 响应 ───────────────────────────────────────────────────────────

/**
 * 统一 API 响应格式
 * @template T 响应数据类型
 */
export interface ApiResult<T = unknown> {
  /** 业务状态码，0 表示成功 */
  code: number
  /** 响应消息 */
  message: string
  /** 是否成功 */
  success: boolean
  /** 响应数据 */
  data?: T
}

/** 分页查询结果 */
export interface PagedResult<T> {
  /** 数据列表 */
  items: T[]
  /** 总记录数 */
  totalCount: number
  /** 当前页码 */
  page: number
  /** 每页条数 */
  pageSize: number
}
```

### src/types/index.ts — 当前用户与租户类型

```typescript
// ─── 当前登录用户 ────────────────────────────────────────────────────────────

/** 当前登录用户信息 */
export interface CurrentUser {
  /** 用户 ID */
  id: string
  /** 用户名 */
  userName: string
  /** 显示名称 */
  displayName: string
  /** 邮箱 */
  email: string
  /** 角色列表 */
  roles: string[]
  // 组织上下文（从 JWT claims 解析）
  /** 主公司 ID */
  primaryCompanyId?: string
  /** 主公司名称 */
  primaryCompanyName?: string
  /** 主公司编码 */
  primaryCompanyCode?: string
  /** 主组织单元（部门）ID */
  primaryOrgUnitId?: string
  /** 主组织单元名称 */
  primaryOrgUnitName?: string
  /** 主岗位 ID */
  primaryPositionId?: string
  /** 主岗位名称 */
  primaryPositionName?: string
  /** 主岗位编码 */
  primaryPositionCode?: string
  /** 工号 */
  employeeNo?: string
  /** 租户 ID */
  tenantId?: string
  /** 租户名称 */
  tenantName?: string
  /** 租户标识符 */
  tenantIdentifier?: string
}

/** 用户可用的租户信息 */
export interface UserTenantInfo {
  /** 租户 ID */
  tenantId: string
  /** 租户名称 */
  tenantName: string
  /** 租户标识符 */
  tenantIdentifier: string
}
```

### src/types/index.ts — SSO 认证模块类型

```typescript
// ===================== AuthServer (SSO) 模块类型 =====================

/** SSO 用户信息 */
export interface User {
  /** 用户 ID */
  id: string
  /** 用户名 */
  userName: string
  /** 邮箱 */
  email: string
  /** 显示名称 */
  displayName: string
  /** 头像 URL */
  avatar?: string
  /** 手机号 */
  phoneNumber?: string
  /** 是否启用 */
  isActive: boolean
  /** 角色列表 */
  roles: string[]
  /** 最后登录时间 */
  lastLoginAt?: string
  /** 创建时间 */
  createdAt?: string
}

/** 创建 SSO 用户请求 */
export interface CreateUserRequest {
  /** 用户名 */
  userName: string
  /** 邮箱 */
  email: string
  /** 显示名称 */
  displayName: string
  /** 密码 */
  password: string
  /** 角色列表 */
  roles: string[]
  /** 手机号 */
  phoneNumber?: string
  /** 关联的租户 ID 列表 */
  tenantIds?: string[]
}

/** 更新 SSO 用户请求 */
export interface UpdateUserRequest {
  /** 显示名称 */
  displayName?: string
  /** 邮箱 */
  email?: string
  /** 手机号 */
  phoneNumber?: string
  /** 是否启用 */
  isActive?: boolean
  /** 角色列表 */
  roles?: string[]
}

/** OAuth2 客户端信息 */
export interface Client {
  /** 客户端 ID（数据库主键） */
  id: string
  /** 客户端标识符 */
  clientId: string
  /** 显示名称 */
  displayName: string
  /** 客户端类型（confidential/public） */
  type: string
  /** 是否启用 */
  isEnabled: boolean
  /** 描述 */
  description?: string
  /** 分组 */
  group?: string
  /** Logo URI */
  logoUri?: string
  /** 允许的重定向 URI 列表 */
  redirectUris: string[]
  /** 登出后重定向 URI 列表 */
  postLogoutRedirectUris: string[]
  /** 权限列表 */
  permissions: string[]
  /** 授权类型列表 */
  grantTypes: string[]
  /** 允许的 Scope 列表 */
  scopes: string[]
  /** Access Token 有效期（分钟） */
  accessTokenLifetimeMinutes?: number
  /** 创建时间 */
  createdAt?: string
}

/** 创建 OAuth2 客户端请求 */
export interface CreateClientRequest {
  /** 客户端标识符 */
  clientId: string
  /** 显示名称 */
  displayName: string
  /** 客户端类型 */
  type: string
  /** 客户端密钥 */
  clientSecret?: string
  /** 描述 */
  description?: string
  /** 分组 */
  group?: string
  /** Logo URI */
  logoUri?: string
  /** 允许的重定向 URI 列表 */
  redirectUris: string[]
  /** 登出后重定向 URI 列表 */
  postLogoutRedirectUris: string[]
  /** 授权类型列表 */
  grantTypes: string[]
  /** 允许的 Scope 列表 */
  scopes: string[]
  /** Access Token 有效期（分钟） */
  accessTokenLifetimeMinutes?: number
}

/** 更新 OAuth2 客户端请求 */
export interface UpdateClientRequest {
  /** 显示名称 */
  displayName?: string
  /** 描述 */
  description?: string
  /** 分组 */
  group?: string
  /** Logo URI */
  logoUri?: string
  /** 是否启用 */
  isEnabled?: boolean
  /** 允许的重定向 URI 列表 */
  redirectUris?: string[]
  /** 登出后重定向 URI 列表 */
  postLogoutRedirectUris?: string[]
  /** 授权类型列表 */
  grantTypes?: string[]
  /** 允许的 Scope 列表 */
  scopes?: string[]
  /** Access Token 有效期（分钟） */
  accessTokenLifetimeMinutes?: number
}

/** API Scope 信息 */
export interface Scope {
  /** Scope ID */
  id: string
  /** Scope 名称 */
  name: string
  /** 显示名称 */
  displayName: string
  /** 描述 */
  description?: string
  /** 关联的资源列表 */
  resources: string[]
}

/** 创建 Scope 请求 */
export interface CreateScopeRequest {
  /** Scope 名称 */
  name: string
  /** 显示名称 */
  displayName: string
  /** 描述 */
  description?: string
  /** 关联的资源列表 */
  resources: string[]
}

/** 更新 Scope 请求 */
export interface UpdateScopeRequest {
  /** 显示名称 */
  displayName?: string
  /** 描述 */
  description?: string
  /** 关联的资源列表 */
  resources?: string[]
}

/** SSO 角色信息 */
export interface Role {
  /** 角色 ID */
  id: string
  /** 角色名称 */
  name: string
  /** 描述 */
  description?: string
  /** 是否系统角色（不可删除） */
  isSystem: boolean
  /** 角色下的用户数 */
  userCount: number
  /** 创建时间 */
  createdAt?: string
}

/** 角色关联的用户摘要 */
export interface RoleUserDto {
  /** 用户 ID */
  id: string
  /** 用户名 */
  userName: string
  /** 显示名称 */
  displayName?: string
  /** 邮箱 */
  email?: string
  /** 是否启用 */
  isActive: boolean
}

/** 角色详情（含用户列表） */
export interface RoleDetail extends Role {
  /** 角色下的用户列表 */
  users: User[]
}
```

### src/types/index.ts — 权限中心模块类型

```typescript
// ===================== PermCenter 模块类型 =====================

/** 权限中心应用 DTO */
export interface AppDto {
  /** 应用 ID */
  id: string
  /** 应用编码 */
  code: string
  /** 应用名称 */
  name: string
  /** 描述 */
  description: string | null
  /** Logo URL */
  logoUrl: string | null
  /** 是否启用 */
  isEnabled: boolean
  /** 排序号 */
  sort: number
}

/** 创建应用请求 */
export interface CreateAppRequest {
  /** 应用编码 */
  code: string
  /** 应用名称 */
  name: string
  /** 描述 */
  description?: string
  /** Logo URL */
  logoUrl?: string
  /** 排序号 */
  sort: number
}

/** 更新应用请求 */
export interface UpdateAppRequest {
  /** 应用名称 */
  name: string
  /** 描述 */
  description?: string
  /** Logo URL */
  logoUrl?: string
  /** 是否启用 */
  isEnabled: boolean
  /** 排序号 */
  sort: number
}

/** 权限定义 DTO（支持树形嵌套） */
export interface PermissionDefinitionDto {
  /** 权限 ID */
  id: string
  /** 权限名称 */
  name: string
  /** 显示名称 */
  displayName: string
  /** 父权限 ID */
  parentId: string | null
  /** 权限类型 */
  type: number
  /** 是否启用 */
  isEnabled: boolean
  /** 排序号 */
  sort: number
  /** 子权限列表 */
  children: PermissionDefinitionDto[]
}

/** 权限分组 DTO */
export interface PermissionGroupDto {
  /** 分组 ID */
  id: string
  /** 分组编码 */
  code: string
  /** 显示名称 */
  displayName: string
  /** 排序号 */
  sort: number
  /** 分组下的权限定义列表 */
  permissions: PermissionDefinitionDto[]
}

/** 创建权限分组请求 */
export interface CreatePermissionGroupRequest {
  /** 应用编码 */
  appCode: string
  /** 分组编码 */
  code: string
  /** 显示名称 */
  displayName: string
  /** 排序号 */
  sort: number
}

/** 更新权限分组请求 */
export interface UpdatePermissionGroupRequest {
  /** 显示名称 */
  displayName: string
  /** 排序号 */
  sort: number
}

/** 创建权限定义请求 */
export interface CreatePermissionDefinitionRequest {
  /** 分组编码 */
  groupCode: string
  /** 应用编码 */
  appCode: string
  /** 权限名称 */
  name: string
  /** 显示名称 */
  displayName: string
  /** 权限类型 */
  type: number
  /** 排序号 */
  sort: number
  /** 父权限 ID */
  parentId?: string
}

/** 更新权限定义请求 */
export interface UpdatePermissionDefinitionRequest {
  /** 显示名称 */
  displayName: string
  /** 权限类型 */
  type: number
  /** 是否启用 */
  isEnabled: boolean
  /** 排序号 */
  sort: number
}

/** 权限中心角色 DTO */
export interface RoleDto {
  /** 角色 ID */
  id: string
  /** 所属应用 ID */
  appId: string
  /** 角色编码 */
  code: string
  /** 角色名称 */
  name: string
  /** 描述 */
  description: string | null
  /** 是否默认角色 */
  isDefault: boolean
  /** 是否静态角色（系统内置） */
  isStatic: boolean
  /** 是否公开角色 */
  isPublic: boolean
  /** 是否启用 */
  isEnabled: boolean
  /** 排序号 */
  sort: number
}

/** 创建角色请求 */
export interface CreateRoleRequest {
  /** 应用编码 */
  appCode: string
  /** 角色编码 */
  code: string
  /** 角色名称 */
  name: string
  /** 描述 */
  description?: string
  /** 是否公开 */
  isPublic: boolean
  /** 排序号 */
  sort: number
}

/** 更新角色请求 */
export interface UpdateRoleRequest {
  /** 角色名称 */
  name: string
  /** 描述 */
  description?: string
  /** 是否公开 */
  isPublic: boolean
  /** 是否启用 */
  isEnabled: boolean
  /** 排序号 */
  sort: number
}

/** 设置角色权限请求 */
export interface SetRolePermissionsRequest {
  /** 权限编码列表 */
  permissions: string[]
}
```

### src/types/index.ts — 菜单类型

```typescript
// ===================== Menu 菜单模块类型 =====================

/** 菜单类型枚举 */
export const MENU_TYPE = {
  Directory: 0,
  Menu: 1,
  Button: 2,
  ExternalLink: 3,
} as const
export type MenuType = (typeof MENU_TYPE)[keyof typeof MENU_TYPE]

export const MENU_TYPE_LABELS: Record<MenuType, string> = {
  [MENU_TYPE.Directory]: "目录",
  [MENU_TYPE.Menu]: "菜单",
  [MENU_TYPE.Button]: "按钮",
  [MENU_TYPE.ExternalLink]: "外链",
}

/** 菜单 DTO（支持树形嵌套） */
export interface MenuDto {
  /** 菜单 ID */
  id: string
  /** 菜单名称 */
  name: string
  /** 菜单编码（同一应用内唯一） */
  code: string
  /** 前端路由路径 */
  path: string | null
  /** 图标名称（lucide icon） */
  icon: string | null
  /** 菜单类型 */
  type: MenuType
  /** 关联的权限名称 */
  permissionName: string | null
  /** 父级菜单 ID */
  parentId: string | null
  /** 是否可见 */
  isVisible: boolean
  /** 是否启用 */
  isEnabled: boolean
  /** 排序号 */
  sort: number
  /** 子菜单列表 */
  children: MenuDto[]
}

/** 创建菜单请求 */
export interface CreateMenuRequest {
  appCode: string
  name: string
  code: string
  path?: string
  icon?: string
  type: MenuType
  permissionName?: string
  parentId?: string
  sort: number
}

/** 更新菜单请求 */
export interface UpdateMenuRequest {
  name: string
  path?: string
  icon?: string
  type: MenuType
  permissionName?: string
  parentId?: string
  isVisible: boolean
  isEnabled: boolean
  sort: number
}

/** 移动菜单请求 */
export interface MoveMenuRequest {
  newParentId: string | null
}

/** 排序项 */
export interface MenuItemSort {
  id: string
  sort: number
}

/** 批量排序请求 */
export interface SortMenuRequest {
  items: MenuItemSort[]
}

/** 切换状态请求 */
export interface ToggleMenuRequest {
  field: "IsVisible" | "IsEnabled"
  value: boolean
}
```

### Vite 代理配置验证（vite.config.ts 节选）

```typescript
// vite.config.ts 中的 server.proxy 配置
export default defineConfig({
  // ...
  server: {
    port: 3000,
    proxy: {
      "/sso-api": {
        target: "http://moklgy.me:10001",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/sso-api/, ""),
      },
      "/perm-api": {
        target: "http://moklgy.me:10002",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/perm-api/, ""),
      },
      "/gw-api": {
        target: "http://moklgy.me:10000",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/gw-api/, ""),
      },
      "/connect": {
        target: "http://moklgy.me:10001",
        changeOrigin: true,
      },
      "/.well-known": {
        target: "http://moklgy.me:10001",
        changeOrigin: true,
      },
      "/file-api": {
        target: "http://moklgy.me:10011",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/file-api/, ""),
      },
      "/print-api": {
        target: "http://moklgy.me:11010",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/print-api/, ""),
      },
      "/prt": {
        target: "http://moklgy.me:11010",
        changeOrigin: true,
      },
      "/_fr": {
        target: "http://moklgy.me:11010",
        changeOrigin: true,
      },
      "/miniapp-api": {
        target: "http://moklgy.me:5020",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/miniapp-api/, ""),
      },
      "/notify-api": {
        target: "http://moklgy.me:10012",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/notify-api/, ""),
      },
      "/ops-api": {
        target: "http://moklgy.me:10013",
        changeOrigin: true,
        rewrite: (p) => p.replace(/^\/ops-api/, ""),
      },
      "/hubs": {
        target: "http://moklgy.me:10012",
        changeOrigin: true,
        ws: true,
      },
    },
  },
})
```

## 代码讲解

### ApiResult\<T\> — 统一响应格式

```typescript
export interface ApiResult<T = unknown> {
  code: number       // 业务状态码，0 = 成功
  message: string    // 响应消息
  success: boolean   // 是否成功
  data?: T           // 响应数据
}
```

这是所有 API 请求的统一返回格式。`createHttp` 的 `get/post/put/del` 方法都返回 `Promise<ApiResult<T>>`，调用方通过 `res.data` 获取实际数据。

> **.NET 类比**：这就像 .NET 的 `Result<T>` 模式——`Success` 对应 `success: true`，`Data` 对应 `data`，`ErrorMessage` 对应 `message`。区别在于 .NET 的 Result 通常用 OneOf/ discriminated union 实现，而前端用 `{ success, data }` 扁平结构。

### PagedResult\<T\> — 分页查询

```typescript
export interface PagedResult<T> {
  items: T[]         // 当前页数据
  totalCount: number // 总记录数
  page: number       // 当前页码
  pageSize: number   // 每页条数
}
```

> **.NET 类比**：完全对应 .NET 的 `PagedResult<T>` / `PaginatedList<T>`。`page` 对应 `PageIndex`，`pageSize` 对应 `PageSize`，`totalCount` 对应 `TotalCount`。

### MENU_TYPE — 从常量对象派生联合类型

```typescript
export const MENU_TYPE = {
  Directory: 0,
  Menu: 1,
  Button: 2,
  ExternalLink: 3,
} as const

export type MenuType = (typeof MENU_TYPE)[keyof typeof MENU_TYPE]
// 等价于: type MenuType = 0 | 1 | 2 | 3
```

为什么不直接用 TypeScript 的 `enum`？

1. **TypeScript enum 的坑**：数字枚举会产生反向映射（`MENU_TYPE[0] = "Directory"`），增加运行时代码
2. **`as const` 常量对象**：零运行时开销，TypeScript 自动推导字面量类型
3. **与后端对齐**：后端返回的是数字（0/1/2/3），常量对象的值也是数字，天然对应

> **.NET 类比**：C# 的 `enum MenuType { Directory = 0, Menu = 1, Button = 2, ExternalLink = 3 }`。TypeScript 用 `as const` 对象 + 派生类型实现了相同的效果，但没有反向映射的运行时开销。

### CurrentUser — JWT Claims 的前端映射

`CurrentUser` 接口中的组织上下文字段（`primaryCompanyName`、`primaryOrgUnitName` 等）来自 JWT Token 的 Claims。登录后前端解码 JWT，提取这些 Claims 填充到 `CurrentUser` 对象中。

> **.NET 类比**：这就像 `HttpContext.User.Claims` 的映射——后端从 JWT 中读取 Claims，填充到 `CurrentUser` DTO 中返回给前端。

### MenuDto 的树形嵌套

```typescript
export interface MenuDto {
  id: string
  name: string
  children: MenuDto[]  // ← 递归引用自身
}
```

`MenuDto` 通过 `children: MenuDto[]` 实现无限层级的树形结构。后端返回的是扁平列表，前端需要自己构建树（或者后端直接返回树形结构）。

> **.NET 类比**：这和 .NET 中 `List<MenuDto>` 的树形结构完全相同——`Children` 属性引用自身类型。

### Vite Proxy — 请求路由映射

```
浏览器请求                   Vite Proxy              后端服务
───────────                 ─────────              ────────
/sso-api/users     ──→     :10001/users     (SSO 服务)
/perm-api/roles    ──→     :10002/roles     (权限中心)
/gw-api/routes    ──→     :10000/routes    (API 网关)
/file-api/files   ──→     :10011/files     (文件服务)
/print-api/jobs   ──→     :11010/jobs      (打印服务)
/notify-api/...   ──→     :10012/...       (通知中心)
/ops-api/...      ──→     :10013/...       (租户运营)
/connect/token    ──→     :10001/connect/token  (OIDC 端点，不 rewrite)
/hubs/notify      ──→     :10012/hubs/notify   (SignalR，ws: true)
```

`rewrite` 函数的作用是去掉 URL 中的前缀。例如 `/sso-api/users` → `http://moklgy.me:10001/users`（去掉了 `/sso-api`）。但 `/connect/token` 不需要 rewrite，因为后端的 OIDC 端点路径就是 `/connect/token`。

## 踩坑提醒

1. **`as const` 不能漏**。如果写成 `{ Directory: 0, Menu: 1 }`（不加 `as const`），TypeScript 会把类型推导为 `{ Directory: number, Menu: number }`，`MenuType` 就变成了 `number` 而不是 `0 | 1 | 2 | 3`，失去类型安全。

2. **`ApiResult<T>` 的 `data` 是可选的（`data?: T`）**。某些 API（如删除操作）不返回 data，只有 `{ success: true, code: 0, message: "ok" }`。调用方需要处理 `data` 可能为 `undefined` 的情况。

3. **Vite Proxy 只在开发环境生效**。生产环境中前端是静态文件，不经过 Vite Dev Server，需要 Nginx 或其他反向代理做相同的路由转发。部署时必须配置 Nginx 的 `location` 块，规则与 Vite proxy 完全一致。

4. **`/connect` 和 `/.well-known` 不做 rewrite**。这两个路径是 OIDC 标准端点，后端 OpenIddict 直接监听 `/connect/token`、`/.well-known/openid-configuration` 等路径。如果做了 rewrite（如去掉前缀），后端会 404。

5. **SignalR 的 `/hubs` 需要 `ws: true`**。默认情况下 Vite proxy 不代理 WebSocket 请求。SignalR 使用 WebSocket 传输，必须设置 `ws: true`。

## 自测题

### 入门题

1. `ApiResult<T>` 中 `code` 和 `success` 有什么区别？既然 `code === 0` 就表示成功，为什么还需要 `success` 字段？

2. `MENU_TYPE` 为什么用 `as const` 常量对象而不是 TypeScript 的 `enum`？

### 进阶题

3. 请解释 `type MenuType = (typeof MENU_TYPE)[keyof typeof MENU_TYPE]` 这个类型推导的过程。如果 `MENU_TYPE` 的值是字符串而非数字（如 `{ Directory: "directory" }`），类型推导的结果是什么？

4. Vite proxy 配置中，`/connect` 和 `/sso-api` 都代理到同一个后端（10001 端口），但 `/connect` 不做 rewrite 而 `/sso-api` 要做 rewrite。请解释原因。

### 架构题

5. 当前所有类型定义都在 `types/index.ts` 一个文件中（2300+ 行）。有人建议按模块拆分为 `types/auth.ts`、`types/perm.ts`、`types/menu.ts` 等。请分析两种方案的优缺点，并给出你的选择和理由。

6. `ApiResult<T>` 是前端的统一响应格式，但并非所有后端 API 都遵守这个格式（如 OIDC 端点返回原生格式）。`createHttp` 的响应拦截器会自动包裹非标准响应。请评价这个设计的优缺点——如果让你重新设计，你会怎么处理响应格式不一致的问题？

## 下一步

[← 上一篇：状态管理](./06-状态管理.md) | [下一篇：SSO单点登录 →](../03-SSO单点登录/01-OAuth2与JWT基础.md)
