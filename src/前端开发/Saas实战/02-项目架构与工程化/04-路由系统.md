# 路由系统

> **这一步解决什么问题？**
> SPA 应用的页面跳转不是靠后端路由，而是靠前端路由。本文将实现 React Router v7 的路由配置，包含懒加载、嵌套路由、路由守卫和默认重定向——让应用知道"访问 /auth/users 时该渲染什么组件"。
>
> 对于 ASP.NET Core 开发者来说，前端路由就是 `app.MapGet()` 的客户端版本——`<Route path="/users" />` 对应 `MapGet("/users", ...)`，`<ProtectedRoute>` 对应 `[Authorize]` 特性，`<Outlet />` 对应 `@RenderBody()`。

## 前置知识

### React Router vs ASP.NET Core Endpoint Routing

| 概念 | React Router | ASP.NET Core |
|------|-------------|-------------|
| 路由定义 | `<Route path="/users" />` | `app.MapGet("/users", ...)` |
| 路由守卫 | `<ProtectedRoute>` 包裹 | `[Authorize]` 特性 |
| 嵌套路由 | `<Route>` 嵌套 `<Route>` | Area / RouteGroup |
| 布局复用 | `<Outlet />` 渲染子路由 | `@RenderBody()` |
| 懒加载 | `lazy(() => import(...))` | Assembly.Load() 按需加载 |
| 重定向 | `<Navigate to="/xxx" replace />` | `RedirectResult` |

### 懒加载的必要性

SPA 打包后所有页面代码都在一个 JS 文件中，首屏加载很慢。`React.lazy()` + `import()` 实现按需加载——用户访问 `/auth/users` 时才下载用户管理页面的代码。

> **.NET 类比**：就像 ASP.NET Core 的 Assembly.Load() 按需加载插件程序集。用户访问某个功能时才加载对应的 DLL，而不是启动时加载所有 DLL。

## 代码实现

### src/components/layout/protected-route.tsx

```typescript
import { Navigate, useLocation } from "react-router-dom"
import { useAuthStore } from "@/stores/auth-store"

/**
 * 路由保护组件
 *
 * 包裹需要认证才能访问的路由，未登录用户会被重定向到登录页。
 * 对于多租户用户，若尚未选择租户，则引导至租户选择页。
 *
 * @param children - 受保护的子组件
 */
export function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const isAuthenticated = useAuthStore((s) => s.isAuthenticated)
  const currentTenantId = useAuthStore((s) => s.currentTenantId)
  const availableTenants = useAuthStore((s) => s.availableTenants)
  const location = useLocation()

  // 未认证 → 跳转登录页，保留来源路径以便登录后回跳
  if (!isAuthenticated) {
    return <Navigate to="/login" state={{ from: location }} replace />
  }

  // 多租户用户尚未选择租户 → 引导到租户选择页
  if (availableTenants && availableTenants.length > 1 && !currentTenantId) {
    return <Navigate to="/select-tenant" replace />
  }

  return <>{children}</>
}
```

### src/App.tsx

```typescript
import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom"
import { Toaster } from "sonner"
import { lazy, Suspense } from "react"
import { AppLayout } from "@/components/layout/app-layout"
import { ProtectedRoute } from "@/components/layout/protected-route"

// ─── 公共页面（首屏需要，保持静态导入） ─────────────────────
import LoginPage from "@/pages/login"
import SelectTenantPage from "@/pages/select-tenant"

// ─── 路由级懒加载（按模块拆分 chunk） ─────────────────────────
const DashboardPage = lazy(() => import("@/pages/dashboard"))

// SSO 认证中心
const AuthUsersPage = lazy(() => import("@/pages/auth/users"))
const AuthClientsPage = lazy(() => import("@/pages/auth/clients"))
const AuthScopesPage = lazy(() => import("@/pages/auth/scopes"))
const AuthRolesPage = lazy(() => import("@/pages/auth/roles"))

// 权限中心
const PermAppsPage = lazy(() => import("@/pages/perm/apps"))
const PermPermissionsPage = lazy(() => import("@/pages/perm/permissions"))
const PermRolesPage = lazy(() => import("@/pages/perm/roles"))
const PermTenantsPage = lazy(() => import("@/pages/perm/tenants"))
const PermUserRolesPage = lazy(() => import("@/pages/perm/user-roles"))
const PermOrganizationPage = lazy(() => import("@/pages/perm/organization"))
const PermPositionsPage = lazy(() => import("@/pages/perm/positions"))
const PermMembersPage = lazy(() => import("@/pages/perm/members"))

// 网关管理
const GatewayClustersPage = lazy(() => import("@/pages/gateway/clusters"))
const GatewayRoutesPage = lazy(() => import("@/pages/gateway/routes"))
const GatewayRateLimitPage = lazy(() => import("@/pages/gateway/rate-limit-policies"))
const GatewayDashboardPage = lazy(() => import("@/pages/gateway/dashboard"))

// 文件服务
const FileManagerPage = lazy(() => import("@/pages/file"))
const FileRecycleBinPage = lazy(() => import("@/pages/file/recycle-bin"))
const FileQuotaPage = lazy(() => import("@/pages/file/quota"))
const FileAccessControlPage = lazy(() => import("@/pages/file/access-control"))
const FileTenantStatsPage = lazy(() => import("@/pages/file/tenant-stats"))
const FileMultipartPage = lazy(() => import("@/pages/file/multipart"))

// 打印服务
const PrintTemplatesPage = lazy(() => import("@/pages/print/templates"))
const PrintJobsPage = lazy(() => import("@/pages/print/jobs"))
const PrintDatasourcesPage = lazy(() => import("@/pages/print/datasources"))

// 通知中心
const NotificationsPage = lazy(() => import("@/pages/notifications"))
const NotificationTemplatesPage = lazy(() => import("@/pages/notifications/templates"))
const NotificationPreferencesPage = lazy(() => import("@/pages/notifications/preferences"))

// 配置中心
const ConfigPage = lazy(() => import("@/pages/config"))

// 租户运营中心
const OpsOverviewPage = lazy(() => import("@/pages/ops/overview"))
const OpsEditionsPage = lazy(() => import("@/pages/ops/editions"))
const OpsTenantsPage = lazy(() => import("@/pages/ops/tenants"))
const OpsTenantDetailPage = lazy(() => import("@/pages/ops/tenants/detail"))
const OpsWebhooksPage = lazy(() => import("@/pages/ops/webhooks"))
const OpsWorkflowPage = lazy(() => import("@/pages/ops/workflow"))
const OpsDataPage = lazy(() => import("@/pages/ops/data"))
const OpsMfaPage = lazy(() => import("@/pages/ops/mfa"))
const OpsFeaturesPage = lazy(() => import("@/pages/ops/features"))
const OpsSearchPage = lazy(() => import("@/pages/ops/search"))

/** 页面懒加载 fallback */
function PageLoader() {
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      加载中...
    </div>
  )
}

/** 包裹懒加载组件，统一 Suspense */
function Lazy({ children }: { children: React.ReactNode }) {
  return <Suspense fallback={<PageLoader />}>{children}</Suspense>
}

/**
 * 应用根组件，定义全局路由表。
 *
 * 路由结构：
 * - /login            → 公开登录页
 * - /select-tenant    → 多租户选择页
 * - /                 → 受保护路由（需认证 + AppLayout 侧边栏布局）
 *   - /auth/*         → SSO 认证中心（用户/客户端/Scope/角色）
 *   - /perm/*         → 权限中心（应用/权限/角色/租户/组织/岗位/成员）
 *   - /gateway/*      → 网关管理（仪表盘/集群/路由/限流策略）
 *   - /file/*         → 文件服务（管理/回收站/配额/访问控制/租户统计/分片上传）
 *   - /notifications/*→ 通知中心（列表/模板/偏好设置）
 *   - /print/*        → 打印服务（模板/数据源/任务）
 *   - /config/*       → 配置中心（各服务的 Consul 配置）
 */
export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* 登录 */}
        <Route path="/login" element={<LoginPage />} />
        {/* 多租户用户登录后选择租户 */}
        <Route path="/select-tenant" element={<SelectTenantPage />} />

        {/* 受保护路由：统一 Layout */}
        <Route
          element={
            <ProtectedRoute>
              <AppLayout />
            </ProtectedRoute>
          }
        >
          {/* 主仪表盘 */}
          <Route index element={<Lazy><DashboardPage /></Lazy>} />

          {/* SSO 认证中心 /auth/* */}
          <Route path="auth">
            <Route index element={<Navigate to="/auth/users" replace />} />
            <Route path="users" element={<Lazy><AuthUsersPage /></Lazy>} />
            <Route path="clients" element={<Lazy><AuthClientsPage /></Lazy>} />
            <Route path="scopes" element={<Lazy><AuthScopesPage /></Lazy>} />
            <Route path="roles" element={<Lazy><AuthRolesPage /></Lazy>} />
          </Route>

          {/* 权限中心 /perm/* */}
          <Route path="perm">
            <Route index element={<Navigate to="/perm/apps" replace />} />
            <Route path="apps" element={<Lazy><PermAppsPage /></Lazy>} />
            <Route path="permissions" element={<Lazy><PermPermissionsPage /></Lazy>} />
            <Route path="roles" element={<Lazy><PermRolesPage /></Lazy>} />
            <Route path="tenants" element={<Lazy><PermTenantsPage /></Lazy>} />
            <Route path="user-roles" element={<Lazy><PermUserRolesPage /></Lazy>} />
            <Route path="organization" element={<Lazy><PermOrganizationPage /></Lazy>} />
            <Route path="positions" element={<Lazy><PermPositionsPage /></Lazy>} />
            <Route path="members" element={<Lazy><PermMembersPage /></Lazy>} />
          </Route>

          {/* 网关管理 /gateway/* */}
          <Route path="gateway">
            <Route index element={<Lazy><GatewayDashboardPage /></Lazy>} />
            <Route path="clusters" element={<Lazy><GatewayClustersPage /></Lazy>} />
            <Route path="routes" element={<Lazy><GatewayRoutesPage /></Lazy>} />
            <Route path="rate-limit-policies" element={<Lazy><GatewayRateLimitPage /></Lazy>} />
          </Route>

          {/* 文件服务 /file/* */}
          <Route path="file">
            <Route index element={<Lazy><FileManagerPage /></Lazy>} />
            <Route path="recycle-bin" element={<Lazy><FileRecycleBinPage /></Lazy>} />
            <Route path="quota" element={<Lazy><FileQuotaPage /></Lazy>} />
            <Route path="access-control" element={<Lazy><FileAccessControlPage /></Lazy>} />
            <Route path="tenant-stats" element={<Lazy><FileTenantStatsPage /></Lazy>} />
            <Route path="multipart" element={<Lazy><FileMultipartPage /></Lazy>} />
          </Route>

          {/* 通知中心 /notifications/* */}
          <Route path="notifications">
            <Route index element={<Lazy><NotificationsPage /></Lazy>} />
            <Route path="templates" element={<Lazy><NotificationTemplatesPage /></Lazy>} />
            <Route path="preferences" element={<Lazy><NotificationPreferencesPage /></Lazy>} />
          </Route>

          {/* 打印服务 /print/* */}
          <Route path="print">
            <Route index element={<Navigate to="/print/templates" replace />} />
            <Route path="templates" element={<Lazy><PrintTemplatesPage /></Lazy>} />
            <Route path="datasources" element={<Lazy><PrintDatasourcesPage /></Lazy>} />
            <Route path="jobs" element={<Lazy><PrintJobsPage /></Lazy>} />
          </Route>

          {/* 配置中心 /config/*：各微服务的运行时配置 */}
          <Route path="config">
            <Route index element={<Navigate to="/config/perm-center" replace />} />
            <Route path="perm-center" element={<Lazy><ConfigPage serviceBaseUrl="/perm-api" serviceLabel="权限中心" /></Lazy>} />
            <Route path="file-service" element={<Lazy><ConfigPage serviceBaseUrl="/file-api" serviceLabel="文件服务" /></Lazy>} />
            <Route path="print-service" element={<Lazy><ConfigPage serviceBaseUrl="/print-api" serviceLabel="打印服务" /></Lazy>} />
            <Route path="miniapp-platform" element={<Lazy><ConfigPage serviceBaseUrl="/miniapp-api" serviceLabel="小程序平台" /></Lazy>} />
            <Route path="notification-center" element={<Lazy><ConfigPage serviceBaseUrl="/notify-api" serviceLabel="通知中心" /></Lazy>} />
          </Route>

          {/* 租户运营中心 /ops/* */}
          <Route path="ops">
            <Route index element={<Navigate to="/ops/overview" replace />} />
            <Route path="overview" element={<Lazy><OpsOverviewPage /></Lazy>} />
            <Route path="editions" element={<Lazy><OpsEditionsPage /></Lazy>} />
            <Route path="tenants" element={<Lazy><OpsTenantsPage /></Lazy>} />
            <Route path="tenants/:id" element={<Lazy><OpsTenantDetailPage /></Lazy>} />
            <Route path="webhooks" element={<Lazy><OpsWebhooksPage /></Lazy>} />
            <Route path="workflow" element={<Lazy><OpsWorkflowPage /></Lazy>} />
            <Route path="data" element={<Lazy><OpsDataPage /></Lazy>} />
            <Route path="mfa" element={<Lazy><OpsMfaPage /></Lazy>} />
            <Route path="features" element={<Lazy><OpsFeaturesPage /></Lazy>} />
            <Route path="search" element={<Lazy><OpsSearchPage /></Lazy>} />
          </Route>
        </Route>

        {/* 未匹配路由 → 重定向到主页 */}
        <Route path="*" element={<Navigate to="/" replace />} />
      </Routes>

      {/* 全局 Toast 通知：右上角显示，支持富文本颜色和关闭按钮 */}
      <Toaster position="top-right" richColors closeButton />
    </BrowserRouter>
  )
}
```

## 代码讲解

### 路由结构的四层嵌套

```
BrowserRouter（路由根容器）
  ├── /login           → 公开页面（无布局、无守卫）
  ├── /select-tenant   → 公开页面（无布局、无守卫）
  ├── 受保护路由        → ProtectedRoute + AppLayout
  │   ├── /            → DashboardPage（index 路由）
  │   ├── /auth/*      → 认证中心子路由
  │   ├── /perm/*      → 权限中心子路由
  │   └── ...          → 其他模块子路由
  └── /*               → 404 兜底，重定向到首页
```

> **.NET 类比**：
> - `BrowserRouter` = `app.MapControllers()`
> - `ProtectedRoute` = `[Authorize]` 特性
> - `AppLayout` + `<Outlet />` = `_Layout.cshtml` + `@RenderBody()`
> - `<Route path="*" />` = `app.UseStatusCodePagesWithReExecute("/")`

### 为什么登录页和租户选择页是静态导入？

```typescript
// 静态导入——这些页面是首屏必须的
import LoginPage from "@/pages/login"
import SelectTenantPage from "@/pages/select-tenant"

// 懒加载——其他页面按需加载
const DashboardPage = lazy(() => import("@/pages/dashboard"))
```

登录页是用户看到的第一个页面，如果懒加载它，用户会看到一个"加载中..."的白屏闪烁。租户选择页同理——用户登录后立即需要选择租户。其他页面可以懒加载，因为用户不会在页面加载的瞬间同时访问所有功能。

> **【性能陷阱】** `React.lazy()` 会在首次渲染时触发网络请求下载 chunk。如果 10 个懒加载组件在首屏同时渲染（如仪表盘页面加载了 10 个子模块），浏览器会同时发起 10 个请求。虽然 HTTP/2 支持多路复用，但过多的并发请求仍可能导致首屏卡顿。Vite 默认的代码分割策略是每个动态 `import()` 生成一个 chunk，你可以通过 `vite.config.ts` 的 `build.rollupOptions.output.manualChunks` 合并相关模块。

> **🤔 导师提问**：假设你的 SPA 有 100 个页面，其中 90 个是管理后台页面，只有 10 个是用户常用的。如果把所有页面都懒加载，用户每次点新菜单都要等一次网络请求。你会如何平衡"首屏加载速度"和"页面切换速度"？

### 嵌套路由中的 `<Outlet />` 是什么？

`<Outlet />` 是 React Router 的"插槽"——它标记了子路由应该渲染的位置。当用户访问 `/auth/users` 时，渲染流程是：

```
AppLayout
  ├── AppSidebar（侧边栏，始终显示）
  ├── AppHeader（顶栏，始终显示）
  └── <main>
       └── <Outlet />  ← 这里渲染 AuthUsersPage
      </main>
```

> **.NET 类比**：`<Outlet />` = `@RenderBody()`。`AppLayout` = `_Layout.cshtml`，子页面 = 各个 View。

### 默认重定向的三种模式

**模式 1：`<Route index>` 指向具体页面**

```typescript
<Route path="gateway">
  <Route index element={<Lazy><GatewayDashboardPage /></Lazy>} />
```

访问 `/gateway` 时直接显示仪表盘页面。

**模式 2：`<Navigate>` 重定向到子路径**

```typescript
<Route path="auth">
  <Route index element={<Navigate to="/auth/users" replace />} />
```

访问 `/auth` 时自动跳转到 `/auth/users`。`replace` 表示替换浏览器历史记录，用户按"后退"不会回到 `/auth`。

**模式 3：动态参数**

```typescript
<Route path="tenants/:id" element={<Lazy><OpsTenantDetailPage /></Lazy>} />
```

`:id` 是动态参数，在组件中用 `useParams()` 获取。

### 配置中心的路由传参

```typescript
<Route path="perm-center" element={<Lazy><ConfigPage serviceBaseUrl="/perm-api" serviceLabel="权限中心" /></Lazy>} />
```

`ConfigPage` 是一个通用配置页面组件，通过 props 接收不同的服务 URL 和标签。这是一种"一个组件 + 多个路由"的设计——类似后端的一个 Controller 方法处理多个路由。

> **【设计取舍】** 配置中心的 5 个路由都用 `ConfigPage` 组件 + props 区分服务。优点：只维护一个组件，减少重复代码。缺点：如果某个服务的配置页需要特殊字段或交互，props 传参会越来越复杂，最终变成"万能组件"。当差异超过 30% 时，应该拆分为独立组件。

> **【易错点】** `<Navigate>` 中的 `replace` 属性。不写 `replace` 时，重定向会在浏览器历史中留下两条记录（原路径 + 目标路径），用户按后退会触发无限重定向。加上 `replace` 后只保留一条记录——这一点在嵌套路由的默认重定向中尤其重要，因为用户不会想按"后退"回到 `/auth` 这个空路径。

> **🤔 导师提问**：当前所有路由都在 `App.tsx` 一个文件中定义。如果项目扩展到 100 个路由，这个文件会变得难以维护。你会如何拆分？提示：想想 .NET 中 `app.MapControllers()` 是自动扫描程序集的——前端有没有类似的"约定大于配置"的方案？

### `Lazy` 包装组件

```typescript
function Lazy({ children }: { children: React.ReactNode }) {
  return <Suspense fallback={<PageLoader />}>{children}</Suspense>
}
```

每次使用懒加载组件都需要 `<Suspense>` 包裹，`Lazy` 组件避免重复写 `<Suspense fallback={...}>`。统一使用 `PageLoader` 作为加载中状态。

## 踩坑提醒

1. **`lazy()` 的回调必须返回 `{ default: Component }` 格式**。如果页面组件用 `export default` 导出，`import()` 会自动包装为这个格式。但如果用命名导出（`export function XxxPage()`），需要手动包装：`lazy(() => import("@/pages/xxx").then(m => ({ default: m.XxxPage })))`。

2. **`<Navigate>` 中的 `replace` 属性**。不写 `replace` 时，重定向会在浏览器历史中留下两条记录（原路径 + 目标路径），用户按后退会触发无限重定向。加上 `replace` 后只保留一条记录。

3. **`<Route element={...}>` 和 `<Route>` 的区别**。有 `element` 的 Route 会渲染组件，没有 `element` 的 Route 只做路径匹配。受保护路由的外层 `<Route element={<ProtectedRoute><AppLayout /></ProtectedRoute>}>` 只渲染布局和守卫，不渲染具体页面——页面由子 Route 的 `<Outlet />` 承载。

4. **路由参数 `:id` 和查询参数 `?key=value` 是不同的**。路由参数用 `useParams()` 获取，查询参数用 `useSearchParams()` 获取。不要混淆。

## 自测题

### 概念级（理解定义）

1. 用户访问 `/auth` 时，会显示什么页面？请描述完整的路由匹配和重定向过程。

2. 为什么登录页使用静态导入而不是懒加载？

### 推理级（分析原因）

3. 请解释 `<ProtectedRoute>` 组件中 `state={{ from: location }}` 的作用。用户登录成功后如何利用这个信息？

4. 配置中心的路由使用 props 传参（`serviceBaseUrl` 和 `serviceLabel`），而不是创建独立的页面组件。这种设计有什么优缺点？

### 动手级（实践验证）

5. 当前所有路由都定义在一个 `App.tsx` 文件中。如果项目继续扩展（比如从 40 个路由变成 100 个），你会如何重构路由配置？请比较集中式路由和模块化路由的方案。

6. 假设后端需要支持"功能开关"控制某些页面的可见性（如"打印服务"功能关闭后，`/print/*` 路由应该不可访问）。你会如何设计这个动态路由方案？

## 验证步骤

1. 创建 `src/components/layout/protected-route.tsx`，确认 TypeScript 无错误
2. 创建 `src/App.tsx`，粘贴路由配置代码，执行 `pnpm dev` 确认项目正常启动
3. 访问 `http://localhost:3000/login`，应显示登录页
4. 未登录状态下访问 `http://localhost:3000/auth/users`，应被重定向到 `/login`
5. 登录后访问 `/auth`，应自动重定向到 `/auth/users`
6. 访问不存在的路径（如 `/nonexistent`），应被重定向到首页

## ✅ 输出检查清单

完成本节后，确认以下各项：

- [ ] 能说出前端路由和 ASP.NET Core Endpoint Routing 的对应关系
- [ ] 理解懒加载的必要性和静态导入登录页的原因
- [ ] 知道 `<Outlet />` 对应 `@RenderBody()` 的作用
- [ ] 能解释 `<Navigate replace />` 的重要性
- [ ] 理解 `<ProtectedRoute>` 的两层守卫逻辑（未认证 → 登录页，未选租户 → 租户选择页）

## 下一步

[← 上一篇：HTTP客户端封装](./03-HTTP客户端封装.md) | [下一篇：布局组件 →](./05-布局组件.md)
