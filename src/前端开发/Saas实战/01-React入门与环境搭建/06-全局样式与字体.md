# 全局样式与字体

> **这一步解决什么问题？**
>
> 配置全局样式文件（Tailwind v4 语法）、Geist 字体、亮暗主题 CSS 变量体系，让项目具备完整的视觉基础。这一步完成后，所有 shadcn/ui 组件将正确渲染样式，亮暗主题切换也能正常工作。

---

## 前置知识

### Tailwind CSS v4 的新语法

Tailwind v4 相比 v3 有重大变化：

| 特性 | v3 | v4 |
|------|----|----|
| 入口指令 | `@tailwind base; @tailwind components; @tailwind utilities;` | `@import "tailwindcss";` |
| 设计令牌 | `tailwind.config.js` 中的 `theme.extend` | CSS 中的 `@theme` 块 |
| 配置文件 | 必须有 `tailwind.config.js` | 不再需要（零配置） |
| 内容检测 | `content: ["./src/**/*.{ts,tsx}"]` | 自动检测 |

> **后端类比**：Tailwind v3 → v4 的迁移类似 .NET Framework → .NET Core 的迁移——配置方式完全不同，但核心概念不变。v4 的 `@theme` 类比 `appsettings.json` 的配置节，`@import` 类比 `Program.cs` 中的 `builder.Services.AddXXX()`。

⚠️ 【易错点】Tailwind v4 和 v3 语法**不兼容**，不能混用。如果网上搜到的教程用 `@tailwind base` 等 v3 语法，需要自行转换。AdminWeb 使用 v4，所有样式代码都是 v4 语法。

### CSS 变量（Custom Properties）

CSS 变量是浏览器原生的样式变量机制：

```css
:root {
  --background: oklch(1 0 0);       /* 定义变量 */
}

.dark {
  --background: oklch(0.145 0 0);    /* 暗黑模式覆盖 */
}

.card {
  background-color: var(--background); /* 使用变量 */
}
```

> **后端类比**：CSS 变量类比 WPF 的 `ResourceDictionary`——你定义一组命名的资源（颜色、间距），在运行时可以通过切换字典来更换主题。也类似 .NET 的 `IConfiguration`——定义配置键值对，运行时按需读取。

### oklch 色彩空间

oklch 是一种感知均匀的色彩空间，比 hsl 更先进：

```
hsl: 色相(H) 饱和度(S) 亮度(L)
  问题：同样的亮度值，黄色看起来比蓝色亮很多（感知不均匀）

oklch: 亮度(L) 色度(C) 色相(H)
  优势：同样的亮度值，不同色相看起来确实一样亮（感知均匀）
```

| 色彩空间 | 感知均匀性 | 浏览器支持 | shadcn/ui 使用 |
|---------|-----------|-----------|---------------|
| hsl | 不均匀 | 全部 | v3 之前 |
| oklch | 均匀 | 现代浏览器 | v4 推荐 |

> **为什么感知均匀很重要？** 当你调整亮暗主题的颜色时，oklch 确保亮色和暗色的视觉对比度一致。用 hsl 可能出现"暗色模式下某些颜色看起来太亮"的问题。

> **🤔 导师提问**：oklch 的浏览器兼容性如何？IE 不支持，但 IE 已停止维护。Chrome 111+、Firefox 113+、Safari 15.4+ 都支持。如果你的项目需要兼容更老的浏览器，Tailwind v4 也支持回退到 hsl——但 AdminWeb 面向现代浏览器，直接使用 oklch。类比后端：这就像选择 .NET 8 而不是 .NET Framework 4.8——旧平台也能跑，但新平台才有最佳体验。

### next-themes 的 dark mode 机制

next-themes 使用 `class` 策略实现暗黑模式：

```
1. ThemeProvider 检测系统偏好 / localStorage 中的主题设置
2. 给 <html> 标签添加或移除 class="dark"
3. CSS 中 .dark 选择器覆盖 :root 中的 CSS 变量
4. 所有使用 CSS 变量的组件自动更新样式
```

> **后端类比**：类似 `IOptionsMonitor<T>` 的 Change Token 机制——主题配置变化时，所有依赖主题的组件自动响应更新，无需手动通知。

---

## 代码实现

### 创建样式目录并迁移 CSS 文件

上一步中 Vite 模板生成的是 `src/index.css`，但 AdminWeb 使用 `src/styles/index.css`。需要创建目录并迁移：

```bash
# 创建 styles 目录
mkdir -p src/styles

# 删除 Vite 模板生成的默认 CSS
rm src/index.css src/App.css
```

### src/styles/index.css 完整代码

```css
@import "tailwindcss";
@import "@fontsource-variable/geist";
@import "@fontsource-variable/geist-mono";

@theme inline {
  --font-sans: "Geist Variable", system-ui, sans-serif;
  --font-mono: "Geist Mono Variable", monospace;
}

@layer base {
  :root {
    --background: oklch(1 0 0);
    --foreground: oklch(0.145 0 0);
    --card: oklch(1 0 0);
    --card-foreground: oklch(0.145 0 0);
    --popover: oklch(1 0 0);
    --popover-foreground: oklch(0.145 0 0);
    --primary: oklch(0.205 0 0);
    --primary-foreground: oklch(0.985 0 0);
    --secondary: oklch(0.97 0 0);
    --secondary-foreground: oklch(0.205 0 0);
    --muted: oklch(0.97 0 0);
    --muted-foreground: oklch(0.556 0 0);
    --accent: oklch(0.97 0 0);
    --accent-foreground: oklch(0.205 0 0);
    --destructive: oklch(0.577 0.245 27.325);
    --destructive-foreground: oklch(0.985 0 0);
    --border: oklch(0.922 0 0);
    --input: oklch(0.922 0 0);
    --ring: oklch(0.708 0 0);
    --radius: 0.625rem;
    --sidebar-background: oklch(0.985 0 0);
    --sidebar-foreground: oklch(0.145 0 0);
    --sidebar-primary: oklch(0.205 0 0);
    --sidebar-primary-foreground: oklch(0.985 0 0);
    --sidebar-accent: oklch(0.97 0 0);
    --sidebar-accent-foreground: oklch(0.205 0 0);
    --sidebar-border: oklch(0.922 0 0);
    --sidebar-ring: oklch(0.708 0 0);
  }

  .dark {
    --background: oklch(0.145 0 0);
    --foreground: oklch(0.985 0 0);
    --card: oklch(0.145 0 0);
    --card-foreground: oklch(0.985 0 0);
    --popover: oklch(0.145 0 0);
    --popover-foreground: oklch(0.985 0 0);
    --primary: oklch(0.985 0 0);
    --primary-foreground: oklch(0.205 0 0);
    --secondary: oklch(0.269 0 0);
    --secondary-foreground: oklch(0.985 0 0);
    --muted: oklch(0.269 0 0);
    --muted-foreground: oklch(0.708 0 0);
    --accent: oklch(0.269 0 0);
    --accent-foreground: oklch(0.985 0 0);
    --destructive: oklch(0.396 0.141 25.723);
    --destructive-foreground: oklch(0.985 0 0);
    --border: oklch(0.269 0 0);
    --input: oklch(0.269 0 0);
    --ring: oklch(0.439 0 0);
    --sidebar-background: oklch(0.205 0 0);
    --sidebar-foreground: oklch(0.985 0 0);
    --sidebar-primary: oklch(0.488 0.243 264.302);
    --sidebar-primary-foreground: oklch(0.985 0 0);
    --sidebar-accent: oklch(0.269 0 0);
    --sidebar-accent-foreground: oklch(0.985 0 0);
    --sidebar-border: oklch(0.269 0 0);
    --sidebar-ring: oklch(0.439 0 0);
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
    font-family: var(--font-sans);
  }
}
```

### src/main.tsx 完整代码

```tsx
import { StrictMode } from "react"
import { createRoot } from "react-dom/client"
import { ThemeProvider } from "next-themes"
import App from "./App"
import "@/styles/index.css"

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
      <App />
    </ThemeProvider>
  </StrictMode>
)
```

### 简单验证页面：亮/暗切换按钮

```tsx
// src/App.tsx
import { useTheme } from "next-themes"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Sun, Moon } from "lucide-react"

function App() {
  const { theme, setTheme } = useTheme()

  return (
    <div className="min-h-screen flex items-center justify-center bg-background p-8">
      <Card className="w-[400px]">
        <CardHeader>
          <CardTitle>主题切换验证</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="mb-4 text-muted-foreground">当前主题：{theme}</p>
          <Button
            onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
            className="gap-2"
          >
            {theme === "dark" ? <Sun className="h-4 w-4" /> : <Moon className="h-4 w-4" />}
            切换到{theme === "dark" ? "亮色" : "暗色"}模式
          </Button>
        </CardContent>
      </Card>
    </div>
  )
}

export default App
```

---

## 代码讲解

### Tailwind v4 的变化

**不再需要 tailwind.config.js**：v4 通过 `@theme` 块在 CSS 中定义设计令牌，取代了 v3 的 JS 配置文件：

```css
/* v3：tailwind.config.js */
module.exports = {
  theme: {
    extend: {
      fontFamily: { sans: ["Inter", "system-ui"] }
    }
  }
}

/* v4：直接在 CSS 中定义 */
@theme inline {
  --font-sans: "Geist Variable", system-ui, sans-serif;
}
```

**`@theme inline` 的含义**：`inline` 表示这些变量只在当前 CSS 文件中定义，不会生成独立的 CSS 层。这样避免了样式优先级冲突。

> **🤔 导师提问**：`@theme inline` 中的 `inline` 如果去掉会怎样？去掉 `inline` 后，`@theme` 块中的变量会生成一个独立的 CSS 层（`@layer theme`），可能和其他样式层产生优先级冲突。`inline` 确保变量只在当前文件作用域内生效——类比 C# 中 `internal` vs `public` 的访问级别。

### CSS 变量的设计令牌体系

shadcn/ui 定义了一整套语义化的 CSS 变量，形成"设计令牌"体系：

```
--background    → 页面背景色
--foreground    → 页面前景色（文字颜色）
--primary      → 主操作色（主按钮）
--secondary    → 次要操作色
--muted        → 弱化区域背景色
--destructive  → 危险操作色（删除按钮）
--border       → 边框颜色
--ring         → 焦点环颜色
```

每个变量都有对应的 `-foreground` 变量，确保前景和背景的对比度。

> **后端类比**：这套变量体系类似 .NET 的 `IConfiguration`——你定义一组语义化的配置键（如 `ConnectionStrings:Default`），运行时根据环境（Development/Production）读取不同的值。亮色模式和暗色模式就是两个不同的"环境"。

### `--destructive-foreground` 的正确值

⚠️ 【易错点】注意亮色模式中 `--destructive-foreground` 的值应该是 `oklch(0.985 0 0)`（白色文字），而不是和 `--destructive` 相同的红色值。红色背景上放红色文字会导致可读性极差。AdminWeb 已修正此值。

### oklch vs hsl 的视觉均匀性差异

```
hsl 的"50% 亮度"：
  黄色 hsl(60, 100%, 50%) → 看起来非常亮
  蓝色 hsl(240, 100%, 50%) → 看起来暗得多
  → 同样的"50% 亮度"，视觉感受完全不同

oklch 的"50% 亮度"：
  黄色 oklch(0.5 0.2 90) → 看起来确实是中等亮度
  蓝色 oklch(0.5 0.2 270) → 看起来也是中等亮度
  → 感知均匀，设计调参时更可预测
```

### ThemeProvider attribute="class" 工作原理

```tsx
<ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
```

| Prop | 作用 |
|------|------|
| `attribute="class"` | 通过给 `<html>` 添加/移除 `class="dark"` 来切换主题 |
| `defaultTheme="light"` | 默认亮色模式 |
| `enableSystem` | 检测系统偏好（如果用户系统设了暗色，自动跟随） |
| `disableTransitionOnChange` | 切换主题时禁用 CSS transition，避免页面元素逐个变色的闪烁 |

⚠️ 【性能陷阱】不加 `disableTransitionOnChange`，切换主题时每个元素会逐个执行 CSS transition 变色，页面会出现"波浪式闪烁"。加上后切换瞬间完成，视觉更干净。

工作流程：
```
1. 页面加载 → ThemeProvider 检查 localStorage 有无存储的主题
2. 没有 → 检查系统偏好（enableSystem）
3. 没有 → 使用 defaultTheme="light"
4. 确定主题 → 给 <html> 添加 class="dark" 或移除
5. CSS 变量切换 → 所有组件自动更新
6. 主题选择写入 localStorage 持久化
```

> **🤔 导师提问**：`next-themes` 名字带"next"但不是 Next.js 专用——为什么一个通用库要带框架名？因为 `next-themes` 最初是为 Next.js 开发的，后来扩展支持了纯 React。命名虽容易困惑，但它确实是 SPA 中最好用的主题切换库。类比后端：`Microsoft.Extensions.Logging` 名字带"Extensions"但已经是 .NET 核心的一部分。

### `@layer base` 的作用

`@layer base` 是 CSS 层叠层（Cascade Layers）语法，用于控制样式的优先级：

```css
@layer base {
  * {
    @apply border-border;  /* 所有元素的边框使用 --border 颜色 */
  }
  body {
    @apply bg-background text-foreground;  /* body 的背景和文字使用 CSS 变量 */
    font-family: var(--font-sans);
  }
}
```

> **后端类比**：CSS 层叠层类似中间件的执行顺序——`@layer base` 是最底层的"默认配置"，组件级样式是更高优先级的"用户配置"。高优先级覆盖低优先级，就像 `appsettings.Production.json` 覆盖 `appsettings.json`。

> **🤔 导师提问**：CSS 变量方案和 CSS-in-JS 方案（如 styled-components）在主题切换方面各有什么优劣？提示：CSS 变量是浏览器原生支持，切换时零 JS 开销；CSS-in-JS 需要重新渲染所有 styled 组件。AdminWeb 选择 CSS 变量方案——对于企业级 SaaS 项目，哪个更适合？

---

## 踩坑提醒

1. **Tailwind v4 和 v3 语法不兼容**：不能混用 `@tailwind base` 和 `@import "tailwindcss"`。如果网上搜到的教程用 v3 语法，需要自行转换。
2. **`@theme inline` 必须写在 index.css 中**：不能拆分到其他文件再 `@import`，因为 `inline` 关键字要求变量和当前 CSS 文件在同一个作用域。
3. **字体文件通过 npm 包引入而非 CDN**：`@import "@fontsource-variable/geist"` 从 `node_modules` 中引入字体，离线可用、不依赖外部 CDN。不要换成 CDN 链接引入。
4. **`disableTransitionOnChange` 避免暗黑切换闪烁**：不加这个 prop，切换主题时每个元素会逐个执行 CSS transition 变色，页面会出现"波浪式闪烁"。加上后切换瞬间完成，视觉更干净。
5. **删除旧的 CSS 文件**：Vite 模板生成的 `src/index.css` 和 `src/App.css` 需要删除，否则可能与 `src/styles/index.css` 产生冲突。

---

## 🤔 自测题

### 概念级

1. Tailwind v4 中如何引入 Tailwind 的基础样式？和 v3 有什么区别？
2. `.dark` 选择器是如何实现暗黑模式切换的？

### 推理级

3. oklch 相比 hsl 有什么优势？为什么 shadcn/ui v4 推荐用 oklch？
4. `@theme inline` 中的 `inline` 关键字有什么作用？如果去掉 `inline` 会怎样？

### 动手级

5. 修改 `src/styles/index.css` 中 `:root` 的 `--primary` 值（如改为 `oklch(0.6 0.2 250)`），刷新页面观察 Button 的颜色变化。改回原值恢复。
6. 点击页面上的"切换到暗色模式"按钮，观察所有组件的颜色变化。确认切换后背景、文字、边框都正确变色。

---

## 验证步骤

> **🔍 验证步骤**
>
> 1. 确认 `src/styles/index.css` 已创建，`src/index.css` 和 `src/App.css` 已删除
> 2. 确认 `src/main.tsx` 中的 import 路径为 `@/styles/index.css`（不是 `./index.css`）
> 3. 执行 `pnpm dev`，浏览器打开 `localhost:3000`，确认页面正常显示（有颜色、有字体、不白屏）
> 4. 点击"切换到暗色模式"按钮，确认背景变深、文字变亮、按钮颜色跟随变化
> 5. 刷新页面，确认暗色模式选择被保留（localStorage 持久化生效）

---

## 输出检查清单

完成本节后，确认以下内容：

- [ ] 创建了 `src/styles/index.css`，包含 Tailwind v4 入口、字体、CSS 变量、亮暗模式
- [ ] 删除了 Vite 模板生成的 `src/index.css` 和 `src/App.css`
- [ ] 修改了 `src/main.tsx`，添加了 ThemeProvider 和 `@/styles/index.css` 引用
- [ ] 理解 Tailwind v4 的 `@import "tailwindcss"` 和 `@theme inline` 语法
- [ ] 理解 CSS 变量设计令牌体系（--background、--foreground、--primary 等）
- [ ] 理解 oklch 色彩空间的感知均匀优势
- [ ] 理解 `disableTransitionOnChange` 避免主题切换闪烁
- [ ] 亮暗主题切换功能正常工作

---

[← 上一篇](./05-shadcn-ui初始化.md) | [下一篇 →](./07-启动与验证.md)
