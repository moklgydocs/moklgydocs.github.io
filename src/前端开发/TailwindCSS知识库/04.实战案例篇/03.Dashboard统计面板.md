---
title: 案例3:Dashboard统计面板—Grid布局+趋势指示器+响应式网格
icon: chart-line
order: 3
category:
  - CSS
  - Tailwind CSS
tag:
  - 实战
  - Dashboard
  - Grid
---

# 案例 3：Dashboard 统计面板

> **TL;DR**
> 用 Tailwind Grid 构建一个中后台 Dashboard 统计面板：4 列统计卡片（含趋势指示器）+ 图表区域 + 最近订单列表。掌握 `grid-cols-*`、`col-span-*`、`tabular-nums`、条件样式等核心用法。

## 目标效果

```
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ 总营收    │ │ 订单数   │ │ 活跃用户  │ │ 转化率   │
│ ¥1,234K  │ │ 12,345  │ │ 3,456   │ │ 4.2%    │
│ ↑ 12.5%  │ │ ↑ 8.3%  │ │ ↓ 2.1%  │ │ ↑ 0.8%  │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
┌───────────────────────┐ ┌─────────────────────┐
│                       │ │   最近订单           │
│   营收趋势图表区域      │ │   GM-001 ¥2,340 ✅  │
│                       │ │   GM-002 ¥1,200 ⏳  │
│                       │ │   GM-003 ¥890   🚚  │
└───────────────────────┘ └─────────────────────┘
```

## 完整实现

### 统计卡片组件

```tsx
// src/components/business/stat-card.tsx
import { cn } from "@/lib/utils";

interface StatCardProps {
  title: string;
  value: string;
  change: number;
  changeLabel: string;
  icon: React.ReactNode;
}

function StatCard({ title, value, change, changeLabel, icon }: StatCardProps) {
  const isPositive = change >= 0;

  return (
    <div className="group rounded-xl border bg-card p-6 transition-all duration-200 hover:shadow-md">
      <div className="flex items-center justify-between">
        <span className="text-sm font-medium text-muted-foreground">{title}</span>
        <div className="rounded-lg bg-primary/10 p-2 text-primary transition-colors group-hover:bg-primary group-hover:text-primary-foreground">
          {icon}
        </div>
      </div>

      <div className="mt-3">
        <p className="text-3xl font-bold tabular-nums tracking-tight">{value}</p>
      </div>

      <div className="mt-2 flex items-center gap-2">
        {/* 趋势指示器 */}
        <span
          className={cn(
            "inline-flex items-center gap-0.5 rounded-full px-2 py-0.5 text-xs font-medium",
            isPositive
              ? "bg-green-50 text-green-700 dark:bg-green-500/10 dark:text-green-400"
              : "bg-red-50 text-red-700 dark:bg-red-500/10 dark:text-red-400"
          )}
        >
          <svg
            className={cn("h-3 w-3", !isPositive && "rotate-180")}
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            strokeWidth={2}
          >
            <path strokeLinecap="round" strokeLinejoin="round" d="M5 15l7-7 7 7" />
          </svg>
          {Math.abs(change)}%
        </span>
        <span className="text-xs text-muted-foreground">{changeLabel}</span>
      </div>
    </div>
  );
}

export { StatCard };
```

### 订单列表组件

```tsx
// src/components/business/recent-orders.tsx
import { cn } from "@/lib/utils";

interface Order {
  id: string;
  customer: string;
  amount: number;
  status: "completed" | "pending" | "shipping" | "cancelled";
  date: string;
}

const statusConfig = {
  completed: { label: "已完成", className: "bg-green-50 text-green-700 dark:bg-green-500/10 dark:text-green-400" },
  pending: { label: "待处理", className: "bg-yellow-50 text-yellow-700 dark:bg-yellow-500/10 dark:text-yellow-400" },
  shipping: { label: "配送中", className: "bg-blue-50 text-blue-700 dark:bg-blue-500/10 dark:text-blue-400" },
  cancelled: { label: "已取消", className: "bg-red-50 text-red-700 dark:bg-red-500/10 dark:text-red-400" },
} as const;

const recentOrders: Order[] = [
  { id: "GM-20240115-001", customer: "张三", amount: 2340, status: "completed", date: "2024-01-15" },
  { id: "GM-20240115-002", customer: "李四", amount: 1200, status: "pending", date: "2024-01-15" },
  { id: "GM-20240114-003", customer: "王五", amount: 890, status: "shipping", date: "2024-01-14" },
  { id: "GM-20240114-004", customer: "赵六", amount: 3560, status: "completed", date: "2024-01-14" },
  { id: "GM-20240113-005", customer: "孙七", amount: 670, status: "cancelled", date: "2024-01-13" },
];

function RecentOrders() {
  return (
    <div className="rounded-xl border bg-card">
      {/* 头部 */}
      <div className="flex items-center justify-between border-b px-6 py-4">
        <h3 className="text-lg font-semibold">最近订单</h3>
        <a
          href="/orders"
          className="text-sm text-primary hover:underline underline-offset-4"
        >
          查看全部
        </a>
      </div>

      {/* 列表 */}
      <div className="divide-y">
        {recentOrders.map((order) => {
          const status = statusConfig[order.status];
          return (
            <div
              key={order.id}
              className="flex items-center justify-between px-6 py-4 transition-colors hover:bg-muted/50"
            >
              <div className="flex items-center gap-4">
                {/* 客户头像占位 */}
                <div className="flex h-10 w-10 items-center justify-center rounded-full bg-gradient-to-br from-blue-100 to-purple-100 text-sm font-medium text-blue-700 dark:from-blue-900 dark:to-purple-900 dark:text-blue-300">
                  {order.customer[0]}
                </div>
                <div>
                  <p className="text-sm font-medium">{order.customer}</p>
                  <p className="text-xs text-muted-foreground">{order.id}</p>
                </div>
              </div>

              <div className="flex items-center gap-4">
                <span className="text-sm font-medium tabular-nums">
                  ¥{order.amount.toLocaleString()}
                </span>
                <span
                  className={cn(
                    "inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium",
                    status.className
                  )}
                >
                  {status.label}
                </span>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}

export { RecentOrders };
```

### Dashboard 页面组装

```tsx
// src/pages/dashboard.tsx
import { StatCard } from "@/components/business/stat-card";
import { RecentOrders } from "@/components/business/recent-orders";

function DashboardPage() {
  return (
    <div className="space-y-6 p-6">
      {/* 页面标题 */}
      <div>
        <h1 className="text-2xl font-bold tracking-tight">运营大盘</h1>
        <p className="mt-1 text-sm text-muted-foreground">
          GlobalMart 全球电商运营数据总览
        </p>
      </div>

      {/* 统计卡片：手机 2 列，桌面 4 列 */}
      <div className="grid grid-cols-2 gap-4 lg:grid-cols-4">
        <StatCard
          title="总营收"
          value="¥1,234K"
          change={12.5}
          changeLabel="较上月"
          icon={
            <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          }
        />
        <StatCard
          title="订单总数"
          value="12,345"
          change={8.3}
          changeLabel="较上月"
          icon={
            <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
            </svg>
          }
        />
        <StatCard
          title="活跃用户"
          value="3,456"
          change={-2.1}
          changeLabel="较上周"
          icon={
            <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          }
        />
        <StatCard
          title="转化率"
          value="4.2%"
          change={0.8}
          changeLabel="较上月"
          icon={
            <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
            </svg>
          }
        />
      </div>

      {/* 下半部分：图表 + 订单列表 */}
      <div className="grid grid-cols-1 gap-6 lg:grid-cols-12">
        {/* 图表区域（占 8 列） */}
        <div className="lg:col-span-7 xl:col-span-8">
          <div className="rounded-xl border bg-card p-6">
            <div className="flex items-center justify-between">
              <h3 className="text-lg font-semibold">营收趋势</h3>
              <div className="flex gap-1">
                {["7天", "30天", "90天"].map((period) => (
                  <button
                    key={period}
                    className="rounded-md px-3 py-1 text-xs font-medium text-muted-foreground transition-colors hover:bg-muted hover:text-foreground first:bg-primary first:text-primary-foreground"
                  >
                    {period}
                  </button>
                ))}
              </div>
            </div>
            {/* 图表占位区域 */}
            <div className="mt-6 flex h-64 items-end justify-between gap-2 rounded-lg bg-muted/30 px-4 pb-4">
              {[40, 65, 55, 80, 72, 90, 85, 95, 78, 88, 92, 100].map((h, i) => (
                <div
                  key={i}
                  className="flex-1 rounded-t-sm bg-primary/80 transition-all duration-300 hover:bg-primary"
                  style={{ height: `${h}%` }}
                />
              ))}
            </div>
          </div>
        </div>

        {/* 最近订单（占 4 列） */}
        <div className="lg:col-span-5 xl:col-span-4">
          <RecentOrders />
        </div>
      </div>
    </div>
  );
}

export { DashboardPage };
```

## 知识点解析

### 1. 响应式 Grid 布局策略

```tsx
// 统计卡片：2 列 → 4 列
"grid grid-cols-2 gap-4 lg:grid-cols-4"

// 下方区域：12 列栅格，图表占 8，列表占 4
"grid grid-cols-1 gap-6 lg:grid-cols-12"
"lg:col-span-7 xl:col-span-8"  // 图表
"lg:col-span-5 xl:col-span-4"  // 列表
```

在手机上全部堆叠为单列，在桌面上用 12 列栅格精确控制比例。

### 2. `tabular-nums` 等宽数字

```tsx
<p className="text-3xl font-bold tabular-nums">¥1,234K</p>
```

`tabular-nums` 让数字字符等宽对齐，数据变化时不会导致布局跳动。在 Dashboard 和表格中特别重要。

### 3. Group 悬停效果

```tsx
<div className="group ...">
  <div className="group-hover:bg-primary group-hover:text-primary-foreground">
    {icon}
  </div>
</div>
```

鼠标悬停在卡片上时，图标容器的背景色和文字色同时变化。

### 4. 暗色模式适配

```tsx
// 状态颜色在暗色模式下使用不同的色调
"bg-green-50 text-green-700 dark:bg-green-500/10 dark:text-green-400"
```

亮色模式用浅底深字（`green-50` + `green-700`），暗色模式用半透明底亮字（`green-500/10` + `green-400`）。

### 5. `divide-y` 简化分隔线

```tsx
<div className="divide-y">
  {orders.map((order) => (
    <div key={order.id} className="py-4">...</div>
  ))}
</div>
```

`divide-y` 自动在相邻子元素之间添加水平分隔线，不需要手动在每一项底部加 `border-b` 再给最后一项加 `last:border-b-0`。

## 避坑指南

**坑：`grid-cols-2 gap-4` 在小屏幕上统计卡片太挤**

```tsx
// 修复：超小屏用单列
"grid grid-cols-1 gap-4 xs:grid-cols-2 lg:grid-cols-4"
// 需要在 tailwind.config 中添加 xs: "475px" 断点
```

**坑：数字宽度变化导致卡片高度不一致**

```tsx
// 确保所有 StatCard 都用 items-center 对齐
// 或给数字行固定高度
<p className="h-10 text-3xl font-bold tabular-nums">{value}</p>
```

---

*下一个案例：[暗色模式电商商品卡](./04.暗色模式电商商品卡.md) — dark: 变体 + 图片悬浮 + 标签系统。*
