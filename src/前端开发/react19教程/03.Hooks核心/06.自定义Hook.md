---
title: 自定义 Hook
icon: wand-magic-sparkles
order: 6
category:
  - React
tag:
  - Hooks
  - 自定义Hook
---

# 自定义 Hook：逻辑复用的终极方案

## 学习目标

- 理解为什么需要自定义 Hook
- 掌握自定义 Hook 的设计原则
- 积累一些实用的自定义 Hook

## 什么是自定义 Hook？

自定义 Hook 就是一个**以 `use` 开头的函数**，里面可以使用其他 Hooks：

```jsx
// 自定义 Hook
function useCounter(initialValue = 0) {
  const [count, setCount] = useState(initialValue);
  const increment = () => setCount(c => c + 1);
  const decrement = () => setCount(c => c - 1);
  const reset = () => setCount(initialValue);
  return { count, increment, decrement, reset };
}

// 在任何组件中使用
function ComponentA() {
  const { count, increment } = useCounter(10);
  return <button onClick={increment}>{count}</button>;
}

function ComponentB() {
  const { count, decrement, reset } = useCounter(100);
  return (
    <div>
      <span>{count}</span>
      <button onClick={decrement}>-</button>
      <button onClick={reset}>重置</button>
    </div>
  );
}
```

> 🔬 **本质**：自定义 Hook 不是什么魔法，它就是一个普通函数。之所以要以 `use` 开头，是为了让 React 和 linter 知道「这个函数里面用了 Hooks」，从而应用 Hooks 的规则检查。

## 实用自定义 Hook 合集

### 1. useLocalStorage：持久化状态

```jsx
function useLocalStorage(key, initialValue) {
  const [storedValue, setStoredValue] = useState(() => {
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch (error) {
      console.warn(`读取 localStorage 失败:`, error);
      return initialValue;
    }
  });

  const setValue = (value) => {
    try {
      const valueToStore = value instanceof Function ? value(storedValue) : value;
      setStoredValue(valueToStore);
      window.localStorage.setItem(key, JSON.stringify(valueToStore));
    } catch (error) {
      console.warn(`写入 localStorage 失败:`, error);
    }
  };

  return [storedValue, setValue];
}

// 使用
function Settings() {
  const [theme, setTheme] = useLocalStorage('theme', 'light');
  // 刷新页面后 theme 仍然保留！
}
```

### 2. useFetch：数据请求

```jsx
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    setError(null);

    fetch(url)
      .then(res => {
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        return res.json();
      })
      .then(data => {
        if (!cancelled) {
          setData(data);
          setLoading(false);
        }
      })
      .catch(err => {
        if (!cancelled) {
          setError(err.message);
          setLoading(false);
        }
      });

    return () => { cancelled = true; };
  }, [url]);

  return { data, loading, error };
}

// 使用
function UserList() {
  const { data: users, loading, error } = useFetch('/api/users');
  
  if (loading) return <p>加载中...</p>;
  if (error) return <p>错误: {error}</p>;
  return <ul>{users.map(u => <li key={u.id}>{u.name}</li>)}</ul>;
}
```

### 3. useDebounce：防抖

```jsx
function useDebounce(value, delay = 300) {
  const [debouncedValue, setDebouncedValue] = useState(value);

  useEffect(() => {
    const timer = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);

    return () => clearTimeout(timer);
  }, [value, delay]);

  return debouncedValue;
}

// 使用：搜索输入防抖
function SearchInput() {
  const [query, setQuery] = useState('');
  const debouncedQuery = useDebounce(query, 500);
  
  useEffect(() => {
    if (debouncedQuery) {
      searchAPI(debouncedQuery); // 只有停止输入 500ms 后才搜索
    }
  }, [debouncedQuery]);

  return <input value={query} onChange={e => setQuery(e.target.value)} />;
}
```

### 4. useToggle：开关状态

```jsx
function useToggle(initialValue = false) {
  const [value, setValue] = useState(initialValue);
  
  const toggle = useCallback(() => setValue(v => !v), []);
  const setTrue = useCallback(() => setValue(true), []);
  const setFalse = useCallback(() => setValue(false), []);
  
  return { value, toggle, setTrue, setFalse };
}

// 使用
function Modal() {
  const { value: isOpen, toggle, setFalse: close } = useToggle();
  return (
    <div>
      <button onClick={toggle}>打开弹窗</button>
      {isOpen && <Dialog onClose={close} />}
    </div>
  );
}
```

### 5. useWindowSize：窗口尺寸

```jsx
function useWindowSize() {
  const [size, setSize] = useState({
    width: window.innerWidth,
    height: window.innerHeight,
  });

  useEffect(() => {
    const handler = () => setSize({
      width: window.innerWidth,
      height: window.innerHeight,
    });
    window.addEventListener('resize', handler);
    return () => window.removeEventListener('resize', handler);
  }, []);

  return size;
}

// 使用
function ResponsiveLayout() {
  const { width } = useWindowSize();
  return width > 768 ? <DesktopLayout /> : <MobileLayout />;
}
```

## 自定义 Hook 设计原则

| 原则 | 说明 |
|------|------|
| **单一职责** | 一个 Hook 只做一件事 |
| **以 `use` 开头** | 这是 React 的约定，不可违反 |
| **返回值清晰** | 返回对象（多个值）或数组（仿 useState） |
| **参数合理** | 接受必要的配置，提供合理默认值 |
| **处理边界** | 考虑 SSR、组件卸载等边界情况 |

## Hook 的规则（必须牢记）

1. **只在顶层调用**：不能在循环、条件或嵌套函数中调用 Hook
2. **只在 React 函数中调用**：只能在函数组件或自定义 Hook 中使用

```jsx
// ❌ 违反规则
function Bad({ condition }) {
  if (condition) {
    const [count, setCount] = useState(0); // 条件中调用！
  }
  
  for (let i = 0; i < 5; i++) {
    useEffect(() => {}); // 循环中调用！
  }
}

// ✅ 正确
function Good({ condition }) {
  const [count, setCount] = useState(0); // 顶层调用
  
  useEffect(() => {
    if (condition) {
      // 条件逻辑放在 Hook 内部
    }
  }, [condition]);
}
```

> 🔬 **原理**：React 通过 Hook 的调用顺序来对应每次渲染的状态。如果 Hook 在条件中调用，顺序可能变化，React 就分不清哪个状态对应哪个 Hook 了。

## 🤔 思考题

1. 为什么自定义 Hook 必须以 `use` 开头？如果不以 `use` 开头会怎样？
2. 自定义 Hook 和普通工具函数有什么区别？
3. 设计一个 `useApi` Hook，要求支持：自动请求、手动刷新、取消请求。你会怎么设计它的 API？

---

> 🎉 **恭喜**！你已经掌握了 React Hooks 的核心知识。
> 
> **下一章**：[React 19 概览](../04.React19新特性/01.React19概览.md) —— 探索 React 的最新特性
