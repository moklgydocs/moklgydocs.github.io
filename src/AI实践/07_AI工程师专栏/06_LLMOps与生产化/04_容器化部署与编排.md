---
title: 容器化部署与 K8s 编排
icon: fa6-solid:docker
order: 4
category:
  - AI工程师
  - LLMOps
---

# 容器化部署与 K8s 编排

## Docker 最佳实践

### AI 服务 Dockerfile

```dockerfile
# 多阶段构建：构建阶段
FROM nvidia/cuda:12.4.0-devel-ubuntu22.04 AS builder

WORKDIR /app

# 安装 Python
RUN apt-get update && apt-get install -y python3.10 python3-pip && \
    rm -rf /var/lib/apt/lists/*

# 安装依赖（利用缓存层）
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 运行阶段：更小的基础镜像
FROM nvidia/cuda:12.4.0-runtime-ubuntu22.04

WORKDIR /app

# 只复制必要的运行时文件
COPY --from=builder /usr/local/lib/python3.10 /usr/local/lib/python3.10
COPY --from=builder /usr/local/bin /usr/local/bin
COPY --from=builder /app /app

# 复制应用代码
COPY src/ ./src/

# 非 root 用户运行
RUN useradd -m appuser && chown -R appuser:appuser /app
USER appuser

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

EXPOSE 8000

CMD ["python", "-m", "vllm.entrypoints.openai.api_server", \
     "--host", "0.0.0.0", "--port", "8000", \
     "--model", "/models/qwen2.5-7b"]
```

### Docker Compose 本地开发

```yaml
# docker-compose.yml
version: "3.8"

services:
  llm-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - model-data:/models
    environment:
      - MODEL_PATH=/models/qwen2.5-7b
      - MAX_MODEL_LEN=4096
      - GPU_MEMORY_UTILIZATION=0.9
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  embedding-api:
    build:
      context: ./embedding_service
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

volumes:
  model-data:
    driver: local
```

## Kubernetes GPU 调度

### GPU 节点准备

```bash
# 安装 NVIDIA GPU Operator
helm repo add nvidia https://helm.ngc.nvidia.com/nvidia
helm install gpu-operator nvidia/gpu-operator \
    --namespace gpu-operator --create-namespace
```

### GPU 资源声明

```yaml
# llm-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-inference
  namespace: ai-serving
spec:
  replicas: 2
  selector:
    matchLabels:
      app: llm-inference
  template:
    metadata:
      labels:
        app: llm-inference
    spec:
      # 调度到 GPU 节点
      nodeSelector:
        nvidia.com/gpu.present: "true"
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      containers:
        - name: vllm
          image: registry.example.com/llm-serving:v1.0
          ports:
            - containerPort: 8000
          resources:
            limits:
              nvidia.com/gpu: 1
            requests:
              nvidia.com/gpu: 1
              memory: "16Gi"
              cpu: "4"
          env:
            - name: MODEL_PATH
              value: "/models/qwen2.5-7b"
            - name: MAX_MODEL_LEN
              value: "4096"
          volumeMounts:
            - name: model-storage
              mountPath: /models
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 10
      volumes:
        - name: model-storage
          persistentVolumeClaim:
            claimName: model-pvc
```

### 模型存储 PVC

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-pvc
  namespace: ai-serving
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 50Gi
```

## HPA 自动伸缩

### 基于 GPU 利用率的 HPA

```yaml
# hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: llm-inference-hpa
  namespace: ai-serving
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: llm-inference
  minReplicas: 1
  maxReplicas: 5
  metrics:
    # 基于 CPU 利用率
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    # 基于 GPU 利用率（需要 DCGM exporter）
    - type: Pods
      pods:
        metric:
          name: gpu_utilization
        target:
          type: AverageValue
          averageValue: "80"
    # 基于并发请求数（自定义指标）
    - type: Pods
      pods:
        metric:
          name: vllm_num_requests_running
        target:
          type: AverageValue
          averageValue: "16"
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 100
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
```

### KEDA 基于 Queue 深度伸缩

```yaml
# keda-scaledobject.yaml
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: llm-scaler
  namespace: ai-serving
spec:
  scaleTargetRef:
    name: llm-inference
  minReplicaCount: 1
  maxReplicaCount: 5
  cooldownPeriod: 300
  triggers:
    - type: prometheus
      metadata:
        serverAddress: http://prometheus.monitoring:9090
        metricName: vllm_avg_queue_length
        threshold: "10"
        query: avg(vllm_num_requests_waiting{app="llm-inference"})
```

## Helm Chart

### Chart 结构

```
llm-serving/
  Chart.yaml
  values.yaml
  templates/
    deployment.yaml
    service.yaml
    hpa.yaml
    ingress.yaml
    configmap.yaml
```

### values.yaml

```yaml
# values.yaml
replicaCount: 2

image:
  repository: registry.example.com/llm-serving
  pullPolicy: IfNotPresent
  tag: "v1.0"

model:
  path: /models/qwen2.5-7b
  maxLength: 4096
  gpuMemoryUtilization: 0.9

resources:
  limits:
    nvidia.com/gpu: 1
    memory: 16Gi
    cpu: "4"
  requests:
    nvidia.com/gpu: 1
    memory: 16Gi
    cpu: "4"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilization: 70

ingress:
  enabled: true
  className: nginx
  host: api.example.com

persistence:
  enabled: true
  size: 50Gi
  storageClass: nfs-storage

monitoring:
  enabled: true
  serviceMonitor: true
```

### 部署命令

```bash
# 安装
helm install llm-serving ./llm-serving -n ai-serving --create-namespace

# 升级
helm upgrade llm-serving ./llm-serving -n ai-serving

# 回滚
helm rollback llm-serving 1 -n ai-serving
```

## 生产部署清单

### 部署前检查

```python
class DeploymentChecklist:
    """生产部署检查清单"""

    checks = [
        # 镜像与模型
        ("镜像已通过安全扫描", "critical"),
        ("模型文件完整性校验（MD5/SHA256）", "critical"),
        ("镜像大小合理（< 20GB）", "warning"),

        # 资源与调度
        ("GPU 资源配额充足", "critical"),
        ("PVC 存储已就绪", "critical"),
        ("节点容忍与亲和性配置正确", "warning"),

        # 网络与安全
        ("Ingress/TLS 证书有效", "critical"),
        ("NetworkPolicy 限制出站流量", "warning"),
        ("ServiceAccount 权限最小化", "warning"),

        # 健康检查
        ("livenessProbe 已配置", "critical"),
        ("readinessProbe 已配置", "critical"),
        ("启动探针 initialDelay 充足", "warning"),

        # 可观测性
        ("Prometheus ServiceMonitor 已配置", "warning"),
        ("日志采集已配置", "warning"),
        ("告警规则已创建", "warning"),

        # 弹性伸缩
        ("HPA 已配置", "warning"),
        ("伸缩阈值合理", "warning"),

        # 灾备
        ("多副本部署", "warning"),
        ("PDB（PodDisruptionBudget）已配置", "warning"),
        ("备份与恢复流程已验证", "warning"),
    ]

    def run(self) -> dict:
        results = {"pass": 0, "fail": 0, "warnings": 0, "details": []}
        # 实际实现中逐项检查
        for check_name, severity in self.checks:
            results["details"].append({
                "check": check_name,
                "severity": severity,
                "status": "pending",  # 需人工确认
            })
        return results
```

### 灰度发布

```yaml
# canary-deployment.yaml
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: llm-inference
  namespace: ai-serving
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: {duration: 5m}
        - setWeight: 30
        - pause: {duration: 5m}
        - setWeight: 50
        - pause: {duration: 10m}
        - setWeight: 100
      canaryService: llm-inference-canary
      stableService: llm-inference-stable
  selector:
    matchLabels:
      app: llm-inference
  template:
    # 与普通 Deployment 相同
    spec:
      containers:
        - name: vllm
          image: registry.example.com/llm-serving:v1.1
          # ...
```

## 关键配置参数

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| `max-model-len` | 最大序列长度 | 根据业务需求 |
| `gpu-memory-utilization` | GPU 显存利用率 | 0.85-0.92 |
| `max-num-seqs` | 最大并发序列数 | 16-64 |
| `enable-prefix-caching` | 前缀缓存 | 开启 |
| `enforce-eager` | 禁用 CUDA Graph（调试用） | 生产关闭 |

## 故障排查

| 现象 | 可能原因 | 排查方法 |
|------|----------|----------|
| OOM Killed | 显存不足 | 降低 gpu-memory-utilization 或 max-model-len |
| 启动超时 | 模型加载慢 | 检查 PVC IOPS，预热模型 |
| 请求超时 | 排队过长 | 增加 replicas 或降低 max-num-seqs |
| GPU 利用率低 | batch 不足 | 增加 max-num-seqs 或降低 HPA 阈值 |
| 响应变慢 | KV Cache 不足 | 增加显存或减少并发 |

## 部署成本模型

企业部署 LLM 应用前必须回答：每月要花多少钱？以下提供三个典型规模的成本估算。

### 典型部署成本估算（月度）

| 成本项 | 小型（1K QPS/天） | 中型（10K QPS/天） | 大型（100K QPS/天） |
|--------|------------------|-------------------|---------------------|
| **LLM API** | | | |
| - GPT-4o | ¥3,000-8,000 | ¥30,000-80,000 | ¥300,000-800,000 |
| - GPT-4o-mini | ¥300-800 | ¥3,000-8,000 | ¥30,000-80,000 |
| - Claude Sonnet | ¥4,000-10,000 | ¥40,000-100,000 | ¥400,000-1,000,000 |
| **向量数据库** | | | |
| - Milvus (托管) | ¥500-1,500 | ¥2,000-5,000 | ¥10,000-30,000 |
| - Pinecone | $70-200 | $700-2,000 | $7,000-20,000 |
| **基础设施** | | | |
| - GPU 推理 (自部署) | ¥5,000-15,000 | ¥50,000-150,000 | ¥500,000-1,500,000 |
| - CPU 服务 + 缓存 | ¥500-1,000 | ¥2,000-5,000 | ¥10,000-30,000 |
| - Redis | ¥200-500 | ¥500-2,000 | ¥2,000-10,000 |
| **总计（API 模式）** | **¥4,000-10,000** | **¥35,000-95,000** | **¥350,000-920,000** |
| **总计（自部署模式）** | **¥6,000-17,000** | **¥55,000-160,000** | **¥530,000-1,600,000** |

> ⚠️ 以上为 2025 年参考价格，基于以下假设：
> - RAG 场景：平均 1K input + 500 output tokens/query
> - 自部署：1× A100 (80GB) 或等效
> - 向量库：100 万文档，1024 维
> - 不含人力成本

### API vs 自部署决策

```python
class DeploymentCostEstimator:
    """部署成本估算器

    帮助决策：API 调用 vs 自部署模型，哪个更划算？
    """

    # 模型定价（$/M tokens，2025 参考）
    API_PRICING = {
        "gpt-4o": {"input": 2.5, "output": 10.0},
        "gpt-4o-mini": {"input": 0.15, "output": 0.6},
        "claude-sonnet-4-20250514": {"input": 3.0, "output": 15.0},
        "deepseek-v3": {"input": 0.27, "output": 1.1},
    }

    # 自部署 GPU 成本（¥/月，含电费）
    GPU_COSTS = {
        "A100_80GB": 15000,   # 云服务器
        "A100_40GB": 10000,
        "L40S_48GB": 8000,
        "RTX_4090_24GB": 4000,  # 自建
    }

    def estimate_api_cost(self, model: str, daily_queries: int,
                          avg_input_tokens: int = 1000,
                          avg_output_tokens: int = 500,
                          cache_hit_rate: float = 0.3) -> dict:
        """估算 API 模式月度成本"""
        pricing = self.API_PRICING.get(model, self.API_PRICING["gpt-4o"])

        # Prompt Caching 折扣
        effective_input_price = pricing["input"] * (
            cache_hit_rate * 0.5 + (1 - cache_hit_rate) * 1.0
        )

        daily_cost = daily_queries * (
            avg_input_tokens * effective_input_price / 1_000_000 +
            avg_output_tokens * pricing["output"] / 1_000_000
        )
        monthly_cost = daily_cost * 30

        return {
            "model": model,
            "daily_queries": daily_queries,
            "monthly_cost_usd": round(monthly_cost, 2),
            "monthly_cost_cny": round(monthly_cost * 7.2, 2),
            "cache_hit_rate": cache_hit_rate,
            "per_query_cost_cny": round(monthly_cost * 7.2 / (daily_queries * 30), 4),
        }

    def estimate_self_deploy_cost(self, gpu_type: str,
                                   num_gpus: int = 1,
                                   daily_queries: int = 10000,
                                   avg_latency_ms: int = 200) -> dict:
        """估算自部署模式月度成本"""
        gpu_monthly = self.GPU_COSTS[gpu_type] * num_gpus

        # 辅助服务成本（Redis, 向量库, 监控等）
        infra_cost = gpu_monthly * 0.3

        # 人力运维成本（估算）
        ops_cost = 5000 if num_gpus <= 2 else 15000

        total = gpu_monthly + infra_cost + ops_cost

        return {
            "gpu_type": gpu_type,
            "num_gpus": num_gpus,
            "gpu_cost": gpu_monthly,
            "infra_cost": infra_cost,
            "ops_cost": ops_cost,
            "monthly_total_cny": total,
            "per_query_cost_cny": round(total / (daily_queries * 30), 4),
        }

    def recommend(self, daily_queries: int,
                   model: str = "gpt-4o") -> dict:
        """推荐部署方式"""
        api = self.estimate_api_cost(model, daily_queries)
        self_deploy = self.estimate_self_deploy_cost("A100_80GB", 1, daily_queries)

        # 简化决策：API 月成本 < 自部署 → 选 API
        use_api = api["monthly_cost_cny"] < self_deploy["monthly_total_cny"]

        return {
            "recommended": "API" if use_api else "self_deploy",
            "api_monthly_cny": api["monthly_cost_cny"],
            "self_deploy_monthly_cny": self_deploy["monthly_total_cny"],
            "savings_cny": abs(api["monthly_cost_cny"] - self_deploy["monthly_total_cny"]),
            "breakeven_queries": self._breakeven(model),
        }

    def _breakeven(self, model: str) -> int:
        """计算 API 与自部署的盈亏平衡点（日查询数）"""
        # 简化：约 10K-50K QPS/天是常见盈亏平衡区间
        # 取决于模型大小、GPU 利用率、查询复杂度
        return 30000 if model in ["gpt-4o", "claude-sonnet-4-20250514"] else 80000
```

### 成本优化清单

| 优化项 | 预期节省 | 实施难度 |
|--------|---------|---------|
| Prompt Caching（OpenAI/Claude） | 50-90% input cost | 低 |
| 语义缓存（相似查询复用） | 20-40% 总调用 | 中 |
| 模型路由（简单查询用小模型） | 30-60% 总调用 | 中 |
| 批量 API（离线处理） | 50% API cost | 低 |
| 自部署开源模型（高频场景） | 70-90% API cost | 高 |
| Prompt 压缩（减少 token） | 10-30% input cost | 中 |

> **关键结论**：日均 < 10K 查询选 API，> 50K 查询考虑自部署，10K-50K 需根据具体场景计算。
