---
title: LLM API 调用实践
icon: fa6-solid:plug
order: 2
category:
  - AI实践
tag:
  - LLM
  - OpenAI
  - 通义千问
  - DeepSeek
---

# LLM API 调用实践

多厂商 LLM 接入是 AI 工程师的基本功。本文覆盖 OpenAI、通义千问、DeepSeek 三大厂商，并给出统一封装方案，让业务代码与具体厂商解耦。

## 1. OpenAI SDK 使用

### 安装与基础调用

```bash
pip install openai
```

```python
from openai import AsyncOpenAI

client = AsyncOpenAI(api_key="sk-...")

async def chat(message: str) -> str:
    response = await client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": message}],
        temperature=0.7,
        max_tokens=2048,
    )
    return response.choices[0].message.content
```

### 流式输出

```python
async def chat_stream(message: str):
    stream = await client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": message}],
        stream=True,
    )
    async for chunk in stream:
        delta = chunk.choices[0].delta.content
        if delta:
            yield delta
```

### 多轮对话

```python
from openai.types.chat import ChatCompletionMessageParam

async def multi_turn_chat(history: list[ChatCompletionMessageParam], new_msg: str) -> str:
    history.append({"role": "user", "content": new_msg})
    
    response = await client.chat.completions.create(
        model="gpt-4o",
        messages=history,
    )
    
    assistant_msg = response.choices[0].message.content
    history.append({"role": "assistant", "content": assistant_msg})
    return assistant_msg

# 使用
history = [{"role": "system", "content": "你是一个专业的 Python 导师"}]
await multi_turn_chat(history, "什么是装饰器？")
await multi_turn_chat(history, "给我一个实际的例子")  # 有上下文
```

### 结构化输出（Structured Output）

```python
from pydantic import BaseModel

class ProductInfo(BaseModel):
    name: str
    price: float
    category: str
    in_stock: bool

async def extract_product(text: str) -> ProductInfo:
    response = await client.beta.chat.completions.parse(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": "从文本中提取产品信息"},
            {"role": "user", "content": text},
        ],
        response_format=ProductInfo,
    )
    return response.choices[0].message.parsed
```

## 2. 通义千问接入

DashScope 兼容 OpenAI SDK，只需改 `base_url` 和 `api_key`。

```bash
pip install openai  # 直接用 openai SDK
```

```python
from openai import AsyncOpenAI

# 通义千问兼容 OpenAI 接口
qwen_client = AsyncOpenAI(
    api_key="sk-...",  # 阿里云 DashScope API Key
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)

async def qwen_chat(message: str) -> str:
    response = await qwen_client.chat.completions.create(
        model="qwen-max",  # 或 qwen-plus / qwen-turbo
        messages=[{"role": "user", "content": message}],
    )
    return response.choices[0].message.content
```

### 通义专有功能：长上下文 + 多模态

```python
# 图片理解（多模态）
async def analyze_image(image_url: str, question: str) -> str:
    response = await qwen_client.chat.completions.create(
        model="qwen-vl-max",
        messages=[{
            "role": "user",
            "content": [
                {"type": "image_url", "image_url": {"url": image_url}},
                {"type": "text", "text": question},
            ]
        }],
    )
    return response.choices[0].message.content
```

## 3. DeepSeek 接入

```python
from openai import AsyncOpenAI

deepseek_client = AsyncOpenAI(
    api_key="sk-...",
    base_url="https://api.deepseek.com",
)

async def deepseek_chat(message: str, use_reasoner: bool = False) -> str:
    model = "deepseek-reasoner" if use_reasoner else "deepseek-chat"
    response = await deepseek_client.chat.completions.create(
        model=model,
        messages=[{"role": "user", "content": message}],
    )
    return response.choices[0].message.content
```

### DeepSeek Reasoner 获取推理过程

```python
async def deepseek_with_reasoning(problem: str):
    response = await deepseek_client.chat.completions.create(
        model="deepseek-reasoner",
        messages=[{"role": "user", "content": problem}],
    )
    msg = response.choices[0].message
    print("=== 推理过程 ===")
    print(msg.reasoning_content)
    print("=== 最终答案 ===")
    print(msg.content)
```

## 4. 统一封装方案

生产环境中需要能灵活切换模型，业务代码不应感知具体厂商。

### 接口定义

```python
# llm/base.py
from abc import ABC, abstractmethod
from typing import AsyncIterator
from dataclasses import dataclass

@dataclass
class LLMMessage:
    role: str  # "system" | "user" | "assistant"
    content: str

@dataclass 
class LLMResponse:
    content: str
    model: str
    prompt_tokens: int
    completion_tokens: int

class BaseLLM(ABC):
    @abstractmethod
    async def complete(
        self,
        messages: list[LLMMessage],
        **kwargs,
    ) -> LLMResponse:
        ...

    @abstractmethod
    async def stream(
        self,
        messages: list[LLMMessage],
        **kwargs,
    ) -> AsyncIterator[str]:
        ...
```

### OpenAI 实现

```python
# llm/openai_llm.py
from openai import AsyncOpenAI
from .base import BaseLLM, LLMMessage, LLMResponse
from typing import AsyncIterator

class OpenAILLM(BaseLLM):
    def __init__(self, api_key: str, model: str = "gpt-4o", base_url: str | None = None):
        self.client = AsyncOpenAI(api_key=api_key, base_url=base_url)
        self.model = model

    def _to_openai_msgs(self, messages: list[LLMMessage]) -> list[dict]:
        return [{"role": m.role, "content": m.content} for m in messages]

    async def complete(self, messages: list[LLMMessage], **kwargs) -> LLMResponse:
        resp = await self.client.chat.completions.create(
            model=self.model,
            messages=self._to_openai_msgs(messages),
            **kwargs,
        )
        return LLMResponse(
            content=resp.choices[0].message.content,
            model=resp.model,
            prompt_tokens=resp.usage.prompt_tokens,
            completion_tokens=resp.usage.completion_tokens,
        )

    async def stream(self, messages: list[LLMMessage], **kwargs) -> AsyncIterator[str]:
        s = await self.client.chat.completions.create(
            model=self.model,
            messages=self._to_openai_msgs(messages),
            stream=True,
            **kwargs,
        )
        async for chunk in s:
            delta = chunk.choices[0].delta.content
            if delta:
                yield delta
```

### 工厂函数

```python
# llm/factory.py
from .base import BaseLLM
from .openai_llm import OpenAILLM
import os

def create_llm(provider: str = "openai", **kwargs) -> BaseLLM:
    match provider:
        case "openai":
            return OpenAILLM(
                api_key=os.getenv("OPENAI_API_KEY"),
                model=kwargs.get("model", "gpt-4o"),
            )
        case "qwen":
            return OpenAILLM(
                api_key=os.getenv("DASHSCOPE_API_KEY"),
                model=kwargs.get("model", "qwen-max"),
                base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
            )
        case "deepseek":
            return OpenAILLM(
                api_key=os.getenv("DEEPSEEK_API_KEY"),
                model=kwargs.get("model", "deepseek-chat"),
                base_url="https://api.deepseek.com",
            )
        case _:
            raise ValueError(f"Unknown provider: {provider}")
```

### 使用示例

```python
from llm.factory import create_llm
from llm.base import LLMMessage

# 只改这一行就能切换厂商
llm = create_llm("deepseek")

messages = [
    LLMMessage(role="system", content="你是一个代码审查专家"),
    LLMMessage(role="user", content="帮我审查这段 Python 代码：\n```python\ndef add(a, b): return a+b\n```"),
]

response = await llm.complete(messages)
print(response.content)
```

## 5. 环境配置与密钥管理

```bash
# .env 文件（不要提交到 git！）
OPENAI_API_KEY=sk-...
DASHSCOPE_API_KEY=sk-...
DEEPSEEK_API_KEY=sk-...

# 默认使用的模型提供商
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-chat
```

```python
# config.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    openai_api_key: str = ""
    dashscope_api_key: str = ""
    deepseek_api_key: str = ""
    llm_provider: str = "deepseek"
    llm_model: str = "deepseek-chat"

    class Config:
        env_file = ".env"

settings = Settings()
```

## 6. 常见模型对比

| 模型 | 适用场景 | 优势 | 注意 |
|------|---------|------|------|
| GPT-4o | 复杂推理、多模态 | 能力最强，接口稳定 | 贵，国内需代理 |
| GPT-4o-mini | 一般任务 | 性价比高 | 复杂任务能力弱些 |
| Qwen-Max | 中文任务 | 中文理解强，不需代理 | 价格适中 |
| DeepSeek-Chat | 代码/逻辑 | 极低价，代码能力强 | 并发限制 |
| DeepSeek-Reasoner | 复杂推理 | 推理能力强，R1 开源版 | 响应较慢 |

## 验收清单

- [ ] 能用 openai SDK 调用 GPT 非流式接口
- [ ] 能用相同代码（改 base_url）调用通义千问
- [ ] 能实现流式输出并在终端实时打印
- [ ] `create_llm("qwen")` 和 `create_llm("deepseek")` 能无缝切换
