---
title: 处方与药品管理
icon: fa6-solid:prescription-bottle-medical
order: 14
category:
  - 行业解决方案/医疗AI
---

# 处方与药品管理

## Pydantic Schema定义

```python
# app/schemas/prescription.py
from pydantic import BaseModel, Field, field_validator
from typing import Optional
from datetime import datetime
from enum import Enum

class PrescriptionType(str, Enum):
    ORDINARY = "普通处方"
    EMERGENCY = "急诊处方"
    PEDIATRIC = "儿科处方"
    ANESTHETIC = "麻醉药品处方"

class PrescriptionItemCreate(BaseModel):
    """处方项目创建"""
    drug_name: str = Field(..., min_length=2, max_length=100, description="药品通用名")
    brand_name: Optional[str] = Field(None, description="商品名")
    specification: str = Field(..., description="规格")
    dosage_form: Optional[str] = Field(None, description="剂型")
    quantity: str = Field(..., description="数量")
    dosage: str = Field(..., description="单次剂量")
    frequency: str = Field(..., description="频次: qd/bid/tid/qid/q8h/q12h/prn")
    route: str = Field("口服", description="给药途径")
    duration_days: Optional[int] = Field(None, ge=1, le=90, description="用药天数")
    sig: Optional[str] = Field(None, description="用法简写")
    remark: Optional[str] = Field(None, description="备注")

    @field_validator("frequency")
    @classmethod
    def validate_frequency(cls, v):
        valid = ["qd", "bid", "tid", "qid", "q8h", "q12h", "qn", "prn", "stat", "qod"]
        if v.lower() not in valid:
            raise ValueError(f"不支持的频次: {v}，有效值: {', '.join(valid)}")
        return v.lower()

class PrescriptionCreate(BaseModel):
    """开具处方请求"""
    patient_id: str = Field(..., description="患者ID")
    record_id: Optional[str] = Field(None, description="关联病历ID")
    prescription_type: PrescriptionType = Field(PrescriptionType.ORDINARY, description="处方类型")
    diagnosis: str = Field(..., min_length=2, description="临床诊断")
    icd10_code: Optional[str] = Field(None, description="诊断ICD-10编码")
    items: list[PrescriptionItemCreate] = Field(..., min_length=1, max_length=5, description="处方药品列表")

class PrescriptionResponse(BaseModel):
    """处方响应"""
    id: str
    patient_id: str
    doctor_id: str
    prescription_type: str
    diagnosis: str
    icd10_code: Optional[str] = None
    items: list[dict]
    is_reviewed: bool = False
    status: str = "active"
    created_at: str
    ai_disclaimer: str = "AI辅助审查结果仅供参考，最终由药师审核确认"

    model_config = {"from_attributes": True}

class PrescriptionCheckRequest(BaseModel):
    """合理用药审查请求"""
    patient_id: str
    drugs: list[dict] = Field(..., description="药品列表")
    patient_allergies: list[str] = Field(default_factory=list, description="过敏史")
    patient_conditions: list[str] = Field(default_factory=list, description="合并疾病")
    patient_age: Optional[int] = Field(None, ge=0, le=150)
    patient_gender: Optional[str] = None
    patient_egfr: Optional[float] = Field(None, ge=0, description="eGFR")

class AlertSeverity(str, Enum):
    INFO = "提示"
    WARNING = "警告"
    CRITICAL = "严重"
    BLOCKED = "禁止"

class MedicationAlertResponse(BaseModel):
    """用药审查提示"""
    alert_type: str
    severity: AlertSeverity
    drug_name: str
    message: str
    suggestion: Optional[str] = None
    reference: Optional[str] = None

class PrescriptionCheckResponse(BaseModel):
    """合理用药审查响应"""
    is_safe: bool
    alerts: list[MedicationAlertResponse]
    checked_at: str
    ai_disclaimer: str = "AI辅助审查结果仅供参考，最终由药师审核确认"
```

## 处方服务路由

```python
# app/routers/prescriptions.py
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from datetime import datetime
from typing import Optional

from app.database import get_db
from app.models.prescription import Prescription, PrescriptionItem
from app.models.patient import Patient
from app.schemas.prescription import (
    PrescriptionCreate, PrescriptionResponse,
    PrescriptionCheckRequest, PrescriptionCheckResponse,
    MedicationAlertResponse, AlertSeverity,
)
from app.services.prescription_service import PrescriptionService, MedicationReviewEngine
from app.core.security import get_current_user, require_role, UserRole, User

router = APIRouter(prefix="/api/v1/prescriptions", tags=["处方与药品管理"])

@router.post("", response_model=PrescriptionResponse, status_code=201)
async def create_prescription(
    data: PrescriptionCreate,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(require_role(UserRole.DOCTOR)),
):
    """开具处方"""
    # 验证患者
    patient = await db.get(Patient, data.patient_id)
    if not patient:
        raise HTTPException(status_code=404, detail="患者不存在")

    now = datetime.now().isoformat()

    # 创建处方
    prescription = Prescription(
        patient_id=data.patient_id,
        doctor_id=current_user.id,
        record_id=data.record_id,
        prescription_type=data.prescription_type.value,
        diagnosis=data.diagnosis,
        icd10_code=data.icd10_code,
        is_reviewed=False,
        status="active",
        created_at=now,
    )
    db.add(prescription)
    await db.flush()

    # 创建处方项目
    for idx, item_data in enumerate(data.items):
        sig = item_data.sig or f"{item_data.dosage} {item_data.route} {item_data.frequency}"
        item = PrescriptionItem(
            prescription_id=prescription.id,
            drug_name=item_data.drug_name,
            brand_name=item_data.brand_name,
            specification=item_data.specification,
            dosage_form=item_data.dosage_form,
            quantity=item_data.quantity,
            dosage=item_data.dosage,
            frequency=item_data.frequency,
            route=item_data.route,
            duration_days=item_data.duration_days,
            sig=sig,
            remark=item_data.remark,
            sort_order=idx,
        )
        db.add(item)

    await db.flush()
    await db.refresh(prescription)

    # 自动触发合理用药审查
    service = PrescriptionService(db)
    patient_allergies = [a.strip() for a in (patient.allergies or "").split(",") if a.strip()]
    check_result = service.review_prescription(
        prescription=prescription,
        patient_allergies=patient_allergies,
        patient_conditions=[],
        patient_egfr=None,
    )

    # 如果有严重或禁止级别警告，标记处方状态
    if any(a.severity in [AlertSeverity.CRITICAL, AlertSeverity.BLOCKED] for a in check_result["alerts"]):
        prescription.status = "pending_review"

    return PrescriptionResponse(
        id=prescription.id,
        patient_id=prescription.patient_id,
        doctor_id=prescription.doctor_id,
        prescription_type=prescription.prescription_type,
        diagnosis=prescription.diagnosis,
        icd10_code=prescription.icd10_code,
        items=[
            {
                "drug_name": item.drug_name,
                "specification": item.specification,
                "dosage": item.dosage,
                "frequency": item.frequency,
                "route": item.route,
                "duration_days": item.duration_days,
                "sig": item.sig,
            }
            for item in prescription.items
        ],
        is_reviewed=prescription.is_reviewed,
        status=prescription.status,
        created_at=prescription.created_at,
    )

@router.get("/{prescription_id}", response_model=PrescriptionResponse)
async def get_prescription(
    prescription_id: str,
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """获取处方详情"""
    prescription = await db.get(Prescription, prescription_id)
    if not prescription:
        raise HTTPException(status_code=404, detail="处方不存在")

    # 加载处方项目
    result = await db.execute(
        select(PrescriptionItem)
        .where(PrescriptionItem.prescription_id == prescription_id)
        .order_by(PrescriptionItem.sort_order)
    )
    items = result.scalars().all()

    return PrescriptionResponse(
        id=prescription.id,
        patient_id=prescription.patient_id,
        doctor_id=prescription.doctor_id,
        prescription_type=prescription.prescription_type,
        diagnosis=prescription.diagnosis,
        icd10_code=prescription.icd10_code,
        items=[
            {
                "drug_name": item.drug_name,
                "specification": item.specification,
                "quantity": item.quantity,
                "dosage": item.dosage,
                "frequency": item.frequency,
                "route": item.route,
                "duration_days": item.duration_days,
                "sig": item.sig,
                "remark": item.remark,
            }
            for item in items
        ],
        is_reviewed=prescription.is_reviewed,
        status=prescription.status,
        created_at=prescription.created_at,
    )

@router.post("/check", response_model=PrescriptionCheckResponse)
async def check_prescription(
    request: PrescriptionCheckRequest,
    current_user: User = Depends(require_role(UserRole.DOCTOR, UserRole.PHARMACIST)),
):
    """合理用药审查"""
    engine = MedicationReviewEngine()
    result = engine.check(
        drugs=request.drugs,
        patient_allergies=request.patient_allergies,
        patient_conditions=request.patient_conditions,
        patient_age=request.patient_age or 30,
        patient_gender=request.patient_gender or "未知",
        patient_egfr=request.patient_egfr,
    )

    is_safe = not any(a.severity in [AlertSeverity.CRITICAL, AlertSeverity.BLOCKED] for a in result)

    return PrescriptionCheckResponse(
        is_safe=is_safe,
        alerts=result,
        checked_at=datetime.now().isoformat(),
    )
```

## 合理用药审查引擎

```python
# app/services/prescription_service.py
from sqlalchemy.ext.asyncio import AsyncSession
from typing import Optional
from datetime import datetime

from app.models.prescription import Prescription, PrescriptionItem
from app.models.patient import Patient
from app.schemas.prescription import (
    MedicationAlertResponse, AlertSeverity,
)

class MedicationReviewEngine:
    """合理用药审查引擎"""

    # 过敏交叉反应映射
    ALLERGY_CROSS_REACTIVITY = {
        "青霉素": ["阿莫西林", "氨苄西林", "哌拉西林", "青霉素V钾", "阿莫西林克拉维酸钾"],
        "磺胺": ["磺胺甲噁唑", "复方新诺明", "甲氧苄啶"],
        "碘造影剂": ["碘海醇", "碘佛醇", "碘普罗胺", "碘帕醇"],
        "头孢菌素": ["头孢呋辛", "头孢曲松", "头孢克洛", "头孢丙烯"],
        " NSAIDs": ["布洛芬", "双氯芬酸", "吲哚美辛", "美洛昔康"],
    }

    # 药物相互作用库
    DRUG_INTERACTIONS = {
        frozenset(["华法林", "阿司匹林"]): {
            "severity": AlertSeverity.CRITICAL,
            "message": "华法林与阿司匹林联用显著增加出血风险",
            "suggestion": "避免联用，或密切监测INR和出血征象",
            "reference": "ACCP抗栓指南",
        },
        frozenset(["华法林", "布洛芬"]): {
            "severity": AlertSeverity.CRITICAL,
            "message": "华法林与NSAIDs联用增加出血风险",
            "suggestion": "如需镇痛，优先使用对乙酰氨基酚",
            "reference": "药品说明书",
        },
        frozenset(["甲氨蝶呤", "布洛芬"]): {
            "severity": AlertSeverity.BLOCKED,
            "message": "布洛芬减少甲氨蝶呤排泄，可致甲氨蝶呤中毒",
            "suggestion": "禁止合用，选用对乙酰氨基酚",
            "reference": "甲氨蝶呤药品说明书",
        },
        frozenset(["ACEI", "螺内酯"]): {
            "severity": AlertSeverity.WARNING,
            "message": "ACEI与螺内酯联用增加高钾血症风险",
            "suggestion": "合用时监测血钾，避免补钾",
            "reference": "ACEI临床应用专家共识",
        },
        frozenset(["地高辛", "胺碘酮"]): {
            "severity": AlertSeverity.WARNING,
            "message": "胺碘酮升高地高辛血药浓度，增加洋地黄中毒风险",
            "suggestion": "合用时地高辛减量50%，监测血药浓度",
            "reference": "地高辛临床应用指南",
        },
        frozenset(["二甲双胍", "造影剂"]): {
            "severity": AlertSeverity.WARNING,
            "message": "使用碘造影剂前后需停用二甲双胍，防止乳酸酸中毒",
            "suggestion": "造影前48h停用，造影后48h复查肾功能后恢复",
            "reference": "二甲双胍临床应用专家共识",
        },
        frozenset(["头孢曲松", "含钙输液"]): {
            "severity": AlertSeverity.BLOCKED,
            "message": "头孢曲松与钙离子可形成致命性沉淀",
            "suggestion": "禁止在同一输液管路中混合使用",
            "reference": "头孢曲松药品说明书",
        },
    }

    # 儿童用药禁忌
    PEDIATRIC_CONTRAINDICATIONS = {
        "喹诺酮类": ["左氧氟沙星", "环丙沙星", "莫西沙星"],
        "四环素类": ["多西环素", "米诺环素"],
        "氨基糖苷类": ["庆大霉素", "阿米卡星"],
    }

    # 孕妇用药禁忌
    PREGNANCY_CONTRAINDICATIONS = [
        "异维A酸", "华法林", "甲氨蝶呤", "利巴韦林",
        "ACEI", "ARB", "喹诺酮类", "四环素类",
    ]

    # 最大日剂量
    MAX_DAILY_DOSAGE = {
        "阿莫西林": {"adult_mg": 4000, "unit": "mg/day"},
        "二甲双胍": {"adult_mg": 2550, "unit": "mg/day"},
        "阿托伐他汀": {"adult_mg": 80, "unit": "mg/day"},
        "布洛芬": {"adult_mg": 2400, "unit": "mg/day"},
        "对乙酰氨基酚": {"adult_mg": 2000, "unit": "mg/day"},
        "奥美拉唑": {"adult_mg": 40, "unit": "mg/day"},
        "氨氯地平": {"adult_mg": 10, "unit": "mg/day"},
        "阿司匹林": {"adult_mg": 300, "unit": "mg/day(抗血小板)"},
    }

    # 肾功能不全禁忌
    RENAL_CONTRAINDICATIONS = {
        "二甲双胍": {"egfr_threshold": 30, "message": "eGFR<30时二甲双胍禁忌"},
        "华法林": {"egfr_threshold": 15, "message": "严重肾功能不全时需调整剂量"},
    }

    def check(
        self,
        drugs: list[dict],
        patient_allergies: list[str],
        patient_conditions: list[str],
        patient_age: int = 30,
        patient_gender: str = "未知",
        patient_egfr: Optional[float] = None,
    ) -> list[MedicationAlertResponse]:
        """完整合理用药审查"""
        alerts = []

        # 1. 过敏检查
        alerts.extend(self._check_allergies(drugs, patient_allergies))

        # 2. 配伍禁忌
        alerts.extend(self._check_interactions(drugs))

        # 3. 剂量审查
        alerts.extend(self._check_dosage(drugs, patient_age))

        # 4. 重复用药
        alerts.extend(self._check_duplicates(drugs))

        # 5. 儿童用药禁忌
        if patient_age < 18:
            alerts.extend(self._check_pediatric(drugs))

        # 6. 孕妇用药禁忌
        if "妊娠" in patient_conditions or "孕妇" in patient_conditions:
            alerts.extend(self._check_pregnancy(drugs))

        # 7. 肾功能不全
        if patient_egfr is not None:
            alerts.extend(self._check_renal(drugs, patient_egfr))

        # 8. 处方药品数量
        if len(drugs) > 5:
            alerts.append(MedicationAlertResponse(
                alert_type="处方规范性",
                severity=AlertSeverity.WARNING,
                drug_name="",
                message=f"处方含{len(drugs)}种药品，超过5种上限",
                suggestion="减少用药种类或分次开具",
            ))

        return alerts

    def _check_allergies(self, drugs: list[dict], allergies: list[str]) -> list[MedicationAlertResponse]:
        """过敏检查"""
        alerts = []
        for allergen in allergies:
            if allergen in self.ALLERGY_CROSS_REACTIVITY:
                cross_drugs = self.ALLERGY_CROSS_REACTIVITY[allergen]
                for drug in drugs:
                    drug_name = drug.get("drug_name", "")
                    if drug_name in cross_drugs:
                        alerts.append(MedicationAlertResponse(
                            alert_type="过敏禁忌",
                            severity=AlertSeverity.BLOCKED,
                            drug_name=drug_name,
                            message=f"患者对{allergen}过敏，禁用{drug_name}",
                            suggestion="更换为替代药物",
                            reference="过敏史记录",
                        ))
        return alerts

    def _check_interactions(self, drugs: list[dict]) -> list[MedicationAlertResponse]:
        """配伍禁忌检查"""
        alerts = []
        drug_names = [d.get("drug_name", "") for d in drugs]

        for i in range(len(drug_names)):
            for j in range(i + 1, len(drug_names)):
                pair = frozenset([drug_names[i], drug_names[j]])
                if pair in self.DRUG_INTERACTIONS:
                    interaction = self.DRUG_INTERACTIONS[pair]
                    alerts.append(MedicationAlertResponse(
                        alert_type="配伍禁忌",
                        severity=interaction["severity"],
                        drug_name=f"{drug_names[i]} + {drug_names[j]}",
                        message=interaction["message"],
                        suggestion=interaction.get("suggestion"),
                        reference=interaction.get("reference"),
                    ))
        return alerts

    def _check_dosage(self, drugs: list[dict], patient_age: int) -> list[MedicationAlertResponse]:
        """剂量审查"""
        alerts = []
        for drug in drugs:
            drug_name = drug.get("drug_name", "")
            daily_dose = drug.get("daily_dose_mg", 0)

            if drug_name in self.MAX_DAILY_DOSAGE and daily_dose:
                max_dose = self.MAX_DAILY_DOSAGE[drug_name]["adult_mg"]
                unit = self.MAX_DAILY_DOSAGE[drug_name]["unit"]

                if patient_age >= 65:
                    max_dose = int(max_dose * 0.75)  # 老年人减量

                try:
                    dose_val = float(daily_dose)
                    if dose_val > max_dose:
                        alerts.append(MedicationAlertResponse(
                            alert_type="剂量异常",
                            severity=AlertSeverity.CRITICAL,
                            drug_name=drug_name,
                            message=f"日剂量{daily_dose}{unit}超过推荐最大剂量{max_dose}{unit}",
                            suggestion=f"建议调整至不超过{max_dose}{unit}",
                        ))
                except (ValueError, TypeError):
                    pass
        return alerts

    def _check_duplicates(self, drugs: list[dict]) -> list[MedicationAlertResponse]:
        """重复用药检查"""
        alerts = []
        DRUG_CLASS = {
            "阿莫西林": "青霉素类", "阿莫西林克拉维酸钾": "青霉素类",
            "头孢呋辛": "头孢菌素类", "头孢曲松": "头孢菌素类",
            "布洛芬": "NSAIDs", "双氯芬酸": "NSAIDs",
            "奥美拉唑": "PPI", "兰索拉唑": "PPI", "泮托拉唑": "PPI",
            "氨氯地平": "CCB", "硝苯地平": "CCB",
            "依那普利": "ACEI", "卡托普利": "ACEI",
        }

        class_drugs: dict[str, list[str]] = {}
        for drug in drugs:
            name = drug.get("drug_name", "")
            drug_class = DRUG_CLASS.get(name)
            if drug_class:
                class_drugs.setdefault(drug_class, []).append(name)

        for drug_class, drug_list in class_drugs.items():
            if len(drug_list) > 1:
                alerts.append(MedicationAlertResponse(
                    alert_type="重复用药",
                    severity=AlertSeverity.WARNING,
                    drug_name=" + ".join(drug_list),
                    message=f"同类别({drug_class})药物重复使用",
                    suggestion="保留一种即可",
                ))
        return alerts

    def _check_pediatric(self, drugs: list[dict]) -> list[MedicationAlertResponse]:
        """儿童用药禁忌"""
        alerts = []
        for category, drug_list in self.PEDIATRIC_CONTRAINDICATIONS.items():
            for drug in drugs:
                if drug.get("drug_name", "") in drug_list:
                    alerts.append(MedicationAlertResponse(
                        alert_type="儿童用药禁忌",
                        severity=AlertSeverity.BLOCKED,
                        drug_name=drug["drug_name"],
                        message=f"{drug['drug_name']}({category})禁用于18岁以下患者",
                        suggestion="更换为儿童适用药物",
                    ))
        return alerts

    def _check_pregnancy(self, drugs: list[dict]) -> list[MedicationAlertResponse]:
        """孕妇用药禁忌"""
        alerts = []
        for drug in drugs:
            drug_name = drug.get("drug_name", "")
            if drug_name in self.PREGNANCY_CONTRAINDICATIONS:
                alerts.append(MedicationAlertResponse(
                    alert_type="孕妇用药禁忌",
                    severity=AlertSeverity.BLOCKED,
                    drug_name=drug_name,
                    message=f"妊娠期禁用{drug_name}",
                    suggestion="更换为妊娠期安全药物",
                    reference="妊娠期用药指南",
                ))
        return alerts

    def _check_renal(self, drugs: list[dict], egfr: float) -> list[MedicationAlertResponse]:
        """肾功能不全审查"""
        alerts = []
        for drug in drugs:
            drug_name = drug.get("drug_name", "")
            if drug_name in self.RENAL_CONTRAINDICATIONS:
                rule = self.RENAL_CONTRAINDICATIONS[drug_name]
                if egfr < rule["egfr_threshold"]:
                    alerts.append(MedicationAlertResponse(
                        alert_type="肾功能禁忌",
                        severity=AlertSeverity.BLOCKED,
                        drug_name=drug_name,
                        message=f"eGFR={egfr:.0f}，{rule['message']}",
                        suggestion="更换为肾安全性药物或调整剂量",
                    ))
        return alerts


class PrescriptionService:
    """处方服务"""

    def __init__(self, db: AsyncSession):
        self.db = db
        self.review_engine = MedicationReviewEngine()

    def review_prescription(
        self,
        prescription: Prescription,
        patient_allergies: list[str],
        patient_conditions: list[str],
        patient_egfr: Optional[float] = None,
    ) -> dict:
        """审查处方"""
        drugs = [
            {
                "drug_name": item.drug_name,
                "dosage": item.dosage,
                "frequency": item.frequency,
            }
            for item in prescription.items
        ]

        alerts = self.review_engine.check(
            drugs=drugs,
            patient_allergies=patient_allergies,
            patient_conditions=patient_conditions,
            patient_egfr=patient_egfr,
        )

        is_safe = not any(a.severity in [AlertSeverity.CRITICAL, AlertSeverity.BLOCKED] for a in alerts)

        return {
            "prescription_id": prescription.id,
            "is_safe": is_safe,
            "alerts": alerts,
        }
```

## 药品数据库管理

```python
# app/services/drug_service.py
from pydantic import BaseModel, Field
from typing import Optional
from enum import Enum

class DrugCategory(str, Enum):
    ANTI_INFECTIVE = "抗感染药物"
    CARDIOVASCULAR = "心血管系统"
    DIGESTIVE = "消化系统"
    RESPIRATORY = "呼吸系统"
    ENDOCRINE = "内分泌系统"
    NERVOUS = "神经系统"
    ONCOLOGY = "抗肿瘤药物"

class DrugInfo(BaseModel):
    """药品信息"""
    drug_name: str
    brand_name: Optional[str] = None
    category: DrugCategory
    specification: str
    dosage_form: str
    route: str
    is_prescription: bool = True
    is_national_essential: bool = False
    atc_code: Optional[str] = None
    max_daily_dose_mg: Optional[float] = None
    storage_condition: str = "密封保存"
    pregnancy_category: Optional[str] = None  # A/B/C/D/X
    pediatric_safe: bool = True

class DrugDatabase:
    """药品数据库"""

    DRUGS: dict[str, DrugInfo] = {
        "阿莫西林": DrugInfo(
            drug_name="阿莫西林", brand_name="弗莱莫星",
            category=DrugCategory.ANTI_INFECTIVE,
            specification="0.5g/粒", dosage_form="胶囊", route="口服",
            is_prescription=True, is_national_essential=True,
            atc_code="J01CA04", max_daily_dose_mg=4000,
            pregnancy_category="B", pediatric_safe=True,
        ),
        "头孢呋辛": DrugInfo(
            drug_name="头孢呋辛", brand_name="西力欣",
            category=DrugCategory.ANTI_INFECTIVE,
            specification="0.75g/支", dosage_form="注射液", route="静脉滴注",
            is_prescription=True, is_national_essential=True,
            atc_code="J01DC02", max_daily_dose_mg=4500,
            pregnancy_category="B", pediatric_safe=True,
        ),
        "阿奇霉素": DrugInfo(
            drug_name="阿奇霉素", brand_name="希舒美",
            category=DrugCategory.ANTI_INFECTIVE,
            specification="0.25g/片", dosage_form="片剂", route="口服",
            is_prescription=True, is_national_essential=True,
            atc_code="J01FA10", max_daily_dose_mg=500,
            pregnancy_category="B", pediatric_safe=True,
        ),
        "奥美拉唑": DrugInfo(
            drug_name="奥美拉唑", brand_name="洛赛克",
            category=DrugCategory.DIGESTIVE,
            specification="20mg/粒", dosage_form="胶囊", route="口服",
            is_prescription=True, is_national_essential=True,
            atc_code="A02BC01", max_daily_dose_mg=40,
            pregnancy_category="C", pediatric_safe=False,
        ),
        "二甲双胍": DrugInfo(
            drug_name="二甲双胍", brand_name="格华止",
            category=DrugCategory.ENDOCRINE,
            specification="0.5g/片", dosage_form="片剂", route="口服",
            is_prescription=True, is_national_essential=True,
            atc_code="A10BA02", max_daily_dose_mg=2550,
            pregnancy_category="C", pediatric_safe=True,
        ),
        "氨氯地平": DrugInfo(
            drug_name="氨氯地平", brand_name="络活喜",
            category=DrugCategory.CARDIOVASCULAR,
            specification="5mg/片", dosage_form="片剂", route="口服",
            is_prescription=True, is_national_essential=True,
            atc_code="C08CA01", max_daily_dose_mg=10,
            pregnancy_category="C", pediatric_safe=False,
        ),
        "阿托伐他汀": DrugInfo(
            drug_name="阿托伐他汀", brand_name="立普妥",
            category=DrugCategory.CARDIOVASCULAR,
            specification="20mg/片", dosage_form="片剂", route="口服",
            is_prescription=True, is_national_essential=True,
            atc_code="C10AA05", max_daily_dose_mg=80,
            pregnancy_category="X", pediatric_safe=False,
        ),
        "布洛芬": DrugInfo(
            drug_name="布洛芬", brand_name="芬必得",
            category=DrugCategory.NERVOUS,
            specification="0.2g/片", dosage_form="片剂", route="口服",
            is_prescription=False, is_national_essential=True,
            atc_code="M01AE01", max_daily_dose_mg=2400,
            pregnancy_category="C(D-晚期)", pediatric_safe=True,
        ),
    }

    @classmethod
    def get_drug(cls, drug_name: str) -> Optional[DrugInfo]:
        """查询药品信息"""
        return cls.DRUGS.get(drug_name)

    @classmethod
    def search_drugs(cls, keyword: str) -> list[DrugInfo]:
        """搜索药品"""
        return [
            drug for drug in cls.DRUGS.values()
            if keyword in drug.drug_name or (drug.brand_name and keyword in drug.brand_name)
        ]

    @classmethod
    def get_drugs_by_category(cls, category: DrugCategory) -> list[DrugInfo]:
        """按分类查询药品"""
        return [drug for drug in cls.DRUGS.values() if drug.category == category]
```

## 处方模板与常用处方

```python
# app/services/prescription_template.py
from pydantic import BaseModel, Field
from typing import Optional

class PrescriptionTemplateItem(BaseModel):
    """处方模板药品项"""
    drug_name: str
    specification: str
    dosage: str
    frequency: str
    route: str
    duration_days: int
    sig: str

class PrescriptionTemplate(BaseModel):
    """处方模板"""
    template_id: str
    name: str
    department: str
    diagnosis: str
    icd10_code: Optional[str] = None
    items: list[PrescriptionTemplateItem]

# 常用处方模板
COMMON_PRESCRIPTIONS: list[PrescriptionTemplate] = [
    PrescriptionTemplate(
        template_id="RX-TPL-001",
        name="社区获得性肺炎(门诊)",
        department="呼吸内科",
        diagnosis="社区获得性肺炎",
        icd10_code="J18.9",
        items=[
            PrescriptionTemplateItem(
                drug_name="阿莫西林", specification="0.5g/粒",
                dosage="0.5g", frequency="tid", route="口服",
                duration_days=7, sig="0.5g po tid x 7天",
            ),
            PrescriptionTemplateItem(
                drug_name="氨溴索", specification="30mg/片",
                dosage="30mg", frequency="tid", route="口服",
                duration_days=7, sig="30mg po tid x 7天",
            ),
        ],
    ),
    PrescriptionTemplate(
        template_id="RX-TPL-002",
        name="胃溃疡(标准三联)",
        department="消化内科",
        diagnosis="胃溃疡",
        icd10_code="K25.9",
        items=[
            PrescriptionTemplateItem(
                drug_name="奥美拉唑", specification="20mg/粒",
                dosage="20mg", frequency="bid", route="口服",
                duration_days=14, sig="20mg po bid x 14天",
            ),
            PrescriptionTemplateItem(
                drug_name="阿莫西林", specification="0.5g/粒",
                dosage="1.0g", frequency="bid", route="口服",
                duration_days=14, sig="1.0g po bid x 14天",
            ),
            PrescriptionTemplateItem(
                drug_name="克拉霉素", specification="0.25g/片",
                dosage="0.5g", frequency="bid", route="口服",
                duration_days=14, sig="0.5g po bid x 14天",
            ),
        ],
    ),
    PrescriptionTemplate(
        template_id="RX-TPL-003",
        name="2型糖尿病(初诊)",
        department="内分泌科",
        diagnosis="2型糖尿病",
        icd10_code="E11.9",
        items=[
            PrescriptionTemplateItem(
                drug_name="二甲双胍", specification="0.5g/片",
                dosage="0.5g", frequency="bid", route="口服",
                duration_days=30, sig="0.5g po bid",
            ),
        ],
    ),
    PrescriptionTemplate(
        template_id="RX-TPL-004",
        name="高血压(初诊)",
        department="心内科",
        diagnosis="原发性高血压",
        icd10_code="I10",
        items=[
            PrescriptionTemplateItem(
                drug_name="氨氯地平", specification="5mg/片",
                dosage="5mg", frequency="qd", route="口服",
                duration_days=30, sig="5mg po qd",
            ),
        ],
    ),
]

def get_template(template_id: str) -> Optional[PrescriptionTemplate]:
    """获取处方模板"""
    for tpl in COMMON_PRESCRIPTIONS:
        if tpl.template_id == template_id:
            return tpl
    return None

def get_templates_by_diagnosis(diagnosis: str) -> list[PrescriptionTemplate]:
    """按诊断搜索处方模板"""
    return [tpl for tpl in COMMON_PRESCRIPTIONS if diagnosis in tpl.diagnosis or tpl.diagnosis in diagnosis]
```

## LLM辅助处方审查

```python
# app/services/llm_prescription_review.py
from pydantic import BaseModel, Field
from typing import Optional

class LLMPrescriptionReviewService:
    """LLM辅助处方审查"""

    REVIEW_PROMPT = """请审查以下处方的合理性和安全性：

## 患者信息
- 年龄：{age}岁，性别：{gender}
- 过敏史：{allergies}
- 合并疾病：{conditions}
- eGFR：{egfr}

## 处方内容
{prescription_content}

## 审查要点
1. 药物适应症是否匹配诊断
2. 剂量是否在推荐范围内
3. 是否存在配伍禁忌
4. 是否考虑了患者特殊情况(过敏/肝肾功能/年龄)
5. 用药方案是否简洁合理

## 输出格式
请以JSON格式输出审查结果：
{{
    "is_appropriate": true/false,
    "issues": [
        {{
            "type": "配伍禁忌/剂量异常/适应症不符/特殊人群/其他",
            "severity": "提示/警告/严重/禁止",
            "drug_name": "药品名",
            "description": "问题描述",
            "suggestion": "修改建议"
        }}
    ],
    "optimization_suggestions": ["优化建议1", "优化建议2"]
}}

## 重要声明
审查结果仅供药师参考，不能替代药师的专业判断。"""

    async def review_with_llm(
        self,
        prescription_data: dict,
        patient_data: dict,
    ) -> dict:
        """使用LLM审查处方"""
        prescription_content = "\n".join(
            f"- {item['drug_name']} {item.get('specification', '')} "
            f"{item.get('sig', item.get('dosage', ''))}"
            for item in prescription_data.get("items", [])
        )

        prompt = self.REVIEW_PROMPT.format(
            age=patient_data.get("age", "未知"),
            gender=patient_data.get("gender", "未知"),
            allergies=patient_data.get("allergies", "无"),
            conditions=patient_data.get("conditions", "无"),
            egfr=patient_data.get("egfr", "未检测"),
            prescription_content=prescription_content,
        )

        # 实际调用LLM API
        # response = await self.llm.call(prompt)
        return {"is_appropriate": True, "issues": [], "optimization_suggestions": []}
```

## 药品不良反应报告

```python
# app/models/adr_report.py
import uuid
from sqlalchemy import Column, String, Text, Boolean
from app.database import Base

class ADRReport(Base):
    """药品不良反应报告"""
    __tablename__ = "adr_reports"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    patient_id = Column(String(36), nullable=False)
    reporter_id = Column(String(36), nullable=False, comment="报告人ID")

    drug_name = Column(String(100), nullable=False, comment="可疑药品")
    dosage = Column(String(50), comment="用药剂量")
    route = Column(String(30), comment="给药途径")
    start_date = Column(String(30), comment="用药开始日期")
    stop_date = Column(String(30), comment="用药结束日期")

    adverse_reaction = Column(Text, nullable=False, comment="不良反应描述")
    onset_date = Column(String(30), comment="不良反应发生时间")
    severity = Column(String(10), comment="一般/严重/危及生命")
    outcome = Column(String(20), comment="好转/未好转/死亡/不详")
    causality = Column(String(20), comment="肯定/很可能/可能/可能无关/无法评价")

    description = Column(Text, comment="详细经过描述")
    management = Column(Text, comment="处理措施")
    rechallenge = Column(String(10), comment="再次用药后是否再次出现：是/否/未再用药")

    is_submitted = Column(Boolean, default=False, comment="是否已上报")
    submitted_at = Column(String(30), comment="上报时间")

    created_at = Column(String(30), nullable=False)
```
