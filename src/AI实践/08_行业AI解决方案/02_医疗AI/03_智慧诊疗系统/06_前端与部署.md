---
title: 前端与部署
icon: fa6-solid:display
order: 15
category:
  - 行业解决方案/医疗AI
---

# 前端与部署

## React前端架构

### 技术栈

| 技术 | 用途 |
|------|------|
| React 18 | UI框架 |
| TypeScript | 类型安全 |
| Ant Design 5 | UI组件库 |
| React Router v6 | 路由管理 |
| Axios | HTTP请求 |
| Zustand | 状态管理 |
| ECharts | 数据可视化 |
| react-dicom-viewer | DICOM影像查看 |

### 项目结构

```
smart-clinic-web/
├── public/
├── src/
│   ├── api/                    # API请求封装
│   │   ├── patient.ts
│   │   ├── record.ts
│   │   ├── diagnosis.ts
│   │   └── prescription.ts
│   ├── components/             # 通用组件
│   │   ├── Layout/
│   │   └── Common/
│   ├── pages/                  # 页面组件
│   │   ├── PatientDashboard/
│   │   ├── MedicalRecordEditor/
│   │   ├── AIDiagnosisPanel/
│   │   ├── PrescriptionEditor/
│   │   └── DicomViewer/
│   ├── stores/                 # 状态管理
│   │   ├── patientStore.ts
│   │   └── diagnosisStore.ts
│   ├── types/                  # TypeScript类型
│   │   └── index.ts
│   ├── utils/                  # 工具函数
│   │   └── request.ts
│   ├── App.tsx
│   └── main.tsx
├── package.json
└── vite.config.ts
```

## 核心页面组件

### PatientDashboard - 患者管理

```tsx
// src/pages/PatientDashboard/index.tsx
import React, { useState, useEffect } from 'react';
import {
  Card, Table, Button, Input, Modal, Form, Space,
  Tag, message, Popconfirm, Row, Col, Statistic,
} from 'antd';
import { UserAddOutlined, SearchOutlined } from '@ant-design/icons';
import type { ColumnsType } from 'antd/es/table';

interface Patient {
  id: string;
  name: string;
  gender: string;
  birth_date: string;
  id_card: string;
  phone: string;
  allergies: string;
  insurance_type: string;
  created_at: string;
}

const PatientDashboard: React.FC = () => {
  const [patients, setPatients] = useState<Patient[]>([]);
  const [loading, setLoading] = useState(false);
  const [total, setTotal] = useState(0);
  const [page, setPage] = useState(1);
  const [search, setSearch] = useState('');
  const [modalVisible, setModalVisible] = useState(false);
  const [form] = Form.useForm();

  const fetchPatients = async (pageNum: number = 1, keyword: string = '') => {
    setLoading(true);
    try {
      const params = new URLSearchParams({
        page: String(pageNum),
        page_size: '20',
        ...(keyword ? { search: keyword } : {}),
      });
      const response = await fetch(`/api/v1/patients?${params}`);
      const data = await response.json();
      setPatients(data.items);
      setTotal(data.total);
    } catch (error) {
      message.error('获取患者列表失败');
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => {
    fetchPatients();
  }, []);

  const handleCreatePatient = async (values: any) => {
    try {
      const response = await fetch('/api/v1/patients', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(values),
      });
      if (response.ok) {
        message.success('患者注册成功');
        setModalVisible(false);
        form.resetFields();
        fetchPatients(page, search);
      } else {
        const err = await response.json();
        message.error(err.detail || '注册失败');
      }
    } catch (error) {
      message.error('网络错误');
    }
  };

  const columns: ColumnsType<Patient> = [
    { title: '姓名', dataIndex: 'name', width: 100 },
    { title: '性别', dataIndex: 'gender', width: 60 },
    { title: '出生日期', dataIndex: 'birth_date', width: 110 },
    { title: '身份证号', dataIndex: 'id_card', width: 180 },
    { title: '手机号', dataIndex: 'phone', width: 130 },
    {
      title: '过敏史', dataIndex: 'allergies', width: 150,
      render: (text: string) => text
        ? text.split(',').map((a: string) => <Tag key={a} color="red">{a}</Tag>)
        : <Tag color="green">无</Tag>,
    },
    { title: '医保类型', dataIndex: 'insurance_type', width: 120 },
    {
      title: '操作', width: 150, fixed: 'right',
      render: (_, record) => (
        <Space>
          <Button type="link" size="small" onClick={() => window.open(`/patients/${record.id}/records`, '_self')}>
            病历
          </Button>
          <Button type="link" size="small" onClick={() => window.open(`/diagnosis?patient_id=${record.id}`, '_self')}>
            AI诊断
          </Button>
        </Space>
      ),
    },
  ];

  return (
    <div>
      <Card title="患者管理" extra={
        <Space>
          <Input
            placeholder="搜索姓名/身份证号"
            prefix={<SearchOutlined />}
            value={search}
            onChange={e => setSearch(e.target.value)}
            onPressEnter={() => fetchPatients(1, search)}
            style={{ width: 250 }}
          />
          <Button type="primary" icon={<UserAddOutlined />} onClick={() => setModalVisible(true)}>
            新增患者
          </Button>
        </Space>
      }>
        <Table
          columns={columns}
          dataSource={patients}
          rowKey="id"
          loading={loading}
          pagination={{
            current: page,
            total,
            pageSize: 20,
            onChange: (p) => { setPage(p); fetchPatients(p, search); },
          }}
          scroll={{ x: 1200 }}
        />
      </Card>

      <Modal
        title="新增患者"
        open={modalVisible}
        onCancel={() => setModalVisible(false)}
        onOk={() => form.submit()}
        width={600}
      >
        <Form form={form} layout="vertical" onFinish={handleCreatePatient}>
          <Row gutter={16}>
            <Col span={12}>
              <Form.Item name="name" label="姓名" rules={[{ required: true }]}>
                <Input />
              </Form.Item>
            </Col>
            <Col span={12}>
              <Form.Item name="gender" label="性别" rules={[{ required: true }]}>
                <Input />
              </Form.Item>
            </Col>
          </Row>
          <Row gutter={16}>
            <Col span={12}>
              <Form.Item name="birth_date" label="出生日期" rules={[{ required: true }]}>
                <Input type="date" />
              </Form.Item>
            </Col>
            <Col span={12}>
              <Form.Item name="id_card" label="身份证号" rules={[{ required: true, len: 18 }]}>
                <Input maxLength={18} />
              </Form.Item>
            </Col>
          </Row>
          <Row gutter={16}>
            <Col span={12}>
              <Form.Item name="phone" label="手机号">
                <Input />
              </Form.Item>
            </Col>
            <Col span={12}>
              <Form.Item name="blood_type" label="血型">
                <Input />
              </Form.Item>
            </Col>
          </Row>
          <Form.Item name="allergies" label="过敏史">
            <Input placeholder="多种过敏原用逗号分隔" />
          </Form.Item>
          <Form.Item name="medical_history" label="既往史">
            <Input.TextArea rows={2} />
          </Form.Item>
        </Form>
      </Modal>
    </div>
  );
};

export default PatientDashboard;
```

### MedicalRecordEditor - 病历编辑器

```tsx
// src/pages/MedicalRecordEditor/index.tsx
import React, { useState, useEffect } from 'react';
import {
  Card, Form, Input, Select, Button, Space, Divider,
  Tabs, Table, Modal, message, Tag, Row, Col,
} from 'antd';
import { SaveOutlined, PlusOutlined } from '@ant-design/icons';

const { TextArea } = Input;

interface DiagnosisItem {
  id: string;
  disease_name: string;
  icd10_code: string;
  is_primary: boolean;
  diagnosis_type: string;
}

const MedicalRecordEditor: React.FC<{ patientId: string }> = ({ patientId }) => {
  const [form] = Form.useForm();
  const [diagnoses, setDiagnoses] = useState<DiagnosisItem[]>([]);
  const [diagModalVisible, setDiagModalVisible] = useState(false);
  const [diagForm] = Form.useForm();

  const handleSave = async () => {
    try {
      const values = await form.validateFields();
      const response = await fetch(`/api/v1/patients/${patientId}/records`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(values),
      });
      if (response.ok) {
        message.success('病历保存成功');
      }
    } catch (error) {
      message.error('保存失败');
    }
  };

  const handleAddDiagnosis = async () => {
    try {
      const values = await diagForm.validateFields();
      // 如果是主诊断，取消其他主诊断
      if (values.is_primary) {
        setDiagnoses(prev => prev.map(d => ({ ...d, is_primary: false })));
      }
      setDiagnoses(prev => [...prev, {
        id: `diag-${Date.now()}`,
        ...values,
      }]);
      setDiagModalVisible(false);
      diagForm.resetFields();
    } catch (error) {
      // validation error
    }
  };

  const diagnosisColumns = [
    { title: '诊断', dataIndex: 'disease_name', key: 'disease' },
    { title: 'ICD-10', dataIndex: 'icd10_code', key: 'icd10', width: 100 },
    {
      title: '主诊断', dataIndex: 'is_primary', key: 'primary', width: 80,
      render: (v: boolean) => v ? <Tag color="red">主</Tag> : null,
    },
    { title: '类型', dataIndex: 'diagnosis_type', key: 'type', width: 100 },
    {
      title: '操作', key: 'action', width: 80,
      render: (_: any, record: DiagnosisItem) => (
        <Button type="link" danger size="small" onClick={() => {
          setDiagnoses(prev => prev.filter(d => d.id !== record.id));
        }}>删除</Button>
      ),
    },
  ];

  return (
    <div>
      <Card title="病历编辑" extra={
        <Space>
          <Button icon={<SaveOutlined />} type="primary" onClick={handleSave}>保存</Button>
        </Space>
      }>
        <Form form={form} layout="vertical">
          <Form.Item name="encounter_type" label="就诊类型" rules={[{ required: true }]}>
            <Select style={{ width: 200 }}>
              <Select.Option value="门诊">门诊</Select.Option>
              <Select.Option value="急诊">急诊</Select.Option>
              <Select.Option value="住院">住院</Select.Option>
            </Select>
          </Form.Item>

          <Form.Item name="chief_complaint" label="主诉" rules={[{ required: true }]}>
            <Input placeholder="主要症状+持续时间，如：反复上腹痛3天" />
          </Form.Item>

          <Form.Item name="present_illness" label="现病史">
            <TextArea rows={4} placeholder="详细描述发病经过、伴随症状、诊治经过" />
          </Form.Item>

          <Row gutter={16}>
            <Col span={12}>
              <Form.Item name="past_history" label="既往史">
                <TextArea rows={3} />
              </Form.Item>
            </Col>
            <Col span={12}>
              <Form.Item name="allergy_history" label="过敏史">
                <TextArea rows={3} />
              </Form.Item>
            </Col>
          </Row>

          <Form.Item name="physical_exam" label="体格检查">
            <TextArea rows={3} placeholder="T: P: R: BP:\n专科检查..." />
          </Form.Item>
        </Form>

        <Divider>诊断</Divider>

        <Table
          columns={diagnosisColumns}
          dataSource={diagnoses}
          rowKey="id"
          pagination={false}
          size="small"
          footer={() => (
            <Button type="dashed" block icon={<PlusOutlined />} onClick={() => setDiagModalVisible(true)}>
              添加诊断
            </Button>
          )}
        />
      </Card>

      <Modal
        title="添加诊断"
        open={diagModalVisible}
        onCancel={() => setDiagModalVisible(false)}
        onOk={handleAddDiagnosis}
      >
        <Form form={diagForm} layout="vertical">
          <Form.Item name="disease_name" label="疾病名称" rules={[{ required: true }]}>
            <Input placeholder="输入疾病名称" />
          </Form.Item>
          <Form.Item name="icd10_code" label="ICD-10编码">
            <Input placeholder="自动填充或手动输入" />
          </Form.Item>
          <Form.Item name="diagnosis_type" label="诊断类型" rules={[{ required: true }]}>
            <Select>
              <Select.Option value="初步诊断">初步诊断</Select.Option>
              <Select.Option value="修正诊断">修正诊断</Select.Option>
              <Select.Option value="出院诊断">出院诊断</Select.Option>
            </Select>
          </Form.Item>
          <Form.Item name="is_primary" label="是否主诊断" valuePropName="checked">
            <Input type="checkbox" />
          </Form.Item>
        </Form>
      </Modal>
    </div>
  );
};

export default MedicalRecordEditor;
```

### AIDiagnosisPanel - AI诊断面板

```tsx
// src/pages/AIDiagnosisPanel/index.tsx
import React, { useState } from 'react';
import {
  Card, Input, Button, Tag, List, Progress, Alert,
  Descriptions, Spin, Steps, Select, Space, Divider, Tooltip,
} from 'antd';
import { RobotOutlined, WarningOutlined, CheckCircleOutlined } from '@ant-design/icons';

interface DifferentialDiagnosis {
  disease_name: string;
  icd10_code: string;
  probability: number;
  confidence_level: string;
  supporting_evidence: string[];
  against_evidence: string[];
  key_differentiator: string;
}

interface ExamSuggestion {
  exam_name: string;
  urgency: string;
  reason: string;
  target_disease: string;
}

const AIDiagnosisPanel: React.FC = () => {
  const [symptoms, setSymptoms] = useState<string[]>([]);
  const [symptomInput, setSymptomInput] = useState('');
  const [loading, setLoading] = useState(false);
  const [diagnoses, setDiagnoses] = useState<DifferentialDiagnosis[]>([]);
  const [exams, setExams] = useState<ExamSuggestion[]>([]);
  const [reasoning, setReasoning] = useState('');

  const addSymptom = () => {
    if (symptomInput.trim() && !symptoms.includes(symptomInput.trim())) {
      setSymptoms(prev => [...prev, symptomInput.trim()]);
      setSymptomInput('');
    }
  };

  const removeSymptom = (symptom: string) => {
    setSymptoms(prev => prev.filter(s => s !== symptom));
  };

  const analyzeSymptoms = async () => {
    if (symptoms.length === 0) return;
    setLoading(true);
    try {
      const response = await fetch('/api/v1/diagnosis/symptoms?patient_id=temp', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          symptoms,
          patient_age: 30,
          patient_gender: '未知',
        }),
      });
      const data = await response.json();
      setDiagnoses(data.differential_diagnoses || []);
      setExams(data.exam_suggestions || []);
      setReasoning(data.reasoning_summary || '');
    } catch (error) {
      // handle error
    } finally {
      setLoading(false);
    }
  };

  const getConfidenceColor = (level: string) => {
    switch (level) {
      case '高': return 'green';
      case '中': return 'orange';
      case '低': return 'red';
      default: return 'default';
    }
  };

  return (
    <div>
      <Card title={
        <Space>
          <RobotOutlined />
          <span>AI辅助诊断</span>
        </Space>
      }>
        <Alert
          message="AI辅助声明"
          description="本系统提供AI辅助诊断分析，所有结果仅供参考，不构成诊断依据。最终诊断须由执业医师确认。"
          type="warning"
          showIcon
          icon={<WarningOutlined />}
          style={{ marginBottom: 16 }}
        />

        <div style={{ marginBottom: 16 }}>
          <Space style={{ width: '100%' }}>
            <Input
              placeholder="输入症状，如：发热、咳嗽、胸痛"
              value={symptomInput}
              onChange={e => setSymptomInput(e.target.value)}
              onPressEnter={addSymptom}
              style={{ width: 300 }}
            />
            <Button type="primary" onClick={addSymptom}>添加</Button>
            <Button type="primary" onClick={analyzeSymptoms} loading={loading}
              disabled={symptoms.length === 0}>
              开始分析
            </Button>
          </Space>

          <div style={{ marginTop: 8 }}>
            {symptoms.map(s => (
              <Tag key={s} closable onClose={() => removeSymptom(s)} color="blue" style={{ marginBottom: 4 }}>
                {s}
              </Tag>
            ))}
          </div>
        </div>

        {loading && (
          <div style={{ textAlign: 'center', padding: 40 }}>
            <Spin size="large" tip="AI分析中..." />
          </div>
        )}

        {!loading && diagnoses.length > 0 && (
          <>
            <Divider>鉴别诊断</Divider>

            <List
              dataSource={diagnoses}
              renderItem={(item, index) => (
                <List.Item>
                  <Card size="small" style={{ width: '100%' }}
                    title={
                      <Space>
                        <span style={{ fontWeight: index === 0 ? 'bold' : 'normal', fontSize: index === 0 ? 16 : 14 }}>
                          {index + 1}. {item.disease_name}
                        </span>
                        <Tag color={getConfidenceColor(item.confidence_level)}>
                          置信度: {item.confidence_level}
                        </Tag>
                        {item.icd10_code && <Tag>{item.icd10_code}</Tag>}
                      </Space>
                    }
                  >
                    <Progress
                      percent={Math.round(item.probability * 100)}
                      strokeColor={item.probability >= 0.7 ? '#52c41a' : item.probability >= 0.4 ? '#faad14' : '#ff4d4f'}
                      format={percent => `${percent}%`}
                    />
                    <Descriptions column={1} size="small">
                      <Descriptions.Item label="支持依据">
                        {item.supporting_evidence.map(e => (
                          <Tag key={e} color="green">{e}</Tag>
                        ))}
                      </Descriptions.Item>
                      {item.against_evidence.length > 0 && (
                        <Descriptions.Item label="不支持">
                          {item.against_evidence.map(e => (
                            <Tag key={e} color="red">{e}</Tag>
                          ))}
                        </Descriptions.Item>
                      )}
                      <Descriptions.Item label="鉴别要点">
                        {item.key_differentiator}
                      </Descriptions.Item>
                    </Descriptions>
                  </Card>
                </List.Item>
              )}
            />

            {exams.length > 0 && (
              <>
                <Divider>推荐检查</Divider>
                <List
                  size="small"
                  dataSource={exams}
                  renderItem={item => (
                    <List.Item>
                      <Space>
                        <Tag color={item.urgency === '紧急' ? 'red' : 'blue'}>{item.urgency}</Tag>
                        <span>{item.exam_name}</span>
                        <span style={{ color: '#888' }}>- {item.reason}</span>
                      </Space>
                    </List.Item>
                  )}
                />
              </>
            )}

            {reasoning && (
              <>
                <Divider>推理过程</Divider>
                <Card size="small" style={{ background: '#f5f5f5' }}>
                  <pre style={{ whiteSpace: 'pre-wrap', margin: 0 }}>{reasoning}</pre>
                </Card>
              </>
            )}

            <Divider />
            <Alert
              message="重要提示：以上AI辅助分析结果仅供临床参考，不构成诊断依据。请结合患者实际情况，由执业医师做出最终诊断。"
              type="info"
              showIcon
              icon={<CheckCircleOutlined />}
            />
          </>
        )}
      </Card>
    </div>
  );
};

export default AIDiagnosisPanel;
```

### PrescriptionEditor - 处方编辑器

```tsx
// src/pages/PrescriptionEditor/index.tsx
import React, { useState } from 'react';
import {
  Card, Form, Input, Select, Button, Table, Space,
  Tag, Alert, message, Modal, Row, Col, Divider,
} from 'antd';
import { PlusOutlined, CheckCircleOutlined, WarningOutlined } from '@ant-design/icons';

interface PrescriptionItem {
  key: string;
  drug_name: string;
  specification: string;
  dosage: string;
  frequency: string;
  route: string;
  duration_days: number;
  sig: string;
}

interface MedicationAlert {
  alert_type: string;
  severity: string;
  drug_name: string;
  message: string;
  suggestion: string;
}

const PrescriptionEditor: React.FC<{ patientId: string }> = ({ patientId }) => {
  const [items, setItems] = useState<PrescriptionItem[]>([]);
  const [alerts, setAlerts] = useState<MedicationAlert[]>([]);
  const [checking, setChecking] = useState(false);
  const [itemForm] = Form.useForm();

  const addItem = () => {
    const values = itemForm.getFieldsValue();
    const sig = `${values.dosage} ${values.route} ${values.frequency}`;
    setItems(prev => [...prev, {
      key: `item-${Date.now()}`,
      ...values,
      sig,
    }]);
    itemForm.resetFields();
  };

  const removeItem = (key: string) => {
    setItems(prev => prev.filter(i => i.key !== key));
  };

  const checkPrescription = async () => {
    if (items.length === 0) {
      message.warning('请先添加药品');
      return;
    }
    setChecking(true);
    try {
      const response = await fetch('/api/v1/prescriptions/check', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          patient_id: patientId,
          drugs: items.map(i => ({
            drug_name: i.drug_name,
            dosage: i.dosage,
            frequency: i.frequency,
          })),
          patient_allergies: [],
          patient_conditions: [],
          patient_age: 30,
        }),
      });
      const data = await response.json();
      setAlerts(data.alerts || []);
      if (data.is_safe) {
        message.success('审查通过，无安全隐患');
      } else {
        message.warning('发现用药安全隐患，请查看详情');
      }
    } catch (error) {
      message.error('审查失败');
    } finally {
      setChecking(false);
    }
  };

  const submitPrescription = async () => {
    // 提交处方逻辑
    message.success('处方已提交');
  };

  const columns = [
    { title: '药品', dataIndex: 'drug_name', key: 'drug' },
    { title: '规格', dataIndex: 'specification', key: 'spec', width: 100 },
    { title: '剂量', dataIndex: 'dosage', key: 'dose', width: 100 },
    { title: '频次', dataIndex: 'frequency', key: 'freq', width: 80 },
    { title: '途径', dataIndex: 'route', key: 'route', width: 80 },
    { title: '天数', dataIndex: 'duration_days', key: 'days', width: 60 },
    { title: '用法', dataIndex: 'sig', key: 'sig' },
    {
      title: '操作', key: 'action', width: 80,
      render: (_: any, record: PrescriptionItem) => (
        <Button type="link" danger size="small" onClick={() => removeItem(record.key)}>
          删除
        </Button>
      ),
    },
  ];

  const getSeverityColor = (severity: string) => {
    switch (severity) {
      case '禁止': return 'red';
      case '严重': return 'volcano';
      case '警告': return 'orange';
      case '提示': return 'blue';
      default: return 'default';
    }
  };

  return (
    <div>
      <Card title="处方编辑" extra={
        <Space>
          <Button icon={<WarningOutlined />} onClick={checkPrescription} loading={checking}>
            合理用药审查
          </Button>
          <Button type="primary" icon={<CheckCircleOutlined />} onClick={submitPrescription}>
            提交处方
          </Button>
        </Space>
      }>
        <Alert
          message="AI辅助审查声明：审查结果仅供参考，最终由药师审核确认。"
          type="info"
          showIcon
          style={{ marginBottom: 16 }}
        />

        <Form form={itemForm} layout="inline" style={{ marginBottom: 16 }}>
          <Form.Item name="drug_name" rules={[{ required: true }]}>
            <Input placeholder="药品名称" style={{ width: 150 }} />
          </Form.Item>
          <Form.Item name="specification">
            <Input placeholder="规格" style={{ width: 100 }} />
          </Form.Item>
          <Form.Item name="dosage" rules={[{ required: true }]}>
            <Input placeholder="单次剂量" style={{ width: 100 }} />
          </Form.Item>
          <Form.Item name="frequency" rules={[{ required: true }]}>
            <Select style={{ width: 100 }} placeholder="频次">
              <Select.Option value="qd">qd</Select.Option>
              <Select.Option value="bid">bid</Select.Option>
              <Select.Option value="tid">tid</Select.Option>
              <Select.Option value="q8h">q8h</Select.Option>
              <Select.Option value="prn">prn</Select.Option>
            </Select>
          </Form.Item>
          <Form.Item name="route">
            <Select style={{ width: 100 }} defaultValue="口服">
              <Select.Option value="口服">口服</Select.Option>
              <Select.Option value="静脉滴注">静脉滴注</Select.Option>
              <Select.Option value="肌肉注射">肌肉注射</Select.Option>
              <Select.Option value="外用">外用</Select.Option>
            </Select>
          </Form.Item>
          <Form.Item name="duration_days">
            <Input type="number" placeholder="天数" style={{ width: 70 }} />
          </Form.Item>
          <Button type="dashed" icon={<PlusOutlined />} onClick={addItem}>
            添加
          </Button>
        </Form>

        <Table
          columns={columns}
          dataSource={items}
          pagination={false}
          size="small"
        />

        {alerts.length > 0 && (
          <>
            <Divider>审查结果</Divider>
            {alerts.map((alert, index) => (
              <Alert
                key={index}
                message={
                  <Space>
                    <Tag color={getSeverityColor(alert.severity)}>{alert.severity}</Tag>
                    <Tag>{alert.alert_type}</Tag>
                    <span>{alert.drug_name}</span>
                  </Space>
                }
                description={
                  <div>
                    <p>{alert.message}</p>
                    {alert.suggestion && <p style={{ color: '#1890ff' }}>建议：{alert.suggestion}</p>}
                  </div>
                }
                type={alert.severity === '禁止' || alert.severity === '严重' ? 'error' : 'warning'}
                showIcon
                style={{ marginBottom: 8 }}
              />
            ))}
          </>
        )}
      </Card>
    </div>
  );
};

export default PrescriptionEditor;
```

### DICOM Viewer简化版

```tsx
// src/pages/DicomViewer/index.tsx
import React, { useState, useRef, useEffect } from 'react';
import { Card, Slider, Select, Button, Space, Row, Col, Tag } from 'antd';

interface WindowPreset {
  label: string;
  center: number;
  width: number;
}

const WINDOW_PRESETS: WindowPreset[] = [
  { label: '肺窗', center: -600, width: 1500 },
  { label: '纵隔窗', center: 40, width: 350 },
  { label: '骨窗', center: 480, width: 2500 },
  { label: '脑窗', center: 40, width: 80 },
  { label: '腹部窗', center: 40, width: 350 },
];

const DicomViewer: React.FC = () => {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const [currentPreset, setCurrentPreset] = useState<WindowPreset>(WINDOW_PRESETS[0]);
  const [windowCenter, setWindowCenter] = useState(-600);
  const [windowWidth, setWindowWidth] = useState(1500);
  const [currentSlice, setCurrentSlice] = useState(0);
  const [totalSlices, setTotalSlices] = useState(100);
  const [aiFindings, setAiFindings] = useState<any[]>([]);

  const applyWindowSettings = () => {
    // 实际中此处将DICOM像素数据应用窗宽窗位后渲染到Canvas
    // 简化示例仅展示UI逻辑
  };

  useEffect(() => {
    applyWindowSettings();
  }, [windowCenter, windowWidth, currentSlice]);

  return (
    <Card title="DICOM影像查看器">
      <Row gutter={16}>
        <Col span={18}>
          <canvas
            ref={canvasRef}
            style={{
              width: '100%',
              height: 500,
              background: '#000',
              border: '1px solid #333',
            }}
          />
          <div style={{ marginTop: 8 }}>
            <span>切片: </span>
            <Slider
              min={0}
              max={totalSlices}
              value={currentSlice}
              onChange={setCurrentSlice}
              style={{ width: '50%', display: 'inline-block', verticalAlign: 'middle' }}
            />
            <span style={{ marginLeft: 8 }}>{currentSlice} / {totalSlices}</span>
          </div>
        </Col>
        <Col span={6}>
          <Space direction="vertical" style={{ width: '100%' }}>
            <div>
              <label>窗位预设:</label>
              <Select
                style={{ width: '100%' }}
                value={currentPreset.label}
                onChange={(label) => {
                  const preset = WINDOW_PRESETS.find(p => p.label === label);
                  if (preset) {
                    setCurrentPreset(preset);
                    setWindowCenter(preset.center);
                    setWindowWidth(preset.width);
                  }
                }}
              >
                {WINDOW_PRESETS.map(p => (
                  <Select.Option key={p.label} value={p.label}>{p.label}</Select.Option>
                ))}
              </Select>
            </div>
            <div>
              <label>窗位(WL): {windowCenter}</label>
              <Slider min={-2000} max={2000} value={windowCenter} onChange={setWindowCenter} />
            </div>
            <div>
              <label>窗宽(WW): {windowWidth}</label>
              <Slider min={1} max={4000} value={windowWidth} onChange={setWindowWidth} />
            </div>
            <div style={{ marginTop: 16 }}>
              <strong>AI检测结果</strong>
              {aiFindings.length === 0 ? (
                <p style={{ color: '#888' }}>暂无AI检测结果</p>
              ) : (
                aiFindings.map((f, i) => (
                  <div key={i} style={{ padding: 8, background: '#f5f5f5', marginBottom: 4 }}>
                    <Tag color="blue">{f.type}</Tag>
                    <span>{f.location} - {f.size}</span>
                    <br />
                    <small>置信度: {(f.confidence * 100).toFixed(0)}%</small>
                  </div>
                ))
              )}
            </div>
          </Space>
        </Col>
      </Row>
    </Card>
  );
};

export default DicomViewer;
```

### 安全加固配置

#### TLS与数据加密

```python
# TLS配置
SSL_CONFIG = {
    "min_version": "TLSv1.2",
    "cipher_suites": [
        "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384",
        "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256",
    ],
}

# 数据库连接SSL
DATABASE_URL = "postgresql+asyncpg://user:pass@db:5432/emr?sslmode=require"

# Redis SSL
REDIS_URL = "rediss://:pass@redis:6380/0?ssl_cert_reqs=required"
```

#### Docker网络隔离

```yaml
# 安全网络配置
networks:
  frontend:
    # 前端网络：仅API服务可访问
  backend:
    # 后端网络：API/Worker/服务间通信
    internal: true    # 不暴露到主机
  data:
    # 数据层：仅后端服务可访问
    internal: true

services:
  api:
    networks: [frontend, backend]
  
  postgres:
    networks: [data]    # 仅数据网络，不暴露端口
    # 不配置ports，禁止外部访问
  
  neo4j:
    networks: [data]
    # 不配置ports，禁止外部访问
  
  redis:
    networks: [data]
    # 不配置ports，禁止外部访问
```

#### 备份策略

```yaml
# 定时备份
services:
  postgres-backup:
    image: postgres:15
    environment:
      PGPASSWORD: ${DB_PASSWORD}
    volumes:
      - ./backups:/backups
    networks: [data]
    entrypoint: >
      bash -c "
        while true; do
          pg_dump -h postgres -U postgres emr | gzip > /backups/emr_$$(date +%Y%m%d_%H%M%S).sql.gz
          find /backups -name '*.sql.gz' -mtime +30 -delete
          sleep 3600
        done
      "
```

### 容灾与业务连续性

#### RPO/RTO目标

| 系统 | RPO | RTO | 说明 |
|------|-----|-----|------|
| 电子病历 | < 5分钟 | < 15分钟 | 患者数据不可丢失 |
| AI诊断服务 | < 30分钟 | < 5分钟 | 可短暂降级，不可中断 |
| 知识图谱 | < 1小时 | < 30分钟 | 相对静态数据 |
| 审计日志 | < 1分钟 | < 10分钟 | 零丢失要求 |

#### 多AZ部署架构

```yaml
# 生产环境K8s部署建议
apiVersion: apps/v1
kind: Deployment
metadata:
  name: diagnosis-api
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: diagnosis-api
              topologyKey: topology.kubernetes.io/zone
      containers:
      - name: api
        image: registry.example.com/diagnosis-api:latest
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "2000m"
            memory: "2Gi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
```

## Docker Compose配置

```yaml
# docker-compose.yml
version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/smart_clinic
      - REDIS_URL=redis://redis:6379/0
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=smartclinic2026
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_BASE_URL=${LLM_BASE_URL:-https://api.openai.com/v1}
      - LLM_MODEL=${LLM_MODEL:-gpt-4}
      - SECRET_KEY=${SECRET_KEY:-change-this-in-production}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      neo4j:
        condition: service_healthy
    volumes:
      - ./app:/app
    restart: unless-stopped

  web:
    build:
      context: ./smart-clinic-web
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: smart_clinic
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  neo4j:
    image: neo4j:5-community
    environment:
      NEO4J_AUTH: neo4j/smartclinic2026
      NEO4J_PLUGINS: '["apoc"]'
      NEO4J_server_memory_heap_initial__size: 512m
      NEO4J_server_memory_heap_max__size: 1G
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:7474 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pg_data:
  redis_data:
  neo4j_data:
  neo4j_logs:
```

### Dockerfile (FastAPI应用)

```dockerfile
FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev && \
    rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
```

### 环境变量配置

```bash
# .env

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/smart_clinic

# Redis
REDIS_URL=redis://redis:6379/0

# Neo4j
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=smartclinic2026

# LLM
LLM_API_KEY=sk-your-api-key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4

# Security
SECRET_KEY=your-secret-key-must-be-changed-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# App
APP_NAME=智慧诊疗系统
DEBUG=false
LOG_LEVEL=INFO
```

## 医学知识图谱初始化脚本

```python
# scripts/init_knowledge_graph.py
"""初始化医学知识图谱数据到Neo4j"""
import asyncio
from app.services.knowledge_graph_service import KnowledgeGraphService

async def main():
    kg = KnowledgeGraphService(
        uri="bolt://localhost:7687",
        user="neo4j",
        password="smartclinic2026",
    )

    print("Initializing schema...")
    kg.init_schema()

    print("Seeding knowledge graph data...")
    from app.services.knowledge_graph_service_extended import seed_knowledge_graph
    seed_knowledge_graph(kg)

    # 验证数据
    with kg.driver.session() as session:
        result = session.run(
            "MATCH (n) RETURN labels(n)[0] AS type, count(n) AS count"
        )
        for record in result:
            print(f"  {record['type']}: {record['count']} nodes")

    kg.close()
    print("Knowledge graph initialized successfully.")

if __name__ == "__main__":
    asyncio.run(main())
```

## 启动与部署指南

### 开发环境启动

```bash
# 1. 启动依赖服务
docker-compose up -d db redis neo4j

# 2. 等待服务就绪后初始化数据库
alembic upgrade head

# 3. 初始化知识图谱
python scripts/init_knowledge_graph.py

# 4. 初始化基础数据(科室/医生)
python scripts/seed_data.py

# 5. 启动API服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 6. 启动前端
cd smart-clinic-web && npm run dev
```

### 生产环境部署

```bash
# 1. 构建并启动所有服务
docker-compose up -d --build

# 2. 初始化数据库
docker-compose exec api alembic upgrade head

# 3. 初始化知识图谱
docker-compose exec api python scripts/init_knowledge_graph.py

# 4. 初始化基础数据
docker-compose exec api python scripts/seed_data.py

# 5. 验证服务
curl http://localhost:8000/health
curl http://localhost:3000
```

## API测试脚本

```python
# tests/test_api.py
"""API集成测试"""
import pytest
from httpx import AsyncClient, ASGITransport

BASE_URL = "http://test"

@pytest.fixture
async def client():
    from app.main import app
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url=BASE_URL) as ac:
        yield ac

@pytest.mark.asyncio
async def test_create_patient(client: AsyncClient):
    response = await client.post("/api/v1/patients", json={
        "name": "测试患者",
        "gender": "男",
        "birth_date": "1990-01-01",
        "id_card": "110101199001010011",
        "phone": "13800138000",
    })
    assert response.status_code == 201
    data = response.json()
    assert data["name"] == "测试患者"
    assert "***" in data["id_card"]  # 验证脱敏

@pytest.mark.asyncio
async def test_create_record(client: AsyncClient):
    # 先创建患者
    patient_resp = await client.post("/api/v1/patients", json={
        "name": "测试患者2",
        "gender": "女",
        "birth_date": "1985-06-15",
        "id_card": "310101198506150022",
    })
    patient_id = patient_resp.json()["id"]

    # 创建病历
    response = await client.post(f"/api/v1/patients/{patient_id}/records", json={
        "encounter_type": "门诊",
        "chief_complaint": "反复上腹痛3天",
        "present_illness": "3天前出现上腹痛，呈阵发性",
    })
    assert response.status_code == 201
    assert response.json()["chief_complaint"] == "反复上腹痛3天"

@pytest.mark.asyncio
async def test_diagnosis_analysis(client: AsyncClient):
    response = await client.post(
        "/api/v1/diagnosis/symptoms?patient_id=test-patient",
        json={
            "symptoms": ["发热", "咳嗽", "咳痰"],
        },
    )
    assert response.status_code == 200
    data = response.json()
    assert len(data["differential_diagnoses"]) > 0
    assert data["ai_disclaimer"] is not None

@pytest.mark.asyncio
async def test_prescription_check(client: AsyncClient):
    response = await client.post("/api/v1/prescriptions/check", json={
        "patient_id": "test",
        "drugs": [
            {"drug_name": "华法林"},
            {"drug_name": "阿司匹林"},
        ],
        "patient_allergies": [],
        "patient_conditions": [],
        "patient_age": 50,
    })
    assert response.status_code == 200
    data = response.json()
    assert data["is_safe"] is False  # 华法林+阿司匹林有严重交互
    assert any(a["alert_type"] == "配伍禁忌" for a in data["alerts"])
```
