---
title: 药品与处方体系
icon: fa6-solid:pills
order: 4
category:
  - 行业解决方案/医疗AI
---

# 药品与处方体系

## 药品分类

### 按管理方式分类

| 分类 | 定义 | 购买方式 | 示例 |
|------|------|----------|------|
| 处方药(Rx) | 需凭医师处方购买 | 凭处方在药店/医院购买 | 抗生素、降压药、胰岛素 |
| 甲类OTC | 必须在药店购买 | 药店执业药师指导下 | 布洛芬、对乙酰氨基酚 |
| 乙类OTC | 可在普通商业渠道购买 | 超市/便利店可售 | 维生素C、创可贴 |

### 按药品来源分类

| 分类 | 英文 | 说明 | 示例 |
|------|------|------|------|
| 化学药品 | Chemical Drugs | 化学合成或提取 | 阿司匹林、二甲双胍 |
| 中药 | Traditional Chinese Medicine | 中药材/饮片/中成药 | 六味地黄丸、板蓝根 |
| 生物制品 | Biologics | 生物技术制备 | 胰岛素、单抗、疫苗 |
| 体外诊断试剂 | IVD Reagents | 体外诊断用 | 早早孕试纸、新冠抗原 |

### 按治疗领域分类

```python
from enum import Enum
from pydantic import BaseModel, Field
from typing import Optional

class DrugCategory(str, Enum):
    ANTI_INFECTIVE = "抗感染药物"
    CARDIOVASCULAR = "心血管系统药物"
    DIGESTIVE = "消化系统药物"
    RESPIRATORY = "呼吸系统药物"
    NERVOUS = "神经系统药物"
    ENDOCRINE = "内分泌系统药物"
    ONCOLOGY = "抗肿瘤药物"
    MUSCULOSKELETAL = "肌肉骨骼系统药物"
    DERMATOLOGICAL = "皮肤科药物"
    IMMUNE = "免疫调节药物"

class DrugInfo(BaseModel):
    """药品信息模型"""
    drug_name: str = Field(..., description="通用名")
    brand_name: Optional[str] = Field(None, description="商品名")
    category: DrugCategory
    specification: str = Field(..., description="规格，如：0.25g/片")
    dosage_form: str = Field(..., description="剂型：片剂/胶囊/注射液等")
    route: str = Field(..., description="给药途径：口服/静脉/肌注等")
    is_prescription: bool = Field(True, description="是否处方药")
    is_national_essential: bool = Field(False, description="是否国家基本药物")
    storage_condition: str = Field(..., description="储存条件")
    atc_code: Optional[str] = Field(None, description="ATC分类编码")

# 常用药品信息示例
COMMON_DRUGS: list[DrugInfo] = [
    DrugInfo(
        drug_name="阿莫西林", brand_name="弗莱莫星",
        category=DrugCategory.ANTI_INFECTIVE,
        specification="0.5g/粒", dosage_form="胶囊",
        route="口服", is_prescription=True,
        is_national_essential=True,
        storage_condition="遮光，密封保存",
        atc_code="J01CA04",
    ),
    DrugInfo(
        drug_name="二甲双胍", brand_name="格华止",
        category=DrugCategory.ENDOCRINE,
        specification="0.5g/片", dosage_form="片剂",
        route="口服", is_prescription=True,
        is_national_essential=True,
        storage_condition="密封保存",
        atc_code="A10BA02",
    ),
    DrugInfo(
        drug_name="阿托伐他汀", brand_name="立普妥",
        category=DrugCategory.CARDIOVASCULAR,
        specification="20mg/片", dosage_form="片剂",
        route="口服", is_prescription=True,
        is_national_essential=True,
        storage_condition="密封保存",
        atc_code="C10AA05",
    ),
    DrugInfo(
        drug_name="奥美拉唑", brand_name="洛赛克",
        category=DrugCategory.DIGESTIVE,
        specification="20mg/粒", dosage_form="胶囊",
        route="口服", is_prescription=True,
        is_national_essential=True,
        storage_condition="遮光，密封保存",
        atc_code="A02BC01",
    ),
]
```

## 药品管理体系

### 药品全生命周期管理

```mermaid
graph LR
    A[药品采购<br/>招标/集采] --> B[入库验收<br/>批号/效期检查]
    B --> C[库存管理<br/>上下限/先进先出]
    C --> D[药房调拨<br/>病区药房/门诊药房]
    D --> E[处方审核<br/>药师审方]
    E --> F[发药<br/>核对/交代用法]
    F --> G[患者用药<br/>依从性跟踪]
    G --> H[不良反应监测<br/>ADR上报]

    style A fill:#3498db,color:#fff
    style E fill:#e74c3c,color:#fff
    style H fill:#f39c12,color:#fff
```

### 药品库存管理

```python
from pydantic import BaseModel, Field
from datetime import date, timedelta
from typing import Optional
from enum import Enum

class DrugStorageStatus(str, Enum):
    normal = "正常"
    low_stock = "低库存"
    out_of_stock = "缺货"
    near_expiry = "近效期"
    expired = "过期"

class DrugInventory(BaseModel):
    """药品库存记录"""
    drug_code: str
    drug_name: str
    batch_no: str = Field(..., description="批号")
    production_date: date
    expiry_date: date
    quantity: int = Field(..., description="库存数量")
    unit: str = Field(..., description="最小包装单位")
    location: str = Field(..., description="货位号")
    purchase_price: float = Field(..., description="采购价")
    retail_price: float = Field(..., description="零售价")
    supplier: str = Field(..., description="供应商")

    @property
    def days_to_expiry(self) -> int:
        return (self.expiry_date - date.today()).days

    @property
    def storage_status(self) -> DrugStorageStatus:
        if self.days_to_expiry <= 0:
            return DrugStorageStatus.expired
        elif self.days_to_expiry <= 90:
            return DrugStorageStatus.near_expiry
        elif self.quantity == 0:
            return DrugStorageStatus.out_of_stock
        elif self.quantity <= 10:  # 简化的低库存阈值
            return DrugStorageStatus.low_stock
        return DrugStorageStatus.normal

# 库存预警规则
INVENTORY_ALERT_RULES = {
    "low_stock_threshold": {
        "口服药": 50,
        "注射剂": 30,
        "外用药": 20,
        "抢救药": 10,
    },
    "near_expiry_days": 90,   # 近效期预警天数
    "expiry_critical_days": 30,  # 效期紧急天数
}
```

## 处方制度

### 处方权限

| 夻印颜色 | 处方类型 | 开具权限 | 限量 |
|----------|----------|----------|------|
| 白色 | 普通处方 | 执业医师 | 一般不超过7日用量 |
| 黄色 | 急诊处方 | 执业医师 | 一般不超过3日用量 |
| 绿色 | 儿科处方 | 执业医师 | 根据病情 |
| 红色 | 麻醉药品处方 | 麻醉药品处方权医师 | 一次用量(注射)/3日(口服) |
| 红色 | 精神药品处方(一类) | 精神药品处方权医师 | 3日/7日用量 |

### 处方结构

```
Rp.
1. 阿莫西林胶囊  0.5g x 24粒
   Sig: 0.5g po tid x 4天
2. 布洛芬片  0.2g x 10片
   Sig: 0.2g po prn 发热>38.5°C
```

### 处方数据模型

```python
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime
from enum import Enum

class PrescriptionType(str, Enum):
    ordinary = "普通处方"
    emergency = "急诊处方"
    pediatric = "儿科处方"
    anesthetic = "麻醉药品处方"
    psychotropic = "精神药品处方"

class DosageForm(str, Enum):
    tablet = "片剂"
    capsule = "胶囊"
    injection = "注射液"
    syrup = "糖浆"
    ointment = "软膏"
    drop = "滴剂"
    aerosol = "气雾剂"

class RouteOfAdministration(str, Enum):
    PO = "口服"       # Per Os
    IV = "静脉注射"   # Intravenous
    IM = "肌肉注射"   # Intramuscular
    SC = "皮下注射"   # Subcutaneous
    TOP = "外用"      # Topical
    INH = "吸入"      # Inhalation
    PR = "直肠给药"   # Per Rectum
    SL = "舌下含服"   # Sublingual

class Frequency(str, Enum):
    QD = "每天一次"
    BID = "每天两次"
    TID = "每天三次"
    QID = "每天四次"
    Q8H = "每8小时一次"
    Q12H = "每12小时一次"
    QN = "每晚一次"
    PRN = "必要时"
    STAT = "立即"
    QOD = "隔天一次"

class PrescriptionItem(BaseModel):
    """处方项目"""
    drug_name: str = Field(..., description="药品通用名")
    specification: str = Field(..., description="规格")
    quantity: str = Field(..., description="数量，如：24粒")
    dosage_form: DosageForm
    route: RouteOfAdministration
    frequency: Frequency
    dosage_per_time: str = Field(..., description="单次剂量，如：0.5g")
    duration_days: int = Field(..., description="用药天数")
    sig: str = Field(..., description="用法简写")
    remark: Optional[str] = Field(None, description="备注")

class Prescription(BaseModel):
    """处方"""
    prescription_id: str
    prescription_type: PrescriptionType
    patient_id: str
    patient_name: str
    patient_age: int
    patient_gender: str
    patient_weight: Optional[float] = Field(None, description="体重(kg)，儿童必填")
    doctor_id: str
    doctor_name: str
    department: str
    diagnosis: str = Field(..., description="临床诊断")
    icd10_code: Optional[str] = Field(None, description="ICD-10编码")
    items: list[PrescriptionItem]
    is_reviewed: bool = False
    reviewer: Optional[str] = None
    created_at: datetime = Field(default_factory=datetime.now)

    @property
    def item_count(self) -> int:
        """处方药品数(一般不超过5种)"""
        return len(self.items)

    @property
    def is_valid_item_count(self) -> bool:
        """处方药品数是否合规"""
        return self.item_count <= 5

# 处方示例
sample_prescription = Prescription(
    prescription_id="RX20260607001",
    prescription_type=PrescriptionType.ordinary,
    patient_id="P20240001",
    patient_name="张某某",
    patient_age=45,
    patient_gender="男",
    doctor_id="D001",
    doctor_name="李某某",
    department="呼吸内科",
    diagnosis="社区获得性肺炎",
    icd10_code="J18.9",
    items=[
        PrescriptionItem(
            drug_name="阿莫西林胶囊",
            specification="0.5g",
            quantity="24粒",
            dosage_form=DosageForm.capsule,
            route=RouteOfAdministration.PO,
            frequency=Frequency.TID,
            dosage_per_time="0.5g",
            duration_days=4,
            sig="0.5g po tid",
        ),
        PrescriptionItem(
            drug_name="氨溴索片",
            specification="30mg",
            quantity="20片",
            dosage_form=DosageForm.tablet,
            route=RouteOfAdministration.PO,
            frequency=Frequency.TID,
            dosage_per_time="30mg",
            duration_days=7,
            sig="30mg po tid",
        ),
    ],
)
```

## 合理用药

### 配伍禁忌

配伍禁忌是指两种或以上药物合用时产生的不良相互作用。

```python
from pydantic import BaseModel, Field
from enum import Enum

class InteractionSeverity(str, Enum):
    contraindicated = "禁忌"          # 严禁合用
    major = "严重"                     # 应避免合用
    moderate = "中等"                   # 需谨慎
    minor = "轻微"                     # 临床意义不大

class DrugInteraction(BaseModel):
    """药物相互作用"""
    drug_a: str
    drug_b: str
    severity: InteractionSeverity
    mechanism: str
    clinical_effect: str
    management: str

# 常见药物配伍禁忌
DRUG_INTERACTIONS: list[DrugInteraction] = [
    DrugInteraction(
        drug_a="华法林", drug_b="阿司匹林",
        severity=InteractionSeverity.major,
        mechanism="华法林与阿司匹林均有抗凝作用，合用增加出血风险",
        clinical_effect="出血风险显著增加",
        management="避免合用，必须合用时密切监测INR和出血征象",
    ),
    DrugInteraction(
        drug_a="ACEI(如依那普利)", drug_b="螺内酯",
        severity=InteractionSeverity.moderate,
        mechanism="两者均保钾，合用致高钾血症",
        clinical_effect="高钾血症风险",
        management="合用时监测血钾，避免补钾",
    ),
    DrugInteraction(
        drug_a="甲氨蝶呤", drug_b="布洛芬",
        severity=InteractionSeverity.major,
        mechanism="布洛芬抑制肾脏前列腺素，减少甲氨蝶呤排泄",
        clinical_effect="甲氨蝶呤血药浓度升高，毒性增加",
        management="禁止合用，选用对乙酰氨基酚替代",
    ),
    DrugInteraction(
        drug_a="头孢曲松", drug_b="含钙输液",
        severity=InteractionSeverity.contraindicated,
        mechanism="头孢曲松与钙离子结合形成沉淀",
        clinical_effect="可致死性沉淀(新生儿尤其危险)",
        management="禁止在同一输液管路中混合使用",
    ),
    DrugInteraction(
        drug_a="地高辛", drug_b="胺碘酮",
        severity=InteractionSeverity.moderate,
        mechanism="胺碘酮抑制P-gp，减少地高辛清除",
        clinical_effect="地高辛血药浓度升高，洋地黄中毒风险",
        management="合用时地高辛减量50%，监测血药浓度",
    ),
]

def check_drug_interaction(drug_list: list[str]) -> list[DrugInteraction]:
    """检查药品列表中的配伍禁忌"""
    found_interactions = []
    for interaction in DRUG_INTERACTIONS:
        if interaction.drug_a in drug_list and interaction.drug_b in drug_list:
            found_interactions.append(interaction)
    return found_interactions
```

### 用药审查规则

```python
from pydantic import BaseModel, Field
from typing import Optional
from enum import Enum

class AlertLevel(str, Enum):
    info = "提示"
    warning = "警告"
    error = "禁止"

class MedicationAlert(BaseModel):
    """用药审查提示"""
    alert_type: str
    alert_level: AlertLevel
    drug_name: str
    message: str
    suggestion: Optional[str] = None

def review_prescription(
    prescription: Prescription,
    patient_allergies: list[str],
    patient_conditions: list[str],
) -> list[MedicationAlert]:
    """处方审查：过敏+剂量+配伍+适应症"""
    alerts = []

    # 1. 过敏检查
    ALLERGY_MAPPING = {
        "青霉素": ["阿莫西林", "氨苄西林", "哌拉西林", "青霉素"],
        "磺胺": ["磺胺甲噁唑", "甲氧苄啶"],
        "碘": ["碘海醇", "碘佛醇"],
    }
    for allergen in patient_allergies:
        if allergen in ALLERGY_MAPPING:
            for item in prescription.items:
                if item.drug_name in ALLERGY_MAPPING[allergen]:
                    alerts.append(MedicationAlert(
                        alert_type="过敏禁忌",
                        alert_level=AlertLevel.error,
                        drug_name=item.drug_name,
                        message=f"患者对{allergen}过敏，禁用{item.drug_name}",
                        suggestion="更换为替代药物",
                    ))

    # 2. 配伍禁忌检查
    drug_names = [item.drug_name for item in prescription.items]
    interactions = check_drug_interaction(drug_names)
    for interaction in interactions:
        level = AlertLevel.error if interaction.severity == InteractionSeverity.contraindicated else AlertLevel.warning
        alerts.append(MedicationAlert(
            alert_type="配伍禁忌",
            alert_level=level,
            drug_name=f"{interaction.drug_a}+{interaction.drug_b}",
            message=interaction.clinical_effect,
            suggestion=interaction.management,
        ))

    # 3. 儿童用药禁忌
    if prescription.patient_age < 18:
        PEDIATRIC_CONTRAINDICATIONS = {
            "喹诺酮类": ["左氧氟沙星", "环丙沙星", "莫西沙星"],
            "四环素类": ["多西环素", "米诺环素"],
            "氨基糖苷类": ["庆大霉素", "阿米卡星"],
        }
        for category, drugs in PEDIATRIC_CONTRAINDICATIONS.items():
            for item in prescription.items:
                if item.drug_name in drugs:
                    alerts.append(MedicationAlert(
                        alert_type="儿童用药禁忌",
                        alert_level=AlertLevel.error,
                        drug_name=item.drug_name,
                        message=f"{item.drug_name}({category})禁用于18岁以下患者",
                        suggestion="更换为儿童适用药物",
                    ))

    # 4. 处方药品数审查
    if not prescription.is_valid_item_count:
        alerts.append(MedicationAlert(
            alert_type="处方规范性",
            alert_level=AlertLevel.warning,
            drug_name="",
            message=f"处方含{prescription.item_count}种药品，超过5种上限",
            suggestion="减少用药种类，必要时分次开具",
        ))

    # 5. 孕妇用药禁忌
    if "妊娠" in patient_conditions:
        PREGNANCY_CONTRAINDICATIONS = [
            "异维A酸", "华法林", "甲氨蝶呤", "利巴韦林",
            "ACEI", "ARB", "喹诺酮类",
        ]
        for item in prescription.items:
            for contrain in PREGNANCY_CONTRAINDICATIONS:
                if contrain in item.drug_name:
                    alerts.append(MedicationAlert(
                        alert_type="孕妇用药禁忌",
                        alert_level=AlertLevel.error,
                        drug_name=item.drug_name,
                        message=f"{item.drug_name}禁用于妊娠期患者",
                        suggestion="更换为妊娠期安全药物",
                    ))

    return alerts
```

### 药品不良反应(ADR)监测

```python
from pydantic import BaseModel, Field
from datetime import datetime
from typing import Optional
from enum import Enum

class ADRSeverity(str, Enum):
    mild = "一般"
    severe = "严重"
    life_threatening = "危及生命"

class ADRReport(BaseModel):
    """药品不良反应报告"""
    report_id: str
    patient_id: str
    patient_age: int
    patient_gender: str
    drug_name: str
    dosage: str
    route: str
    start_date: datetime
    adverse_reaction: str
    severity: ADRSeverity
    outcome: str  # 好转/未好转/死亡/不详
    causality: str  # 肯定/很可能/可能/可能无关/无法评价
    reporter: str
    report_date: datetime = Field(default_factory=datetime.now)
    description: str
```

## 国家基本药物制度

国家基本药物目录是保障基本用药需求的药品清单，优先配备使用。

### 基本药物制度要点

| 项目 | 内容 |
|------|------|
| 目录 | 《国家基本药物目录》(现行2018版) |
| 品种数 | 685种(西药417+中成药268) |
| 基层配备 | 基层医疗机构配备使用比例>=90% |
| 零差率 | 基层医疗机构基本药物零差率销售 |
| 医保衔接 | 基本药物全部纳入医保报销 |

## 药品术语表

| 术语 | 英文 | 含义 |
|------|------|------|
| 处方药 | Prescription Drug | 需凭医师处方购买使用的药品 |
| OTC | Over-The-Counter | 非处方药，可自行购买 |
| 通用名 | Generic Name | 药品的法定名称 |
| 商品名 | Brand Name | 药品的商标名 |
| 剂型 | Dosage Form | 药物的制剂形式(片/胶囊/注射等) |
| 规格 | Specification | 药品的最小剂量单位含量 |
| 用法 | Sig/Direction | 用药方法及用量 |
| 适应症 | Indication | 药物适用的疾病或症状 |
| 禁忌症 | Contraindication | 禁止使用该药物的情况 |
| 不良反应 | Adverse Reaction | 合格药品在正常用法下出现的与用药目的无关的有害反应 |
| 配伍禁忌 | Incompatibility | 两种以上药物合用产生的有害相互作用 |
| 药代动力学 | Pharmacokinetics | 药物在体内的吸收、分布、代谢、排泄过程 |
| 药效学 | Pharmacodynamics | 药物对机体的作用及作用机制 |
| 生物利用度 | Bioavailability | 药物被吸收进入血液循环的程度和速度 |
| 半衰期 | Half-Life | 血药浓度下降一半所需的时间 |
| 治疗窗 | Therapeutic Window | 最小有效浓度与最小中毒浓度之间的范围 |
| ATC编码 | Anatomical Therapeutic Chemical | WHO药物分类编码 |
| 集采 | Centralized Procurement | 国家组织药品集中采购 |
| 零差率 | Zero Markup | 药品按进价销售，不加价 |
