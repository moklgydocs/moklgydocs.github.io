---
title: 数据模型与存储
icon: fa6-solid:database
order: 11
category:
  - 行业解决方案/电商AI
---

# 数据模型与存储

## 数据库设计

系统采用PostgreSQL作为主数据库，Redis作为缓存层，实现数据的持久化存储和高效访问。

### 技术选型

| 组件 | 版本 | 用途 |
|------|------|------|
| PostgreSQL | 16 | 关系型主数据库 |
| SQLAlchemy | 2.0 | Python ORM框架 |
| Alembic | 最新 | 数据库迁移管理 |
| Redis | 7 | 缓存/实时数据 |

## SQLAlchemy模型代码

### 数据库连接配置

```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
from sqlalchemy.orm import DeclarativeBase
from app.config import settings

engine = create_async_engine(
    settings.DATABASE_URL,
    echo=False,
    pool_size=20,
    max_overflow=10,
    pool_pre_ping=True,
)

async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)


class Base(DeclarativeBase):
    pass


async def get_db() -> AsyncSession:
    async with async_session() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
        finally:
            await session.close()
```

### 商品模型 (Product/SPU)

```python
from sqlalchemy import Column, Integer, String, Text, Numeric, DateTime, Boolean, JSON, Enum as SAEnum
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from app.database import Base
import enum


class ProductStatus(str, enum.Enum):
    DRAFT = "draft"
    ACTIVE = "active"
    INACTIVE = "inactive"
    DISCONTINUED = "discontinued"


class Product(Base):
    """商品/SPU模型"""
    __tablename__ = "products"

    id = Column(Integer, primary_key=True, autoincrement=True)
    spu_code = Column(String(64), unique=True, nullable=False, index=True, comment="SPU编码")
    name = Column(String(256), nullable=False, comment="商品名称")
    brand = Column(String(128), comment="品牌")
    category_id = Column(Integer, nullable=False, index=True, comment="品类ID")
    description = Column(Text, comment="商品描述")
    key_attrs = Column(JSON, default=dict, comment="关键属性")
    desc_attrs = Column(JSON, default=dict, comment="描述属性")
    status = Column(SAEnum(ProductStatus), default=ProductStatus.DRAFT, comment="状态")
    created_at = Column(DateTime(timezone=True), server_default=func.now(), comment="创建时间")
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), comment="更新时间")

    # 关联
    skus = relationship("SKU", back_populates="product", lazy="selectin")

    def __repr__(self):
        return f"<Product(spu_code={self.spu_code}, name={self.name})>"


class SKU(Base):
    """SKU模型 - 最小库存单元"""
    __tablename__ = "skus"

    id = Column(Integer, primary_key=True, autoincrement=True)
    sku_code = Column(String(64), unique=True, nullable=False, index=True, comment="SKU编码")
    product_id = Column(Integer, nullable=False, index=True, comment="SPU ID")
    spec_values = Column(JSON, default=dict, comment="规格属性值")
    cost_price = Column(Numeric(12, 2), nullable=False, comment="成本价")
    sale_price = Column(Numeric(12, 2), nullable=False, comment="销售价")
    weight = Column(Numeric(8, 2), default=0, comment="重量(kg)")
    volume = Column(Numeric(8, 4), default=0, comment="体积(m3)")
    barcode = Column(String(64), comment="条形码")
    status = Column(SAEnum(ProductStatus), default=ProductStatus.ACTIVE, comment="状态")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    # 关联
    product = relationship("Product", back_populates="skus")
    inventories = relationship("Inventory", back_populates="sku", lazy="selectin")

    def __repr__(self):
        return f"<SKU(sku_code={self.sku_code})>"
```

### 仓库与库存模型

```python
class WarehouseType(str, enum.Enum):
    CENTRAL = "central"         # 总仓
    REGIONAL = "regional"       # 区域仓
    FRONT = "front"             # 前置仓
    STORE = "store"             # 门店仓


class Warehouse(Base):
    """仓库模型"""
    __tablename__ = "warehouses"

    id = Column(Integer, primary_key=True, autoincrement=True)
    code = Column(String(32), unique=True, nullable=False, index=True, comment="仓库编码")
    name = Column(String(128), nullable=False, comment="仓库名称")
    wh_type = Column(SAEnum(WarehouseType), nullable=False, comment="仓库类型")
    city = Column(String(64), comment="城市")
    address = Column(String(256), comment="地址")
    longitude = Column(Numeric(10, 6), comment="经度")
    latitude = Column(Numeric(10, 6), comment="纬度")
    capacity = Column(Numeric(12, 2), default=0, comment="容量(m3)")
    is_active = Column(Boolean, default=True, comment="是否启用")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    inventories = relationship("Inventory", back_populates="warehouse", lazy="selectin")

    def __repr__(self):
        return f"<Warehouse(code={self.code}, name={self.name})>"


class Inventory(Base):
    """库存模型"""
    __tablename__ = "inventories"

    id = Column(Integer, primary_key=True, autoincrement=True)
    sku_id = Column(Integer, nullable=False, index=True, comment="SKU ID")
    warehouse_id = Column(Integer, nullable=False, index=True, comment="仓库ID")
    available_qty = Column(Integer, default=0, comment="可用库存")
    frozen_qty = Column(Integer, default=0, comment="冻结库存")
    in_transit_qty = Column(Integer, default=0, comment="在途库存")
    pre_sale_qty = Column(Integer, default=0, comment="预售占用量")
    avg_cost = Column(Numeric(12, 2), default=0, comment="加权平均成本")
    safety_stock = Column(Integer, default=0, comment="安全库存")
    reorder_point = Column(Integer, default=0, comment="再订货点")
    last_replenish_date = Column(DateTime(timezone=True), comment="最近补货日期")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    # 关联
    sku = relationship("SKU", back_populates="inventories")
    warehouse = relationship("Warehouse", back_populates="inventories")
    logs = relationship("InventoryLog", back_populates="inventory", lazy="selectin")

    # 唯一约束
    __table_args__ = (
        {"comment": "库存表，每个SKU在每个仓库一条记录"},
    )

    @property
    def total_qty(self) -> int:
        return self.available_qty + self.frozen_qty + self.in_transit_qty

    @property
    def net_available(self) -> int:
        return self.available_qty - self.pre_sale_qty

    def __repr__(self):
        return f"<Inventory(sku_id={self.sku_id}, wh_id={self.warehouse_id}, qty={self.available_qty})>"


class InventoryChangeType(str, enum.Enum):
    INBOUND = "inbound"             # 入库
    OUTBOUND = "outbound"           # 出库
    FREEZE = "freeze"               # 冻结
    UNFREEZE = "unfreeze"           # 解冻
    ADJUSTMENT = "adjustment"       # 盘点调整
    TRANSFER_IN = "transfer_in"     # 调拨入
    TRANSFER_OUT = "transfer_out"   # 调拨出
    RETURN = "return"               # 退货入库
    SCRAPPED = "scrapped"           # 报废


class InventoryLog(Base):
    """库存变动日志"""
    __tablename__ = "inventory_logs"

    id = Column(Integer, primary_key=True, autoincrement=True)
    inventory_id = Column(Integer, nullable=False, index=True, comment="库存记录ID")
    change_type = Column(SAEnum(InventoryChangeType), nullable=False, comment="变动类型")
    quantity = Column(Integer, nullable=False, comment="变动数量(正数增/负数减)")
    before_qty = Column(Integer, nullable=False, comment="变动前数量")
    after_qty = Column(Integer, nullable=False, comment="变动后数量")
    reference_no = Column(String(64), comment="关联单号(采购单/销售单/调拨单)")
    operator = Column(String(64), comment="操作人")
    remark = Column(String(256), comment="备注")
    created_at = Column(DateTime(timezone=True), server_default=func.now())

    inventory = relationship("Inventory", back_populates="logs")

    def __repr__(self):
        return f"<InventoryLog(type={self.change_type}, qty={self.quantity})>"
```

### 供应商与采购模型

```python
class SupplierGrade(str, enum.Enum):
    A = "A"
    B = "B"
    C = "C"
    D = "D"
    E = "E"


class Supplier(Base):
    """供应商模型"""
    __tablename__ = "suppliers"

    id = Column(Integer, primary_key=True, autoincrement=True)
    code = Column(String(32), unique=True, nullable=False, index=True, comment="供应商编码")
    name = Column(String(128), nullable=False, comment="供应商名称")
    contact_person = Column(String(64), comment="联系人")
    contact_phone = Column(String(32), comment="联系电话")
    email = Column(String(128), comment="邮箱")
    address = Column(String(256), comment="地址")
    city = Column(String(64), comment="城市")
    lead_time_days = Column(Integer, default=7, comment="平均交期(天)")
    min_order_qty = Column(Integer, default=1, comment="最小订货量")
    quality_score = Column(Numeric(5, 2), default=0, comment="质量评分")
    price_score = Column(Numeric(5, 2), default=0, comment="价格评分")
    delivery_score = Column(Numeric(5, 2), default=0, comment="交期评分")
    service_score = Column(Numeric(5, 2), default=0, comment="服务评分")
    risk_score = Column(Numeric(5, 2), default=0, comment="风险评分")
    total_score = Column(Numeric(5, 2), default=0, comment="综合评分")
    grade = Column(SAEnum(SupplierGrade), default=SupplierGrade.C, comment="等级")
    is_active = Column(Boolean, default=True, comment="是否合作中")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    purchase_orders = relationship("PurchaseOrder", back_populates="supplier", lazy="selectin")

    def __repr__(self):
        return f"<Supplier(code={self.code}, name={self.name}, grade={self.grade})>"


class PurchaseOrderStatus(str, enum.Enum):
    DRAFT = "draft"
    SUBMITTED = "submitted"
    CONFIRMED = "confirmed"
    PARTIAL_RECEIVED = "partial_received"
    RECEIVED = "received"
    CANCELLED = "cancelled"


class PurchaseOrder(Base):
    """采购订单"""
    __tablename__ = "purchase_orders"

    id = Column(Integer, primary_key=True, autoincrement=True)
    order_no = Column(String(32), unique=True, nullable=False, index=True, comment="采购单号")
    supplier_id = Column(Integer, nullable=False, index=True, comment="供应商ID")
    warehouse_id = Column(Integer, nullable=False, comment="入库仓库ID")
    status = Column(SAEnum(PurchaseOrderStatus), default=PurchaseOrderStatus.DRAFT, comment="状态")
    total_amount = Column(Numeric(12, 2), default=0, comment="订单总额")
    expected_date = Column(DateTime(timezone=True), comment="预计到货日期")
    actual_date = Column(DateTime(timezone=True), comment="实际到货日期")
    remark = Column(String(256), comment="备注")
    created_by = Column(String(64), comment="创建人")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    supplier = relationship("Supplier", back_populates="purchase_orders")
    items = relationship("PurchaseItem", back_populates="order", lazy="selectin")

    def __repr__(self):
        return f"<PurchaseOrder(order_no={self.order_no}, status={self.status})>"


class PurchaseItem(Base):
    """采购明细"""
    __tablename__ = "purchase_items"

    id = Column(Integer, primary_key=True, autoincrement=True)
    order_id = Column(Integer, nullable=False, index=True, comment="采购订单ID")
    sku_id = Column(Integer, nullable=False, comment="SKU ID")
    quantity = Column(Integer, nullable=False, comment="采购数量")
    received_qty = Column(Integer, default=0, comment="已收货数量")
    unit_price = Column(Numeric(12, 2), nullable=False, comment="单价")
    amount = Column(Numeric(12, 2), nullable=False, comment="金额")
    remark = Column(String(256), comment="备注")

    order = relationship("PurchaseOrder", back_populates="items")

    def __repr__(self):
        return f"<PurchaseItem(order_id={self.order_id}, sku_id={self.sku_id}, qty={self.quantity})>"
```

### 销售订单模型

```python
class SalesOrderStatus(str, enum.Enum):
    PENDING = "pending"
    PAID = "paid"
    SHIPPED = "shipped"
    DELIVERED = "delivered"
    CANCELLED = "cancelled"
    REFUNDED = "refunded"


class SalesOrder(Base):
    """销售订单"""
    __tablename__ = "sales_orders"

    id = Column(Integer, primary_key=True, autoincrement=True)
    order_no = Column(String(32), unique=True, nullable=False, index=True, comment="订单号")
    customer_id = Column(Integer, comment="客户ID")
    warehouse_id = Column(Integer, comment="发货仓库ID")
    status = Column(SAEnum(SalesOrderStatus), default=SalesOrderStatus.PENDING, comment="状态")
    total_amount = Column(Numeric(12, 2), default=0, comment="订单总额")
    discount_amount = Column(Numeric(12, 2), default=0, comment="优惠金额")
    payment_amount = Column(Numeric(12, 2), default=0, comment="实付金额")
    shipping_fee = Column(Numeric(10, 2), default=0, comment="运费")
    channel = Column(String(32), comment="销售渠道")
    created_at = Column(DateTime(timezone=True), server_default=func.now(), index=True)
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    items = relationship("SalesItem", back_populates="order", lazy="selectin")

    def __repr__(self):
        return f"<SalesOrder(order_no={self.order_no}, status={self.status})>"


class SalesItem(Base):
    """销售明细"""
    __tablename__ = "sales_items"

    id = Column(Integer, primary_key=True, autoincrement=True)
    order_id = Column(Integer, nullable=False, index=True, comment="销售订单ID")
    sku_id = Column(Integer, nullable=False, comment="SKU ID")
    quantity = Column(Integer, nullable=False, comment="购买数量")
    unit_price = Column(Numeric(12, 2), nullable=False, comment="成交单价")
    amount = Column(Numeric(12, 2), nullable=False, comment="成交金额")

    order = relationship("SalesOrder", back_populates="items")

    def __repr__(self):
        return f"<SalesItem(order_id={self.order_id}, sku_id={self.sku_id}, qty={self.quantity})>"
```

### 定价模型

```python
class PriceRuleType(str, enum.Enum):
    COST_PLUS = "cost_plus"
    COMPETITIVE = "competitive"
    ELASTICITY = "elasticity"
    DYNAMIC = "dynamic"
    PROMOTION = "promotion"


class PriceRule(Base):
    """定价规则"""
    __tablename__ = "price_rules"

    id = Column(Integer, primary_key=True, autoincrement=True)
    name = Column(String(128), nullable=False, comment="规则名称")
    rule_type = Column(SAEnum(PriceRuleType), nullable=False, comment="规则类型")
    sku_id = Column(Integer, comment="SKU ID(null表示品类级规则)")
    category_id = Column(Integer, comment="品类ID")
    target_margin = Column(Numeric(5, 4), default=0.15, comment="目标毛利率")
    min_margin = Column(Numeric(5, 4), default=0.05, comment="最低毛利率")
    max_price_change = Column(Numeric(5, 4), default=0.10, comment="最大价格变动比例")
    elasticity = Column(Numeric(6, 3), comment="价格弹性")
    priority = Column(Integer, default=0, comment="优先级")
    is_active = Column(Boolean, default=True, comment="是否启用")
    params = Column(JSON, default=dict, comment="额外参数")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    def __repr__(self):
        return f"<PriceRule(name={self.name}, type={self.rule_type})>"


class PriceHistory(Base):
    """价格历史"""
    __tablename__ = "price_history"

    id = Column(Integer, primary_key=True, autoincrement=True)
    sku_id = Column(Integer, nullable=False, index=True, comment="SKU ID")
    old_price = Column(Numeric(12, 2), comment="原价格")
    new_price = Column(Numeric(12, 2), nullable=False, comment="新价格")
    reason = Column(String(128), comment="调价原因")
    rule_id = Column(Integer, comment="关联规则ID")
    operator = Column(String(64), comment="操作人")
    created_at = Column(DateTime(timezone=True), server_default=func.now())

    def __repr__(self):
        return f"<PriceHistory(sku_id={self.sku_id}, new_price={self.new_price})>"
```

### 预测与补货模型

```python
class ForecastResult(Base):
    """预测结果"""
    __tablename__ = "forecast_results"

    id = Column(Integer, primary_key=True, autoincrement=True)
    sku_id = Column(Integer, nullable=False, index=True, comment="SKU ID")
    warehouse_id = Column(Integer, nullable=False, index=True, comment="仓库ID")
    forecast_date = Column(DateTime(timezone=True), nullable=False, comment="预测日期")
    predicted_qty = Column(Numeric(12, 2), nullable=False, comment="预测销量")
    predicted_lower = Column(Numeric(12, 2), comment="预测下限")
    predicted_upper = Column(Numeric(12, 2), comment="预测上限")
    actual_qty = Column(Numeric(12, 2), comment="实际销量")
    model_type = Column(String(32), comment="模型类型(prophet/xgboost)")
    model_version = Column(String(32), comment="模型版本")
    mape = Column(Numeric(6, 2), comment="MAPE")
    created_at = Column(DateTime(timezone=True), server_default=func.now())

    __table_args__ = (
        {"comment": "预测结果表"},
    )

    def __repr__(self):
        return f"<ForecastResult(sku_id={self.sku_id}, date={self.forecast_date})>"


class ReplenishPlanStatus(str, enum.Enum):
    PENDING = "pending"
    APPROVED = "approved"
    ORDERED = "ordered"
    COMPLETED = "completed"
    CANCELLED = "cancelled"


class ReplenishPlan(Base):
    """补货计划"""
    __tablename__ = "replenish_plans"

    id = Column(Integer, primary_key=True, autoincrement=True)
    sku_id = Column(Integer, nullable=False, index=True, comment="SKU ID")
    warehouse_id = Column(Integer, nullable=False, index=True, comment="仓库ID")
    current_inventory = Column(Integer, nullable=False, comment="当前库存")
    safety_stock = Column(Integer, nullable=False, comment="安全库存")
    reorder_point = Column(Integer, nullable=False, comment="再订货点")
    suggested_qty = Column(Integer, nullable=False, comment="建议补货量")
    strategy = Column(String(32), comment="补货策略")
    status = Column(SAEnum(ReplenishPlanStatus), default=ReplenishPlanStatus.PENDING, comment="状态")
    forecast_id = Column(Integer, comment="关联预测ID")
    purchase_order_id = Column(Integer, comment="关联采购订单ID")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

    def __repr__(self):
        return f"<ReplenishPlan(sku_id={self.sku_id}, qty={self.suggested_qty})>"
```

## Redis缓存设计

### 缓存键设计

| 键模式 | 数据类型 | TTL | 用途 |
|--------|---------|-----|------|
| `inventory:{sku_id}:{wh_id}` | Hash | 5min | 实时库存缓存 |
| `price:{sku_id}` | String | 10min | 当前售价缓存 |
| `forecast:{sku_id}:latest` | String | 1h | 最新预测结果 |
| `alert:inventory` | List | 30min | 库存预警列表 |
| `task:{task_id}` | String | 24h | 任务状态缓存 |

### 缓存操作

```python
import json
import redis.asyncio as redis
from app.config import settings

redis_client = redis.from_url(settings.REDIS_URL, decode_responses=True)


async def get_inventory_cache(sku_id: int, warehouse_id: int) -> dict | None:
    """获取库存缓存"""
    key = f"inventory:{sku_id}:{warehouse_id}"
    data = await redis_client.hgetall(key)
    return data if data else None


async def set_inventory_cache(sku_id: int, warehouse_id: int, data: dict) -> None:
    """设置库存缓存"""
    key = f"inventory:{sku_id}:{warehouse_id}"
    await redis_client.hset(key, mapping=data)
    await redis_client.expire(key, 300)  # 5分钟过期


async def get_price_cache(sku_id: int) -> float | None:
    """获取价格缓存"""
    key = f"price:{sku_id}"
    data = await redis_client.get(key)
    return float(data) if data else None


async def set_price_cache(sku_id: int, price: float) -> None:
    """设置价格缓存"""
    key = f"price:{sku_id}"
    await redis_client.set(key, str(price), ex=600)  # 10分钟过期


async def invalidate_inventory_cache(sku_id: int, warehouse_id: int) -> None:
    """使库存缓存失效"""
    key = f"inventory:{sku_id}:{warehouse_id}"
    await redis_client.delete(key)
```

## 数据库迁移脚本(Alembic)

### 初始化

```bash
alembic init migrations
```

### 迁移配置

```python
# migrations/env.py
from asyncio import run
from sqlalchemy.ext.asyncio import create_async_engine
from app.database import Base
from app.config import settings
from app.models import *  # noqa: 确保所有模型被导入

target_metadata = Base.metadata


def run_migrations_online():
    connectable = create_async_engine(settings.DATABASE_URL)
    async def do_run():
        async with connectable.connect() as connection:
            await connection.run_sync(target_metadata.create_all)
    run(do_run())
```

### 生成迁移

```bash
# 自动生成迁移
alembic revision --autogenerate -m "create initial tables"

# 执行迁移
alembic upgrade head

# 回滚
alembic downgrade -1
```

## 数据初始化脚本

```python
import asyncio
import numpy as np
from datetime import datetime, timedelta
from sqlalchemy.ext.asyncio import AsyncSession
from app.database import async_session
from app.models.product import Product, SKU, ProductStatus
from app.models.inventory import Warehouse, WarehouseType, Inventory
from app.models.supplier import Supplier, SupplierGrade
from app.models.order import SalesOrder, SalesOrderStatus, SalesItem


async def seed_products(session: AsyncSession):
    """初始化商品数据"""
    products = [
        Product(
            spu_code="SPU001", name="无线蓝牙耳机", brand="SoundMax",
            category_id=1, status=ProductStatus.ACTIVE,
            key_attrs={"brand": "SoundMax", "model": "SM-200"},
        ),
        Product(
            spu_code="SPU002", name="智能手表Pro", brand="TechFit",
            category_id=2, status=ProductStatus.ACTIVE,
            key_attrs={"brand": "TechFit", "model": "TF-Pro"},
        ),
        Product(
            spu_code="SPU003", name="有机纯牛奶", brand="绿源",
            category_id=3, status=ProductStatus.ACTIVE,
            key_attrs={"brand": "绿源", "spec": "250ml*12"},
        ),
    ]
    session.add_all(products)
    await session.flush()

    skus = [
        SKU(sku_code="SKU001-01", product_id=1, spec_values={"color": "黑色"}, cost_price=80, sale_price=199, weight=0.05),
        SKU(sku_code="SKU001-02", product_id=1, spec_values={"color": "白色"}, cost_price=80, sale_price=199, weight=0.05),
        SKU(sku_code="SKU002-01", product_id=2, spec_values={"color": "黑色", "size": "46mm"}, cost_price=350, sale_price=899, weight=0.06),
        SKU(sku_code="SKU002-02", product_id=2, spec_values={"color": "银色", "size": "42mm"}, cost_price=300, sale_price=799, weight=0.05),
        SKU(sku_code="SKU003-01", product_id=3, spec_values={"flavor": "原味"}, cost_price=35, sale_price=69, weight=3.0),
    ]
    session.add_all(skus)


async def seed_warehouses(session: AsyncSession):
    """初始化仓库数据"""
    warehouses = [
        Warehouse(code="WH001", name="华东总仓", wh_type=WarehouseType.CENTRAL, city="上海", capacity=50000),
        Warehouse(code="WH002", name="华南区域仓", wh_type=WarehouseType.REGIONAL, city="广州", capacity=20000),
        Warehouse(code="WH003", name="华北区域仓", wh_type=WarehouseType.REGIONAL, city="北京", capacity=20000),
        Warehouse(code="WH004", name="上海前置仓", wh_type=WarehouseType.FRONT, city="上海", capacity=2000),
    ]
    session.add_all(warehouses)


async def seed_suppliers(session: AsyncSession):
    """初始化供应商数据"""
    suppliers = [
        Supplier(
            code="SUP001", name="深圳声科电子有限公司", contact_person="张经理",
            contact_phone="13800138001", city="深圳", lead_time_days=5,
            quality_score=92, price_score=85, delivery_score=90, service_score=88, risk_score=85,
            total_score=88.5, grade=SupplierGrade.A,
        ),
        Supplier(
            code="SUP002", name="东莞智造科技有限公司", contact_person="李总",
            contact_phone="13900139002", city="东莞", lead_time_days=7,
            quality_score=88, price_score=90, delivery_score=82, service_score=80, risk_score=78,
            total_score=85.4, grade=SupplierGrade.B,
        ),
        Supplier(
            code="SUP003", name="内蒙古绿源乳业集团", contact_person="王经理",
            contact_phone="13700137003", city="呼和浩特", lead_time_days=3,
            quality_score=95, price_score=80, delivery_score=92, service_score=90, risk_score=88,
            total_score=89.1, grade=SupplierGrade.A,
        ),
    ]
    session.add_all(suppliers)


async def seed_sales_history(session: AsyncSession, days: int = 365):
    """生成模拟销售历史数据"""
    np.random.seed(42)
    sku_ids = [1, 2, 3, 4, 5]
    base_demands = [50, 30, 20, 25, 200]

    for sku_id, base_demand in zip(sku_ids, base_demands):
        for day_offset in range(days):
            date = datetime.now() - timedelta(days=days - day_offset)
            # 加入趋势、季节性和噪声
            trend = day_offset * 0.02
            seasonality = 20 * np.sin(2 * np.pi * day_offset / 365)
            promo_boost = 50 if (date.month == 6 and date.day <= 18) or (date.month == 11 and date.day <= 11) else 0
            noise = np.random.normal(0, base_demand * 0.15)
            qty = max(int(base_demand + trend + seasonality + promo_boost + noise), 1)

            order = SalesOrder(
                order_no=f"SO-{sku_id:03d}-{day_offset:05d}",
                status=SalesOrderStatus.DELIVERED,
                total_amount=qty * 100,
                payment_amount=qty * 100,
                warehouse_id=1,
                created_at=date,
            )
            session.add(order)
            await session.flush()

            item = SalesItem(
                order_id=order.id,
                sku_id=sku_id,
                quantity=qty,
                unit_price=100,
                amount=qty * 100,
            )
            session.add(item)


async def main():
    """主初始化函数"""
    async with async_session() as session:
        await seed_products(session)
        await seed_warehouses(session)
        await seed_suppliers(session)
        await seed_sales_history(session, days=180)  # 生成180天历史数据
        await session.commit()
        print("数据初始化完成")


if __name__ == "__main__":
    asyncio.run(main())
```

## 数据索引与查询优化

### 关键索引

```sql
-- 销售订单按日期查询
CREATE INDEX idx_sales_orders_created_at ON sales_orders (created_at);

-- 销售明细按SKU查询
CREATE INDEX idx_sales_items_sku_id ON sales_items (sku_id);

-- 库存按SKU+仓库组合查询
CREATE INDEX idx_inventories_sku_wh ON inventories (sku_id, warehouse_id);

-- 预测结果按SKU+日期查询
CREATE INDEX idx_forecast_results_sku_date ON forecast_results (sku_id, forecast_date);

-- 价格历史按SKU查询
CREATE INDEX idx_price_history_sku ON price_history (sku_id, created_at);

-- 库存变动日志按库存ID查询
CREATE INDEX idx_inventory_logs_inventory ON inventory_logs (inventory_id, created_at);
```

### 查询优化建议

| 场景 | 优化策略 | 预期效果 |
|------|---------|---------|
| 销量历史聚合 | 物化视图/预计算表 | 查询时间降低90% |
| 库存实时查询 | Redis缓存 | 响应<10ms |
| 预测批量查询 | 分区表按月分区 | 扫描数据量减少 |
| 价格历史查询 | 时间序列索引 | 范围查询加速 |
| 统计报表 | 预计算+缓存 | 报表秒级响应 |
