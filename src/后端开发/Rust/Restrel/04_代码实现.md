---
title: 04 · 代码实现
icon: fa6-solid:code
order: 4
category:
  - Restrel
tag:
  - httparse
  - BytesMut
  - keep-alive
  - 代码实现
---

# 04 · Restrel 代码实现

## 一、【Dev】mo-server-core：框架核心层

### 1.1 `error.rs`

```rust
//! 统一错误类型
//! 对应 C# 中分散在各层的异常，这里统一为一个 enum

use thiserror::Error;

#[derive(Debug, Error)]
pub enum ServerError {
    // ── 解析层（parser.rs 产生）──────────────────────────────────────
    /// 对端主动关闭连接（EOF）
    #[error("连接已关闭")]
    ConnectionClosed,

    /// headers 超过配置的最大字节数（对应 Kestrel 返回 431）
    #[error("请求头过大")]
    RequestHeadersTooLarge,

    /// 请求格式错误（对应 Kestrel 返回 400）
    #[error("请求格式错误：{0}")]
    BadRequest(String),

    /// TCP IO 错误
    #[error("IO 错误：{0}")]
    Io(String),

    // ── 路由层（router.rs 产生）──────────────────────────────────────
    #[error("路由未找到：{0}")]
    NotFound(String),

    #[error("方法不允许：{0}")]
    MethodNotAllowed(String),

    #[error("路由冲突：{path}（{cause}）")]
    RouteConflict { path: String, cause: String },

    // ── 鉴权层（auth_mw.rs 产生）─────────────────────────────────────
    #[error("未授权：{0}")]
    Unauthorized(String),

    // ── 数据库层（service_repo.rs 产生）──────────────────────────────
    #[error("数据库错误：{0}")]
    Database(#[from] sqlx::Error),

    // ── 序列化（handlers 产生）───────────────────────────────────────
    #[error("JSON 错误：{0}")]
    Json(#[from] serde_json::Error),
}

impl ServerError {
    /// 将错误映射到 HTTP 状态码
    pub fn status_code(&self) -> u16 {
        match self {
            Self::BadRequest(_)           => 400,
            Self::Unauthorized(_)         => 401,
            Self::NotFound(_)             => 404,
            Self::MethodNotAllowed(_)     => 405,
            Self::RequestHeadersTooLarge  => 431,
            _                             => 500,
        }
    }
}
```

---

### 1.2 `parser.rs` ★ 核心：手写 HTTP/1.1 解析

```rust
//! HTTP/1.1 请求解析器
//!
//! 对应 Kestrel 的两个核心组件：
//!   - `Http1Connection<TTransport>`：调用 llhttp 解析字节流
//!   - `System.IO.Pipelines.PipeReader`：管理缓冲区
//!
//! # 解析算法（Partial 状态机）
//! ```text
//! loop:
//!   ┌─ 尝试在 buf 中解析（httparse::parse）
//!   │    Complete(n) → n = headers 区字节数（含 \r\n\r\n）
//!   │    Partial     → 数据不足，继续读 socket
//!   │    Err         → 400 Bad Request
//!   └─ AsyncReadExt::read_buf(&mut buf)
//! ```

use std::collections::HashMap;

use bytes::{Bytes, BytesMut};
use tokio::io::AsyncReadExt;

use crate::error::ServerError;

const MAX_HEADERS: usize = 64;

/// 触发 431 的 headers 区最大字节数
/// 对应 KestrelServerLimits.MaxRequestHeadersTotalSize（Kestrel 默认 32 KB）
pub const MAX_HEADER_BYTES: usize = 32 * 1024;

/// 从 TCP 流解析得到的原始 HTTP/1.1 请求
#[derive(Debug)]
pub struct RawRequest {
    pub method:  String,
    /// 完整 request-target（含 query string），例如 "/services?page=1"
    pub path:    String,
    /// 0 = HTTP/1.0   1 = HTTP/1.1
    pub version: u8,
    /// 所有请求头，key 已 to_ascii_lowercase()
    pub headers: HashMap<String, String>,
    /// Content-Length 字节已完整读取；无 body 时为 Bytes::new()
    pub body:    Bytes,
}

/// 从 `reader` 读取并解析一个完整的 HTTP/1.1 请求
///
/// # 参数
/// - `reader` : 底层 TCP 流（OwnedReadHalf）
/// - `buf`    : 连接级持久缓冲区（keep-alive 时跨请求保留剩余字节）
///
/// # 等价 C# 代码
/// ```csharp
/// // Kestrel Http1Connection.ParseRequestAsync()
/// while (true) {
///     var result = await _reader.ReadAsync();
///     var n = _parser.Execute(result.Buffer); // llhttp
///     if (n.IsCompleted) { _reader.AdvanceTo(result.Buffer.GetPosition(n)); break; }
///     _reader.AdvanceTo(result.Buffer.Start, result.Buffer.End);
/// }
/// ```
pub async fn read_request<R>(
    reader: &mut R,
    buf:    &mut BytesMut,
) -> Result<RawRequest, ServerError>
where
    R: AsyncReadExt + Unpin,
{
    if buf.capacity() == 0 {
        buf.reserve(4096); // 首次按需分配（对应 PipeOptions.MinimumSegmentSize）
    }

    loop {
        // ── 步骤 1：用现有数据尝试解析 headers ───────────────────────
        if !buf.is_empty() {
            // headers_storage 放循环内，生命周期与 req 对齐，避免借用冲突
            let mut storage = [httparse::EMPTY_HEADER; MAX_HEADERS];
            let mut req     = httparse::Request::new(&mut storage);

            match req.parse(buf) {
                // n = status-line + headers + \r\n\r\n 总字节数
                Ok(httparse::Status::Complete(n)) => {
                    // 立即提取字段，结束对 storage/req 的借用
                    let method  = req.method.unwrap_or("GET").to_string();
                    let path    = req.path.unwrap_or("/").to_string();
                    let version = req.version.unwrap_or(1);

                    let mut headers        = HashMap::new();
                    let mut content_length = None::<usize>;

                    for h in req.headers.iter() {
                        let name  = h.name.to_ascii_lowercase(); // 统一 lowercase
                        let value = String::from_utf8_lossy(h.value).into_owned();
                        if name == "content-length" {
                            content_length = value.trim().parse().ok();
                        }
                        headers.insert(name, value);
                    }
                    // 借用结束 ───────────────────────────────────────────

                    // ── 步骤 2：消费 headers 区 ─────────────────────
                    // split_to(n)：buf 变为 [n..]（等价 reader.AdvanceTo(consumed)）
                    let _ = buf.split_to(n);

                    // ── 步骤 3：读取 body ────────────────────────────
                    let body = read_body(reader, buf, content_length).await?;

                    return Ok(RawRequest { method, path, version, headers, body });
                }

                // 数据不足：继续 loop，在底部 read_buf
                Ok(httparse::Status::Partial) => {}

                // 格式错误 → 400
                Err(e) => return Err(ServerError::BadRequest(e.to_string())),
            }
        }

        // ── 步骤 4：从 socket 读取更多数据 ──────────────────────────
        if buf.len() >= MAX_HEADER_BYTES {
            return Err(ServerError::RequestHeadersTooLarge);
        }

        // read_buf：把 TCP 内核缓冲区数据追加到 buf（零额外拷贝）
        // 对应 await pipeReader.ReadAsync()
        let n = reader
            .read_buf(buf)
            .await
            .map_err(|e| ServerError::Io(e.to_string()))?;

        if n == 0 {
            return Err(ServerError::ConnectionClosed); // 对端 TCP FIN
        }
        // 继续 loop 重试解析
    }
}

/// 读取请求体
///
/// 对应 Kestrel `Http1MessageBody.ConsumeAsync()`。
/// buf 里可能已经有部分 body（TCP 粘包），先检查再读。
async fn read_body<R>(
    reader:         &mut R,
    buf:            &mut BytesMut,
    content_length: Option<usize>,
) -> Result<Bytes, ServerError>
where
    R: AsyncReadExt + Unpin,
{
    let len = match content_length {
        None | Some(0) => return Ok(Bytes::new()),
        Some(n)        => n,
    };

    while buf.len() < len {
        let n = reader
            .read_buf(buf)
            .await
            .map_err(|e| ServerError::Io(e.to_string()))?;
        if n == 0 {
            return Err(ServerError::ConnectionClosed);
        }
    }

    // split_to + freeze：零拷贝切出 len 字节
    // buf 继续保留 [len..] 部分（可能是下一个请求的起始数据）
    Ok(buf.split_to(len).freeze())
}
```

---

### 1.3 `serializer.rs` ★ 核心：手写 HTTP/1.1 响应序列化

```rust
//! HTTP/1.1 响应序列化器
//!
//! 对应 Kestrel 的 `Http1OutputProducer`：
//!   WriteResponseHeaders() → body 追加 → PipeWriter.FlushAsync()
//!
//! Restrel 把三步合并为一个函数，直接产出 Bytes，
//! 由 connection.rs 一次 write_all() 写入 socket。

use bytes::{BufMut, Bytes, BytesMut};

/// 序列化结果
pub struct SerializedResponse {
    /// 完整 HTTP/1.1 字节流（直接 write_all 到 socket）
    pub bytes: Bytes,
    /// connection loop 是否继续等待下一个请求
    pub keep_alive: bool,
}

/// 把 HTTP 响应序列化为 TCP 字节流
///
/// # 输出格式（RFC 7230）
/// ```text
/// HTTP/1.1 200 OK\r\n
/// server: mo-server/0.1.0\r\n
/// content-length: 42\r\n
/// connection: keep-alive\r\n
/// keep-alive: timeout=75, max=1000\r\n
/// content-type: application/json; charset=utf-8\r\n
/// \r\n
/// {"status":"ok"}
/// ```
pub fn serialize_response(
    status:             u16,
    headers:            &[(String, String)],
    body:               &Bytes,
    http_version:       u8,
    request_keep_alive: bool,
) -> SerializedResponse {
    let keep_alive = http_version >= 1 && request_keep_alive;

    let estimated = 256 + headers.len() * 64 + body.len();
    let mut buf   = BytesMut::with_capacity(estimated);

    // 状态行
    let ver    = if http_version >= 1 { "HTTP/1.1" } else { "HTTP/1.0" };
    let reason = status_reason(status);
    buf.put(format!("{ver} {status} {reason}\r\n").as_bytes());

    // 固定响应头
    buf.put(b"server: mo-server/0.1.0\r\n".as_ref());
    buf.put(format!("content-length: {}\r\n", body.len()).as_bytes());

    if keep_alive {
        buf.put(b"connection: keep-alive\r\nkeep-alive: timeout=75, max=1000\r\n".as_ref());
    } else {
        buf.put(b"connection: close\r\n".as_ref());
    }

    // 自定义响应头
    for (name, value) in headers {
        buf.put(format!("{name}: {value}\r\n").as_bytes());
    }

    // 空行 + body
    buf.put(b"\r\n".as_ref());
    buf.put(body.as_ref());

    // freeze()：BytesMut → 不可变 Bytes（同一块内存，不拷贝）
    SerializedResponse { bytes: buf.freeze(), keep_alive }
}

fn status_reason(status: u16) -> &'static str {
    match status {
        200 => "OK",
        201 => "Created",
        204 => "No Content",
        400 => "Bad Request",
        401 => "Unauthorized",
        403 => "Forbidden",
        404 => "Not Found",
        405 => "Method Not Allowed",
        409 => "Conflict",
        413 => "Payload Too Large",
        422 => "Unprocessable Entity",
        431 => "Request Header Fields Too Large",
        500 => "Internal Server Error",
        503 => "Service Unavailable",
        _   => "Unknown",
    }
}
```

---

### 1.4 `context.rs`

```rust
//! HttpContext：在中间件管道中传递的请求上下文
//! 对应 ASP.NET Core `Microsoft.AspNetCore.Http.HttpContext`

use std::{
    any::{Any, TypeId},
    collections::HashMap,
    sync::Arc,
};
use bytes::Bytes;
use crate::parser::RawRequest;

/// 类型安全的扩展包（对应 C# HttpContext.Items，但用 TypeId 作 key）
#[derive(Default)]
pub struct Extensions(HashMap<TypeId, Box<dyn Any + Send + Sync>>);

impl Extensions {
    /// 插入值（类型即 key）
    /// 对应 C#: `httpContext.Items["Claims"] = claims;`
    pub fn insert<T: Send + Sync + 'static>(&mut self, val: T) {
        self.0.insert(TypeId::of::<T>(), Box::new(val));
    }

    /// 取出类型化引用
    /// 对应 C#: `httpContext.Items["Claims"] as Claims`
    pub fn get<T: 'static>(&self) -> Option<&T> {
        self.0.get(&TypeId::of::<T>())?.downcast_ref::<T>()
    }
}

/// 请求上下文（中间件链中逐层传递）
pub struct HttpContext {
    raw:              RawRequest,
    pub route_params: HashMap<String, String>,
    pub extensions:   Extensions,
    /// 类型擦除的应用状态（handler 通过 state_arc::<T>() 取回具体类型）
    state: Arc<dyn Any + Send + Sync>,
}

impl HttpContext {
    pub fn new(raw: RawRequest, state: Arc<dyn Any + Send + Sync>) -> Self {
        Self { raw, route_params: HashMap::new(), extensions: Extensions::default(), state }
    }

    pub fn method(&self) -> &str  { &self.raw.method }

    /// 请求路径（不含 query string）
    pub fn path(&self) -> &str {
        self.raw.path.split_once('?').map(|(p, _)| p).unwrap_or(&self.raw.path)
    }

    pub fn uri(&self) -> &str { &self.raw.path }

    /// 请求头（key 已 lowercase）
    pub fn header(&self, name: &str) -> Option<&str> {
        self.raw.headers.get(name).map(String::as_str)
    }

    pub fn body(&self) -> &Bytes { &self.raw.body }

    pub fn version(&self) -> u8 { self.raw.version }

    /// 路由路径参数（Router 匹配后注入）
    /// 对应 C#: `[FromRoute] Guid id` — Rust 需手动 `.parse::<Uuid>()?`
    pub fn param(&self, name: &str) -> Option<&str> {
        self.route_params.get(name).map(String::as_str)
    }

    /// Query string 参数（例如 ?page=1&size=10）
    pub fn query(&self, name: &str) -> Option<&str> {
        let qs = self.raw.path.split_once('?').map(|(_, q)| q)?;
        qs.split('&').find_map(|kv| {
            let (k, v) = kv.split_once('=')?;
            (k == name).then_some(v)
        })
    }

    /// 获取 Arc<T> 形式的应用状态
    /// 对应 C#: `context.RequestServices.GetRequiredService<AppState>()`
    ///
    /// # 典型用法
    /// ```rust,ignore
    /// let state = ctx.state_arc::<AppState>().unwrap();
    /// let rows = state.db.fetch_all(...).await?;
    /// ```
    pub fn state_arc<T: Any + Send + Sync + 'static>(&self) -> Option<Arc<T>> {
        Arc::clone(&self.state).downcast::<T>().ok()
    }
}
```

---

### 1.5 `response.rs`

```rust
//! HTTP 响应类型及便利构造函数

use bytes::Bytes;

/// 中间件链产出的 HTTP 响应（序列化前）
#[derive(Debug)]
pub struct Response {
    pub status:  u16,
    pub headers: Vec<(String, String)>,
    pub body:    Bytes,
}

impl Response {
    pub fn new(status: u16) -> Self {
        Self { status, headers: Vec::new(), body: Bytes::new() }
    }

    pub fn with_json(mut self, data: &impl serde::Serialize) -> Self {
        self.body = serde_json::to_vec(data).unwrap_or_default().into();
        self.headers.push((
            "content-type".into(),
            "application/json; charset=utf-8".into(),
        ));
        self
    }

    pub fn with_header(mut self, name: impl Into<String>, value: impl Into<String>) -> Self {
        self.headers.push((name.into(), value.into()));
        self
    }
}

// 便利构造函数（等同 C# Results.Ok / Results.Created 等）
pub fn ok(data: &impl serde::Serialize) -> Response     { Response::new(200).with_json(data) }
pub fn created(data: &impl serde::Serialize) -> Response{ Response::new(201).with_json(data) }
pub fn no_content() -> Response                          { Response::new(204) }
pub fn not_found(msg: &str) -> Response {
    Response::new(404).with_json(&serde_json::json!({ "error": msg }))
}
pub fn bad_request(msg: &str) -> Response {
    Response::new(400).with_json(&serde_json::json!({ "error": msg }))
}
```

---

### 1.6 `middleware.rs`

```rust
//! 中间件管道（对应 ASP.NET Core IApplicationBuilder + RequestDelegate）

use std::{future::Future, sync::Arc};
use crate::{context::HttpContext, error::ServerError, response::Response};

pub type BoxFuture<T> = std::pin::Pin<Box<dyn Future<Output = T> + Send + 'static>>;

/// 调用下一个中间件的函数类型（对应 C# RequestDelegate）
pub type Next = Arc<
    dyn Fn(HttpContext) -> BoxFuture<Result<Response, ServerError>> + Send + Sync,
>;

/// 中间件 trait
pub trait Middleware: Send + Sync + 'static {
    fn handle(&self, ctx: HttpContext, next: Next)
        -> BoxFuture<Result<Response, ServerError>>;
}

/// 把 `async fn(ctx, next)` 包装为 `Middleware`
pub struct FnMiddleware<F>(pub F);

impl<F, Fut> Middleware for FnMiddleware<F>
where
    F:   Fn(HttpContext, Next) -> Fut + Send + Sync + 'static,
    Fut: Future<Output = Result<Response, ServerError>> + Send + 'static,
{
    fn handle(&self, ctx: HttpContext, next: Next)
        -> BoxFuture<Result<Response, ServerError>>
    {
        Box::pin((self.0)(ctx, next))
    }
}

/// 中间件管道（对应 C# ApplicationBuilder）
#[derive(Default)]
pub struct Pipeline {
    middlewares: Vec<Box<dyn Middleware>>,
}

impl Pipeline {
    pub fn new() -> Self { Self::default() }

    pub fn add(&mut self, mw: impl Middleware) {
        self.middlewares.push(Box::new(mw));
    }

    /// 仅供 server.rs 内部使用（接受已装箱的中间件）
    pub fn add_boxed(&mut self, mw: Box<dyn Middleware>) {
        self.middlewares.push(mw);
    }

    /// 将中间件列表 fold 为单个 Next 闭包（对应 C# ApplicationBuilder.Build()）
    ///
    /// # 洋葱模型
    /// 从末尾向前 fold，第一个 add 的中间件在最外层（先进先出）。
    /// 编译期单态化：每层是一个 Box::pin 闭包，无虚表开销。
    pub fn build(self, terminal: Next) -> Next {
        self.middlewares
            .into_iter()
            .rev()
            .fold(terminal, |next, mw| {
                let mw = Arc::new(mw);
                Arc::new(move |ctx: HttpContext| -> BoxFuture<_> {
                    let next = Arc::clone(&next);
                    let mw   = Arc::clone(&mw);
                    Box::pin(async move { mw.handle(ctx, next).await })
                })
            })
    }
}

// ── 单元测试 ──────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    use std::sync::{Arc, Mutex};
    use crate::{parser::RawRequest, response::no_content};
    use std::collections::HashMap;

    fn test_ctx() -> HttpContext {
        let raw = RawRequest {
            method:  "GET".into(),
            path:    "/".into(),
            version: 1,
            headers: HashMap::new(),
            body:    bytes::Bytes::new(),
        };
        HttpContext::new(raw, Arc::new(()))
    }

    #[tokio::test]
    async fn test_pipeline_order_is_correct() {
        let log = Arc::new(Mutex::new(vec![]));
        let l1  = Arc::clone(&log);
        let l2  = Arc::clone(&log);
        let l3  = Arc::clone(&log);

        let mut p = Pipeline::new();

        p.add(FnMiddleware(move |ctx, next| {
            let l = Arc::clone(&l1);
            Box::pin(async move {
                l.lock().unwrap().push("mw1-before");
                let r = next(ctx).await;
                l.lock().unwrap().push("mw1-after");
                r
            })
        }));

        p.add(FnMiddleware(move |ctx, next| {
            let l = Arc::clone(&l2);
            Box::pin(async move {
                l.lock().unwrap().push("mw2-before");
                let r = next(ctx).await;
                l.lock().unwrap().push("mw2-after");
                r
            })
        }));

        let terminal: Next = Arc::new(move |_ctx| {
            let l = Arc::clone(&l3);
            Box::pin(async move {
                l.lock().unwrap().push("handler");
                Ok(no_content())
            })
        });

        let composed = p.build(terminal);
        let _ = composed(test_ctx()).await;

        let order = log.lock().unwrap().clone();
        // 洋葱模型：与 C# ASP.NET Core 中间件行为完全一致
        assert_eq!(order, vec!["mw1-before", "mw2-before", "handler", "mw2-after", "mw1-after"]);
    }

    #[tokio::test]
    async fn test_pipeline_empty() {
        let terminal: Next = Arc::new(|_| Box::pin(async { Ok(no_content()) }));
        let result = Pipeline::new().build(terminal)(test_ctx()).await;
        assert!(result.is_ok());
    }
}
```

---

### 1.7 `router.rs`

```rust
//! 路径路由（对应 ASP.NET Core IRouter + MapGet/MapPost）
//! 使用 matchit：axum 内部使用的同款基数树，O(log n) 路径匹配。

use std::{collections::HashMap, sync::Arc};
use crate::{context::HttpContext, error::ServerError, middleware::{BoxFuture, Next}, response::Response};

pub type BoxHandler = Arc<
    dyn Fn(HttpContext) -> BoxFuture<Result<Response, ServerError>> + Send + Sync,
>;

/// HTTP 路由表
pub struct Router {
    routes: HashMap<String, matchit::Router<BoxHandler>>,
}

impl Router {
    pub fn new() -> Self { Self { routes: HashMap::new() } }

    /// 注册路由（对应 C# app.MapGet("/services/{id}", handler)）
    pub fn insert(
        &mut self,
        method:  &str,
        path:    &str,
        handler: BoxHandler,
    ) -> Result<(), ServerError> {
        self.routes
            .entry(method.to_ascii_uppercase())
            .or_insert_with(matchit::Router::new)
            .insert(path, handler)
            .map_err(|e| ServerError::RouteConflict { path: path.into(), cause: e.to_string() })
    }

    /// 包装为 `Next`（Pipeline 的最后一个节点）
    pub fn into_next(self: Arc<Self>) -> Next {
        Arc::new(move |mut ctx: HttpContext| -> BoxFuture<_> {
            let router = Arc::clone(&self);
            Box::pin(async move {
                let method = ctx.method().to_ascii_uppercase();

                let method_router = router.routes.get(&method)
                    .ok_or_else(|| ServerError::MethodNotAllowed(method.clone()))?;

                let matched = method_router.at(ctx.path())
                    .map_err(|_| ServerError::NotFound(ctx.path().to_string()))?;

                // 注入路由参数（对应 C# RouteData.Values）
                for (k, v) in matched.params.iter() {
                    ctx.route_params.insert(k.to_string(), v.to_string());
                }

                let handler = Arc::clone(matched.value);
                handler(ctx).await
            })
        })
    }
}

// ── 单元测试 ──────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    use crate::parser::RawRequest;
    use std::collections::HashMap;

    fn make_ctx(method: &str, path: &str) -> HttpContext {
        let raw = RawRequest {
            method: method.into(), path: path.into(),
            version: 1, headers: HashMap::new(), body: bytes::Bytes::new(),
        };
        HttpContext::new(raw, Arc::new(()))
    }

    fn static_handler(status: u16) -> BoxHandler {
        Arc::new(move |_| {
            let r = Response { status, headers: vec![], body: bytes::Bytes::new() };
            Box::pin(async move { Ok(r) })
        })
    }

    #[tokio::test]
    async fn test_route_param_extraction() {
        let mut router = Router::new();
        router.insert("GET", "/services/:id", Arc::new(|ctx| Box::pin(async move {
            let id = ctx.param("id").unwrap_or("").to_string();
            Ok(Response::new(200).with_json(&serde_json::json!({ "id": id })))
        }))).unwrap();

        let resp = Arc::new(router).into_next()(make_ctx("GET", "/services/abc-123")).await.unwrap();
        assert_eq!(resp.status, 200);
        let body: serde_json::Value = serde_json::from_slice(&resp.body).unwrap();
        assert_eq!(body["id"], "abc-123");
    }

    #[tokio::test]
    async fn test_get_route_not_found() {
        let err = Arc::new(Router::new()).into_next()(make_ctx("GET", "/unknown")).await.unwrap_err();
        assert!(matches!(err, ServerError::NotFound(_)));
    }

    #[tokio::test]
    async fn test_method_not_allowed() {
        let mut r = Router::new();
        r.insert("GET", "/services", static_handler(200)).unwrap();
        let err = Arc::new(r).into_next()(make_ctx("DELETE", "/services")).await.unwrap_err();
        assert!(matches!(err, ServerError::MethodNotAllowed(_)));
    }

    #[tokio::test]
    async fn test_route_conflict_at_build() {
        let mut r = Router::new();
        r.insert("GET", "/services/:id", static_handler(200)).unwrap();
        assert!(r.insert("GET", "/services/:id", static_handler(200)).is_err());
    }

    #[tokio::test]
    async fn test_multiple_methods_same_path() {
        let mut router = Router::new();
        router.insert("GET",    "/services", static_handler(200)).unwrap();
        router.insert("POST",   "/services", static_handler(201)).unwrap();
        router.insert("DELETE", "/services", static_handler(204)).unwrap();
        let router = Arc::new(router);

        for (m, expected) in [("GET", 200u16), ("POST", 201), ("DELETE", 204)] {
            let resp = Arc::clone(&router).into_next()(make_ctx(m, "/services")).await.unwrap();
            assert_eq!(resp.status, expected);
        }
    }
}
```

---

### 1.8 `lib.rs`

```rust
pub mod context;
pub mod error;
pub mod middleware;
pub mod parser;
pub mod response;
pub mod router;
pub mod serializer;

pub mod prelude {
    pub use crate::{
        context::HttpContext,
        error::ServerError,
        middleware::{BoxFuture, FnMiddleware, Middleware, Next, Pipeline},
        response::{bad_request, created, no_content, not_found, ok, Response},
        router::Router,
    };
}
```

---

## 二、【Dev】mo-server：传输层 + 连接管理

### 2.1 `connection.rs` ★ 核心：keep-alive 连接循环

```rust
//! per-connection keep-alive 请求处理循环
//!
//! 对应 Kestrel 的 `Http1Connection<TTransport>.ProcessRequestsAsync()`。
//!
//! # 等价 C# 代码
//! ```csharp
//! // Kestrel Http1Connection.ProcessRequestsAsync()
//! while (true) {
//!     if (!await ParseRequestAsync()) break;       // 读 + 解析
//!     await application.ProcessRequestAsync(ctx); // 中间件管道
//!     await output.WriteResponseAsync(response);  // 序列化 + 写出
//!     if (!keepAlive || cancel.IsCancellationRequested) break;
//! }
//! ```

use std::sync::Arc;

use bytes::BytesMut;
use tokio::io::{AsyncWriteExt, BufWriter};
use tokio::net::TcpStream;
use tokio_util::sync::CancellationToken;

use mo_server_core::{
    context::HttpContext,
    error::ServerError,
    middleware::Next,
    parser::read_request,
    serializer::serialize_response,
};

use crate::{limits::ServerLimits, state::AppState};

/// 处理单个 TCP 连接上的所有 HTTP 请求（keep-alive 循环）
///
/// 函数返回时，TcpStream 自动 drop（连接关闭）。
/// 调用方（transport.rs）在此之后 drop Semaphore permit，归还并发槽位。
pub async fn handle_connection(
    stream:   TcpStream,
    pipeline: Arc<Next>,      // 编译好的中间件链
    state:    Arc<AppState>,
    limits:   ServerLimits,
    cancel:   CancellationToken,
) {
    let (mut reader, writer) = stream.into_split();
    // BufWriter：把多次小 write 合并为一次系统调用（等同 PipeWriter 批量 flush）
    let mut writer = BufWriter::with_capacity(16 * 1024, writer);

    // ★ 连接级持久缓冲区
    //   生命周期 = 连接生命周期（对应 Kestrel Pipe 的生命周期）
    //   TCP 粘包时，上一请求结束后 buf 里可能还有下一请求的起始字节，不会丢失。
    let mut buf = BytesMut::with_capacity(4096);

    loop {
        // ── 读取并解析下一个请求 ─────────────────────────────────────
        // tokio::select! 同时等待两件事：
        //   1. 读取请求（可能阻塞，等客户端发送数据）
        //   2. CancellationToken 触发（SIGTERM 优雅关闭）
        let raw_req = tokio::select! {
            biased; // 优先检查 cancel（避免已收到 SIGTERM 后还处理新请求）

            _ = cancel.cancelled() => {
                // 优雅关闭：当前是空闲 keep-alive 等待，直接关闭连接
                break;
            }

            result = read_request(&mut reader, &mut buf) => {
                match result {
                    Ok(req) => req,
                    Err(ServerError::ConnectionClosed) => break,
                    Err(ServerError::RequestHeadersTooLarge) => {
                        let _ = writer.write_all(
                            b"HTTP/1.1 431 Request Header Fields Too Large\r\nconnection: close\r\ncontent-length: 0\r\n\r\n"
                        ).await;
                        let _ = writer.flush().await;
                        break;
                    }
                    Err(e) => {
                        tracing::debug!("请求解析错误: {e}");
                        break;
                    }
                }
            }
        };

        // body 大小检查（对应 KestrelServerLimits.MaxRequestBodySize）
        if raw_req.body.len() > limits.max_request_body_size {
            let _ = writer.write_all(
                b"HTTP/1.1 413 Payload Too Large\r\nconnection: close\r\ncontent-length: 0\r\n\r\n"
            ).await;
            let _ = writer.flush().await;
            break;
        }

        // 判断客户端是否希望 keep-alive（HTTP/1.1 默认 keep-alive）
        let client_keep_alive = raw_req.version >= 1
            && raw_req.headers.get("connection")
                .map(|v| !v.eq_ignore_ascii_case("close"))
                .unwrap_or(true);

        let http_version = raw_req.version;

        // ── 运行中间件管道（对应 application.ProcessRequestAsync）──────
        let state_any: Arc<dyn std::any::Any + Send + Sync> = state.clone();
        let ctx  = HttpContext::new(raw_req, state_any);
        let resp = pipeline(ctx).await;

        // ── 序列化响应为 HTTP/1.1 字节流 ────────────────────────────────
        let (status, headers, body) = match resp {
            Ok(r)  => (r.status, r.headers, r.body),
            Err(e) => {
                let code  = e.status_code();
                let bytes = serde_json::to_vec(
                    &serde_json::json!({ "error": e.to_string() })
                ).unwrap_or_default();
                let hdrs = vec![(
                    "content-type".to_string(),
                    "application/json; charset=utf-8".to_string(),
                )];
                (code, hdrs, bytes::Bytes::from(bytes))
            }
        };

        let serialized = serialize_response(status, &headers, &body, http_version, client_keep_alive);

        // ── 写入 TCP socket ────────────────────────────────────────────
        if writer.write_all(&serialized.bytes).await.is_err() { break; }
        if writer.flush().await.is_err()                       { break; }

        // keep-alive 判断
        if !serialized.keep_alive || cancel.is_cancelled() { break; }
        // 继续 loop：等待同一连接上的下一个请求
    }
    // 函数返回：stream 自动 drop（TCP 连接关闭）
}
```

---

### 2.2 `transport.rs`

```rust
//! TCP 监听 + 连接生命周期管理
//! 对应 Kestrel 的 ConnectionManager + IConnectionListenerFactory

use std::sync::Arc;

use tokio::{net::TcpListener, sync::Semaphore};
use tokio_util::sync::CancellationToken;

use mo_server_core::middleware::Next;
use crate::{connection::handle_connection, limits::ServerLimits, state::AppState};

/// 启动 TCP 监听循环（对应 Kestrel KestrelServer.StartAsync）
///
/// # 并发控制
/// Semaphore 限制最大并发连接数（对应 KestrelServerLimits.MaxConcurrentConnections）：
/// - accept 前 acquire_owned() 取 permit（超限则阻塞，不丢弃连接）
/// - 连接关闭时 drop(permit)，信号量 +1
///
/// # 优雅关闭（对应 StopAsync + ConnectionManager.CloseAllConnectionsAsync）
/// 1. cancel.cancelled() → 停止 accept
/// 2. acquire_many(max)  → 等待所有 permit 归还（= 所有连接已关闭）
pub async fn run_transport(
    listener:  TcpListener,
    pipeline:  Arc<Next>,
    state:     Arc<AppState>,
    limits:    ServerLimits,
    cancel:    CancellationToken,
) -> anyhow::Result<()> {
    let max       = limits.max_concurrent_connections;
    let semaphore = Arc::new(Semaphore::new(max));

    tracing::info!("TCP 监听开始");

    loop {
        tokio::select! {
            biased;

            _ = cancel.cancelled() => {
                tracing::info!("收到关闭信号，停止 accept");
                break;
            }

            result = listener.accept() => {
                let (stream, addr) = result?;
                tracing::debug!(?addr, "新 TCP 连接");

                // acquire_owned：返回 'static 的 OwnedSemaphorePermit，可 move 进 spawn
                let permit = Arc::clone(&semaphore)
                    .acquire_owned()
                    .await
                    .expect("semaphore 已关闭");

                tokio::spawn({
                    let pipeline = Arc::clone(&pipeline);
                    let state    = Arc::clone(&state);
                    let limits   = limits.clone();
                    let cancel   = cancel.clone();
                    async move {
                        handle_connection(stream, pipeline, state, limits, cancel).await;
                        drop(permit); // RAII：连接关闭后释放槽位
                    }
                });
            }
        }
    }

    // 优雅关闭：等待所有 in-flight 连接关闭
    tracing::info!("等待 {max} 个连接全部完成…");
    let _ = semaphore.acquire_many(max as u32).await;
    tracing::info!("服务器优雅退出");

    Ok(())
}

// ── 单元测试 ──────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use std::{sync::Arc, time::Duration};
    use tokio::sync::Semaphore;

    #[tokio::test]
    async fn test_semaphore_limits_connections() {
        let sem = Arc::new(Semaphore::new(2));

        let p1 = Arc::clone(&sem).acquire_owned().await.unwrap();
        let p2 = Arc::clone(&sem).acquire_owned().await.unwrap();

        let sem_clone = Arc::clone(&sem);
        let waiter = tokio::spawn(async move {
            let _p3 = sem_clone.acquire_owned().await.unwrap();
        });

        drop(p1);
        tokio::time::sleep(Duration::from_millis(10)).await;
        assert!(!waiter.is_finished() || waiter.await.is_ok());
        drop(p2);
    }

    #[tokio::test]
    async fn test_permit_released_on_drop() {
        let sem = Arc::new(Semaphore::new(1));
        {
            let _p = Arc::clone(&sem).acquire_owned().await.unwrap();
            assert_eq!(sem.available_permits(), 0);
        }
        assert_eq!(sem.available_permits(), 1);
    }
}
```

---

### 2.3 `server.rs`

```rust
use std::sync::Arc;

use tokio::net::TcpListener;
use tokio_util::sync::CancellationToken;

use mo_server_core::{
    middleware::{FnMiddleware, Next, Pipeline},
    router::{BoxHandler, Router},
};

use crate::{limits::ServerLimits, state::AppState, transport::run_transport};

pub struct ServerBuilder {
    addr:        String,
    limits:      ServerLimits,
    state:       Arc<AppState>,
    middlewares: Vec<Box<dyn mo_server_core::middleware::Middleware>>,
    routes:      Vec<(String, String, BoxHandler)>,
}

impl ServerBuilder {
    pub fn new(addr: impl Into<String>, state: Arc<AppState>) -> Self {
        Self { addr: addr.into(), limits: ServerLimits::default(), state,
               middlewares: Vec::new(), routes: Vec::new() }
    }

    pub fn with_limits(mut self, limits: ServerLimits) -> Self {
        self.limits = limits; self
    }

    pub fn use_middleware<F, Fut>(mut self, f: F) -> Self
    where
        F: Fn(mo_server_core::context::HttpContext, Next) -> Fut + Send + Sync + 'static,
        Fut: std::future::Future<
            Output = Result<mo_server_core::response::Response, mo_server_core::error::ServerError>
        > + Send + 'static,
    {
        self.middlewares.push(Box::new(FnMiddleware(f)));
        self
    }

    pub fn map_get(mut self, path: &str, h: BoxHandler) -> Self {
        self.routes.push(("GET".into(), path.into(), h)); self
    }
    pub fn map_post(mut self, path: &str, h: BoxHandler) -> Self {
        self.routes.push(("POST".into(), path.into(), h)); self
    }
    pub fn map_put(mut self, path: &str, h: BoxHandler) -> Self {
        self.routes.push(("PUT".into(), path.into(), h)); self
    }
    pub fn map_delete(mut self, path: &str, h: BoxHandler) -> Self {
        self.routes.push(("DELETE".into(), path.into(), h)); self
    }

    pub async fn run(self) -> anyhow::Result<()> {
        // 构建路由
        let mut router = Router::new();
        for (method, path, handler) in self.routes {
            router.insert(&method, &path, handler)?;
        }

        // 编译中间件链（Router 作为 terminal）
        let terminal = Arc::new(router).into_next();
        let mut pipeline = Pipeline::new();
        for mw in self.middlewares { pipeline.add_boxed(mw); }
        let compiled = Arc::new(pipeline.build(terminal));

        // 绑定端口
        let listener = TcpListener::bind(&self.addr).await?;
        tracing::info!(addr = %self.addr, "服务器启动");

        // SIGTERM / Ctrl-C → CancellationToken
        let cancel = CancellationToken::new();
        {
            let cancel = cancel.clone();
            tokio::spawn(async move {
                #[cfg(unix)] {
                    use tokio::signal::unix::{signal, SignalKind};
                    let mut sig = signal(SignalKind::terminate()).unwrap();
                    tokio::select! {
                        _ = sig.recv()              => {}
                        _ = tokio::signal::ctrl_c() => {}
                    }
                }
                #[cfg(not(unix))] {
                    let _ = tokio::signal::ctrl_c().await;
                }
                tracing::info!("收到关闭信号");
                cancel.cancel();
            });
        }

        run_transport(listener, compiled, self.state, self.limits, cancel).await
    }
}
```

---

### 2.4 `main.rs`

```rust
use std::sync::Arc;

use anyhow::Context;
use sqlx::postgres::PgPoolOptions;

mod config;
mod connection;
mod db;
mod handlers;
mod limits;
mod middleware;
mod server;
mod state;
mod transport;

use config::AppConfig;
use server::ServerBuilder;
use state::AppState;
use mo_server_core::router::BoxHandler;

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // 结构化日志（对应 C# builder.Logging.AddJsonConsole）
    tracing_subscriber::fmt()
        .with_env_filter(tracing_subscriber::EnvFilter::from_default_env())
        .json()
        .init();

    let config = AppConfig::load().context("配置加载失败")?;
    let db     = PgPoolOptions::new()
        .max_connections(20)
        .connect(&config.database_url)
        .await
        .context("数据库连接失败")?;

    sqlx::migrate!("./migrations").run(&db).await?;

    let state = Arc::new(AppState::new(db, config.clone()));

    // 把 handler async fn 包装为 BoxHandler
    fn h<F, Fut>(f: F) -> BoxHandler
    where
        F:   Fn(mo_server_core::context::HttpContext) -> Fut + Send + Sync + 'static,
        Fut: std::future::Future<
            Output = Result<mo_server_core::response::Response, mo_server_core::error::ServerError>
        > + Send + 'static,
    {
        Arc::new(move |ctx| Box::pin(f(ctx)))
    }

    ServerBuilder::new(format!("0.0.0.0:{}", config.port), state)
        .use_middleware(middleware::tracing_mw::tracing_mw)
        .use_middleware(middleware::auth_mw::auth_mw)
        .map_get("/health",          h(handlers::health::handle))
        .map_get("/services",        h(handlers::services::list))
        .map_post("/services",       h(handlers::services::create))
        .map_get("/services/:id",    h(handlers::services::get))
        .map_delete("/services/:id", h(handlers::services::delete))
        .run()
        .await
}
```

---

## 三、【Dev】C# vs Rust：关键路径全对比

```
Kestrel (C#)                              Restrel (Rust)
──────────────────────────────────────    ──────────────────────────────────────
PipeReader.ReadAsync()                    AsyncReadExt::read_buf(&mut buf)
ReadOnlySequence<byte>                    BytesMut
llhttp_execute() P/Invoke                 httparse::Request::parse(&buf)
Status::Partial → AdvanceTo(start,end)    Status::Partial → 继续 read_buf
Status::Complete(n)→AdvanceTo(consumed)   Status::Complete(n) → buf.split_to(n)
Http1MessageBody.ConsumeAsync()           while buf.len() < len { read_buf }
PipeWriter + FlushAsync()                 BufWriter::write_all + flush()
RequestDelegate 委托链                    Arc<dyn Fn(ctx) -> BoxFuture> fold 链
HttpContext.Items["Claims"]               ctx.extensions.get::<Claims>()
IServiceProvider.GetService<T>()          ctx.state_arc::<AppState>()
ConnectionManager.MaxConnections          Arc<Semaphore>::acquire_owned()
IHostedService.StopAsync()               CancellationToken::cancel()
Task.WhenAll(in-flight)                  semaphore.acquire_many(max).await
```

---

## 四、【Dev】mo-server：应用层业务代码

### 4.1 `limits.rs`

```rust
//! 服务器限制配置
//! 对应 Kestrel 的 KestrelServerLimits

/// 服务器资源限制（对应 C# KestrelServerOptions.Limits）
#[derive(Debug, Clone)]
pub struct ServerLimits {
    /// 最大并发连接数（对应 KestrelServerLimits.MaxConcurrentConnections）
    /// 超出时 accept 阻塞，不丢弃连接（Semaphore.WaitAsync）
    pub max_concurrent_connections: usize,

    /// 请求体最大字节数（对应 KestrelServerLimits.MaxRequestBodySize）
    /// 超出返回 413 Payload Too Large
    pub max_request_body_size: usize,
}

impl Default for ServerLimits {
    fn default() -> Self {
        Self {
            max_concurrent_connections: 1_000,
            max_request_body_size:      4 * 1024 * 1024, // 4 MB
        }
    }
}
```

---

### 4.2 `config.rs`

```rust
//! 分层配置管理（对应 C# IConfiguration + appsettings.json + 环境变量）
//!
//! 优先级（高 → 低）：环境变量 > config.toml > 默认值

use figment::{
    providers::{Env, Format, Toml},
    Figment,
};
use serde::Deserialize;

/// 应用配置（对应 C# IOptions<AppSettings>）
#[derive(Debug, Clone, Deserialize)]
pub struct AppConfig {
    /// PostgreSQL 连接串（对应 ConnectionStrings:DefaultConnection）
    pub database_url: String,

    /// 监听端口（对应 ASPNETCORE_URLS 中的端口）
    #[serde(default = "default_port")]
    pub port: u16,

    /// JWT 签名密钥（对应 Jwt:Key appsettings）
    pub jwt_secret: String,
}

fn default_port() -> u16 { 8080 }

/// .env.example 内容：
/// ```env
/// MO_DATABASE_URL=postgres://mo:mo@localhost:5432/mo_db
/// MO_PORT=8080
/// MO_JWT_SECRET=change-me-in-production
/// ```
impl AppConfig {
    /// 加载配置（对应 C# builder.Configuration.Build()）
    ///
    /// 读取顺序：
    /// 1. `config.toml`（可选，本地开发）
    /// 2. `MO_*` 环境变量（生产部署）
    pub fn load() -> anyhow::Result<Self> {
        let config = Figment::new()
            .merge(Toml::file("config.toml"))       // 可选本地配置
            .merge(Env::prefixed("MO_"))            // 环境变量覆盖
            .extract::<AppConfig>()?;
        Ok(config)
    }
}
```

---

### 4.3 `state.rs`

```rust
//! 应用共享状态（对应 C# IServiceProvider 的单例注入）
//!
//! 通过 HttpContext::state_arc::<AppState>() 在 handler 中访问

use sqlx::PgPool;

use crate::config::AppConfig;

/// 应用全局状态（Arc<AppState> 在所有连接 / 任务间共享）
///
/// 对应 C# 中注入的 scoped/singleton 服务（PgPool ~ DbContext Pool，config ~ IOptions）
pub struct AppState {
    /// 数据库连接池（对应 C# DbContextPool / NpgsqlDataSource）
    pub db: PgPool,

    /// 应用配置快照（只读，对应 IOptionsSnapshot<AppConfig> 的值快照）
    pub config: AppConfig,
}

impl AppState {
    pub fn new(db: PgPool, config: AppConfig) -> Self {
        Self { db, config }
    }
}
```

---

### 4.4 `db/mod.rs`

```rust
// src/db/mod.rs
pub mod service_repo;
```

---

### 4.5 `db/service_repo.rs`

```rust
//! 服务实体的数据库访问层（对应 C# IRepository<Service> / DbContext）
//!
//! 使用 sqlx 的编译期 query 验证（开发时需 DATABASE_URL 或 sqlx prepare）

use chrono::{DateTime, Utc};
use sqlx::PgPool;
use uuid::Uuid;

/// 服务实体（数据库行 → Rust 结构体，对应 C# Entity / DTO）
#[derive(Debug, serde::Serialize, serde::Deserialize, sqlx::FromRow)]
pub struct ServiceRow {
    pub id:         Uuid,
    pub name:       String,
    pub endpoint:   String,
    pub created_at: DateTime<Utc>,
}

/// 查询所有服务（对应 C# context.Services.OrderByDescending(s => s.CreatedAt).ToListAsync()）
pub async fn list_services(db: &PgPool) -> Result<Vec<ServiceRow>, sqlx::Error> {
    sqlx::query_as!(
        ServiceRow,
        "SELECT id, name, endpoint, created_at FROM services ORDER BY created_at DESC"
    )
    .fetch_all(db)
    .await
}

/// 按 ID 查询服务（对应 C# context.Services.FindAsync(id)）
pub async fn get_service(db: &PgPool, id: Uuid) -> Result<Option<ServiceRow>, sqlx::Error> {
    sqlx::query_as!(
        ServiceRow,
        "SELECT id, name, endpoint, created_at FROM services WHERE id = $1",
        id
    )
    .fetch_optional(db)
    .await
}

/// 创建服务（对应 C# context.Services.Add(entity); context.SaveChangesAsync()）
pub async fn create_service(
    db:       &PgPool,
    name:     &str,
    endpoint: &str,
) -> Result<ServiceRow, sqlx::Error> {
    sqlx::query_as!(
        ServiceRow,
        r#"
        INSERT INTO services (id, name, endpoint)
        VALUES (gen_random_uuid(), $1, $2)
        RETURNING id, name, endpoint, created_at
        "#,
        name,
        endpoint
    )
    .fetch_one(db)
    .await
}

/// 删除服务（对应 C# context.Services.Remove(entity); context.SaveChangesAsync()）
///
/// 返回 `true` 表示删除成功，`false` 表示 ID 不存在
pub async fn delete_service(db: &PgPool, id: Uuid) -> Result<bool, sqlx::Error> {
    let rows = sqlx::query!("DELETE FROM services WHERE id = $1", id)
        .execute(db)
        .await?
        .rows_affected();
    Ok(rows > 0)
}
```

---

### 4.6 `middleware/mod.rs`

```rust
// src/middleware/mod.rs
pub mod auth_mw;
pub mod tracing_mw;
```

---

### 4.7 `middleware/tracing_mw.rs`

```rust
//! 请求日志中间件（对应 ASP.NET Core UseHttpLogging）
//!
//! 注入 tracing span，记录：method, path, status, latency_ms, request_id

use std::time::Instant;

use mo_server_core::{
    context::HttpContext,
    error::ServerError,
    middleware::Next,
    response::Response,
};
use uuid::Uuid;

/// 链路追踪中间件
///
/// 在请求开始时创建 span，在请求结束后记录结果。
/// 对应 C#:
/// ```csharp
/// app.Use(async (ctx, next) => {
///     var sw = Stopwatch.StartNew();
///     await next();
///     logger.LogInformation("{Method} {Path} → {Status} in {Ms}ms",
///         ctx.Request.Method, ctx.Request.Path, ctx.Response.StatusCode, sw.ElapsedMilliseconds);
/// });
/// ```
pub async fn tracing_mw(ctx: HttpContext, next: Next) -> Result<Response, ServerError> {
    let method     = ctx.method().to_string();
    let path       = ctx.path().to_string();
    let request_id = Uuid::new_v4().to_string();
    let start      = Instant::now();

    let span = tracing::info_span!(
        "http_request",
        method   = %method,
        path     = %path,
        req_id   = %request_id,
    );
    let _enter = span.enter();

    tracing::debug!("请求开始");

    let result = next(ctx).await;
    let latency_ms = start.elapsed().as_millis();

    match &result {
        Ok(resp) => tracing::info!(
            status     = resp.status,
            latency_ms = %latency_ms,
            "请求完成"
        ),
        Err(e) => tracing::error!(
            error      = %e,
            latency_ms = %latency_ms,
            "请求失败"
        ),
    }

    result
}

// ── 单元测试 ──────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    use std::{collections::HashMap, sync::Arc};
    use bytes::Bytes;
    use mo_server_core::{context::HttpContext, parser::RawRequest, response::no_content};

    fn ctx() -> HttpContext {
        let raw = RawRequest {
            method: "GET".into(), path: "/test".into(),
            version: 1, headers: HashMap::new(), body: Bytes::new(),
        };
        HttpContext::new(raw, Arc::new(()))
    }

    #[tokio::test]
    async fn test_tracing_mw_records_latency() {
        let terminal: Next = Arc::new(|_| Box::pin(async { Ok(no_content()) }));
        let result = tracing_mw(ctx(), terminal).await;
        assert!(result.is_ok());
        assert_eq!(result.unwrap().status, 204);
    }

    #[tokio::test]
    async fn test_tracing_mw_propagates_error() {
        let terminal: Next = Arc::new(|_| {
            Box::pin(async { Err(ServerError::NotFound("/missing".into())) })
        });
        let result = tracing_mw(ctx(), terminal).await;
        assert!(result.is_err());
    }
}
```

---

### 4.8 `middleware/auth_mw.rs`

```rust
//! JWT 鉴权中间件（对应 ASP.NET Core UseAuthentication + UseAuthorization）
//!
//! 读取 Authorization: Bearer <token>，验证 JWT，将 Claims 注入 HttpContext.extensions

use jsonwebtoken::{decode, encode, DecodingKey, EncodingKey, Header, Validation};
use serde::{Deserialize, Serialize};

use mo_server_core::{
    context::HttpContext,
    error::ServerError,
    middleware::Next,
    response::Response,
};

use crate::state::AppState;

/// JWT payload（对应 C# ClaimsPrincipal 中的 Claims）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Claims {
    /// Subject：用户 ID（对应 C# ClaimTypes.NameIdentifier）
    pub sub: String,
    /// Expiry：Unix 时间戳（对应 C# ClaimTypes.Expiration 隐含在 JwtSecurityToken.ValidTo）
    pub exp: u64,
}

/// JWT 鉴权中间件
///
/// 白名单路由（`/health`）放行；其余路由验证 Bearer token。
/// 验证成功后将 `Claims` 注入 `ctx.extensions`，后续 handler 可通过
/// `ctx.extensions.get::<Claims>()` 取回。
///
/// 对应 C#:
/// ```csharp
/// [Authorize]
/// public async Task<IActionResult> ListServices() { ... }
/// ```
pub async fn auth_mw(mut ctx: HttpContext, next: Next) -> Result<Response, ServerError> {
    // 白名单：健康检查不需要鉴权
    if ctx.path() == "/health" {
        return next(ctx).await;
    }

    let state = ctx
        .state_arc::<AppState>()
        .ok_or_else(|| ServerError::Unauthorized("state unavailable".into()))?;

    let header_val = ctx
        .header("authorization")
        .ok_or_else(|| ServerError::Unauthorized("missing Authorization header".into()))?
        .to_string();

    let token = header_val
        .strip_prefix("Bearer ")
        .ok_or_else(|| ServerError::Unauthorized("Authorization value must start with 'Bearer '".into()))?;

    let key        = DecodingKey::from_secret(state.config.jwt_secret.as_bytes());
    let validation = Validation::default(); // 默认：验证 exp, iat, alg

    let token_data = decode::<Claims>(token, &key, &validation)
        .map_err(|e| ServerError::Unauthorized(format!("invalid token: {e}")))?;

    // 将 Claims 注入 context extensions（对应 C# HttpContext.User）
    ctx.extensions.insert(token_data.claims);

    next(ctx).await
}

/// 测试辅助：生成合法的测试用 JWT
///
/// # 注意
/// 仅用于测试，不要在生产代码中硬编码密钥。
pub fn make_test_token(secret: &str, sub: &str) -> String {
    let claims = Claims {
        sub: sub.to_string(),
        exp: u64::MAX, // 永不过期（测试专用）
    };
    encode(
        &Header::default(),
        &claims,
        &EncodingKey::from_secret(secret.as_bytes()),
    )
    .expect("test token generation failed")
}

// ── 单元测试 ──────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    use std::{collections::HashMap, sync::Arc};
    use bytes::Bytes;
    use mo_server_core::{context::HttpContext, parser::RawRequest, response::ok};

    fn ctx_with_header(path: &str, header: Option<(&str, &str)>) -> HttpContext {
        let mut headers = HashMap::new();
        if let Some((k, v)) = header {
            headers.insert(k.to_string(), v.to_string());
        }
        let raw = RawRequest {
            method: "GET".into(), path: path.into(),
            version: 1, headers, body: Bytes::new(),
        };
        // 构造一个内嵌 jwt_secret 的最小 AppState
        use crate::{config::AppConfig, state::AppState};
        let config = AppConfig {
            database_url: "unused".into(),
            port: 8080,
            jwt_secret: "test_secret".into(),
        };
        // 测试中不需要真实 PgPool，借助 Any 存储 config
        // 这里用 Arc<AppState> -> Arc<dyn Any>
        let state: Arc<dyn std::any::Any + Send + Sync> =
            Arc::new(AppState { db: unsafe { std::mem::zeroed() }, config });
        //        ^^^^ 仅测试目的，PgPool::zeroed() 不会被调用
        // 真实集成测试通过 testcontainers 提供真实 PgPool
        HttpContext::new(raw, state)
    }

    #[tokio::test]
    async fn test_auth_mw_missing_header() {
        let ctx     = ctx_with_header("/services", None);
        let next: Next = Arc::new(|_| Box::pin(async { Ok(ok(&serde_json::json!({}))) }));
        let err = auth_mw(ctx, next).await.unwrap_err();
        assert!(matches!(err, ServerError::Unauthorized(_)));
    }

    #[tokio::test]
    async fn test_auth_mw_invalid_token() {
        let ctx = ctx_with_header("/services", Some(("authorization", "Bearer not-a-jwt")));
        let next: Next = Arc::new(|_| Box::pin(async { Ok(ok(&serde_json::json!({}))) }));
        let err = auth_mw(ctx, next).await.unwrap_err();
        assert!(matches!(err, ServerError::Unauthorized(_)));
    }

    #[tokio::test]
    async fn test_auth_mw_valid_token_injects_claims() {
        let token = format!("Bearer {}", make_test_token("test_secret", "user_42"));
        let ctx   = ctx_with_header("/services", Some(("authorization", &token)));
        let next: Next = Arc::new(|ctx: HttpContext| {
            Box::pin(async move {
                let claims = ctx.extensions.get::<Claims>().unwrap();
                assert_eq!(claims.sub, "user_42");
                Ok(ok(&serde_json::json!({})))
            })
        });
        auth_mw(ctx, next).await.unwrap();
    }

    #[tokio::test]
    async fn test_auth_mw_health_bypasses_auth() {
        let ctx    = ctx_with_header("/health", None); // 无 Authorization header
        let next: Next = Arc::new(|_| Box::pin(async { Ok(ok(&serde_json::json!({}))) }));
        // /health 不验证 token，直接放行
        assert!(auth_mw(ctx, next).await.is_ok());
    }

    #[tokio::test]
    async fn test_auth_mw_expired_token() {
        // exp = 1（1970-01-01 00:00:01 UTC，已过期）
        let claims = Claims { sub: "u".into(), exp: 1 };
        let token = encode(
            &Header::default(),
            &claims,
            &EncodingKey::from_secret(b"test_secret"),
        )
        .unwrap();
        let header = format!("Bearer {token}");
        let ctx    = ctx_with_header("/services", Some(("authorization", &header)));
        let next: Next = Arc::new(|_| Box::pin(async { Ok(ok(&serde_json::json!({}))) }));
        let err = auth_mw(ctx, next).await.unwrap_err();
        assert!(matches!(err, ServerError::Unauthorized(_)));
    }
}
```

---

### 4.9 `handlers/mod.rs`

```rust
// src/handlers/mod.rs
pub mod health;
pub mod services;
```

---

### 4.10 `handlers/health.rs`

```rust
//! 健康检查 handler（对应 C# IHealthCheck）
//!
//! GET /health
//! 检查数据库连通性，返回 {"status":"ok"} 或 500

use mo_server_core::{
    context::HttpContext,
    error::ServerError,
    response::{self, Response},
};

/// 对应 C# HealthCheckService.CheckHealthAsync()
pub async fn handle(ctx: HttpContext) -> Result<Response, ServerError> {
    let state = ctx
        .state_arc::<crate::state::AppState>()
        .expect("AppState not in context");

    // 轻量探活查询（对应 C# NpgsqlConnection.OpenAsync()）
    sqlx::query!("SELECT 1 AS x")
        .fetch_one(&state.db)
        .await
        .map_err(ServerError::Database)?;

    Ok(response::ok(&serde_json::json!({
        "status": "ok",
        "db":     "connected"
    })))
}
```

---

### 4.11 `handlers/services.rs`

```rust
//! Services CRUD handler（对应 C# ApiController + [HttpGet/Post/Delete]）
//!
//! GET    /services      → list_services
//! POST   /services      → create_service（body: CreateRequest）
//! GET    /services/:id  → get_service
//! DELETE /services/:id  → delete_service

use mo_server_core::{
    context::HttpContext,
    error::ServerError,
    response::{self, Response},
};
use serde::Deserialize;
use uuid::Uuid;

use crate::{db::service_repo, state::AppState};

/// POST /services 请求体（对应 C# [FromBody] CreateServiceRequest）
#[derive(Debug, Deserialize)]
struct CreateRequest {
    name:     String,
    endpoint: String,
}

/// GET /services — 查询全部服务列表
pub async fn list(ctx: HttpContext) -> Result<Response, ServerError> {
    let state    = state(&ctx);
    let services = service_repo::list_services(&state.db).await?;
    Ok(response::ok(&services))
}

/// POST /services — 创建新服务
pub async fn create(ctx: HttpContext) -> Result<Response, ServerError> {
    let state = state(&ctx);

    let req: CreateRequest = serde_json::from_slice(ctx.body())
        .map_err(|e| ServerError::BadRequest(format!("JSON 格式错误：{e}")))?;

    // 输入验证（对应 C# [Required] DataAnnotations）
    if req.name.trim().is_empty() {
        return Err(ServerError::BadRequest("name 不能为空".into()));
    }
    if req.endpoint.trim().is_empty() {
        return Err(ServerError::BadRequest("endpoint 不能为空".into()));
    }

    let service = service_repo::create_service(&state.db, &req.name, &req.endpoint).await?;
    Ok(response::created(&service))
}

/// GET /services/:id — 按 ID 查询服务
pub async fn get(ctx: HttpContext) -> Result<Response, ServerError> {
    let state = state(&ctx);
    let id    = parse_id(&ctx)?;

    match service_repo::get_service(&state.db, id).await? {
        Some(s) => Ok(response::ok(&s)),
        None    => Err(ServerError::NotFound(format!("服务 {id} 不存在"))),
    }
}

/// DELETE /services/:id — 删除服务
pub async fn delete(ctx: HttpContext) -> Result<Response, ServerError> {
    let state = state(&ctx);
    let id    = parse_id(&ctx)?;

    if service_repo::delete_service(&state.db, id).await? {
        Ok(response::no_content())
    } else {
        Err(ServerError::NotFound(format!("服务 {id} 不存在")))
    }
}

// ── 私有辅助 ──────────────────────────────────────────────────────────────────

fn state(ctx: &HttpContext) -> std::sync::Arc<AppState> {
    ctx.state_arc::<AppState>().expect("AppState not in context")
}

fn parse_id(ctx: &HttpContext) -> Result<Uuid, ServerError> {
    ctx.param("id")
        .ok_or_else(|| ServerError::BadRequest("路由参数 id 缺失".into()))?
        .parse::<Uuid>()
        .map_err(|_| ServerError::BadRequest("id 不是合法 UUID".into()))
}
```

---

## 五、数据库迁移脚本

```sql
-- migrations/20240301000001_create_services.sql
-- 服务注册表（对应 C# DbContext + Fluent API 配置的 Services 实体）

CREATE TABLE IF NOT EXISTS services (
    id          UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
    name        VARCHAR(255) NOT NULL,
    endpoint    TEXT         NOT NULL,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);

-- 按名称唯一（防重复注册）
CREATE UNIQUE INDEX IF NOT EXISTS idx_services_name ON services (name);

-- 按创建时间降序（list_services 查询用）
CREATE INDEX IF NOT EXISTS idx_services_created_at ON services (created_at DESC);

-- 注释（对应 C# EntityTypeConfiguration 中的 .HasComment()）
COMMENT ON TABLE  services              IS 'Mo 服务注册表';
COMMENT ON COLUMN services.id          IS '服务唯一标识（UUIDv4）';
COMMENT ON COLUMN services.name        IS '服务名称（全局唯一）';
COMMENT ON COLUMN services.endpoint    IS '服务 HTTP 端点（含 scheme + host + port）';
COMMENT ON COLUMN services.created_at  IS '注册时间（UTC）';
```

---

## 六、单元测试补充（mo-server-core）

### 6.1 `parser.rs` 新增：Partial 状态机 + TCP 粘包测试

```rust
// 追加到 parser.rs 末尾的 #[cfg(test)] 块

#[cfg(test)]
mod tests {
    use super::*;
    use bytes::BytesMut;
    use tokio::io::duplex;

    /// 验证 Partial 状态机：多次 read 才能凑齐完整 headers
    ///
    /// 等价场景：Kestrel 的 PipeReader.ReadAsync() 返回 NeedMoreData 多次
    #[tokio::test]
    async fn test_partial_request_eventually_parses() {
        // duplex 返回一对内存管道（等同 MemoryStream，无真实 socket）
        let (mut server_side, mut client_side) = duplex(128);

        // 模拟"慢客户端"：分 3 次写入一个完整请求
        let task = tokio::spawn(async move {
            use tokio::io::AsyncWriteExt;
            client_side.write_all(b"GET /health HTTP").await.unwrap();
            tokio::time::sleep(std::time::Duration::from_millis(5)).await;
            client_side.write_all(b"/1.1\r\nhost: localhost").await.unwrap();
            tokio::time::sleep(std::time::Duration::from_millis(5)).await;
            client_side.write_all(b"\r\n\r\n").await.unwrap();
        });

        let mut buf = BytesMut::new();
        let req = read_request(&mut server_side, &mut buf).await.unwrap();

        task.await.unwrap();

        assert_eq!(req.method,  "GET");
        assert_eq!(req.path,    "/health");
        assert_eq!(req.version, 1);
    }

    /// 验证 TCP 粘包：第一个请求结束后 buf 里有第二个请求的起始字节
    ///
    /// 等价场景：Kestrel keep-alive 时 PipeReader 不 Reset，保留余量数据
    #[tokio::test]
    async fn test_buffer_persists_across_requests() {
        let (mut server_side, mut client_side) = duplex(256);

        // 写入两个连续请求（不含 body）
        let req1 = b"GET /a HTTP/1.1\r\nhost: x\r\n\r\n";
        let req2 = b"GET /b HTTP/1.1\r\nhost: x\r\n\r\n";

        tokio::spawn(async move {
            use tokio::io::AsyncWriteExt;
            // 一次性写入两个请求（模拟 TCP 粘包）
            client_side.write_all(req1).await.unwrap();
            client_side.write_all(req2).await.unwrap();
        });

        let mut buf = BytesMut::new();

        // 第一次解析
        let r1 = read_request(&mut server_side, &mut buf).await.unwrap();
        assert_eq!(r1.path, "/a");

        // buf 里应该残留第二个请求的字节（粘包）
        // 第二次解析直接命中 buf，不用再从 socket 读
        let r2 = read_request(&mut server_side, &mut buf).await.unwrap();
        assert_eq!(r2.path, "/b");
    }

    /// 验证 431 头部超大检测
    #[tokio::test]
    async fn test_headers_too_large_returns_err() {
        let (mut server_side, mut client_side) = duplex(MAX_HEADER_BYTES + 1024);

        tokio::spawn(async move {
            use tokio::io::AsyncWriteExt;
            // 写入超大 header（超过 MAX_HEADER_BYTES）
            let big_value = "x".repeat(MAX_HEADER_BYTES);
            let req = format!("GET / HTTP/1.1\r\nx-big: {big_value}\r\n\r\n");
            client_side.write_all(req.as_bytes()).await.ok();
        });

        let mut buf = BytesMut::new();
        let err = read_request(&mut server_side, &mut buf).await.unwrap_err();
        assert!(matches!(err, ServerError::RequestHeadersTooLarge));
    }
}
```

### 6.2 `context.rs` 新增测试

```rust
// 追加到 context.rs 末尾

#[cfg(test)]
mod tests {
    use super::*;
    use std::{collections::HashMap, sync::Arc};
    use bytes::Bytes;
    use crate::parser::RawRequest;

    fn ctx(path: &str) -> HttpContext {
        let raw = RawRequest {
            method: "GET".into(), path: path.into(),
            version: 1, headers: HashMap::new(), body: Bytes::new(),
        };
        HttpContext::new(raw, Arc::new(()))
    }

    #[test]
    fn test_extensions_type_safety() {
        let mut exts = Extensions::default();
        exts.insert(42u32);
        exts.insert("hello");

        assert_eq!(*exts.get::<u32>().unwrap(), 42u32);
        assert_eq!(*exts.get::<&str>().unwrap(), "hello");
        assert!(exts.get::<u64>().is_none());  // 未插入的类型返回 None
    }

    #[test]
    fn test_query_param_extraction() {
        let c = ctx("/search?q=rust&page=2&size=10");
        assert_eq!(c.query("q"),    Some("rust"));
        assert_eq!(c.query("page"), Some("2"));
        assert_eq!(c.query("size"), Some("10"));
        assert_eq!(c.query("miss"), None);
    }

    #[test]
    fn test_path_strips_query_string() {
        let c = ctx("/services?page=1");
        assert_eq!(c.path(), "/services");
        assert_eq!(c.uri(), "/services?page=1");
    }

    #[test]
    fn test_header_access() {
        let mut headers = HashMap::new();
        headers.insert("content-type".to_string(), "application/json".to_string());
        let raw = RawRequest {
            method: "POST".into(), path: "/".into(),
            version: 1, headers, body: Bytes::new(),
        };
        let ctx = HttpContext::new(raw, Arc::new(()));
        assert_eq!(ctx.header("content-type"), Some("application/json"));
        assert_eq!(ctx.header("accept"),       None);
    }
}
```

### 6.3 `response.rs` 新增测试

```rust
// 追加到 response.rs 末尾

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_json_content_type() {
        let resp = ok(&serde_json::json!({ "k": "v" }));
        assert_eq!(resp.status, 200);
        let ct = resp.headers.iter()
            .find(|(k, _)| k == "content-type")
            .map(|(_, v)| v.as_str());
        assert_eq!(ct, Some("application/json; charset=utf-8"));
        let body: serde_json::Value = serde_json::from_slice(&resp.body).unwrap();
        assert_eq!(body["k"], "v");
    }

    #[test]
    fn test_created_status() {
        let resp = created(&serde_json::json!({ "id": "abc" }));
        assert_eq!(resp.status, 201);
    }

    #[test]
    fn test_no_content_empty_body() {
        let resp = no_content();
        assert_eq!(resp.status, 204);
        assert!(resp.body.is_empty());
    }

    #[test]
    fn test_not_found_json_body() {
        let resp = not_found("resource missing");
        assert_eq!(resp.status, 404);
        let body: serde_json::Value = serde_json::from_slice(&resp.body).unwrap();
        assert_eq!(body["error"], "resource missing");
    }

    #[test]
    fn test_with_header() {
        let resp = Response::new(200)
            .with_header("x-request-id", "abc-123");
        let hdr = resp.headers.iter().find(|(k, _)| k == "x-request-id");
        assert!(hdr.is_some());
    }
}
```

### 6.4 `error.rs` 新增测试

```rust
// 追加到 error.rs 末尾

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_server_error_status_codes() {
        assert_eq!(ServerError::BadRequest("x".into()).status_code(),          400);
        assert_eq!(ServerError::Unauthorized("x".into()).status_code(),        401);
        assert_eq!(ServerError::NotFound("x".into()).status_code(),            404);
        assert_eq!(ServerError::MethodNotAllowed("x".into()).status_code(),    405);
        assert_eq!(ServerError::RequestHeadersTooLarge.status_code(),          431);
        assert_eq!(ServerError::Io("x".into()).status_code(),                  500);
    }
}
```

---

## 七、集成测试（`crates/mo-server/tests/integration.rs`）

```rust
//! 端到端集成测试
//!
//! 使用 testcontainers 启动真实 PostgreSQL，在随机端口跑完整 MoServer，
//! 通过原始 TCP 连接发送 HTTP/1.1 请求，验证响应。
//!
//! 对应 C# WebApplicationFactory<TStartup> 集成测试模式，但
//! 这里直接通过 TCP 发送字节流，以验证 httparse 解析层的正确性。

use std::{net::SocketAddr, sync::Arc, time::Duration};

use sqlx::{postgres::PgPoolOptions, PgPool};
use testcontainers::{clients::Cli, images::postgres::Postgres};
use tokio::{
    io::{AsyncReadExt, AsyncWriteExt},
    net::{TcpListener, TcpStream},
    time::timeout,
};

use mo_server::{
    config::AppConfig,
    middleware::{auth_mw::make_test_token, tracing_mw},
    server::ServerBuilder,
    state::AppState,
};

const TEST_SECRET: &str = "integration-test-secret";

// ── 测试基础设施 ────────────────────────────────────────────────────────────

struct TestServer {
    addr:  SocketAddr,
    token: String,
}

impl TestServer {
    /// 启动完整 MoServer（真实 DB + 真实 TCP）
    async fn start(db: PgPool) -> Self {
        let token = make_test_token(TEST_SECRET, "test_user");

        let config = AppConfig {
            database_url: "unused".into(),
            port:         0, // 随机端口
            jwt_secret:   TEST_SECRET.into(),
        };

        let state = Arc::new(AppState::new(db, config));

        // 绑定随机端口（port=0，OS 分配）
        let listener = TcpListener::bind("127.0.0.1:0").await.unwrap();
        let addr     = listener.local_addr().unwrap();

        use mo_server_core::router::BoxHandler;
        fn h<F, Fut>(f: F) -> BoxHandler
        where
            F:   Fn(mo_server_core::context::HttpContext) -> Fut + Send + Sync + 'static,
            Fut: std::future::Future<
                Output = Result<
                    mo_server_core::response::Response,
                    mo_server_core::error::ServerError,
                >
            > + Send + 'static,
        {
            Arc::new(move |ctx| Box::pin(f(ctx)))
        }

        // 在后台任务中运行服务器
        tokio::spawn(
            ServerBuilder::new_with_listener(listener, state)
                .use_middleware(tracing_mw::tracing_mw)
                .use_middleware(mo_server::middleware::auth_mw::auth_mw)
                .map_get("/health",          h(mo_server::handlers::health::handle))
                .map_get("/services",        h(mo_server::handlers::services::list))
                .map_post("/services",       h(mo_server::handlers::services::create))
                .map_get("/services/:id",    h(mo_server::handlers::services::get))
                .map_delete("/services/:id", h(mo_server::handlers::services::delete))
                .run()
        );

        // 等待服务器就绪（简单重试 accept）
        for _ in 0..20 {
            if TcpStream::connect(addr).await.is_ok() { break; }
            tokio::time::sleep(Duration::from_millis(50)).await;
        }

        TestServer { addr, token }
    }
}

// ── HTTP over TCP 辅助 ──────────────────────────────────────────────────────

/// 发送原始 HTTP/1.1 请求并读取完整响应
async fn send(addr: SocketAddr, request: &[u8]) -> (u16, String) {
    let mut stream = TcpStream::connect(addr).await.unwrap();
    stream.write_all(request).await.unwrap();

    let mut buf = Vec::new();
    loop {
        let mut tmp = [0u8; 4096];
        let n = timeout(Duration::from_secs(5), stream.read(&mut tmp))
            .await
            .expect("read timed out")
            .expect("read error");
        if n == 0 { break; }
        buf.extend_from_slice(&tmp[..n]);

        // 找到 header 结束标志
        if let Some(hdr_end) = buf.windows(4).position(|w| w == b"\r\n\r\n") {
            let status_line = std::str::from_utf8(&buf[..hdr_end]).unwrap_or("");
            let status: u16 = status_line
                .split_whitespace().nth(1).unwrap_or("500")
                .parse().unwrap_or(500);

            let content_length: usize = status_line.lines()
                .find(|l| l.to_lowercase().starts_with("content-length:"))
                .and_then(|l| l.split(':').nth(1))
                .and_then(|v| v.trim().parse().ok())
                .unwrap_or(0);

            let body_start = hdr_end + 4;
            let mut body   = buf[body_start..].to_vec();

            while body.len() < content_length {
                let n = timeout(Duration::from_secs(5), stream.read(&mut tmp))
                    .await.unwrap().unwrap();
                if n == 0 { break; }
                body.extend_from_slice(&tmp[..n]);
            }

            let body_str = String::from_utf8_lossy(&body).to_string();
            return (status, body_str);
        }
    }
    (0, String::new())
}

fn get(path: &str, token: Option<&str>) -> Vec<u8> {
    let auth = token
        .map(|t| format!("authorization: Bearer {t}\r\n"))
        .unwrap_or_default();
    format!("GET {path} HTTP/1.1\r\nhost: localhost\r\n{auth}\r\n").into_bytes()
}

fn post_json(path: &str, token: Option<&str>, body: &str) -> Vec<u8> {
    let auth = token.map(|t| format!("authorization: Bearer {t}\r\n")).unwrap_or_default();
    format!(
        "POST {path} HTTP/1.1\r\nhost: localhost\r\n{auth}content-type: application/json\r\ncontent-length: {}\r\n\r\n{body}",
        body.len()
    )
    .into_bytes()
}

fn delete(path: &str, token: Option<&str>) -> Vec<u8> {
    let auth = token.map(|t| format!("authorization: Bearer {t}\r\n")).unwrap_or_default();
    format!("DELETE {path} HTTP/1.1\r\nhost: localhost\r\n{auth}\r\n").into_bytes()
}

// ── 测试夹具（testcontainers + 迁移）──────────────────────────────────────

async fn setup() -> (TestServer, testcontainers::Container<'static, Postgres>) {
    let docker = Box::leak(Box::new(Cli::default()));
    let container = docker.run(Postgres::default());
    let port = container.get_host_port_ipv4(5432);

    let db_url = format!("postgres://postgres:postgres@127.0.0.1:{port}/postgres");
    let pool   = PgPoolOptions::new()
        .max_connections(5)
        .connect(&db_url)
        .await
        .expect("testcontainers postgres connect failed");

    sqlx::migrate!("../../migrations").run(&pool).await.expect("migrate failed");

    let server = TestServer::start(pool).await;
    (server, container)
}

// ── 集成测试用例 ────────────────────────────────────────────────────────────

#[tokio::test]
async fn test_health_returns_200() {
    let (srv, _c) = setup().await;
    let (status, body) = send(srv.addr, &get("/health", None)).await;
    assert_eq!(status, 200);
    let json: serde_json::Value = serde_json::from_str(&body).unwrap();
    assert_eq!(json["status"], "ok");
}

#[tokio::test]
async fn test_list_services_unauthorized() {
    let (srv, _c) = setup().await;
    let (status, _) = send(srv.addr, &get("/services", None)).await;
    assert_eq!(status, 401);
}

#[tokio::test]
async fn test_list_services_authorized() {
    let (srv, _c) = setup().await;
    let (status, body) = send(srv.addr, &get("/services", Some(&srv.token))).await;
    assert_eq!(status, 200);
    let json: serde_json::Value = serde_json::from_str(&body).unwrap();
    assert!(json.is_array());
}

#[tokio::test]
async fn test_create_service_201() {
    let (srv, _c) = setup().await;
    let body_str = r#"{"name":"svc-a","endpoint":"http://svc-a:8080"}"#;
    let (status, body) = send(
        srv.addr,
        &post_json("/services", Some(&srv.token), body_str),
    )
    .await;
    assert_eq!(status, 201);
    let json: serde_json::Value = serde_json::from_str(&body).unwrap();
    assert_eq!(json["name"], "svc-a");
    assert!(json["id"].is_string());
}

#[tokio::test]
async fn test_create_service_validation() {
    let (srv, _c) = setup().await;
    // 空 name 应返回 400
    let body_str = r#"{"name":"","endpoint":"http://svc:8080"}"#;
    let (status, _) = send(
        srv.addr,
        &post_json("/services", Some(&srv.token), body_str),
    )
    .await;
    assert_eq!(status, 400);
}

#[tokio::test]
async fn test_get_service_200() {
    let (srv, _c) = setup().await;

    // 先创建
    let body_str = r#"{"name":"svc-get","endpoint":"http://svc-get:9090"}"#;
    let (_, created_body) = send(
        srv.addr,
        &post_json("/services", Some(&srv.token), body_str),
    )
    .await;
    let id = serde_json::from_str::<serde_json::Value>(&created_body).unwrap()["id"]
        .as_str().unwrap().to_string();

    // 再查询
    let (status, get_body) = send(srv.addr, &get(&format!("/services/{id}"), Some(&srv.token))).await;
    assert_eq!(status, 200);
    let json: serde_json::Value = serde_json::from_str(&get_body).unwrap();
    assert_eq!(json["id"], id);
}

#[tokio::test]
async fn test_get_service_404() {
    let (srv, _c) = setup().await;
    let fake_id = uuid::Uuid::new_v4();
    let (status, _) = send(
        srv.addr,
        &get(&format!("/services/{fake_id}"), Some(&srv.token)),
    )
    .await;
    assert_eq!(status, 404);
}

#[tokio::test]
async fn test_delete_service_204() {
    let (srv, _c) = setup().await;

    // 先创建
    let body_str = r#"{"name":"svc-del","endpoint":"http://svc-del:7070"}"#;
    let (_, created_body) = send(
        srv.addr,
        &post_json("/services", Some(&srv.token), body_str),
    )
    .await;
    let id = serde_json::from_str::<serde_json::Value>(&created_body).unwrap()["id"]
        .as_str().unwrap().to_string();

    // 删除
    let (status, _) = send(srv.addr, &delete(&format!("/services/{id}"), Some(&srv.token))).await;
    assert_eq!(status, 204);
}

#[tokio::test]
async fn test_delete_service_404() {
    let (srv, _c) = setup().await;
    let fake_id = uuid::Uuid::new_v4();
    let (status, _) = send(
        srv.addr,
        &delete(&format!("/services/{fake_id}"), Some(&srv.token)),
    )
    .await;
    assert_eq!(status, 404);
}

#[tokio::test]
async fn test_delete_already_deleted() {
    let (srv, _c) = setup().await;

    // 创建
    let body_str = r#"{"name":"svc-dd","endpoint":"http://svc-dd:6060"}"#;
    let (_, created_body) = send(
        srv.addr,
        &post_json("/services", Some(&srv.token), body_str),
    )
    .await;
    let id = serde_json::from_str::<serde_json::Value>(&created_body).unwrap()["id"]
        .as_str().unwrap().to_string();

    // 第一次删除 → 204
    let (s1, _) = send(srv.addr, &delete(&format!("/services/{id}"), Some(&srv.token))).await;
    assert_eq!(s1, 204);

    // 第二次删除 → 404（已不存在）
    let (s2, _) = send(srv.addr, &delete(&format!("/services/{id}"), Some(&srv.token))).await;
    assert_eq!(s2, 404);
}

#[tokio::test]
async fn test_keepalive_reuses_connection() {
    let (srv, _c) = setup().await;
    let mut stream = TcpStream::connect(srv.addr).await.unwrap();

    // 在同一 TCP 连接上发送两个请求（keep-alive 验证）
    let req = get("/health", None);
    for _ in 0..2 {
        stream.write_all(&req).await.unwrap();
        let mut buf = [0u8; 1024];
        let n = timeout(Duration::from_secs(3), stream.read(&mut buf))
            .await.unwrap().unwrap();
        let resp = std::str::from_utf8(&buf[..n]).unwrap();
        assert!(resp.starts_with("HTTP/1.1 200"));
        // 服务器返回 connection: keep-alive（TCP 不关闭）
        assert!(resp.to_lowercase().contains("keep-alive"));
    }
    // 连接仍然有效（未被服务器关闭）
    assert!(stream.peer_addr().is_ok());
}

#[tokio::test]
async fn test_max_body_size_413() {
    let (srv, _c) = setup().await;

    // 发送超大 body（> 4MB ServerLimits 默认值）
    let big_body = "x".repeat(5 * 1024 * 1024);
    let request  = format!(
        "POST /services HTTP/1.1\r\nhost: localhost\r\ncontent-length: {}\r\n\r\n{big_body}",
        big_body.len()
    );

    let (status, _) = send(srv.addr, request.as_bytes()).await;
    assert_eq!(status, 413);
}

#[tokio::test]
async fn test_graceful_shutdown_completes_inflight() {
    // 验证：优雅关闭时 in-flight 请求可以完成
    // 向服务器发送一个请求，在请求进入 handler 后触发 cancel，
    // 验证响应仍然正常返回（connection.rs 的 tokio::select! biased 确保了这一点：
    // cancel 只影响 idle 的 keep-alive 等待，不影响正在执行的 pipeline.run()）
    let (srv, _c) = setup().await;

    // 正常请求在关闭前完成
    let (status, _) = send(srv.addr, &get("/health", None)).await;
    assert_eq!(status, 200);
    // 注：完整的 SIGTERM 竞争测试需要 CancellationToken 暴露接口，此处验证基本行为
}
```
