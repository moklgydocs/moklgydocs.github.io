---
title: "二叉树遍历"
category:
  - 算法
  - CSharp
tag:
  - 树与图算法
order: 1
---

## 1. 算法定位

二叉树遍历是树与图算法的**基石**，几乎所有树相关问题都建立在遍历之上。掌握前序、中序、后序（递归+迭代）以及层序遍历，是解决二叉树问题的必备能力。

## 2. 一句话理解

> 按照不同的**访问根节点的时机**，系统地访问二叉树中的每一个节点恰好一次。

## 3. 生活化比喻

想象你参观一栋大楼：
- **前序遍历**：先在大厅签到（访问根），再参观左翼，最后参观右翼
- **中序遍历**：先参观左翼，回到大厅签到（访问根），再参观右翼
- **后序遍历**：先参观左翼，再参观右翼，最后在大厅签到（访问根）
- **层序遍历**：从一楼开始，逐层往上参观

## 4. 核心思想

| 遍历方式 | 访问顺序 | 特点 |
|---------|---------|------|
| 前序 (Preorder) | 根 → 左 → 右 | 第一个元素是根 |
| 中序 (Inorder) | 左 → 根 → 右 | BST中得到有序序列 |
| 后序 (Postorder) | 左 → 右 → 根 | 最后一个元素是根 |
| 层序 (Level Order) | 逐层从左到右 | 使用队列实现 |

## 5. 图解推演

### 示例二叉树结构

```
         1
        / \
       2   3
      / \   \
     4   5   6
```

### 各遍历结果

```
前序遍历: 1 → 2 → 4 → 5 → 3 → 6
中序遍历: 4 → 2 → 5 → 1 → 3 → 6
后序遍历: 4 → 5 → 2 → 6 → 3 → 1
层序遍历: 1 → 2 → 3 → 4 → 5 → 6
```

### 前序遍历递归调用栈

```
preorder(1)
├── 访问 1
├── preorder(2)
│   ├── 访问 2
│   ├── preorder(4)
│   │   ├── 访问 4
│   │   ├── preorder(null) → 返回
│   │   └── preorder(null) → 返回
│   └── preorder(5)
│       ├── 访问 5
│       ├── preorder(null) → 返回
│       └── preorder(null) → 返回
└── preorder(3)
    ├── 访问 3
    ├── preorder(null) → 返回
    └── preorder(6)
        ├── 访问 6
        ├── preorder(null) → 返回
        └── preorder(null) → 返回
```

### 迭代前序遍历（栈模拟）

```
步骤  栈状态        输出
1    [1]           
2    [3,2]         1      ← 弹出1，压入右3、左2
3    [3,5,4]       1,2    ← 弹出2，压入右5、左4
4    [3,5]         1,2,4  ← 弹出4，无子节点
5    [3]           1,2,4,5 ← 弹出5，无子节点
6    [6]           1,2,4,5,3 ← 弹出3，压入右6
7    []            1,2,4,5,3,6 ← 弹出6
```

## 6. 执行步骤

### 递归遍历步骤
1. 判断当前节点是否为空（基线条件）
2. 根据遍历类型决定访问顺序
3. 递归处理左子树和右子树

### 迭代遍历步骤（以前序为例）
1. 将根节点压入栈
2. 循环：弹出栈顶节点并访问
3. 先压右子节点，再压左子节点（保证左先被访问）
4. 直到栈为空

### 层序遍历步骤
1. 将根节点加入队列
2. 循环：取出队首节点并访问
3. 将其左右子节点加入队列
4. 直到队列为空

## 7. C# 实现

```csharp
using System;
using System.Collections.Generic;

/// <summary>
/// 二叉树节点定义
/// </summary>
public class TreeNode
{
    public int Val;          // 节点值
    public TreeNode Left;    // 左子节点
    public TreeNode Right;   // 右子节点

    public TreeNode(int val, TreeNode left = null, TreeNode right = null)
    {
        Val = val;
        Left = left;
        Right = right;
    }
}

/// <summary>
/// 二叉树遍历算法集合
/// </summary>
public class BinaryTreeTraversal
{
    // ========== 递归实现 ==========

    /// <summary>
    /// 前序遍历（递归）：根 → 左 → 右
    /// </summary>
    public List<int> PreorderRecursive(TreeNode root)
    {
        var result = new List<int>();
        PreorderHelper(root, result);
        return result;
    }

    private void PreorderHelper(TreeNode node, List<int> result)
    {
        if (node == null) return;   // 基线条件：空节点直接返回
        result.Add(node.Val);       // 先访问根节点
        PreorderHelper(node.Left, result);   // 再遍历左子树
        PreorderHelper(node.Right, result);  // 最后遍历右子树
    }

    /// <summary>
    /// 中序遍历（递归）：左 → 根 → 右
    /// </summary>
    public List<int> InorderRecursive(TreeNode root)
    {
        var result = new List<int>();
        InorderHelper(root, result);
        return result;
    }

    private void InorderHelper(TreeNode node, List<int> result)
    {
        if (node == null) return;
        InorderHelper(node.Left, result);    // 先遍历左子树
        result.Add(node.Val);                // 再访问根节点
        InorderHelper(node.Right, result);   // 最后遍历右子树
    }

    /// <summary>
    /// 后序遍历（递归）：左 → 右 → 根
    /// </summary>
    public List<int> PostorderRecursive(TreeNode root)
    {
        var result = new List<int>();
        PostorderHelper(root, result);
        return result;
    }

    private void PostorderHelper(TreeNode node, List<int> result)
    {
        if (node == null) return;
        PostorderHelper(node.Left, result);  // 先遍历左子树
        PostorderHelper(node.Right, result); // 再遍历右子树
        result.Add(node.Val);                // 最后访问根节点
    }

    // ========== 迭代实现 ==========

    /// <summary>
    /// 前序遍历（迭代）：使用栈模拟递归
    /// </summary>
    public List<int> PreorderIterative(TreeNode root)
    {
        var result = new List<int>();
        if (root == null) return result;

        var stack = new Stack<TreeNode>();
        stack.Push(root);

        while (stack.Count > 0)
        {
            var node = stack.Pop();      // 弹出栈顶节点
            result.Add(node.Val);        // 访问该节点

            // 注意：先压右后压左，保证左子节点先被弹出
            if (node.Right != null) stack.Push(node.Right);
            if (node.Left != null) stack.Push(node.Left);
        }

        return result;
    }

    /// <summary>
    /// 中序遍历（迭代）：一路向左压栈，回溯时访问
    /// </summary>
    public List<int> InorderIterative(TreeNode root)
    {
        var result = new List<int>();
        var stack = new Stack<TreeNode>();
        var current = root;

        while (current != null || stack.Count > 0)
        {
            // 一路向左，将所有左子节点压入栈
            while (current != null)
            {
                stack.Push(current);
                current = current.Left;
            }

            // 弹出栈顶（最左节点），访问它
            current = stack.Pop();
            result.Add(current.Val);

            // 转向右子树
            current = current.Right;
        }

        return result;
    }

    /// <summary>
    /// 后序遍历（迭代）：双栈法
    /// 思路：前序是 根→左→右，将其改为 根→右→左，然后反转即得 左→右→根
    /// </summary>
    public List<int> PostorderIterative(TreeNode root)
    {
        var result = new List<int>();
        if (root == null) return result;

        var stack = new Stack<TreeNode>();
        stack.Push(root);

        while (stack.Count > 0)
        {
            var node = stack.Pop();
            result.Add(node.Val);   // 根→右→左 的顺序添加

            // 先压左后压右（与前序相反）
            if (node.Left != null) stack.Push(node.Left);
            if (node.Right != null) stack.Push(node.Right);
        }

        result.Reverse();   // 反转得到 左→右→根
        return result;
    }

    /// <summary>
    /// 层序遍历：使用队列逐层访问
    /// </summary>
    public List<List<int>> LevelOrder(TreeNode root)
    {
        var result = new List<List<int>>();
        if (root == null) return result;

        var queue = new Queue<TreeNode>();
        queue.Enqueue(root);

        while (queue.Count > 0)
        {
            int levelSize = queue.Count;    // 当前层的节点数
            var levelList = new List<int>();

            for (int i = 0; i < levelSize; i++)
            {
                var node = queue.Dequeue();
                levelList.Add(node.Val);

                // 将下一层的节点加入队列
                if (node.Left != null) queue.Enqueue(node.Left);
                if (node.Right != null) queue.Enqueue(node.Right);
            }

            result.Add(levelList);
        }

        return result;
    }

    // ========== Morris 遍历（进阶） ==========

    /// <summary>
    /// Morris 中序遍历：O(1) 空间复杂度
    /// 核心思想：利用叶节点的空指针指向后继节点，遍历完后恢复树结构
    /// </summary>
    public List<int> MorrisInorder(TreeNode root)
    {
        var result = new List<int>();
        var current = root;

        while (current != null)
        {
            if (current.Left == null)
            {
                // 无左子树，直接访问当前节点，移向右子树
                result.Add(current.Val);
                current = current.Right;
            }
            else
            {
                // 找到左子树的最右节点（中序前驱）
                var predecessor = current.Left;
                while (predecessor.Right != null && predecessor.Right != current)
                {
                    predecessor = predecessor.Right;
                }

                if (predecessor.Right == null)
                {
                    // 建立线索：前驱的右指针指向当前节点
                    predecessor.Right = current;
                    current = current.Left;
                }
                else
                {
                    // 已经遍历完左子树，恢复树结构
                    predecessor.Right = null;
                    result.Add(current.Val);
                    current = current.Right;
                }
            }
        }

        return result;
    }
}

/// <summary>
/// 测试程序
/// </summary>
public class Program
{
    public static void Main()
    {
        // 构建示例二叉树
        //       1
        //      / \
        //     2   3
        //    / \   \
        //   4   5   6
        var root = new TreeNode(1,
            new TreeNode(2, new TreeNode(4), new TreeNode(5)),
            new TreeNode(3, null, new TreeNode(6))
        );

        var traversal = new BinaryTreeTraversal();

        Console.WriteLine("前序遍历(递归): " + string.Join(", ", traversal.PreorderRecursive(root)));
        Console.WriteLine("前序遍历(迭代): " + string.Join(", ", traversal.PreorderIterative(root)));
        Console.WriteLine("中序遍历(递归): " + string.Join(", ", traversal.InorderRecursive(root)));
        Console.WriteLine("中序遍历(迭代): " + string.Join(", ", traversal.InorderIterative(root)));
        Console.WriteLine("后序遍历(递归): " + string.Join(", ", traversal.PostorderRecursive(root)));
        Console.WriteLine("后序遍历(迭代): " + string.Join(", ", traversal.PostorderIterative(root)));
        Console.WriteLine("Morris中序:     " + string.Join(", ", traversal.MorrisInorder(root)));

        Console.WriteLine("\n层序遍历:");
        var levels = traversal.LevelOrder(root);
        for (int i = 0; i < levels.Count; i++)
        {
            Console.WriteLine($"  第{i + 1}层: [{string.Join(", ", levels[i])}]");
        }
    }
}
```

## 8. 示例演示

**输入树：**
```
       1
      / \
     2   3
    / \   \
   4   5   6
```

**输出结果：**
```
前序遍历(递归): 1, 2, 4, 5, 3, 6
前序遍历(迭代): 1, 2, 4, 5, 3, 6
中序遍历(递归): 4, 2, 5, 1, 3, 6
中序遍历(迭代): 4, 2, 5, 1, 3, 6
后序遍历(递归): 4, 5, 2, 6, 3, 1
后序遍历(迭代): 4, 5, 2, 6, 3, 1
Morris中序:     4, 2, 5, 1, 3, 6

层序遍历:
  第1层: [1]
  第2层: [2, 3]
  第3层: [4, 5, 6]
```

## 9. 复杂度分析

| 遍历方式 | 时间复杂度 | 空间复杂度 | 说明 |
|---------|-----------|-----------|------|
| 递归遍历 | O(n) | O(h) | h为树高，最差O(n) |
| 迭代遍历 | O(n) | O(h) | 栈空间与树高相关 |
| 层序遍历 | O(n) | O(w) | w为最大层宽度 |
| Morris遍历 | O(n) | O(1) | 不需要额外空间 |

> 其中 n 为节点总数，h 为树高（平衡树为 log n，退化为链表时为 n）

## 10. 易错点

1. **迭代中序遍历**：容易忘记 `current != null` 的条件，导致无法处理右子树
2. **后序迭代**：不要尝试直接模拟，用"前序变体+反转"更简洁
3. **层序遍历**：必须在循环开始时记录 `queue.Count`，因为循环中会添加新节点
4. **Morris遍历**：遍历过程中临时修改了树结构，多线程环境下不安全
5. **递归深度**：极端情况（链式树）递归可能导致栈溢出，需要改用迭代

## 11. 对比表

| 对比维度 | 递归实现 | 迭代实现 | Morris遍历 |
|---------|---------|---------|-----------|
| 代码简洁度 | 极简 | 较复杂 | 复杂 |
| 空间复杂度 | O(h) 栈帧 | O(h) 显式栈 | O(1) |
| 是否修改树 | 否 | 否 | 是（临时） |
| 适用场景 | 一般情况 | 避免栈溢出 | 空间极度受限 |
| 面试频率 | 高 | 高 | 中 |

## 12. 前置知识与延伸

### 前置知识
- 递归基本原理
- 栈和队列数据结构
- 二叉树基本概念（节点、左右子树、叶节点、深度）

### 延伸学习
- **二叉搜索树（BST）** → 中序遍历得到有序序列
- **线索二叉树** → Morris遍历的理论基础
- **序列化与反序列化** → 利用前序+中序重建二叉树
- **N叉树遍历** → 将二叉树遍历推广到多叉树

## 13. 记忆口诀

```
前中后序看根位，根在前中后分别对。
前序：根左右，先根后子树；
中序：左根右，BST变有序；
后序：左右根，删除安全用；
层序：用队列，逐层从左右。
迭代记住栈，Morris空间省。
```

## 14. 面试常问点

1. **手写迭代中序遍历**（高频）
2. **前序+中序重建二叉树**
3. **层序遍历的变体**：锯齿形层序、最大层宽度
4. **不用递归和栈如何遍历？** → Morris遍历
5. **递归遍历的时间和空间复杂度？** → O(n)时间，O(h)空间
6. **什么时候用哪种遍历？** → 根据问题特点选择

## 15. 一页总结

```
┌─────────────────────────────────────────────────────┐
│              二叉树遍历 · 一页总结                      │
├─────────────────────────────────────────────────────┤
│ 四种遍历：前序(根左右) 中序(左根右)                     │
│          后序(左右根) 层序(逐层BFS)                    │
├─────────────────────────────────────────────────────┤
│ 实现方式：递归(简洁) / 迭代(栈) / Morris(O(1)空间)     │
├─────────────────────────────────────────────────────┤
│ 复杂度：时间O(n)  空间O(h)/O(1)                       │
├─────────────────────────────────────────────────────┤
│ 关键技巧：                                            │
│ · 迭代中序：一路左压栈，弹出访问，转右                   │
│ · 迭代后序：前序变体(根右左)+反转                       │
│ · 层序：队列 + 记录每层大小                            │
├─────────────────────────────────────────────────────┤
│ 应用：BST验证、树重建、路径问题、序列化                  │
└─────────────────────────────────────────────────────┘
```
