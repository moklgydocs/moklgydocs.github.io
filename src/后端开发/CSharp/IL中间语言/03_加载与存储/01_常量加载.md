---
title: 常量加载
order: 1
category:
  - CSharp
tag:
  - IL
  - ldc
  - ldstr
  - ldnull
  - ldtoken
  - 求值栈
---

# 常量加载

::: tip 核心要点
常量加载指令是 IL 中最基础的指令族，用于将各种类型的常量值压入求值栈。掌握 `ldc`、`ldstr`、`ldnull`、`ldtoken` 是理解所有 IL 代码的基础。
:::

## 一、ldc —— 数值常量加载

`ldc`（Load Constant）系列指令将数值常量压入求值栈。

### 1.1 ldc.i4 —— 32 位整数

`ldc.i4` 有多种编码形式，从最短的 1 字节到最长的 6 字节：

| 指令 | 操作码 | 字节大小 | 加载的值 | 说明 |
|------|--------|---------|---------|------|
| `ldc.i4.m1` | 0x15 | 1 | -1 | 特殊值，常用于表示 -1 |
| `ldc.i4.0` | 0x16 | 1 | 0 | |
| `ldc.i4.1` | 0x17 | 1 | 1 | |
| `ldc.i4.2` | 0x18 | 1 | 2 | |
| `ldc.i4.3` | 0x19 | 1 | 3 | |
| `ldc.i4.4` | 0x1A | 1 | 4 | |
| `ldc.i4.5` | 0x1B | 1 | 5 | |
| `ldc.i4.6` | 0x1C | 1 | 6 | |
| `ldc.i4.7` | 0x1D | 1 | 7 | |
| `ldc.i4.8` | 0x1E | 1 | 8 | |
| `ldc.i4.s` | 0x1F | 2 | -128 ~ 127 | 短格式，1 字节有符号操作数 |
| `ldc.i4` | 0x20 | 5 | -2^31 ~ 2^31-1 | 完整格式，4 字节有符号操作数 |

::: important 编码优化
`ldc.i4.0` 到 `ldc.i4.8` 是**快捷指令（Short Form）**，仅占 1 字节。`ldc.i4.s` 占 2 字节，`ldc.i4` 占 5 字节。C# 编译器会自动选择最短编码：
- 值 -1 到 8：使用 1 字节快捷指令
- 值 -128 到 127（超出快捷范围）：使用 `ldc.i4.s`（2 字节）
- 其他值：使用 `ldc.i4`（5 字节）

这是 IL 编码优化最直观的例子——常用值用更短的编码。
:::

### 1.2 ldc.i8 —— 64 位整数

| 指令 | 操作码 | 字节大小 | 加载的值 |
|------|--------|---------|---------|
| `ldc.i8` | 0x21 | 9 | -2^63 ~ 2^63-1 |

`ldc.i8` 后跟 8 字节有符号整数操作数，共占 9 字节。

### 1.3 ldc.r4 与 ldc.r8 —— 浮点数

| 指令 | 操作码 | 字节大小 | 加载的值 |
|------|--------|---------|---------|
| `ldc.r4` | 0x22 | 5 | 32 位浮点数（float） |
| `ldc.r8` | 0x23 | 9 | 64 位浮点数（double） |

### 1.4 ldc 全家福

| 指令 | 类型 | 字节大小 | C# 类型 |
|------|------|---------|---------|
| `ldc.i4.m1` | int32 | 1 | int (-1) |
| `ldc.i4.0` ~ `ldc.i4.8` | int32 | 1 | int (0-8) |
| `ldc.i4.s` | int32 | 2 | int (-128~127) |
| `ldc.i4` | int32 | 5 | int |
| `ldc.i8` | int64 | 9 | long |
| `ldc.r4` | float32 | 5 | float |
| `ldc.r8` | float64 | 9 | double |

::: warning 没有 ldc.u 系列
IL 没有无符号整数的专用加载指令。`uint`、`ulong` 等无符号类型使用与有符号相同的 `ldc.i4` / `ldc.i8` 指令加载，区别在于类型的使用方式（如 `conv.u4`、`conv.u8` 等转换指令）。
:::

## 二、ldstr —— 字符串常量加载

`ldstr` 从程序集的元数据中加载字符串字面量，将其压入求值栈。

### 2.1 基本用法

```csharp
// C# 源码
string greeting = "Hello, World!";
```

```il
// IL 代码
IL_0000: ldstr        "Hello, World!"   // 从元数据 #US 堆加载字符串
IL_0005: stloc.0                         // 存入局部变量
```

### 2.2 求值栈状态

```
执行前：  [ ]
执行后：  [ "Hello, World!" ]    ← 栈顶是字符串对象引用
```

### 2.3 驻留（Interning）

::: important 字符串驻留
`ldstr` 不会每次创建新的字符串对象。CLR 维护一个**字符串驻留池（Intern Pool）**，相同内容的字符串字面量在运行时指向同一个 `string` 对象。

```csharp
string a = "hello";
string b = "hello";
// a 和 b 引用同一个对象（因为 ldstr 检查驻留池）
Console.WriteLine(ReferenceEquals(a, b)); // True
```

这是 `ldstr` 的特殊行为，不同于 `new string(...)` 或字符串拼接的结果。
:::

## 三、ldnull —— 加载 null 引用

`ldnull` 将一个 `null` 引用压入求值栈。

### 3.1 基本用法

```csharp
// C# 源码
string name = null;
```

```il
// IL 代码
IL_0000: ldnull          // 压入 null 引用
IL_0001: stloc.0         // 存入局部变量
```

### 3.2 求值栈状态

```
执行前：  [ ]
执行后：  [ null ]    ← 栈顶是 null 引用
```

### 3.3 ldnull 的常见用途

```csharp
// 初始化引用类型为 null
string s = null;

// 条件赋值
string result = flag ? "yes" : null;

// 方法参数传递
ProcessData(null);
```

::: tip ldnull 与 ldc.i4.0
`ldnull` 压入的是 `null` 引用（O 类型，对象引用），`ldc.i4.0` 压入的是整数 0（I4 类型）。虽然底层都是 0，但类型语义不同。CLR 验证器不允许将 `null` 当整数使用，反之亦然。
:::

## 四、ldtoken —— 加载元数据标记

`ldtoken` 将元数据标记（Type/Method/Field）对应的运行时表示压入求值栈，用于实现 `typeof()`、`nameof()` 等反射操作。

### 4.1 typeof 的 IL 实现

```csharp
// C# 源码
Type t = typeof(string);
```

```il
// IL 代码
IL_0000: ldtoken       [mscorlib]System.String    // 加载 System.String 的元数据标记
IL_0005: call          class [mscorlib]System.Type [mscorlib]System.Type::GetTypeFromHandle(
                                                       valuetype [mscorlib]System.RuntimeTypeHandle)
IL_000a: stloc.0
```

### 4.2 求值栈状态

```
执行 ldtoken 后：  [ RuntimeTypeHandle ]    ← 元数据标记的运行时句柄
执行 call 后：     [ Type ]                 ← 转换为 Type 对象
```

### 4.3 ldtoken 的三种用法

| 加载目标 | 栈上类型 | C# 用法 |
|---------|---------|---------|
| 类型 | `RuntimeTypeHandle` → `Type` | `typeof(T)` |
| 方法 | `RuntimeMethodHandle` → `MethodInfo` | 反射获取方法 |
| 字段 | `RuntimeFieldHandle` → `FieldInfo` | 反射获取字段 |

### 4.4 nameof 的 IL 实现

```csharp
// C# 源码
string name = nameof(Console.WriteLine);
```

```il
// IL 代码
IL_0000: ldtoken       void [mscorlib]System.Console::WriteLine(string)
IL_0005: call          class [mscorlib]System.Reflection.MethodBase
                       [mscorlib]System.Reflection.MethodBase::GetMethodFromHandle(
                           valuetype [mscorlib]System.RuntimeMethodHandle)
IL_000a: callvirt      instance string [mscorlib]System.Reflection.MemberInfo::get_Name()
IL_000f: stloc.0
```

::: warning nameof 的编译期 vs 运行期
`nameof` 在 C# 7+ 中已优化为编译期常量字符串，不再需要 `ldtoken` + 反射调用。但早期版本中 `nameof` 确实通过 `ldtoken` 实现。如果你在旧版本 .NET 中看到 `nameof` 产生了反射调用，这就是原因。
:::

## 五、完整示例：常量加载对比

```csharp
// C# 源码
public class Constants
{
    public void Demo()
    {
        int i = 42;
        long l = 123456789L;
        float f = 3.14f;
        double d = 2.718281828;
        string s = "Hello";
        object o = null;
        Type t = typeof(int);
    }
}
```

```il
// IL 反编译输出（带求值栈状态注释）
.method public hidebysig instance void Demo() cil managed
{
    .maxstack 1
    .locals init (
        int32 i,      // V_0
        int64 l,      // V_1
        float32 f,    // V_2
        float64 d,    // V_3
        string s,     // V_4
        object o,     // V_5
        class [mscorlib]System.Type t  // V_6
    )

    // int i = 42;
    IL_0000: ldc.i4.s     42            // [ 42 ]
    IL_0002: stloc.0                     // [ ]

    // long l = 123456789L;
    IL_0003: ldc.i8       123456789     // [ 123456789L ]
    IL_000c: stloc.1                     // [ ]

    // float f = 3.14f;
    IL_000d: ldc.r4       3.14          // [ 3.14f ]
    IL_0012: stloc.2                     // [ ]

    // double d = 2.718281828;
    IL_0013: ldc.r8       2.718281828   // [ 2.718281828 ]
    IL_001c: stloc.3                     // [ ]

    // string s = "Hello";
    IL_001d: ldstr        "Hello"       // [ "Hello" ]
    IL_0022: stloc.s      s             // [ ]

    // object o = null;
    IL_0024: ldnull                      // [ null ]
    IL_0025: stloc.s      o             // [ ]

    // Type t = typeof(int);
    IL_0027: ldtoken      [mscorlib]System.Int32   // [ RuntimeTypeHandle ]
    IL_002c: call         class [mscorlib]System.Type
                       [mscorlib]System.Type::GetTypeFromHandle(
                           valuetype [mscorlib]System.RuntimeTypeHandle)
                                              // [ Type ]
    IL_0031: stloc.s      t             // [ ]

    IL_0033: ret
}
```

## 六、求值栈状态追踪

让我们追踪一个稍复杂的表达式：

```csharp
// C# 源码
int result = 10 + 20 * 3;
```

```il
// IL 代码 + 求值栈状态
//                                     求值栈
IL_0000: ldc.i4.s     10        //    [ 10 ]
IL_0002: ldc.i4.s     20        //    [ 10, 20 ]
IL_0004: ldc.i4.3               //    [ 10, 20, 3 ]
IL_0005: mul                    //    [ 10, 60 ]         ← 20 * 3 = 60
IL_0006: add                    //    [ 70 ]             ← 10 + 60 = 70
IL_0007: stloc.0                //    [ ]                ← result = 70
```

::: tip 运算顺序
IL 是"后缀"形式（类似逆波兰表达式），操作数在前，运算符在后。这正适合栈式机器——先把操作数压栈，再执行运算。C# 的运算优先级在编译时已经处理为正确的 IL 指令顺序。
:::

## 七、所有常量加载指令一览

| 指令 | 操作码 | 栈行为 | 字节 | 说明 |
|------|--------|--------|------|------|
| `ldc.i4.m1` | 0x15 | → int32 | 1 | 加载 -1 |
| `ldc.i4.0` | 0x16 | → int32 | 1 | 加载 0 |
| `ldc.i4.1` | 0x17 | → int32 | 1 | 加载 1 |
| `ldc.i4.2` | 0x18 | → int32 | 1 | 加载 2 |
| `ldc.i4.3` | 0x19 | → int32 | 1 | 加载 3 |
| `ldc.i4.4` | 0x1A | → int32 | 1 | 加载 4 |
| `ldc.i4.5` | 0x1B | → int32 | 1 | 加载 5 |
| `ldc.i4.6` | 0x1C | → int32 | 1 | 加载 6 |
| `ldc.i4.7` | 0x1D | → int32 | 1 | 加载 7 |
| `ldc.i4.8` | 0x1E | → int32 | 1 | 加载 8 |
| `ldc.i4.s` | 0x1F | → int32 | 2 | 加载 int8 |
| `ldc.i4` | 0x20 | → int32 | 5 | 加载 int32 |
| `ldc.i8` | 0x21 | → int64 | 9 | 加载 int64 |
| `ldc.r4` | 0x22 | → float32 | 5 | 加载 float32 |
| `ldc.r8` | 0x23 | → float64 | 9 | 加载 float64 |
| `ldstr` | 0x72 | → string | 5+ | 加载字符串 |
| `ldnull` | 0x14 | → null | 1 | 加载 null |
| `ldtoken` | 0xD0 | → handle | 5 | 加载元数据标记 |

## 八、参考资料

- [ECMA-335 Partition III — ldc](https://ecma-international.org/publications-and-standards/standards/ecma-335/) —— ldc 指令的权威规范
- [ECMA-335 Partition III — ldstr](https://ecma-international.org/publications-and-standards/standards/ecma-335/) —— ldstr 指令规范
- [ECMA-335 Partition III — ldnull](https://ecma-international.org/publications-and-standards/standards/ecma-335/) —— ldnull 指令规范
- [ECMA-335 Partition III — ldtoken](https://ecma-international.org/publications-and-standards/standards/ecma-335/) —— ldtoken 指令规范
- [System.Reflection.Emit.OpCodes](https://learn.microsoft.com/en-us/dotnet/api/system.reflection.emit.opcodes) —— 微软官方 OpCodes API

---

## 面试技巧

::: tip 面试常考
1. **"ldc.i4.0 和 ldc.i4.s 0 有什么区别？"** —— 结果相同，但编码不同。`ldc.i4.0` 是 1 字节快捷指令（操作码 0x16），`ldc.i4.s 0` 是 2 字节（操作码 0x1F + 0x00）。编译器优先使用快捷指令以减小代码体积。
2. **"为什么 IL 要为 0-8 设计专用指令？"** —— 这些是最常用的整数值（循环索引、布尔值、数组访问等），1 字节编码相比 `ldc.i4` 的 5 字节编码节省大量空间，对频繁出现的小整数效果显著。
3. **"ldstr 加载的字符串是否每次都创建新对象？"** —— 不是。`ldstr` 使用字符串驻留池，相同内容的字符串字面量指向同一个对象。这是 CLR 的优化。
4. **"ldnull 和 ldc.i4.0 的区别？"** —— 类型语义不同。`ldnull` 压入的是 O 类型（对象引用），`ldc.i4.0` 压入的是 I4 类型（32位整数）。CLR 验证器不允许混淆使用。
5. **"typeof(T) 在 IL 中是如何实现的？"** —— `ldtoken T` 加载类型的运行时句柄（RuntimeTypeHandle），然后调用 `Type.GetTypeFromHandle()` 转换为 `Type` 对象。
6. **"ldc.i4.m1 加载的是 -1，有什么实际用途？"** —— 常用于表示"未找到"的返回值（如 `Array.IndexOf` 返回 -1），以及枚举中的特殊值。`m1` = minus one。
:::
