---
title: 创建对象
order: 1
category:
  - CSharp
tag:
  - IL
  - newobj
  - initobj
  - newarr
  - ldstr
  - 对象创建
  - 值类型
---

# 创建对象

C# 的 `new` 关键字是一个多面手——创建引用类型、值类型、数组和字符串时，编译器会选择完全不同的 IL 指令。理解这些指令，才能真正掌握 .NET 对象创建的全貌。

::: tip 核心要点
- `newobj`：调用构造函数创建对象（引用类型堆分配 / 值类型初始化）
- `initobj`：零初始化值类型（无构造函数调用，更快）
- `newarr`：创建数组
- `ldstr`：创建字符串（字符串驻留优化）
:::

---

## 一、newobj 指令 —— 构造新对象

### 1.1 指令行为

`newobj ctor`：
1. **分配内存**（引用类型：GC 堆；值类型：栈/内联）
2. **初始化内存**为零/null
3. **调用构造函数** `ctor`
4. 将对象引用（引用类型）或值（值类型）压入评估栈

### 1.2 引用类型 vs 值类型

| 特性 | 引用类型（class） | 值类型（struct） |
|------|-----------------|-----------------|
| 内存位置 | GC 堆 | 栈或内联（作为字段/数组元素） |
| 分配开销 | GC 分配 + 类型对象指针 + 同步块 | 无额外开销 |
| 构造函数 | 必须调用 | 可选（可用 `initobj` 替代） |
| 结果类型 | 对象引用 (O) | 值类型 (ValueType) |

---

## 二、newobj 创建引用类型

### 2.1 C# 示例

```csharp
class Person
{
    public string Name;
    public int Age;

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }
}

// 创建对象
var p = new Person("Alice", 30);
```

```il
IL_0000: ldstr "Alice"
IL_0005: ldc.i4.s 30
IL_0007: newobj instance void Person::.ctor(string, int32)
IL_000c: stloc.0
```

### 2.2 内存布局

```mermaid
flowchart TD
    subgraph "GC 堆上的 Person 对象"
        A["┌──────────────────────────────────┐"]
        B["│ 类型对象指针 → Person Type Object │"]
        C["├──────────────────────────────────┤"]
        D["│ 同步块索引 = 0                   │"]
        E["├──────────────────────────────────┤"]
        F["│ Name → \"Alice\" (string ref)       │"]
        G["├──────────────────────────────────┤"]
        H["│ Age = 30 (int32)                 │"]
        I["└──────────────────────────────────┘"]
    end

    style A fill:#E3F2FD
    style B fill:#BBDEFB
    style F fill:#C8E6C9
    style H fill:#FFF9C4
```

### 2.3 newobj 的执行步骤

```mermaid
flowchart LR
    A["1. 计算大小<br/>（字段 + 开销）"] --> B["2. 在 GC 堆分配<br/>（移动 NextObjPtr）"]
    B --> C["3. 零初始化<br/>（所有字段 = 0/null）"]
    C --> D["4. 设置类型对象指针"]
    D --> E["5. 调用 .ctor()"]
    E --> F["6. 压入对象引用"]

    style A fill:#E3F2FD
    style F fill:#C8E6C9
```

::: important 内存零初始化
CLR 保证所有新分配的对象内存被零初始化。这意味着即使构造函数没有设置某个字段，它的值也是确定的：数值为 0，引用为 null，布尔为 false。这是 .NET 消除"未初始化内存"漏洞的关键机制。
:::

---

## 三、newobj 创建值类型

### 3.1 带自定义构造函数

```csharp
struct Point
{
    public int X, Y;

    public Point(int x, int y)
    {
        X = x;
        Y = y;
    }
}

// 创建值类型
var p = new Point(3, 4);
```

```il
IL_0000: ldloca.s p         // 加载局部变量地址
IL_0002: ldc.i4.3
IL_0003: ldc.i4.4
IL_0004: call instance void Point::.ctor(int32, int32)
// 注意：值类型的 newobj 在某些情况下编译为 call 而非 newobj
// 具体取决于上下文（局部变量 vs 表达式）
```

### 3.2 无自定义构造函数

```csharp
struct Point
{
    public int X, Y;
}

// C# new 语法
var p = new Point();    // 编译为 initobj
```

```il
IL_0000: ldloca.s p
IL_0002: initobj Point       // 零初始化，不调用构造函数
```

---

## 四、initobj 指令 —— 零初始化值类型

### 4.1 指令行为

`initobj typeToken`：
1. 将指定地址处的值类型初始化为**零/null**
2. 所有数值字段 → 0，引用字段 → null，布尔字段 → false
3. **不调用任何构造函数**

### 4.2 newobj vs initobj 对比

| 特性 | `newobj` | `initobj` |
|------|---------|----------|
| 构造函数 | 调用 | 不调用 |
| 初始化内容 | 构造函数决定 | 全部置零 |
| 适用类型 | 引用类型 + 值类型 | 仅值类型 |
| 性能 | 较慢（方法调用开销） | 更快（直接内存清零） |
| C# 对应 | `new T(args)` | `new T()` / `default(T)` |

### 4.3 C# 中的 initobj 场景

```csharp
// 1. 无参 new 值类型
Point p1 = new Point();         // initobj Point

// 2. default 关键字
Point p2 = default(Point);      // initobj Point
Point p3 = default;             // initobj Point（C# 7.1+）

// 3. 泛型 default
T GetDefault<T>() => default;   // initobj !!0（泛型参数）
```

```il
// Point p1 = new Point();
IL_0000: ldloca.s p1
IL_0002: initobj Point

// Point p2 = default(Point);
IL_0007: ldloca.s p2
IL_0009: initobj Point
```

::: tip 性能建议
对于没有自定义构造函数的值类型，`initobj` 比 `newobj` 更快（省去方法调用开销）。C# 编译器自动选择最优指令，无需手动干预。
:::

---

## 五、sizeof 指令 —— 获取值类型大小

### 5.1 指令行为

`sizeof valueType`：
1. 将值类型的**字节大小**压入评估栈（作为 `int32`）
2. 包含对齐填充（padding）
3. 不包含对象开销（类型对象指针、同步块索引）

### 5.2 C# 示例

```csharp
using System;

struct Point3D
{
    public double X;   // 8 bytes
    public double Y;   // 8 bytes
    public double Z;   // 8 bytes
}

unsafe
{
    int size = sizeof(Point3D);    // 24
}
```

```il
IL_0000: sizeof Point3D
IL_0006: stloc.0
```

### 5.3 sizeof 的用途

| 场景 | 说明 |
|------|------|
| 不安全指针算术 | 计算指针偏移量 |
| `MemoryMarshal` | 直接内存操作 |
| `Span<T>` 切片 | 按元素大小计算范围 |
| 互操作 | 确定 P/Invoke 结构体布局 |

::: warning sizeof vs Marshal.SizeOf
- `sizeof`（IL 指令）：返回 CLR 内部的托管大小，包含对齐填充
- `Marshal.SizeOf`：返回非托管互操作的大小，受 `StructLayout` 属性影响
- 二者结果可能不同！
:::

---

## 六、字符串创建：ldstr

### 6.1 为什么不用 newobj？

字符串是 .NET 中最特殊的引用类型：
- **字符串驻留（String Interning）**：相同内容的字符串共享同一实例
- **不可变性**：创建后不可修改
- **特殊优化**：CLR 对字符串有专门的分配路径

```csharp
string s1 = "hello";
string s2 = "hello";
// s1 和 s2 引用同一对象（驻留）
```

```il
IL_0000: ldstr "hello"        // 不是 newobj！
IL_0005: stloc.0

IL_0006: ldstr "hello"        // 相同的 token → 相同的对象
IL_000b: stloc.1
```

### 6.2 ldstr 的行为

1. 从元数据的 **用户字符串堆（#US）** 中查找 token
2. 如果字符串已驻留，返回现有引用
3. 如果未驻留，在 GC 堆分配新字符串对象并驻留
4. 压入对象引用

::: important 字符串驻留的性能影响
`ldstr` 对字面量字符串自动驻留，但运行时拼接的字符串（如 `"hel" + "lo"`）不一定驻留。C# 编译器会将常量拼接优化为单个 `ldstr`，但变量拼接则不会。
:::

---

## 七、数组创建：newarr

### 7.1 一维数组

```csharp
int[] numbers = new int[10];
```

```il
IL_0000: ldc.i4.s 10          // 数组长度
IL_0002: newarr [mscorlib]System.Int32
IL_0007: stloc.0
```

### 7.2 多维数组

```csharp
int[,] matrix = new int[3, 4];    // 不用 newarr，用 newobj
```

```il
IL_0000: ldc.i4.3              // 维度1
IL_0001: ldc.i4.4              // 维度2
IL_0002: newobj instance void int32[0...,0...]::.ctor(int32, int32)
```

::: warning 一维数组 vs 多维数组
- 一维零基数组（`int[]`）→ `newarr`：JIT 特殊优化，性能最佳
- 多维数组（`int[,]`）→ `newobj`：无 JIT 优化，性能较差
- 交错数组（`int[][]`）→ 多次 `newarr`：介于两者之间
:::

### 7.3 newarr 内存布局

```
int[5] 数组的内存布局：

┌───────────────────────────────────────────────────┐
│ 类型对象指针 → Int32[] Type                        │
├───────────────────────────────────────────────────┤
│ 同步块索引                                        │
├───────────────────────────────────────────────────┤
│ 长度 = 5                                          │
├──────┬──────┬──────┬──────┬──────┬────────────────┤
│ [0]  │ [1]  │ [2]  │ [3]  │ [4]  │                │
│  0   │  0   │  0   │  0   │  0   │                │
└──────┴──────┴──────┴──────┴──────┴────────────────┘
```

---

## 八、C# new 操作符的完整映射

| C# 代码 | IL 指令 | 说明 |
|---------|---------|------|
| `new MyClass()` | `newobj` | 引用类型堆分配 + 构造函数 |
| `new Point(x, y)` | `newobj` / `call` | 值类型 + 带参构造函数 |
| `new Point()` | `initobj` | 值类型零初始化 |
| `default(Point)` | `initobj` | 值类型零初始化 |
| `"hello"` | `ldstr` | 字符串驻留 |
| `new int[10]` | `newarr` | 一维数组 |
| `new int[3, 4]` | `newobj` | 多维数组 |

---

## 九、指令速查表

| 指令 | 操作码 | 栈行为 | 说明 |
|------|--------|--------|------|
| `newobj` | 0x73 | …, args → …, obj | 创建对象并调用构造函数 |
| `initobj` | 0xFE 0x16 | …, addr → … | 零初始化值类型 |
| `sizeof` | 0xFE 0x1C | … → …, int32 | 获取值类型字节大小 |
| `newarr` | 0x8D | …, numElems → …, array | 创建一维零基数组 |
| `ldstr` | 0x72 | … → …, string | 加载字符串字面量 |

---

## 十、面试要点

::: tip 面试高频问题
1. **new 创建引用类型和值类型在 IL 层的区别？**
   - 引用类型：`newobj` 在 GC 堆分配 + 构造函数；值类型：`newobj`/`call` 栈上初始化 + 构造函数，或 `initobj` 零初始化

2. **newobj 和 initobj 的区别？**
   - `newobj` 分配内存 + 调用构造函数；`initobj` 仅零初始化，不调用构造函数；`initobj` 更快但只能初始化为默认值

3. **C# 的 default(T) 编译成什么 IL？**
   - 值类型 → `initobj`；引用类型 → `ldnull`

4. **字符串为什么用 ldstr 而不是 newobj？**
   - 字符串驻留优化：相同内容的字面量共享同一实例，`ldstr` 自动查找驻留池

5. **一维数组和多维数组的 IL 指令区别？**
   - 一维零基数组 → `newarr`（JIT 优化，更快）；多维数组 → `newobj`（无特殊优化）

6. **sizeof 和 Marshal.SizeOf 的区别？**
   - `sizeof`（IL 指令）返回托管大小含对齐；`Marshal.SizeOf` 返回非托管互操作大小，受 `StructLayout` 影响，二者可能不同
:::

---

## 参考资料

- [OpCodes.Newobj Field](https://learn.microsoft.com/en-us/dotnet/api/system.reflection.emit.opcodes.newobj)
- [OpCodes.Initobj Field](https://learn.microsoft.com/en-us/dotnet/api/system.reflection.emit.opcodes.initobj)
- [OpCodes.Sizeof Field](https://learn.microsoft.com/en-us/dotnet/api/system.reflection.emit.opcodes.sizeof)
- [OpCodes.Newarr Field](https://learn.microsoft.com/en-us/dotnet/api/system.reflection.emit.opcodes.newarr)
- [OpCodes.Ldstr Field](https://learn.microsoft.com/en-us/dotnet/api/system.reflection.emit.opcodes.ldstr)
- [ECMA-335 Partition III - Object model instructions](https://ecma-international.org/publications-and-standards/standards/ecma-335/)
