---
title: 业务数据处理实战
icon: fa6-solid:briefcase
order: 1
category:
  - CSharp
  - Linq
tag:
  - 业务查询
  - 数据分析
  - 报表
  - 订单处理
  - 权限过滤
---

# 业务数据处理实战

LINQ 最大的价值不在于语法优雅，而在于它能把日常业务中"遍历 + 判断 + 累加"的样板代码压缩成一条声明式管道。本章用四个真实业务域——电商订单、ERP 数据、后台管理、ETL 清洗——展示 LINQ 如何解决实际问题。

## 一、电商订单分析

### 数据模型

```csharp
public enum OrderStatus { Pending, Paid, Shipped, Delivered, Cancelled }

public class Order
{
    public int OrderId { get; set; }
    public int CustomerId { get; set; }
    public string CustomerName { get; set; } = "";
    public DateTime OrderDate { get; set; }
    public OrderStatus Status { get; set; }
    public List<OrderItem> Items { get; set; } = new();
    public decimal TotalAmount => Items.Sum(i => i.LineTotal);
}

public class OrderItem
{
    public int ProductId { get; set; }
    public string ProductName { get; set; } = "";
    public int Quantity { get; set; }
    public decimal UnitPrice { get; set; }
    public decimal LineTotal => Quantity * UnitPrice;
}
```

### 案例1：按时间范围筛选订单

需求：查询 2025 年 Q1 已付款及之后状态的订单。

```csharp
var start = new DateTime(2025, 1, 1);
var end = new DateTime(2025, 4, 1);

var q1Orders = orders
    .Where(o => o.OrderDate >= start && o.OrderDate < end)
    .Where(o => o.Status >= OrderStatus.Paid)
    .OrderByDescending(o => o.OrderDate)
    .ToList();

Console.WriteLine($"Q1 有效订单：{q1Orders.Count} 笔，合计 {q1Orders.Sum(o => o.TotalAmount):C}");
```

要点：两个 `Where` 分开写比合并为一个更清晰——日期条件和状态条件是两个独立维度。

### 案例2：客户消费排名

需求：按消费总额排名前 10 客户。

```csharp
var topCustomers = orders
    .Where(o => o.Status != OrderStatus.Cancelled)
    .GroupBy(o => new { o.CustomerId, o.CustomerName })
    .Select(g => new
    {
        g.Key.CustomerId,
        g.Key.CustomerName,
        TotalSpent = g.Sum(o => o.TotalAmount),
        OrderCount = g.Count(),
        AvgOrderValue = g.Average(o => o.TotalAmount)
    })
    .OrderByDescending(c => c.TotalSpent)
    .Take(10)
    .ToList();

foreach (var c in topCustomers)
{
    Console.WriteLine($"{c.CustomerName,-12} 总消费 {c.TotalSpent,10:C}  " +
                      $"{c.OrderCount} 单  客单价 {c.AvgOrderValue:C}");
}
```

要点：`GroupBy` 匿名类型做复合键 → `Select` 投影聚合 → `OrderByDescending` 排名 → `Take` 截取。

### 案例3：商品销售排行榜

需求：统计各商品销量和销售额，按销售额降序。

```csharp
var productRanking = orders
    .Where(o => o.Status != OrderStatus.Cancelled)
    .SelectMany(o => o.Items, (order, item) => new { order.OrderDate, item })
    .GroupBy(x => new { x.item.ProductId, x.item.ProductName })
    .Select(g => new
    {
        g.Key.ProductId,
        g.Key.ProductName,
        TotalQuantity = g.Sum(x => x.item.Quantity),
        TotalRevenue = g.Sum(x => x.item.LineTotal),
        OrderCount = g.Select(x => x.OrderDate).Distinct().Count()
    })
    .OrderByDescending(p => p.TotalRevenue)
    .Take(20)
    .ToList();
```

要点：`SelectMany` 是关键——先把订单明细"展平"为行级数据，再按商品聚合。

### 案例4：月度销售趋势

需求：按年月聚合销售额，观察趋势。

```csharp
var monthlyTrend = orders
    .Where(o => o.Status != OrderStatus.Cancelled)
    .GroupBy(o => new { o.OrderDate.Year, o.OrderDate.Month })
    .Select(g => new
    {
        g.Key.Year,
        g.Key.Month,
        Revenue = g.Sum(o => o.TotalAmount),
        OrderCount = g.Count(),
        AvgValue = Math.Round(g.Average(o => o.TotalAmount), 2)
    })
    .OrderBy(t => t.Year).ThenBy(t => t.Month)
    .ToList();

// 环比增长率
foreach (var t in monthlyTrend)
{
    var label = $"{t.Year}-{t.Month:D2}";
    Console.WriteLine($"{label}  营收 {t.Revenue,12:C}  {t.OrderCount} 单  均价 {t.AvgValue:C}");
}
```

### 案例5：客单价分析

需求：按客户分组，计算客单价，并与全局均值对比。

```csharp
var globalAvg = orders
    .Where(o => o.Status != OrderStatus.Cancelled)
    .Average(o => o.TotalAmount);

var customerAnalysis = orders
    .Where(o => o.Status != OrderStatus.Cancelled)
    .GroupBy(o => o.CustomerName)
    .Select(g => new
    {
        Customer = g.Key,
        AvgValue = g.Average(o => o.TotalAmount),
        DiffFromGlobal = g.Average(o => o.TotalAmount) - globalAvg,
        Level = g.Average(o => o.TotalAmount) switch
        {
            > 500 => "高价值",
            > 200 => "中价值",
            _ => "低价值"
        }
    })
    .OrderByDescending(c => c.AvgValue)
    .ToList();
```

## 二、ERP 数据处理

### 数据模型

```csharp
public class BomItem
{
    public int ParentId { get; set; }
    public int ChildId { get; set; }
    public string ChildName { get; set; } = "";
    public decimal Quantity { get; set; }
    public List<BomItem> Children { get; set; } = new();
}

public class InventoryRecord
{
    public string Warehouse { get; set; } = "";
    public string Sku { get; set; } = "";
    public int Quantity { get; set; }
    public string Location { get; set; } = "";
}

public class PurchaseLine
{
    public int LineId { get; set; }
    public string Sku { get; set; } = "";
    public int Quantity { get; set; }
    public decimal UnitPrice { get; set; }
}

public class Receivable
{
    public int CustomerId { get; set; }
    public string CustomerName { get; set; } = "";
    public decimal Amount { get; set; }
    public DateTime DueDate { get; set; }
    public bool IsPaid { get; set; }
}
```

### 案例1：BOM 展开（递归展平）

需求：将多级 BOM 树展开为所有末级物料清单。

```csharp
public static IEnumerable<BomItem> FlattenBom(BomItem root)
{
    yield return root;
    foreach (var child in root.Children.SelectMany(FlattenBom))
        yield return child;
}

// 使用
var allMaterials = FlattenBom(rootBom)
    .Where(b => b.Children.Count == 0)  // 只取末级
    .GroupBy(b => new { b.ChildId, b.ChildName })
    .Select(g => new
    {
        g.Key.ChildId,
        g.Key.ChildName,
        TotalQty = g.Sum(x => x.Quantity)
    })
    .ToList();
```

### 案例2：库存汇总

需求：按 SKU 汇总各仓库的库存。

```csharp
var inventorySummary = inventoryRecords
    .GroupBy(r => r.Sku)
    .Select(g => new
    {
        Sku = g.Key,
        TotalQty = g.Sum(r => r.Quantity),
        Warehouses = g.Select(r => new { r.Warehouse, r.Quantity, r.Location })
            .OrderByDescending(w => w.Quantity)
            .ToList()
    })
    .OrderBy(s => s.Sku)
    .ToList();
```

### 案例3：采购单比对

需求：比对新旧采购单差异——新增行、删除行、变更行。

```csharp
var oldDict = oldLines.ToDictionary(l => l.Sku);
var newDict = newLines.ToDictionary(l => l.Sku);

var added = newLines
    .Where(l => !oldDict.ContainsKey(l.Sku))
    .ToList();

var removed = oldLines
    .Where(l => !newDict.ContainsKey(l.Sku))
    .ToList();

var changed = newLines
    .Where(l => oldDict.TryGetValue(l.Sku, out var old)
             && (old.Quantity != l.Quantity || old.UnitPrice != l.UnitPrice))
    .Select(l => new
    {
        l.Sku,
        OldQty = oldDict[l.Sku].Quantity,
        NewQty = l.Quantity,
        OldPrice = oldDict[l.Sku].UnitPrice,
        NewPrice = l.UnitPrice
    })
    .ToList();
```

### 案例4：MRP 简易计算

需求：根据需求量和现有库存计算采购建议。

```csharp
var purchaseSuggestions = demandItems
    .GroupBy(d => d.Sku)
    .Select(g => new
    {
        Sku = g.Key,
        DemandQty = g.Sum(d => d.Quantity),
        StockQty = inventoryBySku.TryGetValue(g.Key, out var inv) ? inv : 0
    })
    .Select(x => new
    {
        x.Sku,
        x.DemandQty,
        x.StockQty,
        Shortage = Math.Max(0, x.DemandQty - x.StockQty),
        Suggestion = x.DemandQty > x.StockQty ? "需采购" : "库存充足"
    })
    .Where(x => x.Shortage > 0)
    .OrderByDescending(x => x.Shortage)
    .ToList();
```

### 案例5：应收账龄分析

需求：按账龄区间统计未收金额。

```csharp
var today = DateTime.Today;

var agingAnalysis = receivables
    .Where(r => !r.IsPaid)
    .Select(r => new
    {
        r.CustomerId,
        r.CustomerName,
        r.Amount,
        OverdueDays = (today - r.DueDate).Days,
        AgingBucket = (today - r.DueDate).Days switch
        {
            <= 0 => "未到期",
            <= 30 => "0-30天",
            <= 60 => "31-60天",
            <= 90 => "61-90天",
            _ => "90天以上"
        }
    })
    .GroupBy(r => r.AgingBucket)
    .Select(g => new
    {
        AgingBucket = g.Key,
        TotalAmount = g.Sum(r => r.Amount),
        Count = g.Count(),
        Customers = g.Select(r => r.CustomerName).Distinct().Count()
    })
    .OrderBy(g => g.AgingBucket)
    .ToList();
```

## 三、后台管理系统

### 数据模型

```csharp
public class User
{
    public int UserId { get; set; }
    public string UserName { get; set; } = "";
    public string Role { get; set; } = "";  // Admin, Manager, User
    public int DepartmentId { get; set; }
}

public class AuditLog
{
    public int LogId { get; set; }
    public int UserId { get; set; }
    public string Action { get; set; } = "";
    public string Module { get; set; } = "";
    public DateTime CreatedAt { get; set; }
    public string Detail { get; set; } = "";
}

public class DropdownItem
{
    public string Value { get; set; } = "";
    public string Text { get; set; } = "";
}
```

### 案例1：多条件动态查询

需求：根据前端传入的多个可选条件，动态构建查询。

```csharp
public static IQueryable<Order> BuildQuery(
    IQueryable<Order> source,
    DateTime? startDate,
    DateTime? endDate,
    OrderStatus? status,
    string? keyword)
{
    var query = source;

    if (startDate.HasValue)
        query = query.Where(o => o.OrderDate >= startDate.Value);

    if (endDate.HasValue)
        query = query.Where(o => o.OrderDate < endDate.Value);

    if (status.HasValue)
        query = query.Where(o => o.Status == status.Value);

    if (!string.IsNullOrWhiteSpace(keyword))
        query = query.Where(o => o.CustomerName.Contains(keyword));

    return query;
}
```

更灵活的方式——使用 Expression 动态组合：

```csharp
public static Expression<Func<T, bool>> And<T>(
    this Expression<Func<T, bool>> left,
    Expression<Func<T, bool>> right)
{
    var param = Expression.Parameter(typeof(T));
    var body = Expression.AndAlso(
        Expression.Invoke(left, param),
        Expression.Invoke(right, param));
    return Expression.Lambda<Func<T, bool>>(body, param);
}

// 使用
Expression<Func<Order, bool>> predicate = o => true;

if (startDate.HasValue)
    predicate = predicate.And(o => o.OrderDate >= startDate.Value);
if (status.HasValue)
    predicate = predicate.And(o => o.Status == status.Value);

var result = orders.AsQueryable().Where(predicate).ToList();
```

### 案例2：权限过滤

需求：不同角色只能看到对应范围的数据。

```csharp
public static IQueryable<Order> ApplyPermission(
    IQueryable<Order> query,
    User currentUser,
    List<Department> departments)
{
    return currentUser.Role switch
    {
        "Admin" => query,  // 管理员看全部
        "Manager" => query.Where(o =>
            departments.Any(d => d.DepartmentId == currentUser.DepartmentId
                             && d.CustomerIds.Contains(o.CustomerId))),
        _ => query.Where(o => o.CustomerId == currentUser.UserId)
    };
}
```

### 案例3：数据导出

需求：查询数据并投影为导出格式，支持分页。

```csharp
public class OrderExportRow
{
    public string OrderId { get; set; } = "";
    public string Customer { get; set; } = "";
    public string Date { get; set; } = "";
    public string Status { get; set; } = "";
    public string Amount { get; set; } = "";
}

var exportData = orders
    .Where(o => o.Status != OrderStatus.Cancelled)
    .OrderByDescending(o => o.OrderDate)
    .Select(o => new OrderExportRow
    {
        OrderId = o.OrderId.ToString(),
        Customer = o.CustomerName,
        Date = o.OrderDate.ToString("yyyy-MM-dd"),
        Status = o.Status.ToString(),
        Amount = o.TotalAmount.ToString("N2")
    })
    .Skip((pageIndex - 1) * pageSize)
    .Take(pageSize)
    .ToList();
```

### 案例4：操作日志统计

需求：按模块和日期维度统计操作次数。

```csharp
var logStats = auditLogs
    .Where(l => l.CreatedAt >= DateTime.Today.AddDays(-30))
    .GroupBy(l => new { l.Module, l.CreatedAt.Date })
    .Select(g => new
    {
        g.Key.Module,
        Date = g.Key.Date,
        ActionCount = g.Count(),
        UniqueUsers = g.Select(l => l.UserId).Distinct().Count(),
        TopAction = g.GroupBy(l => l.Action)
                     .OrderByDescending(a => a.Count())
                     .First().Key
    })
    .OrderBy(s => s.Module).ThenBy(s => s.Date)
    .ToList();
```

### 案例5：下拉列表数据源

需求：从数据表生成前端下拉列表选项。

```csharp
// 产品下拉
var productOptions = products
    .Where(p => p.IsActive)
    .OrderBy(p => p.Category).ThenBy(p => p.Name)
    .Select(p => new DropdownItem
    {
        Value = p.ProductId.ToString(),
        Text = $"{p.Category} - {p.Name}"
    })
    .ToList();

// 去重的分类下拉
var categoryOptions = products
    .Select(p => p.Category)
    .Distinct()
    .OrderBy(c => c)
    .Select(c => new DropdownItem { Value = c, Text = c })
    .ToList();
```

## 四、数据清洗与转换（ETL）

### 数据模型

```csharp
public class RawRecord
{
    public string Id { get; set; } = "";
    public string Name { get; set; } = "";
    public string Email { get; set; } = "";
    public string Phone { get; set; } = "";
    public string Region { get; set; } = "";
    public DateTime? CreatedAt { get; set; }
}

public class CleanRecord
{
    public string Id { get; set; } = "";
    public string Name { get; set; } = "";
    public string Email { get; set; } = "";
    public string Phone { get; set; } = "";
    public string Region { get; set; } = "";
    public DateTime CreatedAt { get; set; }
    public string Source { get; set; } = "";
}
```

### 案例1：数据去重

需求：按 Id 去重，保留最新记录。

```csharp
// .NET 6+ 可用 DistinctBy
var deduped = rawRecords
    .OrderByDescending(r => r.CreatedAt)
    .DistinctBy(r => r.Id)
    .ToList();

// .NET 6 以下
var dedupedLegacy = rawRecords
    .GroupBy(r => r.Id)
    .Select(g => g.OrderByDescending(r => r.CreatedAt).First())
    .ToList();
```

### 案例2：数据校验

需求：找出不符合规则的记录。

```csharp
var invalidRecords = rawRecords
    .Select((r, idx) => new { Record = r, Line = idx + 1 })
    .Where(x => string.IsNullOrWhiteSpace(x.Record.Id)
             || string.IsNullOrWhiteSpace(x.Record.Name)
             || !x.Record.Email.Contains('@'))
    .Select(x => new
    {
        x.Line,
        x.Record.Id,
        Errors = new[]
        {
            string.IsNullOrWhiteSpace(x.Record.Id) ? "Id为空" : "",
            string.IsNullOrWhiteSpace(x.Record.Name) ? "名称为空" : "",
            !x.Record.Email.Contains('@') ? "邮箱格式错误" : ""
        }.Where(e => e != "").ToList()
    })
    .ToList();
```

### 案例3：数据格式转换

需求：将原始数据转换为目标格式，标准化字段。

```csharp
var cleaned = rawRecords
    .Where(r => !string.IsNullOrWhiteSpace(r.Id))
    .Select(r => new CleanRecord
    {
        Id = r.Id.Trim(),
        Name = r.Name?.Trim() ?? "",
        Email = r.Email?.Trim().ToLowerInvariant() ?? "",
        Phone = r.Phone?.Trim() ?? "",
        Region = string.IsNullOrWhiteSpace(r.Region) ? "未知" : r.Region.Trim(),
        CreatedAt = r.CreatedAt ?? DateTime.MinValue,
        Source = "Import"
    })
    .ToList();
```

### 案例4：多源数据合并

需求：合并来自不同系统的数据，去重排序。

```csharp
var merged = systemARecords
    .Select(r => new CleanRecord
    {
        Id = $"A-{r.Id}", Name = r.Name, Email = r.Email,
        Phone = r.Phone ?? "", Region = r.Region ?? "未知",
        CreatedAt = r.CreatedAt, Source = "SystemA"
    })
    .Union(systemBRecords.Select(r => new CleanRecord
    {
        Id = $"B-{r.Id}", Name = r.Name, Email = r.Email,
        Phone = r.Phone ?? "", Region = r.Region ?? "未知",
        CreatedAt = r.CreatedAt, Source = "SystemB"
    }))
    .OrderBy(r => r.Region).ThenBy(r => r.Name)
    .ToList();
```

### 案例5：缺失数据填充

需求：用默认值填充缺失字段。

```csharp
var defaults = new Dictionary<string, string>
{
    ["Region"] = "华东",
    ["Phone"] = "000-00000000"
};

var filled = rawRecords
    .Select(r => new CleanRecord
    {
        Id = r.Id,
        Name = r.Name,
        Email = r.Email,
        Phone = string.IsNullOrWhiteSpace(r.Phone) ? defaults["Phone"] : r.Phone,
        Region = string.IsNullOrWhiteSpace(r.Region) ? defaults["Region"] : r.Region,
        CreatedAt = r.CreatedAt ?? DateTime.Today,
        Source = "Filled"
    })
    .ToList();

// 使用 DefaultIfEmpty 处理空序列
var emptyCheck = new List<RawRecord>();
var withDefault = emptyCheck
    .DefaultIfEmpty(new RawRecord { Id = "0", Name = "无数据" })
    .ToList();
```

## 小结

| 业务域 | 核心操作 | 典型模式 |
|--------|---------|---------|
| 电商订单 | SelectMany + GroupBy + 聚合 | 展平→分组→汇总 |
| ERP 数据 | 递归展平 + 字典查找 + 集合比对 | 展开→聚合→差异 |
| 后台管理 | 动态Where + 投影Select + 分页 | 过滤→投影→分页 |
| ETL 清洗 | Distinct + 校验Where + 格式Select | 校验→转换→合并 |

关键心得：

1. **SelectMany 是处理嵌套数据的利器**——订单明细、BOM 子件，都需要先展平再聚合
2. **GroupBy + Select 是分析型查询的标准范式**——分组后投影聚合值，几乎覆盖所有统计场景
3. **动态查询用条件式 Where 叠加**——比拼装 SQL 字符串安全，比 Expression 树简单
4. **数据清洗永远是 Where → Select 的管道**——先过滤异常，再投影为目标格式
