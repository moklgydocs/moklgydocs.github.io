---
title: Kestrel HTTP 服务器
date: 2025-04-09
category:
  - ASP.NET_Core
tag:
  - asp.netcore
  - Kestrel
  - HTTP服务器
  - 源码分析
author: Moklgy
order: 9
---

# ASP.NET Core Kestrel HTTP 服务器：源码级深入

---

## 一、核心认知

> **Kestrel 的本质就是一个"TCP 连接管理器 + HTTP 协议解析器"：它监听 TCP 端口，接收原始字节流，将其解析为 HTTP 请求对象，然后把请求交给中间件管道处理，最后把响应序列化为字节流写回网络。**

```
网络层面发生了什么：

客户端浏览器                                    Kestrel 服务器
    │                                              │
    │  TCP 三次握手                                  │
    │  ─────────────────────────────────────────►   │
    │  ◄─────────────────────────────────────────   │
    │  ─────────────────────────────────────────►   │
    │                                              │
    │  发送 HTTP 请求（原始字节）                     │
    │  "GET /api/users/123 HTTP/1.1\r\n"           │
    │  "Host: sso.example.com\r\n"                 │
    │  "Authorization: Bearer eyJhbG...\r\n"       │
    │  "\r\n"                                      │
    │  ─────────────────────────────────────────►   │
    │                                              │
    │                   Kestrel 内部：               │
    │                   1. 读取字节流                 │
    │                   2. 解析 HTTP 协议             │
    │                   3. 创建 HttpContext           │
    │                   4. 执行中间件管道              │
    │                   5. 序列化响应                 │
    │                                              │
    │  接收 HTTP 响应（原始字节）                     │
    │  "HTTP/1.1 200 OK\r\n"                       │
    │  "Content-Type: application/json\r\n"        │
    │  "Content-Length: 42\r\n"                     │
    │  "\r\n"                                      │
    │  "{\"id\":123,\"name\":\"zhangsan\"}"        │
    │  ◄─────────────────────────────────────────   │
    │                                              │
    │  TCP 连接可能保持（Keep-Alive）                │
    │  下次请求复用同一个TCP连接                     │
```

---

## 二、Kestrel 在整体架构中的位置

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        Kestrel 架构全景                                   │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                      网络层                                        │  │
│  │                                                                    │  │
│  │  操作系统                                                          │  │
│  │  ├── TCP Socket                                                   │  │
│  │  ├── TLS/SSL (HTTPS)                                              │  │
│  │  └── 可选: Unix Domain Socket, Named Pipe                        │  │
│  │                                                                    │  │
│  │  传输层抽象 (IConnectionListenerFactory)                           │  │
│  │  ├── SocketTransport (默认, 基于 Socket)                          │  │
│  │  └── 可选: LibuvTransport (旧版)                                  │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                              │                                           │
│                              ▼                                           │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                    连接管理层                                       │  │
│  │                                                                    │  │
│  │  ConnectionManager                                                 │  │
│  │  ├── 管理所有活跃 TCP 连接                                          │  │
│  │  ├── 连接超时管理                                                   │  │
│  │  └── 优雅关闭（Graceful Shutdown）                                 │  │
│  │                                                                    │  │
│  │  Connection Middleware Pipeline                                    │  │
│  │  ├── TlsConnectionMiddleware (HTTPS)                              │  │
│  │  └── HttpConnectionMiddleware                                     │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                              │                                           │
│                              ▼                                           │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                    HTTP 协议层                                      │  │
│  │                                                                    │  │
│  │  HTTP/1.1:  Http1Connection                                       │  │
│  │  HTTP/2:    Http2Connection                                       │  │
│  │  HTTP/3:    Http3Connection (基于QUIC)                            │  │
│  │                                                                    │  │
│  │  ├── 请求行解析 (Method, Path, Version)                            │  │
│  │  ├── 请求头解析 (Headers)                                          │  │
│  │  ├── 请求体读取 (Body)                                             │  │
│  │  ├── 响应写入                                                      │  │
│  │  └── 连接复用 (Keep-Alive / Multiplexing)                         │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                              │                                           │
│                              ▼                                           │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                    应用层                                           │  │
│  │                                                                    │  │
│  │  HostingApplication                                                │  │
│  │  ├── 创建 HttpContext                                              │  │
│  │  ├── 创建 DI Scope                                                 │  │
│  │  ├── 执行中间件管道 (RequestDelegate)                               │  │
│  │  └── 释放 DI Scope                                                 │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  📁 源码仓库: dotnet/aspnetcore                                          │
│  📁 路径: src/Servers/Kestrel/                                           │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 三、核心接口

```csharp
// ========== 服务器接口 ==========
// 📁 源码位置: src/Hosting/Abstractions/src/IServer.cs

public interface IServer : IDisposable
{
    // 🔥 服务器能力特性集合
    IFeatureCollection Features { get; }
    
    // 🔥 启动服务器
    Task StartAsync<TContext>(
        IHttpApplication<TContext> application,  // 应用处理逻辑
        CancellationToken cancellationToken) where TContext : notnull;
    
    // 🔥 停止服务器
    Task StopAsync(CancellationToken cancellationToken);
}

// Kestrel 实现了 IServer
// KestrelServerImpl : IServer

// ========== 应用接口 ==========
// 📁 源码位置: src/Hosting/Abstractions/src/IHttpApplication.cs

public interface IHttpApplication<TContext>
{
    // 🔥 为每个请求创建上下文
    TContext CreateContext(IFeatureCollection contextFeatures);
    
    // 🔥 处理请求（执行中间件管道）
    Task ProcessRequestAsync(TContext context);
    
    // 🔥 请求完成后清理
    void DisposeContext(TContext context, Exception? exception);
}

// HostingApplication 实现了 IHttpApplication<HostingApplication.Context>
```

```
关系图：

GenericWebHostService（宿主服务）
    │
    │  StartAsync()
    │
    ├── ApplicationBuilder.Build()  → RequestDelegate（中间件管道）
    │
    ├── 创建 HostingApplication（包装 RequestDelegate）
    │
    └── KestrelServer.StartAsync(hostingApplication)
        │
        │  对每个请求：
        │
        ├── hostingApplication.CreateContext(features)
        │   → 创建 HttpContext + DI Scope
        │
        ├── hostingApplication.ProcessRequestAsync(context)
        │   → 执行 RequestDelegate（中间件管道）
        │
        └── hostingApplication.DisposeContext(context)
            → 释放 DI Scope
```

---

## 四、KestrelServer 启动流程

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/KestrelServerImpl.cs

internal sealed class KestrelServerImpl : IServer
{
    private readonly ServerAddressesFeature _serverAddresses;
    private readonly IConnectionListenerFactory _transportFactory;

    public async Task StartAsync<TContext>(
        IHttpApplication<TContext> application,
        CancellationToken cancellationToken)
        where TContext : notnull
    {
        // 🔥 第1步：解析监听地址
        var addresses = _serverAddresses.Addresses;
        // 例如: ["http://localhost:5000", "https://localhost:5001"]
        // 或来自配置: builder.WebHost.UseUrls("http://*:8080")

        // 🔥 第2步：对每个地址创建监听器
        foreach (var address in _bindAddresses)
        {
            var endpoint = ParseAddress(address);
            // "http://localhost:5000" → IPEndPoint(127.0.0.1, 5000)
            // "https://*:443" → IPEndPoint(0.0.0.0, 443)
            
            await BindAsync(endpoint, application, cancellationToken);
        }
    }

    private async Task BindAsync<TContext>(
        EndpointConfig endpointConfig,
        IHttpApplication<TContext> application,
        CancellationToken cancellationToken)
    {
        // 🔥 第3步：创建传输层监听器
        var transport = await _transportFactory.BindAsync(
            endpointConfig.EndPoint, 
            cancellationToken);
        // SocketTransportFactory 
        //   → 创建 Socket
        //   → Socket.Bind(endpoint) 
        //   → Socket.Listen(backlog)

        // 🔥 第4步：构建连接中间件管道
        var connectionDelegate = BuildConnectionPipeline(endpointConfig);
        // 连接中间件管道（和请求中间件管道类似，但是连接级别的）：
        //   TLS Middleware → HTTP Protocol Middleware

        // 🔥 第5步：开始接受连接
        StartAcceptLoop(transport, connectionDelegate, application);
    }

    private void StartAcceptLoop<TContext>(
        IConnectionListener transport,
        ConnectionDelegate connectionDelegate,
        IHttpApplication<TContext> application)
    {
        // 🔥 在后台线程中循环接受新连接
        _ = AcceptConnectionsAsync();
        
        async Task AcceptConnectionsAsync()
        {
            while (true)
            {
                // 等待新的 TCP 连接
                var connection = await transport.AcceptAsync();
                
                if (connection == null)
                    break; // 服务器正在关闭
                
                // 🔥 为每个连接启动处理任务
                // 不 await，允许同时处理多个连接
                _ = ProcessConnectionAsync(connection, connectionDelegate);
            }
        }
    }

    private async Task ProcessConnectionAsync(
        ConnectionContext connection,
        ConnectionDelegate connectionDelegate)
    {
        try
        {
            _connectionManager.AddConnection(connection);
            
            // 🔥 执行连接中间件管道
            await connectionDelegate(connection);
            // TLS → HTTP 协议处理
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Connection {id} error", connection.ConnectionId);
        }
        finally
        {
            _connectionManager.RemoveConnection(connection);
            await connection.DisposeAsync();
        }
    }
}
```

**启动流程时序图：**

```
app.Run() / Host.StartAsync()
    │
    ▼
GenericWebHostService.StartAsync()
    │
    ├── 1. 构建中间件管道
    │   ApplicationBuilder.Build() → RequestDelegate
    │
    ├── 2. 创建 HostingApplication(requestDelegate)
    │
    └── 3. KestrelServer.StartAsync(hostingApplication)
        │
        ├── 解析地址: "https://localhost:5001"
        │
        ├── SocketTransportFactory.BindAsync(IPEndPoint(127.0.0.1, 5001))
        │   │
        │   └── Socket socket = new Socket(AddressFamily.InterNetwork, ...);
        │       socket.Bind(new IPEndPoint(IPAddress.Loopback, 5001));
        │       socket.Listen(512);  // backlog = 512
        │       → 此时操作系统开始接受 TCP 连接
        │
        └── 开始 Accept 循环
            │
            │  while (true)
            │  {
            │      var connection = await transport.AcceptAsync();
            │      // 底层: Socket.AcceptAsync()
            │      // 阻塞直到新TCP连接到达
            │      
            │      _ = ProcessConnectionAsync(connection);
            │      // 不等待，立即回去接受下一个连接
            │  }
            │
            ▼
        服务器就绪，等待请求...
        
        控制台输出：
        info: Microsoft.Hosting.Lifetime
              Now listening on: https://localhost:5001
        info: Microsoft.Hosting.Lifetime
              Application started. Press Ctrl+C to shut down.
```

---

## 五、连接处理 —— 从 TCP 到 HTTP

### 5.1 连接中间件管道

```csharp
// Kestrel 有两层管道：
// 1. 连接级别管道（Connection Middleware）—— 处理TCP连接
// 2. 请求级别管道（Request Middleware）—— 处理HTTP请求（就是我们学过的中间件管道）

// 连接级别管道：
//
// TCP 连接到达
//     │
//     ▼
// [连接中间件1] TLS Handshake (HTTPS)
//     │  如果是HTTPS连接，在这里进行TLS握手
//     │  协商加密算法，验证证书
//     │  之后的数据都是加密传输
//     ▼
// [连接中间件2] HTTP Version Detection
//     │  检测HTTP协议版本
//     │  HTTP/1.1 → Http1Connection
//     │  HTTP/2   → Http2Connection（通过ALPN协商）
//     │  HTTP/3   → Http3Connection（基于QUIC）
//     ▼
// [HTTP处理] 根据协议版本解析请求
//     │  解析请求行、请求头、请求体
//     │  创建 HttpContext
//     │  执行请求中间件管道
//     │  写回响应
```

### 5.2 HttpConnectionMiddleware

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/Middleware/HttpConnectionMiddleware.cs

internal sealed class HttpConnectionMiddleware<TContext> where TContext : notnull
{
    private readonly IHttpApplication<TContext> _application;

    public Task OnConnectionAsync(ConnectionContext connectionContext)
    {
        // 🔥 创建 HttpConnection
        var httpConnection = new HttpConnection(connectionContext);
        
        // 🔥 开始处理HTTP请求
        return httpConnection.ProcessRequestsAsync(_application);
    }
}
```

### 5.3 HttpConnection —— HTTP 协议协商

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/Internal/HttpConnection.cs

internal sealed class HttpConnection : ITimeoutHandler
{
    public async Task ProcessRequestsAsync<TContext>(IHttpApplication<TContext> application)
    {
        // 🔥 判断HTTP协议版本
        
        if (_context.Protocols.HasFlag(HttpProtocols.Http2) && 
            IsHttp2Connection())
        {
            // HTTP/2 连接
            var http2Connection = new Http2Connection(_context);
            await http2Connection.ProcessRequestsAsync(application);
        }
        else
        {
            // HTTP/1.1 连接（最常见）
            var http1Connection = new Http1Connection<TContext>(_context, application);
            await http1Connection.ProcessRequestsAsync();
        }
    }
}
```

---

## 六、Http1Connection —— HTTP/1.1 请求处理核心

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/Internal/Http/Http1Connection.cs

internal sealed partial class Http1Connection : HttpProtocol, IRequestProcessor
{
    // 🔥 循环处理同一TCP连接上的多个请求（Keep-Alive）
    public async Task ProcessRequestsAsync()
    {
        while (true)
        {
            // ====================================
            // 🔥 第1阶段：解析请求行
            // "GET /api/users/123 HTTP/1.1\r\n"
            // ====================================
            var readResult = await Input.ReadAsync();
            var buffer = readResult.Buffer;
            
            if (!TryParseRequestLine(buffer, out var consumed, out var examined))
            {
                if (readResult.IsCompleted)
                    return; // 连接关闭
                continue;   // 数据不完整，继续读取
            }
            
            // 解析结果：
            // Method = "GET"
            // Path = "/api/users/123"
            // HttpVersion = Http11
            
            Input.AdvanceTo(consumed, examined);

            // ====================================
            // 🔥 第2阶段：解析请求头
            // "Host: sso.example.com\r\n"
            // "Authorization: Bearer eyJhbG...\r\n"
            // "Accept: application/json\r\n"
            // "\r\n"  ← 空行表示头结束
            // ====================================
            while (true)
            {
                readResult = await Input.ReadAsync();
                buffer = readResult.Buffer;
                
                if (TryParseHeaders(buffer, out consumed, out examined))
                    break; // 所有请求头解析完毕
                    
                Input.AdvanceTo(consumed, examined);
            }

            // ====================================
            // 🔥 第3阶段：处理请求
            // ====================================
            var messageBody = CreateMessageBody();
            // 根据 Content-Length 或 Transfer-Encoding
            // 创建请求体读取器
            
            // 🔥🔥🔥 调用应用层处理请求
            await _application.ProcessRequestAsync(_context);
            // HostingApplication.ProcessRequestAsync()
            //   → 创建 HttpContext
            //   → 创建 DI Scope
            //   → 执行中间件管道
            //   → 释放 DI Scope
            
            // ====================================
            // 🔥 第4阶段：完成响应
            // ====================================
            await FlushResponseAsync();
            
            // ====================================
            // 🔥 第5阶段：检查是否 Keep-Alive
            // ====================================
            if (!_keepAlive)
            {
                return; // 关闭连接
            }
            
            // Keep-Alive → 重置状态，准备处理下一个请求
            Reset();
            
            // 回到循环开头，等待下一个请求
        }
    }
}
```

### 6.1 请求行解析

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/Internal/Http/HttpParser.cs

internal sealed class HttpParser<TRequestHandler> : IHttpParser<TRequestHandler>
    where TRequestHandler : IHttpHeadersHandler, IHttpRequestLineHandler
{
    public bool ParseRequestLine(
        TRequestHandler handler, 
        ref SequenceReader<byte> reader)
    {
        // 原始字节：
        // 47 45 54 20 2F 61 70 69 2F 75 73 65 72 73 2F 31 32 33 20 48 54 54 50 2F 31 2E 31 0D 0A
        // G  E  T     /  a  p  i  /  u  s  e  r  s  /  1  2  3     H  T  T  P  /  1  .  1  \r \n

        // 🔥 优化：不使用字符串分割，直接在字节级别解析
        
        // 找到第一个空格 → Method结束
        var methodEnd = reader.UnreadSpan.IndexOf((byte)' ');
        var method = reader.UnreadSpan.Slice(0, methodEnd);
        // method = "GET" (字节)
        
        // 🔥 已知HTTP方法的快速匹配
        // 不是逐字比较，而是把前几个字节作为整数比较
        if (method.Length == 3)
        {
            var methodInt = (method[0] << 16) | (method[1] << 8) | method[2];
            if (methodInt == (('G' << 16) | ('E' << 8) | 'T'))
            {
                knownMethod = HttpMethod.Get;
                // 直接识别为GET，避免字符串创建和比较
            }
        }
        
        reader.Advance(methodEnd + 1); // 跳过空格
        
        // 找到第二个空格 → Path结束
        var pathEnd = reader.UnreadSpan.IndexOf((byte)' ');
        var path = reader.UnreadSpan.Slice(0, pathEnd);
        // path = "/api/users/123" (字节)
        
        reader.Advance(pathEnd + 1);
        
        // 剩下的是 HTTP版本 + \r\n
        // "HTTP/1.1\r\n"
        var version = ParseHttpVersion(reader);

        // 通知Handler
        handler.OnStartLine(knownMethod, version, targetPath, ...);
        
        return true;
    }
}
```

### 6.2 请求头解析

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/Internal/Http/HttpParser.cs

public bool ParseHeaders(
    TRequestHandler handler,
    ref SequenceReader<byte> reader)
{
    while (true)
    {
        var span = reader.UnreadSpan;
        
        // 🔥 检查是否到达空行（\r\n）→ 头部结束
        if (span.Length >= 2 && span[0] == '\r' && span[1] == '\n')
        {
            reader.Advance(2);
            return true; // 所有头部解析完毕
        }
        
        // 找到冒号 → 分隔 Header Name 和 Value
        var colonIndex = span.IndexOf((byte)':');
        var headerName = span.Slice(0, colonIndex);
        
        // 跳过冒号后的空格
        var valueStart = colonIndex + 1;
        while (span[valueStart] == ' ') valueStart++;
        
        // 找到 \r\n → Value结束
        var lineEnd = span.IndexOf((byte)'\r');
        var headerValue = span.Slice(valueStart, lineEnd - valueStart);
        
        // 🔥 已知头部的快速匹配
        // ASP.NET Core 维护了一个已知头部名称的查找表
        // "Host"、"Content-Type"、"Authorization" 等
        // 使用长度+前几个字符的组合快速识别
        
        var knownHeader = TryMatchKnownHeader(headerName);
        
        if (knownHeader != null)
        {
            // 已知头部 → 使用预分配的字符串，避免内存分配
            handler.OnHeader(knownHeader, headerValue);
            // 例如：OnHeader(KnownHeaders.Authorization, "Bearer eyJhbG...")
        }
        else
        {
            // 未知头部 → 创建字符串
            handler.OnHeader(
                Encoding.ASCII.GetString(headerName),
                Encoding.ASCII.GetString(headerValue));
        }
        
        reader.Advance(lineEnd + 2); // 跳过 \r\n
    }
}
```

---

## 七、System.IO.Pipelines —— Kestrel 的高性能 I/O 基础

### 7.1 为什么需要 Pipelines？

```
传统的 Stream I/O 问题：

byte[] buffer = new byte[4096];
int bytesRead = await stream.ReadAsync(buffer, 0, buffer.Length);

问题1：缓冲区大小固定
  → 如果请求头超过4096字节，需要多次读取
  → 需要自己管理缓冲区拼接

问题2：每次Read都可能分配新的byte[]
  → GC压力大

问题3：Read和Parse耦合
  → 必须先Read完才能Parse
  → 无法并行读取和解析

Pipelines 的解决方案：

PipeReader reader = connection.Transport.Input;

ReadResult result = await reader.ReadAsync();
ReadOnlySequence<byte> buffer = result.Buffer;
// buffer 可能跨越多个内存段（链表），零拷贝
// 不需要自己管理缓冲区大小
// 底层使用内存池（ArrayPool），减少GC

// 解析后告诉Pipeline消费了多少
reader.AdvanceTo(consumed, examined);
// Pipeline会自动回收已消费的内存
```

### 7.2 Pipe 的工作原理

```
┌───────────────────────────────────────────────────────────────────┐
│                         Pipe 工作原理                              │
│                                                                   │
│  PipeWriter (写入端)              PipeReader (读取端)              │
│  ┌─────────────────┐             ┌─────────────────┐             │
│  │                 │             │                 │             │
│  │  网络层写入      │    Pipe     │  Kestrel解析    │             │
│  │  TCP数据到这里   │ ─────────► │  从这里读取      │             │
│  │                 │   环形     │  HTTP请求数据    │             │
│  │                 │   缓冲区   │                 │             │
│  └─────────────────┘             └─────────────────┘             │
│                                                                   │
│  写入流程：                                                       │
│  1. Memory<byte> mem = writer.GetMemory(sizeHint);               │
│     → 从内存池获取一块内存                                         │
│  2. 将网络数据复制到 mem                                           │
│  3. writer.Advance(bytesWritten);                                 │
│     → 告诉Pipe写了多少字节                                        │
│  4. await writer.FlushAsync();                                    │
│     → 通知Reader有新数据可读                                      │
│                                                                   │
│  读取流程：                                                       │
│  1. ReadResult result = await reader.ReadAsync();                 │
│     → 等待有数据可读（如果没有数据会异步等待）                      │
│  2. ReadOnlySequence<byte> buffer = result.Buffer;                │
│     → 获取可读的字节序列（可能跨越多个内存段）                      │
│  3. 解析数据...                                                   │
│  4. reader.AdvanceTo(consumed, examined);                         │
│     → consumed: 已消费的位置（这些内存可以回收）                    │
│     → examined: 已检查的位置（下次从这里之后开始提供新数据）         │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  内存布局 (ReadOnlySequence<byte>)                       │     │
│  │                                                          │     │
│  │  Segment1[4096]  → Segment2[4096]  → Segment3[2048]     │     │
│  │  ├──已消费──┤├──可读────────────────────────────┤        │     │
│  │             consumed                      examined       │     │
│  │                                                          │     │
│  │  和 Stream 的区别：                                       │     │
│  │  - Stream: 连续的byte[]，大小固定                         │     │
│  │  - Sequence: 链表结构，可以无限增长                        │     │
│  │  - 不需要复制数据到单个连续缓冲区                          │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### 7.3 SocketTransport 中的 Pipe 使用

```csharp
// 📁 源码位置: src/Servers/Kestrel/Transport.Sockets/src/Internal/SocketConnection.cs

internal sealed class SocketConnection : TransportConnection
{
    private readonly Socket _socket;
    private readonly Pipe _inputPipe;   // 网络 → 应用
    private readonly Pipe _outputPipe;  // 应用 → 网络

    // 🔥 接收循环：从Socket读取数据写入Pipe
    private async Task DoReceive()
    {
        while (true)
        {
            // 从Pipe获取写入缓冲区
            var buffer = _inputPipe.Writer.GetMemory(MinAllocBufferSize);
            // 使用内存池，几乎零GC
            
            try
            {
                // 🔥 从Socket读取数据
                var bytesReceived = await _socket.ReceiveAsync(buffer, SocketFlags.None);
                
                if (bytesReceived == 0)
                {
                    // 连接关闭
                    break;
                }
                
                // 告诉Pipe写入了多少字节
                _inputPipe.Writer.Advance(bytesReceived);
            }
            catch (Exception ex)
            {
                _logger.ConnectionError(_connectionId, ex);
                break;
            }
            
            // 🔥 刷新 → 通知读取端有新数据
            var result = await _inputPipe.Writer.FlushAsync();
            
            if (result.IsCompleted || result.IsCanceled)
            {
                break; // 读取端已关闭
            }
        }
        
        // 标记写入完成
        _inputPipe.Writer.Complete();
    }

    // 🔥 发送循环：从Pipe读取数据发送到Socket
    private async Task DoSend()
    {
        while (true)
        {
            // 等待应用层写入的响应数据
            var result = await _outputPipe.Reader.ReadAsync();
            var buffer = result.Buffer;
            
            if (buffer.IsEmpty && result.IsCompleted)
            {
                break;
            }
            
            // 🔥 将数据发送到Socket
            if (!buffer.IsEmpty)
            {
                if (buffer.IsSingleSegment)
                {
                    // 单段 → 直接发送
                    await _socket.SendAsync(buffer.First, SocketFlags.None);
                }
                else
                {
                    // 多段 → 使用 Scatter/Gather I/O
                    foreach (var segment in buffer)
                    {
                        await _socket.SendAsync(segment, SocketFlags.None);
                    }
                }
            }
            
            // 标记已消费
            _outputPipe.Reader.AdvanceTo(buffer.End);
        }
    }
}
```

```
数据流向：

TCP Socket
    │  ReceiveAsync() → 原始字节
    ▼
Input Pipe (Writer端)
    │  Advance() + FlushAsync()
    ▼
Input Pipe (Reader端)
    │  ReadAsync() → ReadOnlySequence<byte>
    ▼
Http1Connection / HttpParser
    │  解析请求行、请求头
    │  创建 HttpContext
    │  请求体通过 RequestBody Pipe 读取
    ▼
中间件管道 (RequestDelegate)
    │  处理请求
    │  写入响应
    ▼
Output Pipe (Writer端)
    │  响应头 + 响应体
    ▼
Output Pipe (Reader端)
    │  ReadAsync()
    ▼
TCP Socket
    │  SendAsync() → 原始字节
    ▼
客户端
```

---

## 八、HostingApplication —— 连接 Kestrel 和中间件管道

```csharp
// 📁 源码位置: src/Hosting/Hosting/src/Internal/HostingApplication.cs

internal sealed class HostingApplication : IHttpApplication<HostingApplication.Context>
{
    private readonly RequestDelegate _application;      // 中间件管道
    private readonly IHttpContextFactory _httpContextFactory;
    private readonly DiagnosticListener _diagnosticSource;
    private readonly ActivitySource _activitySource;

    // =====================================================
    // 🔥 创建请求上下文
    // =====================================================
    public Context CreateContext(IFeatureCollection contextFeatures)
    {
        var context = new Context();
        
        // 🔥 创建 HttpContext
        var httpContext = _httpContextFactory.Create(contextFeatures);
        // DefaultHttpContext 包装了 Kestrel 提供的 Feature 集合
        // contextFeatures 包含：
        //   IHttpRequestFeature    → 请求信息（Method, Path, Headers等）
        //   IHttpResponseFeature   → 响应信息（StatusCode, Headers等）
        //   IHttpResponseBodyFeature → 响应体写入
        //   IHttpConnectionFeature → 连接信息（RemoteIP等）
        
        context.HttpContext = httpContext;

        // 🔥 创建 DI Scope（请求级别的依赖注入范围）
        var scope = _serviceScopeFactory.CreateScope();
        httpContext.RequestServices = scope.ServiceProvider;
        // 从这里开始，Scoped服务生效
        // 如 DbContext, IOpenIddictApplicationManager 等
        
        context.Scope = scope;

        // 🔥 记录请求开始（诊断/日志/追踪）
        if (_diagnosticSource.IsEnabled("Microsoft.AspNetCore.Hosting.HttpRequestIn"))
        {
            _diagnosticSource.StartActivity(context.Activity, ...);
        }

        _logger.RequestStarting(
            httpContext.Request.Protocol,
            httpContext.Request.Method,
            httpContext.Request.Scheme,
            httpContext.Request.Host.ToString(),
            httpContext.Request.Path);

        return context;
    }

    // =====================================================
    // 🔥 处理请求
    // =====================================================
    public Task ProcessRequestAsync(Context context)
    {
        // 🔥🔥🔥 执行中间件管道！
        return _application(context.HttpContext!);
        // _application 就是 ApplicationBuilder.Build() 生成的 RequestDelegate
        //
        // 等价于：
        // AuthenticationMiddleware
        //   → AuthorizationMiddleware
        //     → RoutingMiddleware
        //       → EndpointMiddleware
        //         → Controller/Minimal API
    }

    // =====================================================
    // 🔥 清理上下文
    // =====================================================
    public void DisposeContext(Context context, Exception? exception)
    {
        // 🔥 记录请求结束
        _logger.RequestFinished(
            context.HttpContext!.Response.StatusCode,
            context.HttpContext.Request.ContentLength,
            context.HttpContext.Response.ContentLength,
            context.ElapsedTime);

        // 停止Activity（分布式追踪）
        if (context.Activity != null)
        {
            _diagnosticSource.StopActivity(context.Activity, ...);
        }

        // 🔥🔥🔥 释放 DI Scope
        context.Scope?.Dispose();
        // 所有 Scoped 服务在这里被释放
        // DbContext.Dispose()
        // OpenIddict 的 Scoped 服务释放
        
        // 回收 HttpContext 到对象池
        _httpContextFactory.Dispose(context.HttpContext!);
        // DefaultHttpContext 不是每次都new
        // 使用对象池复用，减少GC压力
    }

    // 内部上下文类
    internal struct Context
    {
        public HttpContext? HttpContext;
        public IServiceScope? Scope;
        public Activity? Activity;
        public long StartTimestamp;
        public long ElapsedTime => Stopwatch.GetTimestamp() - StartTimestamp;
    }
}
```

---

## 九、DefaultHttpContext —— HttpContext 的默认实现

```csharp
// 📁 源码位置: src/Http/Http/src/DefaultHttpContext.cs

public sealed class DefaultHttpContext : HttpContext
{
    // 🔥 Feature集合（Kestrel提供的底层功能）
    private FeatureReferences<FeatureInterfaces> _features;

    // 所有属性都通过 Feature 获取，而不是直接存储
    
    public override HttpRequest Request => _request ??= new DefaultHttpRequest(this);
    public override HttpResponse Response => _response ??= new DefaultHttpResponse(this);
    
    // 🔥 用户信息（认证系统设置）
    public override ClaimsPrincipal User { get; set; } = new ClaimsPrincipal();
    
    // 🔥 请求级 DI 容器（HostingApplication 设置）
    public override IServiceProvider RequestServices { get; set; } = default!;
    
    // 🔥 请求取消令牌
    public override CancellationToken RequestAborted
    {
        get => _features.Fetch(ref _features.Cache.Lifetime)?.RequestAborted 
            ?? CancellationToken.None;
    }
    
    // 🔥 连接信息
    public override ConnectionInfo Connection => _connection ??= new DefaultConnectionInfo(_features);
    
    // 🔥 WebSocket支持
    public override WebSocketManager WebSockets => _websockets ??= new DefaultWebSocketManager(_features);

    // 🔥 路由信息
    // 这是 UseRouting() 设置的
    public Endpoint? GetEndpoint() => _features.Get<IEndpointFeature>()?.Endpoint;
}
```

```csharp
// DefaultHttpRequest 从 Feature 获取数据
// 📁 源码位置: src/Http/Http/src/DefaultHttpRequest.cs

public class DefaultHttpRequest : HttpRequest
{
    // 🔥 Method, Path, Headers 等都是从 Feature 获取的
    // Feature 是 Kestrel 在解析 HTTP 请求时设置的
    
    public override string Method
    {
        get => HttpRequestFeature.Method;
        set => HttpRequestFeature.Method = value;
    }

    public override PathString Path
    {
        get => new PathString(HttpRequestFeature.Path);
        set => HttpRequestFeature.Path = value.Value ?? string.Empty;
    }

    public override IHeaderDictionary Headers => HttpRequestFeature.Headers;
    
    public override Stream Body
    {
        get => HttpRequestFeature.Body;
        set => HttpRequestFeature.Body = value;
    }
    
    public override string? ContentType
    {
        get => Headers.ContentType;
        set => Headers.ContentType = value;
    }
    
    public override long? ContentLength
    {
        get => Headers.ContentLength;
        set => Headers.ContentLength = value;
    }
    
    // 🔥 路由参数值（UseRouting 设置的）
    public override RouteValueDictionary RouteValues { get; set; } = new();
}
```

**Feature 集合的设计：Kestrel 和 HttpContext 的解耦**

```
为什么用 Feature 而不是直接设置属性？

因为不同的服务器（Kestrel, IIS, TestServer）提供不同的功能。
Feature 集合是服务器和应用之间的契约。

Kestrel 提供的 Features：
├── IHttpRequestFeature      → Method, Path, Headers, Body
├── IHttpResponseFeature     → StatusCode, Headers
├── IHttpResponseBodyFeature → 响应体写入（PipeWriter）
├── IHttpConnectionFeature   → RemoteIpAddress, RemotePort
├── IHttpRequestLifetimeFeature → Abort()
├── IHttp2StreamIdFeature    → HTTP/2 流ID
├── ITlsConnectionFeature    → ClientCertificate
└── ...

IIS 提供的 Features（不同的子集）：
├── IHttpRequestFeature
├── IHttpResponseFeature
├── IServerVariablesFeature  → IIS特有
└── ...

TestServer 提供的 Features：
├── IHttpRequestFeature      → 从内存中创建
├── IHttpResponseFeature     → 写入内存
└── ...

通过 Feature 集合，HttpContext 不需要知道底层是什么服务器
只要 Feature 存在就能使用对应功能
```

---

## 十、Kestrel 配置

### 10.1 监听地址配置

```csharp
// 方式1：UseUrls（最简单）
builder.WebHost.UseUrls("http://*:5000", "https://*:5001");

// 方式2：配置文件
// appsettings.json
{
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://*:5000"
      },
      "Https": {
        "Url": "https://*:5001",
        "Certificate": {
          "Path": "./certs/server.pfx",
          "Password": "cert-password"
        }
      }
    }
  }
}

// 方式3：代码配置（最灵活）
builder.WebHost.ConfigureKestrel(options =>
{
    // HTTP
    options.ListenAnyIP(5000);
    
    // HTTPS
    options.ListenAnyIP(5001, listenOptions =>
    {
        listenOptions.UseHttps("./certs/server.pfx", "cert-password");
    });
    
    // Unix Socket
    options.ListenUnixSocket("/tmp/kestrel.sock");
    
    // 指定IP
    options.Listen(IPAddress.Loopback, 5002);
    
    // 同时支持 HTTP/1.1 和 HTTP/2
    options.ListenAnyIP(5003, listenOptions =>
    {
        listenOptions.Protocols = HttpProtocols.Http1AndHttp2;
        listenOptions.UseHttps();
    });
});

// 方式4：环境变量
// ASPNETCORE_URLS=http://+:5000;https://+:5001
// ASPNETCORE_HTTPS_PORT=5001
```

### 10.2 性能相关配置

```csharp
builder.WebHost.ConfigureKestrel(options =>
{
    // ========== 连接限制 ==========
    
    // 🔥 最大并发连接数
    options.Limits.MaxConcurrentConnections = 100_000;
    // null = 无限制（默认）
    
    // 🔥 最大并发已升级连接（WebSocket）
    options.Limits.MaxConcurrentUpgradedConnections = 10_000;
    
    // ========== 请求限制 ==========
    
    // 🔥 请求体最大大小
    options.Limits.MaxRequestBodySize = 30_000_000; // 30MB
    // null = 无限制
    // 对于文件上传可能需要增大
    
    // 🔥 请求头最大大小
    options.Limits.MaxRequestHeadersTotalSize = 32 * 1024; // 32KB
    // 防止超大请求头攻击
    
    // 🔥 单个请求头最大数量
    options.Limits.MaxRequestHeaderCount = 100;
    
    // 🔥 请求行最大长度
    options.Limits.MaxRequestLineSize = 8 * 1024; // 8KB
    // URL太长时会返回 414 URI Too Long
    
    // ========== 超时配置 ==========
    
    // 🔥 Keep-Alive 超时
    options.Limits.KeepAliveTimeout = TimeSpan.FromMinutes(2);
    // TCP连接空闲多久后关闭
    
    // 🔥 请求头超时
    options.Limits.RequestHeadersTimeout = TimeSpan.FromSeconds(30);
    // 收到连接后，等待完整请求头的超时
    // 防止 Slowloris 攻击
    
    // ========== HTTP/2 配置 ==========
    
    // 🔥 HTTP/2 流的最大数量（每个连接）
    options.Limits.Http2.MaxStreamsPerConnection = 100;
    
    // 🔥 HTTP/2 帧大小
    options.Limits.Http2.MaxFrameSize = 16 * 1024; // 16KB
    
    // 🔥 HTTP/2 请求头表大小
    options.Limits.Http2.HeaderTableSize = 4096;
    
    // 🔥 HTTP/2 初始窗口大小
    options.Limits.Http2.InitialConnectionWindowSize = 128 * 1024;
    options.Limits.Http2.InitialStreamWindowSize = 96 * 1024;
});
```

### 10.3 HTTPS 配置

```csharp
builder.WebHost.ConfigureKestrel(options =>
{
    options.ListenAnyIP(5001, listenOptions =>
    {
        listenOptions.UseHttps(httpsOptions =>
        {
            // 🔥 服务器证书
            httpsOptions.ServerCertificate = X509Certificate2.CreateFromPemFile(
                "server.crt", "server.key");
            
            // 或使用PFX
            // httpsOptions.ServerCertificate = new X509Certificate2("server.pfx", "password");
            
            // 🔥 SNI（服务器名称指示）
            // 根据客户端请求的域名选择不同的证书
            httpsOptions.ServerCertificateSelector = (connectionContext, name) =>
            {
                return name switch
                {
                    "sso.example.com" => ssoCert,
                    "api.example.com" => apiCert,
                    _ => defaultCert
                };
            };
            
            // 🔥 TLS 版本
            httpsOptions.SslProtocols = SslProtocols.Tls12 | SslProtocols.Tls13;
            
            // 🔥 客户端证书（mTLS，双向TLS）
            httpsOptions.ClientCertificateMode = ClientCertificateMode.RequireCertificate;
            // NoCertificate     → 不要求客户端证书（默认）
            // AllowCertificate  → 允许但不强制
            // RequireCertificate → 强制要求（用于服务间mTLS）
        });
    });
});
```

---

## 十一、HTTP/1.1 vs HTTP/2 vs HTTP/3

```
┌──────────────────────────────────────────────────────────────────────────┐
│                    HTTP 协议版本对比                                      │
│                                                                          │
│  HTTP/1.1                                                                │
│  ═════════                                                               │
│  Connection: TCP                                                         │
│  复用方式: Keep-Alive（串行复用，同一时间只处理一个请求）                    │
│                                                                          │
│  Client                            Server                                │
│    │  GET /api/users                  │                                   │
│    │  ────────────────────────────►   │                                   │
│    │                                  │  处理...                          │
│    │  ◄───────── 200 OK ──────────   │                                   │
│    │                                  │                                   │
│    │  GET /api/articles               │  ← 必须等上一个请求完成            │
│    │  ────────────────────────────►   │                                   │
│    │  ◄───────── 200 OK ──────────   │                                   │
│                                                                          │
│  队头阻塞问题(Head-of-Line Blocking):                                    │
│  一个慢请求会阻塞同一连接上的后续请求                                      │
│  浏览器解决方案：开多个TCP连接（通常6个/域名）                              │
│                                                                          │
│  ─────────────────────────────────────────────────────────────────────   │
│                                                                          │
│  HTTP/2                                                                  │
│  ═══════                                                                 │
│  Connection: TCP                                                         │
│  复用方式: 多路复用（Multiplexing）                                       │
│                                                                          │
│  Client                            Server                                │
│    │  Stream 1: GET /api/users        │                                   │
│    │  Stream 3: GET /api/articles     │  ← 同时发送多个请求               │
│    │  Stream 5: GET /api/comments     │                                   │
│    │  ════════════════════════════►   │                                   │
│    │                                  │  并行处理...                      │
│    │  Stream 3: 200 OK (articles)     │  ← 响应可以乱序返回               │
│    │  Stream 1: 200 OK (users)        │                                   │
│    │  Stream 5: 200 OK (comments)     │                                   │
│    │  ◄════════════════════════════   │                                   │
│                                                                          │
│  一个TCP连接 = 多个逻辑流 = 并行处理多个请求                               │
│  + 头部压缩（HPACK）                                                     │
│  + 服务器推送（Server Push）                                             │
│  仍有 TCP 级别的队头阻塞                                                 │
│                                                                          │
│  ─────────────────────────────────────────────────────────────────────   │
│                                                                          │
│  HTTP/3                                                                  │
│  ═══════                                                                 │
│  Connection: QUIC (基于UDP)                                              │
│  复用方式: 流级别独立（消除TCP队头阻塞）                                   │
│                                                                          │
│  每个流的丢包只影响那个流，不影响其他流                                     │
│  连接迁移（网络切换时不断连接）                                            │
│  0-RTT 握手（更快的连接建立）                                             │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

### Http2Connection 的多路复用

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/Internal/Http2/Http2Connection.cs

// HTTP/2 连接处理（简化版）
internal sealed class Http2Connection
{
    // 活跃的流（每个流 = 一个独立的请求）
    private readonly ConcurrentDictionary<int, Http2Stream> _streams = new();
    
    public async Task ProcessRequestsAsync<TContext>(IHttpApplication<TContext> application)
    {
        while (true)
        {
            // 🔥 读取 HTTP/2 帧
            var frame = await ReadFrameAsync();
            
            switch (frame.Type)
            {
                case Http2FrameType.HEADERS:
                    // 🔥 新的请求流
                    var stream = new Http2Stream(frame.StreamId, this);
                    _streams[frame.StreamId] = stream;
                    
                    // 🔥 解析HPACK压缩的请求头
                    DecodeHeaders(frame, stream);
                    
                    // 🔥 在独立的Task中处理此流的请求
                    // 不阻塞其他流！
                    _ = stream.ProcessRequestAsync(application);
                    break;
                    
                case Http2FrameType.DATA:
                    // 🔥 请求体数据
                    if (_streams.TryGetValue(frame.StreamId, out var dataStream))
                    {
                        dataStream.OnDataReceived(frame.Payload);
                    }
                    break;
                    
                case Http2FrameType.WINDOW_UPDATE:
                    // 流量控制
                    HandleWindowUpdate(frame);
                    break;
                    
                case Http2FrameType.RST_STREAM:
                    // 取消某个流
                    if (_streams.TryRemove(frame.StreamId, out var cancelledStream))
                    {
                        cancelledStream.Abort();
                    }
                    break;
                    
                case Http2FrameType.GOAWAY:
                    // 关闭连接
                    return;
            }
        }
    }
}

// 每个 Http2Stream 独立处理
internal class Http2Stream
{
    public async Task ProcessRequestAsync<TContext>(IHttpApplication<TContext> application)
    {
        // 和 Http1Connection 一样：
        // 创建 HttpContext → 执行中间件管道 → 写回响应
        
        var context = application.CreateContext(this.Features);
        
        try
        {
            await application.ProcessRequestAsync(context);
        }
        finally
        {
            application.DisposeContext(context, null);
        }
        
        // 🔥 HTTP/2 响应帧
        // 响应头用 HPACK 压缩后通过 HEADERS 帧发送
        // 响应体通过 DATA 帧发送
    }
}
```

---

## 十二、Kestrel 的性能优化技巧

### 12.1 对象池

```csharp
// Kestrel 大量使用对象池来减少GC压力

// HttpContext 对象池
// 📁 源码位置: src/Http/Http/src/DefaultHttpContextFactory.cs

internal sealed class DefaultHttpContextFactory : IHttpContextFactory
{
    // 🔥 对象池：复用 DefaultHttpContext
    private readonly ObjectPool<DefaultHttpContext> _contextPool;

    public HttpContext Create(IFeatureCollection featureCollection)
    {
        // 从池中获取（如果池为空则创建新的）
        var httpContext = _contextPool.Get();
        httpContext.Initialize(featureCollection);
        return httpContext;
    }

    public void Dispose(HttpContext httpContext)
    {
        if (httpContext is DefaultHttpContext defaultContext)
        {
            // 重置状态
            defaultContext.Uninitialize();
            // 归还到池中
            _contextPool.Return(defaultContext);
        }
    }
}
```

### 12.2 已知请求头的零分配优化

```csharp
// Kestrel 对常见的HTTP头部进行了特殊优化

// 📁 源码位置: src/Servers/Kestrel/Core/src/Internal/Http/HttpHeaders.Generated.cs

// 例如 Content-Type 头：
// 不是存为 Dictionary<string, StringValues>
// 而是直接在类中定义字段

internal partial class HttpRequestHeaders
{
    // 🔥 常见头部有专门的字段，避免字典查找
    private StringValues _Host;
    private StringValues _ContentType;
    private StringValues _ContentLength;
    private StringValues _Authorization;
    private StringValues _Accept;
    private StringValues _Connection;
    private StringValues _UserAgent;
    private StringValues _Cookie;
    // ... 更多已知头部

    // 🔥 已知头部名称使用预分配的字符串常量
    // 不需要每次从字节解析成新的字符串
    
    private static readonly byte[] _headerHost = Encoding.ASCII.GetBytes("Host");
    private static readonly byte[] _headerContentType = Encoding.ASCII.GetBytes("Content-Type");
    // ...
    
    // 🔥 设置头部时，通过长度和首字节快速判断
    public void Append(ReadOnlySpan<byte> name, ReadOnlySpan<byte> value)
    {
        switch (name.Length)
        {
            case 4:
                // "Host" (长度4)
                if (name[0] == 'H' && name.SequenceEqual(_headerHost))
                {
                    _Host = new StringValues(value.ToString());
                    _bits |= 0x1; // 标记位
                    return;
                }
                break;
                
            case 12:
                // "Content-Type" (长度12)
                if (name[0] == 'C' && name.SequenceEqual(_headerContentType))
                {
                    _ContentType = new StringValues(value.ToString());
                    _bits |= 0x2;
                    return;
                }
                break;
                
            case 13:
                // "Authorization" (长度13)
                if (name[0] == 'A' && name.SequenceEqual(_headerAuthorization))
                {
                    _Authorization = new StringValues(value.ToString());
                    _bits |= 0x4;
                    return;
                }
                // "Content-Length" (也是长度13)
                if (name[0] == 'C' && name.SequenceEqual(_headerContentLength))
                {
                    _ContentLength = new StringValues(value.ToString());
                    _bits |= 0x8;
                    return;
                }
                break;
        }
        
        // 🔥 未知头部才存入字典
        _unknownHeaders ??= new Dictionary<string, StringValues>();
        _unknownHeaders[name.ToString()] = value.ToString();
    }
}
```

### 12.3 零拷贝响应

```csharp
// 使用 PipeWriter 直接写入响应，避免中间缓冲区

// ❌ 普通写法（多次内存拷贝）
await response.WriteAsync("Hello World");
// 字符串 → UTF8编码到byte[] → 写入PipeWriter → Socket发送

// ✅ 高性能写法（零拷贝）
var writer = response.BodyWriter;  // PipeWriter
var span = writer.GetSpan(sizeHint);
// span 直接指向 Socket 发送缓冲区
Encoding.UTF8.GetBytes("Hello World", span);
writer.Advance(bytesWritten);
await writer.FlushAsync();
// 数据直接从内存发送到Socket，没有中间拷贝
```

---

## 十三、Kestrel 在反向代理后面

```
生产环境推荐架构：

┌────────────────────────────────────────────────────────────────┐
│                         部署架构                                │
│                                                                │
│  互联网                                                        │
│    │                                                           │
│    ▼                                                           │
│  ┌─────────────────────────────────────────────────────┐       │
│  │  反向代理 (Nginx / YARP / Azure Front Door)          │       │
│  │                                                     │       │
│  │  职责：                                              │       │
│  │  ├── SSL/TLS 终止（HTTPS卸载）                       │       │
│  │  ├── 静态文件服务                                    │       │
│  │  ├── 负载均衡（多个Kestrel实例）                      │       │
│  │  ├── 请求过滤 / WAF                                  │       │
│  │  ├── 压缩（gzip/brotli）                             │       │
│  │  ├── 缓存                                           │       │
│  │  └── 设置 Forwarded Headers                          │       │
│  │      X-Forwarded-For: 客户端真实IP                   │       │
│  │      X-Forwarded-Proto: https                        │       │
│  │      X-Forwarded-Host: sso.example.com               │       │
│  └─────────────────────┬───────────────────────────────┘       │
│                        │ HTTP (内网)                            │
│                        ▼                                       │
│  ┌─────────────────────────────────────────────────────┐       │
│  │  Kestrel                                            │       │
│  │                                                     │       │
│  │  职责：                                              │       │
│  │  ├── HTTP 协议处理                                   │       │
│  │  ├── 请求管道执行                                    │       │
│  │  └── 应用逻辑                                       │       │
│  │                                                     │       │
│  │  监听: http://localhost:5000 (仅内网)                │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                                │
│  为什么不让 Kestrel 直接面向互联网？                              │
│  Kestrel 可以直接面向互联网（.NET 5+），但反向代理提供了：       │
│  ├── 更好的 DoS 防护                                           │
│  ├── 更成熟的 SSL/TLS 管理（Let's Encrypt自动续期）            │
│  ├── 多实例负载均衡                                            │
│  └── 对 SSO 项目：多个服务共享同一个入口                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### Forwarded Headers 配置

```csharp
// 🔥 在反向代理后面，必须配置 Forwarded Headers
// 否则你的应用看到的客户端IP是反向代理的IP而不是真实客户端IP

// 📁 源码位置: src/Middleware/HttpOverrides/src/ForwardedHeadersMiddleware.cs

var builder = WebApplication.CreateBuilder(args);

builder.Services.Configure<ForwardedHeadersOptions>(options =>
{
    options.ForwardedHeaders =
        ForwardedHeaders.XForwardedFor |     // 客户端真实IP
        ForwardedHeaders.XForwardedProto |   // 原始协议(https)
        ForwardedHeaders.XForwardedHost;     // 原始域名
    
    // 🔥 信任的代理网络
    // 只有来自这些IP的转发头才会被处理
    options.KnownNetworks.Clear();
    options.KnownProxies.Clear();
    // 如果在Docker/K8s中，可能需要信任所有：
    // options.KnownNetworks.Add(new IPNetwork(IPAddress.Any, 0));
});

var app = builder.Build();

// 🔥 必须在其他中间件之前
app.UseForwardedHeaders();
// 处理后：
// HttpContext.Connection.RemoteIpAddress = 客户端真实IP（而不是代理IP）
// HttpContext.Request.Scheme = "https"（而不是"http"）
// HttpContext.Request.Host = "sso.example.com"

app.UseAuthentication();
app.UseAuthorization();
```

**这对SSO项目至关重要：**

```
为什么 Forwarded Headers 对 SSO 很重要？

1. OpenIddict 签发 Token 时需要知道正确的 Issuer
   Token.Issuer = https://sso.example.com
   如果没有 ForwardedHeaders:
     → Kestrel 以为自己是 http://localhost:5000
     → Token.Issuer = http://localhost:5000  ❌

2. OAuth 回调URL验证
   redirect_uri = https://webapp.example.com/callback
   OpenIddict 验证时需要比较注册的回调URL
   如果 Scheme 不对(http vs https)，验证会失败

3. Cookie 安全性
   SameSite=None 要求 Secure=true
   Secure 要求 HTTPS
   如果 Scheme 被识别为 HTTP → Cookie 不会设置 Secure → SameSite失效
```

---

## 十四、优雅关闭（Graceful Shutdown）

```csharp
// 📁 源码位置: src/Servers/Kestrel/Core/src/KestrelServerImpl.cs

public async Task StopAsync(CancellationToken cancellationToken)
{
    // 🔥 第1步：停止接受新连接
    foreach (var listener in _listeners)
    {
        await listener.UnbindAsync(cancellationToken);
    }
    // 此时新的TCP连接会被操作系统拒绝（Connection Refused）

    // 🔥 第2步：等待正在处理的请求完成
    // 或者等到 cancellationToken 超时
    var tasks = new List<Task>();
    foreach (var connection in _connectionManager.GetConnections())
    {
        // 通知连接应该关闭
        connection.RequestClose();
        // HTTP/1.1: 设置 Connection: close 头
        // HTTP/2: 发送 GOAWAY 帧
        
        tasks.Add(connection.CompletionTask);
    }
    
    // 等待所有连接完成（有超时）
    await Task.WhenAll(tasks).WaitAsync(cancellationToken);
    
    // 🔥 第3步：如果超时，强制关闭
    if (cancellationToken.IsCancellationRequested)
    {
        foreach (var connection in _connectionManager.GetConnections())
        {
            connection.Abort(new ConnectionAbortedException("Server shutting down"));
        }
    }
}
```

```csharp
// 配置优雅关闭超时
builder.WebHost.ConfigureKestrel(options =>
{
    // 等待现有请求完成的超时时间
    // 默认5秒
});

// Host级别的关闭超时
builder.Host.ConfigureHostOptions(options =>
{
    options.ShutdownTimeout = TimeSpan.FromSeconds(30);
    // 收到关闭信号后，最多等30秒让请求完成
});
```

```
优雅关闭时序：

Ctrl+C / SIGTERM / docker stop
    │
    ▼
Host.StopAsync()
    │
    ├── 1. 通知 IHostedService 们停止（后台任务）
    │
    ├── 2. KestrelServer.StopAsync()
    │   │
    │   ├── 停止接受新TCP连接
    │   │   ← 新请求会被拒绝
    │   │
    │   ├── 通知现有连接关闭
    │   │   ← 发送 Connection: close 或 GOAWAY
    │   │
    │   ├── 等待正在处理的请求完成...
    │   │   ← 正在处理的SSO token请求会正常完成
    │   │   ← 不会丢失正在签发的Token
    │   │
    │   └── 超时后强制关闭
    │
    └── 3. ServiceProvider.Dispose()
        └── 释放所有单例服务
```

---

## 十五、SSO项目的Kestrel最佳实践

```csharp
var builder = WebApplication.CreateBuilder(args);

// ========== Kestrel 配置 ==========
builder.WebHost.ConfigureKestrel(options =>
{
    // 🔥 开发环境
    if (builder.Environment.IsDevelopment())
    {
        options.ListenLocalhost(5000);       // HTTP
        options.ListenLocalhost(5001, o =>   // HTTPS（开发证书）
        {
            o.UseHttps();
        });
    }
    
    // 🔥 生产环境（反向代理后面）
    if (builder.Environment.IsProduction())
    {
        // 只监听HTTP（HTTPS由反向代理处理）
        options.ListenAnyIP(5000);
        
        // 或者如果需要端到端加密：
        // options.ListenAnyIP(5001, o =>
        // {
        //     o.UseHttps("/certs/server.pfx", "password");
        //     o.Protocols = HttpProtocols.Http1AndHttp2;
        // });
    }
    
    // 🔥 请求体大小限制
    // SSO服务器不需要处理大文件上传
    options.Limits.MaxRequestBodySize = 1 * 1024 * 1024; // 1MB 足够
    
    // 🔥 请求头大小
    options.Limits.MaxRequestHeadersTotalSize = 64 * 1024; // 64KB
    // JWT Token 可能比较大，给足空间
    
    // 🔥 Keep-Alive
    options.Limits.KeepAliveTimeout = TimeSpan.FromMinutes(2);
    
    // 🔥 防止 Slowloris 攻击
    options.Limits.RequestHeadersTimeout = TimeSpan.FromSeconds(30);
    
    // 🔥 并发连接限制
    options.Limits.MaxConcurrentConnections = 10_000;
});

// ========== Forwarded Headers（反向代理必须）==========
builder.Services.Configure<ForwardedHeadersOptions>(options =>
{
    options.ForwardedHeaders = 
        ForwardedHeaders.XForwardedFor | 
        ForwardedHeaders.XForwardedProto |
        ForwardedHeaders.XForwardedHost;
    options.KnownNetworks.Clear();
    options.KnownProxies.Clear();
});

// ========== 优雅关闭 ==========
builder.Host.ConfigureHostOptions(options =>
{
    options.ShutdownTimeout = TimeSpan.FromSeconds(30);
    // OAuth token 交换请求可能需要一些时间完成
});

var app = builder.Build();

// 🔥 ForwardedHeaders 必须最先执行
app.UseForwardedHeaders();

// 🔥 HTTPS 重定向（如果 Kestrel 直接处理 HTTPS）
if (!app.Environment.IsDevelopment())
{
    app.UseHsts();
}
app.UseHttpsRedirection();

app.UseRouting();
app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();

app.Run();
```

```
SSO项目的部署架构示例：

┌─────────────────────────────────────────────────────────────────┐
│  Nginx 配置                                                     │
│                                                                 │
│  server {                                                       │
│      listen 443 ssl http2;                                      │
│      server_name sso.example.com;                               │
│                                                                 │
│      ssl_certificate /etc/ssl/sso.example.com.crt;              │
│      ssl_certificate_key /etc/ssl/sso.example.com.key;          │
│                                                                 │
│      location / {                                               │
│          proxy_pass http://localhost:5000;                       │
│          proxy_set_header Host $host;                            │
│          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
│          proxy_set_header X-Forwarded-Proto $scheme;             │
│          proxy_set_header X-Forwarded-Host $host;                │
│                                                                 │
│          # WebSocket 支持（如果需要SignalR）                     │
│          proxy_http_version 1.1;                                 │
│          proxy_set_header Upgrade $http_upgrade;                 │
│          proxy_set_header Connection "upgrade";                  │
│      }                                                          │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
│                                                                 
│  proxy_pass http://localhost:5000                                
▼                                                                 
┌─────────────────────────────────────────────────────────────────┐
│  Kestrel                                                        │
│  监听: http://localhost:5000                                    │
│                                                                 │
│  UseForwardedHeaders()  ← 恢复客户端真实信息                     │
│  UseAuthentication()                                            │
│  UseAuthorization()                                             │
│  OpenIddict Server                                              │
│  Controllers                                                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 十六、一个请求的完整生命周期 —— 从第一个字节到最后一个字节

```
客户端发送：
POST /connect/token HTTP/1.1\r\n
Host: sso.example.com\r\n
Content-Type: application/x-www-form-urlencoded\r\n
Content-Length: 157\r\n
\r\n
grant_type=authorization_code&code=abc123&redirect_uri=...&client_id=web-app&client_secret=secret

──────────────────────────────────────────────────────────────────

Kestrel 处理流程：

1. 操作系统层
   │  TCP SYN → SYN-ACK → ACK (三次握手)
   │  Socket.AcceptAsync() 返回新连接
   ▼

2. Transport 层 (SocketConnection)
   │  DoReceive() 循环从Socket读取字节到 InputPipe
   │  InputPipe.Writer.Advance(bytesRead)
   │  InputPipe.Writer.FlushAsync()
   ▼

3. 连接中间件层
   │  TLS 中间件 → 跳过（反向代理已处理HTTPS）
   │  HTTP 版本检测 → HTTP/1.1
   ▼

4. Http1Connection.ProcessRequestsAsync()
   │
   │  4a. 解析请求行
   │  │  InputPipe.Reader.ReadAsync()
   │  │  → "POST /connect/token HTTP/1.1\r\n"
   │  │  Method = POST, Path = "/connect/token", Version = 1.1
   │  │
   │  4b. 解析请求头
   │  │  循环 ReadAsync() 直到遇到 \r\n\r\n
   │  │  Host = "sso.example.com"
   │  │  Content-Type = "application/x-www-form-urlencoded"
   │  │  Content-Length = 157
   │  │
   │  4c. 请求体准备（不立即读取，按需读取）
   │  │  ContentLengthMessageBody { _contentLength = 157 }
   │  │
   │  4d. 设置 Features
   │      
   │  │  IHttpRequestFeature = {
   │  │      Method: "POST",
   │  │      Path: "/connect/token",
   │  │      Headers: { Host, Content-Type, Content-Length }
   │  │  }
   │  │  IHttpResponseFeature = { StatusCode: 200 (默认) }
   │  │  IHttpResponseBodyFeature = { PipeWriter → OutputPipe }
   │  │
   │  ▼
   │
5. HostingApplication.CreateContext(features)
   │
   │  5a. 从对象池获取 DefaultHttpContext
   │  │  httpContext.Initialize(features)
   │  │  → httpContext.Request.Method = "POST"
   │  │  → httpContext.Request.Path = "/connect/token"
   │  │
   │  5b. 创建 DI Scope
   │  │  var scope = serviceScopeFactory.CreateScope()
   │  │  httpContext.RequestServices = scope.ServiceProvider
   │  │  → 此Scope中的Scoped服务：
   │  │    DbContext (新实例)
   │  │    IOpenIddictApplicationManager (新实例)
   │  │    IOpenIddictAuthorizationManager (新实例)
   │  │    IOpenIddictTokenManager (新实例)
   │  │
   │  5c. 开始Activity（分布式追踪）
   │  │  Activity.Start("Microsoft.AspNetCore.Hosting.HttpRequestIn")
   │  │
   │  5d. 记录日志
   │  │  info: Request starting HTTP/1.1 POST https://sso.example.com/connect/token
   │  │
   │  ▼
   │
6. HostingApplication.ProcessRequestAsync(context)
   │  → 执行 RequestDelegate（中间件管道）
   │
   │  ┌──────────────────────────────────────────────────────────────────────┐
   │  │                        中间件管道执行                                 │
   │  │                                                                      │
   │  │  6a. ForwardedHeadersMiddleware                                      │
   │  │  │  X-Forwarded-For: 203.0.113.50                                   │
   │  │  │  → context.Connection.RemoteIpAddress = 203.0.113.50             │
   │  │  │  X-Forwarded-Proto: https                                        │
   │  │  │  → context.Request.Scheme = "https"                              │
   │  │  │  X-Forwarded-Host: sso.example.com                               │
   │  │  │  → context.Request.Host = "sso.example.com"                      │
   │  │  │  await next(context)                                              │
   │  │  ▼                                                                   │
   │  │                                                                      │
   │  │  6b. ExceptionHandlerMiddleware                                      │
   │  │  │  try { await next(context) } catch { ... }                       │
   │  │  ▼                                                                   │
   │  │                                                                      │
   │  │  6c. EndpointRoutingMiddleware (UseRouting)                          │
   │  │  │  DFA 匹配: "/connect/token"                                      │
   │  │  │  → 可能匹配到 AuthorizationController.Exchange()                  │
   │  │  │  → httpContext.SetEndpoint(endpoint)                              │
   │  │  │  → httpContext.Request.RouteValues = {}                           │
   │  │  │  await next(context)                                              │
   │  │  ▼                                                                   │
   │  │                                                                      │
   │  │  6d. AuthenticationMiddleware ⭐                                     │
   │  │  │                                                                   │
   │  │  │  第一阶段：遍历 IAuthenticationRequestHandler                     │
   │  │  │  │                                                                │
   │  │  │  │  OpenIddictServerAspNetCoreHandler.HandleRequestAsync()        │
   │  │  │  │  │                                                             │
   │  │  │  │  │  1. 路径匹配: "/connect/token" == TokenEndpointUri ✅       │
   │  │  │  │  │                                                             │
   │  │  │  │  │  2. 读取请求体（此时才真正读取Body）                          │
   │  │  │  │  │     await context.Request.ReadFormAsync()                    │
   │  │  │  │  │     → Kestrel 从 InputPipe 读取 157 字节                    │
   │  │  │  │  │     → 解析 form-urlencoded                                  │
   │  │  │  │  │     grant_type = "authorization_code"                       │
   │  │  │  │  │     code = "abc123"                                         │
   │  │  │  │  │     redirect_uri = "https://webapp.example.com/callback"    │
   │  │  │  │  │     client_id = "web-app"                                   │
   │  │  │  │  │     client_secret = "secret"                                │
   │  │  │  │  │                                                             │
   │  │  │  │  │  3. 验证客户端                                               │
   │  │  │  │  │     → IOpenIddictApplicationManager                         │
   │  │  │  │  │       .FindByClientIdAsync("web-app")                       │
   │  │  │  │  │     → 查数据库 → 找到客户端 ✅                               │
   │  │  │  │  │     → ValidateClientSecretAsync("secret") ✅                │
   │  │  │  │  │                                                             │
   │  │  │  │  │  4. 验证授权码                                               │
   │  │  │  │  │     → IOpenIddictTokenManager                               │
   │  │  │  │  │       .FindByReferenceIdAsync("abc123")                     │
   │  │  │  │  │     → 查数据库 → 找到Token ✅                               │
   │  │  │  │  │     → 验证未过期 ✅                                          │
   │  │  │  │  │     → 验证未使用 ✅                                          │
   │  │  │  │  │     → 验证 redirect_uri 匹配 ✅                             │
   │  │  │  │  │     → 标记为已使用                                           │
   │  │  │  │  │                                                             │
   │  │  │  │  │  5. 从授权码中恢复 ClaimsPrincipal                          │
   │  │  │  │  │     → 解密授权码内容                                         │
   │  │  │  │  │     → 得到: sub="user-guid-123", name="张三", ...           │
   │  │  │  │  │                                                             │
   │  │  │  │  │  6. 签发 Access Token                                       │
   │  │  │  │  │     → 创建 JWT                                              │
   │  │  │  │  │     → Header: { alg: "RS256", typ: "at+jwt" }              │
   │  │  │  │  │     → Payload: {                                            │
   │  │  │  │  │         sub: "user-guid-123",                               │
   │  │  │  │  │         name: "张三",                                       │
   │  │  │  │  │         iss: "https://sso.example.com",                     │
   │  │  │  │  │         aud: "web-app",                                     │
   │  │  │  │  │         exp: 1700003600,                                    │
   │  │  │  │  │         scope: "openid profile email"                       │
   │  │  │  │  │       }                                                     │
   │  │  │  │  │     → RSA私钥签名                                           │
   │  │  │  │  │     → access_token = "eyJhbG..."                            │
   │  │  │  │  │                                                             │
   │  │  │  │  │  7. 签发 ID Token                                           │
   │  │  │  │  │     → 类似JWT，但包含用户身份信息                             │
   │  │  │  │  │     → id_token = "eyJhbG..."                                │
   │  │  │  │  │                                                             │
   │  │  │  │  │  8. 可选：签发 Refresh Token                                │
   │  │  │  │  │     → 生成随机字符串，存入数据库                              │
   │  │  │  │  │     → refresh_token = "rt_xyz789"                           │
   │  │  │  │  │                                                             │
   │  │  │  │  │  9. 写入响应                                                │
   │  │  │  │  │     context.Response.ContentType = "application/json"       │
   │  │  │  │  │     context.Response.StatusCode = 200                       │
   │  │  │  │  │     await context.Response.WriteAsJsonAsync(new {           │
   │  │  │  │  │         access_token = "eyJhbG...",                         │
   │  │  │  │  │         token_type = "Bearer",                              │
   │  │  │  │  │         expires_in = 3600,                                  │
   │  │  │  │  │         id_token = "eyJhbG...",                             │
   │  │  │  │  │         refresh_token = "rt_xyz789"                         │
   │  │  │  │  │     });                                                     │
   │  │  │  │  │     → JSON 序列化                                           │
   │  │  │  │  │     → 写入 OutputPipe                                       │
   │  │  │  │  │                                                             │
   │  │  │  │  │  10. return true → 短路！                                   │
   │  │  │  │  │                                                             │
   │  │  │  │  └── return  ← 不调用 next                                     │
   │  │  │  │                                                                │
   │  │  │  └── AuthenticationMiddleware 结束，不执行第二阶段                  │
   │  │  │                                                                   │
   │  │  │  ⛔ AuthorizationMiddleware 没有执行                               │
   │  │  │  ⛔ EndpointMiddleware 没有执行                                    │
   │  │  │  ⛔ Controller 没有执行                                            │
   │  │  │                                                                   │
   │  └──────────────────────────────────────────────────────────────────────┘
   │
   ▼

7. HostingApplication.DisposeContext(context)
   │
   │  7a. 记录日志
   │  │  info: Request finished HTTP/1.1 POST https://sso.example.com/connect/token
   │  │        - 200 - application/json 523 42.3ms
   │  │
   │  7b. 停止Activity
   │  │  Activity.Stop()
   │  │
   │  7c. 释放 DI Scope
   │  │  scope.Dispose()
   │  │  → DbContext.Dispose() → 释放数据库连接回连接池
   │  │  → OpenIddict Manager 释放
   │  │  → 所有 Scoped 服务释放
   │  │
   │  7d. 回收 HttpContext 到对象池
   │  │  httpContext.Uninitialize()
   │  │  contextPool.Return(httpContext)
   │  │
   ▼

8. Http1Connection
   │
   │  8a. 刷新响应
   │  │  OutputPipe.FlushAsync()
   │  │  → SocketConnection.DoSend()
   │  │  → Socket.SendAsync()
   │  │
   │  8b. 发送的原始字节：
   │  │  "HTTP/1.1 200 OK\r\n"
   │  │  "Content-Type: application/json;charset=utf-8\r\n"
   │  │  "Content-Length: 523\r\n"
   │  │  "Cache-Control: no-store\r\n"
   │  │  "Pragma: no-cache\r\n"
   │  │  "\r\n"
   │  │  "{\"access_token\":\"eyJhbG...\",\"token_type\":\"Bearer\",..."
   │  │
   │  8c. 检查 Keep-Alive
   │  │  Connection 头没有 close → Keep-Alive
   │  │  重置状态，等待下一个请求
   │  │
   │  8d. 回到 ProcessRequestsAsync() 循环顶部
   │  │  await InputPipe.Reader.ReadAsync()
   │  │  → 等待同一TCP连接上的下一个HTTP请求...
   │  │
   ▼

9. SocketConnection.DoSend()
   │
   │  从 OutputPipe.Reader 读取响应字节
   │  Socket.SendAsync(buffer)
   │  → 操作系统 TCP 栈
   │  → 网卡
   │  → 网络
   │
   ▼

10. 客户端收到响应
    │
    │  HTTP/1.1 200 OK
    │  Content-Type: application/json
    │  {
    │      "access_token": "eyJhbGciOiJSUzI1NiIs...",
    │      "token_type": "Bearer",
    │      "expires_in": 3600,
    │      "id_token": "eyJhbGciOiJSUzI1NiIs...",
    │      "refresh_token": "rt_xyz789"
    │  }
```

---

## 十七、性能关键路径上的内存分配分析

```
一个典型请求的内存分配热点：

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│  零分配（Kestrel 优化过的）                                         │
│  ════════════════════════                                          │
│  ✅ Socket I/O 缓冲区 → ArrayPool<byte> 租借，不new                │
│  ✅ Pipe 内存段 → MemoryPool<byte>，复用                           │
│  ✅ HttpContext → 对象池复用                                        │
│  ✅ 已知HTTP头部名 → 预分配的静态字符串                              │
│  ✅ HTTP方法 → 整数比较，不创建字符串                                │
│  ✅ 常见Status Code文本 → 预分配                                    │
│                                                                    │
│  有分配但被优化的                                                   │
│  ════════════════                                                  │
│  ⚠️ DI Scope → 必须创建（每请求一个）                               │
│  ⚠️ Scoped服务实例 → 必须创建（DbContext等）                        │
│  ⚠️ 路由参数值 → RouteValueDictionary                              │
│  ⚠️ 请求头值 → StringValues（但已知头部有优化）                      │
│  ⚠️ 请求路径 → PathString                                          │
│                                                                    │
│  应用层分配（你的代码可以优化的）                                     │
│  ════════════════════════════                                      │
│  ❌ JSON序列化/反序列化 → 使用 System.Text.Json（比Newtonsoft快）    │
│  ❌ 字符串拼接 → 使用 StringBuilder 或 string.Create              │
│  ❌ LINQ → 可能产生迭代器分配                                       │
│  ❌ async/await → 状态机分配（但通常可忽略）                         │
│  ❌ Lambda捕获 → 闭包分配                                          │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 十八、Kestrel 与其他服务器的对比

```
┌───────────────┬─────────────────┬──────────────────┬──────────────────┐
│               │ Kestrel          │ IIS (In-Process) │ IIS (Out-Process)│
├───────────────┼─────────────────┼──────────────────┼──────────────────┤
│ 跨平台         │ ✅              │ ❌ Windows only  │ ❌ Windows only  │
│ 性能           │ ⭐⭐⭐⭐⭐      │ ⭐⭐⭐⭐         │ ⭐⭐⭐           │
│ HTTP/2         │ ✅              │ ✅               │ ✅               │
│ HTTP/3         │ ✅ (.NET 7+)    │ ❌               │ ❌               │
│ 直接暴露互联网  │ ✅ (.NET 5+)    │ ✅               │ 通过IIS          │
│ Windows认证    │ ❌              │ ✅               │ ✅               │
│ 进程管理       │ 需要systemd等   │ IIS管理          │ IIS管理          │
│                │                 │                  │                  │
│ SSO项目推荐    │ ✅ Linux/Docker │ 可选 Windows     │ 不推荐           │
└───────────────┴─────────────────┴──────────────────┴──────────────────┘

TechEmpower Benchmarks 性能排名：
Kestrel 常年位于各类Web框架性能榜单前列
Plaintext: ~7,000,000+ req/s
JSON:      ~1,000,000+ req/s
（具体数字根据硬件和测试条件而异）
```

---

## 十九、检验你的理解

1. **Kestrel 监听 TCP 端口后，一个完整的请求处理经过哪些层？每层的职责是什么？**
   （答案：① Transport层（SocketConnection）：管理TCP Socket的读写，使用Pipe进行高效I/O。② 连接中间件层：TLS握手（HTTPS）、HTTP协议版本检测。③ HTTP协议层（Http1Connection/Http2Connection）：解析HTTP请求行、请求头、管理请求体和响应写入。④ 应用层（HostingApplication）：创建HttpContext和DI Scope，执行中间件管道（RequestDelegate），释放Scope。）

2. **System.IO.Pipelines 比传统 Stream 好在哪里？为什么 Kestrel 选择用 Pipe？**
   （答案：① 内存管理：Pipe使用MemoryPool/ArrayPool，减少GC。Stream每次Read可能分配新byte[]。② 缓冲区管理：Pipe自动管理可变大小的缓冲区（链表结构），不需要预估大小。Stream需要固定大小的缓冲区。③ 反压（Backpressure）：Pipe天然支持写入端和读取端的速度协调。④ 读写分离：Pipe的Writer和Reader可以在不同线程并行工作。⑤ 零拷贝：ReadOnlySequence可以跨多个内存段，不需要拷贝到连续缓冲区。）

3. **为什么 HttpContext 的属性（Method, Path, Headers等）要通过 Feature 集合获取，而不是直接存储？**
   （答案：为了解耦服务器实现和应用层。不同的服务器（Kestrel、IIS、TestServer）提供不同的Feature实现。HttpContext只依赖Feature接口，不依赖具体服务器。这样Kestrel可以提供高性能的Feature实现（如直接操作Pipe），IIS可以提供IIS特有的Feature（如ServerVariables），而应用代码不需要修改。这是策略模式的应用。）

4. **在反向代理后面运行 Kestrel 时，为什么必须配置 ForwardedHeaders？对 SSO 项目有什么影响？**
   （答案：反向代理接收HTTPS请求后，以HTTP转发给Kestrel。Kestrel看到的是HTTP请求，RemoteIP是代理的IP。不配置ForwardedHeaders会导致：① OpenIddict 的 Issuer 是 "http://localhost:5000" 而不是 "https://sso.example.com"——Token验证会失败。② Cookie 的 Secure 标志不会设置——SameSite=None失效，跨域SSO不工作。③ 审计日志记录的是代理IP而不是客户端真实IP。④ redirect_uri 的 scheme 不匹配——OAuth流程中断。）

5. **HTTP/2 多路复用是怎么实现的？和 HTTP/1.1 Keep-Alive 有什么本质区别？**
   （答案：HTTP/1.1 Keep-Alive 是连接级别的串行复用——同一时间同一连接只能处理一个请求/响应对，必须等上一个完成才能发下一个（队头阻塞）。HTTP/2 多路复用是流级别的并行复用——同一个TCP连接上可以同时有多个Stream（逻辑通道），每个Stream独立传输一个请求/响应对，它们的数据帧可以交错发送。在Kestrel中，Http2Connection为每个Stream创建独立的Http2Stream实例，每个Stream在独立的Task中执行中间件管道，互不阻塞。）

6. **Kestrel 优雅关闭时正在处理的 SSO Token 请求会怎样？**
   （答案：① 首先停止接受新TCP连接——新的请求会被操作系统拒绝（Connection Refused）。② 对已有连接，HTTP/1.1设置Connection: close，HTTP/2发送GOAWAY帧——通知客户端不要发新请求。③ 正在处理的Token请求会继续执行直到完成——授权码验证、Token签发、数据库写入都会正常完成。④ 在配置的ShutdownTimeout内等待所有请求完成。⑤ 超时后强制中断剩余连接。所以只要ShutdownTimeout足够长，正在进行的Token交换不会丢失。）

---

## 二十、第4层总结 —— 六大核心子系统全景

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│                    ASP.NET Core 六大核心子系统关系图                           │
│                                                                              │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────┐                │
│   │                    Kestrel (HTTP服务器)                   │                │
│   │                                                         │                │
│   │  TCP Socket → Pipe I/O → HTTP解析 → HttpContext         │                │
│   │                                                         │                │
│   │  每个请求:                                               │                │
│   │    创建 DI Scope ──────────────────────────┐            │                │
│   │    创建 HttpContext                         │            │                │
│   │    执行中间件管道 ─────────────────────┐     │            │                │
│   │                                      │     │            │                │
│   └──────────────────────────────────────│─────│────────────┘                │
│                                          │     │                             │
│                                          ▼     ▼                             │
│   ┌───────────────────────┐    ┌──────────────────────┐                     │
│   │   中间件管道            │    │   DI 系统              │                     │
│   │                       │    │                      │                     │
│   │   ForwardedHeaders    │    │   ServiceCollection  │                     │
│   │   ↓                   │    │   ServiceProvider    │                     │
│   │   ExceptionHandler    │    │   Scoped/Singleton/  │                     │
│   │   ↓                   │    │   Transient          │                     │
│   │   Routing ────────────│────│── 解析服务             │                     │
│   │   ↓                   │    │                      │                     │
│   │   Authentication ─────│────│── 解析Handlers        │                     │
│   │   ↓                   │    │   解析DbContext       │                     │
│   │   Authorization ──────│────│── 解析Policy/Handlers │                     │
│   │   ↓                   │    │                      │                     │
│   │   Endpoint ───────────│────│── 解析Controllers     │                     │
│   │                       │    │                      │                     │
│   └───────────────────────┘    └──────────────────────┘                     │
│         │    │    │                      ▲                                   │
│         │    │    │                      │                                   │
│         ▼    ▼    ▼                      │                                   │
│   ┌──────────┐ ┌──────────┐ ┌──────────────────────┐                       │
│   │ 路由系统  │ │ 认证系统  │ │  授权系统             │                       │
│   │          │ │          │ │                      │                       │
│   │ DFA匹配  │ │ Cookie   │ │  [Authorize]         │                       │
│   │ Endpoint │ │ JWT      │ │  Policy/Requirement  │                       │
│   │ Metadata │ │ OpenIddict│ │  Handler             │                       │
│   │ RouteVal │ │ Principal│ │  Scope检查            │                       │
│   │          │ │          │ │                      │                       │
│   └──────────┘ └──────────┘ └──────────────────────┘                       │
│         │            │              │                                        │
│         │            │              │                                        │
│         └────────────┼──────────────┘                                        │
│                      │                                                       │
│                      ▼                                                       │
│            ┌──────────────────────┐                                          │
│            │   Configuration 系统  │                                          │
│            │                      │                                          │
│            │   JSON → Env → Cmd   │                                          │
│            │   Options 模式       │                                          │
│            │   热更新             │                                          │
│            │                      │                                          │
│            │   为所有子系统提供     │                                          │
│            │   配置数据            │                                          │
│            └──────────────────────┘                                          │
│                                                                              │
│                                                                              │
│  一个SSO Token请求的完整路径：                                                 │
│                                                                              │
│  Kestrel(TCP→HTTP)                                                           │
│    → DI(创建Scope)                                                           │
│      → Configuration(读取配置)                                                │
│        → Routing(匹配/connect/token)                                         │
│          → Authentication(OpenIddict Handler短路)                             │
│            → DI(解析OpenIddict服务)                                           │
│              → 验证client → 验证code → 签发Token                              │
│                → 写入响应                                                     │
│    → DI(释放Scope)                                                           │
│  → Kestrel(响应→TCP)                                                         │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

**第4层全部6个核心子系统已经完成：**

✅ 依赖注入（DI）  
✅ 配置系统（Configuration）  
✅ 认证系统（Authentication）  
✅ 授权系统（Authorization）  
✅ 路由系统（Routing）  
✅ Kestrel HTTP 服务器

你现在对 ASP.NET Core 的底层有了源码级的理解。**接下来你想进入哪个阶段？**

- **第5层：设计哲学与模式** —— 理解 ASP.NET Core 中运用的设计模式（Builder、Strategy、Chain of Responsibility、Options Pattern 等）
- **第6层：实战调试** —— 如何在源码级别调试 ASP.NET Core 和 OpenIddict 问题
- **第7层：OpenIddict SSO 完整实战** —— 从零搭建 SSO 授权服务器 + 客户端 + API 资源服务器