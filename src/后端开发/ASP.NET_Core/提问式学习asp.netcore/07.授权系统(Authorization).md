---
title: 授权系统(Authorization)
date: 2025-04-09
category:
  - ASP.NET_Core
tag:
  - asp.netcore
  - 授权
  - Authorization
  - 源码分析
author: Moklgy
order: 7
---

# ASP.NET Core 授权系统（Authorization）：源码级深入

---

## 一、核心认知

> **授权系统回答的是："这个人能不能做这件事？"**
>
> 认证告诉你"他是谁"（ClaimsPrincipal），授权告诉你"他能干什么"。

```
认证系统的输出：
  context.User = ClaimsPrincipal
  {
      Name: "zhangsan",
      Claims: [
          { Type: "sub",   Value: "user-guid-123" },
          { Type: "role",  Value: "Admin" },
          { Type: "role",  Value: "Editor" },
          { Type: "email", Value: "zs@example.com" },
          { Type: "scope", Value: "openid profile api" },
          { Type: "department", Value: "Engineering" }
      ]
  }

                    ↓ 传给授权系统 ↓

授权系统的判断：
  [Authorize(Roles = "Admin")]           → ✅ 有Admin角色，通过
  [Authorize(Policy = "RequireEditor")]  → ✅ 有Editor角色，通过
  [Authorize(Policy = "Over18")]         → ❓ 要看策略怎么定义
  [AllowAnonymous]                       → ✅ 任何人都可以
  没有[Authorize]                        → ✅ 默认允许
```

---

## 二、授权系统的整体架构

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           授权系统全景                                    │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                      注册阶段 (DI)                                 │  │
│  │                                                                    │  │
│  │  services.AddAuthorization(options =>                              │  │
│  │  {                                                                 │  │
│  │      // 添加策略                                                    │  │
│  │      options.AddPolicy("AdminOnly", policy =>                      │  │
│  │          policy.RequireRole("Admin"));                              │  │
│  │                                                                    │  │
│  │      options.AddPolicy("CanEditArticle", policy =>                 │  │
│  │          policy.Requirements.Add(new ArticleEditRequirement()));    │  │
│  │                                                                    │  │
│  │      // 默认策略（影响所有 [Authorize]）                              │  │
│  │      options.DefaultPolicy = new AuthorizationPolicyBuilder()      │  │
│  │          .RequireAuthenticatedUser()                                │  │
│  │          .Build();                                                  │  │
│  │                                                                    │  │
│  │      // 回退策略（影响没有 [Authorize] 的端点）                       │  │
│  │      options.FallbackPolicy = null; // 默认null=允许匿名            │  │
│  │  });                                                               │  │
│  │                                                                    │  │
│  │  // 注册自定义 Handler                                              │  │
│  │  services.AddSingleton<IAuthorizationHandler, ArticleEditHandler>(); │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                      运行阶段 (中间件)                              │  │
│  │                                                                    │  │
│  │  app.UseAuthorization()                                            │  │
│  │      │                                                             │  │
│  │      ├── 获取当前 Endpoint                                         │  │
│  │      ├── 读取 Endpoint 上的 [Authorize] 元数据                      │  │
│  │      ├── 构建 AuthorizationPolicy                                  │  │
│  │      ├── 调用 IAuthorizationService.AuthorizeAsync()               │  │
│  │      │       │                                                     │  │
│  │      │       ├── 遍历 Policy 中的所有 Requirement                   │  │
│  │      │       ├── 对每个 Requirement 调用匹配的 Handler              │  │
│  │      │       └── 所有 Requirement 都满足 → 授权成功                  │  │
│  │      │                                                             │  │
│  │      ├── 授权成功 → 继续管道                                        │  │
│  │      └── 授权失败                                                   │  │
│  │          ├── 未认证 → Challenge()                                   │  │
│  │          └── 已认证但无权 → Forbid()                                │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  📁 源码仓库: dotnet/aspnetcore                                          │
│  📁 路径: src/Security/Authorization/                                    │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 三、核心类型关系

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│   AuthorizationPolicy（策略）                                       │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  Requirements: List<IAuthorizationRequirement>              │   │
│   │    ├── RolesAuthorizationRequirement { Roles: ["Admin"] }   │   │
│   │    ├── ClaimsAuthorizationRequirement { ClaimType: "email" }│   │
│   │    ├── DenyAnonymousAuthorizationRequirement                │   │
│   │    └── ArticleEditRequirement (自定义)                       │   │
│   │                                                             │   │
│   │  AuthenticationSchemes: List<string>                        │   │
│   │    ["Bearer", "Cookies"]                                    │   │
│   └─────────────────────────────────────────────────────────────┘   │
│        ↕                                                            │
│   IAuthorizationHandler（处理器）                                    │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  一个Handler可以处理一种或多种 Requirement                    │   │
│   │                                                             │   │
│   │  RolesAuthorizationHandler                                  │   │
│   │    → 处理 RolesAuthorizationRequirement                     │   │
│   │    → 检查 user.IsInRole("Admin")                            │   │
│   │                                                             │   │
│   │  ClaimsAuthorizationHandler                                 │   │
│   │    → 处理 ClaimsAuthorizationRequirement                    │   │
│   │    → 检查 user.HasClaim(type, value)                        │   │
│   │                                                             │   │
│   │  ArticleEditHandler (自定义)                                 │   │
│   │    → 处理 ArticleEditRequirement                            │   │
│   │    → 检查 用户是否是文章作者 或 是Admin                       │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 四、核心接口源码

### 4.1 IAuthorizationRequirement —— 需求

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/IAuthorizationRequirement.cs

// 空接口！只是一个标记
public interface IAuthorizationRequirement
{
    // 没有任何方法
    // 它只是说："我是一个授权需求"
    // 具体怎么检查，由 Handler 决定
}
```

### 4.2 IAuthorizationHandler —— 处理器

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/IAuthorizationHandler.cs

public interface IAuthorizationHandler
{
    Task HandleAsync(AuthorizationHandlerContext context);
}

// AuthorizationHandlerContext 包含：
// - User: ClaimsPrincipal
// - Requirements: 需要满足的所有需求
// - Resource: 被访问的资源（可选）
// - PendingRequirements: 还未满足的需求
// - HasSucceeded: 是否所有需求都已满足
```

### 4.3 AuthorizationHandler\<TRequirement\> —— 泛型基类

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/AuthorizationHandler.cs

// 这是你写自定义 Handler 时最常继承的基类
public abstract class AuthorizationHandler<TRequirement> : IAuthorizationHandler
    where TRequirement : IAuthorizationRequirement
{
    public virtual async Task HandleAsync(AuthorizationHandlerContext context)
    {
        // 🔥 遍历所有 Pending Requirements
        foreach (var req in context.Requirements.OfType<TRequirement>())
        {
            // 只处理匹配自己类型的 Requirement
            await HandleRequirementAsync(context, req);
        }
    }

    // 🔥 子类实现这个方法
    protected abstract Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        TRequirement requirement);
    
    // 在子类中：
    // context.Succeed(requirement)   → 标记此需求已满足
    // context.Fail()                 → 显式失败（阻止其他Handler成功）
    // 什么都不做                      → 此Handler不满足，但其他Handler可能满足
}

// 还有一个带资源的版本
public abstract class AuthorizationHandler<TRequirement, TResource> : IAuthorizationHandler
    where TRequirement : IAuthorizationRequirement
{
    protected abstract Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        TRequirement requirement,
        TResource resource);  // 👈 多了一个资源参数
}
```

### 4.4 AuthorizationPolicy —— 策略

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/AuthorizationPolicy.cs

public class AuthorizationPolicy
{
    // 🔥 需要满足的所有需求（必须全部满足）
    public IReadOnlyList<IAuthorizationRequirement> Requirements { get; }
    
    // 🔥 需要使用的认证方案
    public IReadOnlyList<string> AuthenticationSchemes { get; }

    public AuthorizationPolicy(
        IEnumerable<IAuthorizationRequirement> requirements,
        IEnumerable<string> authenticationSchemes)
    {
        Requirements = new List<IAuthorizationRequirement>(requirements).AsReadOnly();
        AuthenticationSchemes = new List<string>(authenticationSchemes).AsReadOnly();
    }

    // 🔥 合并多个策略
    public static AuthorizationPolicy Combine(params AuthorizationPolicy[] policies)
    {
        var requirements = new List<IAuthorizationRequirement>();
        var schemes = new List<string>();
        
        foreach (var policy in policies)
        {
            requirements.AddRange(policy.Requirements);
            schemes.AddRange(policy.AuthenticationSchemes);
        }
        
        return new AuthorizationPolicy(requirements, schemes.Distinct());
    }
}
```

### 4.5 AuthorizationPolicyBuilder —— 策略构建器

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/AuthorizationPolicyBuilder.cs

public class AuthorizationPolicyBuilder
{
    private readonly List<IAuthorizationRequirement> _requirements = new();
    private readonly List<string> _authenticationSchemes = new();

    // ============================
    // 内置的便捷方法
    // ============================
    
    // 🔥 要求已认证用户（最常用）
    public AuthorizationPolicyBuilder RequireAuthenticatedUser()
    {
        _requirements.Add(new DenyAnonymousAuthorizationRequirement());
        return this;
    }

    // 🔥 要求特定角色
    public AuthorizationPolicyBuilder RequireRole(params string[] roles)
    {
        _requirements.Add(new RolesAuthorizationRequirement(roles));
        return this;
    }

    // 🔥 要求特定声明
    public AuthorizationPolicyBuilder RequireClaim(string claimType, params string[] allowedValues)
    {
        if (allowedValues.Length == 0)
        {
            // 只要有这个声明就行，不管值
            _requirements.Add(new ClaimsAuthorizationRequirement(claimType, null));
        }
        else
        {
            // 声明的值必须在允许列表中
            _requirements.Add(new ClaimsAuthorizationRequirement(claimType, allowedValues));
        }
        return this;
    }

    // 🔥 要求用户名
    public AuthorizationPolicyBuilder RequireUserName(string userName)
    {
        _requirements.Add(new NameAuthorizationRequirement(userName));
        return this;
    }

    // 🔥 自定义断言
    public AuthorizationPolicyBuilder RequireAssertion(Func<AuthorizationHandlerContext, bool> handler)
    {
        _requirements.Add(new AssertionRequirement(handler));
        return this;
    }

    // 🔥 添加自定义需求
    public AuthorizationPolicyBuilder AddRequirements(params IAuthorizationRequirement[] requirements)
    {
        _requirements.AddRange(requirements);
        return this;
    }

    // 🔥 指定认证方案
    public AuthorizationPolicyBuilder AddAuthenticationSchemes(params string[] schemes)
    {
        _authenticationSchemes.AddRange(schemes);
        return this;
    }

    // 构建
    public AuthorizationPolicy Build()
    {
        return new AuthorizationPolicy(_requirements, _authenticationSchemes.Distinct());
    }
}
```

---

## 五、内置 Requirement 和 Handler 的源码

### 5.1 DenyAnonymousAuthorizationRequirement

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/
//              DenyAnonymousAuthorizationRequirement.cs

// RequireAuthenticatedUser() 添加的就是这个
public class DenyAnonymousAuthorizationRequirement :
    AuthorizationHandler<DenyAnonymousAuthorizationRequirement>,
    IAuthorizationRequirement
    // ☝️ 既是 Requirement 又是 Handler！自己处理自己
{
    protected override Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        DenyAnonymousAuthorizationRequirement requirement)
    {
        var user = context.User;
        
        // 🔥 检查是否有已认证的身份
        var userIsAnonymous =
            user?.Identity == null ||
            !user.Identities.Any(i => i.IsAuthenticated);
        
        if (!userIsAnonymous)
        {
            context.Succeed(requirement);  // ✅ 已认证
        }
        // 如果匿名 → 不调用 Succeed，也不调用 Fail
        // 这意味着此需求未满足，但不会阻止其他Handler
        
        return Task.CompletedTask;
    }
}
```

### 5.2 RolesAuthorizationRequirement

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/
//              RolesAuthorizationRequirement.cs

public class RolesAuthorizationRequirement :
    AuthorizationHandler<RolesAuthorizationRequirement>,
    IAuthorizationRequirement
{
    public IEnumerable<string> AllowedRoles { get; }

    public RolesAuthorizationRequirement(IEnumerable<string> allowedRoles)
    {
        AllowedRoles = allowedRoles;
    }

    protected override Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        RolesAuthorizationRequirement requirement)
    {
        if (context.User != null)
        {
            // 🔥 检查用户是否拥有任意一个要求的角色
            foreach (var role in requirement.AllowedRoles)
            {
                if (context.User.IsInRole(role))
                {
                    context.Succeed(requirement);  // ✅ 有角色
                    return Task.CompletedTask;
                }
            }
        }
        // 没有匹配的角色 → 不调用 Succeed
        
        return Task.CompletedTask;
    }
}
```

**`IsInRole()` 的内部实现：**

```csharp
// System.Security.Claims.ClaimsPrincipal

public virtual bool IsInRole(string role)
{
    // 遍历所有身份
    for (int i = 0; i < _identities.Count; i++)
    {
        if (_identities[i] != null && _identities[i].HasClaim(
            _identities[i].RoleClaimType, role))
        //                 ^^^^^^^^^^^^^^^
        // 默认是 ClaimTypes.Role = "http://schemas.microsoft.com/ws/2008/06/identity/claims/role"
        // 但 JWT 通常用 "role" 而不是完整URI
        // OpenIddict 默认也用 "role"
        {
            return true;
        }
    }
    return false;
}
```

### 5.3 ClaimsAuthorizationRequirement

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/
//              ClaimsAuthorizationRequirement.cs

public class ClaimsAuthorizationRequirement :
    AuthorizationHandler<ClaimsAuthorizationRequirement>,
    IAuthorizationRequirement
{
    public string ClaimType { get; }
    public IEnumerable<string>? AllowedValues { get; }

    protected override Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        ClaimsAuthorizationRequirement requirement)
    {
        if (context.User != null)
        {
            if (requirement.AllowedValues == null || !requirement.AllowedValues.Any())
            {
                // 🔥 只检查是否存在此类型的声明（不管值）
                if (context.User.Claims.Any(c =>
                    string.Equals(c.Type, requirement.ClaimType, StringComparison.OrdinalIgnoreCase)))
                {
                    context.Succeed(requirement);
                }
            }
            else
            {
                // 🔥 检查声明值是否在允许列表中
                if (context.User.Claims.Any(c =>
                    string.Equals(c.Type, requirement.ClaimType, StringComparison.OrdinalIgnoreCase) &&
                    requirement.AllowedValues.Contains(c.Value, StringComparer.Ordinal)))
                {
                    context.Succeed(requirement);
                }
            }
        }
        
        return Task.CompletedTask;
    }
}
```

---

## 六、[Authorize] 特性到 Policy 的转换

### 6.1 AuthorizeAttribute

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/AuthorizeAttribute.cs

[AttributeUsage(AttributeTargets.Class | AttributeTargets.Method, 
    AllowMultiple = true, Inherited = true)]
public class AuthorizeAttribute : Attribute, IAuthorizeData
{
    public string? Policy { get; set; }               // 策略名
    public string? Roles { get; set; }                 // 角色（逗号分隔）
    public string? AuthenticationSchemes { get; set; }  // 认证方案（逗号分隔）
}

// IAuthorizeData 接口
public interface IAuthorizeData
{
    string? Policy { get; set; }
    string? Roles { get; set; }
    string? AuthenticationSchemes { get; set; }
}
```

### 6.2 从 [Authorize] 到 Policy 的转换过程

```csharp
// 📁 源码位置: src/Security/Authorization/Policy/src/AuthorizationPolicy.cs

public class AuthorizationPolicy
{
    // 🔥 将 [Authorize] 特性数据转换为 Policy
    public static async Task<AuthorizationPolicy?> CombineAsync(
        IAuthorizationPolicyProvider policyProvider,
        IEnumerable<IAuthorizeData> authorizeData)
    {
        AuthorizationPolicyBuilder? policyBuilder = null;

        foreach (var authorizeDatum in authorizeData)
        {
            policyBuilder ??= new AuthorizationPolicyBuilder();

            // 🔥 处理 Policy 属性
            if (!string.IsNullOrWhiteSpace(authorizeDatum.Policy))
            {
                var policy = await policyProvider.GetPolicyAsync(authorizeDatum.Policy);
                if (policy == null)
                {
                    throw new InvalidOperationException(
                        $"The AuthorizationPolicy named: '{authorizeDatum.Policy}' was not found.");
                }
                policyBuilder.Combine(policy);
            }

            // 🔥 处理 Roles 属性
            if (!string.IsNullOrWhiteSpace(authorizeDatum.Roles))
            {
                var roles = authorizeDatum.Roles.Split(',')
                    .Select(r => r.Trim())
                    .Where(r => r.Length > 0);
                    
                policyBuilder.RequireRole(roles.ToArray());
                // [Authorize(Roles = "Admin,Editor")]
                // → RequireRole("Admin", "Editor")
                // → 用户有 Admin 或 Editor 任意一个就通过
            }

            // 🔥 处理 AuthenticationSchemes 属性
            if (!string.IsNullOrWhiteSpace(authorizeDatum.AuthenticationSchemes))
            {
                var schemes = authorizeDatum.AuthenticationSchemes.Split(',')
                    .Select(s => s.Trim())
                    .Where(s => s.Length > 0);
                    
                policyBuilder.AddAuthenticationSchemes(schemes.ToArray());
            }
        }

        if (policyBuilder == null)
        {
            return null;
        }

        // 🔥 如果没有显式设置 Policy，合并默认策略
        var builtPolicy = policyBuilder.Build();
        
        if (builtPolicy.Requirements.Count == 0 && 
            builtPolicy.AuthenticationSchemes.Count == 0)
        {
            // [Authorize] 没有参数 → 使用默认策略
            var defaultPolicy = await policyProvider.GetDefaultPolicyAsync();
            return defaultPolicy;
            // 默认策略 = RequireAuthenticatedUser()
        }

        return builtPolicy;
    }
}
```

**各种 [Authorize] 写法对应的 Policy：**

```
[Authorize]
→ 默认策略：RequireAuthenticatedUser()
→ Requirements: [ DenyAnonymousAuthorizationRequirement ]

[Authorize(Roles = "Admin")]
→ Requirements: [ RolesAuthorizationRequirement { AllowedRoles: ["Admin"] } ]
  注意：没有 DenyAnonymous！但角色检查本身隐含了要认证

[Authorize(Roles = "Admin,Editor")]
→ Requirements: [ RolesAuthorizationRequirement { AllowedRoles: ["Admin", "Editor"] } ]
  任意一个角色即可（OR关系）

[Authorize(Roles = "Admin")]
[Authorize(Roles = "Editor")]  （两个 [Authorize]）
→ Requirements: [
      RolesAuthorizationRequirement { AllowedRoles: ["Admin"] },
      RolesAuthorizationRequirement { AllowedRoles: ["Editor"] }
  ]
  两个都要满足（AND关系）！必须同时是Admin和Editor

[Authorize(Policy = "CanEditArticle")]
→ 从 IAuthorizationPolicyProvider 获取名为 "CanEditArticle" 的策略
→ 策略中定义了什么需求就是什么需求

[Authorize(AuthenticationSchemes = "Bearer")]
→ AuthenticationSchemes: ["Bearer"]
→ 只用 JWT Bearer 认证，不用 Cookie
→ Requirements: (取决于默认策略)

[Authorize(Policy = "AdminOnly", AuthenticationSchemes = "Bearer")]
→ AuthenticationSchemes: ["Bearer"]
→ Requirements: 来自 "AdminOnly" 策略的需求

[AllowAnonymous]
→ 跳过所有授权检查
```

---

## 七、AuthorizationMiddleware —— 授权中间件源码

```csharp
// 📁 源码位置: src/Security/Authorization/Policy/src/AuthorizationMiddleware.cs

public class AuthorizationMiddleware
{
    private readonly RequestDelegate _next;
    private readonly IAuthorizationPolicyProvider _policyProvider;

    public async Task Invoke(HttpContext context)
    {
        // 🔥 第1步：获取当前 Endpoint
        var endpoint = context.GetEndpoint();
        
        if (endpoint == null)
        {
            // 没有匹配到 Endpoint → 直接继续
            await _next(context);
            return;
        }

        // 🔥 第2步：获取 Endpoint 上的授权元数据
        var authorizeData = endpoint.Metadata.GetOrderedMetadata<IAuthorizeData>();
        // 也就是 [Authorize] 特性的数据

        // 🔥 第3步：检查是否有 [AllowAnonymous]
        if (endpoint.Metadata.GetMetadata<IAllowAnonymous>() != null)
        {
            // 有 [AllowAnonymous] → 跳过授权
            await _next(context);
            return;
        }

        // 🔥 第4步：构建授权策略
        var policy = await AuthorizationPolicy.CombineAsync(
            _policyProvider, authorizeData);
        
        if (policy == null)
        {
            // 没有 [Authorize] → 检查回退策略
            var fallbackPolicy = await _policyProvider.GetFallbackPolicyAsync();
            
            if (fallbackPolicy == null)
            {
                // 没有回退策略 → 允许访问
                await _next(context);
                return;
            }
            
            policy = fallbackPolicy;
        }

        // 🔥 第5步：如果策略指定了认证方案，先用指定方案认证
        if (policy.AuthenticationSchemes.Any())
        {
            ClaimsPrincipal? newPrincipal = null;
            
            foreach (var scheme in policy.AuthenticationSchemes)
            {
                var result = await context.AuthenticateAsync(scheme);
                if (result?.Succeeded ?? false)
                {
                    newPrincipal = SecurityHelper.MergeUserPrincipal(
                        newPrincipal, result.Principal);
                }
            }
            
            if (newPrincipal != null)
            {
                context.User = newPrincipal;
                // 用指定方案的认证结果替换 context.User
            }
        }

        // 🔥 第6步：执行授权检查
        var authorizeResult = await _policyEvaluator.AuthorizeAsync(
            policy, 
            context.AuthenticateAsync,  // 认证结果
            context, 
            resource: endpoint);

        // 🔥 第7步：根据结果处理
        if (authorizeResult.Challenged)
        {
            // 未认证 → Challenge
            if (policy.AuthenticationSchemes.Any())
            {
                foreach (var scheme in policy.AuthenticationSchemes)
                {
                    await context.ChallengeAsync(scheme);
                }
            }
            else
            {
                await context.ChallengeAsync();
                // 使用默认 Challenge 方案
                // Cookie → 302 到登录页
                // JWT → 401
            }
            return;  // ⛔ 短路！
        }
        else if (authorizeResult.Forbidden)
        {
            // 已认证但无权限 → Forbid
            if (policy.AuthenticationSchemes.Any())
            {
                foreach (var scheme in policy.AuthenticationSchemes)
                {
                    await context.ForbidAsync(scheme);
                }
            }
            else
            {
                await context.ForbidAsync();
                // 通常返回 403
            }
            return;  // ⛔ 短路！
        }

        // 🔥 授权通过 → 继续管道
        await _next(context);
    }
}
```

---

## 八、PolicyEvaluator 和 DefaultAuthorizationService —— 授权判断的核心

### 8.1 PolicyEvaluator

```csharp
// 📁 源码位置: src/Security/Authorization/Policy/src/PolicyEvaluator.cs

public class PolicyEvaluator : IPolicyEvaluator
{
    private readonly IAuthorizationService _authorization;

    // 🔥 认证阶段
    public async Task<AuthenticateResult> AuthenticateAsync(
        AuthorizationPolicy policy, HttpContext context)
    {
        if (policy.AuthenticationSchemes.Any())
        {
            // 使用策略指定的方案认证
            ClaimsPrincipal? newPrincipal = null;
            DateTimeOffset? minExpiresUtc = null;

            foreach (var scheme in policy.AuthenticationSchemes)
            {
                var result = await context.AuthenticateAsync(scheme);
                if (result != null && result.Succeeded)
                {
                    newPrincipal = SecurityHelper.MergeUserPrincipal(
                        newPrincipal, result.Principal);
                }
            }
            
            if (newPrincipal != null)
            {
                context.User = newPrincipal;
                return AuthenticateResult.Success(
                    new AuthenticationTicket(newPrincipal, 
                        string.Join(";", policy.AuthenticationSchemes)));
            }
            
            return AuthenticateResult.NoResult();
        }

        // 使用默认认证（已在AuthenticationMiddleware中完成）
        return context.User?.Identity?.IsAuthenticated ?? false
            ? AuthenticateResult.Success(
                new AuthenticationTicket(context.User, "context.User"))
            : AuthenticateResult.NoResult();
    }

    // 🔥 授权阶段
    public async Task<PolicyAuthorizationResult> AuthorizeAsync(
        AuthorizationPolicy policy,
        AuthenticateResult authenticationResult,
        HttpContext context,
        object? resource)
    {
        // 🔥🔥🔥 调用 IAuthorizationService
        var result = await _authorization.AuthorizeAsync(
            context.User!, 
            resource, 
            policy);

        if (result.Succeeded)
        {
            return PolicyAuthorizationResult.Success();
        }

        // 区分 Challenge 和 Forbid
        if (authenticationResult.Succeeded)
        {
            // 已认证但授权失败 → Forbid (403)
            return PolicyAuthorizationResult.Forbid(result.Failure);
        }
        else
        {
            // 未认证 → Challenge (要求登录)
            return PolicyAuthorizationResult.Challenge();
        }
    }
}
```

### 8.2 DefaultAuthorizationService —— 授权判断的核心逻辑

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/DefaultAuthorizationService.cs

public class DefaultAuthorizationService : IAuthorizationService
{
    private readonly IAuthorizationPolicyProvider _policyProvider;
    private readonly IAuthorizationHandlerProvider _handlers;
    private readonly IAuthorizationHandlerContextFactory _contextFactory;
    private readonly AuthorizationOptions _options;
    private readonly IAuthorizationEvaluator _evaluator;

    public async Task<AuthorizationResult> AuthorizeAsync(
        ClaimsPrincipal user,
        object? resource,
        IEnumerable<IAuthorizationRequirement> requirements)
    {
        // 🔥 第1步：创建授权上下文
        var authContext = _contextFactory.CreateContext(requirements, user, resource);
        // AuthorizationHandlerContext 包含：
        //   - User: ClaimsPrincipal
        //   - Requirements: 所有需求
        //   - Resource: 被访问的资源
        //   - PendingRequirements: 还未满足的需求
        
        // 🔥 第2步：获取所有注册的 Handler
        var handlers = await _handlers.GetHandlersAsync(authContext);

        // 🔥 第3步：逐个执行 Handler
        foreach (var handler in handlers)
        {
            await handler.HandleAsync(authContext);
            
            // 如果配置了 InvokeHandlersAfterFailure = false
            // 且已经有明确的失败，就不继续执行其他Handler
            if (!_options.InvokeHandlersAfterFailure && authContext.HasFailed)
            {
                break;
            }
        }

        // 🔥 第4步：评估结果
        var result = _evaluator.Evaluate(authContext);
        
        // 评估逻辑：
        // - 如果 HasFailed = true → 失败（有Handler显式调用了Fail()）
        // - 如果所有 Requirements 都被 Succeed 了 → 成功
        // - 否则 → 失败（有未满足的Requirement）
        
        if (result.Succeeded)
        {
            _logger.UserAuthorizationSucceeded();
        }
        else
        {
            _logger.UserAuthorizationFailed(result.Failure);
        }

        return result;
    }
}
```

### 8.3 AuthorizationHandlerContext —— 授权上下文

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/AuthorizationHandlerContext.cs

public class AuthorizationHandlerContext
{
    private readonly HashSet<IAuthorizationRequirement> _pendingRequirements;
    private bool _failCalled;
    private bool _succeedCalled;

    public AuthorizationHandlerContext(
        IEnumerable<IAuthorizationRequirement> requirements,
        ClaimsPrincipal user,
        object? resource)
    {
        Requirements = requirements;
        User = user;
        Resource = resource;
        _pendingRequirements = new HashSet<IAuthorizationRequirement>(requirements);
    }

    public ClaimsPrincipal User { get; }
    public object? Resource { get; }
    public IEnumerable<IAuthorizationRequirement> Requirements { get; }
    
    // 还未满足的需求
    public IEnumerable<IAuthorizationRequirement> PendingRequirements 
        => _pendingRequirements;

    // 🔥 是否所有需求都已满足
    public bool HasSucceeded 
        => !_failCalled && _succeedCalled && !_pendingRequirements.Any();
    
    public bool HasFailed => _failCalled;

    // 🔥 标记某个需求已满足
    public void Succeed(IAuthorizationRequirement requirement)
    {
        _succeedCalled = true;
        _pendingRequirements.Remove(requirement);
    }

    // 🔥 显式标记失败（即使其他需求都满足了也会失败）
    public void Fail()
    {
        _failCalled = true;
    }
    
    // ⚠️ 注意没有 "Skip" 方法
    // 如果Handler什么都不做 → 需求仍然 Pending → 最终 HasSucceeded = false
}
```

**Succeed、Fail 和 "什么都不做" 的区别：**

```txt
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│  场景：Policy有3个Requirement [R1, R2, R3]                           │
│        有4个Handler [H1, H2, H3, H4]                                │
│                                                                      │
│  ─────────── 正常成功 ───────────                                    │
│  H1: context.Succeed(R1)    → PendingRequirements: [R2, R3]         │
│  H2: context.Succeed(R2)    → PendingRequirements: [R3]             │
│  H3: 什么都不做              → PendingRequirements: [R3]             │
│  H4: context.Succeed(R3)    → PendingRequirements: []               │
│  结果：HasSucceeded = true ✅                                        │
│                                                                      │
│  ─────────── 需求未满足 ───────────                                  │
│  H1: context.Succeed(R1)    → PendingRequirements: [R2, R3]         │
│  H2: context.Succeed(R2)    → PendingRequirements: [R3]             │
│  H3: 什么都不做              → PendingRequirements: [R3]             │
│  H4: 什么都不做              → PendingRequirements: [R3]  ← 仍有未满足│
│  结果：HasSucceeded = false ❌ (R3没有被任何Handler满足)              │
│                                                                      │
│  ─────────── 显式失败 ───────────                                    │
│  H1: context.Succeed(R1)    → PendingRequirements: [R2, R3]         │
│  H2: context.Fail()          → _failCalled = true                    │
│  H3: context.Succeed(R2)    → PendingRequirements: [R3]             │
│  H4: context.Succeed(R3)    → PendingRequirements: []               │
│  结果：HasSucceeded = false ❌ (即使所有R都被Succeed了)              │
│         因为 HasSucceeded = !_failCalled && !pending.Any()           │
│         Fail() 是一票否决 ⛔                                         │
│                                                                      │
│  ⚠️ Fail() 的用途：例如账户被锁定、IP黑名单等                       │
│     不管用户有什么角色/声明，直接拒绝                                  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 九、IAuthorizationHandlerProvider —— Handler 的获取方式

```csharp
// 📁 源码位置: src/Security/Authorization/Core/src/
//              DefaultAuthorizationHandlerProvider.cs

public class DefaultAuthorizationHandlerProvider : IAuthorizationHandlerProvider
{
    private readonly IEnumerable<IAuthorizationHandler> _handlers;

    public DefaultAuthorizationHandlerProvider(
        IEnumerable<IAuthorizationHandler> handlers)
    {
        // 🔥 从DI容器中获取所有注册的 IAuthorizationHandler
        _handlers = handlers;
    }

    public Task<IEnumerable<IAuthorizationHandler>> GetHandlersAsync(
        AuthorizationHandlerContext context)
    {
        return Task.FromResult(_handlers);
    }
}
```

**Handler 的来源：**

```txt
DI容器中的所有 IAuthorizationHandler：

1. 内置的（Requirements 自己实现了 IAuthorizationHandler）：
   - DenyAnonymousAuthorizationRequirement（自己处理自己）
   - RolesAuthorizationRequirement（自己处理自己）
   - ClaimsAuthorizationRequirement（自己处理自己）
   - NameAuthorizationRequirement（自己处理自己）
   - AssertionRequirement（自己处理自己）

2. 框架注册的通用Handler：
   - PassThroughAuthorizationHandler
     → 发现 Requirement 同时是 Handler 时，让它自己处理自己
     → 这就是上面那些 "自己处理自己" 的机制

3. 你自定义注册的：
   services.AddSingleton<IAuthorizationHandler, ArticleEditHandler>();
   services.AddSingleton<IAuthorizationHandler, MinimumAgeHandler>();
```

```csharp
// 📁 PassThroughAuthorizationHandler 源码
// src/Security/Authorization/Core/src/PassThroughAuthorizationHandler.cs

public class PassThroughAuthorizationHandler : IAuthorizationHandler
{
    public async Task HandleAsync(AuthorizationHandlerContext context)
    {
        // 🔥 遍历所有 Requirements
        foreach (var handler in context.Requirements.OfType<IAuthorizationHandler>())
        {
            // 如果 Requirement 自己也是 Handler → 让它自己处理
            await handler.HandleAsync(context);
        }
    }
}
```

---

## 十、自定义授权策略实战 —— SSO 项目中的常见场景

### 10.1 基于 Scope 的授权（API 资源保护）

```csharp
// ========== Requirement ==========
public class ScopeRequirement : IAuthorizationRequirement
{
    public string RequiredScope { get; }
    
    public ScopeRequirement(string requiredScope)
    {
        RequiredScope = requiredScope;
    }
}

// ========== Handler ==========
public class ScopeAuthorizationHandler : AuthorizationHandler<ScopeRequirement>
{
    protected override Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        ScopeRequirement requirement)
    {
        // 🔥 检查 Token 中的 scope 声明
        // OpenIddict 签发的Token中，scope可能是空格分隔的字符串
        // 也可能是多个独立的 scope claim
        
        var scopeClaims = context.User.FindAll("scope");
        
        foreach (var claim in scopeClaims)
        {
            // 处理空格分隔的情况："openid profile api.read"
            var scopes = claim.Value.Split(' ', StringSplitOptions.RemoveEmptyEntries);
            
            if (scopes.Contains(requirement.RequiredScope, StringComparer.Ordinal))
            {
                context.Succeed(requirement);
                return Task.CompletedTask;
            }
        }
        
        // 也检查 "scp" 声明（某些 OIDC Provider 用这个）
        var scpClaims = context.User.FindAll("scp");
        foreach (var claim in scpClaims)
        {
            if (string.Equals(claim.Value, requirement.RequiredScope, StringComparison.Ordinal))
            {
                context.Succeed(requirement);
                return Task.CompletedTask;
            }
        }
        
        return Task.CompletedTask;
    }
}

// ========== 注册 ==========
builder.Services.AddSingleton<IAuthorizationHandler, ScopeAuthorizationHandler>();

builder.Services.AddAuthorization(options =>
{
    options.AddPolicy("read:users", policy =>
        policy.Requirements.Add(new ScopeRequirement("api.users.read")));
    
    options.AddPolicy("write:users", policy =>
        policy.Requirements.Add(new ScopeRequirement("api.users.write")));
    
    options.AddPolicy("admin:all", policy =>
    {
        policy.Requirements.Add(new ScopeRequirement("api.admin"));
        policy.RequireRole("Admin");  // 还要求Admin角色
    });
});

// ========== 使用 ==========
[ApiController]
[Route("api/[controller]")]
public class UsersController : ControllerBase
{
    [HttpGet]
    [Authorize(Policy = "read:users")]
    public IActionResult GetUsers() => Ok(/* ... */);
    
    [HttpPost]
    [Authorize(Policy = "write:users")]
    public IActionResult CreateUser([FromBody] CreateUserDto dto) => Ok(/* ... */);
    
    [HttpDelete("{id}")]
    [Authorize(Policy = "admin:all")]
    public IActionResult DeleteUser(string id) => Ok(/* ... */);
}
```

### 10.2 基于资源的授权（Resource-based Authorization）

```csharp
// 场景：用户只能编辑自己的文章，除非是Admin

// ========== Requirement ==========
public class ArticleOperationRequirement : IAuthorizationRequirement
{
    public string Operation { get; }
    
    public ArticleOperationRequirement(string operation)
    {
        Operation = operation;
    }
    
    public static readonly ArticleOperationRequirement Read = new("Read");
    public static readonly ArticleOperationRequirement Edit = new("Edit");
    public static readonly ArticleOperationRequirement Delete = new("Delete");
}

// ========== Resource ==========
public class Article
{
    public string Id { get; set; } = "";
    public string Title { get; set; } = "";
    public string AuthorId { get; set; } = "";   // 👈 文章作者
    public bool IsPublished { get; set; }
}

// ========== Handler ==========
public class ArticleAuthorizationHandler :
    AuthorizationHandler<ArticleOperationRequirement, Article>
    //                                                ^^^^^^^ 资源类型
{
    protected override Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        ArticleOperationRequirement requirement,
        Article resource)
    {
        var userId = context.User.FindFirst("sub")?.Value;

        switch (requirement.Operation)
        {
            case "Read":
                // 已发布的文章任何人可读，未发布的只有作者可读
                if (resource.IsPublished || resource.AuthorId == userId)
                {
                    context.Succeed(requirement);
                }
                break;

            case "Edit":
                // 作者可以编辑自己的文章
                if (resource.AuthorId == userId)
                {
                    context.Succeed(requirement);
                }
                // Admin也可以编辑任何文章
                if (context.User.IsInRole("Admin"))
                {
                    context.Succeed(requirement);
                }
                break;

            case "Delete":
                // 只有Admin可以删除
                if (context.User.IsInRole("Admin"))
                {
                    context.Succeed(requirement);
                }
                break;
        }

        return Task.CompletedTask;
    }
}

// ========== 注册 ==========
builder.Services.AddSingleton<IAuthorizationHandler, ArticleAuthorizationHandler>();

// ========== 在Controller中使用 ==========
[ApiController]
[Route("api/articles")]
public class ArticlesController : ControllerBase
{
    private readonly IAuthorizationService _authorizationService;
    private readonly IArticleRepository _articleRepo;

    public ArticlesController(
        IAuthorizationService authorizationService,
        IArticleRepository articleRepo)
    {
        _authorizationService = authorizationService;
        _articleRepo = articleRepo;
    }

    [HttpPut("{id}")]
    [Authorize]  // 需要认证，但授权在方法内判断
    public async Task<IActionResult> EditArticle(string id, [FromBody] EditArticleDto dto)
    {
        var article = await _articleRepo.GetByIdAsync(id);
        if (article == null) return NotFound();

        // 🔥 基于资源的授权检查
        var authResult = await _authorizationService.AuthorizeAsync(
            User,                                    // ClaimsPrincipal
            article,                                 // 资源
            ArticleOperationRequirement.Edit);       // 需求

        if (!authResult.Succeeded)
        {
            return Forbid();  // 403
        }

        // 授权通过 → 执行编辑
        article.Title = dto.Title;
        await _articleRepo.UpdateAsync(article);
        return Ok(article);
    }
}
```

### 10.3 动态策略提供者（IAuthorizationPolicyProvider）

```csharp
// 场景：权限太多，不可能全部在启动时注册
// 比如 RBAC 系统，权限存在数据库中

// ========== 自定义 Attribute ==========
public class PermissionAttribute : AuthorizeAttribute
{
    public PermissionAttribute(string permission)
    {
        // 把权限名作为 Policy 名
        Policy = $"Permission:{permission}";
    }
}

// ========== Requirement ==========
public class PermissionRequirement : IAuthorizationRequirement
{
    public string Permission { get; }
    
    public PermissionRequirement(string permission)
    {
        Permission = permission;
    }
}

// ========== Handler ==========
public class PermissionAuthorizationHandler :
    AuthorizationHandler<PermissionRequirement>
{
    private readonly IServiceScopeFactory _scopeFactory;
    
    public PermissionAuthorizationHandler(IServiceScopeFactory scopeFactory)
    {
        _scopeFactory = scopeFactory;
    }
    
    protected override async Task HandleRequirementAsync(
        AuthorizationHandlerContext context,
        PermissionRequirement requirement)
    {
        var userId = context.User.FindFirst("sub")?.Value;
        if (userId == null) return;

        // 从数据库查询用户权限
        using var scope = _scopeFactory.CreateScope();
        var permissionService = scope.ServiceProvider
            .GetRequiredService<IPermissionService>();

        var hasPermission = await permissionService
            .UserHasPermissionAsync(userId, requirement.Permission);

        if (hasPermission)
        {
            context.Succeed(requirement);
        }
    }
}

// ========== 动态策略提供者 ==========
public class PermissionPolicyProvider : IAuthorizationPolicyProvider
{
    private readonly DefaultAuthorizationPolicyProvider _fallbackProvider;

    public PermissionPolicyProvider(IOptions<AuthorizationOptions> options)
    {
        // 回退到默认Provider（处理非Permission策略）
        _fallbackProvider = new DefaultAuthorizationPolicyProvider(options);
    }

    public Task<AuthorizationPolicy> GetDefaultPolicyAsync()
        => _fallbackProvider.GetDefaultPolicyAsync();

    public Task<AuthorizationPolicy?> GetFallbackPolicyAsync()
        => _fallbackProvider.GetFallbackPolicyAsync();

    public Task<AuthorizationPolicy?> GetPolicyAsync(string policyName)
    {
        // 🔥 检查是否是 Permission 策略
        if (policyName.StartsWith("Permission:", StringComparison.OrdinalIgnoreCase))
        {
            var permission = policyName.Substring("Permission:".Length);
            
            // 动态创建策略
            var policy = new AuthorizationPolicyBuilder()
                .RequireAuthenticatedUser()
                .AddRequirements(new PermissionRequirement(permission))
                .Build();
            
            return Task.FromResult<AuthorizationPolicy?>(policy);
        }
        
        // 不是Permission策略 → 回退到默认Provider
        return _fallbackProvider.GetPolicyAsync(policyName);
    }
}

// ========== 注册 ==========
// ⚠️ IAuthorizationPolicyProvider 是 Singleton，且只能注册一个
builder.Services.AddSingleton<IAuthorizationPolicyProvider, PermissionPolicyProvider>();
builder.Services.AddSingleton<IAuthorizationHandler, PermissionAuthorizationHandler>();

// ========== 使用 ==========
[ApiController]
[Route("api/users")]
public class UsersController : ControllerBase
{
    [HttpGet]
    [Permission("users.read")]      // → Policy = "Permission:users.read"
    public IActionResult GetUsers() => Ok();

    [HttpPost]
    [Permission("users.create")]    // → Policy = "Permission:users.create"
    public IActionResult CreateUser() => Ok();

    [HttpDelete("{id}")]
    [Permission("users.delete")]    // → Policy = "Permission:users.delete"
    public IActionResult DeleteUser(string id) => Ok();
}
```

**动态策略的执行流程：**

```bash
请求: DELETE /api/users/123
Endpoint元数据: [Permission("users.delete")]
    → Policy = "Permission:users.delete"

授权中间件：
│
├── 1. 获取策略
│   AuthorizationPolicy.CombineAsync()
│       → policyProvider.GetPolicyAsync("Permission:users.delete")
│       → PermissionPolicyProvider.GetPolicyAsync()
│       → 匹配 "Permission:" 前缀 ✅
│       → 动态创建 Policy:
│           Requirements: [
│               DenyAnonymousAuthorizationRequirement,
│               PermissionRequirement { Permission: "users.delete" }
│           ]
│
├── 2. 执行授权
│   DefaultAuthorizationService.AuthorizeAsync()
│       │
│       ├── Handler1: PassThroughAuthorizationHandler
│       │   → DenyAnonymousRequirement 处理自己
│       │   → user.IsAuthenticated = true
│       │   → context.Succeed(DenyAnonymousRequirement) ✅
│       │
│       ├── Handler2: PermissionAuthorizationHandler
│       │   → 查询数据库：用户"zhangsan"是否有"users.delete"权限
│       │   → 有 → context.Succeed(PermissionRequirement) ✅
│       │
│       └── 所有 PendingRequirements = [] → HasSucceeded = true
│
└── 3. 授权通过 → 继续执行 Controller
```

---

## 十一、DefaultPolicy 和 FallbackPolicy 的区别

```csharp
builder.Services.AddAuthorization(options =>
{
    // ========== DefaultPolicy ==========
    // 影响范围：所有没有指定 Policy 的 [Authorize]
    //
    // [Authorize]  → 使用 DefaultPolicy
    // [Authorize(Roles = "Admin")]  → 使用 DefaultPolicy + RequireRole("Admin")
    // [Authorize(Policy = "MyPolicy")]  → 使用 "MyPolicy"，不受 DefaultPolicy 影响
    
    options.DefaultPolicy = new AuthorizationPolicyBuilder()
        .RequireAuthenticatedUser()
        // 默认就是这个
        .Build();

    // ========== FallbackPolicy ==========
    // 影响范围：所有没有 [Authorize] 也没有 [AllowAnonymous] 的端点
    //
    // 默认是 null（允许匿名）
    
    options.FallbackPolicy = null;  // 默认值：不加限制
    
    // 如果你想让所有端点默认都需要认证：
    // options.FallbackPolicy = new AuthorizationPolicyBuilder()
    //     .RequireAuthenticatedUser()
    //     .Build();
    // ⚠️ 这样所有没标注的端点都需要认证
    // 需要用 [AllowAnonymous] 显式标注允许匿名的端点
});
```

```txt
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  端点标注               FallbackPolicy=null    FallbackPolicy=   │
│                        (默认)                RequireAuth          │
│  ────────────────────── ───────────────────── ──────────────────  │
│                                                                  │
│  [Authorize]            需要认证 ✅            需要认证 ✅         │
│                                                                  │
│  [Authorize(Role="X")]  需要认证+角色 ✅       需要认证+角色 ✅    │
│                                                                  │
│  [AllowAnonymous]       允许匿名 ✅            允许匿名 ✅         │
│                                                                  │
│  无标注                 允许匿名 ✅            需要认证 ⚠️         │
│                                                                  │
│  SSO项目建议：                                                    │
│  授权服务器 → FallbackPolicy = null                              │
│    （登录页、Token端点等需要匿名访问）                              │
│  API服务器 → FallbackPolicy = RequireAuthenticatedUser            │
│    （所有API默认需要Token）                                       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 十二、授权与认证的协作——完整的请求判断流程图

```txt
请求进入
│
▼
UseRouting() ─── 匹配 Endpoint，设置到 HttpContext
│
▼
UseAuthentication() ─── 设置 context.User
│  │
│  ├── OpenIddict Handler → 处理 /connect/* 端点（可能短路）
│  └── 默认认证 → 解析 Cookie/JWT → 设置 context.User
│
▼
UseAuthorization() ─── 检查访问权限
│
├── 获取 Endpoint
│
├── 检查 [AllowAnonymous]? ──Yes──► 跳过授权，继续管道 ✅
│                │
│               No
│                ▼
├── 获取 [Authorize] 元数据
│
├── 有 [Authorize]? ──No──► 检查 FallbackPolicy
│                │          │
│               Yes         ├── FallbackPolicy = null → 允许 ✅
│                │          └── FallbackPolicy = X → 用X作为策略
│                ▼
├── 构建 AuthorizationPolicy
│   │
│   ├── [Authorize] → DefaultPolicy（RequireAuthenticatedUser）
│   ├── [Authorize(Roles="Admin")] → DefaultPolicy + RequireRole
│   ├── [Authorize(Policy="X")] → 从 PolicyProvider 获取 X
│   └── [Authorize(Schemes="Bearer")] → 指定认证方案
│
├── 策略指定了认证方案? ──Yes──► 用指定方案重新认证
│                │                替换 context.User
│               No
│                ▼
├── IAuthorizationService.AuthorizeAsync()
│   │
│   ├── 遍历所有 IAuthorizationHandler
│   │   │
│   │   ├── PassThroughHandler
│   │   │   → DenyAnonymousRequirement.HandleAsync()
│   │   │   → RolesAuthorizationRequirement.HandleAsync()
│   │   │
│   │   ├── 你的自定义 Handler
│   │   │   → ScopeAuthorizationHandler.HandleAsync()
│   │   │   → PermissionAuthorizationHandler.HandleAsync()
│   │   │
│   │   └── 检查所有 Requirements 是否都被 Succeed
│   │
│   └── 返回 AuthorizationResult
│
├── Succeeded? ──Yes──► 继续管道 → 执行 Controller ✅
│       │
│      No
│       ▼
├── 用户是否已认证?
│   │           │
│  Yes         No
│   │           │
│   ▼           ▼
│ Forbid()   Challenge()
│ 403        Cookie → 302 到登录页
│            JWT → 401 + WWW-Authenticate
│
▼
⛔ 管道短路，响应返回
```

---

## 十三、OpenIddict 授权与 ASP.NET Core 授权的关系

```txt
┌──────────────────────────────────────────────────────────────────────┐
│  这是两个层面的"授权"，不要混淆！                                      │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  OAuth 2.0 授权（OpenIddict 处理）                              │  │
│  │                                                                │  │
│  │  "用户是否授权某个应用访问他的资源？"                              │  │
│  │                                                                │  │
│  │  - 用户在同意页面点击"允许"                                      │  │
│  │  - 授权服务器签发 authorization_code                             │  │
│  │  - 客户端用 code 换取 access_token                              │  │
│  │  - access_token 中包含 scope（api.read, api.write 等）          │  │
│  │                                                                │  │
│  │  这是"用户授权应用代表自己行事"                                    │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                      │
│                              ↓                                       │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  ASP.NET Core 授权（Authorization中间件处理）                    │  │
│  │                                                                │  │
│  │  "这个请求（带着Token）是否有权访问这个API端点？"                  │  │
│  │                                                                │  │
│  │  - 检查 Token 中的 Claims                                      │  │
│  │  - 检查角色（role claim）                                       │  │
│  │  - 检查范围（scope claim）                                      │  │
│  │  - 检查自定义权限（从数据库查询）                                 │  │
│  │                                                                │  │
│  │  这是"API端点级别的访问控制"                                      │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  完整流程：                                                           │
│  1. 用户授权应用（OAuth）→ 应用获得 access_token（scope: api.read）   │
│  2. 应用调用 API（Authorization: Bearer xxx）                        │
│  3. API 验证 Token（认证）→ 得到 ClaimsPrincipal                     │
│  4. API 检查权限（授权）→ [Authorize(Policy = "read:users")]         │
│     → ScopeHandler 检查 scope claim 中是否包含 "api.read"            │
│     → 通过 ✅                                                        │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 十四、SSO 项目中的授权设计最佳实践

```csharp
// ========== SSO 授权服务器的 Program.cs ==========

var builder = WebApplication.CreateBuilder(args);

builder.Services.AddAuthorization(options =>
{
    // 授权服务器不设置 FallbackPolicy
    // 因为登录页、Token端点等需要匿名访问
    
    // 管理员策略
    options.AddPolicy("AdminOnly", policy =>
    {
        policy.RequireAuthenticatedUser();
        policy.RequireRole("Admin");
    });
});

var app = builder.Build();

// 注意中间件顺序！
app.UseRouting();
app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();

// Controller 中：
[Route("Account")]
public class AccountController : Controller
{
    [HttpGet("Login")]
    [AllowAnonymous]            // 登录页允许匿名
    public IActionResult Login() => View();

    [HttpPost("Login")]
    [AllowAnonymous]            // 登录提交允许匿名
    public async Task<IActionResult> Login(LoginModel model) { /* ... */ }
}

[Route("connect")]
public class AuthorizationController : Controller
{
    [HttpGet("authorize")]
    [HttpPost("authorize")]
    [Authorize]                  // 授权端点需要用户已登录
    public async Task<IActionResult> Authorize() { /* ... */ }
    
    // Token端点不需要 [Authorize]
    // 因为OpenIddict Handler已经在认证中间件层处理了
    [HttpPost("token")]
    public async Task<IActionResult> Exchange() { /* ... */ }
}

[Route("admin")]
[Authorize(Policy = "AdminOnly")]   // 管理后台需要Admin角色
public class AdminController : Controller
{
    public IActionResult Dashboard() => View();
    
    [HttpGet("clients")]
    public IActionResult ClientList() => View();
}
```

```csharp
// ========== API 资源服务器的 Program.cs ==========

var builder = WebApplication.CreateBuilder(args);

// 注册自定义Handler
builder.Services.AddSingleton<IAuthorizationHandler, ScopeAuthorizationHandler>();

builder.Services.AddAuthorization(options =>
{
    // 🔥 API 服务器：FallbackPolicy = 要求认证
    // 所有端点默认都需要Token
    options.FallbackPolicy = new AuthorizationPolicyBuilder()
        .RequireAuthenticatedUser()
        .Build();
    
    // Scope-based 策略
    options.AddPolicy("read:users", policy =>
        policy.Requirements.Add(new ScopeRequirement("api.users.read")));
    
    options.AddPolicy("write:users", policy =>
        policy.Requirements.Add(new ScopeRequirement("api.users.write")));
    
    options.AddPolicy("read:articles", policy =>
        policy.Requirements.Add(new ScopeRequirement("api.articles.read")));
});

var app = builder.Build();

app.UseRouting();
app.UseAuthentication();      // 验证JWT Token
app.UseAuthorization();       // 检查Scope和角色
app.MapControllers();

// Controller 中：
[ApiController]
[Route("api/users")]
public class UsersController : ControllerBase
{
    [HttpGet]
    [Authorize(Policy = "read:users")]   // 需要 api.users.read scope
    public IActionResult GetUsers() => Ok(/* ... */);

    [HttpPost]
    [Authorize(Policy = "write:users")]  // 需要 api.users.write scope
    public IActionResult CreateUser() => Ok(/* ... */);
    
    [HttpGet("me")]
    // 没有指定Policy → FallbackPolicy → 只需要认证（有有效Token即可）
    public IActionResult GetCurrentUser() => Ok(/* ... */);
    
    [HttpGet("health")]
    [AllowAnonymous]                     // 健康检查不需要Token
    public IActionResult Health() => Ok("healthy");
}
```

---

## 十五、授权系统的完整执行时序

```
DefaultAuthorizationService.AuthorizeAsync(user, resource, requirements)
│
├── 创建 AuthorizationHandlerContext
│   │  User = ClaimsPrincipal { name="zhangsan", role="Admin", scope="api.users.read" }
│   │  Requirements = [ ScopeRequirement("api.users.read") ]
│   │  Resource = Endpoint（当前匹配的Endpoint）
│   │  PendingRequirements = [ ScopeRequirement("api.users.read") ]
│   │
│   ▼
├── 获取所有 IAuthorizationHandler
│   │  handlers = [
│   │    PassThroughAuthorizationHandler,    // 框架内置
│   │    ScopeAuthorizationHandler,          // 你注册的
│   │    PermissionAuthorizationHandler,     // 你注册的
│   │  ]
│   │
│   ▼
├── Handler[0]: PassThroughAuthorizationHandler
│   │  遍历 Requirements 中同时是 IAuthorizationHandler 的
│   │  ScopeRequirement 不是 IAuthorizationHandler → 跳过
│   │
│   │  PendingRequirements = [ ScopeRequirement ] （没变）
│   │
│   ▼
├── Handler[1]: ScopeAuthorizationHandler
│   │  遍历 Requirements.OfType<ScopeRequirement>()
│   │  找到 ScopeRequirement("api.users.read")
│   │  检查 user.Claims 中的 scope
│   │  → 找到 scope = "api.users.read" ✅
│   │  → context.Succeed(requirement)
│   │
│   │  PendingRequirements = [] （已清空）
│   │
│   ▼
├── Handler[2]: PermissionAuthorizationHandler
│   │  遍历 Requirements.OfType<PermissionRequirement>()
│   │  没有找到匹配的 Requirement → 什么都不做
│   │
│   ▼
├── 评估结果
│   │  HasFailed = false
│   │  PendingRequirements.Any() = false
│   │  HasSucceeded = true ✅
│   │
│   ▼
└── 返回 AuthorizationResult.Success()
```

---

## 十六、自己实现一个极简授权系统

```csharp
// ========== 极简实现 ==========

// Requirement
interface IRequirement { }

class RoleRequirement : IRequirement
{
    public string Role { get; }
    public RoleRequirement(string role) => Role = role;
}

class AgeRequirement : IRequirement
{
    public int MinAge { get; }
    public AgeRequirement(int minAge) => MinAge = minAge;
}

// Policy
class Policy
{
    public List<IRequirement> Requirements { get; } = new();
}

// Handler Context
class AuthContext
{
    public Dictionary<string, string> UserClaims { get; }
    public List<IRequirement> Requirements { get; }
    public HashSet<IRequirement> Pending { get; }
    public bool Failed { get; private set; }
    
    public AuthContext(Dictionary<string, string> claims, List<IRequirement> reqs)
    {
        UserClaims = claims;
        Requirements = reqs;
        Pending = new HashSet<IRequirement>(reqs);
    }

    public void Succeed(IRequirement req) => Pending.Remove(req);
    public void Fail() => Failed = true;
    public bool HasSucceeded => !Failed && !Pending.Any();
}

// Handlers
interface IHandler { void Handle(AuthContext context); }

class RoleHandler : IHandler
{
    public void Handle(AuthContext context)
    {
        foreach (var req in context.Requirements.OfType<RoleRequirement>())
        {
            if (context.UserClaims.TryGetValue("role", out var role) 
                && role == req.Role)
            {
                context.Succeed(req);
            }
        }
    }
}

class AgeHandler : IHandler
{
    public void Handle(AuthContext context)
    {
        foreach (var req in context.Requirements.OfType<AgeRequirement>())
        {
            if (context.UserClaims.TryGetValue("age", out var ageStr) 
                && int.Parse(ageStr) >= req.MinAge)
            {
                context.Succeed(req);
            }
        }
    }
}

// Authorization Service
class MiniAuthService
{
    private readonly List<IHandler> _handlers;
    
    public MiniAuthService(List<IHandler> handlers) => _handlers = handlers;
    
    public bool Authorize(Dictionary<string, string> userClaims, Policy policy)
    {
        var context = new AuthContext(userClaims, policy.Requirements);
        
        foreach (var handler in _handlers)
        {
            handler.Handle(context);
        }
        
        return context.HasSucceeded;
    }
}

// ========== 测试 ==========
var handlers = new List<IHandler> { new RoleHandler(), new AgeHandler() };
var authService = new MiniAuthService(handlers);

// 策略：需要Admin角色 + 年满18
var policy = new Policy();
policy.Requirements.Add(new RoleRequirement("Admin"));
policy.Requirements.Add(new AgeRequirement(18));

// 用户1：Admin, 25岁
var user1 = new Dictionary<string, string> { ["role"] = "Admin", ["age"] = "25" };
Console.WriteLine($"User1: {authService.Authorize(user1, policy)}");  // True ✅

// 用户2：Admin, 16岁
var user2 = new Dictionary<string, string> { ["role"] = "Admin", ["age"] = "16" };
Console.WriteLine($"User2: {authService.Authorize(user2, policy)}");  // False ❌

// 用户3：Editor, 30岁
var user3 = new Dictionary<string, string> { ["role"] = "Editor", ["age"] = "30" };
Console.WriteLine($"User3: {authService.Authorize(user3, policy)}");  // False ❌

// 用户4：没有claims
var user4 = new Dictionary<string, string>();
Console.WriteLine($"User4: {authService.Authorize(user4, policy)}");  // False ❌
```

---

## 十七、检验你的理解

1. **[Authorize(Roles = "Admin,Editor")] 和连写两个 [Authorize(Roles = "Admin")][Authorize(Roles = "Editor")] 有什么区别？**
   （答案：前者是 OR 关系——用户有 Admin 或 Editor 任一角色即可通过，因为它们在同一个 RolesAuthorizationRequirement 中，AllowedRoles = ["Admin", "Editor"]。后者是 AND 关系——用户必须同时有 Admin 和 Editor 两个角色，因为生成了两个独立的 RolesAuthorizationRequirement，每个都必须被 Succeed。）

2. **在 AuthorizationHandler 中，调用 `context.Succeed(requirement)` 和什么都不做有什么区别？可以有多个Handler处理同一个Requirement吗？**
   （答案：Succeed 将 Requirement 从 PendingRequirements 中移除。什么都不做则 Requirement 仍然 Pending。可以有多个Handler处理同一个Requirement——只要其中一个Handler Succeed了就行。例如一个Handler检查是否是作者，另一个Handler检查是否是Admin，任一个成功都可以编辑文章。）

3. **DefaultPolicy 和 FallbackPolicy 分别影响哪些端点？SSO项目中应该怎么设置？**
   （答案：DefaultPolicy 影响标注了 [Authorize] 但没指定 Policy 的端点。FallbackPolicy 影响既没有 [Authorize] 也没有 [AllowAnonymous] 的端点。SSO授权服务器的 FallbackPolicy 应为 null（允许匿名，因为登录页等需要匿名访问）。API资源服务器的 FallbackPolicy 应为 RequireAuthenticatedUser（所有API默认需要Token）。）

4. **基于资源的授权（Resource-based Authorization）为什么不能通过 [Authorize] 特性实现？**
   （答案：因为 [Authorize] 特性在编译时就确定了，而资源（如文章对象）需要在运行时从数据库获取后才能判断。比如"用户是否是文章的作者"这个判断，需要先拿到文章对象才能比较。所以必须在Controller方法内部调用 IAuthorizationService.AuthorizeAsync(User, article, requirement) 来进行基于资源的授权。）

5. **IAuthorizationPolicyProvider 为什么是 Singleton？如果权限数据在数据库中且可能变化，怎么处理？**
   （答案：PolicyProvider 是 Singleton 因为它被 AuthorizationMiddleware 使用，不依赖请求级别的数据。如果权限在数据库中，有两种方式：① PolicyProvider 动态生成策略（策略结构固定，如 PermissionRequirement），实际的数据库查询在 Handler 中完成（Handler 可以通过 IServiceScopeFactory 创建 Scope 来访问数据库）。② 使用 IOptionsMonitor 监听配置变化。）

6. **在 AuthenticationMiddleware 的第一阶段，OpenIddict Handler 处理 /connect/token 请求并短路。这时候 UseAuthorization 中间件有没有执行？Token 端点的安全性由谁保证？**
   （答案：UseAuthorization 没有执行，因为请求在 UseAuthentication 层就被短路了。Token 端点的安全性完全由 OpenIddict Server Handler 自己保证——它验证 client_id、client_secret、authorization_code、PKCE 等。这是 OAuth 2.0 协议层面的安全，不依赖 ASP.NET Core 的授权系统。）

---
