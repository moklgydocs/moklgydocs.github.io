---
title: 接入演示
date: 2025-04-09
category:
  - ASP.NET_Core
tag:
  - asp.netcore
  - SSO
  - OpenIddict
  - 权限中心
  - RBAC
author: Moklgy
order: 16
---


# 业务系统怎么接入SSO和权限中心，以及自己怎么管权限。

---

## 全局视角

```
业务系统（比如 ERP）要做的事:

1. 接入 SSO → 用户登录/Token验证
2. 接入权限中心 → 权限校验/菜单获取
3. 定义自己的权限 → 告诉权限中心"我有哪些权限点"
4. 使用权限 → 前端控制显隐，后端控制访问

问题拆解:
  谁来定义权限？→ 业务系统自己定义
  权限存在哪？→ 权限中心的数据库
  怎么同步过去？→ 启动时自动注册
  运行时怎么校验？→ SDK 调权限中心 API
```

---

## 第一步：理解完整流程

```
┌──────────────────────────────────────────────────────────────────┐
│                     ERP 系统完整接入流程                          │
│                                                                  │
│  ┌─────────────┐   ┌─────────────┐   ┌──────────────┐          │
│  │   前端 React │   │ ERP 后端    │   │  SSO 鉴权中心 │          │
│  └──────┬──────┘   └──────┬──────┘   └──────┬───────┘          │
│         │                  │                  │                  │
│  Phase 1: 登录                                                  │
│         │ ─① 跳转登录页 ─────────────────────►│                  │
│         │                                    │                  │
│         │ ◄── ② 返回 code ──────────────────│                  │
│         │                                    │                  │
│         │ ─③ 带 code 请求 Token ─►│          │                  │
│         │                         │──④ code换Token──►│          │
│         │                         │◄── ⑤ 返回 Token ─│          │
│         │ ◄── ⑥ 返回 Token ──────│          │                  │
│         │                                    │                  │
│  Phase 2: 获取权限和菜单         ┌──────────────┐               │
│         │                         │  权限中心    │               │
│         │ ─⑦ 请求菜单+权限 ──►│  │              │               │
│         │                      │──⑧ 查权限 ───►│               │
│         │                      │◄── ⑨ 返回 ────│               │
│         │ ◄── ⑩ 返回菜单+权限 ─│  │              │               │
│         │                         └──────────────┘               │
│  Phase 3: 业务请求                                               │
│         │ ─⑪ 业务API(带Token) ►│                                │
│         │                      │──⑫ 验Token(本地JWT验证)        │
│         │                      │──⑬ 查权限(SDK+缓存)            │
│         │                      │  有权限? → 执行业务             │
│         │                      │  无权限? → 403                  │
│         │ ◄── ⑭ 返回结果 ──────│                                │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 第二步：ERP 后端项目搭建

### 项目结构

```
ERP/
├── src/
│   ├── ERP.Host/                          # 启动层
│   │   ├── Program.cs
│   │   ├── appsettings.json
│   │   └── Controllers/
│   │       ├── PurchaseController.cs
│   │       └── StockController.cs
│   │
│   ├── ERP.Application/                   # 业务层
│   │   ├── Services/
│   │   └── Contracts/
│   │
│   ├── ERP.Domain/                        # 领域层
│   │   └── ...
│   │
│   └── ERP.Permissions/                   # ★ 权限定义层（核心）
│       ├── ErpPermissions.cs              # 权限常量
│       ├── ErpPermissionDefinitionProvider.cs  # 权限定义提供者
│       └── ErpMenuDefinitionProvider.cs   # 菜单定义提供者
│
└── 引用:
    ├── Platform.AspNetCore                # 平台基座
    └── Perm.Client                        # 权限中心 SDK
```

---

## 第三步：定义权限（最关键）

### 权限常量类

```csharp
// ============================================
// ERP.Permissions/ErpPermissions.cs
// 
// 所有权限点的常量定义
// 这是业务开发者最常接触的文件
// ============================================

namespace ERP.Permissions;

/// <summary>
/// ERP 系统权限常量
/// 
/// 命名规范: {AppCode}.{Module}.{Action}
/// 
/// 为什么用常量类而不是枚举:
///   1. 字符串更灵活，支持树形层级
///   2. 和权限中心存储一致
///   3. 新增权限不需要改枚举值
///   4. ABP 也是这样做的
/// </summary>
public static class ErpPermissions
{
    /// <summary>
    /// 应用编码（对应 SSO 的 ClientId 和权限中心的 AppCode）
    /// </summary>
    public const string AppCode = "erp";

    // ============ 采购管理 ============
    public static class Purchase
    {
        public const string Default = "ERP.Purchase";                  // 采购管理（父权限）
        public const string View = "ERP.Purchase.View";                // 查看采购单
        public const string Create = "ERP.Purchase.Create";            // 创建采购单
        public const string Edit = "ERP.Purchase.Edit";                // 编辑采购单
        public const string Delete = "ERP.Purchase.Delete";            // 删除采购单
        public const string Approve = "ERP.Purchase.Approve";          // 审批采购单
        public const string Export = "ERP.Purchase.Export";            // 导出
    }

    // ============ 库存管理 ============
    public static class Stock
    {
        public const string Default = "ERP.Stock";
        public const string View = "ERP.Stock.View";
        public const string Inbound = "ERP.Stock.Inbound";            // 入库
        public const string Outbound = "ERP.Stock.Outbound";          // 出库
        public const string Transfer = "ERP.Stock.Transfer";          // 调拨
        public const string Inventory = "ERP.Stock.Inventory";        // 盘点
    }

    // ============ 报表中心 ============
    public static class Report
    {
        public const string Default = "ERP.Report";
        public const string View = "ERP.Report.View";
        public const string Export = "ERP.Report.Export";
        public const string PurchaseReport = "ERP.Report.Purchase";   // 采购报表
        public const string StockReport = "ERP.Report.Stock";         // 库存报表
    }

    // ============ 系统设置 ============
    public static class Settings
    {
        public const string Default = "ERP.Settings";
        public const string BasicInfo = "ERP.Settings.BasicInfo";     // 基础信息
        public const string UserManage = "ERP.Settings.UserManage";   // 用户管理
    }
}
```

### 权限定义提供者（自动注册到权限中心）

```csharp
// ============================================
// ERP.Permissions/ErpPermissionDefinitionProvider.cs
// 
// 定义权限的结构（组 → 权限 → 子权限）
// 启动时自动同步到权限中心
// ============================================

using Perm.Client.Definitions;

namespace ERP.Permissions;

/// <summary>
/// ERP 权限定义提供者
/// 
/// 作用:
///   告诉权限中心 "ERP 系统有哪些权限点"
///   包括: 权限名、显示名、父子关系、分组
///   
/// 类比 ABP:
///   ABP 的 PermissionDefinitionProvider
///   定义权限 → 启动时注册到 ABP 框架
///   我们: 定义权限 → 启动时注册到权限中心
/// </summary>
public class ErpPermissionDefinitionProvider : IPermissionDefinitionProvider
{
    public string AppCode => ErpPermissions.AppCode;

    public void Define(IPermissionDefinitionContext context)
    {
        // ============ 采购管理组 ============
        var purchaseGroup = context.AddGroup(
            "Purchase",
            "采购管理");

        var purchase = purchaseGroup.AddPermission(
            ErpPermissions.Purchase.Default,
            "采购管理");

        purchase.AddChild(ErpPermissions.Purchase.View, "查看采购单");
        purchase.AddChild(ErpPermissions.Purchase.Create, "创建采购单");
        purchase.AddChild(ErpPermissions.Purchase.Edit, "编辑采购单");
        purchase.AddChild(ErpPermissions.Purchase.Delete, "删除采购单");
        purchase.AddChild(ErpPermissions.Purchase.Approve, "审批采购单");
        purchase.AddChild(ErpPermissions.Purchase.Export, "导出");

        // ============ 库存管理组 ============
        var stockGroup = context.AddGroup(
            "Stock",
            "库存管理");

        var stock = stockGroup.AddPermission(
            ErpPermissions.Stock.Default,
            "库存管理");

        stock.AddChild(ErpPermissions.Stock.View, "查看库存");
        stock.AddChild(ErpPermissions.Stock.Inbound, "入库操作");
        stock.AddChild(ErpPermissions.Stock.Outbound, "出库操作");
        stock.AddChild(ErpPermissions.Stock.Transfer, "调拨");
        stock.AddChild(ErpPermissions.Stock.Inventory, "盘点");

        // ============ 报表中心组 ============
        var reportGroup = context.AddGroup(
            "Report",
            "报表中心");

        var report = reportGroup.AddPermission(
            ErpPermissions.Report.Default,
            "报表中心");

        report.AddChild(ErpPermissions.Report.View, "查看报表");
        report.AddChild(ErpPermissions.Report.Export, "导出报表");
        report.AddChild(ErpPermissions.Report.PurchaseReport, "采购报表");
        report.AddChild(ErpPermissions.Report.StockReport, "库存报表");

        // ============ 系统设置组 ============
        var settingsGroup = context.AddGroup(
            "Settings",
            "系统设置");

        var settings = settingsGroup.AddPermission(
            ErpPermissions.Settings.Default,
            "系统设置");

        settings.AddChild(ErpPermissions.Settings.BasicInfo, "基础信息设置");
        settings.AddChild(ErpPermissions.Settings.UserManage, "用户管理");
    }
}
```

### 菜单定义提供者

```csharp
// ============================================
// ERP.Permissions/ErpMenuDefinitionProvider.cs
// ============================================

using Perm.Client.Definitions;

namespace ERP.Permissions;

public class ErpMenuDefinitionProvider : IMenuDefinitionProvider
{
    public string AppCode => ErpPermissions.AppCode;

    public void Define(IMenuDefinitionContext context)
    {
        // 采购管理
        var purchase = context.AddMenu(new MenuDefinition
        {
            Code = "purchase",
            Name = "采购管理",
            Icon = "shopping-cart",
            Path = "/purchase",
            RequiredPermission = ErpPermissions.Purchase.Default,
            Sort = 1,
        });

        purchase.AddChild(new MenuDefinition
        {
            Code = "purchase-list",
            Name = "采购单列表",
            Path = "/purchase/list",
            RequiredPermission = ErpPermissions.Purchase.View,
        });

        purchase.AddChild(new MenuDefinition
        {
            Code = "purchase-create",
            Name = "新建采购单",
            Path = "/purchase/create",
            RequiredPermission = ErpPermissions.Purchase.Create,
        });

        // 库存管理
        var stock = context.AddMenu(new MenuDefinition
        {
            Code = "stock",
            Name = "库存管理",
            Icon = "database",
            Path = "/stock",
            RequiredPermission = ErpPermissions.Stock.Default,
            Sort = 2,
        });

        stock.AddChild(new MenuDefinition
        {
            Code = "stock-list",
            Name = "库存查询",
            Path = "/stock/list",
            RequiredPermission = ErpPermissions.Stock.View,
        });

        stock.AddChild(new MenuDefinition
        {
            Code = "stock-inbound",
            Name = "入库操作",
            Path = "/stock/inbound",
            RequiredPermission = ErpPermissions.Stock.Inbound,
        });

        // 报表
        context.AddMenu(new MenuDefinition
        {
            Code = "report",
            Name = "报表中心",
            Icon = "bar-chart",
            Path = "/report",
            RequiredPermission = ErpPermissions.Report.Default,
            Sort = 3,
        });

        // 系统设置
        context.AddMenu(new MenuDefinition
        {
            Code = "settings",
            Name = "系统设置",
            Icon = "setting",
            Path = "/settings",
            RequiredPermission = ErpPermissions.Settings.Default,
            Sort = 99,
        });
    }
}
```

---

## 第四步：SDK 设计（Perm.Client）

```csharp
// ============================================
// Perm.Client/Definitions/IPermissionDefinitionProvider.cs
// 权限定义接口（业务系统实现）
// ============================================

namespace Perm.Client.Definitions;

/// <summary>
/// 权限定义提供者接口
/// 每个业务系统实现一个
/// </summary>
public interface IPermissionDefinitionProvider
{
    string AppCode { get; }
    void Define(IPermissionDefinitionContext context);
}

public interface IPermissionDefinitionContext
{
    PermissionGroupBuilder AddGroup(string code, string displayName);
}

public class PermissionGroupBuilder
{
    public string Code { get; }
    public string DisplayName { get; }
    internal List<PermissionBuilder> Permissions { get; } = [];

    internal PermissionGroupBuilder(string code, string displayName)
    {
        Code = code;
        DisplayName = displayName;
    }

    public PermissionBuilder AddPermission(string name, string displayName)
    {
        var builder = new PermissionBuilder(name, displayName);
        Permissions.Add(builder);
        return builder;
    }
}

public class PermissionBuilder
{
    public string Name { get; }
    public string DisplayName { get; }
    internal List<PermissionBuilder> Children { get; } = [];

    internal PermissionBuilder(string name, string displayName)
    {
        Name = name;
        DisplayName = displayName;
    }

    public PermissionBuilder AddChild(string name, string displayName)
    {
        var child = new PermissionBuilder(name, displayName);
        Children.Add(child);
        return child;
    }
}
```

```csharp
// ============================================
// Perm.Client/Definitions/IMenuDefinitionProvider.cs
// ============================================

namespace Perm.Client.Definitions;

public interface IMenuDefinitionProvider
{
    string AppCode { get; }
    void Define(IMenuDefinitionContext context);
}

public interface IMenuDefinitionContext
{
    MenuDefinitionBuilder AddMenu(MenuDefinition menu);
}

public class MenuDefinition
{
    public string Code { get; set; } = default!;
    public string Name { get; set; } = default!;
    public string? Icon { get; set; }
    public string? Path { get; set; }
    public string? RequiredPermission { get; set; }
    public int Sort { get; set; }
}

public class MenuDefinitionBuilder
{
    public MenuDefinition Menu { get; }
    internal List<MenuDefinitionBuilder> Children { get; } = [];

    internal MenuDefinitionBuilder(MenuDefinition menu) => Menu = menu;

    public MenuDefinitionBuilder AddChild(MenuDefinition menu)
    {
        var child = new MenuDefinitionBuilder(menu);
        Children.Add(child);
        return child;
    }
}
```

### 自动注册服务（启动时同步到权限中心）

```csharp
// ============================================
// Perm.Client/Registration/PermissionRegistrationService.cs
// 启动时自动将权限定义同步到权限中心
// ============================================

using Microsoft.Extensions.Hosting;
using Microsoft.Extensions.Logging;
using Perm.Client.Definitions;

namespace Perm.Client.Registration;

/// <summary>
/// 权限自动注册后台服务
/// 
/// 应用启动时:
///   1. 收集所有 IPermissionDefinitionProvider
///   2. 构建权限树
///   3. POST 到权限中心 /api/apps/register
///   4. 权限中心对比差异，自动 新增/更新/标记废弃
///   
/// 这样业务开发者只需要:
///   定义权限常量 → 实现 Provider → 启动 → 自动同步 ✅
///   不需要去权限中心后台手动创建
/// </summary>
public class PermissionRegistrationService : IHostedService
{
    private readonly IEnumerable<IPermissionDefinitionProvider> _permProviders;
    private readonly IEnumerable<IMenuDefinitionProvider> _menuProviders;
    private readonly IPermissionRegistrationClient _client;
    private readonly ILogger<PermissionRegistrationService> _logger;

    public PermissionRegistrationService(
        IEnumerable<IPermissionDefinitionProvider> permProviders,
        IEnumerable<IMenuDefinitionProvider> menuProviders,
        IPermissionRegistrationClient client,
        ILogger<PermissionRegistrationService> logger)
    {
        _permProviders = permProviders;
        _menuProviders = menuProviders;
        _client = client;
        _logger = logger;
    }

    public async Task StartAsync(CancellationToken cancellationToken)
    {
        try
        {
            foreach (var provider in _permProviders)
            {
                _logger.LogInformation(
                    "Registering permissions for app: {AppCode}",
                    provider.AppCode);

                // 构建权限树
                var context = new PermissionDefinitionContext();
                provider.Define(context);

                // 构建菜单树
                var menuContext = new MenuDefinitionContext();
                var menuProvider = _menuProviders
                    .FirstOrDefault(m => m.AppCode == provider.AppCode);
                menuProvider?.Define(menuContext);

                // 同步到权限中心
                var request = new AppRegistrationRequest
                {
                    AppCode = provider.AppCode,
                    PermissionGroups = BuildPermissionGroups(context),
                    Menus = BuildMenus(menuContext),
                };

                await _client.RegisterAsync(request, cancellationToken);

                _logger.LogInformation(
                    "Permissions registered for app: {AppCode}, " +
                    "Groups: {GroupCount}, Permissions: {PermCount}",
                    provider.AppCode,
                    request.PermissionGroups.Count,
                    request.PermissionGroups
                        .SelectMany(g => g.Permissions)
                        .Count());
            }
        }
        catch (Exception ex)
        {
            // 注册失败不阻止应用启动（降级为无权限控制）
            _logger.LogError(ex,
                "Failed to register permissions. " +
                "App will start but permission checks may fail.");
        }
    }

    public Task StopAsync(CancellationToken cancellationToken) => Task.CompletedTask;

    private List<PermissionGroupDto> BuildPermissionGroups(
        PermissionDefinitionContext context)
    {
        return context.Groups.Select(g => new PermissionGroupDto
        {
            Code = g.Code,
            DisplayName = g.DisplayName,
            Permissions = g.Permissions.Select(p => BuildPermission(p)).ToList(),
        }).ToList();
    }

    private PermissionDto BuildPermission(PermissionBuilder builder)
    {
        return new PermissionDto
        {
            Name = builder.Name,
            DisplayName = builder.DisplayName,
            Children = builder.Children.Select(c => BuildPermission(c)).ToList(),
        };
    }

    private List<MenuDto> BuildMenus(MenuDefinitionContext context)
    {
        return context.Menus.Select(m => BuildMenu(m)).ToList();
    }

    private MenuDto BuildMenu(MenuDefinitionBuilder builder)
    {
        return new MenuDto
        {
            Code = builder.Menu.Code,
            Name = builder.Menu.Name,
            Icon = builder.Menu.Icon,
            Path = builder.Menu.Path,
            RequiredPermission = builder.Menu.RequiredPermission,
            Sort = builder.Menu.Sort,
            Children = builder.Children.Select(c => BuildMenu(c)).ToList(),
        };
    }
}

// ============================================
// 注册请求 DTO
// ============================================

public class AppRegistrationRequest
{
    public string AppCode { get; set; } = default!;
    public List<PermissionGroupDto> PermissionGroups { get; set; } = [];
    public List<MenuDto> Menus { get; set; } = [];
}

public class PermissionGroupDto
{
    public string Code { get; set; } = default!;
    public string DisplayName { get; set; } = default!;
    public List<PermissionDto> Permissions { get; set; } = [];
}

public class PermissionDto
{
    public string Name { get; set; } = default!;
    public string DisplayName { get; set; } = default!;
    public List<PermissionDto> Children { get; set; } = [];
}

public class MenuDto
{
    public string Code { get; set; } = default!;
    public string Name { get; set; } = default!;
    public string? Icon { get; set; }
    public string? Path { get; set; }
    public string? RequiredPermission { get; set; }
    public int Sort { get; set; }
    public List<MenuDto> Children { get; set; } = [];
}
```

```csharp
// ============================================
// Perm.Client/Registration/IPermissionRegistrationClient.cs
// HTTP 客户端，调用权限中心注册 API
// ============================================

namespace Perm.Client.Registration;

public interface IPermissionRegistrationClient
{
    Task RegisterAsync(
        AppRegistrationRequest request,
        CancellationToken cancellationToken = default);
}

public class PermissionRegistrationClient : IPermissionRegistrationClient
{
    private readonly HttpClient _http;

    public PermissionRegistrationClient(HttpClient http) => _http = http;

    public async Task RegisterAsync(
        AppRegistrationRequest request,
        CancellationToken cancellationToken = default)
    {
        // POST /api/apps/register
        // 权限中心接收后对比差异:
        //   新增的权限 → INSERT
        //   已有的权限 → UPDATE displayName
        //   不再定义的权限 → 标记 IsEnabled=false（不删除）

        var response = await _http.PostAsJsonAsync(
            "/api/apps/register", request, cancellationToken);

        response.EnsureSuccessStatusCode();
    }
}
```

### 权限检查核心（SDK）

```csharp
// ============================================
// Perm.Client/IPermissionChecker.cs
// 业务系统端的权限检查接口
// ============================================

namespace Perm.Client;

/// <summary>
/// 权限检查器（业务系统端）
/// 
/// 和权限中心的 IPermissionChecker 不同:
///   权限中心: 直接查数据库
///   业务系统: HTTP 调权限中心 + 本地缓存
/// </summary>
public interface IPermissionChecker
{
    /// <summary>
    /// 检查当前用户是否有某权限
    /// </summary>
    Task<bool> IsGrantedAsync(string permissionName);

    /// <summary>
    /// 检查指定用户是否有某权限
    /// </summary>
    Task<bool> IsGrantedAsync(Guid userId, string permissionName);

    /// <summary>
    /// 获取当前用户的所有权限
    /// </summary>
    Task<HashSet<string>> GetGrantedPermissionsAsync();

    /// <summary>
    /// 获取当前用户的菜单树
    /// </summary>
    Task<List<MenuDto>> GetMenusAsync();
}
```

```csharp
// ============================================
// Perm.Client/PermissionChecker.cs
// 权限检查器实现
// ============================================

using Microsoft.Extensions.Caching.Distributed;
using System.Text.Json;
using Platform.EntityFrameworkCore;

namespace Perm.Client;

public class PermissionChecker : IPermissionChecker
{
    private readonly HttpClient _http;
    private readonly IDistributedCache _cache;
    private readonly ICurrentUser _currentUser;
    private readonly PermClientOptions _options;

    public PermissionChecker(
        HttpClient http,
        IDistributedCache cache,
        ICurrentUser currentUser,
        Microsoft.Extensions.Options.IOptions<PermClientOptions> options)
    {
        _http = http;
        _cache = cache;
        _currentUser = currentUser;
        _options = options.Value;
    }

    public async Task<bool> IsGrantedAsync(string permissionName)
    {
        if (!_currentUser.IsAuthenticated)
            return false;

        var permissions = await GetGrantedPermissionsAsync();
        return permissions.Contains(permissionName);
    }

    public async Task<bool> IsGrantedAsync(Guid userId, string permissionName)
    {
        var permissions = await GetGrantedPermissionsForUserAsync(userId);
        return permissions.Contains(permissionName);
    }

    public async Task<HashSet<string>> GetGrantedPermissionsAsync()
    {
        if (_currentUser.Id == null)
            return [];

        return await GetGrantedPermissionsForUserAsync(_currentUser.Id.Value);
    }

    public async Task<List<MenuDto>> GetMenusAsync()
    {
        if (_currentUser.Id == null)
            return [];

        var cacheKey = $"perm:menus:{_options.AppCode}:{_currentUser.Id}";
        var cached = await _cache.GetStringAsync(cacheKey);

        if (cached != null)
            return JsonSerializer.Deserialize<List<MenuDto>>(cached) ?? [];

        // 调权限中心
        var response = await _http.GetAsync(
            $"/api/menus/current?appCode={_options.AppCode}");
        response.EnsureSuccessStatusCode();

        var result = await response.Content
            .ReadFromJsonAsync<ApiResult<List<MenuDto>>>();
        var menus = result?.Data ?? [];

        await _cache.SetStringAsync(cacheKey,
            JsonSerializer.Serialize(menus),
            new DistributedCacheEntryOptions
            {
                AbsoluteExpirationRelativeToNow =
                    TimeSpan.FromMinutes(_options.CacheMinutes)
            });

        return menus;
    }

    private async Task<HashSet<string>> GetGrantedPermissionsForUserAsync(
        Guid userId)
    {
        var cacheKey = $"perm:granted:{_options.AppCode}:{userId}";
        var cached = await _cache.GetStringAsync(cacheKey);

        if (cached != null)
            return JsonSerializer.Deserialize<HashSet<string>>(cached) ?? [];

        // 调权限中心
        var response = await _http.GetAsync(
            $"/api/permissions/granted?appCode={_options.AppCode}&userId={userId}");
        response.EnsureSuccessStatusCode();

        var result = await response.Content
            .ReadFromJsonAsync<ApiResult<List<string>>>();
        var permissions = new HashSet<string>(result?.Data ?? []);

        await _cache.SetStringAsync(cacheKey,
            JsonSerializer.Serialize(permissions),
            new DistributedCacheEntryOptions
            {
                AbsoluteExpirationRelativeToNow =
                    TimeSpan.FromMinutes(_options.CacheMinutes)
            });

        return permissions;
    }

    private class ApiResult<T>
    {
        public bool Success { get; set; }
        public T? Data { get; set; }
    }
}
```

### RequirePermission 特性 + Filter

```csharp
// ============================================
// Perm.Client/Authorization/RequirePermissionAttribute.cs
// ============================================

namespace Perm.Client.Authorization;

/// <summary>
/// 声明式权限校验
/// 
/// 用法:
///   [RequirePermission(ErpPermissions.Purchase.Create)]
///   public async Task<IActionResult> CreatePurchase() { ... }
///   
///   // 多个权限（默认全部满足）
///   [RequirePermission(ErpPermissions.Purchase.Create)]
///   [RequirePermission(ErpPermissions.Purchase.Edit)]
///   
///   // 多个权限满足一个即可
///   [RequirePermission(ErpPermissions.Purchase.Create, RequireAll = false)]
///   [RequirePermission(ErpPermissions.Purchase.Edit)]
/// </summary>
[AttributeUsage(
    AttributeTargets.Class | AttributeTargets.Method,
    AllowMultiple = true)]
public class RequirePermissionAttribute : Attribute
{
    public string[] Permissions { get; }
    public bool RequireAll { get; set; } = true;

    public RequirePermissionAttribute(params string[] permissions)
    {
        Permissions = permissions;
    }
}
```

```csharp
// ============================================
// Perm.Client/Authorization/PermissionAuthorizationFilter.cs
// ============================================

using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.Filters;
using Platform.AspNetCore.ApiResult;

namespace Perm.Client.Authorization;

/// <summary>
/// 权限校验过滤器
/// 
/// 执行顺序:
///   1. ASP.NET Core 的 [Authorize] → 检查是否登录
///   2. 本过滤器 → 检查是否有权限
///   3. Controller Action → 执行业务
/// </summary>
public class PermissionAuthorizationFilter : IAsyncAuthorizationFilter
{
    private readonly IPermissionChecker _checker;

    public PermissionAuthorizationFilter(IPermissionChecker checker)
    {
        _checker = checker;
    }

    public async Task OnAuthorizationAsync(AuthorizationFilterContext context)
    {
        // 收集所有 RequirePermission 特性
        var attributes = context.ActionDescriptor.EndpointMetadata
            .OfType<RequirePermissionAttribute>()
            .ToList();

        if (attributes.Count == 0)
            return;

        // 必须已认证
        if (context.HttpContext.User.Identity?.IsAuthenticated != true)
        {
            context.Result = new ObjectResult(
                ApiResult.Fail("未登录", 401))
            { StatusCode = 401 };
            return;
        }

        // 获取用户所有权限（一次性拉取，有缓存）
        var grantedPermissions = await _checker.GetGrantedPermissionsAsync();

        foreach (var attr in attributes)
        {
            bool hasPermission;

            if (attr.RequireAll)
            {
                // 所有权限都要满足
                hasPermission = attr.Permissions
                    .All(p => grantedPermissions.Contains(p));
            }
            else
            {
                // 满足任一即可
                hasPermission = attr.Permissions
                    .Any(p => grantedPermissions.Contains(p));
            }

            if (!hasPermission)
            {
                var required = string.Join(", ", attr.Permissions);

                context.Result = new ObjectResult(
                    ApiResult.Fail($"没有权限: {required}", 403,
                        "Platform:Forbidden"))
                { StatusCode = 403 };
                return;
            }
        }
    }
}
```

### SDK 一键注册

```csharp
// ============================================
// Perm.Client/ServiceCollectionExtensions.cs
// ============================================

using Microsoft.Extensions.DependencyInjection;
using Perm.Client.Authorization;
using Perm.Client.Definitions;
using Perm.Client.Registration;

namespace Perm.Client;

public static class ServiceCollectionExtensions
{
    /// <summary>
    /// 接入权限中心
    /// 
    /// 用法:
    ///   builder.Services.AddPermissionCenter(options =>
    ///   {
    ///       options.AppCode = "erp";
    ///       options.BaseUrl = "https://perm.example.com";
    ///   });
    /// </summary>
    public static IServiceCollection AddPermissionCenter(
        this IServiceCollection services,
        Action<PermClientOptions> configure)
    {
        var options = new PermClientOptions();
        configure(options);

        services.Configure(configure);

        // HTTP Client
        services.AddHttpClient<IPermissionChecker, PermissionChecker>(client =>
        {
            client.BaseAddress = new Uri(options.BaseUrl);
            client.Timeout = TimeSpan.FromSeconds(5);
        });

        services.AddHttpClient<IPermissionRegistrationClient,
            PermissionRegistrationClient>(client =>
        {
            client.BaseAddress = new Uri(options.BaseUrl);
            client.Timeout = TimeSpan.FromSeconds(30);
        });

        // 权限校验 Filter
        services.AddControllers(mvcOptions =>
        {
            mvcOptions.Filters.Add<PermissionAuthorizationFilter>();
        });

        // 启动时自动注册权限
        if (options.AutoRegisterPermissions)
        {
            services.AddHostedService<PermissionRegistrationService>();
        }

        return services;
    }

    /// <summary>
    /// 注册权限定义提供者
    /// 
    /// 用法:
    ///   builder.Services.AddPermissionDefinition<ErpPermissionDefinitionProvider>();
    ///   builder.Services.AddMenuDefinition<ErpMenuDefinitionProvider>();
    /// </summary>
    public static IServiceCollection AddPermissionDefinition<T>(
        this IServiceCollection services)
        where T : class, IPermissionDefinitionProvider
    {
        services.AddSingleton<IPermissionDefinitionProvider, T>();
        return services;
    }

    public static IServiceCollection AddMenuDefinition<T>(
        this IServiceCollection services)
        where T : class, IMenuDefinitionProvider
    {
        services.AddSingleton<IMenuDefinitionProvider, T>();
        return services;
    }
}

public class PermClientOptions
{
    public string AppCode { get; set; } = default!;
    public string BaseUrl { get; set; } = default!;
    public int CacheMinutes { get; set; } = 5;
    public bool AutoRegisterPermissions { get; set; } = true;
}
```

---

## 第五步：ERP 后端 Program.cs（完整版）

```csharp
// ============================================
// ERP Program.cs — 完整接入
// ============================================

using Platform.AspNetCore.Extensions;
using Platform.EntityFrameworkCore.Extensions;
using Perm.Client;
using ERP.Permissions;

var builder = WebApplication.CreateBuilder(args);

// ========== 1. 平台基础 ==========
builder.Services.AddPlatformAspNetCore(options =>
{
    options.AutoWrapResult = true;
    options.EnableCors = true;
    options.AllowedOrigins =
        builder.Configuration.GetSection("Cors:Origins").Get<string[]>();
});

// ========== 2. 数据库 ==========
builder.Services.AddPlatformDbContext<ErpDbContext>(
    options => options.UseNpgsql(
        builder.Configuration.GetConnectionString("Default")));

// ========== 3. 认证（SSO Token 验证）==========
builder.Services.AddAuthentication("Bearer")
    .AddJwtBearer("Bearer", options =>
    {
        options.Authority = builder.Configuration["Sso:Authority"];
        // https://sso.example.com
        options.Audience = "erp";
        options.RequireHttpsMetadata = !builder.Environment.IsDevelopment();

        options.TokenValidationParameters = new()
        {
            ValidateIssuer = true,
            ValidateAudience = true,
            ValidateLifetime = true,
            ClockSkew = TimeSpan.FromMinutes(1),
        };
    });

// ========== 4. 接入权限中心 ==========
builder.Services.AddPermissionCenter(options =>
{
    options.AppCode = "erp";
    options.BaseUrl = builder.Configuration["Perm:BaseUrl"]!;
    // https://perm.example.com
    options.CacheMinutes = 5;
    options.AutoRegisterPermissions = true;  // 启动时自动注册
});

// 注册权限定义
builder.Services.AddPermissionDefinition<ErpPermissionDefinitionProvider>();
builder.Services.AddMenuDefinition<ErpMenuDefinitionProvider>();

// ========== 5. Swagger ==========
builder.Services.AddPlatformSwagger("ERP 系统", description: "企业资源管理 API");

// ========== 6. 缓存（权限本地缓存用）==========
builder.Services.AddDistributedMemoryCache();
// 生产环境换 Redis:
// builder.Services.AddStackExchangeRedisCache(options => { ... });

// ========== 7. 健康检查 ==========
builder.Services.AddHealthChecks();

var app = builder.Build();
app.UsePlatformPipeline();
app.Run();
```

```json
// appsettings.json
{
  "ConnectionStrings": {
    "Default": "Host=localhost;Database=erp;Username=postgres;Password=xxx"
  },
  "Sso": {
    "Authority": "https://sso.example.com"
  },
  "Perm": {
    "BaseUrl": "https://perm.example.com"
  },
  "Cors": {
    "Origins": ["http://localhost:3000"]
  }
}
```

---

## 第六步：Controller 使用权限（业务开发日常）

```csharp
// ============================================
// 方式一: 声明式（推荐，90% 的场景）
// ============================================

[ApiController]
[Route("api/[controller]")]
[Authorize]  // ← SSO 认证（必须登录）
public class PurchaseController : ControllerBase
{
    private readonly PurchaseService _service;

    public PurchaseController(PurchaseService service)
    {
        _service = service;
    }

    /// <summary>
    /// 查询采购单列表
    /// </summary>
    [HttpGet]
    [RequirePermission(ErpPermissions.Purchase.View)]   // ← 声明需要查看权限
    public async Task<PagedResult<PurchaseDto>> GetList(
        [FromQuery] PurchasePagedRequest request)
    {
        return await _service.GetPagedListAsync(request);
    }

    /// <summary>
    /// 创建采购单
    /// </summary>
    [HttpPost]
    [RequirePermission(ErpPermissions.Purchase.Create)]  // ← 声明需要创建权限
    public async Task<PurchaseDto> Create(
        [FromBody] CreatePurchaseRequest request)
    {
        return await _service.CreateAsync(request);
    }

    /// <summary>
    /// 审批采购单
    /// </summary>
    [HttpPost("{id:guid}/approve")]
    [RequirePermission(ErpPermissions.Purchase.Approve)]
    public async Task Approve(Guid id)
    {
        await _service.ApproveAsync(id);
    }

    /// <summary>
    /// 删除采购单
    /// </summary>
    [HttpDelete("{id:guid}")]
    [RequirePermission(ErpPermissions.Purchase.Delete)]
    public async Task Delete(Guid id)
    {
        await _service.DeleteAsync(id);
    }

    /// <summary>
    /// 导出（需要查看 + 导出两个权限）
    /// </summary>
    [HttpGet("export")]
    [RequirePermission(ErpPermissions.Purchase.View, ErpPermissions.Purchase.Export)]
    [SkipApiResultWrap]  // 文件下载不包装
    public async Task<FileResult> Export([FromQuery] PurchaseExportRequest request)
    {
        var bytes = await _service.ExportAsync(request);
        return File(bytes, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            "采购单.xlsx");
    }
}
```

```csharp
// ============================================
// 方式二: 编程式（复杂业务逻辑中使用）
// ============================================

[ApiController]
[Route("api/[controller]")]
[Authorize]
public class StockController : ControllerBase
{
    private readonly StockService _service;
    private readonly IPermissionChecker _permissionChecker;

    public StockController(
        StockService service,
        IPermissionChecker permissionChecker)
    {
        _service = service;
        _permissionChecker = permissionChecker;
    }

    /// <summary>
    /// 库存调拨
    /// 
    /// 复杂逻辑:
    ///   金额 < 1万: 需要 Transfer 权限
    ///   金额 >= 1万: 还需要 Approve 权限
    ///   
    /// 这种条件性权限用编程式更合适
    /// </summary>
    [HttpPost("transfer")]
    [RequirePermission(ErpPermissions.Stock.Transfer)]  // 基础权限
    public async Task Transfer([FromBody] TransferRequest request)
    {
        // 编程式追加检查
        if (request.Amount >= 10000)
        {
            var hasApprovePermission = await _permissionChecker
                .IsGrantedAsync(ErpPermissions.Purchase.Approve);

            if (!hasApprovePermission)
            {
                throw new BusinessException(
                    "ERP:Stock:TransferNeedApproval",
                    "金额超过1万需要审批权限");
            }
        }

        await _service.TransferAsync(request);
    }

    /// <summary>
    /// 根据权限返回不同数据
    /// </summary>
    [HttpGet("{id:guid}")]
    [RequirePermission(ErpPermissions.Stock.View)]
    public async Task<StockDetailDto> GetDetail(Guid id)
    {
        var dto = await _service.GetDetailAsync(id);

        // 没有成本查看权限 → 隐藏成本字段
        var canViewCost = await _permissionChecker
            .IsGrantedAsync("ERP.Stock.ViewCost");

        if (!canViewCost)
        {
            dto.UnitCost = null;
            dto.TotalCost = null;
        }

        return dto;
    }
}
```

```csharp
// ============================================
// 方式三: Service 层使用权限
// 适合跨 Controller 的权限逻辑
// ============================================

public class PurchaseService
{
    private readonly IRepository<PurchaseOrder, Guid> _repo;
    private readonly IUnitOfWork _uow;
    private readonly IPermissionChecker _permChecker;
    private readonly ICurrentUser _currentUser;

    public PurchaseService(
        IRepository<PurchaseOrder, Guid> repo,
        IUnitOfWork uow,
        IPermissionChecker permChecker,
        ICurrentUser currentUser)
    {
        _repo = repo;
        _uow = uow;
        _permChecker = permChecker;
        _currentUser = currentUser;
    }

    public async Task ApproveAsync(Guid id)
    {
        var order = await _repo.GetAsync(id);

        // 不能审批自己创建的单据
        if (order.CreatorId == _currentUser.Id)
        {
            throw new BusinessException(
                "ERP:Purchase:CannotApproveSelf",
                "不能审批自己创建的采购单");
        }

        order.Approve(_currentUser.Id!.Value);
        // → 聚合根内部: AddDomainEvent(new PurchaseApprovedEvent(...))

        await _uow.SaveChangesAsync();
        // → AuditingInterceptor 自动填充 LastModificationTime
        // → DomainEventInterceptor 自动分发 PurchaseApprovedEvent
    }
}
```

---

## 第七步：前端 React 对接

```typescript
// ============================================
// 前端 React 权限管理
// ============================================

// ============ 1. 登录 ============

// auth.ts - SSO 登录跳转
export const login = () => {
  const ssoUrl = 'https://sso.example.com';
  const clientId = 'erp';
  const redirectUri = encodeURIComponent('http://localhost:3000/callback');
  
  window.location.href = 
    `${ssoUrl}/connect/authorize?` +
    `client_id=${clientId}&` +
    `redirect_uri=${redirectUri}&` +
    `response_type=code&` +
    `scope=openid profile email&` +
    `state=${generateState()}`;
};

// callback.tsx - 回调页面
export const Callback = () => {
  useEffect(() => {
    const code = new URLSearchParams(location.search).get('code');
    
    // 用 code 换 token
    fetch('/api/auth/token', {
      method: 'POST',
      body: JSON.stringify({ code, redirectUri: '...' }),
    })
    .then(res => res.json())
    .then(data => {
      localStorage.setItem('token', data.data.accessToken);
      localStorage.setItem('refreshToken', data.data.refreshToken);
      
      // 登录成功后拉取权限和菜单
      initPermissions();
    });
  }, []);
};


// ============ 2. 拉取权限和菜单 ============

// permission.store.ts
import { create } from 'zustand';

interface PermissionStore {
  permissions: Set<string>;
  menus: Menu[];
  loaded: boolean;
  
  loadPermissions: () => Promise<void>;
  hasPermission: (permission: string) => boolean;
}

export const usePermissionStore = create<PermissionStore>((set, get) => ({
  permissions: new Set(),
  menus: [],
  loaded: false,

  loadPermissions: async () => {
    const token = localStorage.getItem('token');
    
    // 并行拉取权限和菜单
    const [permRes, menuRes] = await Promise.all([
      fetch('https://perm.example.com/api/permissions/granted?appCode=erp', {
        headers: { Authorization: `Bearer ${token}` },
      }),
      fetch('https://perm.example.com/api/menus/current?appCode=erp', {
        headers: { Authorization: `Bearer ${token}` },
      }),
    ]);

    const permData = await permRes.json();
    const menuData = await menuRes.json();

    set({
      permissions: new Set(permData.data),  // ["ERP.Purchase.View", "ERP.Purchase.Create", ...]
      menus: menuData.data,                  // 菜单树
      loaded: true,
    });
  },

  hasPermission: (permission: string) => {
    return get().permissions.has(permission);
  },
}));


// ============ 3. 权限组件 ============

// components/Authorized.tsx
// 权限包裹组件
interface AuthorizedProps {
  permission: string | string[];
  requireAll?: boolean;
  fallback?: React.ReactNode;
  children: React.ReactNode;
}

export const Authorized: React.FC<AuthorizedProps> = ({
  permission,
  requireAll = true,
  fallback = null,
  children,
}) => {
  const { hasPermission } = usePermissionStore();

  const permissions = Array.isArray(permission) ? permission : [permission];
  
  const authorized = requireAll
    ? permissions.every(p => hasPermission(p))
    : permissions.some(p => hasPermission(p));

  return authorized ? <>{children}</> : <>{fallback}</>;
};

// 用法:
// <Authorized permission="ERP.Purchase.Create">
//   <Button type="primary">新建采购单</Button>
// </Authorized>
//
// <Authorized permission="ERP.Purchase.Delete">
//   <Button danger>删除</Button>
// </Authorized>


// ============ 4. 权限 Hook ============

// hooks/usePermission.ts
export const usePermission = () => {
  const { hasPermission, permissions } = usePermissionStore();
  
  return {
    has: hasPermission,
    
    // 便捷方法
    canView: (module: string) => hasPermission(`ERP.${module}.View`),
    canCreate: (module: string) => hasPermission(`ERP.${module}.Create`),
    canEdit: (module: string) => hasPermission(`ERP.${module}.Edit`),
    canDelete: (module: string) => hasPermission(`ERP.${module}.Delete`),
    
    // 所有权限
    all: permissions,
  };
};

// 用法:
// const { canCreate, canDelete } = usePermission();
// {canCreate('Purchase') && <Button>新建</Button>}
// {canDelete('Purchase') && <Button danger>删除</Button>}


// ============ 5. 动态路由 ============

// router.tsx
import { usePermissionStore } from './permission.store';

const buildRoutes = (menus: Menu[]): RouteObject[] => {
  return menus.map(menu => ({
    path: menu.path,
    element: <LazyComponent code={menu.code} />,
    children: menu.children ? buildRoutes(menu.children) : undefined,
  }));
};

export const AppRouter = () => {
  const { menus, loaded, loadPermissions } = usePermissionStore();

  useEffect(() => {
    if (!loaded) loadPermissions();
  }, []);

  if (!loaded) return <Loading />;

  const routes = buildRoutes(menus);
  
  return useRoutes([
    {
      path: '/',
      element: <Layout menus={menus} />,
      children: routes,
    },
    { path: '/login', element: <Login /> },
    { path: '/callback', element: <Callback /> },
    { path: '*', element: <NotFound /> },
  ]);
};


// ============ 6. 页面中使用 ============

// pages/purchase/PurchaseList.tsx
const PurchaseList = () => {
  const { has } = usePermission();
  const [data, setData] = useState([]);

  const columns = [
    { title: '单号', dataIndex: 'orderNo' },
    { title: '金额', dataIndex: 'amount' },
    { title: '状态', dataIndex: 'status' },
    {
      title: '操作',
      render: (record) => (
        <Space>
          {/* 有查看权限才显示 */}
          <Authorized permission="ERP.Purchase.View">
            <Button onClick={() => viewDetail(record)}>查看</Button>
          </Authorized>
          
          {/* 有编辑权限才显示 */}
          <Authorized permission="ERP.Purchase.Edit">
            <Button onClick={() => edit(record)}>编辑</Button>
          </Authorized>
          
          {/* 有审批权限且状态为待审批才显示 */}
          <Authorized permission="ERP.Purchase.Approve">
            {record.status === 'pending' && (
              <Button type="primary" onClick={() => approve(record)}>审批</Button>
            )}
          </Authorized>
          
          {/* 有删除权限才显示 */}
          <Authorized permission="ERP.Purchase.Delete">
            <Popconfirm onConfirm={() => del(record)}>
              <Button danger>删除</Button>
            </Popconfirm>
          </Authorized>
        </Space>
      ),
    },
  ];

  return (
    <div>
      {/* 工具栏 */}
      <div style={{ marginBottom: 16 }}>
        <Authorized permission="ERP.Purchase.Create">
          <Button type="primary" icon={<PlusOutlined />}>
            新建采购单
          </Button>
        </Authorized>
        
        <Authorized permission="ERP.Purchase.Export">
          <Button icon={<ExportOutlined />}>导出</Button>
        </Authorized>
      </div>

      <Table columns={columns} dataSource={data} />
    </div>
  );
};
```

---

## 第八步：权限中心管理后台操作

```
管理员在权限中心后台的操作流程:

┌──────────────────────────────────────────────────────────────┐
│  权限中心管理后台 (React)                                     │
│                                                              │
│  1. 应用管理                                                 │
│     ┌───────────────────────────────────────────┐           │
│     │ 应用列表                                    │           │
│     │ ┌──────┬────────┬────────┬──────┐         │           │
│     │ │ 编码 │ 名称   │ 权限数 │ 状态 │         │           │
│     │ ├──────┼────────┼────────┼──────┤         │           │
│     │ │ erp  │ ERP    │ 19     │ ✅   │         │           │
│     │ │ crm  │ CRM    │ 12     │ ✅   │         │           │
│     │ │ oa   │ OA     │ 8      │ ✅   │         │           │
│     │ └──────┴────────┴────────┴──────┘         │           │
│     │ (ERP 启动时自动注册了 19 个权限点)           │           │
│     └───────────────────────────────────────────┘           │
│                                                              │
│  2. 权限定义（只读，由业务系统自动注册）                      │
│     ┌───────────────────────────────────────────┐           │
│     │ ERP 权限树                                  │           │
│     │                                             │           │
│     │ 📁 采购管理 (Purchase)                      │           │
│     │   ├── ✅ 采购管理 (ERP.Purchase)            │           │
│     │   │   ├── 查看采购单 (ERP.Purchase.View)    │           │
│     │   │   ├── 创建采购单 (ERP.Purchase.Create)  │           │
│     │   │   ├── 编辑采购单 (ERP.Purchase.Edit)    │           │
│     │   │   ├── 删除采购单 (ERP.Purchase.Delete)  │           │
│     │   │   ├── 审批采购单 (ERP.Purchase.Approve) │           │
│     │   │   └── 导出 (ERP.Purchase.Export)        │           │
│     │ 📁 库存管理 (Stock)                         │           │
│     │   ├── ✅ 库存管理 (ERP.Stock)               │           │
│     │   │   ├── 查看库存 (ERP.Stock.View)         │           │
│     │   │   ├── 入库操作 (ERP.Stock.Inbound)      │           │
│     │   │   └── ...                               │           │
│     └───────────────────────────────────────────┘           │
│                                                              │
│  3. 角色管理 ★★★（管理员在这里操作）                         │
│     ┌───────────────────────────────────────────┐           │
│     │ ERP 角色列表                                │           │
│     │ ┌───────────┬────────┬──────────┐         │           │
│     │ │ 角色      │ 编码   │ 权限数   │         │           │
│     │ ├───────────┼────────┼──────────┤         │           │
│     │ │ 管理员    │ admin  │ 全部(19) │ [配置]  │           │
│     │ │ 采购经理  │ pm     │ 8        │ [配置]  │           │
│     │ │ 采购员    │ buyer  │ 3        │ [配置]  │           │
│     │ │ 仓管员    │ keeper │ 5        │ [配置]  │           │
│     │ │ 查看者    │ viewer │ 3        │ [配置]  │           │
│     │ └───────────┴────────┴──────────┘         │           │
│     │                                [+ 新建角色]│           │
│     └───────────────────────────────────────────┘           │
│                                                              │
│  4. 角色权限配置 ★★★（核心操作页面）                         │
│     ┌───────────────────────────────────────────┐           │
│     │ 配置角色权限: 采购经理 (pm)                  │           │
│     │                                             │           │
│     │ 📁 采购管理                                 │           │
│     │   ☑ 采购管理 (ERP.Purchase)                 │           │
│     │     ☑ 查看采购单                            │           │
│     │     ☑ 创建采购单                            │           │
│     │     ☑ 编辑采购单                            │           │
│     │     ☐ 删除采购单          ← 不给删除权限    │           │
│     │     ☑ 审批采购单                            │           │
│     │     ☑ 导出                                  │           │
│     │ 📁 库存管理                                 │           │
│     │   ☑ 库存管理                                │           │
│     │     ☑ 查看库存                              │           │
│     │     ☐ 入库操作            ← 不给入库权限    │           │
│     │     ☐ 出库操作                              │           │
│     │ 📁 报表中心                                 │           │
│     │   ☐ 报表中心              ← 不给报表权限    │           │
│     │                                             │           │
│     │                      [保存] [重置]          │           │
│     └───────────────────────────────────────────┘           │
│                                                              │
│  5. 用户角色分配                                             │
│     ┌───────────────────────────────────────────┐           │
│     │ ERP 用户角色分配                             │           │
│     │ ┌───────┬──────┬─────────────────┐        │           │
│     │ │ 用户  │ 邮箱 │ 角色             │        │           │
│     │ ├───────┼──────┼─────────────────┤        │           │
│     │ │ 张三  │ ...  │ 管理员           │ [编辑] │           │
│     │ │ 李四  │ ...  │ 采购经理, 查看者  │ [编辑] │           │
│     │ │ 王五  │ ...  │ 采购员           │ [编辑] │           │
│     │ │ 赵六  │ ...  │ 仓管员           │ [编辑] │           │
│     │ └───────┴──────┴─────────────────┘        │           │
│     └───────────────────────────────────────────┘           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 完整数据流（一张图看懂）

```
┌─────────────────────────────────────────────────────────────────┐
│                    完整生命周期                                   │
│                                                                 │
│  开发期:                                                        │
│  ┌──────────────────────────────────────────┐                  │
│  │ 1. 业务开发者定义权限常量:                  │                  │
│  │    ErpPermissions.Purchase.Create         │                  │
│  │                                           │                  │
│  │ 2. 实现 Provider:                          │                  │
│  │    ErpPermissionDefinitionProvider        │                  │
│  │                                           │                  │
│  │ 3. Controller 加 [RequirePermission]:      │                  │
│  │    [RequirePermission("ERP.Purchase.Create")]                │
│  │                                           │                  │
│  │ 4. 前端用 <Authorized>:                    │                  │
│  │    <Authorized permission="ERP.Purchase.Create">             │
│  └──────────────────────────────────────────┘                  │
│                        ↓ 部署启动                               │
│  部署期:                                                        │
│  ┌──────────────────────────────────────────┐                  │
│  │ 5. ERP 启动                                │                  │
│  │    → PermissionRegistrationService 运行    │                  │
│  │    → POST 权限定义到权限中心               │                  │
│  │    → 权限中心自动创建/更新权限点           │                  │
│  └──────────────────────────────────────────┘                  │
│                        ↓                                        │
│  运维期:                                                        │
│  ┌──────────────────────────────────────────┐                  │
│  │ 6. 管理员在权限中心后台:                    │                  │
│  │    → 创建角色: "采购经理"                   │                  │
│  │    → 勾选权限: Create ✅ Delete ❌         │                  │
│  │    → 分配用户: 张三 → 采购经理              │                  │
│  └──────────────────────────────────────────┘                  │
│                        ↓                                        │
│  运行期:                                                        │
│  ┌──────────────────────────────────────────┐                  │
│  │ 7. 张三登录 ERP                             │                  │
│  │    → SSO 认证 → Token                      │                  │
│  │    → 拉取权限 → ["ERP.Purchase.Create",...] │                  │
│  │    → 拉取菜单 → 动态渲染侧边栏              │                  │
│  │    → 点击"新建" → 后端 RequirePermission    │                  │
│  │    → SDK 查缓存 → 有权限 → 执行业务         │                  │
│  └──────────────────────────────────────────┘                  │
│                                                                 │
│  权限变更:                                                      │
│  ┌──────────────────────────────────────────┐                  │
│  │ 8. 管理员取消张三的"创建"权限               │                  │
│  │    → 权限中心删除 PermissionGrant 记录      │                  │
│  │    → 清除缓存                               │                  │
│  │    → 5分钟内生效（缓存过期）                 │                  │
│  │    → 张三下次请求 → 403 无权限               │                  │
│  │    → 前端下次拉权限 → 按钮消失               │                  │
│  └──────────────────────────────────────────┘                  │
│                                                                 │
│  新增权限:                                                      │
│  ┌──────────────────────────────────────────┐                  │
│  │ 9. 开发者新增了"采购单打印"功能              │                  │
│  │    → 加常量: Purchase.Print = "ERP.Purchase.Print"           │
│  │    → Provider 加一行:                       │                  │
│  │      purchase.AddChild("ERP.Purchase.Print", "打印采购单")  │
│  │    → Controller 加特性                      │                  │
│  │    → 重新部署                               │                  │
│  │    → 自动同步到权限中心                      │                  │
│  │    → 管理员在后台勾选给角色                  │                  │
│  └──────────────────────────────────────────┘                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 业务开发者日常操作清单

```
新增一个功能模块（如"供应商管理"）:

  Step 1: 在 ErpPermissions.cs 加常量
  ─────────────────────────────────
  public static class Supplier
  {
      public const string Default = "ERP.Supplier";
      public const string View = "ERP.Supplier.View";
      public const string Create = "ERP.Supplier.Create";
      public const string Edit = "ERP.Supplier.Edit";
      public const string Delete = "ERP.Supplier.Delete";
  }

  Step 2: 在 Provider 加定义
  ──────────────────────────
  var supplierGroup = context.AddGroup("Supplier", "供应商管理");
  var supplier = supplierGroup.AddPermission(ErpPermissions.Supplier.Default, "供应商管理");
  supplier.AddChild(ErpPermissions.Supplier.View, "查看供应商");
  supplier.AddChild(ErpPermissions.Supplier.Create, "新建供应商");
  supplier.AddChild(ErpPermissions.Supplier.Edit, "编辑供应商");
  supplier.AddChild(ErpPermissions.Supplier.Delete, "删除供应商");

  Step 3: Controller 加特性
  ────────────────────────
  [RequirePermission(ErpPermissions.Supplier.Create)]
  public async Task<SupplierDto> Create(...) { }

  Step 4: 前端加权限控制
  ────────────────────
  <Authorized permission="ERP.Supplier.Create">
    <Button>新建供应商</Button>
  </Authorized>

  Step 5: 部署 → 自动注册 → 管理员配权限 → 完成
  
  总结: 加 4 处代码 → 部署 → 完成 ✅
```

 