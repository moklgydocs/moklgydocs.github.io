---
title: 角色管理
date: 2025-04-09
category:
  - ASP.NET_Core
tag:
  - asp.netcore
  - SSO
  - OpenIddict
  - 角色管理
author: Moklgy
order: 14
---

# 角色管理

## 一、当前角色相关代码盘点

```
┌──────────────────────────┬────────┬───────────────────────────┐
│ 模块                      │ 状态   │ 说明                      │
├──────────────────────────┼────────┼───────────────────────────┤
│ SsoRole 实体定义          │ ✅ 有  │ 继承 IdentityRole<Guid>   │
│ 种子数据 admin 角色       │ ✅ 有  │ SeedDataAsync 中创建      │
│ 用户分配角色              │ ✅ 有  │ UserAppService 中有       │
│ Token 中携带角色 Claim    │ ✅ 有  │ AuthorizationController   │
│ 角色 CRUD API             │ ❌ 缺  │ 没有 RoleController       │
│ 角色 AppService           │ ❌ 缺  │ 没有 RoleAppService       │
│ 角色 DTO / Request        │ ❌ 缺  │ 没有定义                  │
│ 角色-权限关联             │ ❌ 缺  │ 细粒度权限（可选扩展）     │
└──────────────────────────┴────────┴───────────────────────────┘
```

**确实缺少角色管理的完整代码。** 下面补全。

---

## 二、完整实现

### 1. DTO / Request 定义

```csharp
// Sso.Application/Contracts/Roles/RoleDto.cs
namespace Sso.Application.Contracts.Roles;

public class RoleDto
{
    public Guid Id { get; set; }
    public string Name { get; set; } = default!;
    public string? Description { get; set; }
    public bool IsSystem { get; set; }
    public DateTime CreatedAt { get; set; }
    public int UserCount { get; set; }
}
```

```csharp
// Sso.Application/Contracts/Roles/CreateRoleRequest.cs
using System.ComponentModel.DataAnnotations;

namespace Sso.Application.Contracts.Roles;

public class CreateRoleRequest
{
    [Required(ErrorMessage = "角色名称不能为空")]
    [StringLength(50, MinimumLength = 2, ErrorMessage = "角色名称长度 2-50")]
    [RegularExpression(@"^[a-zA-Z][a-zA-Z0-9_-]*$",
        ErrorMessage = "角色名称只能包含字母、数字、下划线、连字符，且以字母开头")]
    public string Name { get; set; } = default!;

    [StringLength(200)]
    public string? Description { get; set; }

    /// <summary>
    /// 是否为系统角色（系统角色不可删除）
    /// </summary>
    public bool IsSystem { get; set; } = false;
}
```

```csharp
// Sso.Application/Contracts/Roles/UpdateRoleRequest.cs
using System.ComponentModel.DataAnnotations;

namespace Sso.Application.Contracts.Roles;

public class UpdateRoleRequest
{
    [StringLength(200)]
    public string? Description { get; set; }

    // 角色名称一般不允许修改，如果需要可以加上
    // public string? Name { get; set; }
}
```

```csharp
// Sso.Application/Contracts/Roles/RoleQueryRequest.cs
using Sso.Shared.Paging;

namespace Sso.Application.Contracts.Roles;

public class RoleQueryRequest : PagedRequest
{
    public string? Keyword { get; set; }
}
```

```csharp
// Sso.Application/Contracts/Roles/AssignUsersRequest.cs
namespace Sso.Application.Contracts.Roles;

/// <summary>
/// 批量给角色分配用户 / 从角色移除用户
/// </summary>
public class AssignUsersToRoleRequest
{
    public List<Guid> UserIds { get; set; } = new();
}
```

### 2. RoleAppService

```csharp
// Sso.Application/Services/RoleAppService.cs
using Microsoft.AspNetCore.Identity;
using Microsoft.EntityFrameworkCore;
using Sso.Application.Contracts.Roles;
using Sso.Domain.Entities;
using Sso.Infrastructure.Data;
using Sso.Shared.Paging;

namespace Sso.Application.Services;

public class RoleAppService
{
    private readonly RoleManager<SsoRole> _roleManager;
    private readonly UserManager<SsoUser> _userManager;
    private readonly SsoDbContext _dbContext;

    public RoleAppService(
        RoleManager<SsoRole> roleManager,
        UserManager<SsoUser> userManager,
        SsoDbContext dbContext)
    {
        _roleManager = roleManager;
        _userManager = userManager;
        _dbContext = dbContext;
    }

    // ============================
    // 基础 CRUD
    // ============================

    /// <summary>
    /// 创建角色
    /// </summary>
    public async Task<RoleDto> CreateAsync(CreateRoleRequest request)
    {
        if (await _roleManager.RoleExistsAsync(request.Name))
            throw new InvalidOperationException($"角色 '{request.Name}' 已存在");

        var role = new SsoRole
        {
            Name = request.Name,
            Description = request.Description,
            IsSystem = request.IsSystem,
            CreatedAt = DateTime.UtcNow
        };

        var result = await _roleManager.CreateAsync(role);
        if (!result.Succeeded)
        {
            var errors = string.Join("; ", result.Errors.Select(e => e.Description));
            throw new InvalidOperationException($"创建角色失败: {errors}");
        }

        return await MapToDtoAsync(role);
    }

    /// <summary>
    /// 更新角色
    /// </summary>
    public async Task<RoleDto> UpdateAsync(Guid id, UpdateRoleRequest request)
    {
        var role = await _roleManager.FindByIdAsync(id.ToString())
            ?? throw new KeyNotFoundException($"角色 '{id}' 不存在");

        if (role.IsSystem)
            throw new InvalidOperationException("系统角色不允许修改");

        if (request.Description != null)
            role.Description = request.Description;

        var result = await _roleManager.UpdateAsync(role);
        if (!result.Succeeded)
        {
            var errors = string.Join("; ", result.Errors.Select(e => e.Description));
            throw new InvalidOperationException($"更新角色失败: {errors}");
        }

        return await MapToDtoAsync(role);
    }

    /// <summary>
    /// 删除角色
    /// </summary>
    public async Task DeleteAsync(Guid id)
    {
        var role = await _roleManager.FindByIdAsync(id.ToString())
            ?? throw new KeyNotFoundException($"角色 '{id}' 不存在");

        if (role.IsSystem)
            throw new InvalidOperationException("系统角色不允许删除");

        // 检查是否还有用户在使用
        var usersInRole = await _userManager.GetUsersInRoleAsync(role.Name!);
        if (usersInRole.Any())
            throw new InvalidOperationException(
                $"角色 '{role.Name}' 下还有 {usersInRole.Count} 个用户，请先移除用户后再删除");

        var result = await _roleManager.DeleteAsync(role);
        if (!result.Succeeded)
        {
            var errors = string.Join("; ", result.Errors.Select(e => e.Description));
            throw new InvalidOperationException($"删除角色失败: {errors}");
        }
    }

    /// <summary>
    /// 获取单个角色
    /// </summary>
    public async Task<RoleDto?> GetByIdAsync(Guid id)
    {
        var role = await _roleManager.FindByIdAsync(id.ToString());
        return role == null ? null : await MapToDtoAsync(role);
    }

    /// <summary>
    /// 根据名称获取角色
    /// </summary>
    public async Task<RoleDto?> GetByNameAsync(string name)
    {
        var role = await _roleManager.FindByNameAsync(name);
        return role == null ? null : await MapToDtoAsync(role);
    }

    /// <summary>
    /// 分页查询
    /// </summary>
    public async Task<PagedResult<RoleDto>> GetPagedAsync(RoleQueryRequest request)
    {
        var query = _roleManager.Roles.AsNoTracking();

        // 关键字搜索
        if (!string.IsNullOrWhiteSpace(request.Keyword))
        {
            var keyword = request.Keyword.Trim().ToLower();
            query = query.Where(r =>
                r.Name!.ToLower().Contains(keyword) ||
                (r.Description != null && r.Description.ToLower().Contains(keyword)));
        }

        // 排序
        query = request.OrderBy?.ToLower() switch
        {
            "name" => request.IsDesc
                ? query.OrderByDescending(r => r.Name)
                : query.OrderBy(r => r.Name),
            "createdat" => request.IsDesc
                ? query.OrderByDescending(r => r.CreatedAt)
                : query.OrderBy(r => r.CreatedAt),
            _ => query.OrderByDescending(r => r.CreatedAt)
        };

        var total = await query.CountAsync();
        var roles = await query
            .Skip((request.Page - 1) * request.PageSize)
            .Take(request.PageSize)
            .ToListAsync();

        var dtos = new List<RoleDto>();
        foreach (var role in roles)
        {
            dtos.Add(await MapToDtoAsync(role));
        }

        return new PagedResult<RoleDto>(dtos, total, request.Page, request.PageSize);
    }

    /// <summary>
    /// 获取所有角色（下拉选择用）
    /// </summary>
    public async Task<List<RoleDto>> GetAllAsync()
    {
        var roles = await _roleManager.Roles
            .AsNoTracking()
            .OrderBy(r => r.Name)
            .ToListAsync();

        var dtos = new List<RoleDto>();
        foreach (var role in roles)
        {
            dtos.Add(await MapToDtoAsync(role));
        }
        return dtos;
    }

    // ============================
    // 角色-用户关联
    // ============================

    /// <summary>
    /// 获取角色下的所有用户
    /// </summary>
    public async Task<List<RoleUserDto>> GetUsersInRoleAsync(Guid roleId)
    {
        var role = await _roleManager.FindByIdAsync(roleId.ToString())
            ?? throw new KeyNotFoundException($"角色 '{roleId}' 不存在");

        var users = await _userManager.GetUsersInRoleAsync(role.Name!);

        return users.Select(u => new RoleUserDto
        {
            Id = u.Id,
            UserName = u.UserName!,
            DisplayName = u.DisplayName,
            Email = u.Email,
            IsActive = u.IsActive
        }).ToList();
    }

    /// <summary>
    /// 批量添加用户到角色
    /// </summary>
    public async Task AddUsersToRoleAsync(Guid roleId, AssignUsersToRoleRequest request)
    {
        var role = await _roleManager.FindByIdAsync(roleId.ToString())
            ?? throw new KeyNotFoundException($"角色 '{roleId}' 不存在");

        var errors = new List<string>();

        foreach (var userId in request.UserIds)
        {
            var user = await _userManager.FindByIdAsync(userId.ToString());
            if (user == null)
            {
                errors.Add($"用户 '{userId}' 不存在");
                continue;
            }

            if (await _userManager.IsInRoleAsync(user, role.Name!))
                continue; // 已在角色中，跳过

            var result = await _userManager.AddToRoleAsync(user, role.Name!);
            if (!result.Succeeded)
            {
                errors.Add($"用户 '{user.UserName}' 分配失败: " +
                           string.Join(", ", result.Errors.Select(e => e.Description)));
            }
        }

        if (errors.Any())
            throw new InvalidOperationException(
                $"部分用户分配失败: {string.Join("; ", errors)}");
    }

    /// <summary>
    /// 批量从角色移除用户
    /// </summary>
    public async Task RemoveUsersFromRoleAsync(Guid roleId, AssignUsersToRoleRequest request)
    {
        var role = await _roleManager.FindByIdAsync(roleId.ToString())
            ?? throw new KeyNotFoundException($"角色 '{roleId}' 不存在");

        var errors = new List<string>();

        foreach (var userId in request.UserIds)
        {
            var user = await _userManager.FindByIdAsync(userId.ToString());
            if (user == null)
            {
                errors.Add($"用户 '{userId}' 不存在");
                continue;
            }

            if (!await _userManager.IsInRoleAsync(user, role.Name!))
                continue; // 不在角色中，跳过

            var result = await _userManager.RemoveFromRoleAsync(user, role.Name!);
            if (!result.Succeeded)
            {
                errors.Add($"用户 '{user.UserName}' 移除失败: " +
                           string.Join(", ", result.Errors.Select(e => e.Description)));
            }
        }

        if (errors.Any())
            throw new InvalidOperationException(
                $"部分用户移除失败: {string.Join("; ", errors)}");
    }

    // ============================
    // 私有方法
    // ============================

    private async Task<RoleDto> MapToDtoAsync(SsoRole role)
    {
        // 统计角色下的用户数
        var userCount = await _dbContext.UserRoles
            .CountAsync(ur => ur.RoleId == role.Id);

        return new RoleDto
        {
            Id = role.Id,
            Name = role.Name!,
            Description = role.Description,
            IsSystem = role.IsSystem,
            CreatedAt = role.CreatedAt,
            UserCount = userCount
        };
    }
}

/// <summary>
/// 角色下的用户信息
/// </summary>
public class RoleUserDto
{
    public Guid Id { get; set; }
    public string UserName { get; set; } = default!;
    public string? DisplayName { get; set; }
    public string? Email { get; set; }
    public bool IsActive { get; set; }
}
```

### 3. SsoRole 实体补全

```csharp
// Sso.Domain/Entities/SsoRole.cs
using Microsoft.AspNetCore.Identity;

namespace Sso.Domain.Entities;

public class SsoRole : IdentityRole<Guid>
{
    /// <summary>
    /// 角色描述
    /// </summary>
    public string? Description { get; set; }

    /// <summary>
    /// 是否为系统内置角色（不可删除、不可改名）
    /// </summary>
    public bool IsSystem { get; set; }

    /// <summary>
    /// 创建时间
    /// </summary>
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}
```

### 4. RoleManageController

```csharp
// Sso.AuthServer/Controllers/RoleManageController.cs
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Sso.Application.Contracts.Roles;
using Sso.Application.Services;
using Sso.Shared.Results;

namespace Sso.AuthServer.Controllers;

[ApiController]
[Route("api/manage/roles")]
[Authorize(Roles = "admin")]
public class RoleManageController : ControllerBase
{
    private readonly RoleAppService _roleAppService;

    public RoleManageController(RoleAppService roleAppService)
    {
        _roleAppService = roleAppService;
    }

    // ============================
    // 基础 CRUD
    // ============================

    /// <summary>
    /// 创建角色
    /// </summary>
    [HttpPost]
    public async Task<IActionResult> Create([FromBody] CreateRoleRequest request)
    {
        try
        {
            var role = await _roleAppService.CreateAsync(request);
            return Ok(ApiResult<RoleDto>.Ok(role, "创建成功"));
        }
        catch (InvalidOperationException ex)
        {
            return BadRequest(ApiResult.Fail(ex.Message));
        }
    }

    /// <summary>
    /// 更新角色
    /// </summary>
    [HttpPut("{id:guid}")]
    public async Task<IActionResult> Update(Guid id, [FromBody] UpdateRoleRequest request)
    {
        try
        {
            var role = await _roleAppService.UpdateAsync(id, request);
            return Ok(ApiResult<RoleDto>.Ok(role, "更新成功"));
        }
        catch (KeyNotFoundException ex)
        {
            return NotFound(ApiResult.Fail(ex.Message, 404));
        }
        catch (InvalidOperationException ex)
        {
            return BadRequest(ApiResult.Fail(ex.Message));
        }
    }

    /// <summary>
    /// 删除角色
    /// </summary>
    [HttpDelete("{id:guid}")]
    public async Task<IActionResult> Delete(Guid id)
    {
        try
        {
            await _roleAppService.DeleteAsync(id);
            return Ok(ApiResult.Ok("删除成功"));
        }
        catch (KeyNotFoundException ex)
        {
            return NotFound(ApiResult.Fail(ex.Message, 404));
        }
        catch (InvalidOperationException ex)
        {
            return BadRequest(ApiResult.Fail(ex.Message));
        }
    }

    /// <summary>
    /// 获取单个角色
    /// </summary>
    [HttpGet("{id:guid}")]
    public async Task<IActionResult> Get(Guid id)
    {
        var role = await _roleAppService.GetByIdAsync(id);
        return role == null
            ? NotFound(ApiResult.Fail("角色不存在", 404))
            : Ok(ApiResult<RoleDto>.Ok(role));
    }

    /// <summary>
    /// 分页查询角色
    /// </summary>
    [HttpGet]
    public async Task<IActionResult> GetPaged([FromQuery] RoleQueryRequest request)
    {
        var result = await _roleAppService.GetPagedAsync(request);
        return Ok(ApiResult<PagedResult<RoleDto>>.Ok(result));
    }

    /// <summary>
    /// 获取所有角色（下拉选择用，不分页）
    /// </summary>
    [HttpGet("all")]
    public async Task<IActionResult> GetAll()
    {
        var roles = await _roleAppService.GetAllAsync();
        return Ok(ApiResult<List<RoleDto>>.Ok(roles));
    }

    // ============================
    // 角色-用户关联
    // ============================

    /// <summary>
    /// 获取角色下的所有用户
    /// </summary>
    [HttpGet("{id:guid}/users")]
    public async Task<IActionResult> GetUsersInRole(Guid id)
    {
        try
        {
            var users = await _roleAppService.GetUsersInRoleAsync(id);
            return Ok(ApiResult<List<RoleUserDto>>.Ok(users));
        }
        catch (KeyNotFoundException ex)
        {
            return NotFound(ApiResult.Fail(ex.Message, 404));
        }
    }

    /// <summary>
    /// 批量添加用户到角色
    /// </summary>
    [HttpPost("{id:guid}/users")]
    public async Task<IActionResult> AddUsersToRole(
        Guid id, [FromBody] AssignUsersToRoleRequest request)
    {
        try
        {
            await _roleAppService.AddUsersToRoleAsync(id, request);
            return Ok(ApiResult.Ok("用户分配成功"));
        }
        catch (KeyNotFoundException ex)
        {
            return NotFound(ApiResult.Fail(ex.Message, 404));
        }
        catch (InvalidOperationException ex)
        {
            return BadRequest(ApiResult.Fail(ex.Message));
        }
    }

    /// <summary>
    /// 批量从角色移除用户
    /// </summary>
    [HttpDelete("{id:guid}/users")]
    public async Task<IActionResult> RemoveUsersFromRole(
        Guid id, [FromBody] AssignUsersToRoleRequest request)
    {
        try
        {
            await _roleAppService.RemoveUsersFromRoleAsync(id, request);
            return Ok(ApiResult.Ok("用户移除成功"));
        }
        catch (KeyNotFoundException ex)
        {
            return NotFound(ApiResult.Fail(ex.Message, 404));
        }
        catch (InvalidOperationException ex)
        {
            return BadRequest(ApiResult.Fail(ex.Message));
        }
    }
}
```

### 5. PagedRequest 补全排序字段

```csharp
// Sso.Shared/Paging/PagedRequest.cs
namespace Sso.Shared.Paging;

public class PagedRequest
{
    private int _page = 1;
    private int _pageSize = 20;

    public int Page
    {
        get => _page;
        set => _page = value < 1 ? 1 : value;
    }

    public int PageSize
    {
        get => _pageSize;
        set => _pageSize = value switch
        {
            < 1 => 20,
            > 100 => 100,
            _ => value
        };
    }

    /// <summary>
    /// 排序字段
    /// </summary>
    public string? OrderBy { get; set; }

    /// <summary>
    /// 是否降序
    /// </summary>
    public bool IsDesc { get; set; } = true;
}
```

### 6. 注册服务

```csharp
// Sso.Application/ApplicationServiceExtensions.cs
public static class ApplicationServiceExtensions
{
    public static IServiceCollection AddSsoApplication(this IServiceCollection services)
    {
        services.AddScoped<ClientAppService>();
        services.AddScoped<ScopeAppService>();
        services.AddScoped<UserAppService>();
        services.AddScoped<RoleAppService>();              // ← 新增
        services.AddScoped<TokenRevocationService>();
        return services;
    }
}
```

### 7. 种子数据补全

```csharp
// Program.cs SeedDataAsync 中
static async Task SeedDataAsync(WebApplication app)
{
    using var scope = app.Services.CreateScope();
    var services = scope.ServiceProvider;

    var roleManager = services.GetRequiredService<RoleManager<SsoRole>>();

    // ====== 系统角色 ======
    var systemRoles = new[]
    {
        new { Name = "admin",    Description = "系统管理员", IsSystem = true  },
        new { Name = "user",     Description = "普通用户",   IsSystem = true  },
        new { Name = "auditor",  Description = "审计员",     IsSystem = false },
    };

    foreach (var r in systemRoles)
    {
        if (!await roleManager.RoleExistsAsync(r.Name))
        {
            await roleManager.CreateAsync(new SsoRole
            {
                Name = r.Name,
                Description = r.Description,
                IsSystem = r.IsSystem,
                CreatedAt = DateTime.UtcNow
            });
        }
    }

    // ... 其余种子数据不变 ...
}
```

### 8. EF Core 配置（确保新字段有映射）

```csharp
// Sso.Infrastructure/Data/Configurations/SsoRoleConfiguration.cs
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using Sso.Domain.Entities;

namespace Sso.Infrastructure.Data.Configurations;

public class SsoRoleConfiguration : IEntityTypeConfiguration<SsoRole>
{
    public void Configure(EntityTypeBuilder<SsoRole> builder)
    {
        builder.ToTable("SsoRoles");

        builder.Property(r => r.Description)
            .HasMaxLength(200);

        builder.Property(r => r.IsSystem)
            .HasDefaultValue(false);

        builder.Property(r => r.CreatedAt)
            .HasDefaultValueSql("GETUTCDATE()"); // SQL Server
            // .HasDefaultValueSql("NOW()");      // PostgreSQL
    }
}
```

---

## 三、API 测试

```bash
# ====== 基础 CRUD ======

# 创建角色
curl -X POST https://localhost:7008/api/manage/roles \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{
    "name": "editor",
    "description": "内容编辑",
    "isSystem": false
  }'

# 查询所有角色（下拉用）
curl -X GET https://localhost:7008/api/manage/roles/all \
  -b cookies.txt

# 分页查询
curl -X GET "https://localhost:7008/api/manage/roles?page=1&pageSize=10&keyword=admin" \
  -b cookies.txt

# 更新角色
curl -X PUT https://localhost:7008/api/manage/roles/{roleId} \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{"description": "内容编辑（含审核权限）"}'

# 删除角色
curl -X DELETE https://localhost:7008/api/manage/roles/{roleId} \
  -b cookies.txt


# ====== 角色-用户关联 ======

# 查看角色下的用户
curl -X GET https://localhost:7008/api/manage/roles/{roleId}/users \
  -b cookies.txt

# 批量添加用户到角色
curl -X POST https://localhost:7008/api/manage/roles/{roleId}/users \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{"userIds": ["guid-001", "guid-002"]}'

# 批量从角色移除用户
curl -X DELETE https://localhost:7008/api/manage/roles/{roleId}/users \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{"userIds": ["guid-002"]}'
```

---

## 四、当前 SSO 管理端 API 全景

```
┌─────────────────────────────────────────────────────┐
│               SSO 管理端 API 总览                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  /api/account                                       │
│    POST  /login              登录                   │
│    POST  /logout             登出                   │
│    POST  /logout-token       Token登出              │
│    POST  /change-password    修改密码               │
│    GET   /current            当前用户               │
│                                                     │
│  /api/manage/clients         客户端管理              │
│    POST  /                   创建客户端             │
│    GET   /                   分页查询               │
│    GET   /{clientId}         获取详情               │
│    PUT   /{clientId}         更新                   │
│    DELETE/{clientId}         删除                   │
│    POST  /{clientId}/enable  启用                   │
│    POST  /{clientId}/disable 禁用                   │
│                                                     │
│  /api/manage/scopes          Scope管理              │
│    POST  /                   创建                   │
│    GET   /                   分页查询               │
│    PUT   /{name}             更新                   │
│    DELETE/{name}             删除                   │
│                                                     │
│  /api/manage/users           用户管理               │
│    POST  /                   创建用户               │
│    GET   /                   分页查询               │
│    GET   /{id}               获取详情               │
│    PUT   /{id}               更新                   │
│    POST  /{id}/reset-password 重置密码              │
│    POST  /{id}/kick          踢出                   │
│    POST  /{id}/disable       禁用                   │
│                                                     │
│  /api/manage/roles           角色管理 ✅ 新增        │
│    POST  /                   创建角色               │
│    GET   /                   分页查询               │
│    GET   /all                所有角色(下拉)         │
│    GET   /{id}               获取详情               │
│    PUT   /{id}               更新                   │
│    DELETE/{id}               删除                   │
│    GET   /{id}/users         角色下的用户           │
│    POST  /{id}/users         批量添加用户           │
│    DELETE/{id}/users         批量移除用户           │
│                                                     │
│  /connect                    OAuth2/OIDC 端点       │
│    GET   /authorize          授权端点               │
│    POST  /token              令牌端点               │
│    GET   /userinfo           用户信息               │
│    POST  /logout             OIDC登出               │
│    POST  /revoke             令牌撤销               │
│    POST  /introspect         令牌内省               │
│                                                     │
└─────────────────────────────────────────────────────┘
```
