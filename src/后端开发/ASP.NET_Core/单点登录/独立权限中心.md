---
title: 独立权限中心
date: 2025-04-09
category:
  - ASP.NET_Core
tag:
  - asp.netcore
  - SSO
  - OpenIddict
  - 权限中心
  - RBAC
author: Moklgy
order: 15
---

# 从SSO出发，实现一个独立的独立权限中心

---

## 整体定位

```
权限中心 = 独立微服务
  
  项目名: Perm（Permission Center）
  
  对标: ABP 的 PermissionManagement + Identity 模块
  但是: 不依赖 ABP 框架，纯手写
  目标: 轻量、可扩展、兼容 ABAC 演进
```

---

## 和 SSO 的关系

```
SSO（已完成）                    权限中心（现在要做的）
┌───────────────────┐           ┌───────────────────────┐
│                   │           │                       │
│  管:              │           │  管:                  │
│   用户 CRUD       │  Token    │   应用注册            │
│   认证登录        │─────────► │   权限定义            │
│   Token 颁发/验证 │           │   角色定义（应用级）   │
│   客户端管理      │           │   角色-权限映射        │
│                   │           │   用户-角色分配        │
│  不管:            │  用户ID   │   菜单管理            │
│   角色 ✅ 移出    │◄─────────│   数据权限规则         │
│   权限            │  (sub)    │   权限查询 API        │
│   菜单            │           │                       │
│                   │           │  认证方式:             │
│                   │           │   接入 SSO 做认证      │
│                   │           │   自己是资源服务器      │
└───────────────────┘           └───────────────────────┘
```

---

## 项目结构

```
Perm/
├── src/
│   ├── Perm.Domain/                      # 领域层
│   │   ├── Abstractions/                 # 基类（和SSO共享设计）
│   │   │   ├── IEntity.cs
│   │   │   ├── Entity.cs
│   │   │   ├── FullAuditedEntity.cs
│   │   │   ├── ISoftDelete.cs
│   │   │   └── IHasCreationTime.cs
│   │   │
│   │   ├── Entities/                     # 核心实体
│   │   │   ├── App.cs                    # 应用
│   │   │   ├── PermissionGroup.cs        # 权限组定义
│   │   │   ├── PermissionDefinition.cs   # 权限定义
│   │   │   ├── Role.cs                   # 角色（应用级）
│   │   │   ├── RolePermission.cs         # 角色-权限映射
│   │   │   ├── UserRole.cs              # 用户-角色
│   │   │   ├── PermissionGrant.cs        # 权限授予（通用模型）
│   │   │   └── Menu.cs                   # 菜单
│   │   │
│   │   ├── Enums/
│   │   │   ├── PermissionType.cs         # 权限类型
│   │   │   ├── ProviderType.cs           # 授权提供者类型
│   │   │   └── MenuType.cs
│   │   │
│   │   └── Repositories/                 # 仓储接口
│   │       ├── IAppRepository.cs
│   │       ├── IPermissionRepository.cs
│   │       ├── IRoleRepository.cs
│   │       └── IMenuRepository.cs
│   │
│   ├── Perm.Application/                 # 应用层
│   │   ├── Contracts/                    # DTO + 接口
│   │   │   ├── Apps/
│   │   │   ├── Permissions/
│   │   │   ├── Roles/
│   │   │   ├── Menus/
│   │   │   └── Common/
│   │   │       ├── PagedRequest.cs
│   │   │       ├── PagedResult.cs
│   │   │       └── ApiResult.cs
│   │   │
│   │   ├── Services/                     # 应用服务
│   │   │   ├── AppService.cs
│   │   │   ├── PermissionDefinitionService.cs
│   │   │   ├── RoleService.cs
│   │   │   ├── PermissionGrantService.cs
│   │   │   ├── UserPermissionService.cs
│   │   │   └── MenuService.cs
│   │   │
│   │   └── Authorization/               # 权限框架核心
│   │       ├── IPermissionDefinitionProvider.cs
│   │       ├── PermissionDefinitionManager.cs
│   │       ├── IPermissionChecker.cs
│   │       ├── PermissionChecker.cs
│   │       ├── IPermissionValueProvider.cs
│   │       ├── RolePermissionValueProvider.cs
│   │       ├── UserPermissionValueProvider.cs
│   │       └── PermissionValueProviderManager.cs
│   │
│   ├── Perm.Infrastructure/             # 基础设施层
│   │   ├── Data/
│   │   │   ├── PermDbContext.cs
│   │   │   ├── Configurations/           # 所有 EF 配置
│   │   │   └── Repositories/             # 仓储实现
│   │   │
│   │   ├── Caching/
│   │   │   └── PermissionCacheManager.cs
│   │   │
│   │   └── DependencyInjection.cs
│   │
│   └── Perm.Host/                        # 启动层（Web API）
│       ├── Controllers/
│       │   ├── AppController.cs
│       │   ├── PermissionController.cs
│       │   ├── RoleController.cs
│       │   ├── UserPermissionController.cs
│       │   └── MenuController.cs
│       │
│       ├── Program.cs
│       └── appsettings.json
│
├── clients/
│   └── Perm.Client/                      # SDK（给业务系统引用）
│       ├── IPermissionApi.cs
│       ├── PermissionClient.cs
│       ├── RequirePermissionAttribute.cs
│       ├── PermissionAuthorizationFilter.cs
│       └── ServiceCollectionExtensions.cs
│
└── tests/
    └── Perm.Tests/
```

---

## 核心设计 — 模仿 ABP 权限框架

### 1. ABP 的核心概念（我们要复刻的）

```
ABP 权限框架的精髓:

1. PermissionDefinition（权限定义）
   → 代码里声明有哪些权限
   → 启动时自动注册
   → 也可以从数据库加载（我们要做的）

2. PermissionGrant（权限授予）
   → 不直接绑定"角色-权限"
   → 而是 ProviderName + ProviderKey + PermissionName
   → ProviderName = "Role" / "User" / "Client" / "OrganizationUnit"
   → ProviderKey = "manager" / "user-guid" / "client-id" / "ou-id"
   
   这就是 ABP 最精妙的设计 ✅
   这也是兼容 ABAC 的关键

3. IPermissionValueProvider（权限值提供者）
   → 策略模式
   → RolePermissionValueProvider: 检查角色有没有这个权限
   → UserPermissionValueProvider: 检查用户是否被直接授权
   → ClientPermissionValueProvider: 检查客户端权限
   → 未来: AttributePermissionValueProvider → ABAC
   → 按顺序遍历所有 Provider，任何一个说"有权限"就通过

4. IPermissionChecker
   → 最终对外暴露的接口
   → IsGrantedAsync(userId, permissionName)
   → 内部遍历所有 ValueProvider
```

```
我们的复刻:

ABP 的:
  PermissionDefinitionProvider → 我们: IPermissionDefinitionProvider
  PermissionGrant             → 我们: PermissionGrant（同名）
  IPermissionValueProvider    → 我们: IPermissionValueProvider
  PermissionChecker           → 我们: PermissionChecker

  核心区别: 
    ABP 权限定义在代码里（编译时确定）
    我们: 权限定义在数据库里（运行时动态，因为是独立服务）
    我们也支持代码定义（业务系统SDK里）
```

---

## 核心实体

```csharp
// ============================================
// Perm.Domain/Entities/App.cs
// 应用（业务系统）
// ============================================

namespace Perm.Domain.Entities;

public class App : FullAuditedEntity
{
    /// <summary>
    /// 应用编码（对应 SSO 的 ClientId）
    /// </summary>
    public string Code { get; set; } = default!;

    /// <summary>
    /// 应用名称
    /// </summary>
    public string Name { get; set; } = default!;

    public string? Description { get; set; }
    public string? LogoUrl { get; set; }
    public bool IsEnabled { get; set; } = true;
    public int Sort { get; set; }

    // 导航
    public List<PermissionGroup> PermissionGroups { get; set; } = [];
    public List<Role> Roles { get; set; } = [];
    public List<Menu> Menus { get; set; } = [];
}
```

```csharp
// ============================================
// Perm.Domain/Entities/PermissionGroup.cs
// 权限组（分类用）
// ============================================

namespace Perm.Domain.Entities;

/// <summary>
/// 权限组
/// 例: ERP 下的 "采购管理" / "库存管理" / "报表中心"
/// </summary>
public class PermissionGroup : AuditedEntity
{
    public Guid AppId { get; set; }

    /// <summary>
    /// 组编码: "Purchase" / "Stock" / "Report"
    /// </summary>
    public string Code { get; set; } = default!;

    /// <summary>
    /// 显示名称: "采购管理"
    /// </summary>
    public string DisplayName { get; set; } = default!;

    public int Sort { get; set; }

    // 导航
    public App App { get; set; } = default!;
    public List<PermissionDefinition> Permissions { get; set; } = [];
}
```

```csharp
// ============================================
// Perm.Domain/Entities/PermissionDefinition.cs
// 权限定义
// ============================================

namespace Perm.Domain.Entities;

/// <summary>
/// 权限定义
/// 这是权限中心最核心的表
/// 存储所有应用的所有权限点
/// </summary>
public class PermissionDefinition : AuditedEntity
{
    public Guid GroupId { get; set; }

    /// <summary>
    /// 权限全名（唯一标识）
    /// 格式: "AppCode.Group.Action"
    /// 例: "ERP.Purchase.Create" / "ERP.Purchase.Approve"
    /// </summary>
    public string Name { get; set; } = default!;

    /// <summary>
    /// 显示名称: "创建采购单"
    /// </summary>
    public string DisplayName { get; set; } = default!;

    /// <summary>
    /// 父权限ID（树形结构）
    /// null = 顶级权限
    /// </summary>
    public Guid? ParentId { get; set; }

    /// <summary>
    /// 权限类型
    /// </summary>
    public PermissionType Type { get; set; } = PermissionType.Function;

    /// <summary>
    /// 是否启用
    /// </summary>
    public bool IsEnabled { get; set; } = true;

    /// <summary>
    /// 多租户侧（预留）
    /// </summary>
    public MultiTenancySides MultiTenancySide { get; set; } = MultiTenancySides.Both;

    /// <summary>
    /// 额外属性（为 ABAC 预留）
    /// 存 JSON: { "condition": "age > 18", "department": "IT" }
    /// </summary>
    public string? Properties { get; set; }

    public int Sort { get; set; }

    // 导航
    public PermissionGroup Group { get; set; } = default!;
    public PermissionDefinition? Parent { get; set; }
    public List<PermissionDefinition> Children { get; set; } = [];
}
```

```csharp
// ============================================
// Perm.Domain/Enums/PermissionType.cs
// ============================================

namespace Perm.Domain.Enums;

public enum PermissionType
{
    /// <summary>
    /// 功能权限（默认，RBAC）
    /// </summary>
    Function = 0,

    /// <summary>
    /// 菜单权限
    /// </summary>
    Menu = 1,

    /// <summary>
    /// API 权限
    /// </summary>
    Api = 2,

    /// <summary>
    /// 按钮权限
    /// </summary>
    Button = 3,

    /// <summary>
    /// 数据权限（预留，ABAC 方向）
    /// </summary>
    Data = 10,

    /// <summary>
    /// 字段权限（预留）
    /// </summary>
    Field = 11,
}

public enum MultiTenancySides
{
    Host = 1,
    Tenant = 2,
    Both = 3,
}
```

```csharp
// ============================================
// Perm.Domain/Entities/Role.cs
// 角色（应用级）
// ============================================

namespace Perm.Domain.Entities;

/// <summary>
/// 角色
/// 每个应用有自己的角色
/// ERP: admin / manager / purchaser / viewer
/// CRM: admin / sales / viewer
/// </summary>
public class Role : FullAuditedEntity
{
    public Guid AppId { get; set; }

    /// <summary>
    /// 角色编码: "manager"
    /// Token 和权限校验使用此值
    /// </summary>
    public string Code { get; set; } = default!;

    /// <summary>
    /// 角色名称: "经理"
    /// </summary>
    public string Name { get; set; } = default!;

    public string? Description { get; set; }

    /// <summary>
    /// 是否默认角色（新用户加入该应用时自动拥有）
    /// </summary>
    public bool IsDefault { get; set; }

    /// <summary>
    /// 是否静态角色（不可删除，如 admin）
    /// </summary>
    public bool IsStatic { get; set; }

    /// <summary>
    /// 是否公开（所有人可见 vs 仅管理员可见）
    /// </summary>
    public bool IsPublic { get; set; } = true;

    public bool IsEnabled { get; set; } = true;
    public int Sort { get; set; }

    // 导航
    public App App { get; set; } = default!;
}
```

---

### **PermissionGrant — ABP 精髓的复刻** ⭐

```csharp
// ============================================
// Perm.Domain/Entities/PermissionGrant.cs
// 权限授予（ABP 核心设计的复刻）
// ============================================

namespace Perm.Domain.Entities;

/// <summary>
/// 权限授予
/// 
/// ABP 的精髓: 不直接做 RolePermission / UserPermission 表
/// 而是用 ProviderName + ProviderKey 抽象
/// 
/// 这样一张表就能存所有类型的授权:
///   Role 授权:   ProviderName="R", ProviderKey="manager"
///   User 授权:   ProviderName="U", ProviderKey="user-guid"
///   Client 授权: ProviderName="C", ProviderKey="erp"
///   OU 授权:     ProviderName="O", ProviderKey="ou-guid"
///   
/// 未来 ABAC:
///   Attribute:   ProviderName="A", ProviderKey="age>=18&dept=IT"
///   
/// 无需改表结构，只加新的 Provider 就行 ✅
/// </summary>
public class PermissionGrant : CreationAuditedEntity
{
    /// <summary>
    /// 所属应用ID
    /// </summary>
    public Guid AppId { get; set; }

    /// <summary>
    /// 权限名称
    /// "ERP.Purchase.Approve"
    /// </summary>
    public string PermissionName { get; set; } = default!;

    /// <summary>
    /// 授权提供者名称
    /// "R" = Role
    /// "U" = User
    /// "C" = Client
    /// "O" = OrganizationUnit
    /// "A" = Attribute (ABAC 预留)
    /// </summary>
    public string ProviderName { get; set; } = default!;

    /// <summary>
    /// 授权提供者 Key
    /// 当 ProviderName = "R" 时: "manager"（角色编码）
    /// 当 ProviderName = "U" 时: "user-guid"（用户ID）
    /// 当 ProviderName = "C" 时: "erp"（客户端编码）
    /// 当 ProviderName = "A" 时: JSON 条件
    /// </summary>
    public string ProviderKey { get; set; } = default!;

    /// <summary>
    /// 是否授予（true = 授予, false = 显式拒绝）
    /// 显式拒绝优先级高于授予
    /// </summary>
    public bool IsGranted { get; set; } = true;

    // 导航
    public App App { get; set; } = default!;
}
```

```
PermissionGrant 表的数据示例:

┌──────┬──────────────────────┬──────────────┬────────────┬───────────┐
│ App  │ PermissionName       │ ProviderName │ ProviderKey│ IsGranted │
├──────┼──────────────────────┼──────────────┼────────────┼───────────┤
│ ERP  │ ERP.Purchase         │ R            │ manager    │ true      │
│ ERP  │ ERP.Purchase.Create  │ R            │ manager    │ true      │
│ ERP  │ ERP.Purchase.Approve │ R            │ manager    │ true      │
│ ERP  │ ERP.Purchase.Delete  │ R            │ manager    │ true      │
│ ERP  │ ERP.Stock.View       │ R            │ manager    │ true      │
│ ERP  │ ERP.Purchase         │ R            │ purchaser  │ true      │
│ ERP  │ ERP.Purchase.Create  │ R            │ purchaser  │ true      │
│ ERP  │ ERP.Purchase.View    │ R            │ purchaser  │ true      │
│──────│──────────────────────│──────────────│────────────│───────────│
│ ERP  │ ERP.Purchase.Delete  │ U            │ guid-张三  │ false     │ ← 张三被显式拒绝删除权限
│ ERP  │ ERP.Report.Export    │ U            │ guid-李四  │ true      │ ← 李四被直接授予导出权限
│──────│──────────────────────│──────────────│────────────│───────────│
│ CRM  │ CRM.Customer.View    │ R            │ sales      │ true      │
│ CRM  │ CRM.Customer.Edit    │ R            │ sales      │ true      │
│ CRM  │ CRM.Deal.Follow      │ R            │ sales      │ true      │
│──────│──────────────────────│──────────────│────────────│───────────│
│ CRM  │ CRM.Customer.View    │ C            │ crm-mobile │ true      │ ← 客户端级别权限
└──────┴──────────────────────┴──────────────┴────────────┴───────────┘

一张表搞定所有授权类型 ✅
```

```csharp
// ============================================
// Perm.Domain/Entities/UserRole.cs
// 用户-角色分配
// ============================================

namespace Perm.Domain.Entities;

/// <summary>
/// 用户-角色关联
/// 用户在某个应用中拥有的角色
/// </summary>
public class UserRole : CreationAuditedEntity
{
    public Guid AppId { get; set; }

    /// <summary>
    /// 用户ID（来自 SSO 的 sub claim）
    /// </summary>
    public Guid UserId { get; set; }

    public Guid RoleId { get; set; }

    // 导航
    public App App { get; set; } = default!;
    public Role Role { get; set; } = default!;
}
```

```csharp
// ============================================
// Perm.Domain/Entities/Menu.cs
// 菜单
// ============================================

namespace Perm.Domain.Entities;

public class Menu : FullAuditedEntity
{
    public Guid AppId { get; set; }

    /// <summary>
    /// 菜单名称: "采购管理"
    /// </summary>
    public string Name { get; set; } = default!;

    /// <summary>
    /// 菜单编码: "purchase"
    /// </summary>
    public string Code { get; set; } = default!;

    /// <summary>
    /// 前端路由: "/purchase/list"
    /// </summary>
    public string? Path { get; set; }

    /// <summary>
    /// 图标: "shopping-cart"
    /// </summary>
    public string? Icon { get; set; }

    /// <summary>
    /// 菜单类型
    /// </summary>
    public MenuType Type { get; set; } = MenuType.Menu;

    /// <summary>
    /// 关联权限名称
    /// 用户有此权限才能看到此菜单
    /// </summary>
    public string? PermissionName { get; set; }

    public Guid? ParentId { get; set; }
    public bool IsVisible { get; set; } = true;
    public bool IsEnabled { get; set; } = true;
    public int Sort { get; set; }

    // 导航
    public App App { get; set; } = default!;
    public Menu? Parent { get; set; }
    public List<Menu> Children { get; set; } = [];
}

public enum MenuType
{
    /// <summary>
    /// 目录
    /// </summary>
    Directory = 0,

    /// <summary>
    /// 菜单
    /// </summary>
    Menu = 1,

    /// <summary>
    /// 按钮
    /// </summary>
    Button = 2,

    /// <summary>
    /// 外链
    /// </summary>
    ExternalLink = 3,
}
```

---

## 权限框架核心 — 复刻 ABP 的 Provider 模式

```csharp
// ============================================
// Perm.Application/Authorization/PermissionProviderNames.cs
// 提供者名称常量
// ============================================

namespace Perm.Application.Authorization;

/// <summary>
/// ABP 风格的提供者名称
/// 单字母，数据库存储紧凑
/// </summary>
public static class PermissionProviderNames
{
    /// <summary>
    /// 角色 Role
    /// </summary>
    public const string Role = "R";

    /// <summary>
    /// 用户 User（直接授权）
    /// </summary>
    public const string User = "U";

    /// <summary>
    /// 客户端 Client
    /// </summary>
    public const string Client = "C";

    /// <summary>
    /// 组织单元 OrganizationUnit（预留）
    /// </summary>
    public const string OrganizationUnit = "O";

    /// <summary>
    /// 属性 Attribute（ABAC 预留）
    /// </summary>
    public const string Attribute = "A";
}
```

```csharp
// ============================================
// Perm.Application/Authorization/IPermissionValueProvider.cs
// 权限值提供者接口（ABP 核心抽象）
// ============================================

namespace Perm.Application.Authorization;

/// <summary>
/// 权限值提供者
/// 
/// ABP 的核心设计: 每种授权来源是一个 Provider
/// 系统遍历所有 Provider，任何一个返回 Granted 就通过
/// 
/// 扩展 ABAC 时: 只需新增一个 AttributePermissionValueProvider
/// 无需修改任何现有代码 ✅
/// </summary>
public interface IPermissionValueProvider
{
    /// <summary>
    /// 提供者名称: "R" / "U" / "C" / "O" / "A"
    /// </summary>
    string Name { get; }

    /// <summary>
    /// 优先级（越小越先执行）
    /// 显式拒绝类的 Provider 应优先
    /// </summary>
    int Order { get; }

    /// <summary>
    /// 检查是否授予权限
    /// </summary>
    /// <param name="context">权限检查上下文</param>
    /// <returns>
    /// Granted = 授予
    /// Denied = 显式拒绝
    /// Undefined = 该 Provider 不处理，交给下一个
    /// </returns>
    Task<PermissionValueProviderResult> CheckAsync(PermissionCheckContext context);
}

/// <summary>
/// Provider 返回结果
/// </summary>
public enum PermissionValueProviderResult
{
    /// <summary>
    /// 该 Provider 不处理（没有相关授权记录）
    /// </summary>
    Undefined = 0,

    /// <summary>
    /// 授予
    /// </summary>
    Granted = 1,

    /// <summary>
    /// 显式拒绝（优先级最高）
    /// </summary>
    Denied = 2,
}

/// <summary>
/// 权限检查上下文
/// 包含做出权限决定所需的所有信息
/// 为 ABAC 预留了 Properties
/// </summary>
public class PermissionCheckContext
{
    /// <summary>
    /// 应用编码
    /// </summary>
    public string AppCode { get; set; } = default!;

    /// <summary>
    /// 用户ID（来自 Token 的 sub）
    /// </summary>
    public Guid UserId { get; set; }

    /// <summary>
    /// 权限名称
    /// </summary>
    public string PermissionName { get; set; } = default!;

    /// <summary>
    /// 用户的角色列表（由 RoleProvider 填充）
    /// </summary>
    public string[] Roles { get; set; } = [];

    /// <summary>
    /// 额外属性（ABAC 预留）
    /// 例: { "department": "IT", "age": 30, "ip": "192.168.1.1" }
    /// </summary>
    public Dictionary<string, object> Properties { get; set; } = [];

    /// <summary>
    /// 用户 Claims（来自 Token）
    /// </summary>
    public ClaimsPrincipal? Principal { get; set; }
}
```

```csharp
// ============================================
// Provider 1: 用户直接授权检查（最高优先级）
// ============================================

namespace Perm.Application.Authorization.Providers;

public class UserPermissionValueProvider : IPermissionValueProvider
{
    private readonly PermDbContext _db;

    public string Name => PermissionProviderNames.User;
    public int Order => 10; // 最先检查，因为可能有显式拒绝

    public UserPermissionValueProvider(PermDbContext db) => _db = db;

    public async Task<PermissionValueProviderResult> CheckAsync(
        PermissionCheckContext context)
    {
        // 查用户是否被直接授权/拒绝
        var grant = await _db.PermissionGrants
            .Where(x =>
                x.App.Code == context.AppCode &&
                x.ProviderName == Name &&
                x.ProviderKey == context.UserId.ToString() &&
                x.PermissionName == context.PermissionName)
            .FirstOrDefaultAsync();

        if (grant == null)
            return PermissionValueProviderResult.Undefined;

        // 显式拒绝优先
        return grant.IsGranted
            ? PermissionValueProviderResult.Granted
            : PermissionValueProviderResult.Denied;
    }
}
```

```csharp
// ============================================
// Provider 2: 角色授权检查
// ============================================

public class RolePermissionValueProvider : IPermissionValueProvider
{
    private readonly PermDbContext _db;

    public string Name => PermissionProviderNames.Role;
    public int Order => 20;

    public RolePermissionValueProvider(PermDbContext db) => _db = db;

    public async Task<PermissionValueProviderResult> CheckAsync(
        PermissionCheckContext context)
    {
        if (context.Roles.Length == 0)
            return PermissionValueProviderResult.Undefined;

        var granted = await _db.PermissionGrants
            .AnyAsync(x =>
                x.App.Code == context.AppCode &&
                x.ProviderName == Name &&
                context.Roles.Contains(x.ProviderKey) &&
                x.PermissionName == context.PermissionName &&
                x.IsGranted);

        return granted
            ? PermissionValueProviderResult.Granted
            : PermissionValueProviderResult.Undefined;
    }
}
```

```csharp
// ============================================
// Provider 3: 客户端授权检查
// ============================================

public class ClientPermissionValueProvider : IPermissionValueProvider
{
    private readonly PermDbContext _db;

    public string Name => PermissionProviderNames.Client;
    public int Order => 30;

    public ClientPermissionValueProvider(PermDbContext db) => _db = db;

    public async Task<PermissionValueProviderResult> CheckAsync(
        PermissionCheckContext context)
    {
        var clientId = context.Principal?
            .FindFirst("client_id")?.Value;

        if (string.IsNullOrEmpty(clientId))
            return PermissionValueProviderResult.Undefined;

        var granted = await _db.PermissionGrants
            .AnyAsync(x =>
                x.App.Code == context.AppCode &&
                x.ProviderName == Name &&
                x.ProviderKey == clientId &&
                x.PermissionName == context.PermissionName &&
                x.IsGranted);

        return granted
            ? PermissionValueProviderResult.Granted
            : PermissionValueProviderResult.Undefined;
    }
}
```

```csharp
// ============================================
// ABAC Provider（预留骨架，未来实现）
// ============================================

public class AttributePermissionValueProvider : IPermissionValueProvider
{
    private readonly PermDbContext _db;

    public string Name => PermissionProviderNames.Attribute;
    public int Order => 40;

    public AttributePermissionValueProvider(PermDbContext db) => _db = db;

    public async Task<PermissionValueProviderResult> CheckAsync(
        PermissionCheckContext context)
    {
        // 未来实现:
        // 1. 从 PermissionGrant 中取 ProviderName="A" 的记录
        // 2. ProviderKey 是 JSON 条件: {"department":"IT","level":">=3"}
        // 3. 用规则引擎匹配 context.Properties
        // 4. 匹配成功 → Granted

        // 现在先返回 Undefined
        return PermissionValueProviderResult.Undefined;
    }
}
```

---

### **PermissionChecker — 核心编排器**

```csharp
// ============================================
// Perm.Application/Authorization/PermissionChecker.cs
// 权限检查器（核心）
// ============================================

namespace Perm.Application.Authorization;

/// <summary>
/// 权限检查器
/// 
/// 遍历所有 IPermissionValueProvider
/// 执行优先级逻辑:
///   Denied > Granted > Undefined
///   
/// 如果所有 Provider 都返回 Undefined → 拒绝
/// </summary>
public class PermissionChecker : IPermissionChecker
{
    private readonly IEnumerable<IPermissionValueProvider> _providers;
    private readonly PermDbContext _db;
    private readonly IDistributedCache _cache;
    private readonly ILogger<PermissionChecker> _logger;

    public PermissionChecker(
        IEnumerable<IPermissionValueProvider> providers,
        PermDbContext db,
        IDistributedCache cache,
        ILogger<PermissionChecker> logger)
    {
        _providers = providers.OrderBy(p => p.Order);
        _db = db;
        _cache = cache;
        _logger = logger;
    }

    /// <summary>
    /// 检查单个权限
    /// </summary>
    public async Task<bool> IsGrantedAsync(PermissionCheckContext context)
    {
        // 填充用户角色
        if (context.Roles.Length == 0)
        {
            context.Roles = await GetUserRolesAsync(
                context.UserId, context.AppCode);
        }

        // 遍历所有 Provider
        foreach (var provider in _providers)
        {
            var result = await provider.CheckAsync(context);

            switch (result)
            {
                case PermissionValueProviderResult.Denied:
                    // 显式拒绝，立即返回
                    _logger.LogDebug(
                        "Permission {Permission} denied by {Provider} for user {User}",
                        context.PermissionName, provider.Name, context.UserId);
                    return false;

                case PermissionValueProviderResult.Granted:
                    // 授予，立即返回
                    _logger.LogDebug(
                        "Permission {Permission} granted by {Provider} for user {User}",
                        context.PermissionName, provider.Name, context.UserId);
                    return true;

                case PermissionValueProviderResult.Undefined:
                    // 该 Provider 不处理，继续下一个
                    continue;
            }
        }

        // 所有 Provider 都 Undefined → 拒绝
        _logger.LogDebug(
            "Permission {Permission} not granted for user {User} (no provider matched)",
            context.PermissionName, context.UserId);
        return false;
    }

    /// <summary>
    /// 批量检查权限
    /// </summary>
    public async Task<Dictionary<string, bool>> IsGrantedAsync(
        Guid userId, string appCode, string[] permissionNames)
    {
        var roles = await GetUserRolesAsync(userId, appCode);
        var result = new Dictionary<string, bool>();

        foreach (var permName in permissionNames)
        {
            var context = new PermissionCheckContext
            {
                AppCode = appCode,
                UserId = userId,
                PermissionName = permName,
                Roles = roles,
            };

            result[permName] = await IsGrantedAsync(context);
        }

        return result;
    }

    /// <summary>
    /// 获取用户所有已授权的权限
    /// </summary>
    public async Task<List<string>> GetGrantedPermissionsAsync(
        Guid userId, string appCode)
    {
        var cacheKey = $"perm:granted:{appCode}:{userId}";

        var cached = await _cache.GetStringAsync(cacheKey);
        if (cached != null)
            return JsonSerializer.Deserialize<List<string>>(cached) ?? [];

        var roles = await GetUserRolesAsync(userId, appCode);

        // 获取该应用所有权限定义
        var allPermissions = await _db.PermissionDefinitions
            .Include(x => x.Group)
            .Where(x => x.Group.App.Code == appCode && x.IsEnabled)
            .Select(x => x.Name)
            .ToListAsync();

        var granted = new List<string>();

        foreach (var permName in allPermissions)
        {
            var context = new PermissionCheckContext
            {
                AppCode = appCode,
                UserId = userId,
                PermissionName = permName,
                Roles = roles,
            };

            if (await IsGrantedAsync(context))
            {
                granted.Add(permName);
            }
        }

        // 缓存 5 分钟
        await _cache.SetStringAsync(cacheKey,
            JsonSerializer.Serialize(granted),
            new DistributedCacheEntryOptions
            {
                AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5)
            });

        return granted;
    }

    /// <summary>
    /// 获取用户在某应用的角色
    /// </summary>
    private async Task<string[]> GetUserRolesAsync(Guid userId, string appCode)
    {
        var cacheKey = $"perm:roles:{appCode}:{userId}";

        var cached = await _cache.GetStringAsync(cacheKey);
        if (cached != null)
            return JsonSerializer.Deserialize<string[]>(cached) ?? [];

        var roles = await _db.UserRoles
            .Include(x => x.Role)
            .Where(x =>
                x.UserId == userId &&
                x.App.Code == appCode &&
                x.Role.IsEnabled)
            .Select(x => x.Role.Code)
            .ToArrayAsync();

        await _cache.SetStringAsync(cacheKey,
            JsonSerializer.Serialize(roles),
            new DistributedCacheEntryOptions
            {
                AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(10)
            });

        return roles;
    }
}

public interface IPermissionChecker
{
    Task<bool> IsGrantedAsync(PermissionCheckContext context);
    Task<Dictionary<string, bool>> IsGrantedAsync(
        Guid userId, string appCode, string[] permissionNames);
    Task<List<string>> GetGrantedPermissionsAsync(Guid userId, string appCode);
}
```

---

## 权限判定流程

```
请求: 张三在 ERP 是否有 Purchase.Approve 权限

PermissionChecker.IsGrantedAsync:

  Step 1: 获取张三在 ERP 的角色 → ["manager", "purchaser"]

  Step 2: 遍历 Provider（按 Order 排序）

    Provider 1: UserPermissionValueProvider (Order=10)
      → 查 PermissionGrant: ProviderName="U", ProviderKey="张三guid", Perm="Purchase.Approve"
      → 没有记录 → Undefined
      → 继续

    Provider 2: RolePermissionValueProvider (Order=20)
      → 查 PermissionGrant: ProviderName="R", ProviderKey in ["manager","purchaser"], Perm="Purchase.Approve"
      → 找到: R + manager + Purchase.Approve + IsGranted=true
      → Granted ✅
      → 立即返回 true

  结果: true（张三可以审批采购单）


另一个例子: 张三在 ERP 是否有 Purchase.Delete 权限

  Provider 1: UserPermissionValueProvider
    → 查到: U + 张三guid + Purchase.Delete + IsGranted=false
    → Denied ❌
    → 立即返回 false

  结果: false（张三被显式拒绝删除权限）
  即使 manager 角色有 Delete 权限也没用（显式拒绝优先）
```

---

## API Controllers

```csharp
// ============================================
// 权限查询 API（给业务系统和前端调用）
// ============================================

[ApiController]
[Route("api/permissions")]
[Authorize] // SSO Token 认证
public class PermissionController : ControllerBase
{
    private readonly IPermissionChecker _checker;
    private readonly PermDbContext _db;

    public PermissionController(
        IPermissionChecker checker, PermDbContext db)
    {
        _checker = checker;
        _db = db;
    }

    /// <summary>
    /// 检查权限
    /// GET /api/permissions/check?appCode=erp&permission=Purchase.Approve
    /// </summary>
    [HttpGet("check")]
    public async Task<IActionResult> Check(
        [FromQuery] string appCode,
        [FromQuery] string permission)
    {
        var userId = GetUserId();
        var context = new PermissionCheckContext
        {
            AppCode = appCode,
            UserId = userId,
            PermissionName = permission,
            Principal = User,
        };

        var granted = await _checker.IsGrantedAsync(context);
        return Ok(ApiResult.Ok(new { granted }));
    }

    /// <summary>
    /// 获取当前用户在某应用的所有权限
    /// GET /api/permissions/granted?appCode=erp
    /// 前端控制按钮/菜单显隐用
    /// </summary>
    [HttpGet("granted")]
    public async Task<IActionResult> GetGranted([FromQuery] string appCode)
    {
        var userId = GetUserId();
        var permissions = await _checker.GetGrantedPermissionsAsync(userId, appCode);
        return Ok(ApiResult.Ok(permissions));
    }

    /// <summary>
    /// 批量检查权限
    /// POST /api/permissions/check-batch
    /// Body: { "appCode": "erp", "permissions": ["Purchase.Create", "Purchase.Approve"] }
    /// </summary>
    [HttpPost("check-batch")]
    public async Task<IActionResult> CheckBatch([FromBody] BatchCheckRequest req)
    {
        var userId = GetUserId();
        var result = await _checker.IsGrantedAsync(
            userId, req.AppCode, req.Permissions);
        return Ok(ApiResult.Ok(result));
    }

    /// <summary>
    /// 获取权限定义树（管理页面用）
    /// GET /api/permissions/definitions?appCode=erp
    /// </summary>
    [HttpGet("definitions")]
    public async Task<IActionResult> GetDefinitions([FromQuery] string appCode)
    {
        var groups = await _db.PermissionGroups
            .Include(g => g.Permissions.Where(p => p.IsEnabled))
                .ThenInclude(p => p.Children)
            .Where(g => g.App.Code == appCode)
            .OrderBy(g => g.Sort)
            .ToListAsync();

        return Ok(ApiResult.Ok(groups));
    }

    private Guid GetUserId()
    {
        var sub = User.FindFirst("sub")?.Value
            ?? throw new UnauthorizedAccessException();
        return Guid.Parse(sub);
    }
}
```

```csharp
// ============================================
// 角色管理 API
// ============================================

[ApiController]
[Route("api/roles")]
[Authorize] // 需要权限中心自身的管理权限
public class RoleController : ControllerBase
{
    private readonly RoleService _service;

    public RoleController(RoleService service) => _service = service;

    /// <summary>
    /// 获取应用的角色列表
    /// GET /api/roles?appCode=erp
    /// </summary>
    [HttpGet]
    public async Task<IActionResult> GetList([FromQuery] string appCode)
    {
        var roles = await _service.GetListAsync(appCode);
        return Ok(ApiResult.Ok(roles));
    }

    /// <summary>
    /// 创建角色
    /// POST /api/roles
    /// </summary>
    [HttpPost]
    public async Task<IActionResult> Create([FromBody] CreateRoleRequest req)
    {
        var role = await _service.CreateAsync(req);
        return Ok(ApiResult.Ok(role));
    }

    /// <summary>
    /// 设置角色权限
    /// PUT /api/roles/{roleId}/permissions
    /// Body: { "permissions": ["ERP.Purchase.Create", "ERP.Purchase.Approve"] }
    /// </summary>
    [HttpPut("{roleId:guid}/permissions")]
    public async Task<IActionResult> SetPermissions(
        Guid roleId, [FromBody] SetRolePermissionsRequest req)
    {
        await _service.SetPermissionsAsync(roleId, req.Permissions);
        return Ok(ApiResult.Ok("设置成功"));
    }

    /// <summary>
    /// 获取角色的权限
    /// GET /api/roles/{roleId}/permissions
    /// </summary>
    [HttpGet("{roleId:guid}/permissions")]
    public async Task<IActionResult> GetPermissions(Guid roleId)
    {
        var perms = await _service.GetPermissionsAsync(roleId);
        return Ok(ApiResult.Ok(perms));
    }
}
```

```csharp
// ============================================
// 用户角色分配 API
// ============================================

[ApiController]
[Route("api/user-roles")]
[Authorize]
public class UserRoleController : ControllerBase
{
    private readonly UserRoleService _service;

    public UserRoleController(UserRoleService service) => _service = service;

    /// <summary>
    /// 给用户分配角色
    /// PUT /api/user-roles
    /// Body: { "userId": "guid", "appCode": "erp", "roleCodes": ["manager", "purchaser"] }
    /// </summary>
    [HttpPut]
    public async Task<IActionResult> Assign([FromBody] AssignUserRolesRequest req)
    {
        await _service.AssignAsync(req);
        return Ok(ApiResult.Ok("分配成功"));
    }

    /// <summary>
    /// 查看用户在所有应用的角色
    /// GET /api/user-roles/{userId}
    /// </summary>
    [HttpGet("{userId:guid}")]
    public async Task<IActionResult> GetUserRoles(Guid userId)
    {
        var roles = await _service.GetUserRolesAsync(userId);
        return Ok(ApiResult.Ok(roles));
    }

    /// <summary>
    /// 查看应用下所有用户及角色
    /// GET /api/user-roles/by-app?appCode=erp
    /// </summary>
    [HttpGet("by-app")]
    public async Task<IActionResult> GetByApp([FromQuery] string appCode)
    {
        var users = await _service.GetAppUsersAsync(appCode);
        return Ok(ApiResult.Ok(users));
    }
}
```

```csharp
// ============================================
// 菜单 API
// ============================================

[ApiController]
[Route("api/menus")]
[Authorize]
public class MenuController : ControllerBase
{
    private readonly MenuService _service;

    public MenuController(MenuService service) => _service = service;

    /// <summary>
    /// 获取当前用户在某应用的菜单树
    /// GET /api/menus/current?appCode=erp
    /// 前端渲染侧边栏用
    /// </summary>
    [HttpGet("current")]
    public async Task<IActionResult> GetCurrentUserMenus(
        [FromQuery] string appCode)
    {
        var userId = GetUserId();
        var menus = await _service.GetUserMenuTreeAsync(userId, appCode);
        return Ok(ApiResult.Ok(menus));
    }

    /// <summary>
    /// 获取应用完整菜单树（管理用）
    /// GET /api/menus/tree?appCode=erp
    /// </summary>
    [HttpGet("tree")]
    public async Task<IActionResult> GetTree([FromQuery] string appCode)
    {
        var tree = await _service.GetFullTreeAsync(appCode);
        return Ok(ApiResult.Ok(tree));
    }

    // CRUD ...

    private Guid GetUserId()
    {
        var sub = User.FindFirst("sub")?.Value
            ?? throw new UnauthorizedAccessException();
        return Guid.Parse(sub);
    }
}
```

---

## 业务系统接入 SDK

```csharp
// ============================================
// Perm.Client/PermissionClient.cs
// 业务系统引用这个 NuGet 包即可接入权限中心
// ============================================

namespace Perm.Client;

/// <summary>
/// 权限中心 HTTP 客户端
/// </summary>
public class PermissionClient : IPermissionClient
{
    private readonly HttpClient _http;
    private readonly IDistributedCache _cache;
    private readonly PermClientOptions _options;

    public PermissionClient(
        HttpClient http,
        IDistributedCache cache,
        IOptions<PermClientOptions> options)
    {
        _http = http;
        _cache = cache;
        _options = options.Value;
    }

    /// <summary>
    /// 检查权限
    /// </summary>
    public async Task<bool> IsGrantedAsync(
        string token, string permission)
    {
        // 先查本地缓存
        var cacheKey = $"perm:{_options.AppCode}:{HashToken(token)}";
        var cached = await _cache.GetStringAsync(cacheKey);

        List<string>? grantedPerms;

        if (cached != null)
        {
            grantedPerms = JsonSerializer.Deserialize<List<string>>(cached);
        }
        else
        {
            // 请求权限中心
            var request = new HttpRequestMessage(
                HttpMethod.Get,
                $"/api/permissions/granted?appCode={_options.AppCode}");
            request.Headers.Authorization =
                new AuthenticationHeaderValue("Bearer", token);

            var response = await _http.SendAsync(request);
            response.EnsureSuccessStatusCode();

            var result = await response.Content
                .ReadFromJsonAsync<ApiResult<List<string>>>();
            grantedPerms = result?.Data ?? [];

            // 缓存 5 分钟
            await _cache.SetStringAsync(cacheKey,
                JsonSerializer.Serialize(grantedPerms),
                new DistributedCacheEntryOptions
                {
                    AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(5)
                });
        }

        return grantedPerms?.Contains(permission) ?? false;
    }

    /// <summary>
    /// 获取所有权限（前端用）
    /// </summary>
    public async Task<List<string>> GetGrantedPermissionsAsync(string token)
    {
        var request = new HttpRequestMessage(
            HttpMethod.Get,
            $"/api/permissions/granted?appCode={_options.AppCode}");
        request.Headers.Authorization =
            new AuthenticationHeaderValue("Bearer", token);

        var response = await _http.SendAsync(request);
        response.EnsureSuccessStatusCode();

        var result = await response.Content
            .ReadFromJsonAsync<ApiResult<List<string>>>();
        return result?.Data ?? [];
    }

    /// <summary>
    /// 获取用户菜单树
    /// </summary>
    public async Task<List<MenuDto>> GetMenusAsync(string token)
    {
        var request = new HttpRequestMessage(
            HttpMethod.Get,
            $"/api/menus/current?appCode={_options.AppCode}");
        request.Headers.Authorization =
            new AuthenticationHeaderValue("Bearer", token);

        var response = await _http.SendAsync(request);
        response.EnsureSuccessStatusCode();

        var result = await response.Content
            .ReadFromJsonAsync<ApiResult<List<MenuDto>>>();
        return result?.Data ?? [];
    }

    private static string HashToken(string token)
    {
        // 用 token 的后16位做缓存key（避免完整token做key太长）
        return token.Length > 16
            ? token[^16..]
            : token;
    }
}

public class PermClientOptions
{
    /// <summary>
    /// 当前应用编码
    /// </summary>
    public string AppCode { get; set; } = default!;

    /// <summary>
    /// 权限中心地址
    /// </summary>
    public string BaseUrl { get; set; } = default!;

    /// <summary>
    /// 缓存过期时间（分钟）
    /// </summary>
    public int CacheMinutes { get; set; } = 5;
}
```

```csharp
// ============================================
// Perm.Client/RequirePermissionAttribute.cs
// 声明式权限特性（业务系统用）
// ============================================

namespace Perm.Client;

[AttributeUsage(
    AttributeTargets.Class | AttributeTargets.Method,
    AllowMultiple = true)]
public class RequirePermissionAttribute : Attribute
{
    public string Permission { get; }

    /// <summary>
    /// 多个权限时: true=全部满足, false=满足一个即可
    /// </summary>
    public bool RequireAll { get; set; } = true;

    public RequirePermissionAttribute(string permission)
    {
        Permission = permission;
    }
}
```

```csharp
// ============================================
// Perm.Client/PermissionAuthorizationFilter.cs
// 全局权限校验 Filter
// ============================================

namespace Perm.Client;

public class PermissionAuthorizationFilter : IAsyncAuthorizationFilter
{
    private readonly IPermissionClient _client;

    public PermissionAuthorizationFilter(IPermissionClient client)
        => _client = client;

    public async Task OnAuthorizationAsync(AuthorizationFilterContext context)
    {
        var attrs = context.ActionDescriptor.EndpointMetadata
            .OfType<RequirePermissionAttribute>()
            .ToList();

        if (attrs.Count == 0) return;

        var user = context.HttpContext.User;
        if (!user.Identity?.IsAuthenticated ?? true)
        {
            context.Result = new UnauthorizedResult();
            return;
        }

        // 从请求头拿 Token
        var token = context.HttpContext.Request.Headers
            .Authorization.ToString().Replace("Bearer ", "");

        if (string.IsNullOrEmpty(token))
        {
            context.Result = new UnauthorizedResult();
            return;
        }

        foreach (var attr in attrs)
        {
            var granted = await _client.IsGrantedAsync(token, attr.Permission);
            if (!granted)
            {
                context.Result = new JsonResult(new
                {
                    code = 403,
                    message = $"没有权限: {attr.Permission}"
                })
                { StatusCode = 403 };
                return;
            }
        }
    }
}
```

```csharp
// ============================================
// Perm.Client/ServiceCollectionExtensions.cs
// 一行代码接入权限中心
// ============================================

namespace Perm.Client;

public static class ServiceCollectionExtensions
{
    /// <summary>
    /// 接入权限中心
    /// 
    /// 使用方式:
    /// builder.Services.AddPermissionCenter(options => {
    ///     options.AppCode = "erp";
    ///     options.BaseUrl = "https://perm.example.com";
    /// });
    /// </summary>
    public static IServiceCollection AddPermissionCenter(
        this IServiceCollection services,
        Action<PermClientOptions> configure)
    {
        var options = new PermClientOptions();
        configure(options);

        services.Configure(configure);
        services.AddSingleton<IPermissionClient, PermissionClient>();

        services.AddHttpClient<IPermissionClient, PermissionClient>(client =>
        {
            client.BaseAddress = new Uri(options.BaseUrl);
            client.Timeout = TimeSpan.FromSeconds(5);
        });

        // 注册全局 Filter
        services.AddControllers(opts =>
        {
            opts.Filters.Add<PermissionAuthorizationFilter>();
        });

        return services;
    }
}
```

---

## 业务系统接入示例

```csharp
// ============================================
// ERP 系统 Program.cs — 3 行搞定接入
// ============================================

var builder = WebApplication.CreateBuilder(args);

// 1. 接入 SSO 认证
builder.Services.AddAuthentication("Bearer")
    .AddJwtBearer("Bearer", options =>
    {
        options.Authority = "https://sso.example.com";
        options.Audience = "erp";
    });

// 2. 接入权限中心 ← 就这一行
builder.Services.AddPermissionCenter(options =>
{
    options.AppCode = "erp";
    options.BaseUrl = "https://perm.example.com";
});

// 3. 正常写 Controller
var app = builder.Build();
app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();
app.Run();
```

```csharp
// ============================================
// ERP 系统 Controller — 和 ABP 一样的使用体验
// ============================================

[ApiController]
[Route("api/purchase")]
[Authorize]
public class PurchaseController : ControllerBase
{
    [HttpGet]
    [RequirePermission("ERP.Purchase")]
    public async Task<IActionResult> GetList() { ... }

    [HttpPost]
    [RequirePermission("ERP.Purchase.Create")]
    public async Task<IActionResult> Create() { ... }

    [HttpPost("{id}/approve")]
    [RequirePermission("ERP.Purchase.Approve")]
    public async Task<IActionResult> Approve(Guid id) { ... }

    [HttpDelete("{id}")]
    [RequirePermission("ERP.Purchase.Delete")]
    public async Task<IActionResult> Delete(Guid id) { ... }
}

// 和 ABP 的 [Authorize(Permissions.Purchase.Create)] 体验完全一致 ✅
```

---

## EF 配置

```csharp
// ============================================
// Perm.Infrastructure/Data/PermDbContext.cs
// ============================================

namespace Perm.Infrastructure.Data;

public class PermDbContext : DbContext
{
    public PermDbContext(DbContextOptions<PermDbContext> options)
        : base(options) { }

    public DbSet<App> Apps => Set<App>();
    public DbSet<PermissionGroup> PermissionGroups => Set<PermissionGroup>();
    public DbSet<PermissionDefinition> PermissionDefinitions => Set<PermissionDefinition>();
    public DbSet<Role> Roles => Set<Role>();
    public DbSet<PermissionGrant> PermissionGrants => Set<PermissionGrant>();
    public DbSet<UserRole> UserRoles => Set<UserRole>();
    public DbSet<Menu> Menus => Set<Menu>();

    protected override void OnModelCreating(ModelBuilder builder)
    {
        base.OnModelCreating(builder);
        builder.ApplyConfigurationsFromAssembly(typeof(PermDbContext).Assembly);
    }
}
```

```csharp
// ============================================
// EF 配置（核心表）
// ============================================

public class PermissionGrantConfiguration
    : IEntityTypeConfiguration<PermissionGrant>
{
    public void Configure(EntityTypeBuilder<PermissionGrant> builder)
    {
        builder.ToTable("PermissionGrants");
        builder.HasKey(x => x.Id);

        builder.Property(x => x.PermissionName)
               .HasMaxLength(200).IsRequired();
        builder.Property(x => x.ProviderName)
               .HasMaxLength(1).IsRequired();
        builder.Property(x => x.ProviderKey)
               .HasMaxLength(200).IsRequired();

        // 核心索引: 查某个 Provider 的某个权限
        builder.HasIndex(x => new
        {
            x.AppId,
            x.ProviderName,
            x.ProviderKey,
            x.PermissionName
        }).IsUnique();

        // 查某个权限的所有授权
        builder.HasIndex(x => new { x.AppId, x.PermissionName });
    }
}

public class UserRoleConfiguration : IEntityTypeConfiguration<UserRole>
{
    public void Configure(EntityTypeBuilder<UserRole> builder)
    {
        builder.ToTable("UserRoles");
        builder.HasKey(x => x.Id);

        builder.HasIndex(x => new { x.AppId, x.UserId, x.RoleId }).IsUnique();
        builder.HasIndex(x => new { x.AppId, x.UserId });
    }
}

public class RoleConfiguration : IEntityTypeConfiguration<Role>
{
    public void Configure(EntityTypeBuilder<Role> builder)
    {
        builder.ToTable("Roles");
        builder.HasKey(x => x.Id);

        builder.Property(x => x.Code).HasMaxLength(100).IsRequired();
        builder.Property(x => x.Name).HasMaxLength(200).IsRequired();

        builder.HasIndex(x => new { x.AppId, x.Code }).IsUnique();
    }
}

public class PermissionDefinitionConfiguration
    : IEntityTypeConfiguration<PermissionDefinition>
{
    public void Configure(EntityTypeBuilder<PermissionDefinition> builder)
    {
        builder.ToTable("PermissionDefinitions");
        builder.HasKey(x => x.Id);

        builder.Property(x => x.Name).HasMaxLength(200).IsRequired();
        builder.Property(x => x.DisplayName).HasMaxLength(200).IsRequired();
        builder.Property(x => x.Properties).HasMaxLength(2000);

        builder.HasIndex(x => x.Name).IsUnique();

        builder.HasOne(x => x.Parent)
               .WithMany(x => x.Children)
               .HasForeignKey(x => x.ParentId)
               .OnDelete(DeleteBehavior.Restrict);
    }
}

public class MenuConfiguration : IEntityTypeConfiguration<Menu>
{
    public void Configure(EntityTypeBuilder<Menu> builder)
    {
        builder.ToTable("Menus");
        builder.HasKey(x => x.Id);

        builder.Property(x => x.Name).HasMaxLength(200).IsRequired();
        builder.Property(x => x.Code).HasMaxLength(100).IsRequired();
        builder.Property(x => x.Path).HasMaxLength(500);
        builder.Property(x => x.Icon).HasMaxLength(100);
        builder.Property(x => x.PermissionName).HasMaxLength(200);

        builder.HasIndex(x => new { x.AppId, x.Code }).IsUnique();

        builder.HasOne(x => x.Parent)
               .WithMany(x => x.Children)
               .HasForeignKey(x => x.ParentId)
               .OnDelete(DeleteBehavior.Restrict);
    }
}

public class AppConfiguration : IEntityTypeConfiguration<App>
{
    public void Configure(EntityTypeBuilder<App> builder)
    {
        builder.ToTable("Apps");
        builder.HasKey(x => x.Id);

        builder.Property(x => x.Code).HasMaxLength(100).IsRequired();
        builder.Property(x => x.Name).HasMaxLength(200).IsRequired();

        builder.HasIndex(x => x.Code).IsUnique();
    }
}

public class PermissionGroupConfiguration
    : IEntityTypeConfiguration<PermissionGroup>
{
    public void Configure(EntityTypeBuilder<PermissionGroup> builder)
    {
        builder.ToTable("PermissionGroups");
        builder.HasKey(x => x.Id);

        builder.Property(x => x.Code).HasMaxLength(100).IsRequired();
        builder.Property(x => x.DisplayName).HasMaxLength(200).IsRequired();

        builder.HasIndex(x => new { x.AppId, x.Code }).IsUnique();
    }
}
```

---

## 缓存失效策略

```csharp
// ============================================
// Perm.Infrastructure/Caching/PermissionCacheManager.cs
// ============================================

namespace Perm.Infrastructure.Caching;

/// <summary>
/// 权限缓存管理
/// 当权限数据变更时清除相关缓存
/// </summary>
public class PermissionCacheManager
{
    private readonly IDistributedCache _cache;
    private readonly ILogger<PermissionCacheManager> _logger;

    public PermissionCacheManager(
        IDistributedCache cache,
        ILogger<PermissionCacheManager> logger)
    {
        _cache = cache;
        _logger = logger;
    }

    /// <summary>
    /// 用户角色变更时清除缓存
    /// </summary>
    public async Task OnUserRoleChangedAsync(Guid userId, string appCode)
    {
        await _cache.RemoveAsync($"perm:roles:{appCode}:{userId}");
        await _cache.RemoveAsync($"perm:granted:{appCode}:{userId}");
        _logger.LogInformation(
            "Cache cleared for user {UserId} in app {App}", userId, appCode);
    }

    /// <summary>
    /// 角色权限变更时清除缓存
    /// 需要清除该角色下所有用户的缓存
    /// </summary>
    public async Task OnRolePermissionChangedAsync(
        Guid roleId, string appCode, PermDbContext db)
    {
        var userIds = await db.UserRoles
            .Where(x => x.RoleId == roleId)
            .Select(x => x.UserId)
            .ToListAsync();

        foreach (var userId in userIds)
        {
            await _cache.RemoveAsync($"perm:granted:{appCode}:{userId}");
        }

        _logger.LogInformation(
            "Cache cleared for role {RoleId} in app {App}, affected {Count} users",
            roleId, appCode, userIds.Count);
    }
}
```

---

## DI 注册

```csharp
// ============================================
// Perm.Infrastructure/DependencyInjection.cs
// ============================================

namespace Perm.Infrastructure;

public static class DependencyInjection
{
    public static IServiceCollection AddPermInfrastructure(
        this IServiceCollection services, IConfiguration configuration)
    {
        // DbContext
        services.AddDbContext<PermDbContext>(options =>
            options.UseNpgsql(configuration.GetConnectionString("Default")));

        // Redis 分布式缓存
        services.AddStackExchangeRedisCache(options =>
        {
            options.Configuration = configuration.GetConnectionString("Redis");
            options.InstanceName = "perm:";
        });

        // 权限 Value Providers（顺序重要）
        services.AddScoped<IPermissionValueProvider, UserPermissionValueProvider>();
        services.AddScoped<IPermissionValueProvider, RolePermissionValueProvider>();
        services.AddScoped<IPermissionValueProvider, ClientPermissionValueProvider>();
        // 未来: services.AddScoped<IPermissionValueProvider, AttributePermissionValueProvider>();

        // 核心服务
        services.AddScoped<IPermissionChecker, PermissionChecker>();
        services.AddScoped<PermissionCacheManager>();

        // 应用服务
        services.AddScoped<RoleService>();
        services.AddScoped<UserRoleService>();
        services.AddScoped<MenuService>();
        services.AddScoped<PermissionGrantService>();

        return services;
    }
}
```

---

## 完整数据流

```
┌──────────────────────────────────────────────────────────────┐
│                    运维管理流程                                │
│                                                              │
│  1. 注册应用                                                 │
│     POST /api/apps                                           │
│     { "code": "erp", "name": "ERP系统" }                    │
│                                                              │
│  2. 定义权限组                                               │
│     POST /api/permissions/groups                             │
│     { "appCode": "erp", "code": "Purchase", "name": "采购" }│
│                                                              │
│  3. 定义权限点                                               │
│     POST /api/permissions/definitions                        │
│     { "groupCode": "Purchase",                               │
│       "name": "ERP.Purchase.Create",                         │
│       "displayName": "创建采购单" }                           │
│                                                              │
│  4. 创建角色                                                 │
│     POST /api/roles                                          │
│     { "appCode": "erp", "code": "manager", "name": "经理" } │
│                                                              │
│  5. 设置角色权限                                             │
│     PUT /api/roles/{id}/permissions                          │
│     { "permissions": ["ERP.Purchase.*"] }                    │
│                                                              │
│  6. 给用户分配角色                                           │
│     PUT /api/user-roles                                      │
│     { "userId": "guid", "appCode": "erp",                   │
│       "roleCodes": ["manager"] }                             │
│                                                              │
│  7. 配置菜单                                                 │
│     POST /api/menus                                          │
│     { "appCode": "erp", "name": "采购管理",                  │
│       "path": "/purchase",                                   │
│       "permissionName": "ERP.Purchase" }                     │
│                                                              │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                    运行时请求流程                              │
│                                                              │
│  张三打开 ERP:                                               │
│                                                              │
│  1. 登录 → SSO → Token { sub: "张三guid", name: "张三" }    │
│                                                              │
│  2. 前端拉权限:                                              │
│     GET perm.api/permissions/granted?appCode=erp             │
│     Authorization: Bearer {token}                            │
│     → ["ERP.Purchase", "ERP.Purchase.Create",                │
│         "ERP.Purchase.Approve", "ERP.Stock.View"]            │
│                                                              │
│  3. 前端拉菜单:                                              │
│     GET perm.api/menus/current?appCode=erp                   │
│     → 根据权限过滤后的菜单树                                 │
│                                                              │
│  4. 前端渲染:                                                │
│     采购管理 ✅（有 ERP.Purchase）                            │
│       创建按钮 ✅（有 ERP.Purchase.Create）                   │
│       删除按钮 ❌（没有 ERP.Purchase.Delete）                 │
│     库存管理:                                                │
│       查看 ✅（有 ERP.Stock.View）                            │
│       调拨 ❌（没有 ERP.Stock.Transfer）                      │
│                                                              │
│  5. 后端请求:                                                │
│     POST erp.api/purchase                                    │
│     → PermissionFilter → 问权限中心（或本地缓存）             │
│     → ERP.Purchase.Create → granted → 放行 ✅                │
│                                                              │
│     DELETE erp.api/purchase/123                               │
│     → ERP.Purchase.Delete → denied → 403 ❌                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## ABAC 演进路径

```
现在: RBAC
  PermissionGrant: ProviderName="R", ProviderKey="manager"
  → 角色有什么权限

未来: 加 ABAC（无需改现有代码）

  Step 1: 实现 AttributePermissionValueProvider
  
  Step 2: PermissionGrant 加记录:
    ProviderName = "A"
    ProviderKey = '{"department":"IT","level":">=3"}'
    PermissionName = "ERP.Server.Restart"
    → 意思: IT部门且级别>=3的人可以重启服务器
    
  Step 3: 业务系统传属性:
    PermissionCheckContext.Properties = {
      "department": "IT",
      "level": 5,
      "time": "2024-01-01 10:00"
    }
    
  Step 4: AttributePermissionValueProvider 匹配规则

  现有 RBAC 不受任何影响 ✅
  Provider 模式天然支持扩展 ✅
```

```
ABAC 规则引擎（未来）:

简单版: JSON 条件匹配
  {"department": "IT", "level": ">=3"}
  
中级版: 表达式引擎
  "user.department == 'IT' && user.level >= 3 && time.hour >= 9 && time.hour <= 18"
  
高级版: 接入规则引擎
  NRules / RulesEngine
  支持复杂的业务规则
```

---

## 数据库 ER 图

```
┌───────────┐     ┌──────────────────┐     ┌────────────────────────┐
│   Apps    │────►│ PermissionGroups │────►│ PermissionDefinitions  │
│           │     │                  │     │  (树形, ParentId)       │
│ code      │     │ code             │     │  name                  │
│ name      │     │ displayName      │     │  displayName           │
│           │     │                  │     │  type                  │
│           │     │                  │     │  properties (ABAC预留) │
└─────┬─────┘     └──────────────────┘     └────────────────────────┘
      │
      │
      ├──────────────────────────────┐
      │                              │
      ▼                              ▼
┌───────────┐                 ┌───────────────────┐
│   Roles   │                 │ PermissionGrants  │ ← ABP精髓
│           │                 │                   │
│ code      │                 │ permissionName    │
│ name      │                 │ providerName (R/U/C/O/A)
│ isDefault │                 │ providerKey       │
│ isStatic  │                 │ isGranted         │
└─────┬─────┘                 └───────────────────┘
      │
      ▼
┌───────────┐                 ┌───────────────────┐
│ UserRoles │                 │      Menus        │
│           │                 │  (树形, ParentId)  │
│ userId    │                 │  name             │
│ roleId    │                 │  path             │
│ appId     │                 │  permissionName   │
└───────────┘                 └───────────────────┘

共 7 张表，完整的权限中心 ✅
```

---

## 文件清单

```
需要创建的文件:

Perm.Domain/ (11 文件)
  ├── Abstractions/
  │   ├── Entity.cs
  │   ├── FullAuditedEntity.cs
  │   ├── AuditedEntity.cs
  │   └── CreationAuditedEntity.cs
  ├── Entities/
  │   ├── App.cs
  │   ├── PermissionGroup.cs
  │   ├── PermissionDefinition.cs
  │   ├── Role.cs
  │   ├── PermissionGrant.cs        ← 核心
  │   ├── UserRole.cs
  │   └── Menu.cs
  └── Enums/
      ├── PermissionType.cs
      └── MenuType.cs

Perm.Application/ (13 文件)
  ├── Authorization/
  │   ├── PermissionProviderNames.cs
  │   ├── IPermissionValueProvider.cs
  │   ├── PermissionCheckContext.cs
  │   ├── IPermissionChecker.cs
  │   ├── PermissionChecker.cs      ← 核心
  │   └── Providers/
  │       ├── UserPermissionValueProvider.cs
  │       ├── RolePermissionValueProvider.cs
  │       ├── ClientPermissionValueProvider.cs
  │       └── AttributePermissionValueProvider.cs (骨架)
  ├── Services/
  │   ├── RoleService.cs
  │   ├── UserRoleService.cs
  │   ├── PermissionGrantService.cs
  │   └── MenuService.cs
  └── Contracts/ (DTOs)

Perm.Infrastructure/ (10 文件)
  ├── Data/
  │   ├── PermDbContext.cs
  │   └── Configurations/ (7个配置文件)
  ├── Caching/
  │   └── PermissionCacheManager.cs
  └── DependencyInjection.cs

Perm.Host/ (6 文件)
  ├── Controllers/
  │   ├── AppController.cs
  │   ├── PermissionController.cs
  │   ├── RoleController.cs
  │   ├── UserRoleController.cs
  │   └── MenuController.cs
  └── Program.cs

Perm.Client/ (SDK, 5 文件)
  ├── IPermissionClient.cs
  ├── PermissionClient.cs
  ├── PermClientOptions.cs
  ├── RequirePermissionAttribute.cs
  ├── PermissionAuthorizationFilter.cs
  └── ServiceCollectionExtensions.cs

总计: ~45 文件
```

这就是完整的独立权限中心设计。核心亮点:

- **PermissionGrant 的 Provider 模式**（ABP 精髓）→ 一张表存所有类型授权
- **IPermissionValueProvider 策略模式** → 加新的授权类型只需加 Provider
- **ABAC 预留** → Properties 字段 + AttributeProvider 骨架
- **SDK** → 业务系统一行代码接入

要开始按模块写详细代码吗？从哪个模块开始？