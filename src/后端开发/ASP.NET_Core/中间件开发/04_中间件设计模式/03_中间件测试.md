---
title: 中间件测试
icon: fa6-solid:flask-vial
order: 3
category:
  - ASP.NET Core
  - 中间件开发
tag:
  - 中间件
  - 测试
  - 单元测试
  - 集成测试
---

# 中间件测试

中间件是请求管道的核心组件，任何 bug 都可能影响所有请求。系统化的测试是保障中间件可靠性的关键。本章将介绍从单元测试到集成测试的完整方案。

## 一、为什么要测试中间件

### 1.1 中间件的特殊性

中间件不同于普通的服务类，它具有以下特征：

- **与 HttpContext 紧密耦合**：请求和响应的处理高度依赖 HttpContext 的状态
- **执行顺序敏感**：中间件的注册顺序直接决定行为
- **副作用范围大**：一个中间件的 bug 可能影响整个管道
- **异步执行**：请求在中间件之间异步传递，容易引入竞态条件

### 1.2 常见中间件 Bug

| Bug 类型 | 典型表现 | 严重程度 |
|----------|----------|----------|
| 顺序错误 | 认证中间件在授权之后执行 | 高 — 授权永远失败 |
| 响应体读取后未恢复 | 后续中间件读到空 Body | 高 — 请求处理异常 |
| 异常未处理 | 未 catch 的异常导致 500 | 高 — 用户体验差 |
| Response 已开始仍写入 | `InvalidOperationException` | 中 — 日志刷屏 |
| Header 写入时机错误 | 状态码已发送后写 Header | 中 — 运行时异常 |
| 内存泄漏 | 缓存无限增长 | 低(初期) — 逐渐恶化 |

### 1.3 测试金字塔

```
        ╱  集成测试  ╲          ← 少量，验证中间件在完整管道中的行为
       ╱──────────────╲
      ╱   组件测试      ╲        ← 适量，验证中间件与相邻组件的交互
     ╱──────────────────╲
    ╱     单元测试         ╲     ← 大量，验证中间件的核心逻辑
   ╱────────────────────────╲
```

## 二、单元测试

### 2.1 使用 TestServer

`Microsoft.AspNetCore.TestHost` 提供了在内存中运行 ASP.NET Core 应用的能力，无需真实网络绑定：

```xml
<!-- 项目文件 -->
<ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.TestHost" Version="8.0.0" />
    <PackageReference Include="xunit" Version="2.7.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.5.7" />
    <PackageReference Include="Moq" Version="4.20.70" />
</ItemGroup>
```

### 2.2 创建测试 Host

```csharp
public class RequestTimingMiddlewareTests : IAsyncLifetime
{
    private HttpClient _client = null!;
    private TestServer _server = null!;

    public Task InitializeAsync()
    {
        var builder = new WebHostBuilder()
            .ConfigureServices(services =>
            {
                services.AddSingleton<ILogger<RequestTimingMiddleware>>(
                    LoggerFactory.Create(b => b.AddDebug())
                        .CreateLogger<RequestTimingMiddleware>());
            })
            .Configure(app =>
            {
                app.UseMiddleware<RequestTimingMiddleware>();
                app.Run(async context =>
                {
                    await context.Response.WriteAsync("OK");
                });
            });

        _server = new TestServer(builder);
        _client = _server.CreateClient();
        return Task.CompletedTask;
    }

    public Task DisposeAsync()
    {
        _client.Dispose();
        _server.Dispose();
        return Task.CompletedTask;
    }
}
```

### 2.3 发送请求并验证响应

```csharp
[Fact]
public async Task RequestTiming_AddsTimingHeader()
{
    // Arrange
    // (_client 已在 InitializeAsync 中创建)

    // Act
    var response = await _client.GetAsync("/api/test");

    // Assert
    Assert.True(response.Headers.Contains("X-Response-Time"));
    var timing = response.Headers.GetValues("X-Response-Time").First();
    Assert.True(int.TryParse(timing, out var ms));
    Assert.True(ms >= 0);
}

[Fact]
public async Task RequestTiming_PassesRequestToNext()
{
    // Act
    var response = await _client.GetAsync("/api/test");

    // Assert
    response.EnsureSuccessStatusCode();
    var body = await response.Content.ReadAsStringAsync();
    Assert.Equal("OK", body);
}
```

## 三、测试场景

### 3.1 正常请求传递

验证中间件正确调用 `next` 并传递请求：

```csharp
public class AuthMiddlewareTests
{
    private readonly Mock<RequestDelegate> _mockNext;
    private readonly AuthMiddleware _middleware;

    public AuthMiddlewareTests()
    {
        _mockNext = new Mock<RequestDelegate>();
        _middleware = new AuthMiddleware(_mockNext.Object);
    }

    [Fact]
    public async Task ValidToken_CallsNext()
    {
        // Arrange
        var context = new DefaultHttpContext();
        context.Request.Headers.Authorization = "Bearer valid-token";
        context.Items["TokenValidated"] = true;

        // Act
        await _middleware.InvokeAsync(context, Mock.Of<ITokenService>());

        // Assert
        _mockNext.Verify(next => next(context), Times.Once);
    }

    [Fact]
    public async Task ValidToken_SetsUserPrincipal()
    {
        // Arrange
        var context = new DefaultHttpContext();
        context.Request.Headers.Authorization = "Bearer valid-token";

        var mockTokenService = new Mock<ITokenService>();
        mockTokenService
            .Setup(s => s.ValidateTokenAsync("valid-token"))
            .ReturnsAsync(new ClaimsPrincipal(new ClaimsIdentity(
                new[] { new Claim(ClaimTypes.Name, "testuser") },
                "Bearer")));

        // Act
        await _middleware.InvokeAsync(context, mockTokenService.Object);

        // Assert
        Assert.NotNull(context.User);
        Assert.Equal("testuser", context.User.Identity?.Name);
    }
}
```

### 3.2 请求被短路（不调用 next）

```csharp
[Fact]
public async Task MissingAuthorization_Returns401()
{
    // Arrange
    var context = new DefaultHttpContext();
    context.Response.Body = new MemoryStream();

    // Act
    await _middleware.InvokeAsync(context, Mock.Of<ITokenService>());

    // Assert
    Assert.Equal(401, context.Response.StatusCode);
    _mockNext.Verify(next => next(It.IsAny<HttpContext>()), Times.Never);
}

[Fact]
public async Task InvalidToken_Returns401()
{
    // Arrange
    var context = new DefaultHttpContext();
    context.Request.Headers.Authorization = "Bearer invalid-token";
    context.Response.Body = new MemoryStream();

    var mockTokenService = new Mock<ITokenService>();
    mockTokenService
        .Setup(s => s.ValidateTokenAsync("invalid-token"))
        .ReturnsAsync((ClaimsPrincipal?)null);

    // Act
    await _middleware.InvokeAsync(context, mockTokenService.Object);

    // Assert
    Assert.Equal(401, context.Response.StatusCode);
    _mockNext.Verify(next => next(It.IsAny<HttpContext>()), Times.Never);
}
```

### 3.3 异常处理验证

```csharp
public class ExceptionHandlingMiddlewareTests
{
    private ExceptionHandlingMiddleware CreateMiddleware(
        out Mock<RequestDelegate> mockNext,
        out Mock<ILogger<ExceptionHandlingMiddleware>> mockLogger)
    {
        mockNext = new Mock<RequestDelegate>();
        mockLogger = new Mock<ILogger<ExceptionHandlingMiddleware>>();
        return new ExceptionHandlingMiddleware(mockNext.Object, mockLogger.Object);
    }

    [Fact]
    public async Task NoException_CallsNextAndReturns200()
    {
        // Arrange
        var middleware = CreateMiddleware(out var mockNext, out _);
        var context = new DefaultHttpContext();

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        mockNext.Verify(n => n(context), Times.Once);
        Assert.Equal(200, context.Response.StatusCode);
    }

    [Fact]
    public async Task BusinessException_Returns400()
    {
        // Arrange
        var middleware = CreateMiddleware(out var mockNext, out var mockLogger);
        var context = new DefaultHttpContext();
        context.Response.Body = new MemoryStream();

        mockNext
            .Setup(n => n(context))
            .Throws(new BusinessException("用户名已存在"));

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        Assert.Equal(400, context.Response.StatusCode);
        mockLogger.Verify(
            x => x.Log(
                LogLevel.Warning,
                It.IsAny<EventId>(),
                It.IsAny<It.IsAnyType>(),
                It.IsAny<Exception>(),
                (Func<It.IsAnyType, Exception?, string>)It.IsAny<object>()),
            Times.Once);
    }

    [Fact]
    public async Task UnexpectedException_Returns500()
    {
        // Arrange
        var middleware = CreateMiddleware(out var mockNext, out var mockLogger);
        var context = new DefaultHttpContext();
        context.Response.Body = new MemoryStream();

        mockNext
            .Setup(n => n(context))
            .Throws(new InvalidOperationException("数据库连接失败"));

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        Assert.Equal(500, context.Response.StatusCode);
        mockLogger.Verify(
            x => x.Log(
                LogLevel.Error,
                It.IsAny<EventId>(),
                It.IsAny<It.IsAnyType>(),
                It.IsAny<Exception>(),
                (Func<It.IsAnyType, Exception?, string>)It.IsAny<object>()),
            Times.Once);
    }
}
```

### 3.4 特定 Header 检查

```csharp
public class CorsMiddlewareTests
{
    [Fact]
    public async Task OriginHeader_AddsCorsHeaders()
    {
        // Arrange
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new CorsMiddleware(mockNext.Object);
        var context = new DefaultHttpContext();
        context.Response.Headers = new HeaderDictionary();
        context.Request.Headers.Origin = "https://example.com";

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        Assert.Equal("https://example.com",
            context.Response.Headers["Access-Control-Allow-Origin"]);
        Assert.Equal("GET, POST, PUT, DELETE",
            context.Response.Headers["Access-Control-Allow-Methods"]);
    }

    [Fact]
    public async Task NoOriginHeader_NoCorsHeaders()
    {
        // Arrange
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new CorsMiddleware(mockNext.Object);
        var context = new DefaultHttpContext();

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        Assert.False(
            context.Response.Headers.ContainsKey("Access-Control-Allow-Origin"));
    }
}
```

### 3.5 特定路径跳过

```csharp
public class ApiKeyMiddlewareTests
{
    [Fact]
    public async Task HealthCheckPath_SkipsValidation()
    {
        // Arrange
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new ApiKeyMiddleware(mockNext.Object, new ApiKeyOptions());
        var context = new DefaultHttpContext();
        context.Request.Path = "/health";

        // Act
        await middleware.InvokeAsync(context);

        // Assert — next 被调用，说明跳过了验证
        mockNext.Verify(n => n(context), Times.Once);
    }

    [Fact]
    public async Task SwaggerPath_SkipsValidation()
    {
        // Arrange
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new ApiKeyMiddleware(mockNext.Object, new ApiKeyOptions());
        var context = new DefaultHttpContext();
        context.Request.Path = "/swagger/index.html";

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        mockNext.Verify(n => n(context), Times.Once);
    }

    [Theory]
    [InlineData("/api/users")]
    [InlineData("/api/orders")]
    public async Task ApiPath_RequiresApiKey(string path)
    {
        // Arrange
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new ApiKeyMiddleware(mockNext.Object, new ApiKeyOptions());
        var context = new DefaultHttpContext();
        context.Request.Path = path;
        context.Response.Body = new MemoryStream();

        // Act
        await middleware.InvokeAsync(context);

        // Assert — next 未被调用，返回 401
        Assert.Equal(401, context.Response.StatusCode);
        mockNext.Verify(n => n(It.IsAny<HttpContext>()), Times.Never);
    }
}
```

### 3.6 配置选项影响行为

```csharp
public class RateLimitingMiddlewareTests
{
    [Fact]
    public async Task WithinLimit_PassesRequest()
    {
        // Arrange
        var options = new RateLimitOptions
        {
            MaxRequestsPerMinute = 10,
            Path = "/api"
        };
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new RateLimitingMiddleware(mockNext.Object, Options.Create(options));
        var context = new DefaultHttpContext();
        context.Request.Path = "/api/data";

        // Act
        for (int i = 0; i < 10; i++)
        {
            await middleware.InvokeAsync(context);
        }

        // Assert
        mockNext.Verify(n => n(context), Times.Exactly(10));
    }

    [Fact]
    public async Task ExceedsLimit_Returns429()
    {
        // Arrange
        var options = new RateLimitOptions
        {
            MaxRequestsPerMinute = 2,
            Path = "/api"
        };
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new RateLimitingMiddleware(mockNext.Object, Options.Create(options));

        // Act — 发送 3 个请求，第 3 个应该被限流
        for (int i = 0; i < 3; i++)
        {
            var context = new DefaultHttpContext();
            context.Request.Path = "/api/data";
            context.Response.Body = new MemoryStream();
            await middleware.InvokeAsync(context);

            if (i < 2) Assert.NotEqual(429, context.Response.StatusCode);
            else Assert.Equal(429, context.Response.StatusCode);
        }
    }
}
```

## 四、Mock HttpContext

### 4.1 使用 DefaultHttpContext

`DefaultHttpContext` 是 ASP.NET Core 提供的 HttpContext 实现，可以直接实例化：

```csharp
[Fact]
public async Task Middleware_WithDefaultHttpContext_Works()
{
    // Arrange
    var context = new DefaultHttpContext();
    context.Request.Method = "POST";
    context.Request.Path = "/api/users";
    context.Request.ContentType = "application/json";
    context.Response.Body = new MemoryStream();

    var json = """{"name":"张三","email":"zhangsan@example.com"}""";
    context.Request.Body = new MemoryStream(
        Encoding.UTF8.GetBytes(json));
    context.Request.ContentLength = json.Length;

    var mockNext = new Mock<RequestDelegate>();
    var middleware = new RequestValidationMiddleware(mockNext.Object);

    // Act
    await middleware.InvokeAsync(context);

    // Assert
    mockNext.Verify(n => n(context), Times.Once);
}
```

### 4.2 设置 Request/Response 属性

```csharp
public static class HttpContextHelper
{
    /// <summary>
    /// 创建一个可用于测试的 HttpContext
    /// </summary>
    public static HttpContext CreateTestContext(
        string method = "GET",
        string path = "/",
        string? body = null,
        Dictionary<string, string>? headers = null)
    {
        var context = new DefaultHttpContext();

        // 请求属性
        context.Request.Method = method;
        context.Request.Path = path;
        context.Request.Scheme = "https";
        context.Request.Host = new HostString("localhost", 5001);

        // 请求体
        if (body != null)
        {
            context.Request.Body = new MemoryStream(Encoding.UTF8.GetBytes(body));
            context.Request.ContentType = "application/json";
            context.Request.ContentLength = body.Length;
        }

        // 请求头
        if (headers != null)
        {
            foreach (var (key, value) in headers)
            {
                context.Request.Headers[key] = value;
            }
        }

        // 响应体（用于后续读取响应内容）
        context.Response.Body = new MemoryStream();

        return context;
    }

    /// <summary>
    /// 从响应 Body 中读取内容
    /// </summary>
    public static string GetResponseBody(HttpContext context)
    {
        context.Response.Body.Seek(0, SeekOrigin.Begin);
        using var reader = new StreamReader(context.Response.Body);
        return reader.ReadToEnd();
    }
}
```

### 4.3 Mock RequestDelegate（_next）

`RequestDelegate` 是一个委托，可以用 Mock 也可以直接用匿名方法：

```csharp
// 方式1：使用 Moq
var mockNext = new Mock<RequestDelegate>();
mockNext
    .Setup(n => n(It.IsAny<HttpContext>()))
    .Returns(Task.CompletedTask);

var middleware = new MyMiddleware(mockNext.Object);

// 验证 next 是否被调用
mockNext.Verify(n => n(context), Times.Once);

// 方式2：使用匿名委托
var nextCalled = false;
RequestDelegate next = ctx =>
{
    nextCalled = true;
    return Task.CompletedTask;
};

var middleware = new MyMiddleware(next);
await middleware.InvokeAsync(context);

Assert.True(nextCalled);

// 方式3：模拟 next 修改响应
RequestDelegate next = async ctx =>
{
    ctx.Response.StatusCode = 200;
    await ctx.Response.WriteAsync("Hello from next");
};
```

### 4.4 完整单元测试代码

以下是一个完整的中间件单元测试，涵盖各种场景：

```csharp
public class RequestLoggingMiddlewareTests
{
    private readonly Mock<ILogger<RequestLoggingMiddleware>> _mockLogger;
    private readonly Mock<RequestDelegate> _mockNext;

    public RequestLoggingMiddlewareTests()
    {
        _mockLogger = new Mock<ILogger<RequestLoggingMiddleware>>();
        _mockNext = new Mock<RequestDelegate>();
    }

    private RequestLoggingMiddleware CreateMiddleware()
    {
        return new RequestLoggingMiddleware(_mockNext.Object, _mockLogger.Object);
    }

    [Fact]
    public async Task LogRequest_CallsNext()
    {
        // Arrange
        var middleware = CreateMiddleware();
        var context = HttpContextHelper.CreateTestContext(
            method: "GET",
            path: "/api/users");

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        _mockNext.Verify(n => n(context), Times.Once);
    }

    [Fact]
    public async Task LogRequest_LogsRequestMethodAndPath()
    {
        // Arrange
        var middleware = CreateMiddleware();
        var context = HttpContextHelper.CreateTestContext(
            method: "POST",
            path: "/api/orders");

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        _mockLogger.Verify(
            x => x.Log(
                LogLevel.Information,
                It.IsAny<EventId>(),
                It.Is<It.IsAnyType>((v, _) =>
                    v.ToString()!.Contains("POST") &&
                    v.ToString()!.Contains("/api/orders")),
                It.IsAny<Exception>(),
                It.IsAny<Func<It.IsAnyType, Exception?, string>>()),
            Times.Once);
    }

    [Fact]
    public async Task NextThrows_LogsErrorAndRethrows()
    {
        // Arrange
        var middleware = CreateMiddleware();
        var context = HttpContextHelper.CreateTestContext();

        _mockNext
            .Setup(n => n(context))
            .ThrowsAsync(new InvalidOperationException("管道异常"));

        // Act & Assert
        await Assert.ThrowsAsync<InvalidOperationException>(
            () => middleware.InvokeAsync(context));

        _mockLogger.Verify(
            x => x.Log(
                LogLevel.Error,
                It.IsAny<EventId>(),
                It.IsAny<It.IsAnyType>(),
                It.IsAny<InvalidOperationException>(),
                It.IsAny<Func<It.IsAnyType, Exception?, string>>()),
            Times.Once);
    }

    [Fact]
    public async Task SkipPath_DoesNotLog()
    {
        // Arrange
        var options = new RequestLoggingOptions
        {
            SkipPaths = new[] { "/health", "/metrics" }
        };
        var middleware = new RequestLoggingMiddleware(
            _mockNext.Object,
            _mockLogger.Object,
            Options.Create(options));

        var context = HttpContextHelper.CreateTestContext(path: "/health");

        // Act
        await middleware.InvokeAsync(context);

        // Assert
        _mockNext.Verify(n => n(context), Times.Once);
        _mockLogger.Verify(
            x => x.Log(
                LogLevel.Information,
                It.IsAny<EventId>(),
                It.IsAny<It.IsAnyType>(),
                It.IsAny<Exception>(),
                It.IsAny<Func<It.IsAnyType, Exception?, string>>()),
            Times.Never);
    }
}
```

## 五、集成测试

### 5.1 WebApplicationFactory

`WebApplicationFactory<T>` 提供了在内存中运行完整应用的能力：

```csharp
public class MiddlewareIntegrationTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly HttpClient _client;

    public MiddlewareIntegrationTests(WebApplicationFactory<Program> factory)
    {
        _client = factory.WithWebHostBuilder(builder =>
        {
            builder.ConfigureServices(services =>
            {
                // 替换真实服务为测试服务
                services.RemoveAll<IEmailService>();
                services.AddSingleton<IEmailService, FakeEmailService>();
            });
        }).CreateClient();
    }

    [Fact]
    public async Task Request_WithValidToken_Returns200()
    {
        // Arrange
        var request = new HttpRequestMessage(HttpMethod.Get, "/api/users");
        request.Headers.Authorization =
            new AuthenticationHeaderValue("Bearer", "test-token");

        // Act
        var response = await _client.SendAsync(request);

        // Assert
        response.EnsureSuccessStatusCode();
    }

    [Fact]
    public async Task Request_WithoutToken_Returns401()
    {
        // Act
        var response = await _client.GetAsync("/api/users");

        // Assert
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task Request_WithExpiredToken_Returns401()
    {
        // Arrange
        var request = new HttpRequestMessage(HttpMethod.Get, "/api/users");
        request.Headers.Authorization =
            new AuthenticationHeaderValue("Bearer", "expired-token");

        // Act
        var response = await _client.SendAsync(request);

        // Assert
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }
}
```

### 5.2 测试中间件在完整管道中的行为

```csharp
public class FullPipelineTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly WebApplicationFactory<Program> _factory;

    public FullPipelineTests(WebApplicationFactory<Program> factory)
    {
        _factory = factory;
    }

    [Fact]
    public async Task ExceptionMiddleware_CatchesControllerException()
    {
        // Arrange
        var client = _factory.CreateClient();

        // Act — 请求一个会抛出异常的端点
        var response = await client.GetAsync("/api/error/throw");

        // Assert
        Assert.Equal(HttpStatusCode.InternalServerError, response.StatusCode);

        var body = await response.Content.ReadAsStringAsync();
        Assert.DoesNotContain("stack trace", body.ToLower());  // 不暴露堆栈
        Assert.DoesNotContain("exception", body.ToLower());     // 不暴露异常详情
    }

    [Fact]
    public async Task CorsMiddleware_AllowsConfiguredOrigin()
    {
        // Arrange
        var client = _factory.CreateClient();
        var request = new HttpRequestMessage(HttpMethod.Options, "/api/users");
        request.Headers.Add("Origin", "https://allowed.example.com");
        request.Headers.Add("Access-Control-Request-Method", "POST");

        // Act
        var response = await client.SendAsync(request);

        // Assert
        Assert.Equal("https://allowed.example.com",
            response.Headers.GetValues("Access-Control-Allow-Origin").First());
    }

    [Fact]
    public async Task RateLimit_Returns429_WhenExceeded()
    {
        // Arrange
        var client = _factory.CreateClient();
        HttpResponseMessage? lastResponse = null;

        // Act — 发送超过限制的请求
        for (int i = 0; i < 105; i++)
        {
            lastResponse = await client.GetAsync("/api/data");
        }

        // Assert
        Assert.NotNull(lastResponse);
        Assert.Equal(HttpStatusCode.TooManyRequests, lastResponse.StatusCode);
    }
}
```

### 5.3 测试中间件顺序

```csharp
public class MiddlewareOrderTests
{
    [Fact]
    public async Task ExceptionMiddleware_BeforeAuth_BothWork()
    {
        // Arrange
        var builder = new WebHostBuilder()
            .Configure(app =>
            {
                app.UseMiddleware<ExceptionHandlingMiddleware>();
                app.UseMiddleware<AuthMiddleware>();
                app.Run(async context =>
                {
                    if (context.User.Identity?.IsAuthenticated != true)
                    {
                        context.Response.StatusCode = 401;
                        return;
                    }
                    await context.Response.WriteAsync("Secret data");
                });
            });

        using var server = new TestServer(builder);
        var client = server.CreateClient();

        // Act — 无 Token 请求
        var response = await client.GetAsync("/api/secret");

        // Assert — Auth 中间件返回 401，不是 500
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task AuthMiddleware_BeforeException_AuthExceptionCaught()
    {
        // Arrange
        var builder = new WebHostBuilder()
            .Configure(app =>
            {
                app.UseMiddleware<AuthMiddleware>();
                app.UseMiddleware<ExceptionHandlingMiddleware>();
                app.Run(async context =>
                {
                    // Auth 中件件内部抛出异常
                    throw new InvalidOperationException("认证服务不可用");
                });
            });

        using var server = new TestServer(builder);
        var client = server.CreateClient();

        // Act
        var response = await client.GetAsync("/api/secret");

        // Assert — 异常被 Exception 中间件捕获
        Assert.Equal(HttpStatusCode.InternalServerError, response.StatusCode);
    }

    [Fact]
    public async Task LoggingMiddleware_RecordsAllRequests()
    {
        // Arrange
        var logMessages = new List<string>();
        var builder = new WebHostBuilder()
            .ConfigureServices(services =>
            {
                services.AddSingleton<ILogger<RequestLoggingMiddleware>>(
                    new TestLogger<RequestLoggingMiddleware>(logMessages));
            })
            .Configure(app =>
            {
                app.UseMiddleware<RequestLoggingMiddleware>();
                app.UseMiddleware<AuthMiddleware>();
                app.Run(async context =>
                {
                    await context.Response.WriteAsync("OK");
                });
            });

        using var server = new TestServer(builder);
        var client = server.CreateClient();

        // Act — 发送多个请求
        await client.GetAsync("/api/users");
        await client.GetAsync("/api/orders");
        await client.GetAsync("/health");

        // Assert — 所有请求都被记录
        Assert.Equal(3, logMessages.Count);
    }
}
```

## 六、测试最佳实践

### 6.1 每个中间件一个测试类

```csharp
// ✅ 好的组织方式
public class RequestLoggingMiddlewareTests { ... }
public class AuthMiddlewareTests { ... }
public class ExceptionHandlingMiddlewareTests { ... }

// ❌ 避免的做法
public class AllMiddlewareTests
{
    [Fact]
    public async Task TestEverything() { ... }  // 难以定位问题
}
```

### 6.2 测试命名约定

```csharp
// 方法名_场景_期望结果
[Fact]
public async Task InvokeAsync_WithValidToken_CallsNext() { }

[Fact]
public async Task InvokeAsync_WithExpiredToken_Returns401() { }

[Fact]
public async Task InvokeAsync_WhenNextThrows_LogsError() { }

// 使用 Theory 简化参数化测试
[Theory]
[InlineData("/health", true)]      // 跳过路径
[InlineData("/metrics", true)]     // 跳过路径
[InlineData("/api/users", false)]  // 正常处理
[InlineData("/api/orders", false)] // 正常处理
public async Task InvokeAsync_SkipPathCheck(
    string path, bool shouldSkip)
{
    // ...
}
```

### 6.3 测试正常路径和异常路径

```csharp
public class ApiKeyMiddlewareTests
{
    // === 正常路径 ===

    [Fact]
    public async Task ValidApiKey_CallsNext() { }

    [Fact]
    public async Task SkippedPath_CallsNext() { }

    // === 异常路径 ===

    [Fact]
    public async Task MissingApiKey_Returns401() { }

    [Fact]
    public async Task InvalidApiKey_Returns401() { }

    [Fact]
    public async Task EmptyApiKey_Returns401() { }

    // === 边界情况 ===

    [Fact]
    public async Task ApiKeyInQueryParams_Works() { }

    [Fact]
    public async Task ApiKeyWithBearerPrefix_Works() { }

    [Fact]
    public async Task OptionsRequest_SkipsValidation() { }
}
```

### 6.4 测试边界条件

```csharp
public class RateLimitMiddlewareBoundaryTests
{
    [Fact]
    public async Task ExactlyAtLimit_StillAllowed()
    {
        // 刚好在限制上，应该允许
        var options = new RateLimitOptions { MaxRequests = 5 };
        var middleware = new RateLimitMiddleware(
            Mock.Of<RequestDelegate>(), Options.Create(options));

        for (int i = 0; i < 5; i++)
        {
            var context = HttpContextHelper.CreateTestContext(path: "/api/data");
            await middleware.InvokeAsync(context);
            Assert.NotEqual(429, context.Response.StatusCode);
        }
    }

    [Fact]
    public async Task OneOverLimit_Returns429()
    {
        // 超过限制一个，应该返回 429
        var options = new RateLimitOptions { MaxRequests = 5 };
        var middleware = new RateLimitMiddleware(
            Mock.Of<RequestDelegate>(), Options.Create(options));

        // 先发送 5 个合法请求
        for (int i = 0; i < 5; i++)
        {
            var context = HttpContextHelper.CreateTestContext(path: "/api/data");
            await middleware.InvokeAsync(context);
        }

        // 第 6 个请求应该被限流
        var exceededContext = HttpContextHelper.CreateTestContext(path: "/api/data");
        exceededContext.Response.Body = new MemoryStream();
        await middleware.InvokeAsync(exceededContext);
        Assert.Equal(429, exceededContext.Response.StatusCode);
    }

    [Fact]
    public async Task WindowResets_AllowsNewRequests()
    {
        // 时间窗口重置后，应该允许新请求
        var options = new RateLimitOptions
        {
            MaxRequests = 2,
            WindowSeconds = 1
        };
        var mockTimeProvider = new Mock<ITimeProvider>();
        var now = DateTime.UtcNow;
        mockTimeProvider.Setup(t => t.UtcNow).Returns(now);

        var middleware = new RateLimitMiddleware(
            Mock.Of<RequestDelegate>(),
            Options.Create(options),
            mockTimeProvider.Object);

        // 发送 2 个请求
        for (int i = 0; i < 2; i++)
        {
            var context = HttpContextHelper.CreateTestContext(path: "/api/data");
            await middleware.InvokeAsync(context);
        }

        // 模拟时间过了窗口期
        mockTimeProvider.Setup(t => t.UtcNow)
            .Returns(now.AddSeconds(2));

        // 新的请求应该被允许
        var newContext = HttpContextHelper.CreateTestContext(path: "/api/data");
        await middleware.InvokeAsync(newContext);
        Assert.NotEqual(429, newContext.Response.StatusCode);
    }
}
```

### 6.5 测试配置变更

```csharp
public class ConfigurableMiddlewareTests
{
    [Fact]
    public async Task Enabled_False_SkipsMiddleware()
    {
        // Arrange
        var options = new FeatureToggleOptions { Enabled = false };
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new FeatureToggleMiddleware(
            mockNext.Object, Options.Create(options));
        var context = new DefaultHttpContext();

        // Act
        await middleware.InvokeAsync(context);

        // Assert — 中间件被跳过，直接调用 next
        mockNext.Verify(n => n(context), Times.Once);
    }

    [Fact]
    public async Task Enabled_True_ExecutesMiddleware()
    {
        // Arrange
        var options = new FeatureToggleOptions { Enabled = true };
        var mockNext = new Mock<RequestDelegate>();
        var middleware = new FeatureToggleMiddleware(
            mockNext.Object, Options.Create(options));
        var context = new DefaultHttpContext();
        context.Response.Body = new MemoryStream();

        // Act
        await middleware.InvokeAsync(context);

        // Assert — 中间件逻辑被执行
        Assert.True(context.Items.ContainsKey("FeatureProcessed"));
    }

    [Fact]
    public async Task DifferentConfigs_DifferentBehavior()
    {
        // 同一个中间件，不同配置，行为不同
        var strictOptions = new ValidationOptions
        {
            Mode = ValidationMode.Strict,
            AllowEmptyBody = false
        };
        var looseOptions = new ValidationOptions
        {
            Mode = ValidationMode.Loose,
            AllowEmptyBody = true
        };

        var context1 = HttpContextHelper.CreateTestContext(
            method: "POST",
            path: "/api/data");
        context1.Response.Body = new MemoryStream();

        var context2 = HttpContextHelper.CreateTestContext(
            method: "POST",
            path: "/api/data");
        context2.Response.Body = new MemoryStream();

        var strictMiddleware = new ValidationMiddleware(
            Mock.Of<RequestDelegate>(), Options.Create(strictOptions));
        var looseMiddleware = new ValidationMiddleware(
            Mock.Of<RequestDelegate>(), Options.Create(looseOptions));

        await strictMiddleware.InvokeAsync(context1);
        await looseMiddleware.InvokeAsync(context2);

        // Strict 模式拒绝空 Body
        Assert.Equal(400, context1.Response.StatusCode);
        // Loose 模式允许空 Body
        Assert.NotEqual(400, context2.Response.StatusCode);
    }
}
```

## 小结

- 中间件测试分为单元测试和集成测试两个层次，单元测试使用 `DefaultHttpContext` + Mock `RequestDelegate`，集成测试使用 `TestServer` 或 `WebApplicationFactory`
- 每个中间件应独立测试类，覆盖正常路径、异常路径和边界条件
- 使用 `HttpContextHelper` 等工具类减少测试样板代码
- 测试中间件顺序时，使用 `TestServer` 构建不同顺序的管道
- 配置选项变更应通过参数化测试验证不同配置下的行为差异
