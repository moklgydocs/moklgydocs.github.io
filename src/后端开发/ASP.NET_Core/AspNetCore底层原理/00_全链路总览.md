---
title: 00 · 全链路总览
icon: fa6-solid:map
order: 0
category:
  - ASP.NET Core
tag:
  - 底层原理
  - 总览
  - Kestrel
  - 请求管道
---

# 00 · 全链路总览

> 在深入每一个模块之前，先建立全局视野。本文回答一个问题：**一个 HTTP 请求从网卡到 Controller、再从 Controller 回到网卡，经历了哪些关键节点？**

---

## 一、全局流程图

```mermaid
flowchart TD
    NC["🌐 网卡 NIC\n以太网帧到达"]
    OS["OS 内核 TCP/IP 协议栈\nSYN → SYN-ACK → ACK\n三次握手完成"]
    SK["Socket 层\nSocket.AcceptAsync()\n返回已连接 fd"]
    TP["Transport 层\nSocketConnection\nPipeReader ↔ PipeWriter"]
    PP["System.IO.Pipelines\n零拷贝内存池\nMemoryPool&lt;byte&gt;"]
    HP["HTTP 协议解析\nHttpParser 状态机\nHttp1Connection / Http2Connection"]
    FC["IFeatureCollection\n解析结果包装\nIHttpRequestFeature\nIHttpResponseFeature"]
    HA["HostingApplication ⭐\n最关键的交接点\nCreateContext → DefaultHttpContext"]
    MW["中间件管道\nRequestDelegate 委托链\n洋葱圈模型"]
    RT["路由系统\nDFA 状态机匹配\nEndpoint 选择"]
    AU["认证授权\nJwtBearerHandler\nClaimsPrincipal"]
    MB["模型绑定\nFromRoute/Query/Body/Form\nValidation"]
    AE["Action 执行\nController / Minimal API\n你的业务代码"]
    AF["过滤器管道\nIActionFilter / IResultFilter"]
    OF["输出格式化\nSystemTextJsonOutputFormatter\n内容协商"]
    RW["响应写回\nPipeWriter.WriteAsync\nHTTP 报文组装"]
    SK2["Socket 发送\nSocketAsyncEventArgs\nScatter IO 零拷贝"]
    NC2["🌐 网卡 NIC\n数据包发出"]

    NC --> OS --> SK --> TP --> PP --> HP --> FC --> HA
    HA --> MW --> RT --> AU --> MB --> AE --> AF --> OF --> RW --> SK2 --> NC2

    style NC fill:#2d4a6b,color:#fff
    style NC2 fill:#2d4a6b,color:#fff
    style HA fill:#7b2d00,color:#fff,stroke:#ff6b00,stroke-width:3px
    style MW fill:#4a1942,color:#fff
    style RT fill:#3d2b00,color:#fff
    style AU fill:#1a3a3a,color:#fff
```

---

## 二、六大分层架构

```mermaid
graph TB
    subgraph L1["🏠 宿主层 Host（卷一）"]
        H1["WebApplication.CreateBuilder()"]
        H2["GenericHost 启动协调"]
        H3["IServiceCollection DI 容器注册"]
        H4["Configuration / Logging 基础设施"]
    end

    subgraph L2["⚡ 服务器层 Kestrel（卷二）"]
        K1["System.IO.Pipelines 零拷贝"]
        K2["SocketTransport 传输层"]
        K3["Http1Connection 状态机解析"]
        K4["Http2Connection HPACK 解码"]
    end

    subgraph L3["🧅 管道层 Middleware（卷三）"]
        P1["IApplicationBuilder 构建"]
        P2["RequestDelegate 委托链折叠"]
        P3["HostingApplication 交接点"]
    end

    subgraph L4["🗺️ 路由层 Routing（卷四）"]
        R1["EndpointDataSource 收集"]
        R2["DFA 图编译与匹配"]
        R3["RouteConstraint 约束检查"]
    end

    subgraph L5["🎯 执行层 MVC/MinimalAPI（卷五）"]
        M1["模型绑定 ModelBinder"]
        M2["过滤器管道 Filters"]
        M3["IActionResult 结果体系"]
        M4["OutputFormatter 内容协商"]
    end

    subgraph L6["🔐 安全层 Auth（卷六）"]
        A1["IAuthenticationHandler 认证"]
        A2["IAuthorizationService 授权"]
    end

    L1 --> L2 --> L3 --> L4 --> L5
    L6 -.->|横切关注点| L3

    style L1 fill:#1e3a5f,color:#fff
    style L2 fill:#1a4731,color:#fff
    style L3 fill:#4a1942,color:#fff
    style L4 fill:#3d2b00,color:#fff
    style L5 fill:#3d0000,color:#fff
    style L6 fill:#1a3a3a,color:#fff
```

---

## 三、每个模块的一句话总结

| # | 模块 | 一句话 | 核心类 |
|---|------|--------|--------|
| 01 | 网络层 | OS 的 TCP/IP 栈完成三次握手，Kestrel 用 IOCP/epoll 异步接受连接 | `SocketConnectionListener` |
| 02 | Pipelines | 零分配的管道模型取代 `byte[]`，内存池彻底消除 GC 压力 | `PinnedBlockMemoryPool` |
| 03 | HTTP 解析 | 零拷贝状态机在原始字节上划 Span 边界，识别 Method/Path/Headers | `HttpParser<T>` |
| 04 | IFeatureCollection | Kestrel 只知道"特性接口"，不知道 `HttpContext`，通过此接口严格解耦 | `IHttpRequestFeature` |
| 05 | 交接点 | `HostingApplication` 用特性集合孵化 `DefaultHttpContext`，进入 ASP.NET Core 领域 | `HostingApplication` |
| 06 | 中间件管道 | `Build()` 逆序折叠成单一委托链，运行时洋葱圈穿越 | `IApplicationBuilder` |
| 07 | 路由 | 所有路由模板提前编译成 DFA 图，匹配复杂度 O(path_length) 与路由数量无关 | `DfaMatcher` |
| 08 | 认证授权 | 认证确定"你是谁"（ClaimsPrincipal），授权判断"你能做什么"（Policy） | `JwtBearerHandler` |
| 09 | 模型绑定 | 按优先级从 Route/Query/Body/Form 组装强类型参数，Utf8JsonReader 零分配 | `BodyModelBinder` |
| 10 | 过滤器 | Authorization→Resource→Action→Result 四层过滤器，异常过滤器横切 | `ControllerActionInvoker` |
| 11 | 输出格式化 | 按 `Accept` Header 内容协商，直接写 `PipeWriter` 不经 MemoryStream | `SystemTextJsonOutputFormatter` |
| 12 | 响应返回 | `SocketSender` 用 Scatter IO 零拷贝把内存池中的字节发回网卡 | `SocketSender` |

---

## 四、关键设计思想速查

### 4.1 为什么性能高？——三个零

| 零 | 含义 | 实现机制 |
|----|------|---------|
| **零拷贝** | 字节从网卡进来，到序列化完成发出，中间尽量不额外复制 | `PinnedBlockMemoryPool` + `SocketAsyncEventArgs.BufferList` (Scatter IO) |
| **零分配** | 热路径上不 `new` 对象，不触发 GC | `ObjectPool<DefaultHttpContext>` + `heapless Span 解析` |
| **零阻塞** | 所有 IO 操作都是 `async/await`，等待期间线程归还线程池 | `System.IO.Pipelines` + IOCP/epoll |

### 4.2 四个关键接口

```csharp
// 1. 管道中流动的唯一类型
delegate Task RequestDelegate(HttpContext context);

// 2. Kestrel 与 ASP.NET Core 的唯一合同
interface IHttpApplication<TContext> {
    TContext CreateContext(IFeatureCollection features);
    Task ProcessRequestAsync(TContext context);
    void DisposeContext(TContext context, Exception? ex);
}

// 3. Kestrel 向上暴露的数据格式（不是 HttpRequest，是特性集合）
interface IFeatureCollection {
    object? this[Type key] { get; set; }
}

// 4. 中间件的本质
interface IMiddleware {
    Task InvokeAsync(HttpContext context, RequestDelegate next);
}
```

### 4.3 对象生命周期总览

```mermaid
graph LR
    subgraph 进程生命周期 Singleton
        SP["IServiceProvider\n(DI 容器根)"]
        KS["KestrelServer"]
        MP["MemoryPool&lt;byte&gt;"]
        DFA["DfaMatcher\n(路由 DFA 图)"]
    end

    subgraph 连接生命周期 Per-Connection
        SC["SocketConnection"]
        H1C["Http1Connection\n(同时实现 IFeatureCollection)"]
        PIP["Pipe (InputPipe + OutputPipe)"]
    end

    subgraph 请求生命周期 Per-Request
        CTX["DefaultHttpContext\n(来自对象池，用完归还)"]
        REQ["HttpRequest\n(值类型绑定到 Feature)"]
        RES["HttpResponse\n(值类型绑定到 Feature)"]
        SCP["IServiceScope\n(Scoped DI 作用域)"]
    end

    SP --> KS & MP & DFA
    KS --> SC --> H1C --> PIP
    H1C -->|"CreateContext()"| CTX
    CTX --> REQ & RES & SCP

    style 进程生命周期 fill:#1e3a5f,color:#fff
    style 连接生命周期 fill:#1a4731,color:#fff
    style 请求生命周期 fill:#4a1942,color:#fff
```

---

## 五、源码仓库导航

```
github.com/dotnet/aspnetcore
│
├── src/Hosting/Hosting/src/Internal/
│   └── HostingApplication.cs          ← ⭐ 交接点，最值得读
│
├── src/Servers/Kestrel/Core/src/Internal/Http/
│   ├── Http1Connection.cs              ← HTTP/1.1 连接与状态机
│   ├── HttpParser.cs                   ← 零拷贝 Header 解析
│   └── Http2/Http2Connection.cs        ← HTTP/2 帧处理
│
├── src/Servers/Kestrel/Transport.Sockets/src/
│   ├── SocketConnectionListener.cs     ← Accept 循环
│   ├── SocketConnection.cs             ← 读写 Pipe 桥接
│   └── Internal/PinnedBlockMemoryPool.cs ← 内存池
│
├── src/Http/Routing/src/Matching/
│   ├── DfaMatcher.cs                   ← DFA 匹配执行
│   └── DfaMatcherBuilder.cs            ← DFA 图编译
│
├── src/Middleware/RoutingMiddleware/src/
│   └── EndpointRoutingMiddleware.cs    ← UseRouting()
│
├── src/Mvc/Mvc.Core/src/
│   ├── Infrastructure/ControllerActionInvoker.cs ← 过滤器管道
│   ├── ModelBinding/Binders/BodyModelBinder.cs   ← JSON 绑定
│   └── Formatters/SystemTextJsonOutputFormatter.cs ← JSON 序列化
│
└── src/Security/Authentication/JwtBearer/src/
    └── JwtBearerHandler.cs             ← JWT 认证
```

---

## 六、学习路线建议

```mermaid
graph LR
    A["基础：先读 01+02\n理解 IO 模型和 Pipelines\n这是一切的基础"]
    B["核心：读 03+04+05\n理解 Kestrel 如何解析\n以及如何交接给框架"]
    C["管道：读 06+07\n理解中间件和路由\n这是日常扩展点"]
    D["执行：读 08+09+10\n理解安全和 MVC\n这是业务代码入口"]
    E["收尾：读 11+12\n理解响应如何写回\n形成完整闭环"]

    A --> B --> C --> D --> E

    style A fill:#1e3a5f,color:#fff
    style B fill:#1a4731,color:#fff
    style C fill:#4a1942,color:#fff
    style D fill:#3d2b00,color:#fff
    style E fill:#1a3a3a,color:#fff
```
