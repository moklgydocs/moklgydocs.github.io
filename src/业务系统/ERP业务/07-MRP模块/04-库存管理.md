---
title: 库存管理
date: 2025-04-18
author: Moklgy
category:
  - ERP业务
tag:
  - ERP
  - MRP
order: 4
---

# 库存管理

## 概述

库存管理子系统负责企业物料的存储、流转和追溯，涵盖仓库/库位管理、入库/出库事务处理、批次和序列号追踪、库存盘点以及库存报表。它为 MRP 运算提供实时库存数据，为财务模块提供存货变动依据。

## 一、仓库体系

### 1.1 仓库结构

```
企业
├── 原材料仓库 (RM-WH)
│   ├── 库区A (收货待检区)
│   ├── 库区B (合格品区)
│   │   ├── 库位 B-01-01
│   │   ├── 库位 B-01-02
│   │   └── ...
│   └── 库区C (不合格品区)
│
├── 半成品仓库 (WIP-WH)
│   ├── 车间线边仓
│   └── 半成品暂存区
│
├── 成品仓库 (FG-WH)
│   ├── 库区A (常规存储)
│   └── 库区B (待发区)
│
└── 其他仓库
    ├── 备品备件仓
    └── 退货仓
```

### 1.2 仓库实体

```
Warehouse (仓库)
├── Id: Guid
├── TenantId: Guid
├── Code: string(20)                   # 仓库编码
├── Name: string(100)                  # 仓库名称
├── Type: WarehouseType                # 类型
├── Address: string(300)               # 地址
├── ManagerId: Guid?                   # 仓管员
├── IsActive: bool
└── Remark: string(200)

Location (库位)
├── Id: Guid
├── WarehouseId: Guid
├── Code: string(20)                   # 库位编码 (如 A-01-01)
├── Name: string(100)
├── Zone: string(20)                   # 库区
├── Row: string(10)                    # 排
├── Column: string(10)                 # 列
├── Level: string(10)                  # 层
├── MaxCapacity: decimal(18,4)?       # 最大容量
├── CurrentOccupancy: decimal(18,4)?  # 当前占用
├── IsActive: bool
└── Remark: string(200)

枚举:
  WarehouseType: RawMaterial=1, WIP=2, FinishedGoods=3, Spare=4, Return=5, Other=9
```

## 二、库存核心实体

### 2.1 库存记录

```
InventoryItem (库存物料)
├── Id: Guid
├── TenantId: Guid
├── ItemId: Guid                       # 物料主数据ID
├── ItemCode: string(50)
├── ItemName: string(200)
├── WarehouseId: Guid
├── LocationId: Guid?
├── BatchNo: string(50)?              # 批次号
├── SerialNo: string(50)?             # 序列号
├── UnitOfMeasure: string(20)
│
├── OnHandQuantity: decimal(18,4)     # 在库数量
├── ReservedQuantity: decimal(18,4)   # 预留数量（已分配给SO/WO）
├── AvailableQuantity: decimal(18,4)  # 可用数量 = OnHand - Reserved
├── InTransitQuantity: decimal(18,4)  # 在途数量
├── InspectionQuantity: decimal(18,4) # 待检数量
│
├── UnitCost: decimal(18,6)           # 单位成本
├── TotalCost: decimal(18,2)          # 库存总成本
│
├── LastReceiptDate: DateTime?         # 最后入库日期
├── LastIssueDate: DateTime?           # 最后出库日期
├── ExpiryDate: DateTime?              # 有效期
└── Status: InventoryStatus

枚举:
  InventoryStatus: Available=1(可用), Reserved=2(已预留),
                   Inspection=3(待检), Blocked=4(冻结), Expired=5(过期)
```

### 2.2 库存事务

```
InventoryTransaction (库存事务)
├── Id: Guid
├── TenantId: Guid
├── TransactionNo: string(30)          # 事务编号
├── TransactionDate: DateTime
├── TransactionType: InvTransType      # 事务类型
├── Direction: Direction               # 入/出
│
├── ItemId: Guid
├── ItemCode: string(50)
├── ItemName: string(200)
├── WarehouseId: Guid
├── LocationId: Guid?
├── BatchNo: string(50)?
├── SerialNo: string(50)?
│
├── Quantity: decimal(18,4)           # 数量 (正数)
├── UnitCost: decimal(18,6)           # 单位成本
├── TotalCost: decimal(18,2)          # 总成本
├── UnitOfMeasure: string(20)
│
├── SourceDocumentType: string(20)     # 来源单据类型 (PO/SO/WO/...)
├── SourceDocumentId: Guid?
├── SourceDocumentNo: string(50)?
│
├── CounterpartWarehouseId: Guid?      # 对方仓库 (调拨用)
├── VoucherId: Guid?                   # 关联财务凭证
├── OperatorId: Guid                   # 操作人
└── Remark: string(200)

枚举:
  InvTransType:
    PurchaseReceipt=1(采购入库), SalesIssue=2(销售出库),
    ProductionIssue=3(生产领料), ProductionReceipt=4(完工入库),
    Transfer=5(调拨), Adjustment=6(盘点调整),
    Return=7(退货), Scrap=8(报废), Other=9
  Direction: In=1(入库), Out=2(出库)
```

## 三、库存操作类型

| 操作类型 | 方向 | 触发场景 | 财务影响 |
|---------|------|---------|---------|
| 采购入库 | 入 | PO 收货确认 | 原材料增加，暂估入账 |
| 销售出库 | 出 | SO 发货确认 | 库存商品减少，成本结转 |
| 生产领料 | 出 | WO 领料确认 | 原材料→生产成本 |
| 完工入库 | 入 | WO 完工确认 | 生产成本→库存商品 |
| 仓库调拨 | 入/出 | 手工调拨 | 仓库间转移 |
| 盘盈调整 | 入 | 盘点结果 | 库存增加 |
| 盘亏调整 | 出 | 盘点结果 | 库存减少 |
| 采购退货 | 出 | 退货确认 | 原材料减少 |
| 销售退货 | 入 | 退货收货 | 库存商品增加 |
| 报废 | 出 | 报废审批 | 库存减少，损失 |

## 四、批次与序列号

### 4.1 批次管理

```
Batch (批次)
├── Id: Guid
├── TenantId: Guid
├── ItemId: Guid
├── BatchNo: string(50)                # 批次号
├── ManufactureDate: DateTime?         # 生产日期
├── ExpiryDate: DateTime?              # 有效期
├── SupplierBatchNo: string(50)?       # 供应商批次号
├── SupplierId: Guid?
├── Quantity: decimal(18,4)            # 批次数量
├── Status: BatchStatus
└── Remark: string(200)

枚举:
  BatchStatus: Active=1, Expired=2, Blocked=3
```

### 4.2 追溯方向

```
正向追溯: 原材料批次 → 使用该批次的工单 → 产出的产品批次 → 销售给哪些客户
反向追溯: 客户投诉 → 产品批次 → 生产工单 → 使用的原材料批次 → 供应商
```

## 五、库存盘点

### 5.1 盘点流程

```
① 创建盘点计划
   选择仓库/库区/物料范围
              │
              ▼
② 生成盘点单
   系统自动生成待盘物料清单（可选是否显示账面数量）
              │
              ▼
③ 执行盘点
   仓管员清点实物，录入实盘数量
              │
              ▼
④ 差异处理
   系统自动计算差异 = 实盘数量 - 账面数量
   ├── 差异为0 → 无需处理
   ├── 差异>0 (盘盈) → 生成入库调整
   └── 差异<0 (盘亏) → 生成出库调整（需审批）
              │
              ▼
⑤ 审批调整
              │
              ▼
⑥ 生成调整事务和财务凭证
```

### 5.2 盘点实体

```
StockCount (盘点单)
├── Id: Guid
├── TenantId: Guid
├── CountNo: string(30)
├── WarehouseId: Guid
├── CountDate: DateTime
├── CountType: CountType               # 盘点类型
├── Status: CountStatus
├── CountedBy: Guid
├── ApprovedBy: Guid?
└── Lines: List<StockCountLine>

StockCountLine (盘点行)
├── Id: Guid
├── CountId: Guid
├── ItemId: Guid
├── ItemCode: string(50)
├── ItemName: string(200)
├── LocationId: Guid?
├── BatchNo: string(50)?
├── BookQuantity: decimal(18,4)       # 账面数量
├── ActualQuantity: decimal(18,4)     # 实盘数量
├── Variance: decimal(18,4)           # 差异 = 实盘 - 账面
├── UnitCost: decimal(18,6)           # 单价
├── VarianceAmount: decimal(18,2)     # 差异金额
└── Remark: string(200)

枚举:
  CountType: Full=1(全面盘点), Cycle=2(循环盘点), Spot=3(抽盘)
  CountStatus: Draft=0, Counting=1, Completed=2, Approved=3
```

## 六、API 接口设计

### 6.1 仓库管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/mrp/inventory/warehouses` | 仓库列表 |
| POST | `/api/mrp/inventory/warehouses` | 新增仓库 |
| GET | `/api/mrp/inventory/warehouses/{id}/locations` | 库位列表 |
| POST | `/api/mrp/inventory/warehouses/{id}/locations` | 新增库位 |

### 6.2 库存查询

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/mrp/inventory/items` | 库存列表（多维筛选） |
| GET | `/api/mrp/inventory/items/{itemId}` | 物料库存详情 |
| GET | `/api/mrp/inventory/items/{itemId}/transactions` | 物料事务明细 |
| GET | `/api/mrp/inventory/items/{itemId}/batches` | 物料批次列表 |
| GET | `/api/mrp/inventory/availability/{itemId}` | 可用量查询 |

### 6.3 库存操作

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/mrp/inventory/transfer` | 仓库调拨 |
| POST | `/api/mrp/inventory/adjust` | 库存调整 |
| POST | `/api/mrp/inventory/scrap` | 报废处理 |
| POST | `/api/mrp/inventory/reserve` | 预留库存 |
| POST | `/api/mrp/inventory/unreserve` | 取消预留 |

### 6.4 盘点管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/mrp/inventory/stock-counts` | 创建盘点单 |
| GET | `/api/mrp/inventory/stock-counts` | 盘点单列表 |
| PUT | `/api/mrp/inventory/stock-counts/{id}` | 录入实盘数量 |
| POST | `/api/mrp/inventory/stock-counts/{id}/complete` | 完成盘点 |
| POST | `/api/mrp/inventory/stock-counts/{id}/approve` | 审批差异 |

### 6.5 库存报表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/mrp/inventory/reports/summary` | 库存汇总表 |
| GET | `/api/mrp/inventory/reports/aging` | 库龄分析 |
| GET | `/api/mrp/inventory/reports/turnover` | 库存周转率 |
| GET | `/api/mrp/inventory/reports/slow-moving` | 呆滞物料报表 |
| GET | `/api/mrp/inventory/reports/traceability` | 批次追溯 |

## 七、实体关系图

```
┌──────────────┐     ┌──────────────┐
│ Warehouse    │────▶│ Location     │
│ 仓库          │ 1:N │ 库位          │
└──────┬───────┘     └──────────────┘
       │
       ▼
┌──────────────┐     ┌──────────────────┐
│InventoryItem │────▶│InventoryTransact │
│ 库存记录      │ 1:N │ 库存事务          │
└──────────────┘     └──────────────────┘
       │
       ▼
┌──────────────┐     ┌──────────────────┐
│ Batch        │     │ StockCount       │
│ 批次          │     │ 盘点单            │
└──────────────┘     └──────┬───────────┘
                            │ 1:N
                            ▼
                     ┌──────────────────┐
                     │ StockCountLine   │
                     │ 盘点行            │
                     └──────────────────┘
```

## 八、业务规则

| 规则 | 描述 |
|------|------|
| 库存不可为负 | 出库数量不得超过可用库存 |
| 预留优先 | 已预留库存不可被其他订单使用 |
| 批次强制 | 启用批次管理的物料所有事务必须记录批次 |
| FIFO 出库 | 先进先出策略下优先出库早期批次 |
| 有效期管理 | 过期批次自动冻结，不可出库 |
| 盘点冻结 | 盘点进行中冻结该仓库的出入库操作 |
| 差异审批 | 盘亏超过金额阈值需要管理层审批 |
| 调拨同步 | 调拨出库和入库必须同步完成 |
| 事务不可删除 | 库存事务只能冲销，不可删除 |
| 成本同步 | 出入库事务自动更新库存成本 |
