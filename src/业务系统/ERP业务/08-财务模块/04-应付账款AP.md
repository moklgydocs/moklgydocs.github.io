---
title: 应付账款AP
date: 2025-04-18
author: Moklgy
category:
  - ERP业务
tag:
  - ERP
  - 财务
order: 4
---

# 应付账款 AP (Accounts Payable)

## 概述

应付账款（AP）子系统管理企业因采购商品或接受服务而产生的对供应商的欠款。它与采购模块紧密集成，通过**三单匹配**（采购订单-收货单-发票）确保付款的准确性，并提供付款计划和供应商对账能力。

## 一、核心业务流程

### 1.1 应付全流程

```
采购发票 ──▶ 生成应付单 ──▶ 三单匹配 ──▶ 匹配通过 ──▶ 创建付款申请
                                │                           │
                                ▼                           ▼
                          匹配异常处理              付款审批 ──▶ 执行付款 ──▶ 清账
                          (差异调整/退回)                         │
                                                               ▼
                                                        银行付款 → 凭证
```

### 1.2 三单匹配流程

```
┌────────────────┐
│ 采购订单 (PO)   │──┐
│ 数量、单价、金额  │  │
└────────────────┘  │
                    │    ┌────────────────┐    ┌──────────────┐
                    ├───▶│ 三单匹配引擎    │───▶│ 匹配结果      │
                    │    │                │    │              │
┌────────────────┐  │    │ 数量匹配       │    │ ✅ 完全匹配    │
│ 收货单 (GRN)    │──┤    │ 价格匹配       │    │ ⚠️ 容差内匹配  │
│ 实收数量         │  │    │ 金额匹配       │    │ ❌ 匹配失败    │
└────────────────┘  │    └────────────────┘    └──────────────┘
                    │
┌────────────────┐  │
│ 采购发票 (INV)  │──┘
│ 开票数量、金额   │
└────────────────┘

匹配规则:
  数量容差: ±5% (可配置)
  价格容差: ±2% (可配置)
  金额容差: ±100元 (可配置)
```

### 1.3 应付单状态机

```
┌────────┐    ┌──────────┐    ┌───────────┐    ┌──────────┐
│ Open    │───▶│ Matched  │───▶│ PartPaid  │───▶│ Paid     │
│ 待匹配   │    │ 已匹配    │    │ 部分付款   │    │ 已付款    │
└────────┘    └──────────┘    └───────────┘    └──────────┘
    │              │
    ▼              ▼
┌────────┐    ┌──────────┐
│ Dispute │    │ Approved │
│ 争议中   │    │ 已审批    │
└────────┘    └──────────┘
```

## 二、核心实体

### 2.1 应付单

```
Payable (应付单)
├── Id: Guid
├── TenantId: Guid
├── PayableNo: string(30)             # 应付单号 AP-{YYYYMMDD}-{SEQ}
├── SupplierId: Guid                  # 供应商ID
├── SupplierName: string(200)
├── SourceType: PayableSource         # 来源类型
├── SourceDocumentId: Guid?           # 来源单据ID (采购发票ID)
├── SourceDocumentNo: string(50)?     # 来源单据号
├── PurchaseOrderId: Guid?            # 关联采购订单
├── PurchaseOrderNo: string(50)?
├── GoodsReceiptId: Guid?             # 关联收货单
├── GoodsReceiptNo: string(50)?
├── InvoiceNo: string(50)             # 供应商发票号
├── InvoiceDate: DateTime             # 发票日期
├── CurrencyCode: string(3)
├── ExchangeRate: decimal(18,6)
├── TaxRate: decimal(5,2)             # 税率 (如 13%)
├── Amount: decimal(18,2)             # 不含税金额
├── TaxAmount: decimal(18,2)          # 税额
├── TotalAmount: decimal(18,2)        # 含税总额
├── PaidAmount: decimal(18,2)         # 已付金额
├── RemainingAmount: decimal(18,2)    # 未付金额
├── DueDate: DateTime                 # 到期日
├── PaymentTermDays: int              # 账期天数
├── MatchStatus: MatchStatus          # 三单匹配状态
├── Status: PayableStatus             # 应付单状态
├── VoucherId: Guid?
└── Remark: string(500)

枚举:
  PayableSource: PurchaseInvoice=1, ServiceInvoice=2, ExpenseReimbursement=3, ManualEntry=9
  PayableStatus: Open=0, Matched=1, Approved=2, PartPaid=3, Paid=4, Dispute=5
  MatchStatus: Unmatched=0, PartMatched=1, FullMatched=2, Exception=3
```

### 2.2 三单匹配

```
ThreeWayMatch (三单匹配记录)
├── Id: Guid
├── TenantId: Guid
├── PayableId: Guid                    # 应付单ID
├── PurchaseOrderId: Guid              # 采购订单ID
├── PurchaseOrderNo: string(50)
├── GoodsReceiptId: Guid               # 收货单ID
├── GoodsReceiptNo: string(50)
├── InvoiceId: Guid                    # 发票ID
├── InvoiceNo: string(50)
├── MatchDate: DateTime                # 匹配日期
├── Result: MatchResult                # 匹配结果
├── QuantityVariance: decimal(18,4)   # 数量差异
├── PriceVariance: decimal(18,2)      # 价格差异
├── AmountVariance: decimal(18,2)     # 金额差异
├── HandledBy: Guid?                   # 差异处理人
├── HandledAt: DateTime?
├── HandlingNote: string(500)          # 差异处理说明
└── Details: List<MatchDetail>

MatchDetail (匹配明细)
├── Id: Guid
├── MatchId: Guid
├── ItemCode: string(50)              # 物料编码
├── ItemName: string(200)
├── POQuantity: decimal(18,4)         # 订单数量
├── GRNQuantity: decimal(18,4)        # 收货数量
├── InvoiceQuantity: decimal(18,4)    # 发票数量
├── POUnitPrice: decimal(18,6)        # 订单单价
├── InvoiceUnitPrice: decimal(18,6)   # 发票单价
├── QuantityMatch: bool               # 数量是否匹配
├── PriceMatch: bool                  # 价格是否匹配
└── Remark: string(200)

枚举:
  MatchResult: FullMatch=1, WithinTolerance=2, Exception=3
```

### 2.3 付款单

```
Payment (付款单)
├── Id: Guid
├── TenantId: Guid
├── PaymentNo: string(30)             # 付款单号 PAY-{YYYYMMDD}-{SEQ}
├── SupplierId: Guid
├── SupplierName: string(200)
├── PaymentDate: DateTime              # 付款日期
├── PaymentMethod: PaymentMethod       # 付款方式
├── BankAccountId: Guid                # 付款银行账户
├── CurrencyCode: string(3)
├── ExchangeRate: decimal(18,6)
├── OriginalAmount: decimal(18,2)
├── Amount: decimal(18,2)             # 本位币金额
├── Status: PaymentStatus
├── ApprovalStatus: ApprovalStatus
├── ApprovedBy: Guid?
├── ApprovedAt: DateTime?
├── VoucherId: Guid?
├── Remark: string(500)
└── Allocations: List<PaymentAllocation>

PaymentAllocation (付款分配)
├── Id: Guid
├── PaymentId: Guid
├── PayableId: Guid
├── PayableNo: string(30)
├── AllocatedAmount: decimal(18,2)
├── DiscountAmount: decimal(18,2)      # 现金折扣
└── DeductionAmount: decimal(18,2)     # 扣款金额

枚举:
  PaymentStatus: Draft=0, Submitted=1, Approved=2, Paid=3, Cancelled=-1
  PaymentMethod: BankTransfer=1, Cash=2, Check=3, CommercialBill=4, Other=9
```

### 2.4 付款计划

```
PaymentPlan (付款计划)
├── Id: Guid
├── TenantId: Guid
├── PlanNo: string(30)
├── PlanDate: DateTime                 # 计划日期
├── PlanPeriodStart: DateTime          # 计划周期开始
├── PlanPeriodEnd: DateTime            # 计划周期结束
├── TotalPlannedAmount: decimal(18,2) # 计划付款总额
├── AvailableFunds: decimal(18,2)     # 可用资金
├── Status: PlanStatus
└── Items: List<PaymentPlanItem>

PaymentPlanItem (付款计划明细)
├── Id: Guid
├── PlanId: Guid
├── PayableId: Guid                    # 应付单ID
├── SupplierId: Guid
├── SupplierName: string(200)
├── DueDate: DateTime                  # 到期日
├── PayableAmount: decimal(18,2)      # 应付金额
├── PlannedAmount: decimal(18,2)      # 计划付款金额
├── Priority: int                      # 优先级 (1最高)
├── PlannedPayDate: DateTime           # 计划付款日期
└── Remark: string(200)

枚举:
  PlanStatus: Draft=0, Approved=1, Executing=2, Completed=3
```

## 三、付款审批

### 3.1 审批流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 创建付款   │───▶│ 部门主管   │───▶│ 财务主管   │───▶│ 总经理    │
│ 申请      │    │ 审批      │    │ 审批      │    │ 审批(大额) │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                      │
                                                      ▼
                                                ┌──────────┐
                                                │ 出纳付款   │
                                                │ 执行      │
                                                └──────────┘
```

### 3.2 审批层级

| 金额范围 | 审批级别 | 审批人 |
|---------|---------|--------|
| ≤ 10,000 | 一级 | 部门主管 |
| 10,001 - 100,000 | 二级 | + 财务主管 |
| 100,001 - 500,000 | 三级 | + 财务总监 |
| > 500,000 | 四级 | + 总经理 |

## 四、付款会计分录

**正常付款:**

| 摘要 | 科目 | 借方 | 贷方 | 辅助 |
|------|------|------|------|------|
| 支付ABC供应商货款 | 2202 应付账款 | 113,000.00 | | 供应商:ABC |
| 支付ABC供应商货款 | 1002 银行存款 | | 113,000.00 | 银行:工商银行 |

**预付款:**

| 摘要 | 科目 | 借方 | 贷方 | 辅助 |
|------|------|------|------|------|
| 预付DEF供应商 | 1123 预付账款 | 50,000.00 | | 供应商:DEF |
| 预付DEF供应商 | 1002 银行存款 | | 50,000.00 | 银行:工商银行 |

**预付转应付（收货后）:**

| 摘要 | 科目 | 借方 | 贷方 | 辅助 |
|------|------|------|------|------|
| 预付款冲应付 | 2202 应付账款 | 50,000.00 | | 供应商:DEF |
| 预付款冲应付 | 1123 预付账款 | | 50,000.00 | 供应商:DEF |

**采购发票入账（含税）:**

| 摘要 | 科目 | 借方 | 贷方 | 辅助 |
|------|------|------|------|------|
| 采购材料-取得发票 | 1403 原材料 | 100,000.00 | | |
| 采购材料-进项税 | 2221.01 进项税额 | 13,000.00 | | |
| 采购材料-应付 | 2202 应付账款 | | 113,000.00 | 供应商:ABC |

## 五、供应商对账

### 5.1 对账流程

```
① 获取供应商对账单 (供应商提供)
          │
          ▼
② 系统生成内部应付明细
          │
          ▼
③ 逐笔比对
   ├── 金额一致 → ✅ 已核对
   ├── 金额不一致 → ⚠️ 差异调查
   └── 对方有我方无 → 📋 补录
          │
          ▼
④ 差异处理
   ├── 时间差异(在途) → 标记，下期核对
   ├── 金额差异 → 调整或退回
   └── 单据遗漏 → 补录应付
          │
          ▼
⑤ 确认对账结果，双方签字
```

## 六、API 接口设计

### 6.1 应付管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/finance/ap/payables` | 查询应付列表 |
| GET | `/api/finance/ap/payables/{id}` | 应付单详情 |
| POST | `/api/finance/ap/payables` | 手工新增应付单 |
| PUT | `/api/finance/ap/payables/{id}` | 修改应付单 |

### 6.2 三单匹配

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/finance/ap/match/execute` | 执行三单匹配 |
| GET | `/api/finance/ap/match/results` | 匹配结果列表 |
| GET | `/api/finance/ap/match/exceptions` | 异常匹配列表 |
| POST | `/api/finance/ap/match/{id}/resolve` | 处理匹配异常 |

### 6.3 付款管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/finance/ap/payments` | 付款列表 |
| POST | `/api/finance/ap/payments` | 创建付款申请 |
| POST | `/api/finance/ap/payments/{id}/approve` | 审批付款 |
| POST | `/api/finance/ap/payments/{id}/execute` | 执行付款 |
| POST | `/api/finance/ap/payments/{id}/cancel` | 取消付款 |
| POST | `/api/finance/ap/payments/{id}/allocate` | 付款分配到应付单 |

### 6.4 付款计划

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/finance/ap/payment-plans` | 付款计划列表 |
| POST | `/api/finance/ap/payment-plans` | 创建付款计划 |
| PUT | `/api/finance/ap/payment-plans/{id}` | 修改付款计划 |
| POST | `/api/finance/ap/payment-plans/{id}/approve` | 审批付款计划 |

### 6.5 供应商对账

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/finance/ap/supplier-statement/{supplierId}` | 供应商对账单 |
| GET | `/api/finance/ap/aging-report` | 应付账龄分析 |

## 七、实体关系图

```
┌──────────────┐     ┌──────────────┐     ┌────────────────┐
│ Supplier     │     │ Payable      │     │ Payment        │
│ 供应商(采购)  │◀────│ 应付单        │     │ 付款单          │
└──────────────┘     └──────┬───────┘     └───────┬────────┘
                            │                      │
                     ┌──────┴──────┐     ┌────────┴────────┐
                     ▼             ▼     ▼                  │
              ┌────────────┐ ┌──────────────┐  ┌───────────┘
              │ThreeWayMatch│ │PaymentAlloc  │  │
              │ 三单匹配    │ │ 付款分配      │  │
              └──────┬─────┘ └──────────────┘  │
                     │                          │
                     ▼                          ▼
              ┌────────────┐          ┌────────────────┐
              │MatchDetail │          │ PaymentPlan    │
              │ 匹配明细    │          │ 付款计划        │
              └────────────┘          └────────┬───────┘
                                               │
                                               ▼
                                      ┌────────────────┐
                                      │PaymentPlanItem │
                                      │ 计划明细        │
                                      └────────────────┘
```

## 八、业务规则与约束

| 规则 | 描述 |
|------|------|
| 三单必匹配 | 采购类应付必须完成三单匹配后才能付款 |
| 容差可配置 | 数量/价格/金额的匹配容差由管理员配置 |
| 付款不超应付 | 付款分配不得超过应付单未付余额 |
| 审批金额分级 | 不同金额的付款需要不同级别的审批 |
| 重复发票检查 | 同一供应商同一发票号不可重复录入 |
| 付款日期限制 | 付款日期不得早于发票日期 |
| 预付款追踪 | 预付款必须在后续采购中冲抵 |
| 跨期间限制 | 已关闭期间的应付数据不可修改 |
| 供应商余额实时 | 供应商应付余额实时更新 |
| 付款确认生成凭证 | 付款执行后自动生成付款凭证 |
